import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['test/**/*.test.ts'],
    /**
     * Таймаут — аварийный ограничитель, а не мерило скорости машины (#2266).
     *
     * Умолчание Vitest — 5 с. Полный файл `media-library.test.ts` на живой машине регулярно
     * ходит рядом с этим порогом из-за seed/refresh работы и параллельной трансформации тестов,
     * хотя отдельные проверки завершаются штатно. Такой красный отвечает на вопрос «насколько
     * сейчас загружен контур», а не «работает ли библиотека».
     *
     * Порог 30 с ловит настоящее зависание (повисший промис, бесконечный цикл), но не судит
     * нормальную работу длительностью 6-10 с.
     */
    testTimeout: 30_000,
    hookTimeout: 120_000,
  },
});
