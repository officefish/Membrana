/**
 * @deprecated Этот файл оставлен только для обратной совместимости.
 *
 * Реальный класс — `FftAnalyzer` в `./core/fft-analyzer.ts`.
 * Все импорты потребителей должны идти через `@membrana/fft-analyzer-service`
 * (через `./index.ts`), а не напрямую сюда.
 */

export { FftAnalyzer as AudioAnalyzerService } from './core/fft-analyzer.js';
