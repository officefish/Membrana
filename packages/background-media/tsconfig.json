{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "Node16",
    "moduleResolution": "Node16",
    "target": "ES2022",
    "lib": ["ES2022"],
    "rootDir": "./src",
    "outDir": "./dist",
    "composite": true,
    "tsBuildInfoFile": "./.tsbuildinfo",
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "noPropertyAccessFromIndexSignature": false
  },
  "include": ["src/**/*"],
  "//stubs": "Каталоги stubs/ — исполняемые замены соседей на время изоляции коворка. Регламент требует их не мёржить; это исключение делает требование ФАКТОМ СБОРКИ, а не обещанием: до него стабы компилировались в dist наравне с боевым кодом. Вторую половину стережёт зуб stubs-not-in-production.test.ts — что их не импортирует боевой код.",
  "exclude": ["node_modules", "dist", "**/*.spec.ts", "**/*.test.ts", "**/stubs/**"],
  "references": [
    { "path": "../plugin-contracts" }
  ]
}
