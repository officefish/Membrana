/**
 * СТАБ второй двери (M7): закрытый список причин режима «персональной ответственности нет».
 *
 * Список НЕ мой: автор — владелец, названо 30.07 (`OWNER_ANSWERS.md` §2). Пометка
 * `//provisional`, стоявшая здесь в Phase 1, СНЯТА — значение названо. Причин четыре.
 *
 * «Разведка/прототип» в список НЕ входит — отвергнута сознательно: формулировка эластична,
 * разведкой можно назвать почти всё, и это самая вероятная лазейка второй двери. Пятая
 * причина требует нового слова владельца, а не расширения по аналогии.
 *
 * Гейт держит только ФОРМУ: причина обязательна и из списка. Закрытость самого списка —
 * не предмет этого блока, и на Phase 3 она требуется от носителя `membrana-flow`.
 */

/** @type {readonly string[]} */
export const RESPONSIBILITY_WAIVER_REASONS = Object.freeze([
  'mechanical', // механическая правка: профильного суждения не требует
  'no_profile_owner', // в команде нет профиля под эту зону
  'owner_solo', // владелец делает сам и ответственность оставляет за собой
  'urgent_recovery', // срочное восстановление: назначение задержало бы лечение
]);

/** Смысл причины — для отчёта; машинная проверка идёт по списку выше. */
export const REASON_MEANING = Object.freeze({
  mechanical: 'механическая правка: профильного суждения не требует',
  no_profile_owner: 'в команде нет профиля под эту зону',
  owner_solo: 'владелец делает сам и ответственность оставляет за собой',
  urgent_recovery: 'срочное восстановление: назначение задержало бы лечение',
});

/** @param {unknown} reason @returns {boolean} */
export function isAllowedWaiverReason(reason) {
  return RESPONSIBILITY_WAIVER_REASONS.includes(/** @type {string} */ (reason));
}
