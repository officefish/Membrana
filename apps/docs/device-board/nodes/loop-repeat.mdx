---
title: Loop repeat
description: Бесконечный цикл main/alarm.
---

# Loop repeat (`loop-repeat`)

## Назначение

Системный узел в ветках `main` и `alarm` — генерирует повторяющийся `loopTick` exec.

## Runtime

Связан с `ScenarioRuntime` phase machine; не удаляется пользователем.

## Code

`loop-repeat-node.ts` · `SCENARIO_RUNTIME.md`
