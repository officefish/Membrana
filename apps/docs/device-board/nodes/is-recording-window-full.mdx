---
title: IsRecordingWindowFull
description: Условный exec по заполнению recording window.
---

# IsRecordingWindowFull

## Pins

Exec in + `recorder` in → `exec-true-out` / `exec-false-out`.

## Runtime

Проверяет host state recorder session; маршрутизирует exec как `is-valid`.

## Code

`is-recording-window-full-node.ts`
