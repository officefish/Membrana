---
title: PublishReport
description: Публикация ReportRef в JournalRef.
---

# PublishReport

## Pins

Exec + `journal` + `report` in → exec out.

## Runtime

`host.appendReport(journal, report)` — E2E journal reporter v0.6.

## Code

`publish-report-node.ts` · DBJ4
