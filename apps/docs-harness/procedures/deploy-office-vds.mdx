---
title: "Процедура: deploy-office-vds — выкладка на office-VDS с прогоном в журнале"
description: "**Определение.** `deploy-office-vds` — процедура разворачивания на выделенный office-VDS (office.mmbrn.tech, MSK): сервисы **office** и **panel**. Хост и доступ — `getOfficeSshConfig` (`scripts/_ssh-office-config.mjs`); вход исполнителя — `yarn vds:run`."
---

{/* Generated by `node scripts/mintlify-workflow-docs.mjs --render`. Do not edit. */}

**Определение.** `deploy-office-vds` — процедура разворачивания на выделенный office-VDS (office.mmbrn.tech, MSK): сервисы **office** и **panel**. Хост и доступ — `getOfficeSshConfig` (`scripts/_ssh-office-config.mjs`); вход исполнителя — `yarn vds:run`.

## Паспорт

- **ID:** `deploy-office-vds`
- **Род:** не объявлено
- **Держатель:** vesnin
- **Состояние:** `built-valid`
- **Миграция:** `in-migration`
- **Канон:** [`docs/procedures/deploy-office-vds`](https://github.com/officefish/Membrana/blob/main/docs/procedures/deploy-office-vds/README.md)

## Когда входить

деплой — рука владельца; автозапуск прод-выкладки запрещён конструкцией манифеста

Команда или фраза входа: `yarn vds:run`

## Этапы

- `build` — vesnin
- `rollout` — tarasov
- `smoke` — dynin
- `record` — angelina

## Гейты

- **owner-rollout:** ждёт owner; продолжение — владелец сам запускает напечатанную команду выкладки (deploy:when-green печатает, не запускает)

## Выходы и примеры

<Warning>
Портфолио отсутствует. Пробел копит `workflow-examples-marathon`.
</Warning>
