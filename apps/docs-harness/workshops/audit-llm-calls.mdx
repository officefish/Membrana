---
title: "docs/audit/llm-calls — дом доказательств LLM-вызовов"
description: "Дом группы **гранул evidence** вызовов LLM-процедур (LPC): подлинность + параметры, без сырых тел prompt/response. Реализация `GROUP_CONTAINERIZATION`. Эпик: `llm-calls-house` (#1033) · инстанс `llm-calls-house-2026-07-23`."
---

{/* Generated by `node scripts/mintlify-workflow-docs.mjs --render`. Do not edit. */}

Дом группы **гранул evidence** вызовов LLM-процедур (LPC): подлинность + параметры, без сырых тел prompt/response. Реализация `GROUP_CONTAINERIZATION`. Эпик: `llm-calls-house` (#1033) · инстанс `llm-calls-house-2026-07-23`.

## Что она обрабатывает

- **Дом:** [`docs/audit/llm-calls`](https://github.com/officefish/Membrana/blob/main/docs/audit/llm-calls/README.md)
- **Предмет:** `docs/audit/llm-calls`
- **Плоскость:** report
- **Роль:** не объявлена
- **Состояние:** манифест читается

## Как войти

- **audit:** `yarn llm-calls:audit`
- **decompose:** `yarn llm-calls:decompose`

## Примеры

<Warning>
Проверенного примера пока нет. Его накопит `workflow-examples-marathon`.
</Warning>

## Портфолио

<Note>
Отдельное портфолио мастерской не объявлено. Примеры выше считаются только по полю `usage`; пробелы копит `workflow-examples-marathon`.
</Note>
