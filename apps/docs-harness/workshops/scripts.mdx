---
title: "scripts/ — контейнер скриптов монорепо"
description: "Единственный дом группы «скрипты и yarn-обвязка ритуала». Реализация паттерна `GROUP_CONTAINERIZATION` **внутри** уже существующего каталога `scripts/` — **не** заводить второй дом (`docs/audit/scripts/` и т.п.)."
---

{/* Generated by `node scripts/mintlify-workflow-docs.mjs --render`. Do not edit. */}

Единственный дом группы «скрипты и yarn-обвязка ритуала». Реализация паттерна `GROUP_CONTAINERIZATION` **внутри** уже существующего каталога `scripts/` — **не** заводить второй дом (`docs/audit/scripts/` и т.п.).

## Что она обрабатывает

- **Дом:** [`scripts`](https://github.com/officefish/Membrana/blob/main/scripts/README.md)
- **Предмет:** `scripts`
- **Плоскость:** domain
- **Роль:** не объявлена
- **Состояние:** манифест читается

## Как войти

- **audit:** `yarn scripts:orphans`
- **decompose:** `yarn scripts:registry`
- **inspectElement:** `yarn scripts:sets-of`

## Примеры

### audit

прямой ответ: есть ли бесхозные скрипты и сколько — со знаменателем

```text
scripts:orphans · знаменатель 966 (инструменты ∪ тесты)
⚠ правил членства ноль — сиротство ниже означает «правила ещё нет»
✖ бесхозных 45 из 966
```

Проверено: 2026-07-31.

### inspectElement

обратный поиск: в каких наборах лежит файл; различает «набора нет», «набор пуст» и «файл ни в одном»

```text
scripts:sets-of scripts/bridge.mjs · состоит в одном наборе
    кит angelina-bridge
```

Проверено: 2026-07-31.


## Портфолио

<Note>
Отдельное портфолио мастерской не объявлено. Примеры выше считаются только по полю `usage`; пробелы копит `workflow-examples-marathon`.
</Note>
