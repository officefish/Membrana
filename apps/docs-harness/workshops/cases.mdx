---
title: "docs/cases — контейнер кейсов"
description: "Дом-контейнер кейсов: записанных **удачных** импровизаций и прогонов. Кейс — **зеркало прецедента** (`docs/precedents/`): там разбор «что сломалось и почему», здесь — «что сработало и чем доказано». Одна семья: оба про прогон, оба append-only, оба с закрытыми перечнями, оба ссылаются на процедуру. Спека — Issue #1298 (принята капитаном 26.07)."
---

{/* Generated by `node scripts/mintlify-workflow-docs.mjs --render`. Do not edit. */}

Дом-контейнер кейсов: записанных **удачных** импровизаций и прогонов. Кейс — **зеркало прецедента** (`docs/precedents/`): там разбор «что сломалось и почему», здесь — «что сработало и чем доказано». Одна семья: оба про прогон, оба append-only, оба с закрытыми перечнями, оба ссылаются на процедуру. Спека — Issue #1298 (принята капитаном 26.07).

## Что она обрабатывает

- **Дом:** [`docs/cases`](https://github.com/officefish/Membrana/blob/main/docs/cases/README.md)
- **Предмет:** `docs/cases`
- **Плоскость:** domain
- **Роль:** не объявлена
- **Состояние:** манифест читается

## Как войти

- **audit:** `yarn case:register --validate`
- **decompose:** `yarn case:decompose`
- **inspectElement:** `yarn case:inspect`
- **portfolio:** `yarn case:portfolio`
- **generalize:** `yarn case:generalize`

## Примеры

<Warning>
Проверенного примера пока нет. Его накопит `workflow-examples-marathon`.
</Warning>

## Портфолио

<Note>
Отдельное портфолио мастерской не объявлено. Примеры выше считаются только по полю `usage`; пробелы копит `workflow-examples-marathon`.
</Note>
