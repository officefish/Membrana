{
  "name": "@membrana/comms-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Контур внешних коммуникаций (Вариант A, leaf). Сток, не исток: читает канон живьём через fs, пишет только в out/. Не импортирует @membrana/*.",
  "author": "Membrana",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "clean": "rimraf dist .turbo tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
