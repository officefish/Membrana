// specimen: jargon-out — намеренный пример для бестиария; не прод-код.
// Внутреннее имя рядом с внешним запросом.



function bestiarySpecimenJargonOut() {
  const prompt = 'Ask perplexity about MAIN_DAY_ISSUE status';
  return prompt;
}
