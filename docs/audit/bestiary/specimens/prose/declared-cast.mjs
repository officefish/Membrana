// specimen: prose — намеренный пример для бестиария; не прод-код.
// Состав объявлен абзацем, машинного носителя нет ни у кого: детектор обязан дать
// finding при carriersOf === 0 (вещдок #1204: комната объявила три голоса, поднялся
// только детерминированный движок, за остальных говорил агент).

/**
 * Комната ведущей.
 *
 * Ведущая — Мнимова: ведёт конспект и честный отчёт, кода не пишет.
 * Свободный голос — Пустоцветов, участник без права гейта.
 * Роль памяти долгов исполняет Небылицын, немногословен.
 */
export function bestiarySpecimenProseDeclaredCast() {
  return ['конспект', 'голос', 'долги'];
}
