<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: d15c5e61ec1ef6a03e1780206dab655e75df01b0be762160f3af2b307375b682 · signedAt: 2026-09-21T12:01:32.855Z -->
# План дня — 2026-09-21

## Магистраль
- **angelina-hostess-impl** (L) — закрывает отсутствие живого субъекта-ведущей, которая скрепляет сессии и деплои; годится магистралью, потому что ведущая сессия сегодня держит руку на деплое кабинета и модерирует окна мерджей — без её реализации контур наблюдаемости провисает.
- **assets-container** (L) — закрывает боль разрозненного хранения статики и артефактов (буфер 1727 проб / 797 МБ, media-VPS на 76 %, Sentry-контейнер ждёт апгрейда); годится магистралью как фундамент для сторожа диска, библиотеки и будущего Sentry, все три упираются в единый контейнерный слой.
- **batch-collection-run-contour** (L) — закрывает боль ручного разбора буфера и некалиброванного ночного дежурства (сторож, дедупликация, ночная сессия 28.08); годится магистралью, потому что контур пакетного сбора напрямую питает библиотеку и снимает давление на диск до пятницы.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- Написать тест-порчу для `night-triage-secret-scan.mjs`: убедиться, что скрипт режет (а не только детектирует) синтетический секрет в fixture-файле и возвращает ненулевой статус при сухом прогоне без резака — это напрямую закрывает первый критерий вехи `secret-parser-built`.
- Составить датированный манифест ротации засвеченных ключей (список скомпрометированных идентификаторов, дата обнаружения, статус перевыпуска) и зафиксировать его одним коммитом — выполнение второго критерия вехи снимает амнистию на правку архива и открывает гейт для всей последующей работы с архивом сессий.

## Перспективные
- Завершение `secret-parser-built` (резак + датированный проход с манифестом) откроет амнистию на правку архива и разблокирует редактирование накопившихся артефактов.
- Калибровка сторожа диска (`disk-watchdog-calibration`) на дежурстве 28.08 откроет надёжный горизонт наполнения media-VPS — после неё можно планировать плотное полевое дежурство без риска переполнения.
- Разбор буфера (`buffer-sorting`, 1727 проб / 797 МБ) до пятницы откроет следующее дежурство 28.08: запись стартует в quota, а не в упор к потолку.

## Экспериментальные
- **Проба:** запустить `night-triage-secret-scan.mjs` в режиме «резак» на одном историческом снимке сессии из архива и сравнить вывод с режимом «только детектор» — узнаем, не режет ли скрипт незасвеченные токены по ложному совпадению паттерна.
- **Проба:** взять один ротируемый ключ из манифеста и пройти полный цикл вручную (вырезать → зафиксировать → заменить в env) на одном сервисе — узнаем, сколько реально занимает один оборот и где цикл ломается.
- **Проба:** добавить в сторож диска (`DW_WRITE_RATE_REF_MB_MIN`) временный лог фактической скорости записи без изменения порога — узнаем, стабильна ли скорость 5,3 МБ/мин ночи 23.08 или это выброс.

## Санитарные
- Перевыпустить токен бота `@MembranaWatchdog_bot` — дважды попадал в переписку (#2148)
- Проверить `health/deep` на проде кабинета после деплоя: убедиться, что `busy` в простое снят (кусок D, PR #2144)
- Ревизия старых PR: `#1939`, `#1831`, `#1846`, `#1876`, `#1728`, `#1793` — все старше недели, в очередь не входят; пройти `membrana-pr-audit`
- Сверить зуб `union-merge-all-jsonl` (#2096): `git check-attr merge` = union на всех `docs/**/*.jsonl`, конфликт повторился 24.08
- Убедиться, что `pr:ship` в ритуале не выходит нулём без PR — факт мерджа только через `gh pr view --json state`

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашнего протокола нет
- вчерашний день НЕ закрыт (вычислено): протокола команды нет; вечерняя ласточка партнёрам не отправлена
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
