<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 9d0f09b9158d42444d9754d5cbe3ecd4eab553ccd498d3502e103da1143c7fc3 · signedAt: 2026-09-05T09:00:56.179Z -->
# План дня — 2026-09-05

## Магистраль
- **angelina-hostess-impl** (L) — закрывает разрыв «зона не размечена»: hostess-контур Angelina остаётся без реализации, хотя guard уже в горизонте дня; как L-эпик тянет день целиком и даёт одну сквозную поставку вместо россыпи хвостов.
- **assets-container** (L) — боль неупорядоченного хранения/доступа к ассетам в неразмеченной зоне; годится магистралью, потому что контейнер — опорный шов на весь день, без которого смежные контуры продолжат жить на временных обходах.
- **batch-collection-run-contour** (L) — нет цельного контура пакетного прогона коллекций; магистраль уместна как L-рамка дня: один контур вместо точечных прогонов, после чего batch-работа перестаёт быть ручным исключением.

Выбор магистрали — слово владельца (owner-choice)

## Подкрепление
- Фикстурный прогон `night-triage-secret-scan.mjs` на срезе архива с заведомо засвеченными паттернами: резак должен вырезать, а не только детектить — снимает риск «гейт `secret-parser-built` закрыт детектором без агрессивного cut».
- Черновик датированного манифеста ротации засвеченных ключей (под `credential-rotation-biweekly`): один проход → список ключей/мест/статус перевыпуска — усиливает второй критерий вехи и не даёт амнистии архива остаться открытой из‑за пустой инвентаризации.

## Перспективные
- `secret-parser-built` (резак + датированный проход с манифестом ротации) — снимет амнистию на правку архива и откроет безопасный бэкап сессий с redaction до выгрузки.
- Живое подтверждение fanout/квоты self-select на узле после review-pass #2286 — откроет полевую приёмку тарифа на реальном дежурстве, а не только по merge.
- Апгрейд офиса ≥10.09 — откроет контур `sentry-container` (полный self-hosted, не раньше этой даты).

## Экспериментальные
- Прогон `night-triage-secret-scan.mjs` на одной фикстуре с заведомо ложным ключом в «архивном» фрагменте: узнаем, резак ли реально вырезает (бэкап = архив без чтения кодом/промптом) или всё ещё только детектит.
- Сухой черновик манифеста ротации на *одном* датированном фейковом токене (без живого revoke): узнаем, хватает ли формы манифеста как предикату «один проход с ротацией» для снятия амнистии.
- Сверка одного свежего session-backup пути «сначала redaction → потом archive» на пустом/синтетическом транскрипте: узнаем, не уезжает ли сырой хвост на сервер до вырезания.

## Санитарные
- review-pass oversized `#2286` (`1f8df30c`, +1455) и `aa7d8995` (+625): швы `self`/GET·POST/log/fanout — merge без разворота
- прогон `lint typecheck test` по cabinet/tariff после merge: тесты написаны, полный прогон не зафиксирован
- fanout-квота: сервис есть, живое «N узлов обновлено» / нет зелёного кабинета со старой квотой — не подтверждено
- ритуальный хвост: `morning-care` fail → r2; claims-probe — 6 «не подтверждено» по вечернему фидбеку
- deps-гигиена: `fast-uri` (high), `fastify`·`qs` (moderate) — апдейт или accept-risk

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
