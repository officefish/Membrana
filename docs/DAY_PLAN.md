<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 50084e8b7a067cb133db73921b15093d1d0ba21146e80603ec0a978b71bcbb57 · signedAt: 2026-09-16T08:25:48.461Z -->
# План дня — 2026-09-16

## Магистраль
- **`angelina-hostess-impl`** (L) — закрывает боль отсутствия живого субъекта действия на стороне ИИ-хостесса; размер L оправдан как магистраль, если сессия уже несёт контекст реализации персоны.
- **`assets-container`** (L) — закрывает боль разрозненного хранения медиаактивов без единой точки входа; логично ложится магистралью в день, когда библиотека сэмплов активно достраивается сразу на двух фронтах (Studio + кабинет).
- **`batch-collection-run-contour`** (L) — закрывает боль отсутствия контура пакетного сбора данных; актуален в горизонте `secret-parser-built`, где ротация и датированные проходы становятся регулярными операциями.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Написать unit-тест, доказывающий, что `night-triage-secret-scan.mjs` вырезает (а не только детектирует) секрет из фикстурного транскрипта** — прямой зуб в критерий вехи `secret-parser-built`; без зелёного теста гейт не пройти.
- **Перевыпустить токен бота `@MembranaWatchdog_bot`** (строка 6 хендофа: токен дважды попадал в переписку) — убирает засвеченный ключ до датированного прохода с манифестом ротации, иначе сам манифест будет неполным.

## Перспективные
- Прохождение гейта `secret-parser-built` откроет амнистию на правку архива — замороженные исправления в истории станут допустимы.
- Калибровка сторожа диска (`disk-watchdog-calibration`) на дежурстве 28.08 откроет надёжный горизонт до следующего апгрейда media-VPS.
- Закрытие пяти зубов `week-without-repeats` откроет следующую волну тулинга — `deliver-to-main-in-chain` и `union-merge-all-jsonl` перейдут в очередь сессии Б.

## Экспериментальные
- **Проба «резак vs детектор»:** запустить `night-triage-secret-scan.mjs` на синтетическом файле с тестовым секретом — проверить, что скрипт вырезает паттерн, а не только логирует; узнаем, достаточно ли текущего кода для прохождения гейта `secret-parser-built` или нужна отдельная ветка резака.
- **Проба токена бота:** перевыпустить токен `@MembranaWatchdog_bot` (он дважды попадал в переписку) и отправить тестовое сообщение в личный чат владельца — узнаем, устраняет ли это утечку до полного прогона манифеста ротации.
- **Проба `union merge` на одном jsonl:** добавить атрибут `merge=union` к одному append-only файлу из `docs/network/history` и симулировать конфликт — узнаем, достаточно ли одной строки `.gitattributes` или нужна групповая маска на все `docs/**/*.jsonl` сразу.

## Санитарные
- Закрыть `health-deep-busy-in-idle` (#2117, кусок D): деплой кабинета подберёт PR 2144 — сверить `genus` на проде после пересборки
- Перевыпустить токен бота `@MembranaWatchdog_bot` (#2148): дважды попадал в переписку, сделать до дежурства 28.08
- Ревью-долг: PR `#1939`, `#1831/#1846/#1876`, `#1728`, `#1793` — старше недели, без движения; аудит в `membrana-pr-audit` по слову владельца
- Хвост `union-merge-all-jsonl` (#2096): `docs/network/history` конфликтовала 24.08 — проверить, выставлен ли `merge=union` на все `docs/**/*.jsonl`
- Сверить, что `secret-parser` режет (не только детектирует) до бэкапа сессий — веха `secret-parser-built` в фазе approaching, резак не подтверждён

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашнего протокола нет
- вчерашний день НЕ закрыт (вычислено): протокола команды нет; вечерняя ласточка партнёрам не отправлена
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
