<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: ead4bf1b1c36523bb4c13c0eb73c18d70597fea294caf325aa25cf3811bb8d5a · signedAt: 2026-08-12T11:05:51.331Z -->
# План дня — 2026-08-12

## Магистраль
- **`angelina-hostess-impl`** (L) — боль: hostess-контракт объявлен декларацией, но ни одной строки кода нет; два дня подряд переносится, что блокирует панель Ангелины и форму отклика. Годится магистралью: Tarasov прямо назначил её первым шагом 12.08 с `yarn main-day-probe`, вечерний фидбек единодушен.
- **`archivarius-sessions-container`** (L) — боль: сессии AI-агентов уходят в бэкап без гарантии вырезания секретов (кристалл `session-backup-requires-secret-redaction`), а веха `secret-parser-built` — approaching. Годится магистралью: контейнер архивариуса — прямой рычаг для прохождения гейта вехи, без него парсер секретов негде приземлить.
- **`assets-container`** (L) — боль: статические ресурсы не имеют живого инвентаря (`static-mmbrn-live-inventory` в двадцатке), что тормозит продуктовую полосу. Годится магистралью: если hostess ещё раз блокируется зондом, assets — самостоятельный L-блок без внешних зависимостей в двадцатке.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **`ritual-magistral-source-freshness`** (#14 очереди) — перечеканить `main-day-assertions.json` под гейт 11.08 устраняет риск того, что генератор прочитает ложную магистраль и подкрепление уйдёт не в ту сторону: без свежего assertions сам выбор `angelina-hostess-impl` процедурно не закреплён.
- **`morning-gates-two-moments`** (#9 очереди) — ADR-0024 принят, но не реализован; пока гейты утра остаются одним моментом, `yarn main-day-probe` не имеет чёткой точки входа и первый шаг магистрали снова рискует утонуть в параллельной оси.

## Перспективные
- Закрытие `worktrees-align-snapshot-guard` (#1864) откроет безопасный запуск `worktrees:align` в боевом режиме — обход после отказа перестанет рваться на tooling-дереве.
- Прохождение гейта `secret-parser-built` снимет амнезию на правку архива — следующие дни смогут легально чистить засвеченные ключи без ручного обхода.
- Первый реальный прогон `angelina-hostess-impl` через `yarn main-day-probe` превратит `unknown`-посылки карточки в `holds`/`violated` и разблокирует точный DoD для реализации hostess-контракта.

## Экспериментальные
- **MFCC-тембровый детектор vs FFT-гармоника:** запустить оба на одном фрагменте записи и сравнить скоры — узнаем, даёт ли MFCC отличимый сигнал там, где FFT молчит.
- **`mfcc-lib-choice` за 30 минут:** взять топ-2 библиотеки, прогнать одинаковый сэмпл, сравнить форму коэффициентов — узнаем, есть ли расхождение, которое влияет на выбор.
- **Память процедур «показывается, не запрашивается»:** в одном существующем фрейме заменить подтверждение на авто-показ числа вовлечённых — узнаем, держит ли контракт фрейма такую замену без ADR.

## Санитарные
- `main-day-assertions.json` не перечеканен под гейт 11.08 — генератор снова прочтёт магистраль 08.08; перечеканить до планирования
- Карточка `sprint-experience-dead-ends-after-recut` третий день без реестра — завести `yarn task:create` вручную (глагола в scripts нет, создавать руками)
- 9 мёртвых душ из бухгалтерии HANDOFF не повердикчены и не архивированы — пройти список, `task:archive` со свидетельством
- Проверить факт закрытия GitHub-иссью #1764 #1447 #1422 #1272 #554 — отмечены `githubIssueClosedAt`, но PR не влиты и статус не читался
- Порог `133` дублируется в `package.json` и `ci.yml` — вынести в единый источник (P2 Веснина, второй день)

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
