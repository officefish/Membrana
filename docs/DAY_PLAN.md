<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 7172ae2e2844bddafb1db5591d1c2c17a25e7f95b2eddd7ebdef2d8efb165c8b · signedAt: 2026-09-17T11:55:44.967Z -->
# План дня — 2026-09-17

## Магистраль
- **angelina-hostess-impl** (L) — закрывает боль отсутствия живого исполнителя в контуре: персона заявлена, но не вызываема (кристалл `insight-cast-carrier-contract`). Крупный, структурообразующий — хорош магистралью, если владелец готов двигать инфраструктуру персон.
- **assets-container** (L) — закрывает боль рассыпанных ресурсов без единой точки сборки; косвенно помогает и библиотечному контуру (сэмплы, медиа), и CI, который сейчас красный. Хорош магистралью, если приоритет — стабилизировать сборку.
- **batch-collection-run-contour** (L) — закрывает боль ручного запуска сбора: буфер 1727 проб растёт, разбор буфера висит на владельце (строки 8 хендофа), автоматизация контура снимет это давление. Хорош магистралью, если фокус — полевой корпус и разгрузка буфера до дежурства.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- Перевыпустить токен `@MembranaWatchdog_bot` — засвеченный токен держит кристалл `credential-rotation-biweekly` нарушенным, а это прямой риск магистрали `secret-parser-built`: парсер режет секреты ДО бэкапа, но уже утёкший токен вне его досягаемости.
- Зафиксировать шаблон `WORKTREE.md` (имя, цель, статус, дата последнего коммита) в `docs/repo/` — без канона для sprint-open деревьев worktree-sanitation воспроизведёт те же 41 unregistered на следующем аудите, что заблокирует переход к продуктовой магистрали послезавтра.

## Перспективные
- Зелёный CI по `media-library-service` и `background-media` откроет продуктовую работу на завтра — без него сессии не стартуют в код.
- Закрытая `worktree-sanitation` (снимок аудита, снос 36 merged-веток, шаблон `WORKTREE.md`) откроет возврат к продуктовой магистрали послезавтра — договорённость уже есть.
- Перевыпуск токена `@MembranaWatchdog_bot` откроет снятие амнистии на архив и движение к гейту `secret-parser-built`.

## Экспериментальные
- **Проба: dry-run резака секретов на одном реальном файле сессии** — запустить `night-triage-secret-scan.mjs` с флагом `--cut-dry-run` на последнем транскрипте, сравнить вывод с оригиналом; узнаем, режет ли парсер агрессивно или только детектирует.
- **Проба: предикат свежести деревьев через `git log -1 --format=%ct`** — один скрипт, 10 строк, помечает дерево `stale` если последний коммит старше N дней; узнаем, достаточен ли этот маркер для автоматического audit-снимка.
- **Проба: `git branch --merged main`** как источник списка кандидатов на снос — запустить вручную, сравнить вывод с рукописным счётом 36 веток из вчерашнего аудита; узнаем, совпадает ли машинный список с тем, что команда считала глазами.

## Санитарные
- P1: диагностика красных пакетов — `media-library-service` (build fail) и `background-media` (test fail); без зелёного CI работу не открывать
- Перевыпустить токен `@MembranaWatchdog_bot` — засвеченный токен держит амнистию архива закрытой (`credential-rotation-biweekly`)
- Довести worktree-sanitation по вчерашнему DoD: снос 36 merged-веток, снимок `worktree-audit-2026-09-16.md` в git, обновление реестра
- Warning `react-hooks/exhaustive-deps` в `CabinetSampleDuplicatesPanel.tsx:115` — починить или задокументированно отключить
- Uncommitted хвост (4 modified + 4 untracked) — добавить явный `git add docs/procedure-runs/ docs/evidence/` в `ritual:evening`

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
