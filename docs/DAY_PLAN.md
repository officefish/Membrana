<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 565f11c71de1c405fc4d34fd8b862d461861efc9022e05028c6e909376299f89 · signedAt: 2026-08-01T05:28:00.729Z -->
# План дня — 2026-08-01

## Магистраль
- **agent-tooling-night-build (L)** — закрывает боль отсутствия ночных сборок тулинга: агентский инструментарий не обновляется без рук, утро начинается с устаревшим окружением. Годится магистралью, потому что горизонт дня держит гейт `secret-parser-built`, а парсер секретов — инструмент, и его резак должен оказаться в сборке раньше первого прогона.
- **angelina-hostess-impl (L)** — закрывает боль отсутствия принимающей стороны для входящих сессий: без хостес холодный старт каждый раз ручной. Годится магистралью, потому что хендоф прямо называет «знакомство агента с портфолио перед прогонами» как пункт 2 рамки, а хостес — естественный носитель этого знакомства.
- **archivarius-sessions-container (L)** — закрывает боль того, что прогон не оставляет записи о себе: журнал прогона отсутствует вовсе, любая витрина — вид поверх пустоты. Годится магистралью, потому что хендоф ставит журнал прогона выше проигрывателя и выше n8n, а контейнер сессий — прямой путь к этой динамической части.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- Написать минимальный резак в `night-triage-secret-scan.mjs`: функция принимает строку, возвращает строку с вырезанными совпадениями по паттернам — это прямое условие прохождения вехи `secret-parser-built`, без которой амнистия на правку архива не снимается.
- Зафиксировать один датированный манифест ротации засвеченных ключей (список + дата + статус каждого) — второй из трёх критериев вехи; без него гейт не проходится даже при готовом резаке.

## Перспективные
- Merge MFCC в main откроет live-тест на микрофоне и сверку тарифного гейта T2 — сейчас оба заблокированы воркtree.
- Прохождение гейта `secret-parser-built` снимет амнистию на правку архива и откроет работу с историческими секретами.
- Ратификация поимённого списка веток на снос откроет финальный проход гигиены и закроет долг branch-salvage.

## Экспериментальные
- **Проба: журнал прогона процедуры как append-only строка в файле.** Взять любую сегодняшнюю процедуру, дописать в конец одного markdown-файла одну строку с датой, именем процедуры и исходом. Узнаем: достаточно ли одной строки, чтобы прогон оставил след, — или нужна структура.
- **Проба: пол холодной сессии для процедур по образцу мастерских.** Взять готовый пол тулинг-сессии (13 мастерских, 22 строки) и заменить мастерские на три-четыре процедуры из `docs/meeting/workshop-wires/`. Узнаем: ложится ли та же механика обнаружения на процедуры без переизобретения.
- **Проба: агрессивный резак секретов на одном тестовом файле с подставным ключом.** Запустить `night-triage-secret-scan.mjs` с флагом резака на файл-приманку. Узнаем: режет ли резак сейчас или только детектирует — и что именно остаётся в файле после прохода.

## Санитарные
- Проверить `git log -1 --oneline origin/main` — MFCC-ядро в main или осталось в `storm/mfcc-sprint-test-3007`
- Запустить `yarn turbo run lint typecheck test --filter=@membrana/fft-analyzer` — граничные кейсы (тишина, короткий буфер, детерминизм) должны быть зелёными
- Точечное ревью PR #1543 workshop-wires — крупнейший oversized, долг Tarasova и Ozhegova с 31.07
- Проверить `scripts:orphans` — вчера выявлено ложное предупреждение (все 48 сирот валидны, предикат врёт)
- Уточнить статус `RECONCILE_M3` и `BOUNDARY_S7` — разборы тулинга без вердикта владельца, тормозят ратификацию поимённого сноса веток

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
