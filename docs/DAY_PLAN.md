<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: ca06a0e55cb813b9091caea815c6b4c2133de13803e14f4907cb086ae3e4d728 · signedAt: 2026-08-15T07:48:01.231Z -->
# План дня — 2026-08-15

## Магистраль
- **`angelina-hostess-impl`** (L) — третий день в очереди: тимлид трижды рекомендовал её магистралью, сегодня уступала archivarius сознательно; закрывает боль «ведущая персона не работает как хостесс», вход через `yarn main-day-probe` с именованным вердиктом (не `unknown`).
- **`assets-container`** (L) — закрывает боль отсутствия оформленного контейнера ассетов; размер L позволяет занять полный день, зона не размечена, но задача самодостаточна и не блокирована внешними гейтами.
- **`batch-collection-run-contour`** (L) — закрывает боль незамкнутого контура сбора батчей; логически примыкает к archivarius-слою, закрытому вчера, что даёт чистый вход без разгребания хвостов.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **`night-triage-secret-scan.mjs` получает резак (не только детектор)** — прямо закрывает первый критерий вехи `secret-parser-built`; без резака гейт не пройти, и магистраль заблокирована.
- **Датированный проход с манифестом ротации засвеченных ключей** — закрывает второй критерий вехи и снимает риск сырых секретов в бэкапах сессий, что разблокирует амнистию на правку архива.

## Перспективные
- Закрытие `angelina-hostess-impl` откроет магистраль, откладываемую третий день, и разблокирует зависимые probe-проверки посылок ведущей персоны.
- Прохождение вехи `secret-parser-built` (резак + датированный проход) снимет амнезию на правку архива и откроет работы, требующие чистого архива как предпосылки.
- Оформление ADR «одиночный отказ = именованный пропуск» закрепит паттерн PR #1930 как переиспользуемый контракт и откроет применение его в остальных тракте-скриптах.

## Экспериментальные
- **Эксперимент 1.** Запустить `night-triage-secret-scan.mjs` в режиме dry-run на одном архивном файле сессии — узнаем, резак уже вырезает или только детектирует (закроет ли это гейт `secret-parser-built`).
- **Эксперимент 2.** Прогнать `yarn main-day-probe` по посылкам `angelina-hostess-impl` — узнаем, именованный вердикт или снова `unknown` (условие входа в магистраль по рекомендации Тарасова).
- **Эксперимент 3.** Открыть один drift-снимок из `services-api-drift-2026-28` глазами и сравнить с текущим деревом — узнаем, реальное расхождение или шум ночной охоты.

## Санитарные
- `makeIsIgnored`: заменить необработанный throw при exit-коде `git check-ignore` ≠ 0/1 именованным исходом — четвёртый день переноса (Ожегов)
- `aria-live="polite"` на `promo-deny-text` — санитарная правка ~10 минут, третий день переноса (Родченко)
- `acceptance-2026-08-14.md` прописать в каталог `LIVE_SERVICES` — новый дом объявлен без прописки (Веснин)
- Проверить Issue #1330: если OPEN — закрыть с ссылкой на acceptance-документ первым шагом (Тарасов)
- `oversizedSkipped[]`: добавить zod-валидацию элемента превентивно — замечание Веснина не закрыто (Ожегов)

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
