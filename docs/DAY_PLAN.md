<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: e70334ec048a9f21ff0a5ad4378cdda3142d519f29503a68e20d09300e371dd0 · signedAt: 2026-08-11T06:50:28.734Z -->
# План дня — 2026-08-11

## Магистраль
- **`angelina-hostess-impl`** (L) — закрывает боль «персона без тела»: Ангелина объявлена, но не исполняет; магистраль превращает декларацию в живой агент с вызываемым контрактом. Годится осью: L-размер даёт полный день, результат виден — персона либо отвечает, либо нет.
- **`archivarius-sessions-container`** (L) — закрывает боль «сессии уходят на сервер сырыми»; без контейнера парсер секретов (`secret-parser-built`, gate approaching) режет, но класть некуда. Годится осью: прямо питает ближайшую веху и блокирует ротацию ключей.
- **`assets-container`** (L) — закрывает боль «ассеты без адреса»: статика расползается вне единого контейнера, что тормозит любой UI-спринт. Годится осью, если владелец считает инфраструктуру ассетов первичным разблокером для верстальщика и ночного тулинга.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **`friction6-secret-inventory` (#1266) → собрать инвентарь секретов**: карточка свободна, `secret:inventory` отсутствует в `package.json`, а ядро `secret-redact.mjs` уже есть — прямой питатель гейта `secret-parser-built`, без инвентаря резак режет вслепую.
- **`fix-sprint-experience-dead-ends`** → починить шов `RECORDS_PATH` / `FORECAST_RECORDS_REL_PATH` и вычисление `seq`: карточка свободна (S), живой провод `sprint:experience` нужен для замера прогона ротации ключей, который требует веха (датированный проход с манифестом).

## Перспективные
- Как только `night-triage-secret-scan.mjs` получит резак (а не только детектор), откроется амнистия на правку архива и станет возможен первый датированный проход с манифестом ротации ключей.
- Закрытие `fix-sprint-experience-dead-ends` разблокирует честный рост `seq` в ленте опыта — провод `sprint:experience` перестанет молча глотать новые прогнозы после перерезки.
- Разбор oversized-очереди PR (#1785, #1789, #1801) откроет следующие спринты без накопленного долга ревью, который висит третий день без вердикта.

## Экспериментальные
- **Запустить `secret-parser` в режиме `--dry-run` на одном архивном файле сессии** — узнаем, режет ли он реально или только детектирует (разрыв между `secret-parser-cuts-aggressively` и фактическим поведением кода).
- **Добавить в `night-triage-secret-scan.mjs` одну строку вызова резака** (без подключения к CI) — узнаем, сколько времени занимает минимальный шаг от детектора к резаку на живом файле.
- **Прогнать `yarn network:snapshot` дважды с интервалом 2 ч** — узнаем, флайк ли дельта latency 444→918 ms или устойчивый сигнал (одна точка ≠ тренд, зафиксировано Математиком).

## Санитарные
- PR #1785, #1789, #1801 — третий день без вердикта, разобрать oversized-очередь
- Завести карточку `sprint-experience-dead-ends-after-recut` в реестр (зуб в debt-ledger, риск потери)
- Закрыть пять GitHub-иссью заархивированных карточек: #1764, #1447, #1422, #1272, #554
- `docs/tasks/dead-wire-pending.json` — все 6 записей с `until: 2026-08-09` просрочены, проверить зуб `dead-wire:check`
- `yarn network:snapshot` — повторить утром, сравнить с дельтой 444→918 ms от 10.08

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
