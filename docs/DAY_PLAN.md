<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: ab340e4fbe2891aa9d34da1edc997f953db98c3aea02f47fa982d82865e3b873 · signedAt: 2026-09-25T06:18:41.275Z -->
# План дня — 2026-09-25

## Магистраль
- **`angelina-hostess-impl`** (L) — закрывает боль отсутствия живого исполнителя на ключевом хостинг-контуре; размер L делает её весомой ставкой на день, способной сдвинуть целый класс задач разом.
- **`assets-container`** (L) — закрывает боль неупорядоченного хранения ассетов, которая блокирует или замедляет смежные области; L-объём оправдывает магистральный статус как системный фундамент.
- **`batch-collection-run-contour`** (L) — закрывает боль ручного/неструктурированного сбора данных в контурах; L-задача с потенциалом разгрузить буфер и дать ритмичный цикл накопления.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Починить `sequence`-контракт trail-агрегатора** (S) — trail является вещдоком вехи `secret-parser-built`, и пока `runId`-уникальность и глобальная монотонность в merge не гарантированы, счётчики манифеста ротации ненадёжны, а значит гейт технически непроходим.
- **Патч paths `office-image-smoke.yml`** (S) — без `packages/background-cabinet/**` и `apps/panel/**` в триггере CI молча пропускает регрессы образа офиса, блокирующие шаг 1 rollout, который уже второй день стоит на `unknown`-классе поломки.

## Перспективные
- Закрытие DoD `cabinet-registration-rollout` (день 3) открывает продвижение к вехе `secret-parser-built`: второй кабинет на free-v1 выйдет живым и станет стендом для проверки резака секретов.
- Починка `sequence`-контракта trail-агрегатора снимает блок на самой вехе — trail станет надёжным вещдоком для прохождения гейта `secret-parser-built`.
- Получение дословного лога отказа образа офиса (шаг 1 rollout) определит класс поломки и откроет конкретный путь к пересборке образа и окончательному развёртыванию.

## Экспериментальные
- **Проверить класс «stale image packages» экспериментально:** запустить `yarn verify:image-workspace-deps` на упавшем образе офиса, снять digest пакетов — узнаем, является ли stale-образ единственной причиной отказа шага 1 или есть иной класс поломки.
- **Проверить `sequence`-контракт trail-агрегатора на синтетике:** создать два параллельных `runId` с намеренно пересекающимися `sequence`, прогнать merge — узнаем, достаточно ли локального уникального счётчика или нужна глобальная монотонность для прохождения вехи `secret-parser-built`.
- **Добавить один кристалл в граф правды по итогу дня:** записать факт «третий режим чеканки инвайта влит, DoD магистрали не сдвинут» как кристалл — узнаем, удерживает ли граф расхождение между работой и магистралью без ручного напоминания.

## Санитарные
- `sequence`-контракт trail-агрегатора не починен третий день — блокирует веху `secret-parser-built` (гейт читает trail как вещдок)
- Стендап-генератор `_daily-standup.mjs` читает магистраль из синтеза вместо `sources[0]` — P1, четвёртый день
- Lint `titleOf` (`react-hooks/exhaustive-deps`) в `apps/cabinet` — XS, третий день без правки
- `office-image-smoke.yml` не сторожит `packages/background-cabinet/**` и `apps/panel/**` — молчаливые регрессы образа открыты
- `CURRENT_TASK.md` указывает на неактуальный эпик `detector-scoreboard` Ф1 — дезориентирует холодную сессию

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
