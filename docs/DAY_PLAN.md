<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: df495e45933704fe0945084c93e2d3c4ead356bb3ee0a05077bffadd21d8f5d2 · signedAt: 2026-09-18T13:12:16.108Z -->
# План дня — 2026-09-18

## Магистраль
- **`angelina-hostess-impl` (L)** — закрывает боль отсутствия хостес-слоя: без неё продуктовые сценарии не приветствуют пользователя и не ведут его дальше. Вечерний фидбек команды прямо называет её кандидатом в магистраль (17.09, п. 4), и она ждёт уже после двух санитарных дней.
- **`assets-container` (L)** — закрывает боль разрозненного хранения ассетов: без контейнера каждый новый продуктовый кусок тянет своё решение хранения. Размер L и отсутствие зоны делают её хорошим опорным блоком дня — один результат, на который ложится несколько будущих слоёв.
- **`batch-collection-run-contour` (L)** — закрывает боль ручного запуска сбора: без контура сборщик нельзя поставить в расписание или в ритуал. Критерии вехи `secret-parser-built` требуют «датированного прохода с манифестом ротации» — контур батч-сбора прямо примыкает к этому предикату.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Закрыть `procedure-run ritual-day-2026-09-17-r2`** в `2026-09-17.jsonl` close-записью — незакрытый прогон создаёт риск немого «зелёного» в ритуале дня (`secret-parser-built` требует датированного прохода, а не призрачного «зелёного»).
- **Дописать `git add docs/procedure-runs/ docs/evidence/` в `ritual:evening`** — без этого шага хвост uncommitted воспроизведётся и следующий датированный проход (критерий вехи) не попадёт в ствол.

## Перспективные
- Закрытие `procedure-run ritual-day-2026-09-17-r2` close-записью откроет завтрашнее утро без риска немого «зелёного» ритуала (B6).
- Правка `ritual:evening` (`git add docs/procedure-runs/ docs/evidence/`) откроет следующие санитарные и продуктовые дни без хвоста uncommitted-следов.
- Слово владельца о магистрали 18.09 откроет выбор между продуктовым контуром (`tariff-canon-m4r3-m5`, `angelina-hostess-impl`) и третьим санитарным днём — без него очередь не собирается.

## Экспериментальные
- **Проба: предикат свежести дерева через `git log -1 --format=%ct`** — узнаем, работает ли чистая функция как замена ручному взгляду на актуальность воркдерева (эксперимент из MAIN_DAY_ISSUE Математика).
- **Проба: добавить `git add docs/procedure-runs/ docs/evidence/` в `ritual:evening` одной строкой** — узнаем, исчезнет ли воспроизводящийся хвост uncommitted санитарных вещдоков.
- **Проба: запустить `night-triage-secret-scan.mjs` в режиме резки на одном файле архива** — узнаем, даёт ли текущий код реальное удаление паттерна или только детекцию (прямой шаг к гейту `secret-parser-built`).

## Санитарные
- Закрыть открытый `procedure-run ritual-day-2026-09-17-r2` в `2026-09-17.jsonl` — риск немого «зелёного» (B6)
- Дописать `git add docs/procedure-runs/ docs/evidence/` в `ritual:evening` — хвост воспроизведётся без этого
- Починить warning `react-hooks/exhaustive-deps` в `CabinetSampleDuplicatesPanel.tsx:115` — тянется с вечернего ревью
- Разбор oversized-коммита `4eb64dfa` (648 строк) через `yarn code-review:pr` или ручной diff
- Проверить статус issue #2345 (TS2305 в `Membrana-tooling`) — вчера не отражён в коммитах

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
