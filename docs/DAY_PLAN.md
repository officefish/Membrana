<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 000c345a6642939dc711e16338888b0829546695b7b9a2387d9f2440e69920d3 · signedAt: 2026-08-31T09:34:56.249Z -->
# План дня — 2026-08-31

## Магистраль
- **angelina-hostess-impl** (L) — закрывает разрыв вокруг angelina-контура у вехи `secret-parser-built`: hostess-слой нужен, чтобы резак и ночной triage не оставались «голым» скриптом без носителя процедуры. Годится магистралью как L-якорь дня у approaching-гейта, без которого критерий «резак, не только детектор» не закрепляется в контуре.
- **assets-container** (L) — собирает разрозненные артефакты/носители в один контейнер, чтобы датированный проход и манифест ротации засвеченных ключей имели куда лечь целиком. Годится магистралью как каркас «всё в одном пакете» — тот же класс работы, что вчерашний night-procedure container, но под горизонт секретов.
- **batch-collection-run-contour** (L) — закрывает боль разового/ручного прохода: нужен контур пакетного прогона, чтобы критерий «один датированный проход с манифестом ротации» был повторяемым, а не ad-hoc. Годится магистралью как исполнительный контур вехи, без которого amnesty-предикат на архив нечем подтвердить.

Выбор магистрали — слово владельца (owner-choice)

## Подкрепление
- `secret-cutter-before-backup-path` — жёсткий резак в `night-triage-secret-scan.mjs` (не детектор): вырезание до любого write в архив/бэкап сессии; без него веха `secret-parser-built` остаётся паттерн-сканом и ломает кристаллы `secret-parser-cuts-aggressively` / `session-backup-requires-secret-redaction`.
- `dated-key-rotation-manifest-pass` — один датированный проход с манифестом ротации засвеченных ключей (biweekly-контур); снимает второй критерий гейта и риск «амнистия архива висит без предиката».

## Перспективные
- Резак в `night-triage-secret-scan.mjs` + датированный проход с манифестом ротации закрывают гейт `secret-parser-built` и снимают амнистию на правку архива как предикат, а не календарь.
- После гейта сессионный бэкап может идти на сервер только через вырезку секретов — открывается безопасный контур `session-backup-requires-secret-redaction` на следующие ночи.
- Пятничное дежурство 28.08 получает прибор линеаризации журнала и калибровку сторожа диска; до 10.09 остаётся закрытым только self-hosted Sentry на апгрейде офиса.

## Экспериментальные
- Прогнать `night-triage-secret-scan` в режиме «только резак» на одном датированном архиве сессии без записи: узнаем, режет ли парсер секреты до бэкапа агрессивно или оставляет хвосты в сыром виде.
- Сверить один засвеченный ключ из прошлого прохода с манифестом ротации «вчерне» (список, без перевыпуска): узнаем, хватает ли biweekly-контура как формы амнистии архива или дыра в учёте.
- На одном сэмпле из буфера сравнить «детекция первична / запись вспомогательна» глазом оператора: узнаем, держит ли обещание хранения сортировку буфера без обязательного прослушивания всего объёма.

## Санитарные
- красный `@membrana/background-media#test` (с 27.08) — диагноз: помеха прогона или pre-existing; `turbo test --filter` + issue
- вечерний автозабор: чистый tree после ritual-evening либо явный commit только своих артефактов дня
- сверка DAILY_STANDUP / MAIN_DAY_ISSUE с MERGED #2240–#2245; находка ритуала — стендап не перечеканен под owner-choice
- реестровый пробел: карточек `night-procedure-frames` и `secret-parser-built` нет (claims-probe)
- P2-хвосты: хрупкий парсинг `waitsFor` в зубе #2245; lint `CabinetSampleDuplicatesPanel.tsx` (`titleOf`)

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
