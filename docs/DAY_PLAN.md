<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 64ef5edf696e0c129253f319e84f334ce28e740a92f2b0f4883c21cfb8660b0f · signedAt: 2026-08-28T08:26:25.677Z -->
# План дня — 2026-08-28

## Магистраль
- **angelina-hostess-impl** (L) — закрывает разрыв «гварда Angelina на горизонте без рабочей реализации хозяйской роли»: даёт опорный контур дня вокруг hostess-логики, пока веха `secret-parser-built` в фазе approaching, и тянет L-объём как сквозную ось, а не побочный шот.
- **assets-container** (L) — снимает боль неразмеченного хранения/носителя активов, когда буфер, библиотека и квота уже давят на рутину до дежурства; годится магистралью как крупный контейнерный каркас, без которого разрозненные media-хвосты снова расползутся.
- **batch-collection-run-contour** (L) — закрывает отсутствие цельного контура пакетного прогона коллекций (вместо точечных ручных проходов); подходит магистралью как L-ось на день, связывающая сбор и прогон в один контур, а не россыпь мелких задач.

Выбор магистрали — слово владельца (owner-choice)

## Подкрепление
- Прогон `night-triage-secret-scan.mjs` в режиме резака на датированном срезе архива сессий с манифестом «что вырезано / что оставлено» — снимает риск, что гейт `secret-parser-built` закроют детектором без доказанного агрессивного cut до бэкапа.
- Черновик датированного манифеста ротации засвеченных ключей (biweekly-контур) по одному реальному проходу скана — усиливает второй критерий вехи и не даёт амнистии архива опередить готовую ротацию.

## Перспективные
- Закрытие гейта `secret-parser-built` (резак + датированный проход с манифестом ротации) снимает амнистию правок архива и открывает безопасные бэкапы сессий с redaction до выгрузки.
- До дежурства **пт 28.08**: smoke частичной разгрузки буфера и прибор замера линеаризации журнала — без них пятница не принимает #2113 и калибровку сторожа.
- Апгрейд офиса и контейнер Sentry (#2122) не раньше **10.09** — до этой даты контур наблюдаемости E в план не брать.

## Экспериментальные
- Прогнать резак `night-triage-secret-scan` в dry-run на одной копии архивной сессии (без записи обратно): узнаем, режет ли он границы секрета целиком или оставляет читаемый хвост/контекст.
- Собрать черновик манифеста ротации только на заведомо протухших/фейковых ключах из одного датированного прохода: узнаем, хватает ли полей манифеста для «засветил → перевыпустил → подтвердил», или дыры в форме всплывут до боевого прохода.
- Сверить один бэкап-сессии «до резака» vs «после» на объёме и на том, уходит ли сырой фрагмент в путь бэкапа: узнаем, выполняется ли инвариант «секреты вырезаны до бэкапа» на реальном артефакте, а не на детекторе паттернов.

## Санитарные
- P0: довести до green прогон `media-library-service` / `background-media` — красные тесты с вечера 27.08, блокер гигиены
- recut `main-day-assertions.json` sources[0] → мандат 27.08/#2204 (сейчас stale 25.08, расхождение У1)
- хвост DoD #2204 п.3: стоп по порогу (remaining + сигнал наружу) не доказан в диффе — gap-таблица или воспроизводимый предикат
- oversized-хвосты #2208/#2211/#2212 — точечно, если цепляют квоту/прод
- claims-probe 27.08: в scripts нет `yarn turbo run test --filter=…`; карточек `media-library-service` / `background-media` нет в registry

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
