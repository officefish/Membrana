<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 4ff203d40ae1852137d01f7fa5282f41ea9bec2554740edf50f25e7c5124a543 · signedAt: 2026-08-08T07:59:59.055Z -->
# План дня — 2026-08-08

## Магистраль
- **`angelina-hostess-impl`** (L) — закрывает боль отсутствия живого фронт-хоста Ангелины: персона объявлена, но не исполняет; без имплементации любые провода тарифа и промокодов упираются в пустой приёмник. Годится магистралью, если приоритет — сомкнуть клиентский контур.

- **`archivarius-sessions-container`** (L) — закрывает риск безвозвратной потери 106К спанов: бэкапа тома нет ни одного глагола `backup`/`dump`/`restore` по репозиторию, и второй день подряд Teamlead называет это кандидатом №1. Годится магистралью как страховочная работа с ненулевой вероятностью катастрофы.

- **`assets-container`** (L) — закрывает боль отсутствия единого контейнера статики: без него раздача ассетов живёт в нескольких несогласованных местах, что блокирует прод-выкладку office (owner-gate, готово «всё кроме слова владельца»). Годится магистралью, если фокус дня — разблокировать деплой.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Добавить резак в `night-triage-secret-scan.mjs`** — прямое условие прохождения вехи `secret-parser-built`; без него гейт не открывается и блокирует амнистию архива.
- **Провести датированный проход с манифестом ротации засвеченных ключей** — второй обязательный критерий той же вехи; `credential-rotation-biweekly` уже активен как норма, проход фиксирует её исполнение.

## Перспективные
- Закрытие orphaned run `ritual-day-2026-08-07-r2` откроет нормальный старт дня — утренний ритуал перестанет спотыкаться о незакрытый прогон.
- Прокладка серверного роута (`tariff-promo-server-wiring`, блок C) откроет клиентский провод и ревью `TransitionDenyReason` → UI — без роута оба шага заблокированы.
- Вердикты по красному CI трёх детекторов (`cepstral` / `harmonic` / `spectral-flux`) откроют диагностику долга #1647 и достоверный локальный typecheck аудио-зоны.

## Экспериментальные
- **Проверить: гасит ли `spendPromo` реально атомарно при двойном вызове в одной транзакции** — запустить два параллельных вызова в тесте и убедиться, что второй получает `promo_already_redeemed`, а не panic или двойное списание. Узнаем: атомарность — декларация или факт.
- **Дать `night-triage-secret-scan.mjs` один файл с намеренно вшитым dummy-ключом** и проверить, что резак (не детектор) его вырезает и не кладёт в бэкап. Узнаем: парсер режет или только сигнализирует.
- **Сверить одну строку хендофа с кодом до заморозки снимка** — взять любое утверждение вида «X сделан» из сегодняшнего хендофа и проверить `grep`/`ls` за 2 минуты. Узнаем: воспроизводится ли класс ошибки `#team-feedback-claims-code-unverified` в нашем собственном хендофе.

## Санитарные
- Закрыть orphaned run `ritual-day-2026-08-07-r2` (`yarn procedure:close … --status fail --gaps orphaned`) — P1, блокирует утро
- Три вердикта по красному CI детекторов: `cepstral` / `harmonic` / `spectral-flux` — строка «аудио-дефект / инфраструктура» в журнал
- Ревью oversized-очереди: `yarn code-review:pr 1765` / `1767` / `1768` — по одному вердикту, очередь не сокращается третий день
- Дедуп `deps-watch-snapshot.json` (дубль `js-yaml` 1138114/1138115) при первом касании
- Поправить формулировку в `STRATEGY_DAY` или явно закрыть — третью неделю подсасывается генератором магистрали как живой источник

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
