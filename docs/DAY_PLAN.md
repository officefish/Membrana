<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 67ab0818fb2b790afe0b5b3d6d794fce8da106d45cdfa85971f4020354989df0 · signedAt: 2026-08-05T08:11:43.561Z -->
# План дня — 2026-08-05

## Магистраль
- **agent-tooling-night-build** — закрывает боль «ночной тулинг не собирается/не проверяется»: если CI красный, а ночной билд молчит, дефекты живут до утра. Годится магистралью, т.к. чинит P1-диагностику CI (три красных сервиса) и даёт право доверять зелёному перед продуктовым шагом.
- **angelina-hostess-impl** — закрывает боль «хендофф и манифест дня живут вручную» (судьба ветки `Membrana-tooling`, ручная доставка вечера 04.08). Годится магистралью, т.к. автоматизирует точку входа сессии и снимает хвосты «main никем не держится».
- **archivarius-sessions-container** — закрывает боль «сырые транскрипты на сервер не уходят» и хвост гейта `secret-parser-built` (предикат `amnestyLifted` — единственный формальный остаток). Годится магистралью, т.к. это четвёртый перенос уже прожитого спринта: контейнер соединён с жизнью (106К спанов), осталась верификация гейта и нотариальный ADR по Mongo-схеме.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Диагностика красного CI трёх сервисов до любой новой работы** — это разблокировка права доверять зелёному, без которого продуктовый шаг `detector-scoreboard` будет слепым.
- **Подготовка плана-таблицы `detector-scoreboard` Ф1 (DoD до кода)** — числа уже измерены, вход дешёвый (план, не код), и это первый шаг возврата на продуктовую магистраль после 3–4 инструментальных дней.

## Перспективные
- **Диагностика красного CI** — зелёный `main` открывает остальные задачи дня; после неё чистый merge `detector-scoreboard` Ф1 перестаёт быть слепым.
- **`detector-scoreboard` Ф1 (план таблицы до кода)** — первый продуктовый шаг после инструментальной недели; макет на измеренных числах из `dads-lab-report-2026-07-18.md` открывает витрину качества детекции для Курёхина и Родченко.
- **Ревью oversized-коммита `36e38102`** — закрытый вердикт снимает нарушение политики и открывает очередь ~140 oversized-ревью для ежедневного разбора.

## Экспериментальные
- **Проба 1:** `detector-scoreboard` Ф1 как дешёвый возврат на продуктовую магистраль — сядет ли витрина на числа из `dads-lab-report-2026-07-18.md` без новых замеров?
- **Проба 2:** `procedure-run-journal-panel-reader` — что узнаем: лента журнала, прожившая день на самообслуживании, читается первым же читателем или производители писали «в стол»?
- **Проба 3:** проверить, не связан ли красный CI `@membrana/media-library-service#test` с аудиопутём — аудио-дефект ли это или инфраструктурный сбой (влияет на приоритет `media-library-a3`).

## Санитарные
- **CI-диагностика**: `media-library`, `harmonic-detector`, `spectral-flux-detector` — красный P1, без разблокировки дальше нельзя.
- **Оversized-ревью**: дать вердикт по `36e38102` (581 строка) — политика нарушена, вечерний коммит без полного просмотра.
- **#1613**: Архитектору проверить контракт `EXECUTION_PROCEDURE` + `procedureKind` до повторного merge.
- **P2-путь**: поправить `../../docs/prompts/…` в `archive/archivarius-live-wiring.md` — одна строка при касании.

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
