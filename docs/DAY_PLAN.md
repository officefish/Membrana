<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 0c66c913740e92d349cff7342db4958b4137042d26db159e3f6540e6b4cfbfeb · signedAt: 2026-08-06T05:28:53.674Z -->
# План дня — 2026-08-06

## Магистраль
- **`agent-tooling-night-build`** (L) — закрывает боль «магистраль дня второй день остаётся словом без формы»: команда (5.5/10, фидбек 05.08) и архитектор прямо просят ADR-набросок до кода — что зубит ночной прогон, кто судья зелёного, где живёт журнал. Перенесена целиком за 05.08, хендоф требует сдвига минимум на один зубок.
- **`angelina-hostess-impl`** (L) — закрывает боль отсутствия субъекта, который держит сессию и знает её историю; без носителя-хостес оркестрация хендоффов остаётся ручной. Годится магистралью, если владелец считает контракт носителя (`insight:insight-cast-carrier-contract`) приоритетнее ночного билда.
- **`archivarius-sessions-container`** (L) — закрывает боль «106К записей живут без страховки»: #1714 (archivarius-mongo в манифест + бэкап тома) висит четвёртые сутки, данные в проде-по-факту без инвентаря. Годится магистралью, если безопасность архива важнее разблокировки CI-контура.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Красный CI трёх детекторов** (`cepstral`, `harmonic`, `spectral-flux`) — вердикт «аудио-дефект или инфра» по каждому до любой новой работы: без этого ночной билд будет зубить зелёный, которому нельзя верить, и магистраль упрётся в тот же блокер на первом же прогоне.
- **ADR-набросок `agent-tooling-night-build`** — что зубит ночной прогон, кто судья зелёного, где живёт журнал — до кода: снимает главный архитектурный риск магистрали (без формы легко расщепить работу по граблям, ADR-0023 прецедент уже есть).

## Перспективные
- Зелёный CI трёх детекторов (`cepstral`, `harmonic`, `spectral-flux`) откроет право доверять merge и снимет блокер с магистрали `agent-tooling-night-build`.
- ADR-набросок ночного билда откроет следующий зубок кода: без формы «что зубит / кто судья / где журнал» строить нечего.
- План-таблица `detector-scoreboard` Ф1 на измеренных числах откроет первую витрину качества детекции — без неё продуктовый контур стоит шестой день.

## Экспериментальные
- **Зуб 1 — читаемость кроя**: запустить `night-triage-secret-scan.mjs` на одном стареньком снимке сессии и замерить, что выводится — паттерны или уже вырезанный текст; узнаем, есть ли резак фактически или только детектор.
- **Зуб 2 — ADR-набросок одной строкой**: написать три поля (что зубит ночной прогон, кто судья зелёного, где живёт журнал) в черновик ADR `agent-tooling-night-build` — узнаем, нет ли противоречий между этими тремя решениями до написания кода.
- **Зуб 3 — класс красного**: запустить тест одного из трёх детекторов (`cepstral`) изолированно вне CI и сравнить с CI-логом — узнаем, аудиодефект или инфраструктурный шум даёт красный.

## Санитарные
- Красный CI (`cepstral`, `harmonic`, `spectral-flux`) — вердикт по каждому: аудио-дефект или инфраструктура; **до** любой новой работы (фидбек 05.08, хендоф #1)
- Ревью oversized: `yarn code-review:pr 1729` (1076 строк) и `95f139a7` (585) — по одному проходу (хендоф #8, фидбек п.6)
- Санитарка #1613 (`EXECUTION_PROCEDURE` + `procedureKind`) — отдельный маленький проход, ждёт LGTM третьи сутки (хендоф #7, Архитектор)
- `githubIssueClosedAt` для `llm-transport-no-key-class` (#1549 CLOSED) при первом касании registry.json; перечеканить `main-day-assertions.json` под У1 (хендоф #10, фидбек п.7)
- Проба 3: красный `@membrana/media-library-service#test` — аудио или инфра (хендоф #6, Музыкант)

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
