<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 9229f5f388f4d1ea5c0359334351c419b7b029348dd686b93953994ba0966e98 · signedAt: 2026-08-02T05:21:53.296Z -->
# План дня — 2026-08-02

## Магистраль
- **agent-tooling-night-build** (L) — закрывает боль отсутствия ночной сборки агентского тулинга, которая накапливает oversized-долг и мешает утренней стратегии стартовать с чистым деревом; годится магистралью, потому что горизонт дня требует прохождения `secret-parser-built`, а парсер секретов — часть тулинг-инфраструктуры, без которой бэкап сессий небезопасен.
- **angelina-hostess-impl** (L) — закрывает боль отсутствия живого субъекта, который держит процедуры и знает портфолио: хендоф прямо фиксирует, что механизмы проверяют факт шага, но не покрытие предмета; годится магистралью, потому что Angelina как хостес — это носитель портфолио процедур и журнала прогона, то есть пункты 1–2 магистрали владельца в одном воплощении.
- **archivarius-sessions-container** (L) — закрывает боль незаписанных прогонов: хендоф явно называет «журнал прогона отсутствует» как блокер перед любой витриной и оркестратором; годится магистралью, потому что веха `secret-parser-built` требует вырезки секретов ДО бэкапа, а контейнер сессий — ровно то место, где этот предикат должен проверяться механически.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Написать минимальный резак секретов в `night-triage-secret-scan.mjs`** — без резака скрипт остаётся детектором, и гейт `secret-parser-built` не пройти; это прямое условие снятия амнистии на архив.
- **Провести один датированный проход с манифестом ротации** — кристалл `credential-rotation-biweekly` требует двухнедельного цикла, а кристалл `session-backup-requires-secret-redaction` предполагает, что засвеченные ключи уже перечислены и помечены к перевыпуску; без прохода манифест остаётся декларацией.

## Перспективные
- Прохождение `secret-parser-built` откроет амнистию на правку архива — датированный проход с манифестом ротации засвеченных ключей станет возможен.
- Закрытие 16 oversized PR (начиная с #1584) откроет полноценную архитектурную проверку C4-границ в PR с meeting-протоколами и очистит санитарную полосу.
- Влитие MFCC-прибора (#1562) в main откроет smoke-тест на живом микрофоне и разблокирует вердикт по `mfcc-lib-choice`.

## Экспериментальные
- **Замер расхождения дискретизации**: запустить калибратор (48 кГц) и живую считалку подряд на одном сигнале, сравнить векторы — узнаем, опасно ли расхождение для сравнимости отпечатков.
- **Зонд ложного предупреждения `scripts:orphans`**: запустить команду вручную, сверить вывод с реальным числом сирот по `subject_unresolved` — узнаем, врёт ли инструмент системно или только в крайнем случае.
- **Проба `readCatalogs` в изоляции**: вызвать функцию с минимальным фиктивным каталогом без контекста `dead-wire-check` — узнаем, тестируема ли она отдельно или намертво сцеплена с окружением.

## Санитарные
- Точечный ревью PR #1584 (meeting/M-протоколы, 1718 строк) — первый из 16 oversized без развёрнутого диффа
- Проверить намеренность удаления `brace-expansion` из `deps-watch-snapshot.json` — молчаливая потеря, высокая серьёзность
- Исправить ложное предупреждение `scripts:orphans` — печатает «членства ноль» вместо корректного статуса 48 сирот по `subject_unresolved`
- Перезаписать `main-day-assertions.json` с `development-matrix` как магистралью — вещдок устарел, расхождение с `morning-gates-state.json` не закомментировано

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
