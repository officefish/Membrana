<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 29bccb3bdc6230b8ab2178a3f259187145bc605001de841946230a702760cf2b · signedAt: 2026-09-26T08:50:58.376Z -->
# План дня — 2026-09-26

## Магистраль
- **`angelina-hostess-impl`** (L) — закрывает боль отсутствия живого хостесс-слоя: без него холодная сессия не имеет точки входа с контекстом и вынуждена реконструировать состояние вручную. Годится магистралью, если владелец считает, что дезориентация сессий (см. фидбек: `CURRENT_TASK.md` третий день указывает на устаревший эпик) стала основным тормозом дня.

- **`assets-container`** (L) — закрывает боль неструктурированного хранения артефактов: без контейнера активы рассыпаны по зонам, версионирование и ротация (в том числе засвеченных ключей — критерий вехи `secret-parser-built`) опираются на ручные соглашения. Годится магистралью, если владелец считает, что инфраструктурный фундамент активов разблокирует сразу несколько стоящих задач.

- **`batch-collection-run-contour`** (L) — закрывает боль отсутствия воспроизводимого контура пакетного прогона: буфер (1727 проб, хвост из хендофа) и ночные дежурства упираются в неавтоматизированный сбор, что делает разгрузку буфера ручной и дорогой. Годится магистралью, если владелец ставит разблокировку полевого корпуса выше текущих инфраструктурных долгов.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Починить `sequence`-контракт trail-агрегатора** (синтетический тест двух пересекающихся `runId`): контракт монотонности — прямой предикат вехи `secret-parser-built`, без него гейт технически непроходим третий день подряд (P1 по фидбеку Тарасова и Ожегова).
- **Получить дословный лог отказа образа офиса и прогнать `verify:image-workspace-deps`**: rollout день 3 заблокирован именно здесь; разблокировка снимает риск бесконечного ухода дня в санитарию вместо магистрали и создаёт почву для прохождения гейта (пара `cabinet.env` + второй кабинет — следующий шаг той же цепочки).

## Перспективные
- Починка `sequence`-контракта trail-агрегатора откроет прохождение гейта `secret-parser-built` — он остаётся заблокированным четвёртый день именно из-за этого P1.
- Дожатый rollout день 3 (образ офиса + `cabinet.env` на VDS + второй кабинет) откроет пишущий смоук панели и lint-fix `titleOf` — шаг 6 rollout и хвосты Rodchenko ждут только поднятого второго кабинета.
- Проход по трём oversized-PR (#2441, #2450, 4473df99) откроет архитектурную проверку решений, принятых в них молча, и разблокирует правило `PR ≤ 400 строк` для pre-merge check.

## Экспериментальные
- **Прощупать `sequence`-счётчик через два пересекающихся `runId` в тесте** — узнаем, воспроизводится ли P1 синтетически и где именно агрегатор теряет монотонность.
- **Запустить `night-triage-secret-scan.mjs` на одном архивном файле вручную** — узнаем, режет ли он уже или только детектирует (граница гейта `secret-parser-built`).
- **Добавить `friction`-запись за 24.09 в trail и прочитать агрегатор** — узнаем, считает ли он день безупречным при пустом поле (слепота, замеченная вечером 25.09).

## Санитарные
- `sequence`-контракт trail-агрегатора: два `runId` с `sequence:1` в `2026-09-25.jsonl` — P1 четвёртый день, блокирует гейт `secret-parser-built`
- `CURRENT_TASK.md` устарел третий день: убрать `detector-scoreboard`, вписать `cabinet-registration-rollout` (XS)
- Ревью-долг: три oversized-PR (#2441/725 строк, #2450/1074, 4473df99/1147) влиты без разбора — архитектурные решения не проверены
- Lint-fix `titleOf` (`react-hooks/exhaustive-deps`) в `apps/cabinet` висит третий день — XS
- `friction`-запись за 24.09 в trail не добавлена: агрегатор читает день как безупречный

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
