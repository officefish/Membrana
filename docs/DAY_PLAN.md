<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 41aca9866c965fde765958819f7c917de3f54a3c77791ae16dccfc573ebb0633 · signedAt: 2026-09-02T11:28:16.977Z -->
# План дня — 2026-09-02

## Магистраль
- **angelina-hostess-impl** (L) — закрывает боль «нет живого хозяина контура» у вехи `secret-parser-built`: hostess проводит резак и датированный проход с манифестом ротации, а не оставляет гейт набором детекторов. Тянет в магистраль, потому что без носителя амнистия на правку архива так и не снимется.
- **assets-container** (L) — закрывает боль рассинхрона носителей секретов и артефактов: единый контейнер активов, чтобы ключи и срезы не жили россыпью до бэкапа сессий. Годится магистралью как каркас под `session-backup-requires-secret-redaction` и biweekly-ротацию.
- **batch-collection-run-contour** (L) — закрывает боль ручного/фрагментированного прогона: один контур batch-прохода (манифест → резак → ротация) вместо разовых сканов. Магистрален тем, что прямо закрывает критерий вехи «один датированный проход с манифестом».

Выбор магистрали — слово владельца (owner-choice)

## Подкрепление
- **Фикстурный корпус «засвеченных» фрагментов для `night-triage-secret-scan.mjs`** — прогон резака на известных паттернах (ключи, токены, env-строки) до боевого датированного прохода; без этого гейт `secret-parser-built` рискует пройти на детекторе без доказанного cut.
- **Черновик манифеста ротации засвеченных ключей** (формат записи: что/где/когда перевыпустить) — снимает риск, что датированный проход найдёт утечки, но не сможет закрыть критерий «манифест ротации» в тот же контур.

## Перспективные
- Прохождение гейта `secret-parser-built` (резак + датированный проход с манифестом ротации) снимет амнистию на правку архива и откроет безопасные бэкапы сессий без сырых секретов.
- Закрытие двух красных тестов (`background-media`, `background-cabinet`) как предусловия любого merge разблокирует ствол и очередь PR на следующие дни.
- Узкое закрытие `sample-move-between-collections` в обоих домах откроет продуктовую подвижность набор↔набор и следующие фичи библиотеки-близнецов без локальных костылей.

## Экспериментальные
- Прогнать `night-triage-secret-scan.mjs` на одном датированном срезе архива в dry-run с записью «что бы вырезал» рядом с «что нашёл» — узнаем, резак уже режет или пока только детектит.
- Сверить 3–5 засвеченных ключей из последнего скана с фактическим статусом ротации (жив/отозван/заменён) без правок — узнаем, хватает ли сигнала для манифеста ротации или дыра в учёте.
- На одном бэкапе сессии сравнить «до парсера» vs «после» по объёму и наличию secret-подобных токенов (без чтения содержимого в промпт) — узнаем, выполняется ли инвариант «сырое на сервер не уходит».

## Санитарные
- два красных теста до любого merge: `@membrana/media-library-service` и `@membrana/background-cabinet` — диагноз «помеха vs pre-existing»
- три oversized над стволом без разворота: `caf5208d` (494), `f70b9064` (1559), `9f49a1c0` (628)
- ревью-долг PR #2244 — diff / merge-gate или списать в ретро
- P1 `scripts/lib/repo-links.mjs`: `ISSUE_LINK` с `/g` в модульном скоупе → `lastIndex` между вызовами
- токен бота сторожа диска (#2148) — перевыпуск (дважды светился в переписке)

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
