<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: e919a75e669dee99230654c027496e45804a96744fd4b710ea3b715f435c2bde · signedAt: 2026-07-31T05:11:36.080Z -->
# План дня — 2026-07-31

## Магистраль
- **archivarius-sessions-container** — закрывает боль сырых транскриптов и архива без резака: бэкап сессий требует вырезания секретов до ухода на сервер, а амнистия на правку архива снимается только гейтом `secret-parser-built`. Годится магистралью, потому что напрямую двигает веху (контейнер сессий + агрессивный парсер + датированный проход с манифестом ротации).
- **agent-tooling-night-build** — закрывает боль ночного контура, где `night-triage-secret-scan.mjs` всё ещё детектор без резака, а исследования и триаж живут ночью. Годится магистралью как L-каркас тулинга под ночной проход и ротацию ключей, без которого гейт `secret-parser-built` не из чего собрать.
- **angelina-hostess-impl** — закрывает боль отсутствующего носителя/хозяйки контура (guard `angelina` уже в горизонте), без которого ночной триаж и архивные проходы не на чем стабильно гонять. Годится магистралью как L-реализация хозяина процедуры, связывающая ночной проход и снятие амнистии архива.

Выбор магистрали — слово владельца (owner-choice)

## Подкрепление
- Корпус «один тракт — два класса» (один канал записи, дрон vs фон): снимает `undecided_corpus_bias` и делает любой AUC/MFCC-вердикт утверждением о цели, а не о тракте — без него магистраль «доказать MFCC» опирается на неинтерпретируемую цифру 0.942.
- Сборка ядра в потребляемый вид (`dist` + установленная `meyda`, не stand-in): убирает пометку `core: stand-in` с каждой цифры вчерашнего прогона и закрывает риск, что магистраль сравнивает детекторы на подставке, а не на пакете.

## Перспективные
- Корпус «один тракт — два класса» откроет интерпретируемый вердикт MFCC и оба зависимых инсайта (тембровый + комбинированный детекторы).
- Гейт `secret-parser-built` (резак + датированный проход с манифестом ротации) снимет амнистию на правку архива.
- Провод «предсказание ↔ исход» в архив персоны откроет калибровку оценки объёма на следующих честных спринтах без ручного занесения промахов.

## Экспериментальные
- Прогнать MFCC-дистанцию на 3–5 парах «один класс / разные тракты записи» из уже лежащего корпуса — узнаем, канал или класс двигает AUC 0.942.
- На одном засвеченном ключевом фрагменте сравнить текущий детектор паттернов с черновым резаком (backup vs redacted side-by-side) — узнаем, хватает ли «вырезать до архива» без ложного сноса соседнего текста.
- Склеить 10-секундный синтетический кадр «гармоника + чужой тембр» и прогнать FFT-only vs MFCC-вектор — узнаем, ловит ли тембровый контур то, что гармонический уже пропускает.

## Санитарные
- Вчерашний день не закрыт: `ritual:evening`, `team-evening-feedback`, ласточка — итога нет ни у партнёров, ни у команды
- `storm/mfcc-sprint-test-3007` (12 коммитов, MFCC-ядро + спринт + исход) живёт вне main — «показал ≠ доставил»
- 19 oversized (>400 строк) неразвёрнуты в дайджесте; точечный `yarn code-review:pr` на крупнейший (#1499 / #1467), не пакет
- Три worktree/ветки коворка живы (`Membrana-cut-contract`, `-execution-gate`, `-experience-loop`) — снятие по ADR-0020
- Архив `17cd343b` (`ritual/evening-2026-07-29`) вне main — слово владельца: довезти или списать

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
