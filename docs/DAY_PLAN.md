<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 6a67ec19d54157613377ae7e17949fa9f991edaafc8d6696d2488ed8b8c9a7fb · signedAt: 2026-08-03T06:03:28.197Z -->
# План дня — 2026-08-03

## Магистраль
- **agent-tooling-night-build** (L) — закрывает боль отсутствия надёжного ночного тулинга для агентов; при живом резаке секретов ночной билд получит чистый субстрат и не потащит в прогон сырые транскрипты. Годится магистралью, если владелец ставит инфраструктуру агентов выше безопасности архива.

- **angelina-hostess-impl** (L) — закрывает боль отсутствия хостес-слоя: персона, которая встречает сессию, не имеет реализации, хотя лифт всплытия уже наполовину построен и некому его «включать». Годится магистралью, если приоритет — замкнуть двухконтурную память в рабочий цикл.

- **archivarius-sessions-container** (L) — закрывает прямую боль: около 1 500 архивных записей существуют, но ни одна не всплыла, потому что контейнер не поставлен; гейт `secret-parser-built` является его предусловием — амнистия на правку архива нужна именно архивариусу. Годится магистралью сильнее остальных двух: резак секретов → амнистия → архивариус — единственная цепочка, где шаги идут друг за другом, а не параллельно. Тимлид, структурщик и резюме teamlead второй день называют резак «малой работой с высокой пользой»; не закрыть его сегодня значит снова расщепить магистраль.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Написать резак секретов в `night-triage-secret-scan.mjs`** — это прямое условие прохождения гейта `secret-parser-built`, который и есть веха дня; без резака амнистия на архив не снимается, и весь контур бэкапа сессий остаётся незакрытым третий день подряд.
- **Перечеканить `main-day-assertions.json` под актуальную магистраль** — расхождение канона дня с гейтом порождает ложный диагноз каждый раз, когда probe читает файл; малая работа, устраняет системную помеху, которую команда зафиксировала открытой уже во вчерашнем фидбеке.

## Перспективные
- Написать резак в `night-triage-secret-scan.mjs` → пройдёт гейт `secret-parser-built` и снимет амнистию на архив, открыв правку истории сессий.
- Закрыть #1649 (журнал прогонов без производителя) → разблокирует `insight-server-only-ritual-run` и даст наблюдаемость server-only опыту.
- Замер расхождения дискретизации 48 кГц (калибратор vs живая считалка на одном сигнале) → откроет интерпретируемость MFCC-отпечатков и сравнимость детекторных замеров.

## Экспериментальные
- **Зонд лжи `scripts:orphans`**: запустить вручную, сравнить счётчик `no_rule` со счётчиком `subject_unresolved` — узнаем, всегда ли прибор врёт или только в конкретном сценарии.
- **Минимальный резак секретов**: написать чистую функцию `redact(transcript) → {redacted, manifest}` без контекста файловой системы — узнаем, хватит ли простого regex-прохода или нужен парсер состояний.
- **Расхождение дискретизации**: прогнать калибратор и живую считалку на одном коротком сигнале подряд — узнаем, критично ли расхождение 48 кГц для сравнимости MFCC-отпечатков или можно продолжать без перекалибровки.

## Санитарные
- Резак секретов в `night-triage-secret-scan.mjs` не написан третий день — гейт `secret-parser-built` заблокирован, амнистия на архив не снята
- `main-day-assertions.json` несёт заявку от 30.07 (MFCC-ядро), гейт — сегодняшний; перечеканить до запуска основной работы
- Иссью #1590 и #1595 открыты, работа в стволе — закрыть батчем
- `scripts:orphans` печатает «членства ноль» вместо 48 сирот по `subject_unresolved` — зонд вручную, зафиксировать расхождение
- Точечный ревью oversized PR: начать с #1584 (1718 строк), 16 в очереди без диффов

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
