<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: e95629d3b5b83747438b8c3e85200cac15e5bf7bdc5c115e88c353f84f2d7e73 · signedAt: 2026-09-24T10:41:45.530Z -->
# План дня — 2026-09-24

## Магистраль
- **angelina-hostess-impl** (L) — закрывает боль отсутствия живого хост-слоя: персонаж Angelina не реализован, и каждый день без неё — это день без интерфейса приёма сессий. Размер L оправдан масштабом, но в горизонте дня нет прямой связи с вехой `secret-parser-built`.

- **assets-container** (L) — закрывает боль отсутствия контейнера хранения ассетов: без него медиа-артефакты кочуют без адреса. Годится магистралью как фундаментальная инфраструктура, но зона не размечена и связь с вехой `secret-parser-built` не прослеживается по материалу.

- **batch-collection-run-contour** (L) — закрывает боль ручного запуска сборов: без контура пакетного прогона сбор данных зависит от ручного вмешательства. Ближе всего к текущей фазе `approaching` по веху `secret-parser-built`, потому что парсер секретов требует контура прогона с манифестом ротации — именно это описано в критериях вехи («выполнен один датированный проход с манифестом ротации засвеченных ключей»).

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Патч `_daily-standup.mjs`**: источник магистрали строго из `sources[0].claim`, синтез запретить — снимает P1-расхождение стендап↔owner-choice, которое второй день блокирует доверие к плану дня и тем самым замедляет прохождение любой магистрали, включая `secret-parser-built`.
- **Закрыть хвост двух runId с пересекающимися sequence в trail-агрегаторе** — пересекающиеся sequence делают trail ненадёжным вещдоком прохождения гейта, а `secret-parser-built` требует датированного прохода с манифестом ротации именно в trail.

## Перспективные
- Выкатка прода `cabinet-registration-rollout` (офис-стек → env → образ кабинета → лендинг → приёмка) откроет закрытие #2369 и снятие контейнера заседания `cabinet-registration-promo`.
- Патч `_daily-standup.mjs` (источник магистрали строго из `sources[0].claim`) откроет следующий день без P1-расхождения стендап↔owner-choice — блокер третьего дня снимается навсегда.
- Прохождение гейта `secret-parser-built` (резак + датированный проход с манифестом ротации) откроет снятие амнезии на правку архива.

## Экспериментальные
- **Проба 1 — friction-запись как триггер резака.** Добавить одну JSONL-строку `friction` в trail вручную и прогнать `night-triage-secret-scan.mjs` — узнаем, видит ли резак friction-записи или пропускает их как безопасные.
- **Проба 2 — пустой слот DAY_PLAN как B6.** Запустить план-генератор с заведомо пустым каналом insights и проверить exit-код — узнаем, падает ли он с ненулём или молча кладёт заглушку.
- **Проба 3 — `sources[0].claim` в стендапе.** Вручную подставить в `_daily-standup.mjs` чтение `sources[0].claim` вместо синтеза и прогнать на вчерашнем `main-day-assertions.json` — узнаем, снимает ли одна строка P1-расхождение воспроизводимо.

## Санитарные
- Закрыть P1-расхождение стендап↔`sources[0]` — патч `_daily-standup.mjs`, источник магистрали строго из `main-day-assertions.json`, синтез запретить (третий день)
- Добавить `friction`-запись в trail за 22–23.09 — одна строка JSONL, второй день висит
- Закрыть хвост двух runId с пересекающимися sequence в trail-агрегаторе (P1 из ревью 22.09)
- Lint-warning `react-hooks/exhaustive-deps` (`titleOf` в `useCallback`) в `apps/cabinet` — одна строка, третий день
- Диагноз плавающего теста `journal/selection/chain-rehearsal.test.ts` (#2399) — корневая причина или `.skip`

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
