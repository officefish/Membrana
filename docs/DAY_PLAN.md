<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: eed134cedf1011fa64d9a2d6ba1b47f0b742e9c3e5ab29574ae89eaad8fa83c3 · signedAt: 2026-08-12T12:06:01.684Z -->
# План дня — 2026-08-12

## Магистраль
- **`angelina-hostess-impl`** (L) — третий день висит декларацией без единой строки кода: hostess-контракт не закрывает боль «непонятно, что умеет Ангелина и в каком формате отвечает», а без этого панель (Rodchenko) и вся продуктовая полоса стоят на паузе. Teamlead дважды (HANDOFF + вечерний фидбек) назвал её магистралью завтра с обязательным первым шагом `yarn main-day-probe`.
- **`archivarius-sessions-container`** (L) — закрывает боль неконтролируемого роста сырых сессий: кристалл `session-backup-requires-secret-redaction` требует, чтобы транскрипты вырезались до бэкапа, а без контейнера архивариуса цепочка «резак → бэкап → хранилище» не замкнута. Годится магистралью, если владелец решит двигать веху `secret-parser-built` напрямую.
- **`assets-container`** (L) — закрывает боль отсутствия единого хранилища статических ресурсов, на которое опираются несколько контуров (`static-mmbrn-live-inventory`, `static-mmbrn-live-services` — оба живые в очереди двадцати малых). Годится магистралью как разблокирующий контейнер для A2/C-фаз static.mmbrn, но прямого сигнала срочности от команды вчера не было.

Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **`ritual-magistral-source-freshness`** (#14 из очереди) — перечеканить `main-day-assertions.json` под гейт 11.08; без этого генератор завтра снова прочитает магистраль 08.08 и подкормит фантомный выбор вместо `angelina-hostess-impl`, что напрямую подрывает старт магистрали дня.
- **`morning-gates-two-moments`** (#9 из очереди) — реализовать два момента утреннего гейта по ADR-0024; карточка ADR принята, реализация не начата, а без честного gate-момента owner-choice снова будет висеть декларацией без фиксации в файле — тот же класс поломки, что уронил магистраль 11.08.

## Перспективные
- Завершение `secret-parser-built` (резак + манифест ротации) снимет амнезию на правку архива и откроет следующие дни для полноценной работы с историей сессий.
- Первый шаг `yarn main-day-probe` по `angelina-hostess-impl` переведёт посылки `unknown` в `holds`/`violated` и разблокирует продуктовую полосу, которая стоит с 08.08.
- Перечеканка `main-day-assertions.json` под гейт 12.08 позволит генератору завтра читать живую магистраль, а не артефакт трёхдневной давности.

## Экспериментальные
- **MFCC-библиотека без установки**: взять три кандидата из README-ов (mfcc-js, node-mfcc, librosa-wasm), сравнить API по типам входа — узнаем, какой принимает Float32Array без конвертации.
- **Парсер секретов как резак — smoke-тест на фикстуре**: создать файл с тремя паттернами ключей, прогнать текущий `night-triage-secret-scan.mjs`, посмотреть, режет ли он или только детектирует — узнаем, закрыт ли гейт `secret-parser-built` фактически.
- **Память процедур «показывается, не запрашивается»**: в одном фрейме утреннего ритуала подставить последние 3 записи журнала без запроса подтверждения — узнаем, достаточно ли контекста для осмысленного продолжения без явного ввода.

## Санитарные
- Перечеканить `main-day-assertions.json` под гейт 11.08 — иначе генератор читает магистраль 08.08 (хвост третий день)
- Завести карточку `sprint-experience-dead-ends-after-recut` (`yarn task:create --id sprint-experience-dead-ends-after-recut --size S`) — зуб в `debt-ledger.jsonl` есть, реестра нет
- Вынести порог `133` в единый источник: убрать дублирование между `package.json` и `ci.yml`
- Проверить факт закрытия GitHub-иссью #1764 #1447 #1422 #1272 #554, отмеченных `githubIssueClosedAt`
- Повердиктить 9 мёртвых душ и отправить в `task:archive` со свидетельством (в т.ч. `detectors-window-single-carrier`, commit `07928a67`)

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
