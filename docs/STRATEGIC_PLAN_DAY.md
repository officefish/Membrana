<!-- Сгенерировано: 2026-07-14T04:02:10.182Z (yarn plan:day) -->
<!-- Период: последние сутки (since="1 day ago"); горизонт: следующий день -->
<!-- Источник цели: WHITE_PAPER.md -->

# План на следующий день

## 1. Что сделано за период (последние сутки (since="1 day ago"))

- **`packages/background-office` (инфра/интеграции):** новый модуль `modules/telegram` — outbound-дайджесты ритуалов для нетехнических союзников (`TelegramClient` fire-and-forget, чистые format-функции, `POST /v1/telegram/ritual-digest` под `ApiTokenGuard`, push-ingest по образцу ADR 0004). Тесты office 78/78. Отдельным фиксом добавлен proxy-aware `TelegramClient` (undici ProxyAgent при `HTTPS_PROXY`) и приоритет `OFFICE_API_TOKEN` для локального пуша (#428, #431).
- **`packages/background-office` (LLM-провайдеры):** DeepSeek-direct как fallback нарратива ночных агентов (цепочка Claude → DeepSeek → graceful, ADR 0005); OpenRouter выведен (403). Модуль-зеркало `openrouter`, метка канала в отчётах (#424, #426).
- **`packages/services/rag` (analyzer/инфра):** второй embeddings-провайдер `voyage` через фабрику `embed/factory` по существующему контракту `RagEmbeddingProvider` (тип не менялся), undici ProxyAgent для обхода DPI, retry на 429. 37 тестов pass (#425).
- **`@membrana/core` + инфра CI (drift-anchor):** контракт `DriftAnchorRecord` + чистые `buildCodeAnchorRecord`/`evaluateProdMainDivergence` (ADR 0003); CI-гейт детекторов (`detector-drift-gate.yml`), divergence-алерт «Прод ≠ main», секция дрейфа в ритуалах; office-модуль `drift-anchor` (POST records / GET digest, ADR 0004); data-anchor `__tariff_dataset__` на background-media (warning-only). E2E подтверждён на живом office+media (#396, #413/#414, #418/#419).
- **`apps/client` + `device-board` (FREE-магистраль, S2):** yamnet в живом combined-детекторе — fusion спектр+нейро, TTL-субдискретизация нейро-инференса, метка модальностей; DEV-лог `latencyMs` yamnet для перф-замера (#415/#417/#416, консилиум 2026-07-13). Обновлён `free-tier-user-case-entries.ts`.
- **Ритм/скиллы/память (инфра):** скилл `membrana-insight-overview` (read-only обзор инсайтов + фаворит) с крест-проверкой занятости; фаза 1 `persona-memory` (детерминированный журнал персоны Dynin, инъекция в `ask`/`consilium` за флагом); бэкфилл `sprintPhase` в реестре инсайтов.

## 2. Привязка к стратегической цели

**Где мы на дорожной карте (WHITE_PAPER §8):** формально между **Этапом 1.B** (нейро-эшелон на одном узле — yamnet уже в prod-бенчмарке, F1 0.803) и **продуктовой финализацией FREE-тарифа** (форсайт 2026-07-06). Многоузловые этапы (2–7: TDOA, локализация, трекинг) остаются заморожены до прохождения hard-gate на VDR-корпусе.

**Что приближает к цели:**
- yamnet в live combined-fusion (#417) — прямой вклад в **S2 combined UC** продуктовой магистрали и в принцип §3 «слияние модальностей в одной шине» (сырой confidence нейро + спектр). Это ядро того, что увидит пользователь FREE.
- Обновление `free-tier-user-case-entries.ts` — движение к **S3 (упаковка UserCases)**.

**Что нейтрально (полезная инфра, но не двигает этапы напрямую):**
- drift-anchor контур (#396) — гарантирует, что «Прод = main» для детекторов и датасета; страхует качество, но не поднимает recall/precision.
- Telegram-дайджесты, DeepSeek/voyage-провайдеры, persona-memory, insight-overview — инфраструктура команды и коммуникаций, не путь роста качества детекции.

**Что отвлекает:** явных отвлечений в коммитах не видно — все работы либо на FREE-магистрали, либо в поддерживающей инфре. Риск в том, что доля инфра-коммитов за сутки заметно выше доли продуктовых (S2/S3), при дедлайне FREE ~17.07.

**Недостающие сервисы (пока НЕ начинать — этап заморожен):** `tdoa-service`, `localizer-service`, `tracker-service`, `transport-service` — все относятся к Этапам 2–7, заблокированным до hard-gate. Появление рабочего дерева `docs/reviews/tdoa-localizer-spec-s1/` в статусе (untracked) — это **спецификационная разведка**, не реализация; она допустима как бумажная подготовка, но не должна превращаться в код до прохождения шлюза.

**Детекция (эпик #84, `FFT_METRICS_POTENTIAL_AND_LIMITS.md`):** эшелон 0 (чистый DSP/FFT) на free-v1 исчерпан — потолок trends `DRONE_TIGHT` 95%/30%; одиночные harmonic/cepstral/flux — no-go как селекторы. Дальнейший рост качества — только через fusion (trends + yamnet) и новые данные (VDR-корпус ~17.07), а НЕ через повторный тюнинг DSP.

## 3. Риски и долг

- **Долг объяснимости fusion:** trends `DRONE_TIGHT` (объяснимый бэкап) и yamnet (основной) нигде не сведены одной таблицей на общем `val` — непонятно, кто «основной» в hard-gate, а кто объяснимый резерв. Растёт риск, что к hard-gate ~17.07 придём без формального обоснования выбора.
- **Незакрытые хвосты живых смоуков:** DeepSeek/voyage работают только через прокси и упираются в биллинг аккаунтов (402/rate-limit); полный `rag:index --full` и нарратив ночных агентов — «хвосты после пополнения». Это внешний блокер, не архитектурный, но он висит.
- **Перф-долг нейро-fusion:** для yamnet добавлен только DEV-лог `latencyMs` (#416) — фактический замер p95 инференса на типовом железе ещё не снят; принцип §2 (Этап 1.A: latency p95 < 100 ms) для нейро-канала не подтверждён.
- **Untracked-артефакты в рабочем дереве:** `docs/reviews/tdoa-localizer-spec-s1/` и `branch-feat-telegram-ally-reports-code-review.md` не закоммичены — риск потери/дрейфа. Спека TDOA/localizer относится к замороженному Этапу 2 — нужно явно пометить как «preserved / за stage-gate», чтобы не спровоцировать преждевременную реализацию.
- **Границы пакетов:** нарушений по diff не видно. Провайдеры (deepseek/voyage) корректно инкапсулированы в `background-office` с локальным DTO (автономность, §BACKGROUND_SERVERS) и в `rag` через фабрику без изменения контракта. yamnet-fusion живёт в `apps/client/plugins` — допустимо. Push-ingest ADR 0004 фиксирует три инварианта (локальный DTO вместо core-импорта, in-memory, публичный GET только несекретного) — их надо держать при третьем повторе паттерна (сработает триггер консилиума).
- **Ограничения WHITE_PAPER, релевантные сейчас:** синхронизация времени и многолучёвость (§9) пока неактуальны — мы на одном узле. Релевантен риск «ложных тревог < 5%» (§11): текущий live-fusion опирается на yamnet FPR ~36.7% — далеко от целевого, что подтверждает: FREE-версия сигнализирует присутствие, не даёт автотревогу.

## 4. План на следующий день

### Задача A — Сводная таблица trends+`DRONE_TIGHT` vs yamnet на общем `val`
- **Цель** — одна таблица «основной vs объяснимый бэкап» на held-out `val` (recall/precision/FPR/F1) для входа в hard-gate.
- **Пакет / слой** — analyzer (`packages/services/*` детекторы) + документ `DETECTOR_BENCHMARK.md`; без нового кода детекторов.
- **Связь с WHITE_PAPER** — §8 stage-gate 1→2, §11 (доля ложных тревог); поддерживающая полоса детекции.
- **Definition of Done** —
  1. таблица на одном и том же `val`-срезе, обе конфигурации в одной шкале метрик;
  2. явно назван «основной для hard-gate» и «объяснимый бэкап» с обоснованием;
  3. отражена слабая корреляция профилей ошибок DSP/нейро (заметка ND3);
  4. записано в `DETECTOR_BENCHMARK.md` без повторного тюнинга порогов.
- **Роль** — Математик (ведёт), LGTM Teamlead.
- **Размер** — M.

### Задача B — Перф-замер p95 yamnet-инференса в live combined
- **Цель** — подтверждённая цифра p95 latency нейро-канала на типовом ноутбуке.
- **Пакет / слой** — client (`apps/client/plugins/mic-combined-detection`).
- **Связь с WHITE_PAPER** — §2 (задержка появления → отображения ≤ 3 c), §8 (latency p95 < 100 ms для узла); принцип §4 (слияние модальностей).
- **Definition of Done** —
  1. снят p95 из DEV-лога `latencyMs` (#416) на репрезентативном прогоне;
  2. цифра зафиксирована в задаче/`CURRENT_TASK`;
  3. если p95 > бюджета — заведён follow-up на TTL-субдискретизацию;
  4. тесты плагина остаются зелёными.
- **Роль** — Музыкант (аудио-инференс) при поддержке Верстальщика (UI-метка).
- **Размер** — S.

### Задача C — Продвижение S2 combined UC к упаковке (шаг к S3)
- **Цель** — combined UC (спектр+нейро + alarm-loop «ближе/дальше» по громкости) оформлен как готовый пользовательский кейс для FREE.
- **Пакет / слой** — client (`apps/client`) + `device-board` (каталог UserCase, `free-tier-user-case-entries.ts`).
- **Связь с WHITE_PAPER** — §4.6 (ситуационный слой), продуктовая магистраль S2→S3 (форсайт 2026-07-06).
- **Definition of Done** —
  1. alarm-loop «ближе/дальше» по громкости работает поверх fusion-выхода;
  2. UC зарегистрирован через `MembranaRegistry` (lazy-module, §1c ARCHITECTURE);
  3. карточка UC присутствует в device-board-каталоге;
  4. используется сырой confidence yamnet, не бинарный вердикт (форсайт).
- **Роль** — Верстальщик (ведёт), Музыкант по fusion-контракту, LGTM Teamlead.
- **Размер** — M.

### Задача D — Пометить TDOA/localizer-спеку как preserved за stage-gate
- **Цель** — untracked-спека `docs/reviews/tdoa-localizer-spec-s1/` зафиксирована как бумажная разведка Этапа 2, не как заявка на реализацию.
- **Пакет / слой** — infra/docs (без кода в `packages/services/*`).
- **Связь с WHITE_PAPER** — §8 (TDOA/localizer заморожены до stage-gate 1→2), §6 (fusion не меняется).
- **Definition of Done** —
  1. каталог закоммичен с явной пометкой `@stage 2 / preserved / за hard-gate`;
  2. в документе указано, что реализация не начинается до прохождения hard-gate на VDR-корпусе;
  3. ссылка из реестра задач как «разведка, не спринт».
- **Роль** — Структурщик (границы пакетов) + Teamlead (гейт).
- **Размер** — S.

### Задача E — Закрыть хвосты живых смоуков провайдеров (учёт долга)
- **Цель** — статус DeepSeek/voyage-хвостов (биллинг/rate-limit) явно зафиксирован, чтобы не блокировать FREE молча.
- **Пакет / слой** — infra (`background-office`, `packages/services/rag` — только учёт/документация, не код).
- **Связь с WHITE_PAPER** — нейтральная инфра, но снимает скрытый блокер команды.
- **Definition of Done** —
  1. в `CURRENT_TASK`/реестре зафиксировано, что смоук ждёт owner-гейтов (баланс аккаунтов);
  2. подтверждено, что fallback-цепочка graceful не ломает ночные ритуалы при 402/429;
  3. VDR-железо ~17.07 явно помечено как «не блокер FREE».
- **Роль** — Teamlead (учёт), Структурщик (проверка graceful).
- **Размер** — S.

## 5. Что НЕ делаем на этом горизонте

- **НЕ проводим повторный unified benchmark harmonic/cepstral/spectral-flux на free-v1** без нового датасета/алгоритма/fusion — потолок эшелона 0 зафиксирован (`FFT_METRICS_POTENTIAL_AND_LIMITS.md` §6); одиночные DSP — no-go как селекторы.
- **НЕ начинаем реализацию `tdoa-service` / `localizer-service` / `tracker-service` / `transport-service`** — Этапы 2–7 заморожены до прохождения hard-gate (WHITE_PAPER §8). Спека допустима только как бумажная разведка (Задача D).
- **НЕ «разведываем» yamnet и не переизобретаем нейро-детектор/плагин/бенчмарк** — эшелон 2 де-факто открыт, yamnet уже в prod (#266/#268, F1 0.803).
- **НЕ переизобретаем «Этап 1.A / доведение трёх DSP до stage-gate»** — это тупиковая магистраль по вердиктам консилиума.
- **НЕ гонимся за precision hard-gate 85/90 на free-v1** — hard-gate проверяется на независимом VDR-корпусе (~17.07), калибровка на free-v1 «ещё раз» бессмысленна.

## 6. Проверки в конце периода

- **Задача A:** в `DETECTOR_BENCHMARK.md` есть одна таблица на общем `val` с явным вердиктом «основной / объяснимый бэкап»; LGTM Teamlead.
- **Задача B:** p95 latency yamnet-инференса измерен и записан; если превышает бюджет — есть follow-up; тесты плагина зелёные.
- **Задача C:** combined UC с alarm-loop зарегистрирован через `MembranaRegistry`, карточка видна в device-board-каталоге, fusion использует сырой confidence.
- **Задача D:** спека TDOA/localizer закоммичена с пометкой preserved/@stage 2; untracked-артефакты рабочего дерева очищены.
- **Задача E:** долг живых смоуков зафиксирован в `CURRENT_TASK`; подтверждено, что ночные ритуалы graceful при недоступных провайдерах.
- **Границы:** ни один новый код не нарушает граф зависимостей `ARCHITECTURE.md`/`SERVICES.md` (analyzer не зависит от analyzer; сервисы не зависят от `apps/client`; провайдеры инкапсулированы); проверяется на code-review.

---

## 🧭 Дрейф-якоря (read-only, DRIFT_2026-07-13.json)

Сводка: ok 8 · drift 0 · broken 0 — снимок 2026-07-13T05:16:22.454Z.

Все якоря в норме. Вердикты вынесены чистой `computeDrift`, не LLM.