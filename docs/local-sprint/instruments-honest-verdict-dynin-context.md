[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
[память:подсознание] emerge dynin-2026-07-19-insight-archive-lifecycle-c2-state-axes-r5 — прямой прецедент: там я формализовал ось как тип-сумму `None ⊕ positive ⊕ negative`, чтобы «нет assertion» не путалось с «отрицательным assertion». Ровно та же ошибка здесь: `auth` схлопывает `no_key` (нет предиката вызова) и `rejected` (отрицательный ответ провайдера) в одно значение.

[память:подсознание] emerge dynin-2026-07-22-insight-precedent-container — оттуда рабочая рамка «детектор рецидива как чистая функция + порог». Класс `rejected` без счётчика повторов на одном и том же ключе за окно T = слепой; нужен предикат `flaky(key, window)`, иначе транзиентный 403 попадёт в HANDOFF как факт.

Теперь по существу.

(а) **Нет, «rejected при живом ключе» — это не факт, а гипотеза с p<1.** Вещдок 05.08 её и опровергает: тот же ключ, 401/403 → 200 без вмешательства. Различить `rejected` и `transient` изнутри одного ответа нельзя — это твоё «ложная точность» верно. Предлагаю трёхзначную классификацию:

- `no_key` — предикат вычисляется ДО вызова, наблюдаем на 100%, честный класс.
- `auth_denied_unstable` (не `rejected`) — 401/403 при наличии ключа, класс **подозрения**, не факта. Пишется в HANDOFF только после подтверждения: N≥2 отказов за окно T с тем же ключом, без промежуточного 200. Иначе — `transient`.
- `transient` — 5xx, сеть, status 0, таймаут, **и одиночный 401/403 без подтверждения**.

То есть: 401/403 сам по себе → `transient` до подтверждения; подтверждённый повтор → `auth_denied_unstable`. Это устраняет ложный факт 05.08 без введения несуществующей точности.

(б) **Перерезка с ратификацией, честного пути внутри блока нет.** Замороженный enum в `llm-procedure-emit.mjs` — контракт эмиссии, а не деталь транспорта. Расширить его молча = сломать инвариант «эмиттер отвергает вне-enum». Два честных хода:

1. Блок #1549 поставляет только `classifyTransportError` с новым enum во **внутреннем** типе транспорта + adapter `toEmitClass(internal) → legacy` (пока мапит `no_key|auth_denied_unstable → auth`, `transient → protocol|unknown` по подпризнаку). Ничего в эмиттере не трогаем — вещдок 05.08 всё ещё пишется как `auth`, но уже с флагом `unconfirmed` в теле.
2. Отдельный микро-блок «ratify ERROR_CLASSES v2» со словом владельца — расширяет enum и снимает adapter.

Рекомендую путь 1 сейчас, путь 2 — следующим ходом. Согласуешь такой раскрой?
