[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
Контекст → контракт блока — словарная статья, а не «ещё один флажок в planPrepushTypecheck». Сужу целиком, по границам.

Вердикт: **pass**.

Что закрывает мои опасения (по терминам):
- **Слабая связанность держится.** `scopeUnderResolution` — чистый слой поверх; `planPrepushTypecheck` не знает про резолюцию. Зуб «резолюция, протёкшая внутрь → изменила бы результат» — правильная формулировка нормы, а не иллюстрация.
- **Один термин — один смысл.** `full`/`scoped`/`skip` не размываются: при FOREIGN понижение до `skip` с `downgradedFrom` — это именно понижение, а не «тихий full». Судья (`TYPES_JUDGE = "ci"`) назван, замер «2 из 37» втянут — причина не растворяется в тексте.
- **Отказ от ложной точности.** «Свежий-чужой ≡ протухший-чужой» — верно; различать их без замка на revisionAt было бы синонимией.
- **Границы приоритетов.** Docs-skip не переписывается; неизвестная резолюция ≠ повод скипать — обе нормы исключают «серый» проход.
- **Композит.** `planPrepushEffective` как отдельное имя — правильно: композит и базовый план не синонимы, и это видно по названию.
- **Литералы.** `DOWNGRADABLE_MODES`, `TYPES_JUDGE` вместо магических строк — снимает риск расхождения словаря дома.

Про замок: согласен, `revisionAt` — не тот ключ, что даётся судимому; `stale_partial` здесь — работа гейта, а не дефект блока.

Мелочь на будущее (не findings): в шапке словаря явно закрепить, что `downgradedFrom ∈ DOWNGRADABLE_MODES` — тогда инвариант читается по типу, без чтения кода.
→ акт [ozhegov]: облако показано, акт не совершён — ни всплытия, ни отказа
