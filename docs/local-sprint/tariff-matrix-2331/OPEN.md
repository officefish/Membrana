# Membrana Local Sprint OPEN: tariff-matrix-2331

| Поле | Значение |
|------|----------|
| Sprint | `tariff-matrix-2331` |
| Issue | [#2331](https://github.com/officefish/Membrana/issues/2331) · слово владельца 08.09 через ведущую: «Саму матрицу делает сессия Б… Все делаем через спринты. Не забываем подключать команду» |
| Вход | шторм `storm-tariff-single-truth-2026-09-08` (T1, T2/T10, T13–T16, вердикт о наборах); консилиум одним вопросом [`tariff-matrix-scalars-fate-2026-09-08`](../../seanses/tariff-matrix-scalars-fate-2026-09-08.md) (36 реплик) |
| Plan | [`docs/sprint/cut/tariff-matrix-2331.json`](../../sprint/cut/tariff-matrix-2331.json) · v1 ратифицирована владельцем 08.09 12:30 (через ведущую), перерезка b3 ратифицирована напрямую 08.09 19:54 («ратифицирую»); акты — [`trail/tariff-matrix-2331.jsonl`](../../sprint/cut/trail/tariff-matrix-2331.jsonl) |
| Cutter | vesnin ([прогон контекста](../../discussions/cut-tariff-matrix-2331-vesnin-run.md)) |
| Blocks | b1 matrix-passport-storage (ozhegov) → b2 matrix-granules-rights (ozhegov) → b3 matrix-template-release (vesnin) → b4 reseed-grid (dynin) → b5 sprint-closure (vesnin) |
| Status | gate pass 5/5 honest_pair · experience miss (точность 2/5) · PR — см. LOG |

## Предмет

Матрица тарифов — единственный источник правды в контейнере `docs/containers/strategic-docs/`:
гранулы **по ресурсу** с паспортом (версия, дата закрепления, владелец, ресурс, единица, домен,
правило пересева), шаблон `tariff-matrix`, релиз + корневой указатель `docs/TARIFF_MATRIX.md`;
**пересев как глагол** — `docs/tariffs/tariff-grid.json` генерируется ИЗ релиза, зуб «сетка
совпадает с релизом» (порча: правка сетки руками → красный).

## Границы

Только матрица и сетка как производная. Пересев базы и приборов, поведение при переходах,
холодное хранилище как режим — заседание `tariff-single-truth` и задание В. Публикация в Affine
заморожена (26.07) — не тронута. Скаляры `tariff-scalars.json` — по консилиуму: замороженная эпоха
сида, не источник правды; шапка supersede’ит формулу S0, числа не тронуты, сид читает их как прежде;
расхождение S↔R печатается зубом списком и не краснеет.

## Сверено в стволе до нарезки (08.09, `d0bcf8aa`)

| Факт | Состояние |
|---|---|
| `docs/tariffs/tariff-grid.json` (S1, 29.07) | рукописный канон: реестр 11 прав × 3 тарифа; буфер 1024 МиБ у всех, hot 512/2048/2048 — расходится с линейкой T1 |
| `docs/tariffs/tariff-scalars.json` (S0) | «единственный источник чисел» — читает сид кабинета; `null` у старших тарифов |
| Контейнер strategic-docs | движок `generate` есть; function-гранулы есть; **каркас шаблона движок игнорировал** (склейка через пустую строку) — найдено первой сборкой релиза |
| `docs/TARIFF_MATRIX.md` | рукописный черновик v0.5 (23.06) с тарифами `indie-v1`/`business-v1` — заменён указателем |
| Потребители сетки | `scripts/product-docs-tariffs.mjs` (`docs:product:tariffs`, красит на `scalarsCrossFindings`), `tariff-cutover`, кабинет `tariff-grid-source.ts` (режим сетки выключен по S9) |

## Итог блоков

- **b1 — паспорт и хранение** (`950f98a3`). Дизайн `TARIFF-MATRIX-DESIGN.md`; чистое ядро
  `scripts/lib/tariff-matrix/passport.mjs` (закрытые списки: 3 тарифа по rank, 4 домена, 5 родов,
  7 правил пересева, связка род↔правило); гранулы `tariff-matrix-purpose` (literal), `tariff-buffer`,
  `tariff-collections`, `tariff-cold` — `resource.json` (паспорт + значения по тарифам, у каждого
  значения `ratifiedAt`) + `render.mjs` (строка таблицы). Значения T1: 512/2048/4096 МиБ; холодное —
  0/2048/2048 не закреплено. 25 зубов, 16 порч, три зуба на живых файлах.
- **b2 — права** (`6a22bdf8`, словарная правка `4cb55a29`). `tariff-nodes` 1/4/9, `tariff-workspaces`
  3/3/3 (старшие — по аналогии с датчиком, не закреплено), `tariff-datasets` (заём набора блокпоста
  у наблюдательного пункта — словами, `ratifiedAt: null` у значения), `tariff-system-datasets`
  (matrix-only, вне квоты — T2/T10), `tariff-instruments` (составная, 5 прав).
- **b3 — шаблон, релиз, указатель, движок** (`e45dedfa`). Шаблон `tariff-matrix` (шапка и порядок
  колонок — в каркасе); релиз `releases/tariff-matrix/` — таблица 12 строк; `docs/TARIFF_MATRIX.md`
  — указатель; движок `strategic-docs-generate.mjs` переведён на `renderBySkeleton(template)`
  (перерезка, ратифицирована владельцем) с зубом регрессии 5/5: старые релизы — с точностью до пустых
  строк, порча «вернуть склейку» → красный.
- **b4 — пересев** (`e120b703`). `scripts/lib/tariff-matrix/reseed.mjs` (`projectGrid`, детерминизм),
  `yarn tariff:reseed [--dry-run|--check]`, зуб `yarn tariff:grid`: форма → **G↔R красный** → S↔R
  печать; `tariff-scalars.json` — только шапка (seed-epoch, `//supersededBy`); провода `package.json`,
  `workshop.catalog.json`. 47 зубов. Живой пересев (`7e72825a`): сетка = релиз `tariff-matrix@0.1.0`,
  зуб зелёный, drift S↔R напечатан (буфер датчика 1024→512; null старших), продуктовые доки
  `apps/docs/product/tariffs.mdx` перегенерированы, `docs:product:tariffs:check` OK.

## Команда

Прогоны контекста: Веснин (резчик; b3), Ожегов (b1, b2), Дынин (b4) — до и после перерезок
(первые прогоны гейт дисквалифицировал актом перерезки, повторены по ратифицированной версии).
Ревью: Дынин b4 — pass; Ожегов b1/b2 — первый заход fail на посылках без файлов (персона не читала
дерево: «гранула без паспорта» по несуществующему пути, «правило не в списке» — в списке), одно
верное замечание по словарю («нижняя граница» при равных ячейках → «по аналогии») внесено; повторный
заход с приложенным предметом — pass. Веснин b3 — см. протокол ревью. Урок: ревью персоны без
приложенного предмета судит воображаемый файл — предмет прикладывать `--ticket-file`.

## Замер объёма против нарезки

[`SEGMENTS.json`](./SEGMENTS.json): b1 1092 (прогноз 260), b2 393 (240), b3 437 (260), b4 1523
(380), b5 — по факту. Прогноз промахнулся в 2–4 раза: не учтены зубы с порчами (passport.test 377
строк, reseed/grid-check тесты ~900) и `resource.json` с паспортами на каждое значение. Вне зон:
регенерированные релизы (пустые строки), `apps/docs/product/tariffs.mdx` (производная сетки),
протоколы персон и консилиума, ленты актов и следов.

## Что НЕ сделано

Пересев базы и приборов (сид читает скаляры как прежде); режим холодного хранилища; переходы
между тарифами; наполнение наборов; публикация в Affine. Наблюдательный пункт ездит на наборе
блокпоста до назначения владельцем.

## Гейт

- `yarn sprint:gate` — **5/5 `honest_pair`**, следов 10 (пара context_run + review_pass у каждого блока по ратифицированной версии плана; первые прогоны контекста дисквалифицированы актами перерезки и повторены; ревью b1/b2/b4 повторены после свежих прогонов — порядок канона context_run ≺ review_pass), находок 0, код 0; журнал прогона `docs/procedure-runs/trail/2026-09-08.jsonl` закрыт инструментом.
- `yarn sprint:experience` — род `vesnin-tariff-matrix-2331-cut-1`, исход **miss**: точность нарезки 40% (2/5), переполнение 3/5 (b1 1092 против 260, b3 437 против 260, b4 1523 против 380): не учтены зубы с порчами и паспорта на каждое значение. Запись — `docs/sprint/experience/forecast-records.jsonl`, архив персоны Веснина. Урок в следующую нарезку: оценка блока = код + зубы (×2–3 к коду) + данные с паспортами.
- Ревью команды перед мерджем — `yarn code-review:pr` по PR; консилиум одним вопросом — по скалярам (см. «Вход»).
