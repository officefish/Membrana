# Membrana Local Sprint OPEN: s-queue-tail-2026-08-10

| Поле | Значение |
|------|----------|
| Sprint | `s-queue-tail-2026-08-10` |
| Procedure | `membrana-local-sprint` |
| Registry card | `s-queue-tail-2026-08-10` (без Issue: S-очередь дня, след — реестр + прогон) |
| Plan | [`docs/sprint/cut/s-queue-tail-2026-08-10.json`](../../sprint/cut/s-queue-tail-2026-08-10.json) (v4; ратификации v1 12:22Z · v2 12:39Z · v3 12:51Z · v4 13:22Z) |
| Prompt | [`S_QUEUE_TAIL_2026_08_10_PROMPT.md`](../../prompts/S_QUEUE_TAIL_2026_08_10_PROMPT.md) |
| Cutter | vesnin ([конспект](../../discussions/cut-s-queue-tail-vesnin.md)) |
| Blocks | b1-typecheck-verdict (ozhegov) · b2-adr-forecast-amnesty (vesnin) · b3-forecast-gate (dynin) |
| Status | **gate pass** (3/3 honest_pair, 0 остановок) · прогноз↔исход записан (`miss`: b3 406 против порога 400) |

## Зачем

Хвост очереди S десятки хендофа 09.08 (слово владельца 10.08: «делаем s-ки по очереди
шотами или спринтами»). Две строки закрылись шотами (`e387e2ba` вердикты детекторов,
`1ada87e4` проба резака); третий шот получил честный отказ предиката —
`capability_chaining` — и по прецеденту 04.08 остаток поехал спринтом (штамп tarasov).

## Итоги блоков

- **b1** — долг typecheck из ревью 08.08 судим вещдоком
  [`docs/audit/typecheck-verdict-2026-08-10.md`](../../audit/typecheck-verdict-2026-08-10.md):
  cabinet — долг закрыт (локально + CI); office — закрыт условно по CI-наблюдению
  `273f936d`, локальная краснота — дефект среды класса #1647, карточка
  `fix-node-modules-links-1647` (S, lead dynin) с заполненным промптом.
- **b2** — [`ADR-0026`](../../adr/ADR-0026-forecast-required-amnesty-by-schema.md)
  (ACCEPTED, LGTM владельца 10.08): amnesty-by-schema — `procedure-run-journal@2`, `forecastRequired`
  только у open и ставит держатель прогона, `@1` амнистирован по построению, гейт читает.
- **b3** — гейт обязательности записи «предсказание ↔ исход»: журнал принимает `{@1,@2}`,
  `ensureSprintRunOpen` ставит флаг, новый провод `forecast-record-gate` (закрытое
  множество причин), стоп в `closeSprintRunFromReport` + exit `max(reportExit,1)`.
  Попутно вычинен дефект закрытия: грубый «уже закрыт» по всей истории делал переоткрытый
  прогон (ADR-0022) незакрываемым — теперь порядковый матчинг, как в журнале. Зубы:
  +8 в `execution-gate.test.mjs`, +2 в `sprint-producer.test.mjs`, фикстуры соседа выучили
  контракт. Долг мостика `forecast-record-step-optional` — **repaid** (fact_ref).

## Прогон прожил свой же гейт

Переоткрытие после ложно-красного закрытия записано как `@2` с `forecastRequired=true`:
финальное закрытие `pass` прошло **через новый гейт** — запись прогноза существовала до
close. Исход записан честно: `miss` — b3 набрал 406 строк против порога 400 (оценки:
b1 100→144, b2 70→79, b3 260→406; рост b3 — две перерезки из-за дыр нарезки v1).

## Шероховатости — все названы, ни одна не молчит

1. **Окно назначено наперёд** (v1: from 13:50 при первом следе 12:15) — гейт дал
   `plan_lied` по всем блокам и закрыл прогон fail; вылечено перерезкой v4 и
   переоткрытием по канону ловушек (порядком строк). Тот самый класс `eg-window-after-first-trace`.
2. **Отметки из головы — трижды**: review-след b3 (13:58 будущего), штампы актов
   cut/recut в ленте актов (13:20/13:55/14:25 при фактах 12:38/12:50/13:21). Все
   поправлены по фактам ратификаций до коммита — прецедент хендофа «правил по date -u»
   воспроизведён буквально.
3. **Дыры нарезки v1** ловились исполнением: сеттер флага (v2, `sprint-run.mjs`) и зубы
   соседа (v3, `sprint-producer.test.mjs`) добавлены в зону перерезками с ратификацией.
4. **Сверх зоны**: две строки паттернов в `scripts/tasks-decompose.config.json` —
   механическое следствие `task:register` (CI-зуб полноты декомпозиции).
5. **Отступление от буквы Дынина**: связка прогноз↔план по множеству `cutBlockId`, не по
   `planDigest` (носитель прогнозов digest не несёт; правка `forecast-record.mjs` вне
   зоны). Кандидаты следующего захода: поле `planDigest` у записей рода; зуб
   инъективности `cutBlockId` в пределах окна плана (оговорка Дынина).
6. **4 красных `test:scripts`** — symlink EPERM среды Windows (worktree-resolution-пробы);
   воспроизводятся без диффа спринта (3882/3886 до, 3889/3893 после — все новые зубы зелёные).
