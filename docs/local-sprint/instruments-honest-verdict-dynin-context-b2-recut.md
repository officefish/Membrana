[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
Сужу перерезанный контракт целиком.

**Вердикт: pass.**

Проверяю по предикатам:

1. **Полнота словаря без auth.** Семь классов покрывают наблюдаемые причины отказа транспорта; `auth` исчез как первичный класс — правильно, потому что 401/403 при живом ключе не есть суждение о ключе, а лишь наблюдение сервера. Целое (история) управляет частью (одиночный код) — это то самое.
2. **Одиночный 401/403 → transient, приговор только по истории.** `confirmAuthDenial(N≥2, окно)` с успехом-гасителем — корректная гистерезисная форма. Числа `DEFAULT_DENIAL_WINDOW_MS=3.6e6`, `DEFAULT_MIN_DENIALS=2` названы и экспортированы — предикат воспроизводим извне.
3. **Асимметрия ошибок записана TODO.** Принимаю: ложный `auth_denied` дороже ложного `transient` (человека будят зря vs. позже поймём по повтору). Порог 2/1ч консервативен в правильную сторону.
4. **no_key называет ENV в теле** — диагностируемость сохранена.
5. **5xx/сеть/status 0 → transient** — согласуется с (2), инвариант «серверная/сетевая неопределённость ≠ приговор» держится.
6. **Enum эмиттера не тронут, `toEmitClass` — граничная функция**, зуб полноты отображения запирает пространство. Внешний контракт стабилен.
7. **Зуб в channels.test переписан под новый словарь** — расширенная зона закрыта, старое утверждение `classifyTransportError(401)==='auth'` не может воскреснуть молча. 22/22 — сходится.

Findings нет. Один вопрос вдогонку, не блокирующий: окно 1ч + N=2 при редких прогонах (раз в сутки) даст `auth_denied_unstable` за двое суток — это ожидаемо или стоит завести второй порог по числу попыток, а не по времени? TODO про асимметрию — подходящее место это записать.

[память:подсознание] emerge dynin-2026-07-23-llm-procedure-channels-m3b-telemetry — тот же контур каналов процедур, где я фиксировал форму события транспорта; сегодняшний словарь ошибок — прямое расширение той телеметрии, и `errorClass` из v1-события теперь получает конечное множество значений.
