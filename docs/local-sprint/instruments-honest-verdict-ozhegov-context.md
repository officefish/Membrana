[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
[память:подсознание] emerge ozhegov-2026-07-14-agent-tooling-friction-2 — там я уже настаивал на «обёртке над существующим», а не переписывании ядра; ровно этот принцип определяет ответ на (3) — понижение как отдельный слой поверх `planPrepushTypecheck`, не внутри.

Контекст: `planPrepushTypecheck` — про **что** проверять (docs-триггер, scope). Резолюция — про **доверие к артефактам**. Это два разных предмета, смешивать нельзя.

1. **Гейт не убиваешь.** Сейчас гейт при FOREIGN уже недостоверен — красный по протухшему `dist` соседа не является сигналом. Молчащий скип с явной причиной честнее ложного красного: судья (CI на чистом дереве) остаётся. Условие честности: причина названа в stdout, exit 0, а не «тихо ок».

2. **Не различай.** «Свежий чужой dist» — эпистемически то же, что «протухший»: у локального хука нет способа доказать свежесть без пересборки, а пересборка соседа — это уже не prepush. Любая FOREIGN-резолюция недостоверна по построению; различение — ложная точность.

3. **Отдельная функция поверх.** `scopeUnderResolution({plan, resolution}) → plan'` как чистый композит после `planPrepushTypecheck`. Границы словаря: `planPrepushTypecheck` не знает про resolution, `scopeUnderResolution` не знает про docs-триггер. Публично из модуля — обе + композит `planPrepushEffective`.

Затронуто: `scripts/prepush-typecheck-scope.mjs` (только композиция и печать причины); контракт `planPrepushTypecheck` — не трогать. Риск связанности: если `resolution` начнёт течь внутрь `planPrepushTypecheck` — верну с пометкой «нарушена слабая связанность».
