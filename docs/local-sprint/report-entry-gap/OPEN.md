# Membrana Local Sprint OPEN/CLOSURE: report-entry-gap

| Поле | Значение |
|------|----------|
| Sprint | `report-entry-gap` (S, фаза #1961, продолжение #2039) |
| Cut plan | [`report-entry-gap.json`](../../sprint/cut/report-entry-gap.json) · ратифицирован владельцем 21.08; перерезка окна ×1 (ниже) |
| PR | [#2056](https://github.com/officefish/Membrana/pull/2056) r1 · `564b4719` MERGED · вещдок r2 — отдельным PR |
| Гейт | **2/2 honest_pair**, журнал pass |
| Опыт | r1 260→176, r2 140→323 |

## Повод — факт боевого пути

После пересборки образа (сессия Г, ствол с #2041) плагин оказался зарегистрирован, но вход
ответил **501 «прогон не определён»**: `requestRun` умел ровно один плагин — mfcc. Свод был
смонтирован в доме, а вход к нему не вёл. **Лаборатория этого видеть не могла** — там
исполнитель звался напрямую. Пробел нашёл боевой путь за две минуты.

## Что сделано

- **r1 (Ожегов):** повод свода → `collections.collection_created` (тот же закрытый словарь,
  `sampleId` не требует — свод идёт по окну); отпечатки рода `report` — свои (`configHash` от
  пресета свода, `inputHash` по пробам окна, не всей коллекции); ветвление входа — **словарь**
  «pluginId → сборщик контекста» вместо лестницы `if`. Окно проведено насквозь: `RunRequest`,
  DTO, контроллер.
- **r2 (Курёхин):** боевой прогон часа и правка вещдока.

## Результат прогона

`runId 01a024dc-dfca-7ee9-bf31-a047e1f1f798` · HTTP 201 за 11.96 с · `bridge sent` · 666 треков
в окне из 1040 · 810 событий · 20 опорных и 20 негативных, недобор ноль · `stale: false`.
**Первый `RunRecord` рода `report` в доме результатов.**

**Боевой отбор совпал с лабораторным полностью: 20/20 и 20/20**, событие в событие, при разном
окне и разном пути исполнения. Прибор детерминирован, лабораторный вещдок был честным.

## Названное честно

- Окно исправлено по слову владельца: старт `09:45:18`; прежний разрыв 199 с — **не дыра**, а
  пауза между пробой после установки и стартом сеанса. Осталась одна дыра, 118 с (перезапуск).
- **Перерезка окна плана — третий раз за день по одной причине:** окно писалось раньше, чем я
  смотрел на часы (назначил 17:30 МСК, работа шла с 17:05). Следы под окно не подгонялись ни
  разу — гейт прямо говорит «перерезать ОКНО, не следы».
- Прогноз r2 промахнулся вдвое (140→323): правка вещдока с двумя новыми разделами и заменой
  путей весит больше, чем «поправить строку».
