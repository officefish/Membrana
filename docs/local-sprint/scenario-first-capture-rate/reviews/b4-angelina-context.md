# b4 context — angelina

The sprint remains non-deploying by owner constraint: ship to PR/main is allowed, production deploy is not. Verification must name both the code-level guard and the local environment gap, because the worktree lacks dependency installation and cannot resolve workspace packages for local Vitest/tsc.
