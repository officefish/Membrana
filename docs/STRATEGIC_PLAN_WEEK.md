<!-- Сгенерировано: 2026-05-17T09:56:45.423Z (yarn plan:week) -->
<!-- Период: последние 7 дней (since="7 days ago"); горизонт: следующая неделя -->
<!-- Источник цели: WHITE_PAPER.md -->
<!-- Epic-candidate (ручная отметка 2026-06-25): insight-vesnin-adopted-epic-bridge → реализация 2026-06-26 -->
<!-- Пересмотр 2026-06-26: консилиум neural-detectors-strategy — магистраль DB3H-S1..S4, нейро deferred -->

# План на следующая неделя (2026-05-17…2026-05-23)

## Пересмотр 2026-06-26 (консилиум + Teamlead)

| Приоритет | Epic | Статус |
|-----------|------|--------|
| **1** | `db3h-s1-tech-debt` | активен — **сейчас** |
| **2** | `db3h-s2-cabinet-host` | после S1 |
| **3** | `db3h-s3-studio-host` | после S2 |
| **4** | `db3h-s4-microphone-detectors` | после S3 |
| **5** | `neural-tier-1b-contract` | **deferred** |
| **6** | `neural-free-tier-dataset-report` | неделя 2+ |

**Фокус недели:** UserCase async-v2 на cabinet + Membrana Studio + Device Board.  
**Протокол:** [`seanses/neural-detectors-strategy-2026-06-26.md`](./seanses/neural-detectors-strategy-2026-06-26.md) · **канон дня:** [`MAIN_DAY_ISSUE.md`](./MAIN_DAY_ISSUE.md)

---

## Epic-candidate (инсайт, LGTM 2026-06-25)

| Поле | Значение |
|------|----------|
| **ID** | `insight-vesnin-adopted-epic-bridge` |
| **Тема** | Мост adopted insight → week epic (LGTM gate) |
| **Weight** | 7.0 |
| **Реализация** | 2026-06-26 (day sprint) |
| **Артефакт** | [`insights/insight-vesnin-adopted-epic-bridge/INSIGHT.md`](./insights/insight-vesnin-adopted-epic-bridge/INSIGHT.md) |

**Scope MVP:** § bridge в `INSIGHT_REGULATION.md`, hook в `plan:week`, шаблон epic-candidate в OPEN/active docs. `yarn task:register` — после merge регламента.

---

**Дата плана:** 2026-05-17  
**Привязка к стратегии:** WHITE_PAPER.md §8 (Дорожная карта) — **Stage-gate 1→2: Single-Node Detection First**  
**Статус:** Основано на git-истории последних 7 дней и текущем снимке репо.

---

## 📍 Текущее положение

### Завершено (последние 7 дней)

1. **Этап 1.A (DSP-эшелон) — осовременён**
   - `@membrana/harmonic-detector-service` реализована с полной математикой (гармоники, классификатор).
   - Демо на Vite (порт 5178) с live-детекцией, UI-плагин `harmonic-detector-viz` в клиенте.
   - Чувствительный слайдер, спектральные признаки, smoothing детекций.

2. **Benchmark-инфраструктура готова**
   - `yarn benchmark:detectors` — runner с автогенерацией отчёта `DETECTOR_BENCHMARK.md`.
   - Синтетический датасет v0.1: 9 WAV-файлов (дроны, шум, фон).
   - Таблица метрик (precision, recall, F1).

3. **Архитектурный документ актуализирован**
   - `docs/WHITE_PAPER.md` переопределена с **Single-Node Detection First** как условием stage-gate.
   - `docs/DETECTOR_BENCHMARK.md` + `docs/DATASET.md` созданы.
   - Все 6 детекторов (harmonic, cepstral, spectral-flux, yamnet, clap, agentic-claude) выставлены в scaffold.

4. **Инфраструктурные достижения**
   - `@membrana/telemetry-service` (журнал событий).
   - `packages/background-office/` (NestJS HTTP-шлюз к Claude/Linear/GitHub).
   - Улучшена регистрация модулей и lifecycle плагинов (plugin.install/teardown).

### ⚠️ Критическая точка: Stage-Gate 1→2

**Шлюз блокирует Этап 2 (TDOA, многоузловая синхронизация, локализация).**

Текущие метрики детектора (из коммитов):
- Harmonic v0.1 пробегался на синтетике, но **precision/recall не опубликованы** в DETECTOR_BENCHMARK.md.
- Целевые KPI шлюза: **precision ≥ 85%, recall ≥ 90%** (см. WHITE_PAPER.md §8).

**Статус шлюза:** ❌ **НЕ ПРОЙДЕН** — метрики не достигнуты или не измерены.

---

## 🎯 План на неделю (2026-05-17…2026-05-23)

### **Приоритет 1: Закрыть stage-gate 1→2** ✅

**Задача:** Прогнать harmonic-детектор на всех 9 синтетических WAV и обновить DETECTOR_BENCHMARK.md с реальными числами.

**Что делать:**
1. **Запустить benchmark harmonic-детектора**
   ```bash
   yarn workspace @membrana/harmonic-detector run test
   yarn benchmark:detectors
   ```
   → Обновить `data/detectors-benchmark/v0.1/reports/latest.json`.

2. **Проанализировать результаты**
   - Если precision ≥ 85% и recall ≥ 90% → шлюз **ПРОЙДЕН**.
   - Если нет → **быстрая итерация**: подстроить пороги классификатора в `classifySpectrum()` или порог энергии в `harmonicDetector.detect()`.

3. **Обновить DETECTOR_BENCHMARK.md**
   - Заполнить таблицу итоговых метрик (harmonic v0.1).
   - Примечание: benchmark пока на одном детекторе (остальные в scaffold).
   - Подписать дату и версию dataset'а.

4. **Консилиум "Шлюз пройден"** (если условия выполнены)
   - Обновить `docs/seanses/single-node-detection-first-2026-05-16.md` с датой и решением.
   - Раздвинуть этап 2 (TDOA) — **разморозить** `@membrana/tdoa-service` из scaffold.

**Ответственность:** Роль **Математик** (harmonic-детектор) + **CI/Benchmark-runner** (infrastr).  
**DoD:** PR с обновлённым DETECTOR_BENCHMARK.md, числа >= целевые или обоснованный отвод шлюза.

---

### **Приоритет 2: Подготовить второго детектора (DSP-эшелон) к benchmark** 📊

**Выбор:** `@membrana/cepstral-detector-service` (цепстральный анализ) или `@membrana/spectral-flux-detector-service` (спектральный флюкс).

**Что делать:**
1. Реализовать базовую логику детектора (соответствовать контракту `DroneDetector` из `@membrana/detector-base`).
2. Unit-тесты на мок-буферах.
3. Включить в `yarn benchmark:detectors` на том же синтетическом датасете.
4. Сравнить с harmonic в таблице DETECTOR_BENCHMARK.

**Зачем:** К концу недели иметь 2 рабочих детектора для сравнения и выбора лучшего перед stage-gate финализацией.

**Ответственность:** Роль **Математик** (DSP-логика).  
**DoD:** Рабочая реализация + тесты + результаты в benchmark таблице.

---

### **Приоритет 3: Расширить синтетический датасет**

**Текущее состояние:** 9 файлов, всё на синтезе (нет реальных записей).

**Что добавить:**
1. **Варианты дронов**: мульти-ротор на 80 Гц, 120 Гц, 150 Гц, 180 Гц + самолётный тип (выше 200 Гц).
2. **Шумовой фон**: город (трафик), ветер, дождь, птицы.
3. **Каверы**: дрон на расстоянии (ослабленная амплитуда), дрон в тумане (добавлен белый шум).
4. **Размер**: минимум 30–50 файлов для надёжного бенчмарка.

**Инструмент:** Скрипт `scripts/generate-dataset-synthetics.mjs` уже есть. Расширить генератор WAV'ов с параметризацией.

**Ответственность:** Роль **Музыкант** (синтез звука) + **Структурщик** (организация данных).  
**DoD:** Скрипт генерирует N файлов; обновлён manifest.json; benchmark пересчитан на новом датасете.

---

### **Приоритет 4: Stage 2 подготовка (параллельно, разморозка)** 🔓

**Условие:** Только после stage-gate 1→2 ПРОЙДЕН.

**Что предпринять на неделе (если шлюз пройден):**
1. Разморозить `packages/services/tdoa/` из scaffold → live.
2. Выписать контракт `TdoaResult` в `@membrana/core` (type `SyncedTimestamp`, `TimeSyncProvider`).
3. Начать реализацию TDOA-сервиса (минимальный: GCC-PHAT на паре микрофонов).
4. Чек: синхронизация времени — NTP / GPS-PPS? Для dev достаточно NTP, но набросать архитектуру.

**Ответственность:** Роли **Математик** (TDOA-алгоритм), **Структурщик** (контракты), **Архитектор** (синхронизация).  
**DoD:** PR с live TDOA-сервисом и test-примером на двух узлах (эмулированных).

---

### **Приоритет 5: Клиент — микрофонный модуль stabilization** 🎤

**Текущее:** Модуль и плагины работают, но есть шероховатости:

1. **Микрофонный модуль** (`MicrophoneModule.tsx`)
   - ✅ Регистрация через `MembranaRegistry`.
   - ⚠️ Нужна проверка: reliably ли `LiveSampler` поднимает FFT-узел при монтировании?
   - ⚠️ Teardown при unmount модуля — протечки ли MediaStream?

2. **Harmonic-detector-viz плагин**
   - ✅ Визуализация работает.
   - ⚠️ Нужна проверка: graceful shutdown при отключении микрофона?

3. **Тестовое покрытие**
   - Юнит-тесты для `microphoneCaptureCoordinator` есть.
   - Нужна интеграционная проверка: плагин установился → детектор слушает → удаления плагина → очистка.

**Что делать:**
1. Пошаговый тест с отключением/включением модуля в UI.
2. Memory profiler: утечки при повторном открытии микрофона?
3. Patch: если обнаружены — fixes.

**Ответственность:** Роль **Разработчик клиента** (React/hooks).  
**DoD:** Нет console-ошибок, нет очевидных утечек памяти, модуль корректно переключается.

---

### **Приоритет 6: Документация и статус-отчёты** 📋

**Что обновить:**
1. `docs/CURRENT_TASK.md` — актуальный статус stage-gate 1→2.
2. `docs/DETECTOR_BENCHMARK.md` — свежие метрики.
3. `docs/STRATEGIC_PLAN_WEEK.md` — результаты плана предыдущей недели + итоги этой.
4. `docs/seanses/single-node-detection-first-*.md` — консилиум-отчёт (пройден/отложен шлюз).
5. GitHub Issue #47 (task registry) — обновить статус детекторов и stage-gate.

**Ответственность:** Роль **Структурщик** (документация).  
**DoD:** Все doc'ы актуальны, консилиум задокументирован.

---

## 📊 Таблица задач на неделю

| № | Приоритет | Задача | Роль | Статус | DoD |
|----|-----------|--------|------|--------|-----|
| 1 | 🔴 1 | Benchmark harmonic на датасете v0.1, gate decision | Математик + CI | TODO | Реальные метрики в DETECTOR_BENCHMARK.md |
| 2 | 🟠 2 | Реализовать 2-й детектор (cepstral или flux) + benchmark | Математик | TODO | Рабочий детектор + таблица сравнения |
| 3 | 🟠 3 | Расширить синтетический датасет (30–50 файлов) | Музыкант + Структурщик | TODO | Скрипт, manifest, новые benchmark'и |
| 4 | 🟡 4 | Разморозить Stage 2, контракты TDOA, начало реализации | Математик + Структурщик | BLOCKED (на gate) | Live TDOA-сервис, тесты |
| 5 | 🟡 5 | Stabilize микрофонный модуль и плагины | React-разработчик | TODO | Нет утечек, graceful shutdown |
| 6 | 🟡 6 | Обновить документацию и консилиум-отчёты | Структурщик | TODO | Актуальные doc'ы, решение gate |

---

## 🚀 Последовательность работы

**Понедельник–Среда (2026-05-17…2026-05-19):**
- **Задача 1** (Приоритет 1) — запуск benchmark harmonic.
- **Задача 5** (Приоритет 5) — стабилизация клиента параллельно.

**Среда–Пятница (2026-05-20…2026-05-22):**
- **Консилиум gate-решение** (среда утро).
  - Если gate пройден: разморозить Этап 2 (задача 4).
  - Если gate не пройден: итерация на harmonic или выбор другого детектора.
- **Задача 2** (2-й детектор) — параллельно идёт вся неделя.
- **Задача 3** (датасет) — по мере готовности.
- **Задача 6** (документация) — финальный день.

**Пятница–Суббота (2026-05-23):**
- Финальная сборка, PR review, merge основных веток.
- Архив daily-day артефактов.

---

## ⚠️ Риски и зависимости

| Риск | Вероятность | Смягчение |
|------|-------------|-----------|
| **Gate не пройден на первый раз** | ⚠️ Средняя | Harmonic требует тюнинга пороговых параметров. Запас времени на итерацию: вторая попытка → середина пятницы. |
| **Датасет слишком мал** | 🔴 Высокая | Bench на 9 файлах — статистически ненадёжно. Расширение до 30–50 **критично**. |
| **Сквозные юнит-тесты detectors неполны** | 🟠 Средняя | Каждый детектор обязан иметь test-fixtures (см. `@membrana/detector-base/src/test-fixtures.ts`). Контроль в PR-review. |
| **Клиент: утечки памяти LiveSampler** | 🟡 Низкая–средняя | Lifecycle плагина уже переработан (vesnin-ветка merged). Проверка — dev-tools + memory profiler. |
| **Stage 2 требует синхронизации времени, которой нет** | 🔴 Высокая | Если gate пройден, немедля набросать архитектуру NTP / GPS-PPS. На локальных эмулях можно обойтись часами ОС. |

---

## 📌 Связь со стратегией (WHITE_PAPER)

Этот план **полностью подчинён** §8 дорожной карты:

- **Single-Node Detection First** — главное правило недели.
- **Stage-gate 1→2** — решение на шлюзе определит, переходим ли на многоузловую архитектуру.
- **Metrics of Success** (WHITE_PAPER §11):
  - P95-точность цели — пока не актуально (single-node).
  - Задержка детекции — фокусируемся на latency harmonic-детектора (p95 < 100 ms).
  - Доля ложных тревог < 5% — **это целевое число для gate decision**.

---

## 📝 Заметки для ролей

### Математик
- Harmonic-детектор: пороги `HARMONIC_ENERGY_THRESHOLD`, `FUNDAMENTAL_FREQUENCY_RANGE` могут требовать тюнинга под датасет.
- Cepstral или Spectral-flux: начать с классического DSP без ML, затем (если понадобится) добавить нейросеть.

### Музыкант
- Синтез: используй `packages/services/detectors/harmonic/src/math/harmonics.ts` как эталон для частот дронов.
- Датасет: слой шума должен быть реалистичным — раннее внимание к SNR-ratio.

### Структурщик
- Контракты: `DetectionResult` из `detector-base` используется всеми детекторами одинаково.
- Документация: каждый PR детектора должен обновлять DETECTOR_BENCHMARK.md.
- Registry: новые детекторы автоматически регистрируются в `yarn benchmark:detectors`?

### React-разработчик (клиент)
- Проверить lifecycle: `plugin.install()` → `teardown()` вызывается корректно.
- Тесты: интеграционный e2e на модуль + плагин.

### Архитектор / Teamlead
- Gate-решение принимается по фактическим метрикам, не обещаниям.
- Если gate отложен — скорректировать STRATEGIC_PLAN_WEEK и объявить следующую итерацию.
- Stage 2 разморозка требует явного одобрения.

---

## ✅ Определение успеха недели

1. ✅ **DETECTOR_BENCHMARK.md обновлён** с реальными числами harmonic v0.1.
2. ✅ **Stage-gate 1→2 решение принято** (пройден или отложен с обоснованием).
3. ✅ **Второй детектор** реализован и добавлен в benchmark таблицу.
4. ✅ **Датасет расширен** минимум до 30 файлов.
5. ✅ **Микрофонный модуль клиента** протестирован на утечки и graceful shutdown.
6. ✅ **Документация актуальна**: CURRENT_TASK, DETECTOR_BENCHMARK, консилиум-отчёт.

---

**Дата составления:** 2026-05-17  
**Автор плана:** Стратегический планировщик (Cursor Reasoning)  
**Следующий пересмотр:** 2026-05-23 (Friday EOD)