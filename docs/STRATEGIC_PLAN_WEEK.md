<!-- Сгенерировано: 2026-08-24T07:14:18.790Z (yarn plan:week@bd7f8e8d) -->
<!-- Период: последние 7 дней (since="7 days ago"); горизонт: следующая неделя -->
<!-- Источник цели: WHITE_PAPER.md -->

# План на следующую неделю (24–31 августа 2026)

## Стратегическая цель (из WHITE_PAPER.md)

> Membrana отвечает на вопрос: **«Кто сейчас в небе над этим квадратом и куда он летит?»**

**Текущая стадия:**
- **Гейт Stage-gate 1→2:** мягкий гейт пройден (trends-детектор 95% recall / 30% FPR на fv1-корпусе). **Hard-gate 85/90** проверяется на независимом пилотном корпусе (30–35 сэмплов) — эпик `vdr-hard-gate`.
- **Продуктовая магистраль (форсайт 2026-07-06):** финализация FREE-тарифа до ~17.07 → S2 combined UC → S3 упаковка UserCases → S4 студия к скачиванию → S5 лендинг.

> ⚠ **Снимок магистрали устарел (49 дн.):** необходимо проверить актуальный статус перед планированием.

---

## Ключевые факты из git-истории за неделю

### Выполнено (24–30 августа):
1. **Полевые испытания:** ночное дежурство 23.08 (1136 проб, 99.5 мин, 525 МБ) → найден квадратичный затор в `fetchRemoteItemsFromUnifiedApi()` (каждый запрос перечитывает весь журнал) → **документирован антипаттерн**.
2. **Фикс `chart-list`:** порог отсева `minDistanceRatio=0.05` оставлен словом владельца; вытесненные приходят **адресами** (можно проверить слухом); пик стал **абсолютным dBFS**, а не повтором превышения; подписи перестали врать о материале; нумерация с единицы.
3. **Кабинет:** сайдбар прилипает, виджеты не залезают под него; одна полоса прокрутки на страницу; галочка = показ (включённость); сворачивание стало самостоятельным.
4. **Инфраструктура:** append-only журналы под союзным слиянием (union в `.gitattributes` + сторож `verify-journal-merge.mjs`); фикс утечки токена в deploy-media; предфильтр выкатки ограничен контекстом сборки.
5. **Ритуалы:** артефакты утра/вечера, ласточки, память персон извлекается.

### Документированные открытия:
- **Инсайт:** спектральный анализ под задачу дрона — «отличает станок от аппарата в воздухе не тембр, а **неподвижность**»; предложены порядковый анализ (спектр по углу поворота вала), спектр огибающей, постоянная добротность.
- **Пост «двор, ель»:** три собственных источника поста — зонт-навес как отражатель, растяжки к ветвям, растущий хмель (фон «плывёт» между калибровками).

---

## Фокус недели: VDR-hard-gate + подготовка к S5 (лендинг)

### Приоритет A — VDR-hard-gate (стратегический шлюз)

**Что:** проверить hard-gate 85/90 на независимом пилотном корпусе (30–35 сэмплов, intra-rater ≥95%).

**Задачи:**
1. **Собрать пилотный корпус** (30–35 сэмплов) с размеченными labels (VDR). Сэмплы должны покрывать: дрон реальный (со слов владельца), не-дрон (фон, пост «двор, ель»), пограничные случаи из ночного дежурства.
2. **Прогнать trends-детектор (DRONE_TIGHT)** на корпусе; зафиксировать recall/FPR.
3. **Сравнить с yamnet** одной таблицей на val — определить, кто основной в hard-gate, кто объяснимый бэкап. *(поддерживающая задача, не блокирует)*
4. **Задокументировать результат** в `DETECTOR_BENCHMARK.md` (обновить таблицу).

**Критерий успеха:** recall ≥ 90% И precision ≥ 85% на пилотном корпусе.

**Если провал:** запустить R&D-эпик нейросетевого подхода (эшелон 1.B) — `yamnet` уже в prod-бенчмарке (F1 0.803), может стать основным.

> ⚠ **Не пересматривать вердикты FFT-стека** без нового датасета/алгоритма. DSP-детекторы — только объяснимость в журнале, не селекторы.

---

### Приоритет B — Продуктовая магистраль (параллельно)

**Что:** финализация FREE-тарифа → подготовка к лендингу (S5).

**Задачи:**
1. **S2 combined UC:** fusion спектр+нейро (сырой confidence yamnet + trends) + alarm-loop «ближе/дальше» по громкости. **НЕ предлагать** «улучшение DSP-детекторов» как магистраль.
2. **S3 упаковка UserCases** (3+1 в device-board) — проверить актуальную нарезку (снимок устарел).
3. **S4 студия к скачиванию** — приоритет владельца, уточнить объём.
4. **Подготовка к S5 (лендинг):** собрать материалы из полевых документов (ночное дежурство, пост «двор, ель») для реальных сценариев демо.

---

### Приоритет C — Известные долги и риски

**Безопасность:**
- 🔴 **API_INTERNAL_TOKEN скомпрометирован** (утечка 23.08). **Требуется замена токена владельцем** — кодом не лечится. Проверить, что новый токен не попадёт в логи (фикс `_ssh-media-deploy.mjs` уже в стволе).

**Полевая инфраструктура:**
- **Пост «двор, ель»:** провести дешёвую пробу — две записи (с зонтом и без) в сухую погоду, чтобы отделить конструкцию от события. Это влияет на достоверность калибровок.
- **Квадратичный затор журнала:** зафиксирован как антипаттерн №1; полное исправление (пагинация на сервере) — отдельный эпик, **не в этой неделе** (не блокер, база отпускает себя сама).

**Инсайты для следующей недели:**
- Спектральный анализ (инсайт 24.08): проверить гипотезу «неподвижность ≠ аппарат в воздухе» — возможно, добавить признак движения в трекинг (дальняя перспектива, не блокирует gate).

---

## Разведывательные шаги (если картина не ясна)

1. **Проверить актуальную магистраль:** снимок форсайта устарел (49 дн.). Прочитать `docs/seanses/foresight-2026-07-06.md` и `docs/tasks/registry.json` — что уже сделано из S2/S3/S4/S5.
2. **Проверить статус VDR-эпика:** есть ли уже собранный пилотный корпус? Если да — начать прогон сразу; если нет — собрать.
3. **Проверить hard-gate статус в `docs/insights/registry.json`** — возможно, эпик уже завершён или заморожен.

---

## Что НЕ делать на этой неделе

- ❌ **Не переизобретать yamnet**: детектор, плагин, бенчмарк реализованы (#266/#268).
- ❌ **Не тюнинговать пороги DSP** на free-v1 без новых данных — потолок зафиксирован.
- ❌ **Не запускать «unified benchmark трёх DSP»** — это поддерживающая задача, не магистраль.
- ❌ **Не двигать `minDistanceRatio`** — решение владельца зафиксировано.
- ❌ **Не пересматривать оценки фидбека** задним числом (урок 23.08).

---

## Итоговая сводка

| Приоритет | Задача | Критерий успеха | Статус |
|-----------|--------|-----------------|--------|
| **A** | VDR-hard-gate: прогон trends + yamnet на пилотном корпусе | recall ≥ 90%, precision ≥ 85% | В работе/начать |
| **B** | S2 combined UC (fusion спектр+нейро) | Демо: alarm-loop по громкости | Проверить статус |
| **B** | S3 упаковка UserCases | 3+1 в device-board | Проверить статус |
| **B** | S4 студия к скачиванию | Готовый артефакт | Проверить статус |
| **B** | Подготовка к S5 (лендинг) | Материалы из полевых документов | Начать |
| **C** | Замена скомпрометированного токена | Новый токен, нет утечек в логах | 🔴 Действие владельца |
| **C** | Проба поста «двор, ель» (зонт/без зонта) | Две записи, сухая погода | Запланировать |
| **C** | Сравнение trends vs yamnet одной таблицей | Обновлённый `DETECTOR_BENCHMARK.md` | Параллельно |

---

## Открытые вопросы к Teamlead

1. Актуальный статус VDR-hard-gate (есть ли пилотный корпус?).
2. Приоритет между VDR-гейтом и продуктовой магистралью (S4 студия) — если придётся выбирать.
3. Подтверждение замены токена (утечка 23.08).
4. Нужно ли планировать отдельный эпик на исправление квадратичного затора журнала (или это отложенная техническая задолженность).