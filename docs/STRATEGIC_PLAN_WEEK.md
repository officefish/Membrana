<!-- Сгенерировано: 2026-07-06T16:47:26.111Z (yarn plan:week) -->
<!-- Период: последние 7 дней (since="7 days ago"); горизонт: следующая неделя -->
<!-- Источник цели: WHITE_PAPER.md -->

# План на следующую неделю — Membrana

> Период: неделя после 2026-07-06.
> Стратегическая привязка: **WHITE_PAPER §8 «Single-Node Detection First» + §6 (отображение на архитектуру)**, продуктовая магистраль форсайта 2026-07-06 (FREE-тариф S1→S5).
> Хранитель плана: Teamlead. Правки — через PR.

---

## 1. Где мы сейчас (по фактам из git и контекста)

Картина из коммитов **ясная** — прошедшая неделя разбивается на три чётких потока:

1. **Neural-разворот детекции (магистральный, WHITE_PAPER §8, Этап 1.B).**
   Эпик `neural-drone-plugin` (#266/#268) закрыт: `@membrana/yamnet-detector-service` (zero-shot YAMNet TF.js), клиентский плагин `neural-drone-analyzer`, калибровка порога в `benchmark:detectors`. Итог — **F1 0.803 (P 71.4 / R 91.7 / FPR 36.7 @0.01)** на free-v1 v0.2, лучший в таблице, обошёл `DRONE_TIGHT` (F1 0.771). Это де-факто снятие «Этап 2 заморожен» через free-tier канал.

2. **Контур внешних коммуникаций (comms-studio).** Два спринта закрыты полностью: `comms-contour-environment` (CC1–CC9, #254–264) и `comms-sandbox-docs-adaptation` (CD1–CD6, #265). Leaf-контур `apps/comms-studio` с leaf-zero инвариантом, канон Слой 1–3, агент живого чтения, первый выход v0.1. Зарегистрирован спринт `partner-tutorials` (PT0–PT3) — **работа ещё не начата**.

3. **Device-board / presence (Этап 7 инфраструктура).** `pl2b-node-heartbeat` (#263) — persist node heartbeat liveness; `device-board-journal-sidebar` (#269) — вкладка «Журнал» + drag-ресайз.

**Что важно для планирования:** утренний канон дня (`DRONE_TIGHT` A/B/C) **не был выполнен** — команда дважды за неделю дрейфовала от предписанного маршрута к comms/neural (team-evening-feedback 2026-07-05 и 2026-07-06). Teamlead рекомендует **легализовать нейро-разворот в каноне**, а не бороться с ним.

**Чего в коммитах НЕ видно (пробелы):** нет ни одного коммита по **S2 combined UC** (fusion спектр+нейро + alarm-loop), по **S3 упаковке UserCases в device-board**, по **S4 студии к скачиванию**, по **S5 лендингу**. То есть keystone-задача S1 (нейро) сделана, но следующие звенья критического пути форсайта ещё не тронуты.

---

## 2. Стратегический фокус недели

Опираясь на **приоритет форсайта над детекционной магистралью** (явно зафиксирован в контексте планирования) и на то, что S1 закрыт:

> **Магистраль недели: продвинуть критический путь FREE-тарифа S1→S2→S3, финализация до ~17.07.**
> S2 (combined UC) — следующий keystone; S4 (студия) идёт параллельно.

Это прямо служит WHITE_PAPER §4.5 (классификация) и §4.6 (ситуационный слой), собирая уже готовые кирпичи (спектр ✅, нейро ✅, библиотека ✅) в продуктовое целое.

---

## 3. Задачи недели (приоритизированные)

### P0 — S2: Combined UC (fusion спектр + нейро + alarm-loop)
**Почему:** прямой следующий шаг критического пути после закрытого S1; разблокирован нейро-детектором #266.
- **S2.1** — Combined UserCase: комбинирование сырого confidence YAMNet + trends-FFT в одном сценарии.
  ⚠️ **Обязательно по контексту:** использовать **сырой confidence YAMNet, НЕ бинарный вердикт** — профили ошибок DSP/нейро слабо коррелированы (DETECTOR_BENCHMARK, заметка ND3). Это архитектурное требование, не деталь.
- **S2.2** — Alarm-loop «ближе/дальше» по громкости (грубый индикатор по RMS, как решено в форсайте — не точная локализация).
- **Границы:** fusion-логика не должна нарушать граф зависимостей (analyzer-сервисы не зависят друг от друга; комбинирование — на уровне клиента/UserCase, не внутри сервиса). Сверить с ARCHITECTURE §1a / SERVICES «граф зависимостей».
- **Артефакт:** рабочий combined UserCase в device-board + запись в журнал.

### P1 — S3: Упаковка UserCases в device-board (3+1)
**Почему:** следующее звено после S2; собирает 4 free-UC (спектр / нейро / библиотека / combined+alarm) в device-board.
- **S3.1** — Оформление 4 UserCase как сценариев device-board (используя `insight-agent-scenario-builder` weight 7.8 и `insight-slide-fullscreen-presentation` weight 7.8 как backlog-опоры, если применимо).
- **Зависимость:** блокируется завершением S2.

### P1 — S4: Студия к скачиванию (Electron) — параллельный трек
**Почему:** форсайт явно разрешил вести S4 **параллельно** критическому пути.
- **S4.1** — Разведка + каркас упаковки Electron Studio (офлайн-гарантия — YAMNet веса уже бандлятся, сеть в рантайме не нужна, подтверждено #266).
- ⚠️ **Разведочный шаг:** в коммитах нет следов текущего состояния Electron-сборки — **до старта S4 нужна ревизия**, что уже есть в `media-library-service` / `background-media` (см. WHITE_PAPER §6). Не начинать код без этой сверки.

### P2 — Партнёрские туториалы (comms), поддерживающий трек
**Почему:** спринт `partner-tutorials` зарегистрирован (PT0–PT3), но не начат; comms-контур готов принять работу. Не магистраль — фоновая полоса.
- **PT0→PT1** — шаблон туториала + чтение FACTS_SHEET. Дальше (PT2/PT3) — по остаточному приоритету.
- Инвариант: leaf-zero, запись только в `out/`, термины ⊆ GLOSSARY.

### P2 — Долг объяснимости детекции (поддерживающая, из контекста приоритетов)
**Почему:** контекст прямо предлагает как поддерживающую задачу, не магистраль.
- Одна таблица на val: **trends `DRONE_TIGHT` vs yamnet** — кто основной в hard-gate, кто объяснимый бэкап. Материал уже есть (оба в одном прогоне benchmark:detectors #268). Задача — свести и зафиксировать в `DETECTOR_BENCHMARK.md`.

---

## 4. Что НЕ делаем на этой неделе (явные анти-приоритеты)

Согласно жёстким вердиктам контекста планирования:
- ❌ «Довести Этап 1.A» / unified benchmark трёх DSP / stage-gate через harmonic/cepstral/flux.
- ❌ Повторный тюнинг порогов DSP на free-v1 без новых данных.
- ❌ Переизобретение yamnet-детектора/плагина/бенчмарка — **уже сделано** (#266/#268).
- ❌ TDOA / мультиузел / локализация — заморожены до hard-gate (WHITE_PAPER §8).
- ❌ VDR-железо как **блокер** выпуска free — по форсайту это валидация качества (~17.07), НЕ блокер.

---

## 5. Разведочные шаги (там, где картина неполная)

Коммиты не показывают состояния следующих областей — перед стартом соответствующих задач нужна короткая разведка:

1. **S4 (Electron Studio):** нет коммитов о текущем состоянии сборки — ревизовать `media-library-service` + `background-media` перед планированием кода.
2. **S2 fusion placement:** где физически живёт combined-логика — в клиенте (UserCase) или в новом analyzer? Граф зависимостей запрещает межсервисные импорты; нужно решение до реализации (совпадает с поддерживающей задачей контекста «где живут шаблоны: template-match vs background-media vs client»).
3. **VDR hard-gate (~17.07):** пилотный корпус (30–35 сэмплов, intra-rater ≥95%) — подготовка независима от free-выпуска, но её стоит запустить фоново к 17.07.

---

## 6. Критерий успеха недели

- **S2 combined UC работает** в device-board (спектр+нейро fusion на сыром confidence + alarm-loop по громкости) — **главный маркер**.
- **S3 начат** (хотя бы 2 из 4 UC оформлены как сценарии), если S2 закрыт вовремя.
- **S4 разведан** и есть каркас или явный отчёт «что мешает».
- Легализован нейро-разворот в утреннем каноне (рекомендация Teamlead из вечернего фидбэка 2026-07-06) — чтобы `yarn plan:day` перестал считать это дрейфом.

---

## 7. Замечание о процессе (из вечерних фидбэков)

Два дня подряд зафиксирован **дрейф от MAIN_DAY_ISSUE**: канон целил в DRONE_TIGHT/VDR, работа шла в comms и neural. При этом Teamlead оценивает дрейф как «движение по духу стратегии сильнее плана» (7.4/10). **Вывод для этого плана:** он выстроен вокруг того, куда команда реально движется (форсайт FREE-тариф + нейро), а не вокруг устаревшего DSP-канона — это должно снизить расхождение план↔факт.

---

*Документ основан только на предоставленном контексте (WHITE_PAPER, ARCHITECTURE/SERVICES выдержки, git-лог за 7 дней, приоритеты детекции, форсайт, активные инсайты). Факты вне контекста не домысливались; пробелы отмечены как разведочные шаги.*