<!-- канал: llm — протокол произведён yarn consilium, гейт структуры вердикта пройден -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-19T14:40:20.843Z |
| Команда | `yarn consilium` |
| Модель | claude-opus-4-8 |
| Файл | `docs/seanses/security-posture-m3-response-2026-07-19.md` |
| Порядок ролей | Teamlead → Структурщик → Верстальщик → Математик → Музыкант |
| Повестка | `docs/meeting/security-posture/M3-topic.md` |

**Вопрос:**

C1 — Единый регламент реагирования: (а) протокол входящих security-сообщений, (б) правило выдачи third-party доступов и каденс ревизии, (в) дисциплина секретов/.env поверх gitleaks-гейта; где живёт документ, владельцы, DoD? Вердикт обязан завершаться секцией «## Список посылок» (только входы).

---

# Консилиум: единый регламент реагирования на security-контакты (M3)

**Повестка:** C1 — единый регламент реагирования Membrana на внешние security-контакты и доступы: (а) протокол входящих security-сообщений, (б) правило выдачи third-party доступов и каденс ревизии, (в) дисциплина секретов/.env поверх gitleaks-гейта. Где живёт документ, кто владелец, DoD.

**Участники:** Teamlead (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Teamlead → Структурщик → Верстальщик → Математик → Музыкант

---

[Teamlead]: Открываю M3, вершину компоненты A. Ограда жёсткая: один вопрос — регламент реагирования, три секции одного документа, один вердикт. Не трогаем дозор (закрыт M1), апгрейд (M2), сам gitleaks-гейт (стоит листом — пишем норму поведения). Форма следует функции: документ должен быть настолько коротким, чтобы им реально пользовались в момент паники, когда пришло письмо вроде patchhog. Предлагаю каркас из трёх секций и одного владельца. Начнём с (а).

[Структурщик]: По размещению — термин должен быть однозначным. Документ живёт в `docs/security/INCIDENT_RESPONSE_REGULATION.md`, одна лемма, одна статья. Не плодить синонимы: у нас уже есть `DEPS_WATCH_REGULATION.md` (дозор) и baseline gitleaks — новый документ ссылается на них, но не переписывает. Секция (б) — про доступы GitHub Apps/OAuth/токены — это чистая топология «кто гейт», без кода. Я против размазывания правил по README пакетов: один входной файл, ссылки наружу.

[Верстальщик]: Даже регламент — это интерфейс. Протокол входящих должен читаться как чек-лист сверху вниз, в момент фиксации взгляда: 3–4 шага «проверь», блок «⛔ запрещено» красным акцентом (`--color-danger`), блок «куда фиксировать». Никаких абзацев-простыней. Инцидент patchhog показал: владелец перешёл по токен-ссылке за секунды — значит документ должен ловить именно этот рефлекс первой строкой «НЕ кликай, сначала сверь». Конструкция экрана регламента = порядок действий под стрессом.

[Математик]: Поддержу численный взгляд на протокол входящих. Инцидент patchhog проверился за минуты тремя дешёвыми операциями: сверка версии с `package.json`, декодирование токен-ссылки, локальный `yarn npm audit`. Стоимость проверки ≈ 3 минуты; стоимость доверия чужому сканеру — потенциально write-доступ к репо. Асимметрия абсурдная в пользу проверки. Правило (а) формализую так: **любое входящее security-сообщение считается недостоверным до сверки с локальными фактами репозитория**. «Совпало имя репо» — не доказательство, это как раз то, что подделывают.

[Музыкант]: Согласен, и добавлю по существу воронки. patchhog — это «магическая кнопка» в чистом виде: «Unlock finding → Sign in with GitHub → дай write». Это то, против чего я всегда: не бывает бесплатного отчёта, за которым просят доступ. Практический запрет в (а): **никаких «Sign in with GitHub» на чужих отчётах и никакой установки предлагаемого ими тулинга**. Свой аудит — своими инструментами (`yarn npm audit`, gitleaks), которые уже в репо. Чужой сканер в наш контур не заходит.

[Teamlead]: Фиксирую (а). Три шага проверки: (1) сверить заявленное с `package.json`/lock и реальным именем репо; (2) декодировать любые ссылки, не переходить по токен-трекинг URL; (3) локальный `yarn npm audit` + при подозрении gitleaks-скан. Запрещено: переход по токен-ссылкам, «Sign in with GitHub» на чужих находках, установка предлагаемого тулинга. Фиксация эпизода — куда? Ozhegov, где память?

[Структурщик]: Эпизод фиксируется в `docs/security/incidents/<дата>-<slug>.md` — короткая карточка: источник, что заявлено, что сверено, вердикт (ложь/правда), действия. patchhog-19.07 становится первой такой карточкой и эталоном формата. Это словарная статья инцидента: один инцидент — одна запись, без дублирования в чатах и issue. Ссылка на карточку — из основного регламента, чтобы был индекс.

[Верстальщик]: Тогда карточка инцидента тоже получает шаблон-заголовок с фиксированными полями — оператор заполняет по слотам, не сочиняет прозу. Поля: `источник`, `заявлено`, `проверено (чем)`, `вердикт`, `действия`, `адрес-статус` (в patchhog адрес помечен живым — это важное поле). Единый шаблон = сравнимость инцидентов и быстрый повторный разбор.

[Математик]: Перехожу к (б) — доступы. Здесь ключевое различие: репо-уровень API отдаёт (webhooks, deploy keys, коллабораторы, Actions-секреты — всё сверяемо машинно), аккаунт-уровень (settings/installations, settings/applications) API **не отдаёт** — только глазами владельца. Значит каденс двухслойный: репо-уровень — машинная проверка в дозоре/CI-ритме; аккаунт-уровень — ручная ревизия владельцем с фиксированной периодичностью. Предлагаю ручную ревизию раз в квартал и обязательно после каждого зафиксированного входящего-инцидента.

[Teamlead]: Гейт выдачи доступов — принципиально: **третьесторонний доступ (GitHub App / OAuth / токен с write) выдаёт только владелец, по умолчанию — нет**. Это дефолт-deny. Никакая роль команды, включая меня, не санкционирует установку App в обход владельца. Каденс ручной ревизии аккаунт-уровня — квартал + после инцидента, как сказал Dynin. Принимаю.

[Структурщик]: Уточню норму на репо-уровне, чтобы термины не расплылись. Что проверяем машинно по факту 19.07: webhooks (ожидание 0), deploy keys (0), список коллабораторов (1 ожидаемый), Actions-секреты (6 ожидаемых). Отклонение от ожидаемого набора = сигнал. Это не переделка gitleaks — это отдельный чек-лист «репо-инвентарь», который логически принадлежит регламенту доступов, а исполняется тем же дозорным ритмом. Границу с `DEPS_WATCH_REGULATION.md` держим ссылкой, не копией.

[Музыкант]: По третьесторонним App — добавлю живую практику. Часто App просят «read-only, безопасно», а потом скоуп расширяется молча. Правило: фиксировать не только «да/нет», но и **скоуп на момент выдачи** в карточке доступа, чтобы ручная квартальная ревизия сверяла «что просили» vs «что стоит сейчас». Без этой записи ревизия слепая.

[Верстальщик]: Тогда в регламенте нужна маленькая таблица «активные third-party доступы»: App/токен, скоуп, дата выдачи, кто санкционировал (всегда владелец), дата последней ревизии. Табличные данные, `tabular-nums` в духе наших экранов — но здесь это просто markdown-таблица, честные факты без украшений. Пустая таблица на старте — это тоже валидное состояние (0 сторонних App = хорошо).

[Математик]: Секция (в) — секреты и `.env`. Память проекта: инцидент office-token-in-main-worktree, `.env` живёт в каждом worktree отдельно и проверяется глазами. gitleaks стоит гейтом (pre-commit мягкий + CI, baseline из 3 фикстур, скан 1439 коммитов — живых секретов нет). Норма поверх гейта: **`.env` никогда не коммитится, проверяется per-worktree; при подозрении на утечку — немедленная ротация секрета, потом разбор**. Ротация первой, потому что стоимость промедления растёт монотонно.

[Teamlead]: Кто санкционирует добавление в gitleaks-baseline? Это точка, где машинный гейт встречается с человеческим решением — нельзя оставлять самотёком.

[Математик]: Добавление в baseline санкционирует владелец, по явной записи в PR: что за фикстура, почему это не живой секрет. Сейчас baseline = 3 фикстуры, это норма. Любое расширение — осознанное решение, а не «замолчать алерт». Молчаливое добавление в baseline = анти-паттерн, эквивалент swallow.

[Структурщик]: Согласуется с нормой swallow-гейта из входов: показать владельцу ДО действия, молчаливая автоматика запрещена. Значит и baseline, и отправка ответа на входящее security-письмо подчиняются одному принципу — **человек-в-контуре перед необратимым действием**. Это делает документ цельным: три секции, один сквозной принцип. Термин один — «санкция владельца перед необратимым».

[Музыкант]: Тогда прошу явно записать про исходящие ответы на такие письма: если вообще отвечаем patchhog-подобному отправителю — черновик показываем владельцу до отправки, как swallow-прецедент. Лучший ответ на холодное токен-письмо — обычно молчание плюс карточка инцидента. Не кормить воронку.

[Верстальщик]: Резюме по форме документа: секция (а) — чек-лист + красный блок запретов + ссылка на шаблон карточки; (б) — правило deny-by-default + таблица активных доступов + каденс; (в) — норма `.env`/worktree + процедура ротации + правило baseline. Один экран на секцию, читается под стрессом. a11y-аналог здесь — «не полагаться только на цвет»: запреты дублируются словом «ЗАПРЕЩЕНО», не только акцентом.

[Teamlead]: Владелец документа — я (Vesnin) как Teamlead по процессу; исполнитель-редактор регламента и карточек — Математик (Dynin) как ops/security вторичная компетенция. Ручную аккаунт-ревизию и санкцию доступов/baseline держит владелец проекта (человек). Каденс: репо-инвентарь — в дозорном ритме (машинно); аккаунт-уровень — квартал + после инцидента. Собираю вердикт.

[Структурщик]: Принимаю. Один документ `docs/security/INCIDENT_RESPONSE_REGULATION.md`, карточки в `docs/security/incidents/`, ссылки на `DEPS_WATCH_REGULATION.md` и gitleaks-baseline без дублирования. Границы чистые.

[Математик]: Принимаю. Асимметрия «дешёвая проверка vs дорогое доверие» зашита в (а); ротация-первой в (в); санкция владельца на baseline. Численно обосновано.

[Музыкант]: Принимаю. Чужой тулинг в контур не заходит, воронку не кормим, скоуп доступов фиксируем на момент выдачи.

[Верстальщик]: Принимаю. Регламент как чек-лист под стрессом, единый шаблон карточки инцидента, честная таблица доступов.

[Teamlead]: Принимаю и закрываю M3 — компонента A замкнута.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Где живёт документ | `docs/security/INCIDENT_RESPONSE_REGULATION.md` (один файл, 3 секции); карточки инцидентов — `docs/security/incidents/<дата>-<slug>.md` |
| (а) Протокол входящих | Презумпция недостоверности до сверки. Шаги: (1) сверить заявленное с `package.json`/lock и реальным именем репо; (2) декодировать ссылки, НЕ переходить по токен-трекинг URL; (3) локальный `yarn npm audit` (+ gitleaks при подозрении). ЗАПРЕЩЕНО: переход по токен-ссылкам, «Sign in with GitHub» на чужих отчётах, установка предлагаемого чужого тулинга. Фиксация — карточка инцидента (эталон: patchhog-19.07) |
| (б) Доступы third-party | **Deny-by-default.** GitHub App/OAuth/токен с write выдаёт ТОЛЬКО владелец. Таблица активных доступов (App/скоуп/дата/санкция/последняя ревизия). Репо-уровень (webhooks=0, deploy keys=0, коллабораторы=1, Actions-секреты=6) — машинная сверка в дозорном ритме; аккаунт-уровень (installations/applications) — ручная ревизия владельцем **квартал + после каждого инцидента** |
| (в) Секреты/.env | `.env` не коммитится, проверяется **per-worktree глазами** (память: office-token-in-main-worktree). При подозрении на утечку — **ротация первой**, потом разбор. gitleaks-гейт остаётся как есть; расширение baseline санкционирует владелец явной записью в PR (молчаливое добавление = swallow, запрещено) |
| Сквозной принцип | Человек-в-контуре перед необратимым: ответ на письмо, выдача доступа, правка baseline — санкция владельца ДО действия (норма swallow-гейта) |
| Владелец | Процесс/LGTM — Teamlead (Vesnin); редактор регламента и карточек — Математик (Dynin, ops/security); санкция доступов/baseline и аккаунт-ревизия — владелец проекта (человек) |

**Definition of Done:**
- Написан `docs/security/INCIDENT_RESPONSE_REGULATION.md` с тремя секциями (а/б/в) и сквозным принципом «санкция владельца перед необратимым».
- Заведён каталог `docs/security/incidents/` с шаблоном карточки и первой записью `patchhog-19.07` как эталоном.
- В регламенте: чек-лист входящих + блок «ЗАПРЕЩЕНО» (дублирование словом, не только акцентом); таблица активных third-party доступов (на старте пустая = 0 App); процедура ротации.
- Зафиксированы каденсы: репо-инвентарь — дозорный ритм (машинно); аккаунт-уровень — квартал + после инцидента.
- Названы владельцы (Teamlead — процесс, Dynin — редактор, владелец проекта — санкции).
- Ссылки на `DEPS_WATCH_REGULATION.md` и gitleaks-baseline проставлены без дублирования их содержимого.
- LGTM Teamlead (Vesnin); M3 помечено закрытым, компонента A замкнута.

---

## Список посылок

1. Инцидент patchhog 19.07: холодное письмо с выдуманной версией vite и чужим именем репозитория; токен-трекинг ссылка; воронка «Unlock finding → Sign in with GitHub» с запросом write-доступа; владелец перешёл по токен-ссылке (адрес помечен живым).
2. Проверка patchhog заняла минуты: сверка с `package.json`, декодирование ссылки, локальный `yarn npm audit` — чужой сканер не понадобился.
3. Листы 19.07 (исполнены), репо-уровень: webhooks 0, deploy keys 0, коллабораторов 1, Actions-секретов 6 (ожидаемые).
4. Аккаунт-уровень (settings/installations, settings/applications) API не отдаёт — только глазами владельца.
5. gitleaks-гейт стоит: pre-commit мягкий + CI, baseline из 3 фикстур; скан 1439 коммитов — живых секретов нет.
6. Память проекта: инцидент office-token-in-main-worktree — `.env` живёт в каждом worktree отдельно, проверяется глазами.
7. Норма swallow-гейта: показать черновик владельцу ДО отправки; молчаливая автоматика запрещена.
8. Аудит-вердикты M0/M1/M2 ратифицированы 19.07 (предшественники M3 закрыты); M3 замыкает компоненту A.
9. Действующий регламент дозора `DEPS_WATCH_REGULATION.md` (закрыт M1) и апгрейд-карточки sec-upgrade-* (закрыты M2) — вне вердикта M3.

---

*Реплик в диалоге: 25; каждый участник высказался не менее одного раза.*
