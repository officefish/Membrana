# Аудит сборки председателя — M3 «Регистрация в кабинете по промокоду панели», порядок гашения и режим отказа — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Предмет: сборка `docs/seanses/cabinet-registration-promo-m3-order-failure-assembly-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `76bd7f2f`) против протокола `…-m3-order-failure-2026-09-21.md` и аудита `…-m3-audit-2026-09-21.md` (PR #2382). Три проверки по заданию председателя: (а) цитаты дословны и у заявленных ролей на заявленных строках; (б) сборка не меняет ничего, кроме одного названного заголовка; (в) председатель ничего не решил сам. Ствол для сверки: `origin/main` = `1c5a0499`. Сборка, протокол и повестка не правились.

## Итог

**ЧИСТО** по всем трём проверкам, без уточнений к самоописанию: диапазоны строк, число пунктов готовности и пересказ порядка шагов в сборке совпадают с протоколом.

## Таблица

| # | Проверка | Вердикт | Цитата / команда |
|---|---|---|---|
| а | Цитаты дословны, роль и строка | PASS | Три цитаты (сборка стр. 14, 16) проверены скриптом: жирное снято, кавычки нормализованы, точная подстрока на **заявленной** строке протокола, роль — по ближайшему маркеру `[Роль]:` выше. Все три на заявленных строках и у заявленных ролей: [Teamlead] 56 «Фиксирую расширение границ: `auth.dto.ts` и `auth.module.ts`…»; [Структурщик] 109 «Подключение клиента: `auth.module.ts` импортирует `OfficeRegistrationModule`…»; [Teamlead] 99 «Утилиту sliding window — только если уже есть в cabinet packages; иначе тонкий private helper в модуле auth…». |
| б | Ничего не изменено, кроме одного заголовка | PASS | Сборка не воспроизводит ни одну таблицу и готовность: строк таблиц — 0, нумерованных пунктов — 0. Протокол на ветке побайтно равен копии аудита #2382 (`git show <ветка>:<протокол> \| diff -q - <копия>` → без различий). Единственная правка — заголовок стр. 220: протокол `## Definition of Done`, сборка «читать как „Определение готовности“» (стр. 10); пунктов под ним в протоколе семь (стр. 222–228), в сборке названо семь. Диапазоны сборки совпадают с протоколом: 177 `## Итоговое решение консилиума` … 195 `\| Что НЕ строить`; 197 `### Расширение границ сессии C (явно)` … 206 `\| тесты`; 208 `### Карта двери \`POST /v1/auth/register\`` … 216 `\| ok + create`. Пересказ порядка шагов (сборка стр. 22) сверен с протоколом стр. 181 и 187: «пустой или длиннее 128 → 403 без офиса», «логин занят → 403 без офиса» — существо то же. Замечания аудита к заголовкам строк 183/192/214/216 и опечатке 210 сборка не правит и говорит почему (стр. 10) — это допустимо: замечания не были провалом. Изменения ветки `d033d9d8` → `76bd7f2f`: сборка (+29) и одна строка статуса M3 в `MEETING_ACTIVE.md` (`git diff --stat` → 2 files, +30 −1). |
| в | Председатель ничего не решил сам | PASS | Сборка стр. 14: «Границы председателем не двигаются; расширение на `auth.dto.ts` и `auth.module.ts` — решение владельца при ратификации»; стр. 16 по ограничителю: «Председатель **предлагает** владельцу при ратификации включить этот файл в расширение C» — предложение названо предложением. Раздел «Сессии (ратифицированы четыре)» в `MEETING_ACTIVE.md` не менялся (`git diff … \| grep -c "Сессии (ратифицированы"` → 0); новая строка статуса M3: «расширение C (`auth.dto.ts`, `auth.module.ts`, помощник ограничителя) — решение владельца при ратификации. Ждёт аудита сборки». Факты сборки о стволе сходятся с аудитом: `createSlidingWindowLimiter` только в `panel-auth-core.ts:237`, в кабинете нет; `auth.controller.ts:21–23` без `@HttpCode` → 201. |

## Что аудитор не делает

Своих решений по контракту и по границам сессии C не предлагает; сборку, протокол и повестку не правит. Ратификация M3 и состав расширения C — слово владельца.
