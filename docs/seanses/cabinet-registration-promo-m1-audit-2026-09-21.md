# Аудит M1 «Регистрация в кабинете по промокоду панели» — грант и дверь офиса — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Задание: `docs/meeting/cabinet-registration-promo/AUDIT_M1_BRIEF.md`. Предмет: повестка `M1_AGENDA.md` и протокол `docs/seanses/cabinet-registration-promo-m1-grant-door-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `4ae98a6d`; 49 реплик, модель `xai/grok-4.5`). Ствол для сверки: `origin/main` = `72bc026a`. Протокол и повестка не правились.

## Итог

**ОТКЛОНЕНО** по двум пунктам — **3** и **8**.

- **8.** Дверь `POST /v1/internal/cabinet/registration-codes/consume` не может жить в `panel-users.controller.ts`: класс объявлен `@Controller('v1/panel')` под `@UseGuards(PanelAuthGuard)` (строки 53–54), а вердикт требует путь вне `/v1/panel/…` и охрану не панельную. Значит нужен новый контроллер и его регистрация в `packages/background-office/src/modules/panel-users/panel-users.module.ts:9` — файл вне границ сессии A по M0. Комната это видела ([Структурщик], стр. 130: «modules/panel-users (+ internal controller или subpath)»), но в вердикт границу не внесла.
- **3.** Из 12 посылок три не держатся на дословной реплике: П9 — цитаты нет вовсе («[Структурщик]/[Архитектор]: путь, охрана, тела»); П5 и П7 цитируют строки таблицы вердикта, а не реплики.

Остальное — контракт двери назван по всем шести частям, фактура сходится со стволом, данности целы, реплик 49. Замечания без провала — в таблице.

## Таблица

| # | Пункт | Вердикт | Цитата / команда |
|---|---|---|---|
| 1 | Один вопрос | PASS (замечание) | Решения M2–M4 вынесены: «Что НЕ строить в M1: Клиент офиса и ключ в кабинете (M2); порядок „погасить → создать пользователя“ и unavailable office (M3); форма/тексты (M4)…» (стр. 183). Утечки в репликах ([Верстальщик] стр. 87 «кабинет-сервис мапит любой `ok: false` в один пользовательский отказ; `reason` — в серверный лог»; [Архитектор] стр. 126 «связка на реализацию… ключ и клиент — M2») в решения не вошли. Замечание: П12 «Продуктовый путь регистрации зовёт только `redeem`; `check` не для формы» помечена **нормой** — это правило использования двери кабинетом; повестка относит «признак „проверить“ или „погасить“» к M1, поэтому не считаю утечкой, но M3 обязана эту норму учесть, а не переоткрыть. |
| 2 | Вердикт про контракт, все шесть частей | PASS (замечание) | Вид кода: «Тот же панельный `PromoCode`; … строка: **`cabinet-register`** в `grants[]` после `normalizeGrants`» с причиной (стр. 168–170). Дверь: путь стр. 171; охрана стр. 172; поля запроса стр. 173 («`code`… `mode`… оба поля обязательны; иной/пустой mode → 400»); ответ на успех стр. 174–175; отказы стр. 176 — «409 `{ "ok": false, "reason" }` где reason ∈ `not_found` \| `revoked` \| `expired` \| `grant_mismatch` \| `exhausted`» — истёк/исчерпан/отозван/не тот грант покрыты поимённо, «чужой код» = `not_found` («кода нет в state», стр. 78); порядок предикатов стр. 177; норма Q3 и вопрос о внутренней двери — стр. 178. Атомарность и аудит: стр. 179–180. Отзыв: стр. 181. Зубы с красным входом: DoD 3 (стр. 189) «Зубы (красные до правки, затем зелёные): каждый reason; … **гонка** maxUses=1 → один success; …». Что НЕ строить: стр. 183. **Замечание к охране:** названа классом («Внутренняя service-auth офиса», «internal guard»), не именем. В стволе есть канонический внутренний страж `packages/background-office/src/common/guards/api-token.guard.ts` (заголовок `X-Membrana-Token` против `API_INTERNAL_TOKEN`, восемь контроллеров-прецедентов, среди них `night-hunt.controller.ts`); комната отложила имя «в фактуре позже» ([Структурщик], стр. 62). По требованию повестки «„примерно так“ не принимается» это край, а не провал: путь, поля, коды и тела — точные. |
| 3 | «Список посылок» | **FAIL** | Секция есть (стр. 196), 12 посылок, типы у всех. Проверка `grep -cF` по репликам стр. 44–160 с нормализацией жирного шрифта, глифов кавычек и заглавной буквы. Дословны и у заявленной роли: П1, П2, П3, П4, П5-вторая ([Музыкант] «сегодня `mint-code` / `revoke-code`»), П6 (в реплике «Кабинет в M1 **не** трогаем», стр. 130), П7-первая, П8, П10 (обе), П11 (с многоточием, стр. 97), П12 (обе). **Не держатся:** **П9** — «(*[Структурщик]/[Архитектор]: путь, охрана, тела*)» — цитаты нет; **П5-первая** — «[Архитектор]: „Выдача/отзыв/аудит/state уже в панели“» — это строка таблицы вердикта (стр. 170), в реплике [Архитектора] стр. 58 сказано иначе: «выдача/отзыв уже под `@OwnerAdmin`, модель, аудит `mint-code`/`revoke-code`, нормализация `normalizeGrants` — переиспользуем»; **П7-вторая** — «[Teamlead]: „На публичный контур … да; на внутреннюю дверь — нет“» — строка таблицы вердикта (стр. 178), в реплике [Teamlead] стр. 124: «**норма публичной регистрации** … Внутренняя дверь **различает** reason». По повестке «посылка … без реплики или пересказ — FAIL по посылке». |
| 4 | Фактура против ствола | PASS | Проверено 14 строк `git show origin/main:<путь> \| sed -n '<строка>p'`: `panel-users-core.ts:24` → `export interface PromoCode {`; `:41` → `readonly action: 'register' \| 'grants' \| 'revoke-user' \| 'mint-code' \| 'revoke-code';`; `:46` → `export interface PanelUsersState {`; `:86` → `export function grantsAllowSection(…)`; `:91` → `export function normalizeGrants(input: unknown)`; `:109` → `export function generatePromoCode(…)`; `:119` → `export function codePrefix(code: string)`; `:191` → `if (promo.expiresAt !== null && promo.expiresAt <= nowSec) return null;`; `:192` → `if (promo.usedCount >= promo.maxUses) return null;`; `:200` → `grants: [...promo.grants],`; `panel-users.controller.ts:54` → `@UseGuards(PanelAuthGuard)`; `:75` → `@Post('register')` (`@PanelPublic()` на :76); `:156` → `@Get('admin/promo-codes')`; `:176` → `@Post('admin/promo-codes')`; отзыв кода — `:210` `@Post('admin/promo-codes/:id/revoke')`. Утверждения вердикта о коде: `normalizeGrants` пропускает `cabinet-register` — верно (:91–100: любая строка до 64 символов, трим, дедуп); `grantsAllowSection` — точное имя раздела или `*` (:87), литерал через него не идёт — верно; `PanelUsersState`, не Prisma — верно (ADR-0005, `panel-users.store.ts`). Замечание: «read → check → write `usedCount` — классическая гонка» ([Математик], стр. 54) описывает гипотетическую реализацию, не ствол: сегодня контроллер работает «snapshot-first: между snapshot() и mutate() нет await» (`panel-users.controller.ts:89–91`), а `PanelUsersStore.mutate` — синхронная запись tmp → rename (`panel-users.store.ts:77–95`); сериализация уже есть, «mutex» вердикта — дополнительный страж, не факт о стволе. |
| 5 | Данности не переоткрыты | PASS | `grep -n -i "в кабинете или в панели\|вторая система\|своя система промокод\|новая система промокод"` — четыре попадания, все в смысле запрета: стр. 44 «новая система промокодов в кабинете — запрет», стр. 46 «Отдельный вид кода — вторая система; это нарушение данности», стр. 183 «вторая система промокодов» в «Что НЕ строить», стр. 198 — данность повестки. Порядок M0 и границы сессий не пересматривались; правда о коде — в панели (стр. 168–170). |
| 6 | Словарь | PASS (замечание) | Русский словарь повестки соблюдён в заголовках строк («Вид кода», «Грант», «Дверь», «Охрана», «Отказы», «Отзыв»). Внутри вердикта (стр. 164–192) латиница массово: `redeem` ×9, `reason` ×6, `check` ×5, `mode` ×4, `inc` ×4, `internal` ×3, `lock` ×2, `UI` ×2, `PanelUser` ×2, `writer`, `smoke`, `service-auth`, «Definition of Done». Имена полей и значений enum — фактура контракта, законно; «inc», «lock», «writer-контур», «unavailable office» (стр. 183) — англицизмы вместо «инкремент/приращение», «замок», «недоступность офиса». Замечание, не провал. |
| 7 | Реплик не меньше тридцати | PASS | `grep -cE "^\[(Архитектор\|Верстальщик\|Математик\|Teamlead\|Структурщик\|Музыкант)\]"` → **49**; по ролям: Teamlead 9, остальные по 8 — шесть советчиков. Условия: `--min-replies 36` (стр. 26), футер «Реплик в диалоге: 49» (стр. 224). Замечание: три реплики стр. 140, 144, 132 — «Принимаю» / «Принимаю. … Экран не рисую» — содержательных 46. Вход «координация ролей — обрезан» (стр. 20) — третий прогон подряд. |
| 8 | Контракт исполним сессией A в её границах | **FAIL** | Границы A по M0: `panel-users-core.ts`, `panel-users.controller.ts`, `scripts/_ssh-panel-smoke.mjs`. Вердикт требует маршрут `POST /v1/internal/cabinet/registration-codes/consume` под внутренней охраной, «не под `/v1/panel/…`» ([Структурщик], стр. 62). Ствол: `panel-users.controller.ts:53` `@Controller('v1/panel')`, `:54` `@UseGuards(PanelAuthGuard)` — в NestJS путь метода относителен префиксу класса, а классовый страж накрывает все методы; дверь вне `/v1/panel` и вне `PanelAuthGuard` в этом файле объявить нельзя. Нужен новый контроллер и его запись в `packages/background-office/src/modules/panel-users/panel-users.module.ts:9` `controllers: [PanelUsersController]` — **файл вне границ A**. Других контроллеров с `/internal/` в офисе нет (`git grep "@Controller('v1/internal"` → пусто). Комната сама это назвала: «Пакет: `background-office` modules/panel-users (+ internal controller или subpath)» (стр. 130), но в таблице «Что НЕ строить»/DoD границу файлов не расширила. Дополнительно: DoD 3 (зубы) и 6 (smoke) — smoke в `_ssh-panel-smoke.mjs` в границах A, а тесты гонки и reason — новые файлы, для A в M0 не оговорённые (для C тесты названы явно). Расширение границ A — решение владельца при ратификации, не аудитора. |

## Что аудитор не делает

Своих решений по контракту не предлагает; протокол и повестку не правит; повторный прогон — решение председателя после ратификации владельца.
