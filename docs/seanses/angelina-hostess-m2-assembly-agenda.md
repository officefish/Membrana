# Заседание «Ангелина — хозяйка утра» — M2 (B: сборка канона кодом)

**Объединяющее задание:** утро не воспроизводится старым; гейты — механика; ведёт хозяйка.
Порядок (ратифицирован, с поправкой): `C → B → G → H → GC`. C закрыт (M1 ратифицирован).
Сейчас — B. Вердикты ritual-refactor M0–M5 (20.07) не переоткрываются — особенно M2-K
(стенка `Slot → Text`, `frame()`/`buildTop3` уже в main). Киты вне скоупа.

## Вопрос M2 (ровно один)

**B1 — Как генератор main-day-issue собирает канон дня КОДОМ (frame собирает 5 секций,
LLM только наполняет слоты), и как подписывается ручная чеканка владельца?**

⚠️ **Обязательное эхо в вердикте:** строки «Сборка секций: <решение>» и «Подпись ручной
чеканки: <решение>». Вердикт обязан нести секцию-заголовок **«Список посылок»** (без неё
заседание не состоялось, S-M2; задним числом запрещено).

Решаем только B. Не залезать в гейты (G — там owner-choice и ружьё), хозяйку (H), GC.
Ратифицирует владелец.

## Грани вопроса B (простым текстом)

- **Живой дефект (журнал 21.07):** промпт генератора — смесь двух структур (старый скелет
  «Метаданные/Посылки/Сегодня делаем» + вставленные 5 слотов) → LLM роняет 3–5 слотов из
  5 и приделывает заголовкам хвосты (подражая чеканке владельца из входа). Гейт скелета
  отбил 4 кривых прогона подряд — оборона работает, но генерация не проходит НИКОГДА.
- **Архитектура по M2-K (уже ратифицирована, раскрыть исполнение):** код владеет
  `id/order/title` — значит генератор НЕ просит LLM выдать документ с заголовками, а
  **сам собирает 5 секций** и зовёт LLM per-slot (`fill(slot, context) → text`). Что
  становится с остальным содержимым старого промпта (Метаданные, Посылки probe, DoD,
  Ссылки) — куда они ложатся в 5-слотовой сборке.
- **Fill-вызовы:** один LLM-вызов на слот или один на всё с разбором? Стоимость ×5 против
  надёжности; частичный отказ (слот не наполнился) → «— пусто —» с пометкой или retry?
- **Подпись ручной чеканки (Д4):** владелец чеканит руками → документ должен получить
  провенанс `author=human` без ручной хирургии (сегодня подписано скриптом-однодневкой).
  Инструмент: команда/шаг «подписать чеканку», или генератор принимает `--from-minting`.
- **Вещдок отказа:** `MAIN_DAY_ISSUE.rejected.md` уже пишется (патч 21.07) — судьба
  вещдока (gitignore? архив?), retry-политика гейта.

## Посылки-подсказки (для вердикта, не сам вердикт)

- M2-K (20.07, ратифицирован): стенка `Slot → Text`; `frame()`, `buildTop3`,
  `fillInput` (не отдаёт id/order/title) — код в main (`scripts/lib/day-plan-frame.mjs`).
- Журнал живого прогона 21.07: 4 прогона отбиты гейтом скелета; вещдок-патч внесён.
- Чеканка владельца — легитимный второй путь канона (прецедент 21.07: вторая чеканка дня);
  сегодня подписана руками `author=human` — Ангелина приняла («не проверено», не блок).
- Гейт скелета (`missingSlotHeadings`) уже в main — потребитель сборки.
