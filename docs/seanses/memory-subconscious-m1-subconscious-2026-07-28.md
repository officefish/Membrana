<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-28T07:25:45.942Z |
| Команда | `yarn consilium` |
| Модель | xai/grok-4.5 |
| Файл | `docs/seanses/memory-subconscious-m1-subconscious-2026-07-28.md` |
| Порядок ролей | Музыкант → Верстальщик → Архитектор → Teamlead → Математик → Структурщик |
| Повестка | `docs/meeting/memory-subconscious/M1_AGENDA.md` |

**Вопрос:**

C1: устройство подсознания — append-only архив, форма записи, переток с суммаризацией, граница контуров, v1 против слотов v2

---

```text
# Консилиум: C1 — устройство подсознания (append-only, переток, граница контуров)

**Повестка:** C1 — каково устройство подсознания персоны: структура append-only архива, форма записи, механика перетока из оперативной (суммаризация против дословности), граница между контурами — и что из этого контракт v1, а что именованный слот v2. Список посылок обязателен.

**Участники:** Teamlead (Tarasov), Архитектор (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Музыкант → Верстальщик → Архитектор → Teamlead → Математик → Структурщик

---

[Музыкант]: На слух это второй тракт, не mute первого. Оперативная — мониторы: сделанный стек, то, что сейчас в миксе. Подсознание — лента take’ов: ничего не вырезаем ножницами, иначе 27.07 у Дынина «исчезло 10» — клиппинг без индикатора. Переток = bounce в архив с указателем на полный текст, не delete с шины.

[Верстальщик]: С витрины потребителя (персона, extractor, человек в PR) два контура должны читаться без легенды. Оперативная — видимый топ под бюджетом; подсознание — не «пустота», а честный слой «утонуло, но адресуемо». Фальшивое «записи нет» при вытеснении — декор без смысла. Не рисую UI: требую в схеме статус и pointer, иначе позже лента соврет.

[Архитектор]: Конструкция: два контура — один субъект, разные носители. Оперативная = пересобираемый проекционный файл `memory/<id>.md` под бюджет. Подсознание = append-only журнал событий/записей с иммутабельными телами. Граница: запись либо в проекции (live-top), либо только в архиве с ref; правка in-place архива запрещена. Форма решения до кода; v1 — минимальный контракт хранения и перетока, без отбора C2/C3.

[Teamlead]: Положение: регрессия названа — вытеснение = безвозвратное удаление; матч проигран на устройстве, не на вкусе. Решение комнаты — только C1: дом, форма записи, переток, граница, v1/v2. Не лезем в MMR, расписание, «кого вытеснять». Связка: Архитектор+Структурщик — форма и homes; Математик — предикаты и схема; Музыкант — слух на «ничто не умирает»; Верстальщик — честные статусы в контракте данных. Планка: без посылок и DoD по C1 — BLOCK.

[Математик]: Формализую без UI. Пусть O — множество записей оперативной проекции, |serialize(O)| ≤ B (бюджет ~5K токенов — факт регрессии). A — append-only множество архивных записей. Переток τ: o ↦ a сохраняет `id`/`provenance` и даёт `fullRef` на полное тело. Предикат «ничто не умирает»: ∀e когда-либо принятой в контур памяти ∃ адрес в A ∪ O, по которому тело или суммаризация+fullRef восстановимы. Удаление из O без τ — запрещённый оператор в v1.

[Структурщик]: Леммы, иначе синонимы. **Оперативная** — проекция (view), файл `docs/virtual-team/memory/<id>.md`, пересборка целиком легитимна. **Подсознание** — home архива, не второй md «вручную». **Запись архива** — иммутабельный unit с meta+body. **Переток** — событие, не молчаливый truncate. **importance.json** — человек-флаг по provenance, отдельный словарь; extractor сегодня его не проводит (факт) — в C1 обязаны сказать, как переток флаг уважает на уровне схемы, не как C2 ранжирует.

[Музыкант]: Образец уже играет в доме: debt-ledger.jsonl — свёртка событий, не правка прошлого такта; кристаллы — supersededBy, не mute. Беру событийную схему: утонувший take остаётся в ленте, суммаризация — send на bus с pointer на master-запись. Дословность в архиве; в оперативной может звучать compact-refrain.

[Верстальщик]: Тогда в метаданных строки архива вижу минимум бейджей-полей: `id`, `persona`, `ts`, `kind`, `provenance`, `importance` (наследуется/копируется с флага), `body` или `summary+fullRef`, `status: archived`. Пустой fullRef при kind=summary — ложь витрины; v1 запрещает. Не проектирую экран — фиксирую, что поле либо есть, либо явно `fullRef: inline` для коротких.

[Архитектор]: Дом v1: один журнал на персону — `docs/virtual-team/memory/archive/<id>.jsonl` (или эквивалент jsonl под тем же деревом memory/), append-only, одна строка = одно событие/запись. Не tar истории git как API. Проекция `memory/<id>.md` остаётся человеком-читаемым top; генератор пишет «руками не редактировать» как сейчас. Цена альтернативы «один общий файл на всех» — смешение словарей персон и гонки; отвергаем.

[Teamlead]: Принимаю дом «на персону, jsonl». Отвергаю «архив = git blame md». Дальше форма записи — без расползания в extractor-политику: C6 соседняя. Кто владеет записью перетока в коде — не сейчас; сейчас контракт полей. Держим темп: спорим сущность summary vs verbatim, не скрипт.

[Математик]: Форма записи (тип): `ArchiveRecord = { id, personaId, ts, provenance, source: operative|ingest, content: Verbatim | Summary }`. `Verbatim = { text }`. `Summary = { text, fullRef, ratio? }` где fullRef указывает на verbatim-запись в том же A (id) или на content-address (sha256 тела). Инвариант: Summary ⇒ fullRef резолвится в Verbatim. Сжатие 5–20× — ориентир research, не число в зубе v1; ratio опционален.

[Структурщик]: Публичный словарь: не `helper`/`utils2`. Имена: `ArchiveRecord`, `fullRef`, `operativeProjection`, `transferEvent`. Граница пакета/дерева: archive не импортирует UI; projection-writer читает A и importance, пишет md. importance.json ключ = provenance — при τ копируем в запись архива snapshot флага `{ flagged: true|false, at: ts }`, чтобы позднейшая смена флага не переписывала историю (append-only уважение = снимок, не live-join mute).

[Музыкант]: Снимок флага — верно: как automation recall на take, а не «перекрасить старый stem». Если капитан позже снял importance — новое событие в ленте, старое не затираем. Иначе «ничто не умирает» лицемерит: метаданные умирают первыми.

[Верстальщик]: Согласен со снимком. На границе контуров оператору (и модели в промпте) нужно одно предложение-контракт: «O — топ; A — полная лента; вытеснение из O всегда сопровождается τ». В шапке md-проекции — счётчики candidately: `в топе N · в архиве M · flagged K` без обещания алгоритма C2. Честная цифра против «33 из 239» без адреса 239.

[Архитектор]: Граница контуров v1: (1) запись создаётся в O или сразу в A; (2) выбытие из O допустимо только через transferEvent в A; (3) O ⊆ проекция(A_top ∪ hot), но O не источник истины; (4) A — source of truth для прошлого. Слот v2 (только имена, без реализации): `ttlEpisodic`, `semanticIndex`, `crossPersonaLinks`, `embeddingRef` — не блокируют v1. v1 не строит RAG-подсознание.

[Teamlead]: Фиксирую: source of truth — архив; md — view. Это бьёт манифест «дорого менять устройство» — меняем сейчас, пока delete-регрессия жива. v2 слоты — список имён в вердикте, без DoD на них. Математик — предикат перетока; Структурщик — home path и запрет edit-in-place.

[Математик]: Предикат перетока: `transfer(o) allowed ⇔ o∈O ∧ ¬exists active verbatim hole`. После τ: `o∉O_next ∧ a∈A ∧ preserves(provenance,o,a) ∧ (verbatim(a) ∨ (summary(a) ∧ resolves(fullRef(a))))`. Человек-флаг: если `importance[provenance]=true`, v1 **не** обязывает оставлять o в O (это C2), но обязывает `a.importanceSnapshot.flagged=true` и запрещает drop без τ. «Ничто не умирает» = нет оператора erase(body) в алгебре v1.

[Структурщик]: Прецедент формы — кристаллы и кейсы: изменение смысла = новая запись / superseded, не patch. Для архива: `supersededBy?: id` опционален в v1 как слот совместимости с графом, но минимальный v1 может обойтись append-only без mute полей. Отвергать прецедент без причины нельзя — берём иммутабельность тела; supersededBy = именованный слот v1.1/v2, не обязателен в первом зубе.

[Музыкант]: Дословность vs суммаризация на слух: в A по умолчанию кладём verbatim уходящего фрагмента — master take. Summary — когда размер или шум требуют refrain; тогда summary живёт как отдельная запись ИЛИ как поле проекции, но full text не схлопывается. Research «не удалять» = наш bounce-to-archive. Риск: double storage — принимаем в v1, это цена «ничто не умирает».

[Верстальщик]: Для md-проекции после перетока: утонувшие не обязаны текстом в файле; достаточно, если генератор не врёт заголовком «записей: 33 из 239» — 239 должны быть перечислимы в A. Иначе витрина «из 239» без архива — снова фальшь. Прошу в DoD: extractor/проектор не имеет пути delete-only.

[Архитектор]: Переток с суммаризацией в v1: разрешены два mode на transferEvent — `mode: verbatim | summarize`. summarize без fullRef — невалид. Кто пишет summary-текст (модель/правило) — вне C1, слот `summarizer` v2/C6. C1 задаёт только валидность схемы после перетока. Граница: C2 решает *кого* вытеснять; C1 — *что остаётся на диске*, когда вытеснили.

[Teamlead]: Слышу попытку утащить summarizer — режу. Владелец summarizer не в этой комнате. Вердикт: mode в событии есть; агент суммаризации — нет. Дальше: один файл vs журнал — Архитектор сказал jsonl; есть возражение по сути?

[Математик]: Возражений нет: jsonl = монотонный лог, append проверка = «файл только растет / id уникален». Зуб: `validateArchive(persona): ∀id unique ∧ ∀Summary resolves(fullRef) ∧ ∀line JSON.parse`. Бюджет B применяется только к serializer(O), не к |A|. Метрики размера A — C5, не здесь.

[Структурщик]: Home: `docs/virtual-team/memory/archive/<personaId>.jsonl` + опционально `docs/virtual-team/memory/archive/README.md` как словарная статья (лемма дерева). Запрет: писать архив в `memory/<id>.md`; запрет: второй параллельный «теневой md». Проекция читает archive+importance+отбор (отбор не наш). Слабая связанность: C6 extractor зависит от схемы ArchiveRecord, не наоборот.

[Музыкант]: Ещё раз границу контуров на тракте: operative = то, что в ушах у роли на сеансе; subconscious = библиотека ленты. Всплытие в микс — C3, не C1. Нам достаточно разъёма: запись в A адресуема по id/provenance, чтобы C3 было куда ткнуть. Без адреса C3 будет изобретать заново — дыра в шине.

[Верстальщик]: Разъём адресов — да. В контракте v1 поле `id` стабилен, `provenance` сохраняется. Для flagged — визуальный маркер в данных (`importanceSnapshot`), чтобы будущая витрина долгов/памяти не рисовала «обычное утонувшее» и «несущее» одним серым. Несущие плотвы: «не подлежат вытеснению» — норма C2; в C1 только не потерять признак.

[Архитектор]: Сводка конструкции v1: (1) A = per-persona jsonl append-only; (2) O = md projection ≤B; (3) transferEvent обязателен при выбытии из O; (4) Summary⇒fullRef; (5) importance snapshot on transfer; (6) source of truth = A. v2 именованные слоты: `ttlEpisodic`, `semanticIndex`, `embeddingRef`, `crossPersonaLinks`, `summarizer`, `supersededBy` (если не войдёт в v1.1), `ratio`. ADR не обязателен, если вердикт ляжет в docs meeting/memory-subconscious и схему.

[Teamlead]: Принимаем конструкцию. Риск обратного ранга: без C1 соседние C2–C6 строят на delete — запрещаю «отложим архив». DoD только на устройство и схему, без MMR. Кто в связке после: Структурщик — paths+типы; Математик — validate предикаты/тесты чистые; Teamlead — приёмка. Верстальщик/Музыкант — ревью честности статусов в примере фикстуры, не UI-фича.

[Математик]: «Ничто не умирает» на схеме: ¬∃ erase; есть только append и rebuild(O). Регрессии 27–28.07 (`вытеснено 48`) в алгебре v1 невозможны как silent drop: счётчик displaced обязан равняться числу transferEvent за прогон ± явный ingest. Это проверяемый инвариант для будущего C5/C6, в C1 — формулируем как схему событий.

[Структурщик]: Согласую со статусом записи в O vs A: не плодим enum жизни на два файла без нужды. В A запись `kind: verbatim|summary` + transfer meta `{ fromProjection: true, runId? }`. runId — слот v2/C4. В v1 достаточно ts+provenance. Отвергаю физическое «два sqlite» в v1 — jsonl в git-friendly дереве memory/, как debt-ledger образец.

[Музыкант]: Git-friendly jsonl — хороший master: слышно diff, append в PR виден. Не Mixed-down blob. Принимаю. Осторожно: не коммитить секреты в verbatim — это не C1-суммаризация, а существующий secret-contour; помечаю как внешний гейт, не слот подсознания.

[Верстальщик]: Внешний secret-gate — ок, не колонизируем. В примере фикстуры для DoD: две строки jsonl (verbatim + summary с fullRef), md-проекция без удалённого id в тексте, importance snapshot true на flagged provenance. Читается без легенды — достаточно для приёмки формы.

[Архитектор]: Уточнение границы контуров для соседей (не их решение): C2 читает importanceSnapshot и политики несущих; C3 резолвит id из A; C4 планирует когда τ; C5 меряет |A|, ratio, displaced; C6 пишет τ вместо delete. C1 не включает scheduling. Рекомендация: вердикт = контракт v1 + список слотов v2; правка extractor — отдельная задача после, не hidden scope.

[Teamlead]: Исполнение: вердикт закрываем на контракте. Любой PR extractor с delete-only после ратификации C1 — BLOCK по имени зверя «молчаливое вытеснение». Связка на реализацию схемы — после сеанса, не здесь. Последний круг: явные «Принимаю» по таблице.

[Математик]: Принимаю: инварианты A append-only, Summary⇒fullRef, B только на O, importance snapshot, запрет erase. Чистое validate — моя зона в DoD.

[Структурщик]: Принимаю: homes `memory/<id>.md` + `memory/archive/<id>.jsonl`, словарь ArchiveRecord, снимок флага, слабая связанность projection←archive. Леммы в README архива — в DoD.

[Музыкант]: Принимаю: второй тракт, bounce не mute, verbatim master, summary с pointer, прецедент ledger/crystals на слух соблюдён.

[Верстальщик]: Принимаю: честные поля статусов и счётчики; запрет витрины «N из M» без M в A; фикстура как вещдок формы.

[Архитектор]: Принимаю: конструкция dual-contour, source of truth = A, v1 минимален, v2 слоты именованы без реализации. Форма оправдана функцией; декора-API нет.

[Teamlead]: Принимаю. Матч C1 сыгран на устройстве. Вердикт — в таблицу; DoD — только C1. Соседям C2–C6 не колонизировать этот DoD.

---

## Список посылок

| # | Посылка | Тип |
|---|---------|-----|
| P1 | Слово владельца 27–28.07: у персон две памяти (оперативная и подсознание); поручение создать второй контур | норма (владелец) |
| P2 | M0: C1 — фундамент; C2 приоритизация, C3 всплытие, C4 цикл, C5 метрики, C6 extractor — соседние комнаты, не предмет C1 | норма (порядок M0) |
| P3 | Живой extractor пересобирает топ под бюджет ~5K токенов («N из K кандидатов»); вытеснение сегодня = безвозвратное удаление из проекции без архива | факт (журналы memory, регрессии 27–28.07) |
| P4 | 27.07 у персоны Dynin зафиксировано молчаливое исчезновение позиций; 28.07 «записано 46 · вытеснено 48» | факт (вещдоки регрессии / #1366) |
| P5 | Research (Issue #1366, 9 источников): не рекомендовать простое удаление; суммаризация или перенос в архив; сжатие ориентир 5–20×; схемы working / episodic / semantic | факт (research-сводка в issue) |
| P6 | Плотва 27.07 (#1366): разделение слоёв логическое; несущие принципы не должны тонуть в потоке без сохранения признака | норма (вводная комнаты) |
| P7 | Носитель оперативной: `docs/virtual-team/memory/<id>.md`, перезапись целиком, «руками не редактировать» | факт (код/дерево репо) |
| P8 | Человек-флаг `docs/virtual-team/memory/importance.json` (ключ = provenance); extractor флаг не проводит | факт (дерево + поведение extractor) |
| P9 | Образцы append-only в проекте: `docs/bridge/debt-ledger.jsonl`; кристаллы графа — append-only с `supersededBy`; кейсы иммутабельны (новый id) | факт (репо / прецеденты) |
| P10 | В проектной норме уже звучит «ничто не умирает» через событийные схемы (кристалл/кейс) — комната опирается на прецедент | норма+факт (прецедент формы) |
| P11 | Ограничение повестки C1: только устройство хранения и перетока; не отбор, не MMR/serendipity, не расписание сжатия | норма (M1_AGENDA) |

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Два контура | **Да.** Оперативная (O) — проекция/top под бюджетом; подсознание (A) — append-only архив. Source of truth = **A**; `memory/<id>.md` = view. |
| Дом архива v1 | **Per-persona jsonl:** `docs/virtual-team/memory/archive/<personaId>.jsonl`. Не общий монолит; не git-blame как API; не второй «ручной» md. |
| Форма записи v1 | `ArchiveRecord`: `id`, `personaId`, `ts`, `provenance`, `source`, `kind: verbatim\|summary`, тело текста; при `summary` — обязательный **`fullRef`** → verbatim. Опционально meta перетока (`fromProjection`). |
| Переток | Выбытие из O **только** через `transfer` в A. Modes: `verbatim` (по умолчанию для уходящего фрагмента) \| `summarize` (только с fullRef). Агент/модель summarizer — **не C1** (слот v2/C6). |
| «Ничто не умирает» | На схеме: **нет оператора erase**; есть append(A) и rebuild(O). Silent delete — запрещён. |
| importance | Переток **уважает** флаг снимком `importanceSnapshot` в запись A (не live-join с mute истории). Обязанность «не вытеснять несущих из O» — **C2**, не C1. |
| Граница контуров | O — то, что в сеансовом бюджете роли; A — полная адресуемая лента. C3+ резолвят id/provenance из A. Scheduling τ — C4; кто вытесняется — C2; метрики — C5; провод extractor — C6. |
| v1 vs v2 | **v1 (контракт):** homes, ArchiveRecord, transfer+fullRef, snapshot importance, append-only, md=projection, validate-инварианты. **v2 слоты (имена):** `ttlEpisodic`, `semanticIndex`, `embeddingRef`, `crossPersonaLinks`, `summarizer`, `ratio`, `runId`, `supersededBy` (если не вынесен в v1.1). |
| Прецедент crystals/cases/ledger | **Принят** для иммутабельности и jsonl-событийности; полный isomorphic graph-mint — не требуется в v1. |
| Отложено | Реализация отбора, всплытия, цикла, метрик, extractor-rewrite — соседние Ci; не блокируют формулировку C1, но блокируют «оставить delete». |

**Definition of Done (C1 only):**

1. Зафиксирован контракт v1 в артефакте дома подсознания (doc + JSON Schema или эквивалент TS-типа `ArchiveRecord`) с полями и инвариантом `summary ⇒ fullRef resolves`.
2. Описаны homes: `docs/virtual-team/memory/<id>.md` (projection) и `docs/virtual-team/memory/archive/<id>.jsonl` (A); короткий README-словарь лемм в `archive/`.
3. Запрет erase и обязательность transfer при выбытии из O — явные нормативные правила контракта (для последующего C6).
4. Правило `importanceSnapshot` на transfer — в схеме; без политики ранжирования C2.
5. Чистые тесты/validate (зона Математика): unique id; parse jsonl; fullRef резолв; файл архива append-монотонен в фикстуре.
6. Фикстура-вещдок: ≥1 verbatim, ≥1 summary+fullRef, flagged snapshot, пример md-проекции без delete-only пути.
7. Список именованных слотов v2 — в том же контрактном doc, без реализации.
8. **Не входит в DoD C1:** MMR/всплытие, cron сжатия, код extractor, UI пульта, метрики дашборда.

---

*Реплик в диалоге: 40; каждый участник высказался не менее одного раза.*
```
