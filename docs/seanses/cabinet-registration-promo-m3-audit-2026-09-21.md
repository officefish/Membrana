# Аудит M3 «Регистрация в кабинете по промокоду панели» — порядок гашения и режим отказа — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Задание: `docs/meeting/cabinet-registration-promo/AUDIT_M3_BRIEF.md`. Предмет: повестка `M3_AGENDA.md` и протокол `docs/seanses/cabinet-registration-promo-m3-order-failure-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `d033d9d8`; 42 реплики, модель `xai/grok-4.5`, `--min-replies 36`). Ствол для сверки: `origin/main` = `1c5a0499`. Протокол и повестка не правились. Хвост «Полное эхо вопроса и повестки» (стр. 236) — добавка инструмента, не комнаты; не аудируется.

## Итог

**ОТКЛОНЕНО** по одному пункту — **6**, формально: заголовок раздела «Definition of Done» (стр. 220) латиницей, при том что повестка M3 (требование 4) назвала именно это «провалом формы» по уроку M2. Существо вердикта по всем семи содержательным пунктам — PASS: порядок из восьми шагов списком, карта двери с кодами числами и одной фразой на каждый класс, обе компенсации, ограничитель 10 / 600000 мс / IP / 429, поле `code` с нормализацией, подключение клиента, зубы, расширение границ C названо отдельной таблицей. Все 17 цитат в 10 посылках дословны и у заявленных ролей.

Замечания для ратификации, не провалы: (1) файл ограничителя — вердикт помещает `hit` в `auth.controller.ts`, а утилиту «только если уже есть в cabinet; иначе тонкий private helper в модуле auth» (стр. 99); по стволу `createSlidingWindowLimiter` живёт в офисе, `packages/background-office/src/modules/panel-auth/panel-auth-core.ts:237`, в кабинете её нет — значит helper неизбежен, и если он станет отдельным файлом в `modules/auth/`, этого файла нет ни в границах C по M0, ни в таблице расширения (стр. 197–206); (2) код успеха назван «успех как сегодня» / «201/200 как сегодня» (стр. 82, 185) — по стволу `auth.controller.ts:21–24` без `@HttpCode`, значит 201 по умолчанию Nest; число определимо командой, но в таблице его нет.

## Таблица

| # | Пункт | Вердикт | Цитата / команда |
|---|---|---|---|
| 1 | Один вопрос | PASS | Решения M4/M1/M2 в таблицу не вошли: «Что НЕ строить: un-redeem, ретраи, outbox, check-before-redeem, кэш кодов, вторая система кодов, трогать тарифный PromoCode, мембрану на register, контракты M1/M2, UI-тексты M4» (стр. 195). Фразы наружу (`Registration was not accepted`, `Please try again later`) — ответ на вопрос повестки «HTTP-код числом и фраза наружу», не текст формы: «Локализация UI — M4» ([Верстальщик], стр. 128). Утечки в репликах («M4 не получит ветку „займите другой логин“», стр. 93) решениями не стали. |
| 2 | Вердикт про контракт | PASS (замечания) | Порядок шагов списком 1–8 — стр. 181; причина — стр. 182. Исходы: `refused` → **403** `Registration was not accepted` (186); локальный отказ до гашения → 403, та же фраза (187); `office-unavailable` → **503** `Please try again later` (188); `config-invalid` → 503 + лог `registration_office_config_invalid` (189); выключено → **401** `Registration is disabled` (190). Компенсация A — стр. 183 (код сгорел, `exhausted` на повтор, 403, лог error `registration_redeem_orphaned`, un-redeem нет); компенсация B — стр. 184 («create прошёл при неизвестном гашении» запрещён порядком; 503; лог warn `registration_redeem_outcome_unknown`). Ограничитель — стр. 191: ключ IP, окно **600000** мс, порог **10**, **429** `Too many requests`, hit в `auth.controller.ts`. DTO — стр. 192: `code: string` обязательное, trim, «пустой или length>128 → отказ без офиса» (181). Подключение — стр. 193: `auth.module.ts` импортирует модуль M2, DI в `AuthService`. Замечания: исход `ok` — «успех как сегодня» (185), в реплике «201/200 как сегодня» (82): по стволу `@Post('register')` без `@HttpCode` → 201; число в таблице не стоит. Файл helper-а ограничителя не назван (см. п. 8). |
| 3 | «Список посылок» | PASS | Секция есть (стр. 160), 10 посылок P1–P10, тип у каждой, 17 цитат с ролью. Скрипт: жирное и кавычки нормализованы, точная подстрока по репликам стр. 44–156, роль по ближайшему маркеру `[Роль]:` выше. **17 из 17 дословны, роли совпадают:** P1 [Teamlead] 44, [Архитектор] 46; P2 [Архитектор] 46 (с многоточием), [Структурщик] 54; P3 [Верстальщик] 71, [Архитектор] 83; P4 [Математик] 52 (с многоточием), [Структурщик] 75; P5 [Архитектор] 89, [Математик] 95; P6 [Teamlead] 44; P7 [Teamlead] 56, [Структурщик] 54 (с многоточием); P8 [Математик] 142, [Музыкант] 103; P9 [Teamlead] 77; P10 [Архитектор] 46. Строк таблицы вердикта вместо реплик — ноль. |
| 4 | Фактура против ствола | PASS | Проверено 24 строки `git show origin/main:<путь> \| sed -n '<строка>p'`. `auth.service.ts:15` `async register(login: string, password: string)`, `:16` `if (!this.config.ALLOW_REGISTRATION)`, `:19–21` нормализация и длины → `UnauthorizedException`, `:24–26` `findUnique` → `ConflictException('Login already taken')`, `:29–31` `hashPassword` / `prisma.user.create({ login, passwordHash, role: 'user' })`, `:34` `createSessionForUser`; `auth.controller.ts:21` `@Post('register')`; `auth.dto.ts` — `login`, `password`, поля `code` нет; `auth.module.ts:8–9` `controllers: [AuthController]`, `providers: [AuthService, SessionGuard, AdminGuard]`, `imports` нет; `env.schema.ts:37` `ALLOW_REGISTRATION: boolFromEnv.optional()`, `:47` `?? data.NODE_ENV === 'development'`; `generate-cabinet-env.sh:34` `ALLOW_REGISTRATION=false`; `panel-users.controller.ts:56` `registerLimiter … createSlidingWindowLimiter(`, `:86` `if (!this.registerLimiter.hit(key, Date.now()))`, `:93–96` `redeemCode(this.store.snapshot(), …)` / `store.mutate` / `ForbiddenException('code was not accepted')`; `membrane.service.ts:157` `getOrCreateMembraneForUser`; `schema.prisma:314` `model PromoCode {`; `tariff.controller.ts:139` `async redeemPromo(`. `$transaction` в кабинете — только `modules/tariff/tariff-transition.service.ts` (`git grep -l`); ограничителя в `modules/auth` и `common` нет (`git grep -il "throttle\|rate-limit\|429\|SlidingWindow"` → пусто); тесты `auth` — только `password.util.test.ts`. Утверждений о несуществующем — ноль. |
| 5 | Данности и M1–M2 | PASS | `grep -n -iE "check\|retry\|ретра\|очеред\|кэш\|targetTariffId\|redeemPromo"` → 15 попаданий, все в смысле запрета или фактуры: стр. 46 «`check` до `redeem` запрещён M1 — TOCTOU», 52 «не сага и не ретрай `redeem`», 75 «По M2 ретраев нет», 103 «Тарифный `redeemPromo` не трогаем», 124 и 195 «Что НЕ строить: ретраи … `check` … кэш», 138 «мигнуть retry только на 503» (о браузере M4, не о клиенте), 222 «без `check` и без ретраев», 227 «Тарифный `PromoCode` / `redeemPromo` … не изменены». Тела двери и `reason` не менялись; `mode: "check"` на продуктовом пути нет. |
| 6 | Словарь | **FAIL** (формально) | Заголовок раздела «## Definition of Done» (стр. 220) — латиницей; повестка M3, требование 4: «заголовки строк вердикта и разделов — по-русски (урок M2: „Маппинг“, „Definition of Done“ — провал формы)». Заголовки строк с латиницей вне обратных кавычек: «Компенсация A: redeem ok, create fail» (183), «refused / локальный отказ / orphan create» (214), «ok + create» (216), «DTO» (192) — замечание; идентификаторы в кавычках (`office-unavailable`, `config-invalid`, `auth.dto.ts`) — законны. Опечатка «Гашение с стороны кабинета» (210). |
| 7 | Реплик не меньше тридцати | PASS | `grep -cE "^\[(Архитектор\|Верстальщик\|Математик\|Teamlead\|Структурщик\|Музыкант)\]"` → **42**; по ролям — по 7 у каждой из шести. Условия `--min-replies 36` (стр. 26), футер «Реплик в диалоге: 42» (232). «Принимаю» без содержания — 0: пять заключительных (стр. 148–156) несут по фразе существа («Принимаю DI через `auth.module.ts`, поле `code` в DTO, hit в controller»). Вход «координация ролей — обрезан» (стр. 20) — пятый прогон подряд. |
| 8 | Исполнимость в границах C | PASS (замечание) | Таблица «Расширение границ сессии C (явно)» (стр. 197–206): `auth.controller.ts` и `auth.service.ts` — M0; `config/env.schema.ts` — «не менять семантику 37,47, только чтение флага»; **`auth.dto.ts` — «править (расширение) — поле `code` иначе дверь не примет код»; `auth.module.ts` — «править (расширение) — импорт M2-модуля иначе нет DI»**; тесты `auth` — M0, новый файл. Реплики-опоры: [Teamlead] 56 «Фиксирую расширение границ…», [Структурщик] 109. Урок M1/M2 усвоен: расширение названо словами в вердикте. **Замечание — ограничитель:** решение «hit в `auth.controller.ts` до service» (191) в границах; утилита — «только если уже есть в cabinet packages; иначе тонкий private helper в модуле auth, без новых пакетов» ([Teamlead], стр. 99). По стволу `createSlidingWindowLimiter` определена в `packages/background-office/src/modules/panel-auth/panel-auth-core.ts:237` и используется только офисом; в кабинете (`modules/auth`, `common/`) её нет — условие «если есть» ложно, helper неизбежен. Если он окажется внутри `auth.controller.ts` — в границах; если отдельным файлом в `modules/auth/` — файл не назван ни в M0, ни в таблице расширения. Назвать файл — решение владельца при ратификации. |

## Что аудитор не делает

Своих решений по контракту и по границам сессии C не предлагает; протокол и повестку не правит; повторный прогон — решение председателя после ратификации владельца.
