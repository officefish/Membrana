<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-29T10:19:45.534Z |
| Команда | `yarn consilium` |
| Модель | xai/grok-4.5 |
| Файл | `docs/seanses/tariff-grid-m2-rail-2026-07-29.md` |
| Порядок ролей | Teamlead → Архитектор → Структурщик → Математик → Музыкант → Верстальщик |
| Повестка | `docs/meeting/tariff-grid/M2_AGENDA.md` |

**Вопрос:**

Отношение тарифной сетки к рельсу прав сценариев: поглощение, надстройка или проекция

---

```text
# Консилиум: отношение тарифной сетки к рельсу прав сценариев

**Повестка:** M2 — как `TariffGridDocument` соотносится с работающим рельсом `Tariff.entitledTariffSkus` → server → wire → конфиг клиента → бейджи `bundled/community/entitled/locked`: поглощение, надстройка или проекция. Точка единственного источника истины о правах; судьба поля `entitledTariffSkus`.

**Участники:** Teamlead (Tarasov), Архитектор (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Teamlead → Архитектор → Структурщик → Математик → Музыкант → Верстальщик

---

[Teamlead]: Планка матча узкая. M1 закрыт: сетка — versioned контракт, home истины — сервер, рельс `entitledTariffSkus` «не ломается», стыковка — наша. Три кандидата: поглощение, надстройка, проекция. Запрет двух источников истины — не предмет спора, а правило задания. Связка: Архитектор — форма отношения; Структурщик — леммы и homes; Математик — предикат «кто автор права»; Музыкант — слух на обрыв тракта до бейджа; Верстальщик — честность витрины. Не лезем в M3–M8.

[Архитектор]: Конструкция. Рельс — уже работающий конвейер от Prisma `Tariff.entitledTariffSkus` до бейджей. Сетка M1 — декларативная матрица SKU × entitlementId. Поглощение = вырезать рельс и переписать потребителей на resolve по ячейкам. Надстройка = сетка рядом, рельс живёт своей жизнью — два автора права. Проекция = сетка пишет истину; `entitledTariffSkus` (и wire-поле) — вычисленный срез матрицы для рода `catalog`. Рекомендую проекцию: форма M1 + запрет двух SOТ + «рельс не ломается».

[Структурщик]: Леммы, иначе синонимы. **Рельс** — замкнутый путь носителей: Prisma-поле → node-realtime → wire DTO → `device-board-module-config.entitledTariffSkus` → `ClientUserCaseCatalogService` → gate бейджей. **Сетка** — `TariffGridDocument` (registry + matrix), home на сервере. **Проекция** — детерминированная функция `projectCatalogSkus(grid, sku) → string[]`, не второй источник. **Поглощение** — удаление леммы рельса из словаря потребителей. **Надстройка** — два homes истины. Словарь требует: один home истины = matrix; рельс = view.

[Математик]: Формализую без UI. Пусть G — grid, s — tariffSku мембраны, E_cat ⊂ registry — id с kind=`catalog`. Предикат entitled по каталогу: `entitled(s, id) ⇔ resolve(G,s,id).status = entitled`. Проекция списка SKU карточек: `P(G,s) = { cardSku | ∃ id∈E_cat: payload(id) покрывает cardSku ∧ entitled(s,id) }` — точный контракт уточнит владелец registry, но форма — pure. Инвариант единственности: ¬∃ источника S≠G такого, что wire.entitledTariffSkus пишется из S в обход P. Надстройка нарушает инвариант. Поглощение = P не экспонируется, потребители зовут resolve напрямую — допустимо математически, но ломает «рельс не ломается» как норму M1.

[Музыкант]: На слух тракт уже сведён: от базы до бейджа — одна шина, без обрывов. Поглощение = перепаять все мониторы на новый разъём в одном take — риск клиппинга на кабинете/борде/гейте. Надстройка = два fader’а на одну громкость: кто последний тронул — тот и «правда», на слух это всегда фальшь. Проекция = master — сетка; `entitledTariffSkus` — stem/bus send, тот же сигнал, старые мониторы продолжают играть. Беру проекцию.

[Верстальщик]: С витрины оператора бейджи `bundled | community | entitled | locked` уже читаются без легенды. Если после M2 один и тот же сценарий станет entitled на сервере и locked на клиенте — это фальшь витрины, хуже пустоты. Проекция обязана дать тот же класс бейджа при том же SKU мембраны. Не проектирую троичность M3; требую инвариант: вход gate не меняет контракт четырёх меток, пока M3 не скажет иное. Пустота списка — честный `locked` по deny-by-default, не «—».

[Teamlead]: Слышу крен в проекцию. Проверю нагрузку: «рельс не ломается» — это не «поле Prisma священно навечно», а «конвейер до бейджа не обрываем в этом матче». Поглощение обрывает. Надстройка — два автора, BLOCK по заданию. Дальше копаем судьбу поля и смежные потолки `maxUserWorkspaces` / `maxNodesPerMembrane`.

[Архитектор]: Судьба поля. В Prisma `entitledTariffSkus` сегодня — хранимая истина тарифа. После проекции хранимая истина — документ сетки (versioned); поле либо (a) денормализованный кэш проекции при записи тарифа/сетки, либо (b) уходит из write-path и собирается в read-model при сборке wire. Цена (a): риск рассинхрона, нужен один writer. Цена (b): чище SOT, wire по-прежнему несёт массив — рельс жив. Рекомендую (b) как целевую форму; (a) — только если read path без grid-документа на горячем WS недопустим по латентности — это уже зуб M7, не вердикт отношения.

[Структурщик]: Уточняю лемму **writer**. Единственный writer проекции каталога — серверный модуль, который знает G и s мембраны (office / node-realtime path). Клиент **не** считает P из своей копии матрицы в обход wire, если wire уже несёт проекцию — иначе два resolve-контура. Допустимо: клиент держит G (wire DTO сетки) и зовёт pure `resolveEntitlement` для *новых* id; для рельса сценариев v1 — потребляет `entitledTariffSkus` как проекцию, без пересчёта. Словарь: **consumer of projection** ≠ **author of entitlement**.

[Математик]: Смежные числовые потолки. `maxUserWorkspaces=3`, `maxNodesPerMembrane=1` на том же `Tariff`, вне списка SKU. В родах M1 это `quota{limit,unit}`. Предикат: они **не** входят в P (P — только catalog-срез), но входят в G. Значит рельс сценариев — *частная* проекция G, не *полная*. Полная функция прав: `resolve(G,s,·)`. Частичная: `P_catalog`, `P_quota(id)`, … Инвариант: любой legacy-потолок либо ячейка quota в G, либо explicit gap «выразимо, строится отдельно» — но не третий home на `Tariff` в обход G после интеграции write-path.

[Музыкант]: Узлы палитры «прав нет вовсе» — на слух это не дыра рельса сценариев, а другой канал. Не тащим палитру в P_catalog. Если когда-то понадобится — отдельный entitlementId рода instrument/gated в G, не расширение `entitledTariffSkus` до свалки. Иначе stem сценариев начнёт вести чужие дорожки.

[Верстальщик]: Кабинет и борд показывают потолки и бейджи из разных полос. Честная конструкция: полоса сценариев продолжает есть проекцию списка SKU; полоса квот — отдельные числа/бейджи, когда M8 донесёт. Смешивать в один badge-cluster «тариф» без kind — декор и ложь. Пока квоты не в wire-проекции для UI — витрина не рисует их из сетки «наугад».

[Teamlead]: Фиксирую промежуточное: отношение = **проекция**; SOT = **G на сервере**; рельс = **catalog-проекция**, не полный образ G. Спорный остаток: остаётся ли колонка Prisma `entitledTariffSkus` «как есть» в схеме или «становится проекцией» на уровне смысла. Задание: «остаётся как есть · становится проекцией · выводится из матрицы · иное». Нужна одна клетка, не каша.

[Архитектор]: Одна клетка смысла: **выводится из матрицы** (и тем самым *является проекцией*). «Остаётся как есть» — ложь: сегодня поле — author. «Становится проекцией» и «выводится из матрицы» — синонимы в нашей лемме; в вердикте пишем канон: **выводится из матрицы (проекция catalog-среза)**. Физическое хранение колонки — деталь миграции read/write path, не отдельное отношение. Пользователей нет — миграционных ветвей не проектируем (граница M2).

[Структурщик]: Согласен с каноном имени. В словаре API: deprecated-as-author, live-as-projection. Публичный контракт wire может сохранить имя `entitledTariffSkus`, чтобы не ломать `device-board-module-config` и gate — это стабильность леммы *носителя*, не стабильность *авторства*. Home истины в SERVICES/office sense — документ сетки + resolve; Prisma-колонка не в списке authors после стыка.

[Математик]: Предикат стыка (для DoD M2, не код): `wire.entitledTariffSkus ≡ P(G, membrane.tariffSku)` в момент сборки DTO. Расхождение = баг writer’а, не «мнение клиента». Deny-by-default M1: если G не загружен / version mismatch — P даёт ∅ → всё tariff-tier locked, community/bundled по своим правилам tier вне матрицы (bundled/community — класс `UserCaseTier`, не ячейка G, если так устроено сегодня).

[Музыкант]: Bundled/community на слух — не тарифом открытые stem’ы. Если засунем их в матрицу без нужды — переусложним master. Рельс бейджей уже различает tier; P закрывает ветку `tariff` → entitled/locked. Не трогаем семантику bundled/community в M2 — только источник entitled для tariff-карточек.

[Верстальщик]: Подтверждаю с витрины: `UserCaseTier = bundled | tariff | community` остаётся; для `tariff` ключ — вхождение card.sku в проекцию. Визуально four-state gate не меняем. Если P пустой — tariff-карточки locked, без фальшивого entitled «по старой колонке».

[Teamlead]: Антинагрузка: кто-то захочет «надстройку на переходный период» — два writer’а. Это не переход, это проигрыш матча. Либо G ещё не author (до M8 integrate) — тогда рельс живёт по-старому *до* включение writer’а; либо G author — тогда только P. Полусезон с двойной записью — BLOCK. Архитектор, граница фазы?

[Архитектор]: Фаза не = надстройка как отношение. Отношение вердиктим целевое: проекция. План интеграции (M8) назовёт порядок включения writer’а; M2 не рисует dual-run. Норма: с момента, когда G объявлен SOT для entitlement, legacy write в `entitledTariffSkus` как author запрещён. До этого момента поле author — факт текущего кода, не целевое отношение.

[Структурщик]: `AdminGuard` и тарифные права — разные оси (M1). Не смешиваем: admin не клетка G, G не заменяет admin. Рельс сценариев не про admin. В леммах M2 admin не участвует — только чтобы не притащили поглощение «все права в сетку».

[Математик]: Полная картина функций. Author: `G`. Derived: `P_catalog(G,s)`, будущие `P_quota`, pure `resolve`. Legacy read совместимость: DTO.field = P_catalog. Никакого `entitled ⇔ sku ∈ Tariff.entitledTariffSkus` как *определения* после стыка — только как равенство к P. Это снимает поглощение (определение через resolve везде в UI) и надстройку (определение через поле ∧ через G).

[Музыкант]: Риск «проекция отстала от G» = рассинхрон monitors и master. Лечится одним writer’ом в том же запросе/транзакции сборки wire, не кэшем на клиенте без version. Version сетки (M1 versioned) обязан ехать в wire рядом — иначе не слышно, чей take. Деталь зуба — M7; требование отношения — versioned G → P.

[Верстальщик]: Для честной пустоты на витрине: при отсутствии G на клиенте не подставлять stale entitled из localStorage как truth. Либо wire принёс P, либо gate в safe locked для tariff. Это не M3, это анти-фальшь.

[Teamlead]: Сводим к вердикту. Отношение — **проекция** (catalog-срез). SOT — **серверный G**. `entitledTariffSkus` — **выводится из матрицы**, wire-имя может жить. Поглощение и надстройка — отвергнуты с основаниями. Квоты — в G как quota, не в P_catalog. Возражения по существу — сейчас или «принимаю».

[Архитектор]: Принимаю. Форма: target relation = projection; author = TariffGridDocument @ server; rail = derived catalog projection; no dual author. ADR-уровень: одна страница в M8/plan, не размывая M2.

[Структурщик]: Принимаю. Леммы: author home = grid document; rail carriers сохраняют имена; writer проекции — один, server-side; client — consumer.

[Математик]: Принимаю. Инвариант `wire.entitledTariffSkus ≡ P(G,s)`; SOT = G; P — pure.

[Музыкант]: Принимаю. Master = G; stem сценариев = P; без двух fader’ов.

[Верстальщик]: Принимаю. Бейджи и tier без фальши; tariff entitled только из проекции.

[Teamlead]: Матч сыгран. Вердикт — проекция; LGTM на протокол при наличии «Список посылок» и DoD только M2. Связка на исполнение формы — не здесь (M8). Двойной writer — сразу BLOCK по имени.

---

## Список посылок

| # | Посылка | Тип |
|---|---------|-----|
| 1 | M1 ратифицирован: объект — versioned `TariffGridDocument` (EntitlementRegistry + матрица SKU × entitlementId → EntitlementValue); home истины о правах — сервер; клиент — wire DTO + pure `resolveEntitlement`; deny-by-default; полнота ячеек | норма |
| 2 | M1: пять родов kind-union закрыты (`quota`, `catalog`, `instrument`, `gated`, `produce`); `AdminGuard` и тарифные права — разные оси | норма |
| 3 | M1 / задание заседания: рельс `entitledTariffSkus` не ломается; стыковка — предмет M2; запрет двух источников истины о правах | норма |
| 4 | Рельс сценариев замкнут в коде: `Tariff.entitledTariffSkus` (Prisma) → `node-realtime` → wire → `device-board-module-config.entitledTariffSkus` → `ClientUserCaseCatalogService` → бейджи `bundled/community/entitled/locked` (`user-case-settings-gate`); ключ — `tariffSku` карточки; `UserCaseTier = bundled \| tariff \| community` | факт |
| 5 | На том же `Tariff` живут числовые потолки `maxUserWorkspaces` (=3), `maxNodesPerMembrane` (=1) вне списка SKU; у узлов палитры отдельного рельса прав нет | факт |
| 6 | Границы M2: не троичность UI (M3), не две памяти (M4), не produce (M5), не смена тарифа/промо (M6), не зубы (M7), не план интеграции (M8); форму сетки M1 не пересматриваем; миграционных ветвей нет (пользователей нет) | норма |
| 7 | Ядро процедур и потребители рельса (gate, catalog filter, config) сегодня завязаны на массив SKU в wire/config, не на matrix resolve | факт |

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Отношение сетки к рельсу | **Проекция** (не поглощение, не надстройка) |
| Основания | (1) SOT один — G; надстройка = два author → запрет задания; (2) поглощение = обрыв носителей рельса → против нормы M1 «рельс не ломается»; (3) проекция = G author, рельс = derived catalog-срез, wire/бейджи живы |
| Точка единственного источника истины о правах | **Серверный `TariffGridDocument` (matrix + registry)**; pure `resolveEntitlement(grid, sku, id)` — канон чтения ячейки |
| Судьба `entitledTariffSkus` | **Выводится из матрицы** (проекция catalog-среза для `membrane.tariffSku`); не author. Имя поля в wire/config **может сохраниться** как носитель проекции (стабильность леммы рельса), не как хранимая истина тарифа |
| Инвариант стыка | `wire.entitledTariffSkus ≡ P_catalog(G, membrane.tariffSku)` в момент сборки DTO; writer проекции — **один, server-side**; dual-write legacy+G как author — запрещён с момента объявления G SOT |
| Scope проекции рельса | Только **catalog**-права сценариев / tariff-tier карточек. `quota` (`maxUserWorkspaces`, `maxNodesPerMembrane`) — ячейки G рода quota, **не** элементы `entitledTariffSkus`. Палитра узлов и admin — вне этого рельса |
| Bundled / community | Семантика `UserCaseTier` и бейджей **не переопределяется** M2; P закрывает ветку entitled/locked для **tariff**-карточек |
| Отвергнуто | **Поглощение** — потребители обязаны сразу уйти с массива на resolve; ломает рельс в смысле M1. **Надстройка** — сетка «поверх» живого author-поля; два источника истины |
| Не решено здесь (чужие комнаты) | Троичность экрана; UI квот; order включения writer (M8); version/зубы рассинхрона (M7) |

**Definition of Done (только M2):**

1. В каноне/ADR-заметке заседания зафиксировано отношение: **проекция**; SOT = server grid; `entitledTariffSkus` = **вывод из матрицы** (P_catalog), не author.
2. Явно записан инвариант `wire.entitledTariffSkus ≡ P_catalog(G, s)` и запрет dual-author (legacy write ∧ G).
3. Разведены: catalog-проекция рельса сценариев vs quota-поля vs admin vs палитра — без требования кода M3–M8.
4. Форма M1 и работающий конвейер до бейджей не объявлены obsolete; план переключения writer’а **не** подменяется вердиктом M2 (остаётся M8).
5. Протокол содержит секцию **«Список посылок»** и не колонизирует DoD соседних Id.

---

*Реплик в диалоге: 31; каждый участник высказался не менее одного раза.*
```
