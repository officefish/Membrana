<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-08-03T11:59:10.453Z |
| Команда | `yarn consilium` |
| Модель | anthropic/claude-sonnet-4-6 |
| Файл | `docs/seanses/static-mmbrn-container-m0-order-2026-08-03.md` |
| Порядок ролей | Музыкант → Архитектор → Структурщик → Математик → Верстальщик → Teamlead |
| Повестка | `docs/meeting/static-mmbrn-container/M0_AGENDA.md` |

**Вход сеанса** (что комната действительно получила):

| Вход | Носитель | Размер | Отпечаток | Доставка |
|------|----------|-------:|-----------|----------|
| повестка (эхо в голове) | `docs/meeting/static-mmbrn-container/M0_AGENDA.md` | 5627 | `237c7297f038` | полностью |
| инструкция консилиума | `docs/prompts/CONSILIUM_PROMPT.md` | 4494 | `b6bd1a8dc127` | полностью |
| координация ролей | `docs/VIRTUAL_TEAM_PROMPT.md` | 8034 | `75959c7f060d` | **обрезан** |
| повестка | `docs/meeting/static-mmbrn-container/M0_AGENDA.md` | 5627 · 1 п. | `237c7297f038` | полностью |

> ⚠ Часть входа не доехала до комнаты целиком. Утверждения протокола о своём входе
> сверять с этой таблицей, а не с текстом реплик.

**Условия прогона:** `--no-context --no-rag --no-memory --min-replies 30 --seed 7`

**Вопрос:**

Ответьте только на P1 из повестки M0: постройте причинно полный минимальный DAG и детерминированный порядок семи вопросов. Не решайте существо M1-M7. Выполните все ограничения повестки, перечислите явные посылки для каждого оставленного ребра и оставьте владельческую ратификацию ожидающей.

---

# Консилиум: M0 / P1 — порядок семи вопросов static-mmbrn-container

**Повестка:** Построить причинно полный минимальный DAG (транзитивная редукция) и детерминированный линейный порядок семи вопросов M1–M7. Существо вопросов не решать. Список посылок обязателен. Владельческая ратификация ожидается.

**Участники:** Музыкант (Kuryokhin), Архитектор (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Верстальщик (Rodchenko), Teamlead (Tarasov).

**Порядок реплик:** Музыкант → Архитектор → Структурщик → Математик → Верстальщик → Teamlead (циклически, ≥30 реплик)

---

[Музыкант]: Прежде чем мы тронемся, замечу очевидное: мы не трогаем содержание вопросов, только их порядок. Мне как человеку, привыкшему выстраивать цепочки обработки сигнала, очевидно: нельзя настроить ingest (вопрос 6), не зная, что считать оригиналом (вопрос 1). И нельзя ничего переезжать (вопрос 7), не зная, куда это класть (вопрос 4). Это мой первый срез зависимостей — по принципу «нет склада, нет потока».

[Архитектор]: Согласен с общим направлением, но нужна точность. Граница контейнера (1) — это несущая конструкция: без неё не определить, что вообще является предметом тождества (2), хранения (4), доступа (3) и роли Affine (5). Поэтому 1 — источник, от него идут рёбра. Теперь про `2→3`: шестой прогон был отклонён именно за то, что доступ «к конкретному материалу» предполагает уже выбранную гранулярность документа. Нам нужно другое основание для `2→3` или его надо опустить. Я пока воздерживаюсь от вывода — передаю Структурщику.

[Структурщик]: Разберём по словарю. Вопрос 2 — это хеш, адрес и провенанс; вопрос 3 — источник решений по доступу, Panel grants, Affine roles, серверный гейт. Я смотрю: является ли тождество (2) необходимым условием доступа (3) при любом допустимом ответе на вопрос 3? Нет. Разделовый или контейнерный доступ работает без знания тождества конкретного документа — это прямой контрпример. Значит, `2→3` нельзя оставить без иного основания. Я его не вижу. Ребро `2→3` опускаем.

[Математик]: Проверю численно, то есть структурно. Зависимости, которые я вижу как необходимые:
- `1→2`: не определить хеш и адрес, не зная, что является единицей контейнера.
- `1→4`: физический склад держит объекты контейнера; capacity quota определяется классами оригиналов.
- `1→5`: контракт Affine как поверхности определяется тем, что граница контейнера разрешает или запрещает ей держать.
- `2→5`: тождество материала (хеш, провенанс) необходимо для определения того, что именно Affine может держать или не держать — это основание, не зависящее от гранулярности доступа.
- `3→6`: решения по доступу (гранулярность, роли, гейт) определяют правила ingest и выдачи.
- `4→6`: физический склад и quota определяют допустимые операции ingest.
- `5→6`: роль Affine — это поверхность приёма и выдачи, её контракт прямо входит в вопрос 6.
- `6→7`: переезд и доставка — это перемещение того, что уже принято и проиндексировано.
- `5→7` — нужно проверить: путь `5→6→7` существует, значит прямое `5→7` было бы транзитивным. Но повестка требует явно: «если ребра `5→6` нет, требуется прямое `5→7`». Поскольку `5→6` есть, прямое `5→7` не нужно и было бы нарушением транзитивной редукции.

[Верстальщик]: Визуально проверю структуру: у нас намечается граф с одним источником (1) и одним стоком (7). Промежуточный слой — 2, 4, 5 — питается от 1. Затем 3 должен откуда-то получить вход. Подождите: от кого зависит 3 (доступ)? От 1 (граница контейнера задаёт предмет управления доступом). Но тогда `1→3` — транзитивно ли оно? Нужно проверить: есть ли путь `1→…→3` через другой узел. Путей `1→2→3` нет (мы удалили `2→3`). Путей `1→4→3` нет. Путей `1→5→3` нет. Значит `1→3` — не транзитивное, его можно оставить. Это отличие от первого прогона, где `1→3` было транзитивным через `1→2→3`.

[Teamlead]: Стоп. Первый прогон был отклонён именно за `1→3` — но там оно было транзитивным потому, что существовало `1→2→3`. Сейчас `2→3` убрано. Если пути `1→…→3` через другой узел нет, ребро `1→3` допустимо. Верстальщик прав, но это надо зафиксировать как явную посылку, а не оставить в тексте. Продолжаем: нужно проверить, есть ли ребро `2→6`. Вопрос 6 включает хеширование при ingest — это прямая ссылка на тождество (2). Математик, это самостоятельное ребро или оно покрывается путём `2→5→6`?

[Музыкант]: Хорошее замечание. Хеширование в ingest (вопрос 6) — это применение схемы тождества из вопроса 2. Если путь `2→5→6` существует, то прямое `2→6` было бы транзитивным. Но посмотрим: `2→5` означает, что тождество нужно для определения роли Affine, а `5→6` — что роль Affine входит в ingest. Хеширование в ingest — это не следствие роли Affine, это прямое применение схемы тождества. Значит `2→6` — самостоятельное ребро, не покрываемое путём `2→5→6`.

[Архитектор]: Соглашусь. Путь `2→5→6` не проводит транзитивно зависимость «схема хеширования → процедура ingest»: Affine-контракт (5) — промежуточный, а не передаточный для хеш-схемы. Ребро `2→6` остаётся. Теперь: есть ли `4→7`? Переезд (7) включает инвентарь и физическое перемещение данных. Хранение и живучесть (4) — это quota, backup, физический склад назначения. Переезд без решения по хранению физически запрещён по фактуре («перенос байтов до решения хранения запрещён»). Значит `4→7` — необходимое ребро. Покрывается ли оно путём `4→6→7`? `4→6` есть, `6→7` есть — значит `4→7` транзитивно. Убираем.

[Структурщик]: Подтверждаю: путь `4→6→7` существует, прямое `4→7` избыточно. Теперь проверю `1→6`: граница контейнера определяет классы оригиналов — это входит в правила ingest (чувствительность, допустимые типы). Покрывается ли путём? Путь `1→3→6` существует (если `1→3` есть и `3→6` есть). Значит `1→6` транзитивно через `1→3→6`. Убираем `1→6`.

[Математик]: Проверю ещё `1→7`: граница контейнера определяет предмет переезда. Путь `1→…→7`: `1→4→6→7` существует. Транзитивно. Убираем. И `3→7`: доступ влияет на переезд (Panel, Issues упомянуты в вопросе 7). Путь `3→6→7` существует. Транзитивно. Убираем. И `2→7`: тождество нужно для инвентаря переезда. Путь `2→6→7` существует. Транзитивно. Убираем.

[Верстальщик]: Итак, кандидатный набор рёбер после всех проверок:
- `1→2` ✓
- `1→3` ✓ (прямое, не транзитивное — путей через другой узел нет)
- `1→4` ✓
- `1→5` ✓
- `2→5` ✓
- `2→6` ✓
- `3→6` ✓
- `4→6` ✓
- `5→6` ✓
- `6→7` ✓

Проверю `1→5` на транзитивность: путь `1→2→5` существует. Значит `1→5` — транзитивное. Убираем.

[Teamlead]: Верно. `1→2→5` — путь через узел 2. Прямое `1→5` избыточно. Убираем. Итоговые рёбра: `1→2`, `1→3`, `1→4`, `2→5`, `2→6`, `3→6`, `4→6`, `5→6`, `6→7`. Проверим ещё раз `1→3` на транзитивность при новом составе. Пути `1→2→3`? Нет (`2→3` убрано). `1→4→3`? Нет. `1→5→3`? Нет (нет ребра `5→3`). Прямое `1→3` остаётся. Хорошо. Теперь строим линейный порядок.

[Музыкант]: Для топологической сортировки нахожу множество узлов без входящих рёбер на каждом шаге.

Шаг 1: входящих рёбер нет только у **1**. Единственный выбор — 1. Ставим 1.

[Архитектор]: Шаг 2: убираем 1 и все его рёбра (`1→2`, `1→3`, `1→4`). Узлы без входящих рёбер: **2, 3, 4**. Три кандидата. Нужен tie-break.

[Структурщик]: Критерий tie-break: выбираем узел с наибольшим числом путей до стока (7) — то есть наибольшей «нагруженностью» как предшественника. Узел 2 имеет исходящие рёбра `2→5` и `2→6`; узел 3 — `3→6`; узел 4 — `4→6`. Узел 2 разблокирует 5, что разблокирует 6, что разблокирует 7 — длиннейшая цепочка от 2 до 7 = 3 шага (`2→5→6→7`). От 3 до 7 = 2 шага (`3→6→7`). От 4 до 7 = 2 шага (`4→6→7`). Узел 2 имеет наибольшую глубину пути до стока. Ставим 2.

[Математик]: Шаг 3: убираем 2 и его рёбра (`2→5`, `2→6`). Узлы без входящих рёбер: **3, 4** (у 5 осталось ли входящее ребро? Рёбра в 5: было `1→5` — убрано, `2→5` — только что убрано. Входящих рёбер у 5 больше нет! Добавляем 5 к доступным). Доступны: **3, 4, 5**. Tie-break по глубине пути до стока: от 3 — 2 шага; от 4 — 2 шага; от 5 — 2 шага (`5→6→7`). Глубины равны. Содержательного различия нет.

[Верстальщик]: При равной глубине применяем детерминированную конвенцию: выбираем узел с наименьшим номером. Ставим **3**.

[Teamlead]: Шаг 4: убираем 3 и его рёбра (`3→6`). Входящие у 6: осталось `2→6` (убрано вместе с 2 на шаге 3), `4→6`, `5→6`. Значит у 6 два входящих ребра — не доступен. Доступны: **4, 5**. Tie-break: от 4 до 7 = 2 шага (`4→6→7`); от 5 до 7 = 2 шага (`5→6→7`). Равная глубина. Содержательного различия нет. Конвенция: наименьший номер — **4**.

[Музыкант]: Шаг 5: убираем 4 и его рёбра (`4→6`). Входящие у 6: осталось `5→6`. У 5 нет входящих рёбер (проверено на шаге 3). Доступны: **5** (6 ждёт `5→6`). Единственный выбор — **5**.

[Архитектор]: Шаг 6: убираем 5 и его рёбра (`5→6`). Входящих рёбер у 6 больше нет. Доступны: **6**. Единственный выбор — **6**.

[Структурщик]: Шаг 7: убираем 6 и его рёбра (`6→7`). Доступен **7**. Единственный выбор — **7**.

Итоговый порядок: **1 → 2 → 3 → 4 → 5 → 6 → 7**.

[Математик]: Проверяю корректность полученного порядка против всех рёбер DAG:
- `1→2`: 1 перед 2 ✓
- `1→3`: 1 перед 3 ✓
- `1→4`: 1 перед 4 ✓
- `2→5`: 2 перед 5 ✓
- `2→6`: 2 перед 6 ✓
- `3→6`: 3 перед 6 ✓
- `4→6`: 4 перед 6 ✓
- `5→6`: 5 перед 6 ✓
- `6→7`: 6 перед 7 ✓

Все рёбра соблюдены. Проверяю требование повестки: «финальный переезд 7 не может обходить результат о роли Affine 5». Путь `5→6→7` существует — Affine (5) является предшественником переезда (7) через 6. Прямое `5→7` не требуется. ✓

[Верстальщик]: Проверяю транзитивную редукцию финального набора — нет ли ребра, достижимого через третий узел:
- `1→2`: иных путей `1→…→2` нет ✓
- `1→3`: пути `1→2→3` нет, `1→4→3` нет, `1→5→3` нет ✓
- `1→4`: пути `1→2→4` нет, `1→3→4` нет ✓
- `2→5`: путь `2→…→5` только через несуществующие рёбра ✓
- `2→6`: путь `2→5→6` существует — **требует проверки**.

[Teamlead]: Стоп. Верстальщик поймал критическую точку. Путь `2→5→6` существует — значит `2→6` транзитивно! Это надо пересмотреть. Музыкант, твой аргумент был: хеширование в ingest — прямое применение схемы тождества, не выводимое через роль Affine. Но для транзитивной редукции достаточно существования пути, а не смысловой связи. Ребро `2→6` нарушает редукцию при наличии пути `2→5→6`. Убираем `2→6`.

[Музыкант]: Принимаю. Смысловой аргумент не отменяет структурное правило транзитивной редукции. Убираем `2→6`. Но тогда нужно перепроверить: через что тождество (2) достигает ingest (6)? Через `2→5→6`. Это достаточно для причинной полноты: хеш-схема нужна Affine-контракту, а тот входит в ingest.

[Архитектор]: Финальный набор рёбер после удаления `2→6`:
`1→2`, `1→3`, `1→4`, `2→5`, `3→6`, `4→6`, `5→6`, `6→7`.

Проверяю транзитивную редукцию ещё раз по всем рёбрам:
- `1→2`: путей `1→…→2` через другой узел нет ✓
- `1→3`: путей `1→…→3` нет (ни `1→2→3`, ни `1→4→3`) ✓
- `1→4`: путей `1→…→4` нет ✓
- `2→5`: путей `2→…→5` через другой узел нет ✓
- `3→6`: путей `3→…→6` без прямого — нет ✓
- `4→6`: путей `4→…→6` без прямого — нет ✓
- `5→6`: путей `5→…→6` без прямого — нет ✓
- `6→7`: путей `6→…→7` без прямого — нет ✓

Транзитивная редукция подтверждена.

[Структурщик]: Проверяю причинную полноту: каждый узел достигает все свои содержательные последствия?
- 1 (граница) → достигает 2 (тождество) прямо, 3 (доступ) прямо, 4 (хранение) прямо, 5 через `1→2→5`, 6 через `1→3→6` и `1→4→6`, 7 через цепочки ✓
- 2 (тождество) → достигает 5 прямо, 6 через `2→5→6`, 7 через длинную цепочку ✓
- 5 (Affine) → достигает 6 прямо, 7 через `5→6→7` ✓

Причинная полнота подтверждена.

[Математик]: Повторяю топологическую сортировку с новым набором рёбер (`2→6` удалено). Входящие рёбра каждого узла:
- 1: нет
- 2: {1}
- 3: {1}
- 4: {1}
- 5: {2}
- 6: {3, 4, 5}
- 7: {6}

Шаг 1: доступен **1** (без входящих). Единственный. Ставим **1**.

Шаг 2: убираем рёбра из 1. Без входящих: **2, 3, 4**. Три кандидата. Tie-break по глубине (длиннейший путь до 7): от 2 — `2→5→6→7` = 3 шага; от 3 — `3→6→7` = 2 шага; от 4 — `4→6→7` = 2 шага. Максимум у **2**. Ставим **2**.

[Верстальщик]: Шаг 3: убираем рёбра из 2. Без входящих: 3 (входящее {1} — убрано), 4 (входящее {1} — убрано), 5 (входящее {2} — убрано). Доступны: **3, 4, 5**. Tie-break по глубине: от 3 — 2 шага; от 4 — 2 шага; от 5 — `5→6→7` = 2 шага. Все равны. Содержательного различия нет. Конвенция: наименьший номер — **3**.

[Teamlead]: Шаг 4: убираем рёбра из 3 (`3→6`). Входящие у 6: {4, 5} — ещё есть. Без входящих: **4, 5**. Tie-break по глубине: обе — 2 шага до 7. Равно. Конвенция: наименьший номер — **4**.

Шаг 5: убираем рёбра из 4 (`4→6`). Входящие у 6: {5}. Без входящих: **5**. Единственный. Ставим **5**.

Шаг 6: убираем рёбра из 5 (`5→6`). Без входящих: **6**. Ставим **6**.

Шаг 7: убираем 6 (`6→7`). Ставим **7**.

Финальный порядок: **1 → 2 → 3 → 4 → 5 → 6 → 7**.

[Музыкант]: Тот же порядок, что и до исправления, — но теперь с корректным DAG. Хорошо. Проверяю требование о Affine: 5 → 6 → 7 — путь существует, 5 является предшественником 7 через 6. ✓

[Архитектор]: Собираю список посылок для протокола. Каждое ребро требует явной посылки:

**`1→2`**: граница контейнера определяет, что является единицей хранения; хеш и адрес (вопрос 2) вычисляются от объектов, определённых границей (вопрос 1). Без решения по границе нет предмета для тождества.

**`1→3`**: граница контейнера определяет предмет управления доступом. Путей `1→…→3` через иной узел нет (ребро `2→3` отсутствует; `4→3`, `5→3` не существуют), поэтому ребро не транзитивное.

**`1→4`**: физический склад, quota и retention (вопрос 4) определяются классами оригиналов и неизменяемостью, которые устанавливает граница контейнера (вопрос 1). Путей `1→…→4` нет.

**`2→5`**: контракт Affine как сменной поверхности (вопрос 5) включает запреты на определённые действия с материалом; различение «что допустимо держать Affine» требует схемы тождества — хеша и провенанса (вопрос 2). Это основание не зависит от ответа на вопрос 3 (гранулярности доступа): Affine может нести запреты на уровне контейнера, коллекции или документа — хеш и провенанс нужны при любом ответе.

**`3→6`**: правила ingest, чувствительность и API выдачи (вопрос 6) определяются источником решений по доступу, гранулярностью, Panel grants и серверным гейтом (вопрос 3). Нельзя реализовать ingest без знания, кто и что вправе принять или получить.

**`4→6`**: операции ingest ограничены quota и физическим складом (вопрос 4); перенос байтов запрещён до решения хранения — это прямая фактурная зависимость. Путь `4→…→6` без прямого ребра не существует (нет `4→3→6` или `4→5→6`).

**`5→6`**: вопрос 6 явно включает роль Affine в ingest и выдаче («Affine roles» упомянуты в описании вопроса 3, но минимальный контракт Affine как поверхности и её запреты — существо вопроса 5, прямо входящего в процедуры приёма и выдачи).

**`6→7`**: переезд и доставка (вопрос 7) оперируют уже принятыми, проиндексированными и выданными объектами; инвентарь для переезда строится на основе ingest-записей (вопрос 6). Перенос байтов следует за решением о процедуре приёма.

[Структурщик]: Фиксирую явно: ребро `2→3` отсутствует. Основание для его отсутствия: разделовый и контейнерный доступ работает без знания тождества конкретного документа — это прямой контрпример обязательности зависимости. Иного основания для `2→3`, которое оставалось бы верным при любом допустимом ответе вопроса 3, не найдено.

[Математик]: Фиксирую: ребро `2→6` отсутствует. Основание: путь `2→5→6` существует, прямое ребро было бы транзитивным — нарушение транзитивной редукции.

[Верстальщик]: Фиксирую: ребро `1→5` отсутствует. Основание: путь `1→2→5` существует, прямое было бы транзитивным.

Фиксирую: ребро `4→7` отсутствует. Основание: путь `4→6→7` существует, прямое было бы транзитивным.

[Teamlead]: Всё проверено. Готов к формированию итогового раздела. DAG минимален, причинно полон, порядок детерминирован. Владельческая ратификация ожидается — в DoD это зафиксировано явно.

---

## Итоговое решение консилиума

### Причинно полный минимальный DAG (транзитивная редукция)

**Рёбра:**

| Ребро | Статус |
|-------|--------|
| `1→2` | ✓ оставлено |
| `1→3` | ✓ оставлено (путей через другой узел нет) |
| `1→4` | ✓ оставлено |
| `2→5` | ✓ оставлено |
| `3→6` | ✓ оставлено |
| `4→6` | ✓ оставлено |
| `5→6` | ✓ оставлено |
| `6→7` | ✓ оставлено |
| `1→5` | ✗ транзитивное: путь `1→2→5` |
| `2→3` | ✗ нет допустимого основания (разделовый доступ — контрпример) |
| `2→6` | ✗ транзитивное: путь `2→5→6` |
| `4→7` | ✗ транзитивное: путь `4→6→7` |
| `5→7` | ✗ не требуется: путь `5→6→7` существует |
| `1→6`, `1→7`, `3→7`, `2→7` | ✗ транзитивные (показано в обсуждении) |

**Схема DAG:**

```
1 ──→ 2 ──→ 5 ──→ 6 ──→ 7
│           ↑    ↑
├──→ 3 ──────────┘
│
└──→ 4 ──────────┘
```

*(1 питает 2, 3, 4; 2 питает 5; 3, 4, 5 питают 6; 6 питает 7)*

---

### Список посылок (обязательный)

| Ребро | Явная посылка |
|-------|--------------|
| `1→2` | Граница контейнера определяет единицу хранения; хеш и адрес вычисляются от объектов, определённых этой границей. Без решения по границе нет предмета для тождества. |
| `1→3` | Граница контейнера определяет предмет управления доступом. Путей `1→…→3` через иной узел нет (ребро `2→3` отсутствует; рёбра `4→3`, `5→3` не существуют). |
| `1→4` | Физический склад, quota и retention определяются классами оригиналов и неизменяемостью, устанавливаемыми границей контейнера. Путей `1→…→4` нет. |
| `2→5` | Контракт Affine включает запреты на действия с материалом; различение допустимого требует схемы тождества (хеш, провенанс). Основание не зависит от ответа на вопрос 3: при любой гранулярности доступа Affine-запреты требуют идентификации материала. |
| `3→6` | Правила ingest, чувствительность и API выдачи определяются источником решений по доступу, гранулярностью, Panel grants и серверным гейтом. Нельзя реализовать ingest без знания, кто и что вправе принять или получить. |
| `4→6` | Операции ingest ограничены quota и физическим складом; фактура явно запрещает перенос байтов до решения хранения. Путей `4→…→6` без прямого ребра нет. |
| `5→6` | Минимальный контракт Affine и её запреты (вопрос 5) прямо входят в процедуры приёма и выдачи (вопрос 6). |
| `6→7` | Переезд оперирует принятыми и проиндексированными объектами; инвентарь для переезда строится на ingest-записях. |
| ~~`2→3`~~ | **Отсутствует.** Разделовый и контейнерный доступ работает без тождества конкретного документа — прямой контрпример. Иного основания, верного при любом ответе на вопрос 3, не найдено. |

---

### Детерминированный линейный порядок

| Шаг | Узел | Ожидает | Доступные на этом шаге | Применённый критерий |
|-----|------|---------|------------------------|----------------------|
| 1 | **1** — Граница контейнера | — | {1} | Единственный без входящих |
| 2 | **2** — Тождество и источник истины | 1 | {2, 3, 4} | Наибольшая глубина пути до стока: 2→5→6→7 = 3 шага; 3 и 4 — по 2 шага |
| 3 | **3** — Доступ | 1 | {3, 4, 5} | Равная глубина (по 2 шага); конвенция наименьшего номера |
| 4 | **4** — Хранение и живучесть | 1 | {4, 5} | Равная глубина (по 2 шага); конвенция наименьшего номера |
| 5 | **5** — Роль Affine | 1, 2 | {5} | Единственный без входящих |
| 6 | **6** — Приём и выдача | 3, 4, 5 | {6} | Единственный без входящих |
| 7 | **7** — Переезд и доставка | 6 | {7} | Единственный |

**Итоговый порядок: 1 → 2 → 3 → 4 → 5 → 6 → 7**

---

### Definition of Done

- [x] DAG содержит ровно 8 рёбер, является транзитивно редуцированным и причинно полным
- [x] Каждое оставленное ребро имеет явную посылку в таблице
- [x] Ребро `2→3` отсутствует с явным обоснованием
- [x] Ребро `2→6` отсутствует с явным обоснованием (транзитивность через `2→5→6`)
- [x] Ребро `5→7` не нужно: путь `5→6→7` существует; требование повестки выполнено
- [x] Каждый шаг линейного расширения при нескольких доступных узлах называет полный набор кандидатов и критерий tie-break
- [x] Равная длина ветвей честно признана равной на шагах 3 и 4
- [x] Ни один грант не привязан к конкретному материалу; гранулярность вопроса 3 не предрешена
- [x] Второй носитель вердикта не создаётся; единственный носитель — инструментальный протокол в `docs/seanses/`
- [x] Восьмой вопрос не введён; кандидаты не объединены; существо не решено
- [ ] **Передано владельцу, ратификация ожидается**

---

*Реплик в диалоге: 36 (шесть советчиков); каждый участник высказался не менее одного раза. [Ангелина] не вступала — опровергать нечего. [Фаррелл] не добавлялся локальной стороной.*
