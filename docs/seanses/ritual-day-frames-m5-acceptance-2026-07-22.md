<!-- канал: hand — протокол произведён председателем вручную; Anthropic API usage limit до 2026-08-01 UTC; слово владельца 22.07: «проведи руками» -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-22T10:36:00.000Z |
| Команда | hand (председательство; не `yarn consilium`) |
| Модель | — (агент сессии, пять ролей по VIRTUAL_TEAM_PROMPT) |
| Файл | `docs/seanses/ritual-day-frames-m5-acceptance-2026-07-22.md` |
| Порядок ролей | Teamlead → Структурщик → Математик → Музыкант → Верстальщик |
| Повестка | `docs/meeting/ritual-day-frames/M5_AGENDA.md` |

**Вопрос:**

A1 — какова приёмка раскладки утреннего ритуала на фреймы и план миграции на живой ritual-day: что считать DoD заседания, что можно объявить декларативно до зубьев #900, что ждёт validateProcedure/auditPins в main?

---

# Консилиум: приёмка раскладки утра на фреймы (M5 / A1)

**Повестка:** DoD заседания vs живой прогон; что писать в MANIFEST до/после зубьев #927–#929; стоп-критерий; follow-up вне комнаты. Барьер фактуры M0 назван явно.

**Участники:** Teamlead (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Teamlead → Структурщик → Математик → Музыкант → Верстальщик (цикл)

---

[Teamlead]: Рамку фиксирую сразу — A1 легко сползает в код #900. Мы **не** пишем validateProcedure, **не** трогаем kitVersion, **не** пересматриваем M1–M4. Два разных DoD обязаны жить раздельно: (1) раскладка **как решение** заседания — артефакты в `docs/meeting/ritual-day-frames/`; (2) раскладка **жива на прогоне** — только после зубьев #927–#929 в main и исполнения #929. Пока Issues OPEN — второй DoD ложен по построению. Форма следует функции: заседание закрывает смысл очереди; спринт #900 закрывает машину.

[Структурщик]: Начну с термина. «Декларативно в MANIFEST» — омоним. Лемма A: **норма-таблица в EPIC/контейнере заседания** — человекочитаемая очередь, холодная сессия читает без `package.json`. Лемма B: **ключи в живом `docs/procedures/ritual-day/MANIFEST.json`**, которые зуб `validateProcedure` обязан понимать. Пока #927 OPEN, лемма B для `preflight`/`frames`/`post` **запрещена**: лишний ключ сегодня — дефект пятиполёвки Р1 (посылка EPIC `procedure-frames` Ф1: `frames` станет optional только после зуба). Сливать A и B = тихий красный CI или ложно-зелёный.

[Математик]: Формализую стоп-критерии. Пусть `D_meeting` — предикат «решение закрыто»: существуют EPIC + ACTIVE + пять протоколов M0…M5 с вердиктами, таблица очереди и карта holder’ов читаются из контейнера. Пусть `D_live` — «жива на прогоне»: `validateProcedure(ritual-day)` зелёный **с** объявленными ключами очереди ∧ `auditPins` на preflight-дверях ∧ missing = STOP на живом запуске. Замер 22.07: #927–#929 OPEN ⇒ `D_live = false`. `D_meeting` независим от `D_live` и может стать true сегодня. Инвариант: `D_live ⇒ D_meeting`, обратное неверно.

[Музыкант]: Флаг тихой дыры. Если сейчас вписать `frames[]` в живой MANIFEST «чтобы было видно» — получим либо сломанный зуб (лишний ключ), либо мёртвую дверь (ключ есть, аудита нет). Это ровно антипаттерн «магическая кнопка»: экран врёт, что очередь живая. Требую: до #927–#928 в main — **ноль** новых ключей очереди в живом MANIFEST. Видимость — только в контейнере заседания и EPIC. Ласточка/post остаются ручными (M2) — не затаскивать в автопрогон под видом «миграции».

[Верстальщик]: Со стороны потребителя — холодная сессия и человек в PR. DoD заседания на холсте = один файл EPIC с таблицей `preflight / frames / post`, holder, классом пина (M4), ссылками на протоколы. Без раскопок по пяти seanses. Бейдж статуса обязателен: `решение ✅` / `прогон ⏳ ждёт #900`. Иначе читатель примет раскладку за уже подключённый ритуал — нарушим `main-day-accuracy`.

[Teamlead]: Принимаю разведение `D_meeting` / `D_live`. Тогда миграция в три слоя: **слой 0 (сейчас, заседание)** — EPIC + таблицы в контейнере, живой MANIFEST не трогаем. **Слой 1 (#927 в main)** — optional `frames` (и согласование носителей `preflight`/`post` из M2 с контрактом Ф1 — шов, не новый зуб здесь). **Слой 2 (#928+#929)** — segmentHash/auditPins + wiring на прогоне. Этот заседание пишет только слой 0 и называет 1–2 follow-up’ами.

[Структурщик]: Шов назву явно, иначе M2 столкнётся с Ф3 `procedure-frames`. EPIC #900 / вердикт Ф3 сказал: `morning-wiring` = `frames[0]` в ritual-day. Вердикт M2 этого заседания: wiring ∈ `preflight`, ∉ `frames`. Это **не пересмотр дверей m3** (состав и holder ozhegov стоят), а уточнение **носителя ранга**. Follow-up исполнения #929: реализовать wiring как preflight-гейт (норма M2), обновив проекцию Ф3 «frames[0]» → «preflight[0] / ключ preflight» без переоткрытия трёх дверей. Заседание `ritual-day-frames` не пересобирает зуб — передаёт шов в фазу #900.

[Математик]: Предикат запрета до зубьев: `writeLive(keys) = false` пока `¬inMain(#927) ∨ ¬inMain(#928)` для машинно-проверяемых pin; для одной структуры ключей достаточно #927, для audit STOP на дверях — #928∧#929. Декларация `range` без хэша в **живом** MANIFEST тоже запрещена: M4 разрешил декларацию как норму в артефакте заседания, не как полуживой JSON, который зуб ещё не понимает. Иначе получим артефакт «почти контракт» без валидатора — худший класс.

[Музыкант]: Поддержу запрет полуживого JSON. И флаг на post: `swallow-send` / `hermes-brief` в DoD_live не обязаны входить в авто `ritual:day` — M2 оставил их ручными. Приёмка «жива на прогоне» = preflight+frames на автоцепочке; post — отдельный чеклист «после артефакта, руками holder angelina». Не смешивать зелёный CI утра с фактом отправки ласточки.

[Верстальщик]: Тогда в EPIC две таблицы приёмки. Таблица A — DoD заседания (чекбоксы артефактов контейнера). Таблица B — DoD прогона (чекбоксы #927/#928/#929 + «post вручную»). Читатель видит A зелёной при ратификации владельцем; B — серой до мерджа. Одна полоса «всё готово» запрещена.

[Teamlead]: Стык `preflight`/`frames`/`post` с контрактом #900 — норма потребления, не новый дизайн зуба. Ф1 дал optional `frames` с элементом `{id, holder, pin?}`. M2 добавил два соседних ключа-носителя порядка. Follow-up к #927: либо расширить optional-схему тремя ключами (`preflight`, `frames`, `post`) одной поправкой валидатора, либо временно держать preflight/post как документированную проекцию над `frames[]` с маркером роли — **выбор реализации в #900**, ниже этой комнаты; эта комната требует: порядок M2 не потерять при имплементации.

[Структурщик]: Согласен: комната не выбирает синтаксис JSON для трёх ключей — только инвариант порядка `eval(preflight) ≺ eval(frames) ≺ hint(post manual)`. Словарь: раскладка = очередь с лицами и классами pin; миграция = перенос раскладки в машину #900 без дубля whole-file (M4).

[Математик]: DoD_meeting перечисляю проверяемо: (1) `MEETING_BRIEF` + `MEETING_ACTIVE` + повестки M0–M5; (2) протоколы seanses M0–M5 с вердиктом и «Список посылок»; (3) `EPIC.md` со сводной таблицей очереди (id, носитель, holder, класс pin A/B/C/G из M4), стоп-критериями, follow-up #900; (4) ратификация порядка M0 владельцем (факт) + ратификация EPIC владельцем (после этой комнаты). Без (3)–(4) `D_meeting` не закрыт.

[Музыкант]: Follow-up вне заседания, без нового скоупа кода здесь: Issue #927 — схема ключей очереди; #928 — auditPins/segmentHash; #929 — morning-wiring + гейт утра с учётом шва preflight; не открывать отдельный day-sprint «раскладка утра» — это и есть потребление #900. Если понадобится seed-комментарий на #900/ #929 про шов M2↔Ф3 — одной репликой после ратификации EPIC, не код.

[Верстальщик]: Принимаю. На холсте EPIC сверху — статус «решение / прогон», ниже очередь, ниже follow-up со ссылками на Issues. Холодная сессия открывает EPIC и видит сценарий утра целиком — T6 выполнен на уровне решения.

[Teamlead]: Свожу. DoD заседания = контейнер + EPIC + ратификация владельцем; живой MANIFEST до #927–#929 **не трогаем**; `D_live` отделён; шов preflight vs Ф3 frames[0] — follow-up #929; post остаётся ручным. Все роли — принимаете?

[Структурщик]: Принимаю. Леммы A/B разведены; живой MANIFEST до зубьев не загрязняем.

[Математик]: Принимаю. `D_meeting` ⟂ `D_live` при ложном `D_live`; инварианты проверяемы.

[Музыкант]: Принимаю. Нет полуживого JSON, нет автопуска post, шов назван вслух.

[Верстальщик]: Принимаю. Две таблицы приёмки на холсте — честно.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| DoD **заседания** (`D_meeting`) | Контейнер `docs/meeting/ritual-day-frames/` несёт BRIEF, ACTIVE, M0–M5 agendas, ссылки на 6 протоколов; **`EPIC.md`** со сводной очередью (носитель · id · holder · класс pin), стоп-критериями и follow-up; холодная сессия читает очередь из EPIC без раскопок в `package.json`. Закрытие — **ратификация EPIC владельцем**. |
| DoD **прогона** (`D_live`) | Отдельный: #927 + #928 + #929 в main; preflight-гейт на старте `ritual:day`; frames автоцепочки валидны; post (`swallow-send`, `hermes-brief`) — ручной чеклист после артефакта, не часть авто-зелёного. Сейчас `D_live = false` (Issues OPEN). |
| Что писать в живой MANIFEST **сейчас** | **Ничего** из `preflight` / `frames` / `post` / полудекларативных pin. Живой `ritual-day/MANIFEST.json` не меняется этим заседанием. |
| Что можно объявить декларативно | Только в артефактах заседания (EPIC, таблицы) — норма M4 «декларация scope/range». Не в живом JSON до зубьев. |
| Что ждёт #927–#929 | Машинная схема ключей; segmentHash/auditPins; wiring на прогоне; зелёный `validateProcedure` с очередью. |
| Стык M2 ↔ контракт #900 | Инвариант порядка `preflight ≺ frames`, post manual — обязан сохраниться. Синтаксис трёх ключей vs проекция над `frames[]` — **выбор реализации #900**, не этой комнаты. Шов с Ф3 (`frames[0]` → носитель preflight): follow-up Issue #929, двери m3 не переоткрывать. |
| Стоп-критерий | «Раскладка готова как решение» = `D_meeting` ∧ LGTM владельца на EPIC. «Раскладка жива на прогоне» = `D_live`. Не смешивать в один бейдж. |
| Follow-up вне заседания | #927 схема; #928 auditPins; #929 wiring+гейт с швом preflight; опционально seed-комментарий на #900/#929 после ратификации EPIC. Нового day-sprint и прод-кода здесь нет. |

**Definition of Done (A1):**
- Зафиксированы два предиката приёмки: `D_meeting` и `D_live`, с явным `D_live=false` на замере 22.07.
- Запрет записи ключей очереди в живой MANIFEST до #927–#929 назван нормой вердикта.
- Шов M2↔Ф3 (preflight vs frames[0]) назван follow-up #929 без пересмотра дверей.
- Перечень артефактов DoD заседания включает обязательный `EPIC.md`.
- Follow-up ограничен Issues #927–#929 (+ опциональный seed); скоуп кода #900 не открыт в этой комнате.

## Список посылок

- **факт** — Issues #927, #928, #929 OPEN; код зубьев очереди/`auditPins` в `origin/main` отсутствует (замер 22.07).
- **факт** — Живой `docs/procedures/ritual-day/MANIFEST.json` без `frames` / `preflight` / `post`.
- **факт** — Вердикты M1–M4 этого заседания (инвентарь, preflight/frames/post, holders, граница кит↔кадр).
- **факт** — EPIC `procedure-frames` Ф1: `frames` — optional поверх Р1 после зуба; Ф3: wiring holder ozhegov, три двери; формулировка дома «frames[0]».
- **факт** — M0: кандидат 5 заблокирован фактурой #900 до разрешимости `D_live`.
- **факт** — Канал прогона: hand по слову владельца; Anthropic usage limit до 2026-08-01 UTC.
- **норма** — M0 порядок `1→{2,3,4}→5` ратифицирован владельцем 22.07.
- **норма** — BRIEF: не реализовывать зубы #900 в заседании; потреблять контракт.
- **норма** — M4: декларация pin без segmentHash допустима как норма артефакта, не как полуживой MANIFEST.

---

*Реплик в диалоге: 20; каждый участник высказался не менее одного раза.*
*Провенанс: ручной прогон председателя; не выдавать за `yarn consilium` / канал llm.*
