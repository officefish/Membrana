# Сборка председателя — M3 «Регистрация в кабинете по промокоду панели», порядок гашения и режим отказа, прогон 1

> Основание: протокол `docs/seanses/cabinet-registration-promo-m3-order-failure-2026-09-21.md` (42 реплики, 6 ролей по 7, модель `xai/grok-4.5`) и аудит `docs/seanses/cabinet-registration-promo-m3-audit-2026-09-21.md` (PR #2382): PASS по пунктам 1, 2, 3, 4, 5, 7, 8; **FAIL по пункту 6, формально** — заголовок раздела «## Definition of Done» (стр. 220) латиницей; повестка M3 (требование 4) назвала ровно это провалом формы по уроку M2.
> Председатель сверил до сборки: `sed -n '220p'` протокола → `## Definition of Done`; реплики стр. 56, 99, 109 прочитаны целиком; ствол: `createSlidingWindowLimiter` определена только в `packages/background-office/src/modules/panel-auth/panel-auth-core.ts:237`, в `packages/background-cabinet` её нет (`git grep` → пусто); `auth.controller.ts:21–23` — `@Post('register')` без `@HttpCode`, значит успех сегодня — 201.

## Решение комнаты — по существу без изменений; одна словарная правка названа

Сборка **не воспроизводит** таблицу решения (стр. 177–195), таблицу «Расширение границ сессии C (явно)» (стр. 197–206), карту двери (стр. 208–216) и определение готовности (стр. 220–228, семь пунктов): они действуют в протоколе как есть. Аудит пунктов 2 и 8 — PASS: контракт назван с числами, расширение границ C комната вынесла сама.

Одна правка **только словаря**, по пункту 6 аудита: заголовок раздела (стр. 220) «Definition of Done» читать как **«Определение готовности»**. Семь пунктов под ним — без изменений. Замечания аудита к заголовкам строк с латиницей вне кавычек («Компенсация A: redeem ok, create fail», стр. 183; «refused / локальный отказ / orphan create», стр. 214; «ok + create», стр. 216; «DTO», стр. 192) и к опечатке «Гашение с стороны кабинета» (стр. 210) — сборка не правит: это идентификаторы исходов и полей контракта, они читаются в задании как имена; опечатка не меняет смысла.

## Пункт 8 — PASS, с одним замечанием аудита для ратификации

Комната сама вынесла расширение границ C: *[Teamlead], стр. 56: «Фиксирую расширение границ: `auth.dto.ts` и `auth.module.ts` — в зоне C этого вердикта, потому что без DTO кода и без DI моста сессия C не исполнит контракт. Аудитор M0 их не назвал — комната называет.»*; *[Структурщик], стр. 109: «Подключение клиента: `auth.module.ts` импортирует `OfficeRegistrationModule` (M2); `AuthService` получает `OfficeRegistrationBridgeService` (или как назван провайдер M2) через constructor DI. Это расширение границы C — явно.»* Границы председателем не двигаются; расширение на `auth.dto.ts` и `auth.module.ts` — решение владельца при ратификации, как в M1 и M2.

**Замечание аудита, не провал — ограничитель.** *[Teamlead], стр. 99: «Утилиту sliding window — только если уже есть в cabinet packages; иначе тонкий private helper в модуле auth, без новых пакетов. Не тащить panel-код кросс-приложением.»* По стволу первого условия нет: ограничитель скользящего окна определён только в офисе (`panel-auth-core.ts:237`), в кабинете его нет. Значит вторая ветвь реплики неизбежна: **новый файл-помощник ограничителя внутри `packages/background-cabinet/src/modules/auth/`** (имя — за сессией) и его тест. Комната ветвь произнесла, файл в таблицу расширения не внесла. Председатель предлагает владельцу при ратификации включить этот файл в расширение C вместе с `auth.dto.ts` и `auth.module.ts`; ни один путь не пересекается с A, B, D.

## Замечания аудита, которые уходят в задание C как факты ствола (решение не трогают)

- **Код успеха.** Вердикт: «успех как сегодня» (стр. 185); в стволе `auth.controller.ts:21` — `@Post('register')` без `@HttpCode` → 201 с телом сессии. Задание C фиксирует 201, контракт сессии не меняется.
- **Помощник ограничителя** — см. выше: ключ адрес, окно 600000 мс, порог 10, ответ 429 «Too many requests»; удар в `auth.controller.ts` до сервиса; код панели офиса не импортировать.
- **Порядок как список** (стр. 181): выключено (`ALLOW_REGISTRATION === false` **или** `office === null`) → 401 «Registration is disabled»; ограничитель → 429; длины логина и пароля; `code = trim(code)`, пустой или длиннее 128 → 403 без офиса; логин занят → 403 без офиса; хеш; `redeemRegistrationCode(code)`; только при `kind === 'ok'` → `user.create` и сессия.
- **Полуудача A** (код погашен, создание упало): наружу 403 «Registration was not accepted», лог уровня error `registration_redeem_orphaned` с логином, кодом и ошибкой базы; отката гашения нет; разбор — руками через панель. **Полуудача B** (исход гашения неизвестен): создание не вызывается, 503 «Please try again later», лог warn `registration_redeem_outcome_unknown`.
- **Мембрана** на регистрации не создаётся (лениво при первом входе, `membrane.service.ts:157`); тарифный `PromoCode` кабинета и `redeemPromo` не трогать.
- **Тесты C** — новые файлы `modules/auth/*.test.ts` с подменным клиентом M2 (не сетью): все четыре исхода, оба условия выключения, 429, гонка одним кодом `maxUses=1`, гонка одним логином, лог полуудачи A, `reason` не в теле ответа.

## Аудит сборки

Тем же аудитором (S-M5, сессия Г), только чтение: пункт 6 — одна словарная правка названа, семь пунктов готовности не изменены; сборка не воспроизводит и не меняет ни таблицу решения, ни таблицу расширения, ни карту двери; цитаты стр. 56, 99, 109 дословны и у заявленных ролей; по ограничителю председатель ничего не решил сам — предложение владельцу названо предложением. Ратификация M3 и границы C — слово владельца.
