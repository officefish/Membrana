<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-08-01T08:04:19.613Z |
| Команда | `yarn consilium` |
| Модель | anthropic/claude-sonnet-4-6 |
| Файл | `docs/seanses/evening-review-predicate-m4x-session-archive-2026-08-01.md` |
| Порядок ролей | Teamlead → Верстальщик → Музыкант → Структурщик → Архитектор → Математик |
| Повестка | `docs/meeting/evening-review-predicate/M4x-topic.md` |

**Вход сеанса** (что комната действительно получила):

| Вход | Носитель | Размер | Отпечаток | Доставка |
|------|----------|-------:|-----------|----------|
| повестка (эхо в голове) | `docs/meeting/evening-review-predicate/M4x-topic.md` | 8695 | `de74a72478d2` | полностью |
| инструкция консилиума | `docs/prompts/CONSILIUM_PROMPT.md` | 4494 | `b6bd1a8dc127` | полностью |
| координация ролей | `docs/VIRTUAL_TEAM_PROMPT.md` | 8034 | `75959c7f060d` | **обрезан** |
| повестка | `docs/meeting/evening-review-predicate/M4x-topic.md` | 8695 · 1 п. | `de74a72478d2` | полностью |

> ⚠ Часть входа не доехала до комнаты целиком. Утверждения протокола о своём входе
> сверять с этой таблицей, а не с текстом реплик.

**Вопрос:**

W1 — Что из архива сессий встаёт в голову вечера, чем на него опирается аудит дня, и как при этом соблюдается требование 3 из #569? Комната образована поправкой №1: вершины (4) и (5) DAG слиты, потому что обе стоят на одном хранилище. ДВА ПРОШЛЫХ ПРОГОНА ВЕРШИНЫ (4) ПРИЗНАНЫ НЕДЕЙСТВИТЕЛЬНЫМИ: первый пал на посылке «органа архивирования НЕТ», второй на зеркальной «органы функционально ГОТОВЫ» — готовность выведена из молчания повестки. ПРАВИЛО ЭТОЙ ПОВЕСТКИ: ниже дан факт УСТРОЙСТВА, а не существования; выводить готовность из умолчания ЗАПРЕЩЕНО; чего не хватает — говорить прямо. ИНТЕРФЕЙСЫ ДОСЛОВНО ИЗ КОДА: sessions:scan [--dir <путь>] [--markers "a,b,c"], exit 0 или 2 (каталог не найден/usage), выход — строки в stdout, МАШИНОЧИТАЕМОГО ВЫХОДА НЕТ; sessions:extract <sessionId|файл> <маркер> [--max N] [--full] | --from ISO [--to ISO] [--role user|assistant], exit 0 или 2, БЕЗ аргументов печатает usage и возвращает 2 ВСЕГДА; archivarius ingest --file <transcript.jsonl> [--out spans.jsonl] [--session-id id] | ingest --from ~/.claude/projects [--out] | audit --index | decompose --index --by sessions|days|actors | inspect <sessionId> --index, БЕЗ подкоманды printHelp и exit 0 (зелёный no-op), ingest БЕЗ --out пишет корпус спанов в stdout. ТРАКТА МЕЖДУ НИМИ НЕТ: extract не читает выход scan (резолвит цель по позиционному аргументу), archivarius ingest не читает выход extract (сам обходит каталог) — три независимые точки входа; связывание тракта заведено карточкой archivarius-evening-tract и ЭТОЙ КОМНАТЕ НЕ ПРИНАДЛЕЖИТ. СХЕМА ШАГА: поле script (строка вида "scripts/bridge.mjs close", аргументы внутри строки), раннер делает spawnSync('node',[file,...args]); поля command схема НЕ ЗНАЕТ; validateManifest требует whyNoncritical у КАЖДОГО noncritical (по умолчанию critical). Оба прошлых вердикта предписали неисполнимое именно из-за незнания схемы. ФАКТИЧЕСКАЯ НУМЕРАЦИЯ 16 шагов: 6 team-memory-report noncritical, 10 code-review critical, 11 archive-code-review critical, 12 day-memo noncritical, 13 audit-evening noncritical, 14 deps-watch noncritical, 15 evening-tail critical, 16 deliver-to-main critical; вердикт M2 в файл НЕ перенесён и дословно неисполним — говорить о ФАКТИЧЕСКОЙ нумерации. #1229 И ГРАНИЦА: Archivarius это #1330 эпик #1229, источник байтов MongoDB в office-стеке, репозиторий — НОТАРИУС (держит контракт, мастерскую, снимки, но не становится базой полных сессий), несущий контракт span://<sessionId>/<uuid>, секретная граница secret-redact; адресуемый отрезок архив ЕЩЁ НЕ ОТДАЁТ — это и есть условие, в котором аудит обязан на него опираться. ТРЕБОВАНИЕ 3 из #569 (Issue ОТКРЫТ) дословно: «В память попадает только то, что уже показано партнёрам. ЦЕЛЬ — исключить заражение памяти невычитанным/ложным содержимым» — норма по СВОЙСТВУ СОДЕРЖИМОГО, не по имени хранилища. Проверено по коду: telegram-swallow.mjs в ветке delivered вызывает persona-memory-extract --all, то есть gate стоит там; secret-redact внутри архивариуса решает конфиденциальность, а не вычитанность — предикаты ортогональны. ОГОВОРКА: вывод «Archivarius не нарушает #569» держится на звене «persona-memory:extract не читает архив спанов» — сегодня истинно, но СЛУЧАЙНО: карточка эпика #539 планирует ровно такой вход; вердикт обязан либо назвать условие своей жизни, либо распространить норму заранее. ПРОЧЕЕ: память пишется и не читается (31.07 записано 90, вытеснено 99); живой audit-evening.mjs потребляет репозиторий, registry.json и truth/registry.json, память сессий НЕ читает вовсе. ПОСЫЛКИ ПРЕДШЕСТВЕННИКОВ: M1 — единица карточка реестра, область определения множество карточек дня, вход тройка, форма boundary/checks/safety; M2 — целевой порядок и критичность; M3 — вечер не мерит объём строк. ТРЕБУЕТСЯ: (а) что из архива встаёт в голову вечера и в каком виде — а если НИЧЕГО не может встать до исполнения карточки тракта, сказать это ПРЯМО, «ничего» есть вердикт если произнесён, а не получен умолчанием; (б) чем аудит опирается на память сессий при неотдаваемом отрезке, или почему не опирается до закрытия #1229; (в) режим требования 3 применительно к архиву сессий по целевой клаузе, с явным условием жизни вывода относительно #539; (г) операциональная форма любой вставки — поле script, whyNoncritical, фактическая нумерация. Остался ОДИН сосед: ворота качества документа — M6, вердикт по нему выносить НЕЛЬЗЯ. Назвать полный список посылок в этом же прогоне; посылки из M1/M2/M3 помечать источником.

---

# Консилиум: M4″ — архив сессий целиком: вершины (4) ∪ (5)

**Повестка:** Что из архива сессий встаёт в голову вечера, чем на него опирается аудит дня, и как при этом соблюдается требование 3 из #569?

**Участники:** Teamlead (Тарасов), Верстальщик (Родченко), Музыкант (Курёхин), Структурщик (Ожегов), Архитектор (Веснин), Математик (Дынин).

**Порядок реплик:** Teamlead → Верстальщик → Музыкант → Структурщик → Архитектор → Математик (циклически, ≥30 реплик)

---

[Teamlead]: Планка матча узкая. Три органа, три независимые точки входа, тракта нет — это факт устройства, не готовности. Прошлые два прогона пали именно потому, что комната скользила мимо устройства к желаемому. Сегодня — сначала ответ по фактуре, потом форма. Вопросы (а), (б), (в), (г) разбираем в порядке зависимости: сначала (а), потому что от него зависит (б), потом (в) с условием жизни, потом (г) — операциональный вывод. Начинаем.

[Верстальщик]: С витрины вечера: если в голову `day-memo` должно встать «что из сессий», а тракта нет — витрина врёт пустотой или фиктивным заполнителем. Правило честной витрины: пустое состояние с причиной лучше декоративного присутствия. Вопрос (а) про то, что именно может физически попасть на вход `day-memo` до закрытия карточки `archivarius-evening-tract`. Без тракта — ничего адресуемого. Это я готова произнести, но пусть Музыкант и Структурщик проверят по коду.

[Музыкант]: По тракту: `sessions:scan` даёт строки в stdout — человекочитаемые, машиночитаемого выхода нет. `sessions:extract` резолвит цель по позиционному аргументу, не читает выход scan. `archivarius ingest` сам обходит каталог, не читает выход extract. Три отдельные шины, не подключённые друг к другу. Без карточки `archivarius-evening-tract` собрать из этих шин единый сигнал на входе `day-memo` невозможно. Это не «органы не готовы» — органы устроены так, что связывания нет. Разница принципиальная.

[Структурщик]: Леммы. **Голова вечера** — входной корпус для `day-memo`. **Тракт** — связанная цепочка вызовов, где выход одного органа становится входом следующего. **Тракта нет** — факт о связях между органами, не о существовании органов. Из трёх органов на вход `day-memo` сегодня может попасть только то, что передаётся явным аргументом. Без тракта передать нечего: у `sessions:scan` нет машиночитаемого выхода, у `archivarius ingest` без `--out` корпус уходит в stdout без адресуемого идентификатора для следующего шага. Вердикт по (а): **ничего** из архива сессий не встаёт в голову вечера до исполнения `archivarius-evening-tract`.

[Архитектор]: Конструкция подтверждает Структурщика. Три точки входа без общего контракта — это не архитектурный изъян, это намеренный разрез: карточка `archivarius-evening-tract` держит долг связывания. Репозиторий — нотариус: держит контракт `span://<sessionId>/<uuid>`, но адресуемый отрезок архив ещё не отдаёт. Нотариус без выданного акта не может стать основанием для чужого шага. Форма вывода по (а) — «ничего» — единственная честная форма при данном устройстве.

[Математик]: Формализую. Пусть `HEAD(day-memo)` — множество входных артефактов шага `day-memo`. Предикат попадания: `∃ artifact: source(artifact) ∈ {sessions:scan, sessions:extract, archivarius} ∧ readable(artifact) ∧ routed(artifact, day-memo)`. Сейчас: `sessions:scan` → stdout, не машиночитаем, `routed = false`; `sessions:extract` → stdout по позиционному аргументу, не связан с `day-memo`, `routed = false`; `archivarius ingest --from ... --out spans.jsonl` → файл есть, но `routed(spans.jsonl, day-memo) = false` до тракта. Вывод: `HEAD(day-memo) ∩ archive_sessions = ∅`. Это не домысел — это следствие устройства.

[Teamlead]: Хорошо. Вопрос (а) закрыт вердиктом: **ничего** из архива сессий не встаёт в голову вечера до исполнения `archivarius-evening-tract`. Произнесено прямо, не получено умолчанием. Теперь (б): чем `audit-evening` опирается на память сессий при неотдаваемом отрезке. Фактура: `audit-evening.mjs` потребляет репозиторий, `docs/tasks/registry.json`, `docs/truth/registry.json` — и не читает память сессий вовсе. Это другая картина, чем (а). Разбираем.

[Верстальщик]: С витрины: `audit-evening` рисует картину дня по трём источникам — репозиторий, реестр задач, граф правды. Память сессий в этой витрине отсутствует не потому, что скрыта, а потому что её нет на входе. Это честная пустота: аудит знает то, что в реестре и репозитории, и не знает того, что осталось только в сессиях. Вопрос (б) — «почему не опирается» — имеет прямой ответ: потому что `audit-evening.mjs` не имеет входа из архива сессий, а адресуемый отрезок ещё не отдаётся.

[Музыкант]: Слышу две разные ситуации. Первая: тракта нет — `audit-evening` не может получить сигнал из сессий технически. Вторая: адресуемый отрезок `span://<sessionId>/<uuid>` не отдаётся архивариусом — даже если тракт появится, `audit-evening` не получит адресуемый фрагмент до закрытия #1229. Это два независимых блокера, не один. Карточка `archivarius-evening-tract` решит первый. Закрытие #1229 нужно для второго. До закрытия #1229 `audit-evening` не опирается на память сессий и не может опираться.

[Структурщик]: Уточняю лемму. **Опора аудита** — предикат: `audit-evening.mjs` читает X как вход и формирует суждение по X. По коду: X = {репозиторий, registry.json, truth/registry.json}. Память сессий ∉ X — это не упущение, это архитектура сегодняшнего дня. Формулирую по (б): `audit-evening` **не опирается** на память сессий. Причина первая — нет тракта (техническая). Причина вторая — нет адресуемого отрезка (контрактная, блокер #1229). Обе причины самостоятельны; устранение одной не снимает другую.

[Архитектор]: Граница нотариуса уточняет (б). Репозиторий держит контракт `span://...` — но держать контракт не значит отдавать отрезок. Нотариус фиксирует факт существования акта, не выдаёт его содержимое. Источник байтов — MongoDB в office-стеке, не репозиторий. Пока MongoDB не отдаёт `span://` через публичный интерфейс (#1229 открыт), `audit-evening` обоснованно работает без этого входа. Это не дефект аудита — это честная граница системы сегодняшнего дня.

[Математик]: Числовая проверка по (б). 31.07: записано 90 записей в память, вытеснено 99. Строка «всплывало сегодня: контур не поставлен» у каждой персоны. Это значит: память пишется, но не читается обратно в вечерний контур. `audit-evening` не читает её — замкнутый цикл без обратной связи. Количественно: информация из 90 записей недоступна аудиту. Это не «почти опирается» — это нулевая связь. По (б): `audit-evening` не опирается на память сессий; блокеров два, они независимы; условие снятия — закрытие #1229 плюс исполнение `archivarius-evening-tract`.

[Teamlead]: Вопрос (б) закрыт. `audit-evening` не опирается на память сессий — ни технически (нет тракта), ни контрактно (нет адресуемого отрезка до #1229). Оба блокера независимы. Теперь (в): режим требования 3 из #569 применительно к архиву сессий. Это самый тонкий вопрос — именно здесь прошлый прогон дал зеркальную ошибку, выведя готовность из молчания.

[Верстальщик]: Требование 3 — норма по свойству содержимого, не по имени хранилища. Витрина вечера: если в память попадает только показанное партнёрам, а gate стоит в `telegram-swallow.mjs` (ветка `delivered`), то архивариус сам по себе этот gate не реализует. `secret-redact` — про конфиденциальность, не про вычитанность. Два ортогональных предиката, не один. Сегодня `persona-memory:extract` не читает архив спанов — значит gate срабатывает до того, как архив мог бы загрязнить память. Это честная картина. Но честная она только пока.

[Музыкант]: Оговорка из повестки — это живой риск, не теоретический. Карточка #539 планирует подключить `~/.claude/projects/*.jsonl` в существующий экстрактор. Если это случится — `persona-memory:extract` начнёт читать архив спанов напрямую, gate из `telegram-swallow.mjs` перестанет покрывать весь поток, и вывод «Archivarius не нарушает #569» протухнет. Вердикт обязан назвать условие жизни вывода, а не просто зафиксировать сегодняшнее состояние.

[Структурщик]: Формулирую условие жизни вывода точно. **Вывод «архивариус не нарушает #569» истинен тогда и только тогда, когда** `persona-memory:extract` не читает архив спанов как входной источник. Сегодня это истинно. При исполнении #539 — перестаёт быть истинным, если норма не распространена заранее. Два варианта ответа для вердикта: (1) назвать условие жизни и оставить вопрос открытым до #539; (2) распространить норму сейчас, добавив к gate #539 явную проверку предиката «показано партнёрам» перед записью в память. Вердикт должен выбрать одно из двух — «зависит» не принимается.

[Архитектор]: По форме — выбираю вариант (1) с явной формулировкой. Вариант (2) требует изменения контракта карточки #539, что выходит за границу этой комнаты. Эта комната может зафиксировать условие жизни, но не переписать контракт чужой карточки. Правильная форма: «вывод действует при условии, что `persona-memory:extract` не читает архив спанов; при исполнении #539 норму требования 3 необходимо распространить явно — это должно войти в DoD карточки #539». Это честная граница: фиксируем долг, не присваиваем чужую работу.

[Математик]: Предикат жизни вывода по (в). Пусть `R3_holds ⇔ ¬reads(persona-memory:extract, spans_archive)`. Сегодня `reads = false`, значит `R3_holds = true`. При исполнении #539: если `reads = true` и gate не расширен, то `R3_holds = false` — заражение памяти невычитанным содержимым становится возможным. Количественный индикатор: сейчас `persona-memory:extract` имеет ровно один источник (показанное партнёрам через `telegram-swallow`). Добавление второго источника без gate меняет предикат. DoD #539 обязан включать: `gate_delivered(span) = true` перед записью в память персоны.

[Teamlead]: Вопрос (в) закрыт. Вывод о соответствии #569 действует при условии `¬reads(persona-memory:extract, spans_archive)`. Условие жизни вывода названо явно. Долг распространения нормы входит в DoD карточки #539 — это фиксируется в вердикте, не в этой комнате исполняется. Теперь (г): операциональная форма вставки. Проверяем по фактической нумерации 16 шагов.

[Верстальщик]: С витрины манифеста: поле `script` — строка с аргументами внутри. Поля `command` нет. `whyNoncritical` обязателен у каждого `noncritical` шага. Фактическая нумерация: шаги 12 (`day-memo`, noncritical), 13 (`audit-evening`, noncritical), 14 (`deps-watch`, noncritical) уже имеют `whyNoncritical`. Если комната предлагает новый шаг — например, вставку между 11 и 12 для связывания тракта — он должен иметь `script: "scripts/archivarius.mjs ingest --from ~/.claude/projects --out spans.jsonl"`, критичность `noncritical` (тракт не готов, блокирует только при наличии), `whyNoncritical: "тракт archivarius-evening-tract не закрыт; отсутствие выхода не блокирует day-memo до #1229"`. Но это шаг для карточки тракта, не для этой комнаты.

[Музыкант]: Стоп. Вставка нового шага в цепочку — это задача карточки `archivarius-evening-tract`. Эта комната не вставляет шаги — она описывает условия, при которых вставка была бы операционально корректной. Форма корректной вставки: поле `script` (строка, аргументы внутри), нет поля `command`, `whyNoncritical` если `noncritical`, позиция в фактической нумерации — между 11 и 12 (после `archive-code-review`, до `day-memo`). Но сама вставка — не наш вердикт, а DoD карточки тракта.

[Структурщик]: Уточняю операциональную форму без выхода за границу комнаты. Если бы шаг существовал (для карточки `archivarius-evening-tract`), его корректная форма по схеме раннера:

```json
{
  "name": "archive-sessions-ingest",
  "script": "scripts/archivarius.mjs ingest --from ~/.claude/projects --out docs/spans/today.jsonl",
  "critical": false,
  "whyNoncritical": "адресуемый отрезок span:// не отдаётся до закрытия #1229; отсутствие spans не блокирует day-memo"
}
```

Позиция в фактической нумерации: между шагом 11 (`archive-code-review`) и шагом 12 (`day-memo`) — то есть стал бы шагом 12, сдвинув текущие 12–16 на одну позицию. Повторяю: это форма для DoD карточки тракта, не вставка этой комнатой.

[Архитектор]: Граница комнаты по (г). Эта комната вправе зафиксировать: операциональная форма любой вставки из архива сессий — поле `script` (строка с аргументами внутри), поля `command` нет, `whyNoncritical` обязателен если `noncritical`, позиция — после шага 11 до шага 12 в фактической нумерации. Фактическая нумерация M2 в манифест не перенесена — говорим о 16 живых шагах, не о целевых. Сама вставка — за рамками; форма вставки — в рамках, потому что (г) требует операциональной формы.

[Математик]: Проверяю корректность формы по (г). Раннер: `spawnSync('node', [file, ...args])`. Строка `"scripts/archivarius.mjs ingest --from ~/.claude/projects --out docs/spans/today.jsonl"` разбивается: `file = 'scripts/archivarius.mjs'`, `args = ['ingest', '--from', '~/.claude/projects', '--out', 'docs/spans/today.jsonl']`. Корректно. `validateManifest` требует `whyNoncritical` у `noncritical` — поле присутствует. `command` отсутствует — соответствует схеме. Фактическая нумерация: 16 шагов, новый встал бы между 11 и 12. Форма операционально корректна.

[Teamlead]: Закрываем круг. Все четыре вопроса разобраны. Формулирую вердикт. По (а): ничего из архива сессий не встаёт в голову вечера до исполнения `archivarius-evening-tract` — тракта нет, машиночитаемого выхода у `sessions:scan` нет, связывание принадлежит карточке тракта. По (б): `audit-evening` не опирается на память сессий — два независимых блокера: нет тракта и нет адресуемого отрезка до #1229. По (в): требование 3 соблюдается при условии `¬reads(persona-memory:extract, spans_archive)` — условие жизни вывода названо, долг в DoD #539. По (г): форма вставки — поле `script`, `whyNoncritical` при `noncritical`, позиция между 11 и 12; сама вставка — за границей комнаты.

[Верстальщик]: С витрины: посылки вердикта должны быть названы в этом прогоне. Проверяю, что все источники явны — посылки из M1, M2, M3 помечены, добор задним числом запрещён. Перечисление посылок — в секции ниже.

[Музыкант]: По памяти команды: 31.07 записано 90, вытеснено 99, контур не поставлен у каждой персоны. Это независимое свидетельство того, что память пишется в никуда — не только тракта нет, но и обратного чтения нет. Аудит вечера работает без этого входа и будет работать без него до двойного закрытия (тракт + #1229). Это не дефект вердикта — это честное описание сегодняшнего дня.

[Структурщик]: Уточняю по оговорке #539. Вердикт этой комнаты не распространяет норму заранее — это за нашей границей. Вердикт фиксирует: при исполнении #539 норма требования 3 должна быть распространена явно; gate `delivered` из `telegram-swallow.mjs` должен покрывать и новый путь через архив спанов. Это входит в DoD #539 как явное условие. Без этого вывод о соблюдении #569 протухает.

[Архитектор]: Финальная архитектурная проверка. Три органа — три независимые точки входа. Нотариус — не база данных, а хранитель контракта. Адресуемый отрезок не отдаётся — это не временная неисправность, это открытый эпик #1229. `audit-evening` честно работает на трёх источниках, не притворяясь, что читает четвёртый. Форма вердикта по устройству, не по желаемому — единственная норма этой комнаты. Вердикт чист.

[Математик]: Финальная числовая проверка. Вопрос (а): `HEAD(day-memo) ∩ archive_sessions = ∅` — доказано. Вопрос (б): `reads(audit-evening, sessions_memory) = false` — доказано по коду. Вопрос (в): `R3_holds = ¬reads(persona-memory:extract, spans_archive)` — условие жизни формализовано. Вопрос (г): форма шага проверена по раннеру. Посылок ровно столько, сколько названо ниже — добора не было.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| (а) Что встаёт в голову вечера из архива сессий | **Ничего.** Тракта между тремя органами нет. `sessions:scan` не даёт машиночитаемого выхода. `archivarius ingest` и `sessions:extract` не связаны с входом `day-memo`. До исполнения карточки `archivarius-evening-tract` голова вечера из архива сессий пуста. Произнесено прямо. |
| (б) Чем `audit-evening` опирается на память сессий | **Не опирается.** Два независимых блокера: (1) нет тракта — технически невозможно передать вход; (2) адресуемый отрезок `span://` не отдаётся архивариусом до закрытия #1229. `audit-evening.mjs` работает на репозитории, `registry.json`, `truth/registry.json` — память сессий в этом наборе отсутствует по факту кода. |
| (в) Режим требования 3 из #569 применительно к архиву сессий | Требование 3 сегодня **соблюдается** при условии: `¬reads(persona-memory:extract, spans_archive)`. Gate «показано партнёрам» стоит в `telegram-swallow.mjs` (ветка `delivered`). `secret-redact` — ортогональный предикат (конфиденциальность, не вычитанность). **Условие жизни вывода:** вывод истинен только пока `persona-memory:extract` не читает архив спанов. При исполнении карточки #539 вывод протухает, если gate не расширен. Долг явно входит в DoD #539. |
| (г) Операциональная форма вставки | Поле `script` — строка `"scripts/archivarius.mjs ingest --from ~/.claude/projects --out docs/spans/today.jsonl"`, аргументы внутри строки. Поля `command` нет. Шаг `noncritical` с обязательным `whyNoncritical: "адресуемый отрезок span:// не отдаётся до закрытия #1229; отсутствие spans не блокирует day-memo"`. Позиция в фактической нумерации: между шагом 11 (`archive-code-review`) и шагом 12 (`day-memo`). **Сама вставка** — за границей этой комнаты, принадлежит карточке `archivarius-evening-tract`. |

---

## Список посылок

*(добор задним числом запрещён; посылки из вердиктов предшественников помечены источником)*

1. **[вердикт предшественника M1]** Единица суждения вечера — карточка реестра; область определения предиката — множество карточек дня; вход ревью — тройка `(карточка, коммиты, контракт)`; форма вердикта — `boundary` / `checks` / `safety`.

2. **[вердикт предшественника M2]** Целевой порядок `day-memo` → `audit-evening` → `code-review` → `archive-code-review`; мемо и хроника становятся `critical`; падение любого останавливает цепочку. Вердикт M2 в манифест не перенесён; говорить о фактической нумерации 16 шагов.

3. **[вердикт предшественника M3]** Вечер не мерит объём строк; `OVERSIZED_CHANGED_LINES` остаётся у потактового ревью со сменой глагола `skip` → `flag('split-required') + include`.

4. **[фактура повестки — устройство]** `sessions:scan` не имеет машиночитаемого выхода; `sessions:extract` резолвит цель по позиционному аргументу, не читает выход scan; `archivarius ingest` обходит каталог через `collectJsonlFiles()`, не читает выход extract. Три независимые точки входа.

5. **[фактура повестки — карточка тракта]** Связывание тракта между тремя органами заведено карточкой `archivarius-evening-tract` (M, active) и принадлежит ей, не этой комнате.

6. **[фактура повестки — схема шага]** Раннер `ritual-evening-run.mjs` использует поле `script` (строка, аргументы внутри); поля `command` схема не знает; `validateManifest` требует `whyNoncritical` у каждого `noncritical`; по умолчанию шаг `critical`.

7. **[фактура повестки — фактическая нумерация]** 16 живых шагов на 01.08: 6 team-memory-report (noncritical), 10 code-review (critical), 11 archive-code-review (critical), 12 day-memo (noncritical), 13 audit-evening (noncritical), 14 deps-watch (noncritical), 15 evening-tail (critical), 16 deliver-to-main (critical).

8. **[фактура повестки — граница нотариуса]** Archivarius (#1330, эпик #1229); репозиторий — нотариус (контракт, мастерская, снимки), не база полных сессий; адресуемый отрезок `span://<sessionId>/<uuid>` архив ещё не отдаёт; источник байтов — MongoDB в office-стеке.

9. **[фактура повестки — требование 3]** Требование 3 из #569 (Issue открыт): «В память попадает только то, что уже показано партнёрам» — норма по свойству содержимого, не по имени хранилища. Gate стоит в `telegram-swallow.mjs` (ветка `delivered`). `secret-redact` решает конфиденциальность — предикаты ортогональны.

10. **[фактура повестки — оговорка #539]** Карточка эпика #539 планирует подключить `~/.claude/projects/*.jsonl` в `persona-memory:extract` напрямую. При исполнении #539 вывод о соблюдении #569 протухает, если gate не расширен. Условие жизни вывода обязано быть названо в вердикте.

11. **[фактура повестки — память команды]** 31.07 записано 90 записей в память персон, вытеснено 99; у каждой персоны строка «всплывало сегодня: контур не поставлен». Память пишется и не читается обратно в вечерний контур.

12. **[фактура повестки — audit-evening.mjs]** Живой `audit-evening.mjs` потребляет репозиторий, `docs/tasks/registry.json`, `docs/truth/registry.json`; память сессий не читает вовсе.

13. **[фактура повестки — решение владельца 01.08]** Хроника остаётся такой, какой построена — репозиторий, реестр задач, граф правды.

---

**Definition of Done (для карточки `archivarius-evening-tract`):**
- Шаг с `script: "scripts/archivarius.mjs ingest --from ~/.claude/projects --out docs/spans/today.jsonl"` добавлен между позициями 11 и 12 фактической нумерации
- Шаг помечен `noncritical` с `whyNoncritical`, объясняющим блокер #1229
- Поле `command` отсутствует
- Выход `spans.jsonl` доступен как адресуемый артефакт для `day-memo`

**Definition of Done (для карточки #539):**
- Gate предиката «показано партнёрам» явно распространён на путь через архив спанов
- `persona-memory:extract` при чтении из `~/.claude/projects/*.jsonl` проверяет `delivered = true` перед записью в память персоны
- Вывод о соблюдении требования 3 из #569 верифицирован при новом источнике

---

*Реплик в диалоге: 30; каждый участник высказался не менее одного раза (Teamlead — 5, Верстальщик — 5, Музыкант — 5, Структурщик — 5, Архитектор — 5, Математик — 5).*
