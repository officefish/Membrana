<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-08-17T10:06:23.905Z |
| Команда | `yarn consilium` |
| Модель | anthropic/claude-sonnet-4-6 |
| Файл | `docs/seanses/server-plugin-foundation-m6-first-wave-2026-08-17.md` |
| Порядок ролей | Музыкант → Teamlead → Структурщик → Верстальщик → Архитектор → Математик |
| Повестка | `docs/meeting/server-plugin-foundation/M6_AGENDA.md` |

**Вход сеанса** (что комната действительно получила):

| Вход | Носитель | Размер | Отпечаток | Доставка |
|------|----------|-------:|-----------|----------|
| повестка (эхо в голове) | `docs/meeting/server-plugin-foundation/M6_AGENDA.md` | 3192 | `30f2cf7df751` | полностью |
| инструкция консилиума | `docs/prompts/CONSILIUM_PROMPT.md` | 4494 | `b6bd1a8dc127` | полностью |
| координация ролей | `docs/VIRTUAL_TEAM_PROMPT.md` | 8034 | `75959c7f060d` | **обрезан** |
| повестка | `docs/meeting/server-plugin-foundation/M6_AGENDA.md` | 3192 · 1 п. | `30f2cf7df751` | полностью |

> ⚠ Часть входа не доехала до комнаты целиком. Утверждения протокола о своём входе
> сверять с этой таблицей, а не с текстом реплик.

**Условия прогона:** `--no-context --no-rag --min-replies 30`

**Вопрос:**

Первая волна плагинов и приёмка основы: состав, первый живой плагин, форма живого следа, приёмочный список, границы не-делаем

---

# Консилиум: Первая волна плагинов и приёмка основы (M6)

**Повестка:** Состав первой волны плагинов, первый живой плагин (Т3.11), форма живого следа, приёмочный список (DoD из M1–M5), границы «не делаем».

**Участники:** Teamlead (Тарасов), Архитектор (Веснин), Структурщик (Ожегов), Математик (Дынин), Музыкант (Курёхин), Верстальщик (Родченко).

**Порядок реплик:** Музыкант → Teamlead → Структурщик → Верстальщик → Архитектор → Математик (циклически, ≥30 реплик)

---

[Музыкант]: Смотрю на шесть детекторов как на инструменты в ансамбле — у каждого своя тембральная роль. Но в первой волне нам нужен не полный оркестр, а квартет, который доиграет до конца. mfcc — самый отработанный тракт: чистая функция, предсказуемый выход, работает на любом WAV из «Полевых записей 2026-08». Предлагаю его первым живым плагином. Остальные пять — в первую волну, но после доказательства жизни на mfcc.

[Teamlead]: Стоп. Вопрос не в том, что нам нравится звуком — вопрос в том, что сдаётся. Т3.11 говорит прямо: основа сдаётся вместе с первым живым плагином, без него приёмки нет. Это не дискуссия о тембрах, это условие ворот. Нужно услышать обоснование против альтернатив — mfcc против yamnet, против harmonic. Просто «отработанный» не аргумент.

[Структурщик]: Перед выбором первого плагина уточню лемму «первая волна». Волна — это множество плагинов, поставляемых одним PR-контуром к приёмке основы. Если мы говорим «все шесть сразу», это один контур с шестью точками сбоя. Если «mfcc первым, остальные пятью отдельными PR» — это не одна волна, а каскад. Нам нужно решить: волна — это момент регистрации или момент готовности? Предлагаю определение: первая волна — это зарегистрированные плагины, прошедшие приёмку основы в едином DoD-прогоне. Первый живой — часть волны, не отдельная поставка.

[Верстальщик]: Меня здесь интересует один вопрос формы: ShowcaseManifest из M5 требует displayForm из закрытого словаря и description. Если в первой волне нет хотя бы одного плагина с заполненными витринными полями — витрину нечем рисовать при любой будущей работе по UI. Но витрина на клиенте — это отдельная работа, M5 закрыл только контракт. Вывод: в первой волне витринный плагин не обязателен как самостоятельная единица, достаточно чтобы каждый обработчик нёс корректный ShowcaseManifest. Пример владельца Т2 (сортировка по громкости / пиковые зоны) — это не обработчик коллекций, это отдельный вид, не делаем сейчас.

[Архитектор]: Конструкция волны. M2 зафиксировал два дома: journal и collections. Шесть детекторов — плагины рода «обработчик» дома collections. Это значит mountTarget у всех шести — collections. Витринный пример владельца Т2 (сортировка по громкости) имеет другой mountTarget и другой kind — он не обработчик в смысле M1, он требует отдельного контракта. Включать его в первую волну — значит открывать M1. Граница чёткая: первая волна = шесть детекторов collections. Витринный Т2 — за пределами волны.

[Математик]: Формализую выбор первого живого плагина. Пусть критерий — минимальная зависимость от внешних весов, детерминированность выхода, покрываемость юнит-тестом без мока аудиостека. Проверяю кандидатов: yamnet требует TensorFlow-модели — внешняя зависимость, нет в пакете локально, риск недетерминированности при cold start. template-match требует эталонного шаблона в хранилище — зависимость от данных репозитория, не чистая функция в изоляции. harmonic, cepstral, spectral-flux — чистые DSP, но у harmonic нестабильный выход на шумных записях без предобработки. mfcc: вход — Float32Array сэмплов, выход — матрица коэффициентов фиксированной размерности, детерминирован, тест пишется в три строки. mfcc выигрывает по всем трём критериям.

[Музыкант]: Поддерживаю mfcc по ещё одному основанию: на «Полевых записях 2026-08» с пробами тракта mfcc даст осмысленный выход даже на сыром захвате без нормализации. yamnet на тех же записях может вернуть мусорную классификацию без препроцессинга — живой след будет формально есть, но содержательно пуст. Это не доказательство жизни, это доказательство запуска.

[Teamlead]: Хорошо, mfcc как первый живой — слышу три независимых обоснования: минимальные зависимости (Дынин), работа на реальных данных без препроцессинга (Курёхин), детерминированный выход (оба). Против альтернатив — зафиксировано. Теперь состав волны: Архитектор назвал шесть детекторов collections, Т2-витринный — за границей. Это предложение к голосованию. Верстальщик, ты сказал «витринный не обязателен» — ты с этой границей?

[Верстальщик]: Да, с границей согласен. ShowcaseManifest есть у всех шести — этого достаточно для контракта M5. Отдельный витринный плагин в первой волне = лишняя точка сбоя без добавленной стоимости для приёмки.

[Структурщик]: Зафиксирую структуру DoD из вердиктов M1–M5. М1 даёт: packages/plugin-contracts существует, экспортирует пять полей манифеста, PluginExecutor.execute, RunResult — это должно быть зелёным. M2 даёт: IPluginHost реализован в обоих домах, HOME_REGISTRY содержит записи для обоих домов, дефицит модулей закрыт фактически. M3 даёт: коллекция plugin-results в Mongo офиса существует, RunAddress резолвится, StateRecord с resumeMode записывается. M4 даёт: три триггера зарегистрированы, каналы notify/request работают, fire-and-forget с догонялкой через M3 проходит. M5 даёт: ShowcaseManifest у каждого плагина волны содержит корректный displayForm и description.

[Архитектор]: К структуре DoD добавлю порядок поставки. Первый PR: packages/plugin-contracts — словарь авторитетен, гейты владельца зелёные. Это блокирует всё остальное, без него нет контракта. Второй PR: IPluginHost + HOME_REGISTRY в обоих домах — дефицит M2 закрыт. Третий PR: plugin-results коллекция + RunAddress + StateRecord — M3 закрыт. Четвёртый PR: PLUGIN_TRIGGERS + каналы + fire-and-forget — M4 закрыт. Пятый PR: ShowcaseManifest у шести плагинов — M5 закрыт. Шестой PR: первый живой плагин (mfcc) с прогоном на «Полевых записях 2026-08» — живой след, Т3.11 закрыт. Все шесть — граница сдачи основы.

[Математик]: Уточню форму живого следа. RunAddress по M3 содержит два отпечатка — это pluginId + collectionId как минимум. Для «Полевых записей 2026-08» на media/field-node-2026-08: collectionId фиксирован фактом существования коллекции. Запись в plugin-results должна содержать: RunAddress с pluginId=«mfcc-detector», collectionId коллекции «Полевые записи 2026-08», resumeMode из StateRecord, ConvergenceRecord с отметкой завершения. Это четыре поля, все определены M3 — не изобретаем нового.

[Музыкант]: Хочу уточнить «настоящую коллекцию». «Полевые записи 2026-08» на field-node-2026-08 — это живые данные, не тестовый фикстур. Прогон первого живого плагина на них — не smoke-тест, это боевой прогон. Значит живой след = запись в plugin-results Mongo офиса с реальными данными из этой коллекции. Форма предъявления: документ в plugin-results с заполненными RunAddress, StateRecord (resumeMode=«completed» или эквивалент), ConvergenceRecord. Это и есть доказательство жизни.

[Teamlead]: Предъявление живого следа — не скриншот, не лог. Это документ в Mongo, читаемый через RunAddress. Кто может возразить против этой формы? Если молчите — форма принята.

[Структурщик]: Нет возражений по форме. Добавлю уточнение по адресу: RunAddress должен резолвиться из HOME_REGISTRY — это норма M3, не новое решение. Значит в живом следе проверяется цепочка: HOME_REGISTRY → RunAddress → document в plugin-results. Если любое звено разорвано — живой след не принят.

[Верстальщик]: Смотрю на предъявление с точки зрения читаемости: ConvergenceRecord должен быть не просто полем, а читаемой записью — дата, pluginId, статус. Если это blob без структуры — витрина никогда не сможет его отобразить. Но реализация витрины — за пределами этого собрания. Фиксирую как требование к форме: ConvergenceRecord структурирован по M3-контракту, не произвольный JSON.

[Архитектор]: Теперь границы «не делаем». Первая: испускание событий журналом — Т3.7 — отложено, цена названа в повестке. Вторая: витрина на клиенте — отдельная работа, M5 дал только контракт ShowcaseManifest, не реализацию UI. Третья: витринный плагин Т2 (сортировка по громкости / пиковые зоны) — другой mountTarget, другой kind, вне первой волны. Четвёртая: нарезка задач и код — за границей этого собрания по MEETING_BRIEF.md (#1950). Пятая: переоткрытие контрактов M1–M5 — запрещено явно повесткой.

[Математик]: Добавлю шестую границу: валидация качества результатов детекторов (бенчмарки, сравнение алгоритмов, настройка гиперпараметров) — это не задача первой волны. Первая волна доказывает, что плагин запускается и пишет результат. Правильность результата — следующий эшелон.

[Музыкант]: Седьмая граница: embedded и edge-захват звука на field-node-2026-08 — это не задача первой волны. «Полевые записи 2026-08» уже существуют как коллекция, плагин работает с уже собранными данными. Захват нового материала — вне этого собрания.

[Teamlead]: Итого по границам: семь позиций названы тремя участниками. Верстальщик, Структурщик — есть что добавить?

[Верстальщик]: Восьмая граница: A11y и UI-разметка витрины — не в этом собрании. ShowcaseManifest — контракт данных, не контракт рендеринга.

[Структурщик]: Девятая граница: межплагинные зависимости — прямые импорты плагин → плагин запрещены нормой, и первая волна этого не вводит. Если у кого-то возникнет желание переиспользовать логику — через packages/plugin-contracts или общий пакет, не напрямую.

[Архитектор]: По составу волны последний вопрос — порядок внутри волны. Все шесть детекторов имеют одинаковый mountTarget (collections), одинаковый kind (обработчик по M1), одинаковую структуру ShowcaseManifest. Нет архитектурного основания поставлять их в строгом порядке кроме первого живого (mfcc). После доказательства жизни на mfcc остальные пять могут идти параллельно в одном PR или серией — это решение исполнителя, не архитектора.

[Математик]: Согласен с параллельностью пяти. Единственное условие: у каждого из пяти должна быть своя запись в plugin-results на той же коллекции «Полевые записи 2026-08» — иначе приёмка неполна. Но это следствие DoD, не новое требование.

[Музыкант]: Слышу полную картину. mfcc первым — доказательство жизни. Пять остальных после — доказательство масштабируемости контракта. Витринный Т2 — вне волны. Живой след — документ в Mongo. Границы — девять позиций. Принимаю.

[Teamlead]: Прогоняю по пунктам повестки. Состав волны: шесть детекторов collections, без Т2. Первый живой: mfcc, обоснован против yamnet (внешние веса), template-match (зависимость от данных), harmonic (нестабильность на шуме). Живой след: документ в plugin-results с RunAddress, StateRecord (resumeMode), ConvergenceRecord — на коллекции «Полевые записи 2026-08». Приёмочный список: шесть PR по M1–M5 плюс живой плагин — всё зелёное. Границы: девять позиций. Если нет возражений — выношу на принятие.

[Структурщик]: Принимаю. Одно уточнение по форме DoD: порядок шести PR — не жёсткая очередь, но PR №1 (plugin-contracts) блокирует все остальные. Это должно быть зафиксировано в DoD явно.

[Верстальщик]: Принимаю. Форма ConvergenceRecord структурирована — требование зафиксировано.

[Архитектор]: Принимаю. Граница волны по mountTarget и kind — чёткая, переоткрытия M1 нет.

[Математик]: Принимаю. Критерий первого живого плагина — три предиката, mfcc выигрывает по всем трём.

[Музыкант]: Принимаю.

[Teamlead]: LGTM по всем пяти граням повестки. Вердикт M6 сформирован.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Состав первой волны | Шесть детекторов-обработчиков дома collections: harmonic, cepstral, spectral-flux, template-match, yamnet, mfcc. Все шесть — в первой волне. Витринный плагин Т2 (сортировка по громкости / пиковые зоны) — **вне первой волны**: иной mountTarget и kind, включение требует переоткрытия M1 |
| Первый живой плагин | **mfcc-detector**. Обоснование против альтернатив: yamnet — внешние TF-веса, недетерминированный cold start; template-match — зависимость от эталонных данных в хранилище, не чистая функция в изоляции; harmonic — нестабильный выход на шумных записях без предобработки; mfcc: вход Float32Array → выход матрица фиксированной размерности, детерминирован, тест без мока аудиостека, осмысленный выход на сыром захвате |
| Живой след (форма предъявления) | Документ в коллекции plugin-results (Mongo офиса) с полями: RunAddress (pluginId=«mfcc-detector», collectionId коллекции «Полевые записи 2026-08» на field-node-2026-08), StateRecord с resumeMode, структурированный ConvergenceRecord по контракту M3. Цепочка HOME_REGISTRY → RunAddress → document обязана быть целой. Форма — не лог и не скриншот: читаемый документ БД |
| Приёмочный список (DoD основы) | PR-1: packages/plugin-contracts — пять полей манифеста, PluginExecutor.execute, RunResult, гейты владельца зелёные (**блокирует всё остальное**). PR-2: IPluginHost + HOME_REGISTRY в обоих домах (journal, collections) — дефицит M2 закрыт. PR-3: коллекция plugin-results + RunAddress + StateRecord/resumeMode + ConvergenceRecord — M3 закрыт. PR-4: PLUGIN_TRIGGERS (три триггера) + каналы notify/request + fire-and-forget с догонялкой — M4 закрыт. PR-5: ShowcaseManifest (displayForm + description) у каждого из шести плагинов — M5 закрыт. PR-6: mfcc-detector с прогоном на «Полевых записях 2026-08» и живым следом в plugin-results — Т3.11 закрыт. Порядок PR-1 → (PR-2…PR-5 могут идти параллельно после PR-1) → PR-6 |
| Границы «не делаем» | 1. Испускание событий журналом (Т3.7) — отложено. 2. Витрина на клиенте — отдельная работа вне собрания. 3. Витринный плагин Т2 — вне первой волны. 4. Нарезка задач и код — за границей собрания (#1950). 5. Переоткрытие контрактов M1–M5 — запрещено повесткой. 6. Валидация качества / бенчмарки детекторов — следующий эшелон. 7. Edge-захват на field-node-2026-08 — коллекция уже существует, новый захват вне волны. 8. A11y и UI-разметка витрины — не в этом собрании. 9. Прямые межплагинные зависимости — запрещены нормой, в первой волне не вводятся |

---

## Список посылок

| № | Посылка | Тип |
|---|---------|-----|
| 1 | Словарь M1 авторитетен: пять полей манифеста (id, version, kind, mountTarget, triggers), PluginExecutor.execute, RunResult; дом словаря — packages/plugin-contracts; гейты владельца словаря определены | **норма** |
| 2 | Два дома плагинов (M2): background-office/journal и background-media/collections; IPluginHost; HOME_REGISTRY; дефицит обоих модулей назван фактами | **факт** |
| 3 | Дом результатов (M3): коллекция plugin-results в Mongo офиса; RunAddress; два отпечатка; StateRecord/resumeMode; ConvergenceRecord | **норма** |
| 4 | PLUGIN_TRIGGERS закрыт (M4): journal.entry_created, collections.collection_created, collections.sample_added; каналы notify/request; fire-and-forget с догонялкой через M3 | **норма** |
| 5 | ShowcaseManifest (M5): displayForm из закрытого словаря + description; setPluginEnabled включён; у handler/report витринных полей нет | **норма** |
| 6 | Шесть детекторов — пакеты packages/services/* с зубами: harmonic, cepstral, spectral-flux, template-match, yamnet, mfcc | **факт** |
| 7 | На media живёт устройство field-node-2026-08 и коллекция «Полевые записи 2026-08» с пробами тракта | **факт** |
| 8 | Т3.11: основа сдаётся только вместе с первым живым плагином (конспект шторма) | **норма** |
| 9 | Т2: пример владельца — сортировка по громкости / пиковые зоны (конспект шторма) | **факт** |
| 10 | Код производится только после вердиктов; PR #1950 зафиксирован как граница (MEETING_BRIEF.md) | **норма** |

---

**Definition of Done (M6):**
- [ ] PR-1 зелёный: packages/plugin-contracts экспортирует все контракты M1, гейты владельца проходят
- [ ] PR-2 зелёный: IPluginHost реализован в journal и collections, HOME_REGISTRY содержит оба дома
- [ ] PR-3 зелёный: plugin-results в Mongo, RunAddress резолвится, StateRecord/ConvergenceRecord записываются
- [ ] PR-4 зелёный: три триггера зарегистрированы, fire-and-forget с догонялкой работает
- [ ] PR-5 зелёный: ShowcaseManifest у каждого из шести плагинов — displayForm + description корректны
- [ ] PR-6 зелёный: mfcc-detector прогнан на коллекции «Полевые записи 2026-08», документ в plugin-results с RunAddress + StateRecord + ConvergenceRecord существует и читаем

Все шесть PR зелёные = основа сдана.

---

*Реплик в диалоге: 32; каждый участник высказался не менее двух раз.*
