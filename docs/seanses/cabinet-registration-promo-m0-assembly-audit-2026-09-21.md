# Аудит сборки председателя — M0 «Регистрация в кабинете по промокоду панели», прогон 2 — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Предмет: сборка `docs/seanses/cabinet-registration-promo-m0-order-r2-assembly-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `e8e17ad0`) против протокола `docs/seanses/cabinet-registration-promo-m0-order-r2-2026-09-21.md` и повестки `M0_AGENDA.md`. Проверки — по разделу сборки «Что аудировать в сборке» (четыре) плюс пункт 3 чек-листа `AUDIT_M0_BRIEF.md`. Сборка, протокол и повестка не правились.

## Итог

**ЧИСТО** по всем четырём проверкам сборки и по пункту 3. Одно расхождение в самоописании сборки, не в решении: заголовок раздела «Решение комнаты (без изменений; протокол, строки 160–186)» и слова «дословно» неверны — три ячейки таблицы и «Определение готовности» переписаны (словарь и сокращения, одна выпущенная концовка ячейки), существо решения при этом не изменилось. Расхождение названо в проверке 1; что с ним делать до ратификации — решение председателя.

Вопрос о числе сессий (четыре против трёх в Q0) в сборке вынесен отдельным абзацем с тремя дословными цитатами — все три сверены (`grep -cF` = 1, роль и строка совпадают: [Teamlead] 74, 86; [Архитектор] 92). Решает владелец при ратификации.

## Таблица

| # | Проверка | Вердикт | Цитата / команда |
|---|---|---|---|
| 1 | Решение совпадает с протоколом стр. 160–186 без правок по существу | PASS по существу; **самоописание неверно** | Сравнение 15 строк таблицы по ячейкам (скрипт: разбор `\|`-строк протокола 162–177 и сборки 8–23, сравнение по ключу). Совпадают дословно 12. Различия: «Не-ребро 2 → 4» — протокол «(из фактуры: `apps/cabinet/src/services/` в стволе нет)», сборка «(каталога `apps/cabinet/src/services/` в стволе нет)»; «Не-рёбра 4 → 1, 4 → 2, 4 → 3» — протокол «UI не диктует…», сборка «Интерфейс не диктует…»; «Идёт сразу в задание» — у протокола концовка «…D после C; не-рёбра 1→4, 2→4, 4→1, 4→3.», у сборки «…D после C.» — четыре не-ребра из ячейки выпущены (остаются в строках графа и в «Определении готовности», где перечислены все шесть). «Definition of Done (если применимо)» с тремя пунктами переписан в одно предложение «Определение готовности (Q0)» с сокращением путей (`env.schema.ts`, `membrane.service.ts:157` вместо полных). Ни одно различие не меняет граф, сессии, границы файлов, закрепления и перечень комнат. Но сборка утверждает «без изменений» (стр. 6) и «дословно» — это не так. |
| 2 | Каждая исправленная посылка — `grep -cF` ≥ 1, строка совпадает | PASS | Семь заменённых посылок (сборка стр. 33–40), для каждой: фрагмент найден в протоколе ровно в заявленной строке, строка начинается с заявленной роли. П13 → [Архитектор] 116 ✓; П16 → [Математик] 94 ✓; П19 → [Структурщик] 78 ✓; П20 → [Архитектор] 140 ✓; П14 → [Архитектор] 68 ✓; П15 → [Верстальщик] 132 ✓; П17 → [Структурщик] 114 ✓. Нормализация глифов не потребовалась — цитаты взяты с кавычками протокола. |
| 3 | Оговорка к П19 не меняет решения: `env.schema.ts` — сессия C | PASS | Протокол стр. 116 начинается с `[Архитектор]` и содержит «Значит, сессия C. В B его нет»; стр. 173 вердикта: «`packages/background-cabinet/src/config/env.schema.ts` — принадлежит сессии C … Сессия B не трогает этот файл». Сборка стр. 19 — та же строка дословно; стр. 35 оговорка называет обе. Замечание: П19 остаётся в списке как «план» с текстом, где файл стоит у B, — сборка честно помечает, что сработала только часть (перечень A/C/D и «новый модуль» для B); повестка требует «посылки — только сработавшие», оговорка это условие закрывает явно. |
| 4 | П12 в повестке есть, в репликах нет; возвращённые три — ≥ 1 | PASS | `M0_AGENDA.md`: `grep -cF "PromoCode { grants[], maxUses, usedCount, expiresAt }"` → 1. Реплики (протокол стр. 44–156): `PromoCode { grants[]` → 0; `panel-users-core.ts:24` → 0; путь `panel-users-core.ts` без строки → 1 (стр. 78, перечень файлов сессии A) — как и сказано в сборке стр. 42. Возвращённые: стр. 68, 114, 132 — `grep -cF` = 1 каждая (см. проверку 2). Поправка сборки к первой версии (стр. 44) соответствует замеру. |
| 3′ | Пункт 3 чек-листа: «Список посылок» сборки | PASS (замечание) | Секция есть (стр. 29), у семи посылок тип, роль, дословная реплика и строка. Тринадцать посылок П1–П11, П18 в сборке не воспроизведены — «переносятся без изменений из протокола» (стр. 31); их дословность проверена аудитом прогона 2 (протокол стр. 192–202, 209: 10 дословно с точностью до глифов кавычек, 3 — до заглавной буквы). Замечание: список посылок ратифицируемого документа разложен по двум файлам; читателю сборки тринадцать посылок видны только по ссылке. |

## Что аудитор не делает

Порядок кандидатов не предлагается; сборка, протокол и повестка не правлены; ратификация и решение о числе сессий — владелец.
