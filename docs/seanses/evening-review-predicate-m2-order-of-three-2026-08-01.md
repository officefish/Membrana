<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-08-01T06:46:46.842Z |
| Команда | `yarn consilium` |
| Модель | anthropic/claude-sonnet-4-6 |
| Файл | `docs/seanses/evening-review-predicate-m2-order-of-three-2026-08-01.md` |
| Порядок ролей | Математик → Архитектор → Верстальщик → Teamlead → Структурщик → Музыкант |
| Повестка | `docs/meeting/evening-review-predicate/M2-topic.md` |

**Вход сеанса** (что комната действительно получила):

| Вход | Носитель | Размер | Отпечаток | Доставка |
|------|----------|-------:|-----------|----------|
| повестка (эхо в голове) | `docs/meeting/evening-review-predicate/M2-topic.md` | 3583 | `b075b3ef3778` | полностью |
| инструкция консилиума | `docs/prompts/CONSILIUM_PROMPT.md` | 4494 | `b6bd1a8dc127` | полностью |
| координация ролей | `docs/VIRTUAL_TEAM_PROMPT.md` | 8034 | `75959c7f060d` | **обрезан** |
| повестка | `docs/meeting/evening-review-predicate/M2-topic.md` | 3583 · 1 п. | `b075b3ef3778` | полностью |

> ⚠ Часть входа не доехала до комнаты целиком. Утверждения протокола о своём входе
> сверять с этой таблицей, а не с текстом реплик.

**Вопрос:**

S1 — В каком порядке вечер порождает мемо, хронику и ревью, и что происходит с действующим вердиктом M4 заседания meeting-evening-auditor от 18.07, ставящим хронику ПОСЛЕ ревью: отзыв, совместимость или переоткрытие? Фактический порядок шагов в docs/tasks/evening-ritual-steps.json: code-review — шаг 10, day-memo — 12, audit-evening — 13; ревью выходит ПЕРВЫМ из трёх. Требование владельца 01.08: мемоизация, затем аудит, затем ревью. Пара мемо-перед-хроникой уже стоит верно и намеренно (шов archive-evidence Ожегова, условие существования каталога Веснина), перестановка требуется одна: вынуть code-review из позиции 10 за audit-evening вместе с зависимым archive-code-review. КОЛЛИЗИЯ: вердикт M4 от 18.07, ратифицированный владельцем, строкой Lifecycle фиксирует «вечерняя цепочка ПОСЛЕ truth.mjs cool и code-review» — положение хроники после ревью есть действующий вердикт, и требование 01.08 его отзывает; по S-M3 отзыв предшественника обнуляет зависимые каскадом, значит надо сказать, что именно умирает, а что переживает отзыв. ЦЕНА: day-memo и audit-evening объявлены noncritical, а code-review — critical с историей инцидента rt-9 (|| true проглотил exit 127, downstream заархивировал вчерашний отчёт под сегодняшней датой); если ревью встанет на них зависимостью, пара обязана сменить критичность, иначе будет ревью на пустоте и зелёный вечер. ПОСЫЛКА ОТ ПРЕДШЕСТВЕННИКА M1, не предмет спора: единица суждения вечера — карточка реестра, опознаётся машинно; вход — тройка (карточка, коммиты, контракт) вместо consumes: []; форма вердикта — типизированные boundary/checks/safety; вечер выносит реляционное суждение о множестве карточек дня. Требуется: порядок трёх документов с номерами шагов; судьба M4 (отзыв целиком, отзыв одной строки Lifecycle при сохранении формы генератора и тернарного вердикта, совместимость или переоткрытие) с явным перечнем умирающего каскадом; критичность day-memo и audit-evening после перестановки и поведение вечера при их падении. Соседние вопросы упоминать можно, вердикт по ним выносить НЕЛЬЗЯ: мерка объёма — M3, мемоизация против #569 — M4, опора аудита на память сессий — M5, ворота качества документа — M6. Назвать полный список посылок в этом же прогоне; посылки из вердикта M1 помечать как вердикт предшественника, а не как собственное решение этой комнаты.

---

# Консилиум: M2 — порядок трёх документов и судьба вердикта M4 от 18.07

**Повестка:** В каком порядке вечер порождает мемо, хронику и ревью; что происходит с вердиктом M4 заседания `meeting-evening-auditor` от 18.07 (хроника после ревью): отзыв, совместимость или переоткрытие.

**Участники:** Математик (Dynin), Архитектор (Vesnin), Верстальщик (Rodchenko), Teamlead (Tarasov), Структурщик (Ozhegov), Музыкант (Kuryokhin).

**Порядок реплик:** Математик → Архитектор → Верстальщик → Teamlead → Структурщик → Музыкант (циклически, ≥30 реплик)

---

[Математик]: Зафиксирую область как предикат. Три объекта: `day-memo` (шаг 12), `audit-evening` (шаг 13), `code-review` (шаг 10). Текущий порядок в источнике: code-review(10) < day-memo(12) < audit-evening(13). Требование владельца 01.08 задаёт порядок: day-memo < audit-evening < code-review. Перестановка одна: вынуть code-review из позиции 10, поставить после audit-evening(13). Новые номера шагов — вопрос исполнения, но порядковое отношение фиксирую: day-memo = N, audit-evening = N+1, code-review = N+2, где N — ближайший свободный номер после текущего 11 (освободившегося). Предикат зависимости: code-review зависит от audit-evening зависит от day-memo; граф ацикличен, ребро существует.

[Архитектор]: Форма коллизии простая. M4 от 18.07 содержит строку Lifecycle: «вечерняя цепочка после `truth.mjs cool` и `code-review`». Это не украшение — это контракт порядка, ратифицированный владельцем. Требование 01.08 меняет именно этот контракт: ревью теперь идёт после цепочки, а не до. Значит строка Lifecycle M4 и новый порядок несовместимы — не в смысле «неудобно», а в смысле «оба не могут быть верны одновременно». Совместимость исключена. Вопрос только в хирургии: отзывается строка Lifecycle, или весь M4 целиком.

[Верстальщик]: С витрины исполняемого источника смотрю на то, что машина читает. `docs/tasks/evening-ritual-steps.json` — источник истины для рантайма. Если строка Lifecycle M4 расходится с JSON, рантайм идёт по JSON, а не по тексту вердикта. Но честность требует, чтобы вердикт и источник говорили одно. Сейчас они противоречат друг другу: JSON будет перестановлен по требованию 01.08, а M4 продолжает фиксировать старый порядок. Это фальшивое присутствие вердикта — он числится действующим, но источник его не исполняет. Нужно явное решение о судьбе M4, иначе витрина врёт.

[Teamlead]: Без утешений. Коллизия реальная, не терминологическая. У нас действующий вердикт M4 (ратифицирован владельцем), новое требование владельца 01.08 и правило S-M3 об отзыве. Три вопроса по существу: что именно отзывается в M4; что умирает каскадом; что стоит вечеру падение `day-memo` или `audit-evening` после перестановки. Всё три надо закрыть без «зависит». Начинаем работать по порядку.

[Структурщик]: Леммы, иначе путаница. **Строка Lifecycle M4** — одна именованная норма внутри вердикта M4: «вечерняя цепочка после `truth.mjs cool` и `code-review`». **Форма генератора M4** — отдельная часть вердикта: как формируется документ хроники (генератор, тернарный вердикт, двухфазный `cycle.consumed`). **Зависимый вердикт** — любой последующий вердикт, область определения которого содержит строку Lifecycle M4 как посылку. Различие существенно: если строка Lifecycle умирает, а форма генератора не зависит от неё, форма генератора выживает. Это надо проверить.

[Музыкант]: На слух — это две разные дорожки в одном M4. Дорожка A: «когда играть хронику в цепочке» (строка Lifecycle). Дорожка B: «как звучит хроника» (генератор, тернарный вердикт, `cycle.consumed`). Требование 01.08 переключает фейдер A — оно говорит только о позиции в цепочке, не о форме документа. Если дорожки не перепутаны в зависимостях, можно снять только A и сохранить B. Но прежде нужно убедиться, что B не ссылается на позицию «после code-review» как на техническое условие своей работы.

[Математик]: Проверяю, зависит ли форма генератора от позиции в цепочке. Форма генератора M4 описывает: вход — состояние дня; выход — тернарный вердикт; механизм — двухфазный `cycle.consumed`. Ни одна из этих переменных не содержит «позицию в цепочке» как аргумент. `cycle.consumed` читает накопленное состояние дня — это не функция от «был ли до меня code-review». Вывод: форма генератора независима от строки Lifecycle. Строка Lifecycle и форма генератора — ортогональные части M4. Хирургический отзыв одной строки технически возможен.

[Архитектор]: Тогда форма отзыва ясна. Отзывается **одна строка Lifecycle M4**: «вечерняя цепочка после `truth.mjs cool` и `code-review`». Сохраняются: форма генератора хроники, тернарный вердикт, двухфазный `cycle.consumed`. Это не переоткрытие заседания `meeting-evening-auditor` — там ничего не пересматривается по существу кроме позиции. Это не отзыв M4 целиком — большая часть вердикта не затронута. Термин по S-M3: **частичный отзыв** — отзывается именованная норма внутри действующего вердикта.

[Верстальщик]: Уточняю про каскад. По S-M3: отзыв нормы обнуляет вердикты, область определения которых содержит эту норму как посылку. Строка Lifecycle M4 («хроника после ревью») могла быть посылкой в последующих вердиктах, которые рассуждали о порядке вечерней цепочки. Надо назвать конкретно что умирает. Из фактуры повестки: инцидент rt-9 (15.07) и его downstream — это следствие конкретного порядка, а не посылка из M4. Механика критичности `code-review` — отдельный факт. Что именно из последующих вердиктов опиралось на строку Lifecycle M4?

[Teamlead]: Хорошо. По фактуре в материалах комнаты: строка Lifecycle M4 фигурирует как действующий вердикт о порядке; других заседаний, явно ратифицирующих эту строку как посылку, в переданном контексте не названо. Инцидент rt-9 — это факт о поведении рантайма, не вердикт, опирающийся на M4. Значит каскад ограничен: умирает только строка Lifecycle M4. Если комната не знает о существовании других вердиктов, зависящих от этой строки — их надо искать при исполнении; декларировать несуществующий каскад нельзя. Фиксирую: каскад в пределах известного контекста — одна норма.

[Структурщик]: Добавлю точность. Умирает каскадом: **норма «вечерняя цепочка после `truth.mjs cool` и `code-review`»** из строки Lifecycle M4. Выживает: форма генератора хроники (M4), тернарный вердикт (M4), двухфазный `cycle.consumed` (M4), обоснование шва `archive-evidence` (Ожегов — в `audit-evening`, не в M4 Lifecycle), условие существования каталога (Веснин — в `audit-evening`). Эти двое выживают потому, что опираются на иные посылки, не на строку Lifecycle M4.

[Музыкант]: Теперь по цене. Дорожка A умерла — позиция ревью сдвинулась за хронику. Но code-review объявлен `critical`: его падение роняет цепочку. Если code-review теперь стоит после `day-memo` и `audit-evening`, а те — `noncritical`, то ревью честно встаёт на вход, который может не существовать. Это как записывать мастер-трек поверх пустой сессии: зелёный bounce, нулевой сигнал. Проблема не в порядке как таковом — в рассогласовании критичности.

[Математик]: Формализую предикат критичности. Пусть `critical(step)` означает: падение step останавливает цепочку. Пусть `depends(A, B)` означает: A использует выход B. Если `depends(code-review, audit-evening)` и `depends(code-review, day-memo)`, то корректность code-review предполагает, что audit-evening и day-memo выполнились успешно. Если audit-evening = `noncritical`, то при его падении цепочка продолжается — code-review запускается на пустом или неполном входе. Предикат честности: `honest(code-review) ⇔ ∀B: depends(code-review, B) → critical(B)`. Сейчас предикат нарушен для B = audit-evening и B = day-memo.

[Архитектор]: Вывод из предиката Математика прямой. После перестановки `day-memo` и `audit-evening` становятся **структурными предшественниками** `code-review`. Структурный предшественник, от которого зависит корректность последующего шага, обязан быть `critical`. Иначе контракт нарушен: `code-review` обещает вердикт по тройке `(карточка, коммиты, контракт)` — посылка M1; если мемо и хроника не записаны, тройка неполна. Форма решения: оба шага — `day-memo` и `audit-evening` — должны получить статус `critical` как условие честности `code-review`.

[Верстальщик]: С витрины: если `day-memo` упал и остался `noncritical`, цепочка идёт дальше. `code-review` запускается. Он строит вердикт по тройке, но мемо отсутствует. Вердикт выходит зелёным — владелец видит «вечер завершён». Это фальшивое присутствие итога: документ есть, основание пустое. Аналогично при падении `audit-evening`: хроника не записана, ревью на неё не ссылается. Инцидент rt-9 показал ровно эту механику — `|| true` скрыл ошибку, downstream принял пустоту за данные. Повторение паттерна недопустимо.

[Teamlead]: Фиксирую позицию комнаты по критичности. `day-memo` и `audit-evening` — `critical` после перестановки. Без этого перестановка декоративна: меняем порядок шагов, но не меняем поведение при падении. Зелёный вечер на пустом входе — это не тренировка, это фальшивый матч. Отдельно: если один из них упал и цепочка всё же дошла до `code-review` — что вечер делает? Это поведенческое правило, комната обязана его назвать.

[Структурщик]: Поведение при падении называю как лемму. **Правило останова**: если шаг помечен `critical` и упал — цепочка останавливается, последующие шаги не запускаются, вечер фиксируется как `incomplete`, не `success`. При падении `day-memo` (critical после перестановки): `audit-evening` не запускается, `code-review` не запускается, вечер = `incomplete`. При падении `audit-evening` (critical после перестановки): `code-review` не запускается, вечер = `incomplete`. Оба варианта дают явный сигнал владельцу, не зелёный фантом.

[Музыкант]: Это правильная шина. `incomplete` — честный статус: «вечер не завершён, причина — шаг X». Владелец видит сигнал, а не тишину под зелёным баннером. Инцидент rt-9 был именно тишиной: `|| true` заглушил крик, downstream принял сигнал «всё хорошо». После перестановки с правильной критичностью тот же паттерн невозможен: первое падение в цепи ломает её явно.

[Математик]: Теперь соберу числа для источника. Текущий JSON: code-review = 10, archive-code-review = 11, day-memo = 12, audit-evening = 13. После перестановки: day-memo остаётся на 12, audit-evening на 13, code-review и archive-code-review вставляются после 13. Минимальный вариант без перенумерации всего хвоста: day-memo = 12, audit-evening = 13, code-review = 14, archive-code-review = 15. Номера 10 и 11 освобождаются — их судьба вне мандата этой комнаты, но в источнике надо отметить удаление или переназначение явно, не молчаливой дырой.

[Архитектор]: Уточнение по archive-code-review. Он зависим от code-review (шаг 11 по нынешней нумерации, зависимый от шага 10). Если code-review перемещается на 14, archive-code-review следует на 15. Критичность archive-code-review наследует критичность своего хозяина code-review — `critical`. Это не отдельное решение комнаты, это вывод из транзитивности зависимости.

[Верстальщик]: Сводка по номерам для источника — кратко и без двусмысленности. После перестановки в `docs/tasks/evening-ritual-steps.json`: `day-memo` — шаг 12 (без изменений, `critical`), `audit-evening` — шаг 13 (без изменений в номере, статус меняется на `critical`), `code-review` — шаг 14 (был 10, перемещён, `critical` сохраняется), `archive-code-review` — шаг 15 (был 11, следует за code-review, `critical`). Это единственная конфигурация, которая честно читается без легенды.

[Teamlead]: Хорошо. Теперь закрываю вопрос судьбы M4 финально. Не переоткрытие — заседание `meeting-evening-auditor` не созывается повторно. Не совместимость — строка Lifecycle и требование 01.08 логически несовместимы. Не отзыв целиком — форма генератора, тернарный вердикт, `cycle.consumed` не затронуты и работают. Решение: **частичный отзыв M4** — отзывается строка Lifecycle «вечерняя цепочка после `truth.mjs cool` и `code-review`»; остальное остаётся в силе. Это минимальная хирургия, соответствующая принципу S-M3.

[Структурщик]: Фиксирую список умирающего каскадом и список выживающего — точно, без синонимов.

**Умирает** (отзывается частичным отзывом M4):
- Норма «вечерняя цепочка идёт после `truth.mjs cool` и `code-review`» (строка Lifecycle M4).

**Выживает** (не затронуто, остаётся в силе):
- Форма генератора хроники (M4).
- Тернарный вердикт хроники (M4).
- Двухфазный `cycle.consumed` (M4).
- Шов `archive-evidence` Ожегова: оба шага пишут в `docs/archive/daily-day/<date>/`, мемо первым (из `audit-evening`, не из Lifecycle M4 — независимое обоснование, не затронуто).
- Условие существования каталога Веснина (из `audit-evening`, независимо от Lifecycle M4).

[Музыкант]: Важный аккорд: выживание шва `archive-evidence` и условия каталога — именно потому, что они не зависели от строки Lifecycle M4. Они опираются на собственные обоснования в `audit-evening`. Это хорошая архитектура: форма мемо-перед-хроникой держится на своих ногах, не на позиции ревью.

[Математик]: Проверяю полноту. Вопросы повестки: (1) порядок с номерами — закрыт (12, 13, 14, 15); (2) судьба M4 — закрыт (частичный отзыв строки Lifecycle); (3) что умирает каскадом — закрыт (одна норма); (4) что выживает — закрыт (список дан); (5) критичность после перестановки — закрыт (оба `critical`); (6) поведение при падении — закрыт (правило останова, статус `incomplete`). Соседние вопросы — M3 (мерка объёма), M4 этого заседания (мемоизация против #569), M5 (память сессий), M6 (ворота качества) — упомянуты в фактуре, вердикт по ним не выносился. Пробела не вижу.

[Архитектор]: По посылкам. Список должен быть закрытым — лишняя посылка лжёт об отзыве. Перечисляю всё, на чём стоит вердикт: факты из источника, нормы предшественника M1, правило S-M3, требование владельца 01.08, фактура инцидента rt-9, определение `critical`/`noncritical`. Отдельно — посылки, пришедшие из M1 как вердикт предшественника. Это не наше решение, это вход.

[Верстальщик]: Одно уточнение к номерам. Если шаги 10 и 11 освобождаются, в источнике должна быть явная пометка — удалены, переназначены или зарезервированы. Молчаливая дыра в нумерации создаёт риск коллизии при следующей редакции JSON. Это не вердикт по соседнему вопросу — это условие корректности исполнения нашего вердикта в источнике.

[Teamlead]: Принимаю. Дыра в нумерации — технический долг исполнения, не архитектурный вопрос. Фиксируем в DoD: шаги 10 и 11 в источнике должны быть явно удалены или помечены, не оставлены молчащими. Комната сделала свою работу. Передаю в итоговое решение.

[Структурщик]: Последняя лемма перед итогом. Перестановка code-review за audit-evening — одна операция в JSON. Смена критичности day-memo и audit-evening — вторая операция (изменение поля `critical: false` → `critical: true`). Это два независимых изменения в одном источнике; оба обязательны; оба верифицируемы машинно. Вердикт этой комнаты даёт обе операции явно.

[Музыкант]: Финальный слух: после всех изменений вечерняя цепочка звучит честно. Мемо(12) → хроника(13) → ревью(14) → архив-ревью(15). Каждый шаг `critical`. Падение любого — явный `incomplete`, не фантомный зелёный. Форма хроники из M4 работает как прежде — только позиция изменилась. Это чистый такт.

---

## Итоговое решение консилиума

### Порядок трёх документов в исполняемом источнике

| Шаг | Задача | Критичность | Изменение |
|-----|--------|-------------|-----------|
| 12 | `day-memo` | **critical** | номер без изменений; критичность: `noncritical` → `critical` |
| 13 | `audit-evening` | **critical** | номер без изменений; критичность: `noncritical` → `critical` |
| 14 | `code-review` | **critical** | был шаг 10; перемещён за `audit-evening`; критичность сохранена |
| 15 | `archive-code-review` | **critical** | был шаг 11; следует за `code-review`; критичность по зависимости |

Шаги 10 и 11 в источнике: подлежат явному удалению или явной пометке — молчащая дыра в нумерации недопустима.

### Судьба вердикта M4 от 18.07

| Вопрос | Решение |
|--------|---------|
| Форма отзыва | **Частичный отзыв** — не переоткрытие, не совместимость, не отзыв целиком |
| Что умирает | Норма «вечерняя цепочка после `truth.mjs cool` и `code-review`» (строка Lifecycle M4) |
| Что выживает | Форма генератора хроники; тернарный вердикт; двухфазный `cycle.consumed` |
| Что выживает независимо | Шов `archive-evidence` Ожегова; условие существования каталога Веснина — обоснования в `audit-evening`, не в Lifecycle M4 |
| Каскад | В пределах переданного контекста: умирает одна норма; других вердиктов, зависящих от строки Lifecycle M4, в материалах не названо |

### Критичность и поведение при падении

| Ситуация | Поведение вечера |
|----------|-----------------|
| `day-memo` упал | Цепочка останавливается; `audit-evening` не запускается; `code-review` не запускается; вечер = `incomplete` |
| `audit-evening` упал | Цепочка останавливается; `code-review` не запускается; вечер = `incomplete` |
| `code-review` упал | Цепочка останавливается; вечер = `incomplete` (поведение не изменилось — был `critical`) |
| Все три завершились | Вечер = `success`; вердикт ревью честно построен на непустом входе |

---

## Список посылок

| № | Посылка | Тип | Источник |
|---|---------|-----|---------|
| 1 | Единица суждения вечера — карточка реестра; опознаётся машинно по коммиту с ref на `id` либо по ветке карточки | **факт** · вердикт предшественника M1 |
| 2 | Вход вечернего ревью — тройка `(карточка, коммиты карточки, контракт карточки)` на каждую карточку дня, вместо `consumes: []` | **норма** · вердикт предшественника M1 |
| 3 | Форма вердикта — типизированные `boundary` / `checks` / `safety`; вечер выносит реляционное суждение о множестве карточек дня | **норма** · вердикт предшественника M1 |
| 4 | Фактический порядок в `docs/tasks/evening-ritual-steps.json`: `code-review` = шаг 10, `day-memo` = шаг 12, `audit-evening` = шаг 13 | **факт** · исходный файл |
| 5 | Шаги `day-memo` и `audit-evening` объявлены `noncritical`; шаг `code-review` объявлен `critical` | **факт** · исходный файл |
| 6 | `archive-code-review` — шаг 11, зависимый от `code-review` | **факт** · исходный файл |
| 7 | Требование владельца 01.08: порядок `day-memo → audit-evening → code-review` | **норма** · решение владельца 01.08 |
| 8 | Пара мемо-перед-хроникой стоит верно и намеренно: шов `archive-evidence` Ожегова, условие существования каталога Веснина — зафиксировано в `audit-evening` | **факт** · `audit-evening` step, обоснование в источнике |
| 9 | Вердикт M4 заседания `meeting-evening-auditor` от 18.07, ратифицированный владельцем, строкой Lifecycle фиксирует: «вечерняя цепочка после `truth.mjs cool` и `code-review`» | **факт** · вердикт M4 от 18.07 |
| 10 | По правилу S-M3: отзыв нормы-предшественника обнуляет зависимые вердикты каскадом | **норма** · правило S-M3 |
| 11 | Инцидент rt-9 (15.07): `\|\| true` проглотил `exit 127`; downstream заархивировал вчерашний отчёт под сегодняшней датой | **факт** · история инцидента rt-9 |
| 12 | Форма генератора M4, тернарный вердикт и двухфазный `cycle.consumed` не содержат «позицию в цепочке» как аргумент; они независимы от строки Lifecycle M4 | **факт** · анализ структуры M4 в этом прогоне |

---

## Definition of Done

Вопрос S1 закрыт, когда выполнены все следующие условия:

- [ ] В `docs/tasks/evening-ritual-steps.json`: `code-review` перемещён на позицию после `audit-evening` (новый шаг 14), `archive-code-review` следует за ним (шаг 15).
- [ ] Шаги `day-memo` (12) и `audit-evening` (13) получили `critical: true` в источнике.
- [ ] Шаги 10 и 11 явно удалены из источника или помечены — молчащей дыры нет.
- [ ] В документации вердикта M4 от 18.07 зафиксирован частичный отзыв строки Lifecycle с датой и ссылкой на требование владельца 01.08 и этот протокол.
- [ ] Рантайм при падении `day-memo` или `audit-evening` возвращает статус `incomplete`, не продолжает цепочку и не возвращает `success`.
- [ ] Форма генератора хроники, тернарный вердикт и `cycle.consumed` не изменены (проверяется diff по M4-генератору).

---

*Реплик в диалоге: 30; каждый участник высказался не менее одного раза (Математик — 5, Архитектор — 5, Верстальщик — 5, Teamlead — 4, Структурщик — 5, Музыкант — 5; гости — не вызывались, опровержений с вещдоком не поступало).*
