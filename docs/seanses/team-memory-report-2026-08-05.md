<!-- канал: код — yarn team-memory:report (детерминированный, без LLM); база: da157e1b169f -->

# Память команды — 2026-08-05

> Форма — кристалл token 121: записал / утонуло / всплывало. v2 (02.08): вытеснение и
> всплытие — поимённо, с причиной персоны (op-log: `transfer_to_archive`, `emerge`, `reject`).

## SYSTEM
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## angelina
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## dynin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (7): dynin-2026-07-19-insight-archive-lifecycle-c2-state-axes-r5 ← архив (прямой прецедент: там я формализовал ось как тип-сумму None ⊕ positive ⊕ negative, чтобы «нет assertion» не путалось с «отрицательным assertion». Ровно та же ошибка здесь: auth схл… (полностью — в журнале)) · dynin-2026-07-22-insight-precedent-container ← архив (оттуда рабочая рамка «детектор рецидива как чистая функция + порог». Класс rejected без счётчика повторов на одном и том же ключе за окно T = слепой; нужен предикат flaky(key, wind… (полностью — в журнале)) · dynin-2026-07-25-insight-cast-carrier-contract ← архив (там мой критерий: контракт зачтён, если предикат разрешим офлайн, за один проход, без модели. Здесь ровно тот же приём — «зуб проверяет, что каждый внутренний класс отображается в … (полностью — в журнале)) · dynin-2026-07-30-sprint-honest-m8-memory ← архив (там я вводил admit(e) ⇔ holdsContract(...) как предикат допуска записи в журнал исполнения; здесь тот же ход: дверь отзыва должна срабатывать по предикату над контрактом плана (key… (полностью — в журнале)) · dynin-2026-07-18-evening-auditor-doc2-report-generator ← архив (там я ровно этот стиль формализовал: аудит как чистая функция от детерминированного входа. Здесь та же геометрия — гейт как функция от plan, planKey берёт на себя нормализацию ключ… (полностью — в журнале)) · dynin-2026-07-23-tasks-workshop-m4a-inspect ← архив (там я настаивал: паспорт объекта задаётся кортежем и предикат разрешимости — единственный судья. Здесь planKey(plan) = sprintId ?? planId — ровно такой кортежный ключ; аудит класса… (полностью — в журнале)) · dynin-2026-07-23-llm-procedure-channels-m3b-telemetry ← архив (тот же контур каналов процедур, где я фиксировал форму события транспорта; сегодняшний словарь ошибок — прямое расширение той телеметрии, и errorClass из v1-события теперь получает… (полностью — в журнале))

## farrell
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## kuryokhin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## ozhegov
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (1): ozhegov-2026-07-14-agent-tooling-friction-2 ← архив (там я уже настаивал на «обёртке над существующим», а не переписывании ядра; ровно этот принцип определяет ответ на (3) — понижение как отдельный слой поверх planPrepushTypecheck, н… (полностью — в журнале))

## rodchenko
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## tarasov
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (2): tarasov-ritual-magistral-source-freshness-cut-1 ← архив (прямой прецедент моей же нарезки: систематическая недооценка объёма на 40% суммой при 4 блоках. Ставит вопрос к оценкам этого спринта до ратификации.) · tarasov-2026-08-01-evening-review-predicate-m1-subject ← архив (карточка судится по коммитам; блок 2 при закрытии #1549 требует явной сшивки коммитов со спринт-темой, иначе вечер её не увидит.) · и отвергнутых облаков: 2

## vesnin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (3): vesnin-2026-07-17-truth-graph-q1-adr-or-registry ← архив (там я держал ту же линию: не плодить вторую копию реестра, свести через единый механизм при узкой рамке вопроса; здесь ровно та же развилка «одна таблица знания vs join двух осей».) · vesnin-2026-07-19-insight-archive-lifecycle-c2-state-axes-r4 ← архив (прямой прецедент: «форма следует функции, декор без конструкции недопустим», и там же — асимметрия зубьев по разным subjects. Здесь ровно тот же ход: два зуба на две разные находки… (полностью — в журнале)) · vesnin-2026-07-16-code-review-honesty-refactor ← архив («имя обещает — функция обязана исполнить»: там code-review не ревьюил код; здесь зонд обязан различать «канал без пробы» и «проба без канала», иначе имя лжёт. Прямой аналог принцип… (полностью — в журнале))

**Итог:** записано 0 · вытеснено 0.
