<!-- канал: код — yarn team-memory:report (детерминированный, без LLM); база: 6ab7236ef193 -->

# Память команды — 2026-08-19

> Форма — кристалл token 121: записал / утонуло / всплывало. v2 (02.08): вытеснение и
> всплытие — поимённо, с причиной персоны (op-log: `transfer_to_archive`, `emerge`, `reject`).

## SYSTEM
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## angelina
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## dynin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (2): dynin-2026-08-17-server-plugin-foundation-m6r-first-wave ← архив (там я уже фиксировал состав RunRecord и адрес (pluginId, version, collectionId, runId, mountTarget); текущий b4 — ровно вход, порождающий такую запись, и швы совпадают.) · dynin-2026-07-23-llm-procedure-channels-m4a-agent-usage ← архив (там же схема идемпотентности через клиентский eventId и dedupe; здесь ровно этот приём нужен для повторов POST /request.)

## farrell
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## kuryokhin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (2): kuryokhin-2026-06-09-media-library-dataset ← архив (про то же самое: реальному датасету нужен путь «запись с микрофона + метка класса + метаданные», иначе команда копирует файлы руками. Спутник узла — как раз слой метаданных, без ко… (полностью — в журнале)) · kuryokhin-2026-06-09-media-library-dataset ← архив (там я как раз настаивал, что без строки «класс + условия записи» датасет утечёт в копипасту руками; сейчас спутник узла именно это и вводит на входе. Проверяю, что моя тогдашняя пр… (полностью — в журнале))

## ozhegov
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (17): ozhegov-2026-08-17-server-plugin-foundation-m4-triggers ← архив (там я уже отклонял string[] вместо PluginTrigger[]: тот же дефект в b2 будет, если zod продублирует форму вместо вывода из контрактов.) · ozhegov-2026-08-13-network-container-m5-storage ← архив (там я уже развёл «ленту» (append-only jsonl наблюдений) и «перезаписываемый снимок» (state узла); для Firebat это ровно тот же водораздел: наблюдения = лента наружу, локальный WAV-… (полностью — в журнале)) · ozhegov-2026-06-17-desktop-product-line ← архив (там я держал границу «парринг и WebSocket — уровень device-board, независим от звука»; здесь та же норма: identity Firebat идёт через device-board, а не приклеивается к capture-цеп… (полностью — в журнале)) · ozhegov-2026-07-29-tariff-grid-m2-rail ← архив (рельс носителей DTO→registry применим к «узел→приёмник»: тот же паттерн Prisma-поле → wire DTO → потребитель.) · ozhegov-2026-08-17-server-plugin-foundation-m2-mount-homes ← архив («дом» приёма для background-media уже канон: Nest-модуль с контрактом регистрации/приёма — сюда садится firebat-node.) · ozhegov-2026-08-17-server-plugin-foundation-m2-mount-homes ← архив (там я уже фиксировал контракт «дома» плагина в background-: точка регистрации, границы модуля, что экспорт публичен только через фасад. Нарезка firebat-node-device по блокам — тот … (полностью — в журнале)) · ozhegov-2026-08-17-server-plugin-foundation-m1-vocabulary ← архив («дом словаря» вне одного background-: если у нас poll-api и poller — это разные леммы, они обязаны жить в разных типах/файлах, а не в одном types.ts с двумя значениями.) · ozhegov-2026-08-17-server-plugin-foundation-m2-mount-homes ← архив («дом = Nest-модуль в background-media с явным контрактом приёма»: ровно паттерн для b3 (packages/background-media/src/modules/firebat-node), фиксирую как прецедент границы дома.) · ozhegov-2026-08-17-server-plugin-foundation-m1-vocabulary ← архив (норма закрытого словаря исходов: b3 обязан вернуть ok · stale_key · backoff как лемму, без синонимов; беру как чек публичного контракта poll.) · ozhegov-2026-08-17-server-plugin-foundation-m1-vocabulary ← архив (там я уже фиксировал, что «дом» словаря/контракта нельзя ставить внутрь одного background-сервера, если контракт потребляется несколькими. Здесь ситуация обратная и подтверждает Р2… (полностью — в журнале)) · ozhegov-2026-08-17-server-plugin-foundation-m2-mount-homes ← архив (про «дом = Nest-модуль с публичным контрактом через index.ts, внутренности не просачиваются». Прямо ложится на Р2: packages/background-media/src/modules/firebat-node/ с экспортом ч… (полностью — в журнале)) · ozhegov-2026-08-17-server-plugin-foundation-m2-mount-homes ← архив (там я уже фиксировал: «дом в background-» = Nest-модуль с явным контрактом приёма; здесь ровно тот случай — firebat-node как дом внутри background-media, и мы обязаны назвать носит… (полностью — в журнале)) · ozhegov-2026-07-19-registry-relocation-m0-order ← архив (оттуда правило: инвариант «единственный писатель» и явная форма холодного хранилища важнее удобства; ключ узла — отдельная сущность жизненного цикла (issue/revoke), и склеивать её … (полностью — в журнале)) · ozhegov-2026-08-17-server-plugin-foundation-m2-mount-homes ← архив (прямо про «дом = Nest-модуль с публичным контрактом приёма»: тот же вопрос, где монтируется точка приёма (здесь — FirebatNodeModule в app.module.ts), и почему это часть зоны, а не … (полностью — в журнале)) · ozhegov-2026-08-06-static-mmbrn-container-m5-affine-role ← архив (закрытый словарь операций (upload-revision, write-metadata): подтверждает, что «сдать результат» и «залить файл» разводятся не двумя ручками, а одной операцией upload-revision, выз… (полностью — в журнале)) · ozhegov-2026-08-17-server-plugin-foundation-m1-vocabulary ← архив (тем же приёмом фиксирую словарь исходов и .env-контракт как леммы до подписи.) · ozhegov-2026-07-23-llm-procedure-channels-m2b-control-plane ← архив (там я держал границу «канал = замкнутый путь носителей до потребителя»; здесь та же проверка: путь «оператор → -NodeKey → .env узла → poller» замкнут, служебный токен в него не вхо… (полностью — в журнале))

## rodchenko
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## tarasov
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (1): tarasov-2026-08-13-network-container-m6-k1-rules ← архив (там я держал планку «один вердикт без довесков»; здесь та же ситуация: приёмка предмета спринта не должна размазаться на пойманное деплоем.)

## vesnin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (1): vesnin-2026-08-03-static-mmbrn-container-m1-boundary ← архив (тот же приём: положительная граница + отрицательная + инварианты. Здесь форма моста ровно так и построена (что соединяется, против альтернатив, словарь исходов) — образец применён,… (полностью — в журнале))

**Итог:** записано 0 · вытеснено 0.
