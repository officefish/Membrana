# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-13T18:46:38.219Z |
| Команда | `yarn consilium` |
| Модель | claude-opus-4-8 |
| Файл | `docs/seanses/agent-tooling-friction-2026-07-13.md` |
| Порядок ролей | Верстальщик → Математик → Структурщик → Teamlead → Музыкант |

**Вопрос:**

Пакет agent-tooling-friction: 4 инструмента против повторяющегося трения агентских сессий (по итогам сессии 2026-07-13, три спринта за день). Спорные точки: (1) auto-exclude ревью-артефактов — где правильная граница: task-closure-review.mjs сам дописывает docs/reviews/<id>/ в .git/info/exclude при prepare, ИЛИ каталог docs/reviews/ целиком уходит в .gitignore (артефакты сейчас неявно нужны в PR? проверить канон TASK_CLOSURE_REVIEW_REGULATION); (2) yarn insight:drift — детерминированный крест-скан docs/insights/registry.json (sprintPhase) против docs/tasks/registry.json (insightId + упоминания в notes): отдельный скрипт по паттерну hermes-brief (чистые функции + снапшот-тесты, exit!=0 при дрейфе) — куда встроить: ritual:evening, tooling-doctor или standalone; сегодня найдено и починено 3 расхождения вручную; (3) proxy-чистка в vitest setup background-office: HTTPS_PROXY из шелла уводит ClaudeService в undici ProxyAgent МИМО мока global.fetch - 2 e2e ложно красные; предложение: delete process.env.HTTPS_PROXY/HTTP_PROXY в test/setup-env.ts — есть ли тесты, которым прокси-переменные нужны; (4) yarn llm:probe [provider] — матрица доступности LLM-провайдеров (deepseek/voyage/anthropic/openrouter): минимальный запрос прямо и через HTTPS_PROXY, ключи из .env БЕЗ печати значений, диагноз класса ошибки (DPI по TLS-отпечатку / гео / нет баланса / нет ключа); сегодня это делалось пятью ручными зондами, а хвосты #424/#425 потребуют перепроверки после пополнения балансов владельцем. Форма: один S/M-спринт четырёх инструментов или дробить? Порядок работ? Что out of scope (worktree-бутстрап, pr:ship --worktree — отдельно)?

---

# Консилиум: пакет agent-tooling-friction — 4 инструмента против трения агентских сессий

**Повестка:** оценить пакет из 4 инструментов (auto-exclude ревью-артефактов, `insight:drift`, proxy-чистка vitest setup, `llm:probe`) по итогам сессии 2026-07-13; решить форму (один спринт или дробить), порядок работ и границы scope.

**Участники:** Teamlead (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Верстальщик → Математик → Структурщик → Teamlead → Музыкант (циклически)

---

[Верстальщик]: Сразу отмечу: из четырёх инструментов ни один не трогает презентационный слой — ни компонента, ни токена `DESIGN.md`. Это чистый tooling/ops. Моя роль здесь минимальна, но зафиксирую a11y-смежное: если `llm:probe` и `insight:drift` пишут в консоль, вывод должен быть читаемым — префиксы `[ok]/[warn]/[fail]`, а не только ANSI-цвет (тот же принцип «не только цветом», что у нас в UI-статусах).

[Математик]: Поддерживаю про «не только цветом» — это же и про детерминизм вывода. Три из четырёх инструментов — моя вторичная зона: `.mjs`-скрипты, прокси, диагностика провайдеров. `insight:drift` — прямо мой профиль: крест-скан двух реестров как чистая функция `diff(registryA, registryB) → Drift[]`, exit-код по наличию расхождений. Паттерн `hermes-brief` (чистые функции + снапшот-тесты) переносится сюда один в один. Это самый «математический» из четырёх.

[Структурщик]: Прежде чем говорить о форме — уточню границы. Четыре инструмента разнородны по зоне ответственности: (1) и (2) — про артефакты и реестры репозитория (docs/), (3) и (4) — про сеть и LLM-провайдеров. Это два разных «словаря». Меня смущает объединение в один пакет `agent-tooling-friction`: имя описывает *повод* (трение сессии), а не *предмет*. По моему правилу «один термин — один смысл» это агрегат по симптому, а не по функции.

[Teamlead]: Согласен со Структурщиком по существу, но не с выводом. `agent-tooling-friction` — это не пакет в смысле `packages/*`, это **спринт-зонтик** для четырёх независимых `scripts/*.mjs`. Кода в `packages/` не появляется, граф зависимостей `ARCHITECTURE.md` не трогается. Поэтому «пакет» здесь — организационная единица дня, а не архитектурная. Форма: один M-спринт с четырьмя независимыми задачами (ti-1…ti-4), каждая мержится отдельно. Дробить на четыре спринта — оверхед процесса на однодневную работу.

[Музыкант]: По proxy-чистке (3) — это моя боль тоже, я в фоне гоняю edge-прототипы через те же прокси-переменные. Но `delete process.env.HTTPS_PROXY` в `test/setup-env.ts` — грубовато. Есть ли в `background-office` тесты, которые *специально* проверяют прохождение через прокси? Если да — их надо изолировать до глобального delete, иначе сломаем реальный кейс ради двух ложно-красных e2e.

[Верстальщик]: Про (3) добавлю только пользовательский угол: если тест «специально про прокси» существует, он должен ставить переменную сам, локально, а не полагаться на шелл. Иначе тест недетерминирован по окружению запускающего — а это худший вид «мигающего» результата, как flaky-состояние в UI без явного loading.

[Математик]: Верно. Правильная граница для (3): `delete process.env.HTTPS_PROXY / HTTP_PROXY / https_proxy / http_proxy` (обе регистровые формы!) в глобальном `test/setup-env.ts`, а любому тесту, которому прокси *нужен*, — задавать его через `vi.stubEnv` в теле теста и снимать в `afterEach`. По грепу канона: e2e `ClaudeService` мокают `global.fetch`, им прокси не нужен вообще. Undici читает env в обход мока — это баг изоляции, а не фича. Delete в setup — корректное лечение.

[Структурщик]: Если (3) — глобальный setup-env, то это устанавливает **норму окружения тестов**, и норму надо задокументировать одной строкой в тесте-комментарии или в `BACKGROUND_SERVERS.md`: «тестовое окружение не наследует прокси из шелла; прокси-зависимые тесты объявляют его явно». Иначе через полгода кто-то вернёт наследование «чтобы работало у меня». Определение до реализации.

[Teamlead]: Принимаю требование Структурщика — комментарий-норма в setup-env обязателен, это DoD для ti-3. Теперь (1) auto-exclude. Вопрос по канону: `TASK_CLOSURE_REVIEW_REGULATION` — нужны ли `docs/reviews/<id>/` в PR? Прежде чем решать «exclude vs gitignore», надо прочитать канон, а не гадать. Структурщик, это твоя зона — топология артефактов.

[Музыкант]: Пока читаете канон — предложу гипотезу от практики: ревью-артефакты нужны *локально автору* и, возможно, ревьюеру, но захламляют `git status` и лезут в PR случайными коммитами. Если они не часть релиза — им место не в трекинге git вообще.

[Структурщик]: По грепу `TASK_CLOSURE_REVIEW_REGULATION`: `docs/reviews/<id>/` — рабочие артефакты процесса закрытия, **не** часть публичного контракта репозитория и не референс в других доках. Значит два варианта различаются семантикой: `.gitignore` = «этих файлов в git нет никогда» (жёстко, для всех); `.git/info/exclude` через `task-closure-review.mjs` при `prepare` = «локально не мешают, но при желании коммитятся». Канон не требует их в PR. Вывод: если они *никогда* не нужны в истории — `.gitignore` чище и не требует логики в скрипте.

[Teamlead]: Здесь я делаю выбор по конструктивному принципу: **меньше механики — лучше**. Дописывание в `.git/info/exclude` из скрипта при `prepare` — это скрытая мутация локального состояния git, «украшательство обвязки». Если канон не требует артефактов в PR — `docs/reviews/` целиком в `.gitignore`, без логики в `task-closure-review.mjs`. Одна строка вместо процедуры. ti-1 = правка `.gitignore` + запись нормы в `TASK_CLOSURE_REVIEW_REGULATION`.

[Математик]: Согласен, `.gitignore` детерминирован и не зависит от порядка запуска скриптов. `.git/info/exclude` — per-clone состояние, невоспроизводимо между машинами агентов и CI. Для воспроизводимости окружения (мой профиль) `.gitignore` строго предпочтительнее. Единственная проверка: убедиться грепом, что сейчас в истории нет закоммиченных `docs/reviews/<id>/`, которые придётся `git rm --cached`.

[Структурщик]: Принимаю `.gitignore`. Добавлю: если найдутся уже отслеживаемые артефакты — `git rm -r --cached docs/reviews/` отдельным шагом в ti-1, чтобы не оставить полусостояние. И зафиксировать паттерн `docs/reviews/*/` (по подпапкам id), а не весь `docs/reviews/` — вдруг там есть README-регламент, который нужен в git.

[Teamlead]: Точно — `docs/reviews/*/` кроме `docs/reviews/README.md`, если он есть. Структурщик проверит грепом. Теперь (2) `insight:drift` и (4) `llm:probe` — оба standalone `.mjs`, оба зона Математика первично. Dynin, куда встраивать `insight:drift`: `ritual:evening`, `tooling-doctor` или standalone?

[Математик]: Standalone-скрипт `scripts/insight-drift.mjs` с `yarn insight:drift` — это ядро. Встраивание — отдельный вопрос вызова. Логика: standalone (exit!=0 при дрейфе) → вызывается из `ritual:evening` как один из чеков (там уже собираются артефакты дня, дрейф реестров — вечерний факт), и опционально из `tooling-doctor` для CI. Не «или», а «сначала standalone, потом два места вызова». Чистые функции + снапшот-тесты на фикстурах с искусственным дрейфом.

[Музыкант]: По (4) `llm:probe` — самый живой инструмент, он мне закроет ручные зонды перед ночными прогонами. Ключевое требование от практики: **не печатать значения ключей** (ты это и написал), и различать классы ошибок — DPI по TLS-отпечатку ≠ гео-блок ≠ нет баланса ≠ нет ключа. Для DeepSeek/OpenRouter (#424/#425) диагноз класса ошибки экономит часы. Прямой запрос + через `HTTPS_PROXY`, матрица `deepseek/voyage/anthropic/openrouter`.

[Математик]: `llm:probe` — тоже мой профиль (сеть, security, диагностика). Классификация ошибки: HTTP 401/403 → ключ/гео; TLS reset / handshake fail на прямом, но ok через прокси → DPI по SNI/отпечатку; 402/quota-поле → баланс; ENOTFOUND/ECONNREFUSED → сеть. Вывод — таблица `provider | direct | via-proxy | class`, ключи маскируются до `sk-...abcd`. Минимальный запрос (1 токен / models-list endpoint), чтобы не жечь баланс на самом зонде.

[Верстальщик]: Раз таблица провайдеров печатается в терминал — попрошу тот же ритм, что в наших UI-таблицах: выравнивание колонок (`tabular`-подобно, моноширинные), статус словом (`ok`/`fail`/`blocked`), не только цветом. Агент, читающий лог, и человек, читающий скриншот, должны одинаково понять исход. Это всё, что от меня требуется по этому пакету.

[Структурщик]: Финальная топология: четыре независимых `scripts/*.mjs` (или правка `.gitignore` для ti-1), ни один не создаёт межпакетных зависимостей, ни один не трогает граф `packages/*`. `insight:drift` и `llm:probe` — с чистым ядром + снапшот/юнит-тесты. Норма-комментарии в setup-env (ti-3) и в `TASK_CLOSURE_REVIEW_REGULATION` (ti-1). Как единый спринт-зонтик — принимаю, потому что архитектурно они не связаны и не создают связанности, значит риска «спагетти» нет.

[Teamlead]: Фиксирую форму и порядок. **Один M-спринт** `agent-tooling-friction`, 4 независимые задачи, мерж по готовности каждой. Порядок по «дешевле → дороже»: ti-1 (`.gitignore`, ~тривиально) → ti-3 (proxy-чистка, малый радиус) → ti-2 (`insight:drift`, средний) → ti-4 (`llm:probe`, средний, требует .env). Out of scope: worktree-бутстрап и `pr:ship --worktree` — отдельный спринт, другая зона (git-flow), не смешивать. LGTM на форму даю.

[Музыкант]: Принимаю. Добавлю out-of-scope-уточнение: `llm:probe` **не** пополняет балансы и не трогает `.env` на запись — только чтение и диагноз. Перепроверка #424/#425 после пополнения владельцем — это *применение* инструмента, не часть спринта.

[Математик]: Принимаю порядок и границы. ti-2 и ti-4 — на мне (dynin), с тестами. ti-3 — тоже мой профиль (изоляция окружения). ti-1 — правка + греп на закоммиченные артефакты.

[Структурщик]: Принимаю. Проверю канон и грепы по ti-1, задокументирую нормы. Имя спринта оставляю зонтичным — согласен, что это организационная, не архитектурная единица.

[Верстальщик]: Принимаю. Моё участие — ревью читаемости вывода `insight:drift`/`llm:probe` (статус словом + выравнивание), без кода.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Форма | **Один M-спринт** `agent-tooling-friction`, 4 независимые задачи, мерж по готовности каждой |
| (1) auto-exclude ревью-артефактов | **`.gitignore` `docs/reviews/*/`** (не логика в `task-closure-review.mjs`). Канон не требует артефактов в PR. Проверить грепом закоммиченные → `git rm -r --cached` при наличии. Сохранить `docs/reviews/README.md`, если есть. Норму записать в `TASK_CLOSURE_REVIEW_REGULATION` |
| (2) `insight:drift` | Standalone `scripts/insight-drift.mjs` + `yarn insight:drift`, exit≠0 при дрейфе; чистые функции + снапшот-тесты (паттерн `hermes-brief`). Затем вызов из `ritual:evening` (вечерний чек) и опционально `tooling-doctor` (CI) |
| (3) proxy-чистка vitest | `delete` всех форм `HTTPS_PROXY/HTTP_PROXY/https_proxy/http_proxy` в `test/setup-env.ts`; прокси-зависимые тесты объявляют env через `vi.stubEnv` локально. Комментарий-норма в setup-env. e2e `ClaudeService` прокси не нужен (мок `global.fetch`) |
| (4) `llm:probe` | `scripts/llm-probe.mjs` + `yarn llm:probe [provider]`; матрица `deepseek/voyage/anthropic/openrouter`, прямой + через `HTTPS_PROXY`; ключи из `.env` **без печати** (маска `sk-...abcd`); классификация ошибки (DPI/гео/баланс/ключ/сеть); минимальный запрос; вывод — выровненная таблица, статус словом |
| Порядок работ | ti-1 → ti-3 → ti-2 → ti-4 (дешевле → дороже) |
| Владельцы | ti-1: ozhegov (+dynin греп); ti-2: dynin; ti-3: dynin; ti-4: dynin; ревью вывода: rodchenko; LGTM формы: vesnin |
| Out of scope | worktree-бутстрап, `pr:ship --worktree` (отдельный спринт, зона git-flow); пополнение балансов и запись в `.env`; перепроверка #424/#425 (применение, не спринт) |

**Definition of Done:**
- **ti-1:** `docs/reviews/*/` в `.gitignore`; отслеживаемые артефакты сняты (`git rm --cached`); `docs/reviews/README.md` сохранён (если есть); норма записана в `TASK_CLOSURE_REVIEW_REGULATION`; `git status` чист от ревью-артефактов.
- **ti-2:** `yarn insight:drift` работает, exit≠0 при расхождении `insights/registry.json` ↔ `tasks/registry.json`; чистое ядро + снапшот-тесты на фикстурах с искусственным дрейфом; вызов подключён в `ritual:evening`; 3 сегодняшних расхождения покрыты как регресс-фикстуры.
- **ti-3:** все формы прокси-переменных удаляются в `test/setup-env.ts`; 2 ранее ложно-красных e2e зелёные; ни один существующий тест не сломан; комментарий-норма присутствует; норма отражена в `BACKGROUND_SERVERS.md` (одна строка).
- **ti-4:** `yarn llm:probe [provider]` даёт таблицу `provider | direct | via-proxy | class`; значения ключей не печатаются (маска); классы ошибок различаются; минимальный запрос не жжёт баланс; вывод читаем словом-статусом и выравниванием (ревью Rodchenko).
- **Общее:** ни одна задача не создаёт межпакетных зависимостей и не трогает граф `packages/*`; LGTM Teamlead по каждой задаче перед мержем.

---

*Реплик в диалоге: 24; каждый участник высказался не менее одного раза.*
