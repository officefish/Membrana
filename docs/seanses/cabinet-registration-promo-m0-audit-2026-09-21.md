# Аудит M0 заседания «Регистрация в кабинете по промокоду панели» — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Задание: `docs/meeting/cabinet-registration-promo/AUDIT_M0_BRIEF.md`. Предмет: повестка `docs/meeting/cabinet-registration-promo/M0_AGENDA.md` и протокол `docs/seanses/cabinet-registration-promo-m0-order-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`). Ствол для сверки: `origin/main` = `8403b5a0`. Протокол и повестка не правились.

## Итог

**ОТКЛОНЕНО.** FAIL по пунктам 1, 3, 4, 7, 8. Самый тяжёлый — пункт 4: вердикт раскладывает серверный клиент офиса и интеграцию регистрации в `apps/cabinet/src/services/` — каталога нет в стволе, а `apps/cabinet` — браузерное приложение на Vite/React, куда ключ офиса класть нельзя. Раскладка на сессии стоит на несуществующих путях.

## Таблица

| # | Пункт | Вердикт | Цитата / команда |
|---|---|---|---|
| 1 | Один вопрос | **FAIL** | Утечки следующих комнат превращены в решения вердикта. Содержание К3: «Третья … К3 — задание: порядок „сначала погасить, затем создать“» (стр. 144) и П5/«Что не требует заседания: К3 (кроме режима отказа) — применение паттерна #2313» (стр. 155) — из реплики [Математик] «сначала погасить код в офисе, затем создать пользователя в кабинете» (стр. 48). Содержание К2: П10 «клиент офиса размещается как сервис внутри кабинета … не как отдельный пакет» помечена **нормой** (стр. 181) — из [Музыкант] стр. 66 и [Архитектор] стр. 68. Содержание К4: П9 «одна фраза ошибки на все неудачи — норма безопасности» (стр. 180). Порядок вопросов (Q0) решён, но три решения по содержанию кандидатов вынесены в вердикт под видом норм и заданий. |
| 2 | Вердикт есть и он про порядок | PASS | Граф с рёбрами и причинами — таблица «Рёбра и их причины» (стр. 119–126); не-рёбра — таблица стр. 128–134; три сессии с границами файлов — стр. 138–144; «Что не требует заседания» — стр. 148–155; «Что решается только в комнате» — стр. 157–164. Все четыре части на месте. Качество границ — пункт 4 и 8. |
| 3 | «Список посылок» | **FAIL** | Секция есть (стр. 168), типы у всех десяти. Ссылок вида «[Роль] #N» нет ни одной: реплики в протоколе не нумерованы (`grep -oE "\[(Роль)\] #[0-9]+" → 0`), основание даётся цитатой. **П1 — без реплики вовсе**: основание «Фактура повестки: …» (стр. 172), `sed -n 172p \| grep -c реплика → 0`. Из девяти цитат четыре — не дословные: П2 «К1 → К2: клиент строится под конкретную дверь» (в реплике: «К1 → К2 (клиент строится под дверь и грант, решённые в К1)», стр. 56; `grep -cF → 0`); П4 «…чем К2 зафиксировал добавления» (в реплике «свои добавления», стр. 52); П8 «К2: config/env.schema.ts, office-promo.service.ts. К4: LoginPage.tsx, auth.ts» — пересказ стр. 76; П5 и П10 дословны с точностью до заглавной буквы. Дословны: П3, П6, П7, П9. |
| 4 | Фактура против ствола | **FAIL** | Проверено 13 утверждений командой `git show origin/main:<путь> \| sed -n '<строка>p'` и `git cat-file -e origin/main:<путь>`. Подтверждены: `panel-users-core.ts:24` → `export interface PromoCode {`; `:191` → `if (promo.expiresAt !== null && promo.expiresAt <= nowSec) return null;`; `:192` → `if (promo.usedCount >= promo.maxUses) return null;`; `panel-users.controller.ts:75` → `@Post('register')`; `auth.service.ts:15` → `async register(login: string, password: string)`; `env.schema.ts:37` → `ALLOW_REGISTRATION: boolFromEnv.optional(),`; `:47` → `ALLOW_REGISTRATION: data.ALLOW_REGISTRATION ?? data.NODE_ENV === 'development'`; `deploy/generate-cabinet-env.sh:34` → `ALLOW_REGISTRATION=false`; `membrane.service.ts:157` → `async getOrCreateMembraneForUser(userId: string) {`; `deploy/root-site/index.html:154` → `<p class="card-title">Кабинет — регистрация</p>`; `LoginPage.tsx`, `api/auth.ts` — есть; #2313 — MERGED `2bddfd21`. **Нет в стволе:** `apps/cabinet/src/services/` (стр. 143, 181), `apps/cabinet/src/services/auth.service.ts` — назван как файл интеграции К3 (стр. 76, 144), `apps/cabinet/src/config/env.schema.ts` — вердикт кладёт `config/env.schema.ts` в одну строку с путями `apps/cabinet/src/…` (стр. 143). Существуют `packages/background-cabinet/src/modules/auth/auth.service.ts` и `packages/background-cabinet/src/config/env.schema.ts`. `apps/cabinet/src/` содержит `api, components, context, lib, pages, plugins` и читает `import.meta.env.VITE_*` — это браузер. Дверь регистрации кабинета — `packages/background-cabinet/src/modules/auth/auth.controller.ts:21` `@Post('register')`, в вердикте не названа. Вывод: серверный клиент офиса с ключом и интеграция регистрации отнесены к браузерному приложению. |
| 5 | Данности не переоткрыты | PASS | Вердикт не обсуждает «в кабинете или в панели»; правда о коде остаётся в панели: «К1: дверь — новый маршрут „проверить-и-погасить“ в офисе» (стр. 162); второй системы кодов не заводится — «грант … в `PromoCode.grants[]` или отдельный вид кода» (стр. 161), оба варианта внутри панели. |
| 6 | Словарь | PASS (замечание) | «Панель», «кабинет», «код», «грант», «дверь» выдержаны. В вердикте (стр. 100–195): «эндпоинт» ×3, «клиент» ×4, «паттерн» ×2, «Definition of Done» ×1, «ADR» ×1 — `sed -n '100,195p' \| grep -oiE … \| uniq -c`. «Эндпоинт» вместо «дверь» в собственных таблицах вердикта (стр. 126, 132) — замечание, не провал. |
| 7 | Реплик не меньше тридцати | **FAIL** | Механика: `grep -cE "^\[(Архитектор\|Верстальщик\|Математик\|Teamlead\|Структурщик\|Музыкант)\]" → 27`; по ролям: Архитектор 5, Верстальщик 5, Teamlead 5, Математик 4, Структурщик 4, Музыкант 4 — шесть советчиков представлены. Условия прогона: `--min-replies 30` (стр. 26). Футер инструмента: «Реплик в диалоге: 27; каждый участник высказался не менее одного раза» (стр. 195) — число ниже заказанного минимума, отказа нет. Побочно: вход «координация ролей `docs/VIRTUAL_TEAM_PROMPT.md` — обрезан» (стр. 20). |
| 8 | Порядок исполним тремя сессиями без общих файлов | **FAIL** | Общий файл двух сессий: `config/env.schema.ts` — сессия вторая, К2: «`config/env.schema.ts`, `apps/cabinet/src/services/office-promo.service.ts`» (стр. 143); сессия третья, К3: «флаг `ALLOW_REGISTRATION` в `config/env.schema.ts:37,47` … К3 должен либо обойти его … либо переключить» ([Структурщик] стр. 88; [Структурщик] стр. 52: «`config/env.schema.ts` затрагивают и К2 … и К3»). Вердикт сам признаёт пересечение и снимает его очерёдностью: «конфликт по `config/env.schema.ts` явно снят последовательностью сессий» (стр. 188). По букве пункта — две сессии, один файл. Внутри параллельной пары К2 ∥ К4 (сессия вторая) пересечений нет — но её граница проведена по путям, которых нет в стволе (пункт 4), и К4-форма требует двери сервера `auth.controller.ts:21`, не отнесённой ни к одной сессии. |

## Что аудитор не делает

Порядок кандидатов не предлагается; протокол и повестка не правлены; повторный прогон — решение председателя после ратификации владельца.
