# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-16T06:50:04.450Z |
| Команда | `yarn consilium` |
| Модель | claude-opus-4-8 |
| Файл | `docs/seanses/ritual-inputs-echo-and-extracts-2026-07-16.md` |
| Порядок ролей | Структурщик → Верстальщик → Музыкант → Teamlead → Математик |
| Повестка | `docs/seanses/ritual-inputs-echo-and-extracts-topic-2026-07-16.md` |

**Вопрос:**

Две темы, которые первый консилиум 16.07 обошёл молча. ТЕМА 1 — эхо-камера: detection-planning-priorities.mjs подключён к трём шагам ритуала, и одна замороженная строка от 06.07 прозвучала в таблице обоснования как три независимых подтверждения; единственный несогласный источник оказался единственным правым и был отброшен. Должен ли план помечать производные источники, кто считает независимость, и какое правило разрешения конфликта план-vs-факт? ТЕМА 2 — предложение владельца: экстракты сессий Claude Code/Codex/Cursor как вход планирования. 114 jsonl лежат неиспользованными, машинерия дистилляции (persona-memory-extract: без LLM, provenance, токен-бюджет) готова, недостаёт источника. Но сессия знает намерение, а не результат — и это ровно тот класс источника, который сегодня подвёл. Экстракт — гипотеза для сверки с кодом или факт для плана? Что попадает в репозиторий и как гейтить секреты? Не станет ли это третьим эхом: агент писал, агент дистиллировал, агент спланировал? Решения с размером и приоритетом, реализуемые ПОСЛЕ дедлайна FREE ~17.07. Если тема не готова — скажите прямо, не выдавайте компромисс ради протокола.

---

# Консилиум: экстракты сессий агентов и защита от эхо-камеры во входах ритуала

**Повестка:** два обойдённых первым консилиумом корня — (1) подключать ли экстракты сессий Claude Code / Codex / Cursor как вход планирования дня и в какой форме; (2) как во входах ритуала отличать независимые источники от отражений одного снимка (защита от эхо-камеры). Оба — про ВХОДЫ ритуала, не про проверку выхода.

**Участники:** Teamlead (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Структурщик → Верстальщик → Музыкант → Teamlead → Математик (циклически)

---

[Структурщик]: Начну с терминов, иначе оба вопроса поплывут. «Экстракт сессии» и «источник планирования» — разные леммы. Сессия агента (jsonl в `~/.claude/projects/`) — это **свидетельство о работе**, а не решение о работе. Мы уже решили 16.07: маркер в коде первичен. Значит экстракт сессии — не новый источник истины, а **третий класс свидетельства** рядом с «код» и «Issue»: гипотеза-для-сверки. Если так его и назвать в контракте — он ляжет в граф чисто, если назвать «входом планирования» — размоем уже принятую иерархию.

[Верстальщик]: Поддержу разделение, но добавлю про «эхо-камеру» с конструктивной стороны — это в первую очередь **проблема отображения**, а уже потом данных. Сегодняшний сбой: один снимок `detection-planning-priorities.mjs` выглядел в таблице «Почему магистраль» как три независимые галочки. Читатель не отличил отражение от подтверждения, потому что источники **не были размечены**. Любой вход ритуала обязан нести в таблице обоснования колонку «происхождение»: `код` / `issue` / `сессия` / `снимок-хардкод`, и производные — с явной стрелкой на родителя. Без метки происхождения три отражения всегда выглядят как три голоса.

[Музыкант]: Из аудио-опыта скажу: это ровно та же болезнь, что декоррелированность детекторов в ансамбле — мы 13.07 решили, что yamnet ценен не точностью, а тем, что **ошибается не там же**, где DSP. Три источника, производные от одного снимка, полностью коррелированы — вес ансамбля от них равен весу одного. Значит правило простое и оно уже в нашей ДНК: **веса складываются только по независимым источникам**. Снимок-хардкод, issue-статус и сессия, описывающие один факт, — это один голос, не три. Технически: перед подсчётом «за магистраль» надо схлопывать корродированные входы.

[Teamlead]: Фиксирую рамку, чтобы второй консилиум не повторил ошибку первого — не утонуть в громком вопросе, оставив тихий. Два вопроса, оба про входы. По первому: экстракты сессий беру как **свидетельство**, не как источник истины — терминология Структурщика принята, это разрешает половину риска приватности сразу (свидетельство сверяется, не исполняется). По второму: эхо-камера — это про **provenance-разметку и дедупликацию по родителю**, форму задаёт Верстальщик (колонка) и Музыкант (схлопывание весов). Прошу Математика дать оба численно: что такое «независимый источник» формально и как дёшево детектировать корреляцию.

[Математик]: Дам оба формально. Эхо-камера: у каждого свидетельства есть **origin-hash** — хеш первоисточника факта. Снимок `detection-planning-priorities.mjs` от 06.07 → origin = `sha(файл@коммит)`. Три строки таблицы с одинаковым origin-hash — это `n=1`, не `n=3`. Дедуп — тривиальная группировка по origin: `distinct(origin_hash)` даёт истинное число независимых голосов. Это чистая функция `dedupeByOrigin(evidence[]) → evidence[]`, без LLM, детерминированная. По экстрактам: сессия — свидетельство с origin = `sha(jsonl-fragment)`, живёт в том же контуре, тем же дедупом.

[Структурщик]: origin-hash решает мою тревогу по графу — это не новый пакет, а поле в существующей структуре свидетельства. Но зафиксирую границу для экстрактов сессий: **машинерия уже есть**. `persona-memory-extract.mjs` — детерминированная дистилляция, provenance, токен-бюджет <5000, отбор top-N. Не изобретаем второй экстрактор. Сессии подключаются как **новый источник в существующий extractor**, а не как отдельный контур. Один термин — один механизм: «дистилляция свидетельств» уже определена, добавляем ей вход `~/.claude/projects/*.jsonl`, не плодим синоним.

[Верстальщик]: Тогда по форме таблицы «Почему это магистраль» — конкретный контракт. Колонки: `утверждение` | `происхождение` (badge: `код`/`issue`/`сессия`/`снимок`) | `origin-hash` (короткий, 7 симв, `font-mono`) | `свежесть` (дата первоисточника). Строки с одинаковым `origin-hash` группируются визуально (общая левая граница `border-l-2 border-warning`), и над группой — счётчик «1 источник, 3 отражения». Читатель мгновенно видит: это не три подтверждения. Badge не только цветом — текст обязателен, по DESIGN.md.

[Музыкант]: По приватности экстрактов — помечу риск заранее, как ожог LE rate-limit не хочу повторять. 114 jsonl в `~/.claude/projects/` содержат сырые промпты, возможно секреты, пути, куски кода. В репозиторий сырьё пускать нельзя. У нас **уже есть** `night-triage-secret-scan.mjs` — готовый сканер. Гейт: экстракт сессии проходит secret-scan ДО попадания в дистилляцию, при срабатывании — фрагмент выбрасывается, не редактируется. И в репо едет только дистиллят (<5000 токенов, provenance), не транскрипт. Сырьё остаётся локальным.

[Teamlead]: Принимаю обе границы — extractor переиспользуется (Структурщик), secret-scan как гейт приватности (Музыкант). Но задам развилку по экстрактам, чтобы не было «зависит»: сессии как вход ритуала — **включаем сейчас или откладываем**? Сегодня последний день перед FREE, большие стройки неуместны. Моё предложение: origin-hash + дедуп + provenance-колонка — **сейчас** (это дёшево, чистые функции, чинит уже случившуюся боль). Экстракты сессий — **отложено с условием**: после FREE, отдельной задачей, потому что secret-scan-гейт и подключение jsonl — это стройка, а не правка. Прошу возражения.

[Математик]: Поддерживаю развилку численно. Дедуп по origin-hash — это ~40 строк чистой функции плюс поле в таблице, вход `main-day-probe` уже существует (PR #532), добавить туда `dedupeByOrigin` — правка, не стройка. Стоимость сегодня приемлема. Экстракты сессий: secret-scan на 114 jsonl — это I/O + прогон сканера на мегабайтах, плюс интеграция в extractor, плюс калибровка top-N на новом типе входа. Это M-задача, не S. Откладывание корректно: сегодня чиним эхо-камеру (корень, уже ударивший), сессии — профилактика (не ударила ещё).

[Структурщик]: Согласен с разделением на «сейчас/потом» и уточню границу отложенной задачи, чтобы она не расплылась. Экстракт сессий = новый **источник** для `persona-memory-extract.mjs` + гейт `night-triage-secret-scan` перед дистилляцией + origin = `sha(jsonl-fragment)`. Три готовых актива, склеиваемых контрактом. Никакого нового пакета, никакого нового extractor. Если задача при разработке потребует второй экстрактор или новый контур — это сигнал `нарушена слабая связанность`, вернуть на переосмысление.

[Верстальщик]: По отложенной задаче добавлю UI-условие, чтобы её DoD был честным: если сессии войдут во вход, в таблице обоснования появится badge `сессия` — и он обязан читаться как **свидетельство**, не как решение. Предлагаю визуально приглушённый вариант (`badge-ghost` + иконка «черновик»), чтобы читатель не путал «агент это делал» с «это решено». Provenance-колонка уже несёт origin-hash сессии — клик раскрывает фрагмент дистиллята, не сырьё.

[Музыкант]: Принимаю «сейчас/потом». По сегодняшней части последнее: схлопывание весов должно быть **видимым в CLI-выводе** `main-day-probe`, не только в markdown-таблице. Когда ритуал печатает «магистраль подтверждена N источниками» — N обязан быть **после** дедупа. Если печатает сырое N=3, а после дедупа n=1 — это возврат эхо-камеры через чёрный ход. Один AudioContext на страницу — один счётчик правды на ритуал.

[Teamlead]: Собираю консенсус. Сегодня (S, впаять в существующий `main-day-probe` / PR #532-контур): (1) поле `origin-hash` в каждом свидетельстве; (2) чистая `dedupeByOrigin`; (3) provenance-колонка в таблице «Почему магистраль» с визуальной группировкой отражений; (4) счётчик источников в CLI — после дедупа. После FREE (M, отдельная задача): экстракты сессий как новый вход `persona-memory-extract` через secret-scan-гейт, origin = `sha(jsonl-fragment)`, badge `сессия`=свидетельство. Все согласны — фиксирую?

[Математик]: Принимаю. Численный инвариант сегодняшней части: `голосов_за_магистраль = distinct(origin_hash)`, не `count(строк)`. Тестируемо без моков: вход — массив свидетельств, выход — число. Юнит-тест эхо-камеры 16.07: три строки, один origin → n=1. Регресс-защита от точного повторения сегодняшнего сбоя.

[Структурщик]: Принимаю. Границы держатся: новых пакетов нет, `main-day-probe` остаётся read-only препроцессором, extractor не дублируется. Термины разведены: свидетельство ≠ источник истины, дистилляция ≠ транскрипт.

[Верстальщик]: Принимаю. UI-контракт таблицы обоснования и badge-семантику беру в DoD сегодняшней части (колонки + группировка отражений) и отложенной (badge `сессия`=свидетельство).

[Музыкант]: Принимаю. Secret-scan-гейт для отложенной части, видимость дедупа в CLI — для сегодняшней. Сырьё сессий остаётся локальным, в репо только дистиллят.

[Teamlead]: Принимаю как Teamlead. Приоритет `ARCHITECTURE.md` соблюдён: детерминизм, read-only препроцессор, слабая связанность. LGTM на сегодняшнюю S-часть в текущий контур ритуала; отложенная M-часть — отдельная карточка реестра после FREE. Оба обойдённых корня закрыты: эхо-камера — механизмом дедупа, экстракты — принятым классом «свидетельство» с планом внедрения.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Экстракты сессий агентов как вход ритуала | **Отложено с условием**: после FREE, отдельная M-карточка. Класс — «свидетельство» (гипотеза-для-сверки), не источник истины. Механизм — новый вход в существующий `persona-memory-extract.mjs`, без нового extractor/пакета |
| Приватность экстрактов | Гейт `night-triage-secret-scan.mjs` ДО дистилляции; при срабатывании фрагмент выбрасывается. В репо едет только дистиллят (<5000 токенов, provenance), сырьё jsonl — локально |
| Защита от эхо-камеры | **Сейчас (S)**: поле `origin-hash` = `sha(первоисточник@коммит)` в каждом свидетельстве; чистая `dedupeByOrigin(evidence[])`; `голосов = distinct(origin_hash)` |
| Разметка производных источников | provenance-колонка в таблице «Почему магистраль»: `утверждение` \| `происхождение` (badge) \| `origin-hash` (7 симв) \| `свежесть`; отражения одного origin группируются визуально + счётчик «1 источник, N отражений» |
| Видимость дедупа | CLI `main-day-probe` печатает N источников **после** дедупа, не сырое число строк |
| Границы | Новых пакетов нет; `main-day-probe` остаётся read-only; extractor не дублируется; свидетельство ≠ источник истины |

**Definition of Done:**

Сегодняшняя S-часть (впаять в контур PR #532 / `main-day-probe`):
- [ ] Поле `originHash` в структуре свидетельства (`sha(первоисточник@коммит)`, 7-символьный short для UI).
- [ ] Чистая функция `dedupeByOrigin(evidence[]) → evidence[]`, юнит-тест эхо-камеры 16.07 (3 строки, 1 origin → n=1).
- [ ] `main-day-probe` печатает «магистраль подтверждена N источниками», где N = `distinct(originHash)` после дедупа.
- [ ] Таблица «Почему это магистраль» в промпте MAIN_DAY_ISSUE: колонки `утверждение` / `происхождение` (badge, текст+цвет) / `origin-hash` / `свежесть`; визуальная группировка отражений (`border-l-2 border-warning`) + счётчик «1 источник, N отражений».
- [ ] Соответствие DESIGN.md (badge не только цветом), read-only препроцессор без автопочинки.

Отложенная M-часть (карточка реестра после FREE):
- [ ] Новый вход `~/.claude/projects/*.jsonl` в `persona-memory-extract.mjs` (без второго extractor).
- [ ] Гейт `night-triage-secret-scan.mjs` перед дистилляцией; фрагмент с секретом выбрасывается, не редактируется.
- [ ] origin = `sha(jsonl-fragment)`; в репо только дистиллят (<5000 токенов, provenance).
- [ ] Badge `сессия` = свидетельство (`badge-ghost` + иконка «черновик»), клик раскрывает дистиллят, не сырьё.
- [ ] `нарушена слабая связанность` при попытке ввести второй экстрактор или новый контур.

---

*Реплик в диалоге: 20; каждый участник высказался не менее одного раза (Структурщик ×4, Верстальщик ×4, Музыкант ×4, Teamlead ×4, Математик ×4).*
