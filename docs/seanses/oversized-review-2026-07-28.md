<!-- канал: код+агенты — точечное ревью oversized-долга (P1 DAILY_CODE_REVIEW 27.07); 5 параллельных ревьюеров + верификация; LLM панели не использовался -->

# Ревью-долг oversized — точечное ревью шести PR (28.07)

Долг из P1 вчерашнего ревью: шесть крупных PR прошли в main без развёрнутого diff-ревью
(«молчание = ложная зелень»). Каждый пройден отдельным ревью (не пакетом, как велел
Teamlead 27.07). Вердикты — ниже; находки НЕ требуют отката ни одного PR, но три
тяжёлые заведены долгами попугая (явное рождение, origin: detector).

| PR | Строк | Вердикт | Сводка |
|----|-------|---------|--------|
| [#1311](https://github.com/officefish/Membrana/pull/1311) утро 27.07 + LLM-цепочка | 913 | **FINDINGS (5)** | Тяжёлое: в `morning-gates-state.json` закоммичен вчерашний выбор магистрали — `magistralChosen` утверждает НЕ ту магистраль дня (перенос состояния через день); канонический `MAIN_DAY_ISSUE.md` уехал с чат-преамбулой LLM над заголовком. Среднее: `LLM_NO_OVERLAY=1` не обнуляет явно переданный `args.overlay`; `overlayDroppedSteps` сравнивает только провайдеров (подмена моделей молчит); escape-hatch без юнита. Хорошее: гласность канала — цепочка называет себя и источник до попыток |
| [#1315](https://github.com/officefish/Membrana/pull/1315) контейнер тестов | 773 | **FINDINGS (7)** | Тяжёлое: `changedFromGit` при отказе git молча даёт «изменений нет» → gate ужимается до smoke с зелёным exit 0 (B6); селектор графа слеп к spawn-зависимостям (правка CLI не выбирает его тесты). Среднее: `DEFAULT_TEST_CATALOG` дублирует JSON-каталог без синка; JSON-схема каталога декоративна; TypeError на флаге без значения; cwd-зависимый тест; `built-external-home.valid=true` по факту существования каталога. Хорошее: пустой прогон = ошибка, «not run» печатается честно |
| [#1323](https://github.com/officefish/Membrana/pull/1323) nightly + handoff-guards | 649 | **FINDINGS (7, non-blocking)** | Тяжёлое: `blocksMorningWhen` объявлен без единого потребителя, `tests:nightly-full` не вшит ни в одну цепочку — гейт-заглушка («объявленное ≠ работающее»); freeze-гейт fail-open при `unknown`-статусе каталога. Среднее: красный путь guard без юнита; liveness-regex ловит `PR #N` как issue → один кривой номер красит весь батч в unknown; exit 0 при любом dead; фолбэк `officefish/Membrana` в форке; blocked-путь nightly без теста. Проверено живьём: 6/6 тестов, freeze доказан падением |
| [#1331](https://github.com/officefish/Membrana/pull/1331) рефакторинг команды | 691 | **LGTM** | Развязка роли от имени без переписывания истории (835 старых упоминаний легитимны); честные «нет» в callable; живой прогон `verify:voices` 8/8, тесты 7/7. Заметка P3: резолв callable текстовым грепом по исходникам ask/consilium — хрупок к рефактору формата карт (ложный красный вероятнее ложного зелёного) |
| [#1332](https://github.com/officefish/Membrana/pull/1332) контейнер кейсов | 1035 | **АВТОР НЕ СУДИТ** | Мой PR — по оговорке владельца не ревьюю своё. Прошёл CI + встроенные зубы (10 юнитов, красный доказан пробой), но развёрнутое diff-ревью должно прийти от соседней сессии или капитана. Долг остаётся открытым ровно на этот один PR |
| [#1335](https://github.com/officefish/Membrana/pull/1335) архивариус | 1266 | **FINDINGS (8, самые тяжёлые дня)** | Тяжёлое: `docs/archivarius/cache/spans.jsonl` НЕ в .gitignore — документированный ingest пишет редактированные транскрипты в трекаемый каталог (git add -A утащит сессии в историю); `?limit=abc` → NaN → пустой 200 (B6: мусор = молчаливый зелёный), то же с датами и `?by=`; ingest-нотариус не пересчитывает sha256 принятых span'ов (заверяет не глядя). Среднее: mongo lazy-connect без таймаутов и с гонкой двух клиентов; все глаголы через полный `listSpans()` в память + мёртвый text-индекс; Mongo-путь (дефолт docker) без единого теста; bodyLimit 1 MiB < реального батча; LIVE_SERVICES без строки о новом доме данных (B8-условно, компенсировано honest-заметкой в BACKGROUND_SERVERS). Хорошее: маска секретов реальная и тестируется негативным матчем |

## Куда пошли тяжёлые находки

Три находки рождены долгами попугая (`yarn bridge:debt birth --origin detector`,
вещдок — этот файл): архивный `spans.jsonl` вне gitignore · перенос магистрали через
день в gates-state · NaN-параметры архивариуса дают пустой 200. Остальные — материал
вечернего code-review и хозяев соответствующих контуров; ни одна не требует отката.

## Провенанс

Ревью #1311/#1315/#1323/#1335 — четыре параллельных read-only агента (полные отчёты
в транскрипте сессии), верификация выборкой: живые прогоны `verify:voices`,
freeze-гейта и тестов повторены рукой. #1331 — ревью рукой (агент упал об лимит
сессии). Даты, SHA и объёмы — из `DAILY_CODE_REVIEW-2026-07-27` (архив).
