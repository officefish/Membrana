# Night Hunt: design-token-drift

| Поле | Значение |
|------|----------|
| Week | 2026-39 |
| Generated (UTC) | 2026-09-25T12:44:45.969Z |
| Channel | claude |

---

# Weekly-отчёт: `design-token-drift` — неделя 2026-39

## Находки

- **Все 8 токенов DESIGN.md отсутствуют в конфиге темы.** Документ определяет палитру через CSS-переменные с конкретными значениями: `--color-bg` (`#0A0F1A`), `--color-surface` (`#111827`), `--color-border` (`#1F2937`), `--color-text` (`#F9FAFB`), `--color-text-muted` (`#9CA3AF`), `--color-accent` (`#7C3AED`), `--color-accent-hover` (`#6D28D9`), `--color-danger` (`#EF4444`). Ни один из них не объявлен в `apps/client/tailwind.config.js`.

- **Код опирается на семантику DaisyUI, а не на переменные документа.** В выборке 13 семантических классов и 41 употребление; доминирует `text-base-content` (19 употреблений, первый адрес `apps/client/src/components/AppFooter.tsx:15`). Это другой слой именования, чем `--color-text`/`--color-surface` из DESIGN.md.

- **Прямых значений цвета в коде нет.** В прочитанной выборке (`apps/client/src/components`, без подкаталогов, ≤25 файлов, тесты исключены) hardcoded-цветов не найдено — код дисциплинированно ходит через классы тем.

- **Одно исключение из семантики — `bg-black`** в `apps/client/src/components/DeletionConfirmDialog.tsx:165`. Это не семантический токен темы, а абсолютный цвет из палитры Tailwind (вероятно, оверлей диалога), стоящий особняком от `bg-base-*`/`bg-primary`.

- **Ролевые пары документа и кода частично соответствуют по смыслу.** `--color-danger` из DESIGN.md смыслово покрывается классом `text-error` (`apps/client/src/components/DeletionConfirmDialog.tsx:183`) и `--color-accent` — классами `bg-primary`/`text-primary-content` (`apps/client/src/components/StorageRuntimeIndicator.tsx:56`), но связь эта неформальная, нигде не зафиксирована.

## Риски

- **DESIGN.md и рантайм цвета не сверяемы.** Значения `--color-accent` (`#7C3AED`), `--color-bg` (`#0A0F1A`) и остальные 6 живут только в документе; фактический цвет задают 5 тем DaisyUI (`forest`, `sunset`, `emerald`, `business`, `dark`). Документ не описывает ни одну из них, поэтому проверить соответствие невозможно.

- **`bg-black` в `DeletionConfirmDialog.tsx:165` не переключается вместе с темой.** Абсолютный чёрный не реагирует на `dark`/`sunset`/прочие темы, что даёт риск неверного контраста оверлея в части тем.

- **Множественность тем не имеет источника истины.** DESIGN.md с 8 токенами описывает по сути одну тёмную палитру, тогда как код рассчитан на 5 тем — документ не покрывает эту вариативность, и дрейф между ним и любой из тем не отлавливается.

## Рекомендации

- **Признать разницу слоёв законной, но зафиксировать мост.** Расхождение «значения в DESIGN.md ↔ семантические классы в коде» — это разные слои (палитра vs роли), а не ошибка кода. Добавить в DESIGN.md таблицу соответствия: `--color-danger` → `error`, `--color-accent` → `primary`, `--color-text` → `base-content`, `--color-surface` → `base-100/base-200`.

- **Определиться со статусом DESIGN.md.** Либо перенести 8 токенов в `apps/client/tailwind.config.js` как значения конкретной темы (напр. `dark`), либо явно пометить документ как описание одной референс-темы, чтобы 8 «не объявленных» токенов перестали читаться как дрейф.

- **Заменить `bg-black` на семантический класс.** В `apps/client/src/components/DeletionConfirmDialog.tsx:165` перейти на токен темы (например `bg-neutral` с прозрачностью), чтобы оверлей следовал активной теме.

- **Расширить границу замера.** Текущая выборка — 11 файлов одного каталога без подкаталогов; выводы о всём приложении делать нельзя. Прогнать замер по вложенным каталогам `components/*` и по остальным путям `apps/client/src`, прежде чем судить о дрейфе в целом.

---
*Замер охватывает 11 файлов каталога `apps/client/src/components` без подкаталогов (≤25, тесты исключены). Утверждения о токенах DESIGN.md и конфиге относятся к документу и конфигу целиком.*
