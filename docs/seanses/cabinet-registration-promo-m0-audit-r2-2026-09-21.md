# Аудит M0 «Регистрация в кабинете по промокоду панели», прогон 2 — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Задание: `docs/meeting/cabinet-registration-promo/AUDIT_M0_BRIEF.md` (редакция после прогона 1: пункт 3 — роль плюс дословная цитата, проверка `grep -cF`). Предмет: повестка `docs/meeting/cabinet-registration-promo/M0_AGENDA.md` (дополненная) и протокол `docs/seanses/cabinet-registration-promo-m0-order-r2-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `1a865399`). Ствол для сверки: `origin/main` = `d646e813`. Протокол и повестка не правились. Прогон 1 — `…-m0-audit-2026-09-21.md`, ОТКЛОНЕНО по 1/3/4/7/8; здесь те же восемь пунктов заново.

## Итог

**ОТКЛОНЕНО** по одному пункту — **3 «Список посылок»**: 7 из 20 посылок не являются дословными цитатами реплик. Шесть посылок-фактов (П12–П17) подписаны «[Роль] (цитата из фактуры повестки)» — это текст повестки, а не реплики: у четырёх из них в репликах нет ни одного фрагмента длиннее трёх слов. П19 — цитата с молчаливым пропуском, к тому же закрепляет `env.schema.ts` за сессией B, что вердикт ниже отменил. П20 — отредактированная пунктуация. Пункты 1, 2, 4, 5, 6, 7, 8 — PASS; все уроки прогона 1 (пути, дверь `auth.controller.ts:21`, общий файл, счёт реплик) исправлены.

Для председателя отдельно: вердикт отвечает «три сессии A/B/C параллельно, D четвёртой после C» на вопрос о **трёх** сессиях. Расхождение названо в комнате открыто ([Teamlead], стр. 74 и 86; [Архитектор], стр. 92) и обосновано: у D другой корень (`apps/cabinet/src/`) и зависимость от C. Это не провал по чек-листу, а отступление от формулировки Q0 — решать председателю и владельцу.

## Таблица

| # | Пункт | Вердикт | Цитата / команда |
|---|---|---|---|
| 1 | Один вопрос | PASS | Содержание кандидатов вынесено из решений: «Решается в комнатах: Комната 1 — вид гранта и форма двери офиса. Комната 2 — … место клиента офиса. Комната 3 — порядок гашения кода и создания пользователя, ответ при недоступном офисе, тесты гонки. Комната 4 — форма, фраза ошибки, частота, правда на лендинге» (стр. 177). В «Идёт сразу в задание» (стр. 176) — только порядок, рёбра и границы файлов. Утечки в репликах есть ([Математик] стр. 94 о приёме #2313 для кандидата 3; [Верстальщик] стр. 96 о фразе ошибки), в решения не превращены. Оговорка: граница сессии B «новый модуль клиента офиса внутри `packages/background-cabinet/src/modules/`» (стр. 172) фиксирует корень клиента, тогда как «место клиента офиса» отдано комнате 2 (стр. 177); корень следует из фактуры повестки («клиент офиса принадлежит только серверу кабинета»), имя модуля не названо — считаю границей, не содержанием. |
| 2 | Вердикт есть и он про порядок | PASS (отступление от Q0, см. итог) | Рёбра с причинами — стр. 165–167; не-рёбра — стр. 168–171 (1→3, 1→4, 2→4, 4→1, 4→2, 4→3); раскладка по границам файлов — стр. 172; «Идёт сразу в задание» — стр. 176; «Решается в комнатах» — стр. 177. Все четыре части есть. Сессий четыре: «Три сессии A, B, C — параллельно … Сессия D — четвёртая, стартует по контракту после C» (стр. 175). |
| 3 | «Список посылок» | **FAIL** | Секция есть (стр. 190), 20 посылок, тип у каждой (норма 11, факт 6, план 3). Проверка `grep -cF` по репликам (стр. 44–156) с нормализацией глифов кавычек и заглавной буквы. **Дословны 13:** П1–П11, П18 (П6, П11, П18 — с точностью до заглавной буквы; П2, П9, П10 — «» в реплике против "" в посылке). **Не дословны 7:** П12 «Панельный код: … `PromoCode { grants[], maxUses, usedCount, expiresAt }`» — [Архитектор] такого не говорил, ни одного фрагмента ≥4 слов в репликах; П13 — в реплике [Архитектора] стр. 116 есть «`ALLOW_REGISTRATION` в `config/env.schema.ts:37,47` (в проде `false`)», посылка добавляет «Флаг» и обрезает контекст; П14 «Каталога `apps/cabinet/src/services/` в стволе нет; ключ офиса в браузере жить не может» — фрагментов нет ([Архитектор] стр. 68 говорил иначе: «ключ офиса в браузер не попадает — из фактуры, `apps/cabinet/src/services/` в стволе нет»); П15 — фрагментов нет ([Верстальщик] стр. 132 перечислял пути, но не фразу повестки); П16 — [Математик] стр. 94 сказал «гонка двух запросов на один код уже чинилась приёмом…», посылка цитирует повестку «Гонка на общий ресурс в кабинете…»; П17 — фрагментов нет ([Структурщик] стр. 114: «`membrane.service.ts:157` — приёмка второго кабинета на `free-v1`»). Все шесть подписаны «(цитата из фактуры повестки)» — по повестке «посылка „из фактуры повестки“ без реплики — не посылка». П19 [Структурщик]: из стр. 78 выпущено «(четвёртый кандидат)» без многоточия; и она относит `packages/background-cabinet/src/config/env.schema.ts` к сессии B — вердикт стр. 173 относит его к C, то есть посылка не «сработавшая», а отменённая комнатой. П20 [Архитектор]: стр. 140 «комната 1 — …; комната 2 — …» переписано как «Комната 1 — … . Комната 2 — …» — правка регистра и знаков. |
| 4 | Фактура против ствола | PASS | Проверено 15 путей `git cat-file -e origin/main:<путь>` и 9 строк `sed -n`. Есть: `panel-users-core.ts` (:24 `export interface PromoCode {`, :191–192 проверки срока и исчерпания), `panel-users.controller.ts` (:75 `@Post('register')`), `scripts/_ssh-panel-smoke.mjs`, `packages/background-cabinet/src/modules/`, `…/config/`, `deploy/generate-cabinet-env.sh` (:34 `ALLOW_REGISTRATION=false`), `…/modules/auth/auth.controller.ts` (:21 `@Post('register')`), `…/auth.service.ts` (:15 `async register(login: string, password: string)`), `…/config/env.schema.ts` (:37, :47), `apps/cabinet/src/pages/LoginPage.tsx` (:79 «Prod: учётная запись выдаётся администратором при деплое»), `apps/cabinet/src/api/auth.ts`, `deploy/root-site/index.html` (:154 «Кабинет — регистрация»), `…/membrane/membrane.service.ts` (:157 `getOrCreateMembraneForUser`). Нет в стволе и вердиктом так и названы: `apps/cabinet/src/services/` (стр. 170, как отрицательный факт — верно); `…/config/office-client.env.ts` (стр. 172, «например», новый файл сессии B — не утверждение о стволе). Утверждений о несуществующем как о существующем — ноль. Оговорка: «тесты `auth`» сессии C — в `modules/auth/` сейчас только `password.util.test.ts`; тесты службы будут новыми файлами. |
| 5 | Данности не переоткрыты | PASS | `grep -n -i "в кабинете или в панели\|вторая система\|своя система промокод\|новая система промокод"` по протоколу → пусто. Правда о коде остаётся в панели: «Комната 1 — вид гранта и форма двери офиса» (стр. 177); «Сессия A (кандидат 1): `packages/background-office/src/modules/panel-users/…`» (стр. 172). |
| 6 | Словарь | PASS (замечание) | «Панель», «кабинет», «код», «грант», «дверь» выдержаны. В вердикте (стр. 160–212): «клиент/клиента» ×7, «транзакция» ×3, «дизъюнктны» ×2, «UI» ×1 (стр. 171), «Definition of Done» ×1 (стр. 179). «UI» вместо «браузерная форма» в собственной таблице вердикта — замечание, не провал. |
| 7 | Реплик не меньше тридцати | PASS | `grep -cE "^\[(Архитектор\|Верстальщик\|Математик\|Teamlead\|Структурщик\|Музыкант)\]"` → **57**; по ролям: Архитектор 10, Верстальщик 10, Математик 10, Teamlead 9, Музыкант 9, Структурщик 9 — шесть советчиков. Условия: `--min-replies 36` (стр. 26), футер «Реплик в диалоге: 57» (стр. 186). Замечание: пять реплик стр. 148–156 — одно слово «Принимаю»; содержательных 52, порог всё равно пройден. Вход «координация ролей `docs/VIRTUAL_TEAM_PROMPT.md` — обрезан» (стр. 20) — повторяется с прогона 1; модель `deepseek/deepseek-chat` (стр. 9). |
| 8 | Порядок исполним без общих файлов | PASS | По таблице вердикта (стр. 172): A = {`panel-users-core.ts`, `panel-users.controller.ts`, `scripts/_ssh-panel-smoke.mjs`}; B = {новый модуль в `packages/background-cabinet/src/modules/`, новый `…/config/office-client.env.ts`, `deploy/generate-cabinet-env.sh`}; C = {`…/modules/auth/auth.controller.ts`, `…/auth.service.ts`, `…/config/env.schema.ts`, тесты `auth`}; D = {`apps/cabinet/src/pages/LoginPage.tsx`, `apps/cabinet/src/api/auth.ts`, `deploy/root-site/index.html`}. Попарные пересечения пусты. Общий файл прогона 1 разведён явно: «`packages/background-cabinet/src/config/env.schema.ts` — принадлежит сессии C … Сессия B не трогает этот файл» (стр. 173). Дверь `auth.controller.ts:21` — «Отнесена к сессии C» (стр. 174). Два замечания, не провал: (а) граница B задана каталогом `modules/`, внутри которого лежит `auth/` сессии C — дизъюнктность держится на том, что новый модуль не `auth/`, имя не названо; (б) `deploy/generate-cabinet-env.sh:34` несёт `ALLOW_REGISTRATION=false` — флаг двери C, а файл отдан B; если комната 3 решит переключать флаг в проде, C тронет файл B. |

## Что аудитор не делает

Порядок кандидатов не предлагается; протокол и повестка не правлены; повторный прогон — решение председателя после ратификации владельца.
