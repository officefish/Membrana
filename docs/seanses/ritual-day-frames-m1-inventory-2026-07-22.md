<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-22T09:05:55.972Z |
| Команда | `yarn consilium` |
| Модель | claude-opus-4-8 |
| Файл | `docs/seanses/ritual-day-frames-m1-inventory-2026-07-22.md` |
| Порядок ролей | Музыкант → Teamlead → Математик → Структурщик → Верстальщик |
| Повестка | `docs/meeting/ritual-day-frames/M1_AGENDA.md` |

**Вопрос:**

I1 — каков полный инвентарь очереди фреймов утреннего ритуала: какие id кадров покрывают цепочку yarn ritual:day и пост-артефактные шаги, что входит в каждый кадр, а что остаётся только корнем кита без отдельного фрейма?

---

# Консилиум: полный инвентарь очереди фреймов утреннего ритуала (M1/I1)

**Повестка:** I1 — упорядоченный инвентарь id фреймов, покрывающих цепочку `yarn ritual:day` и пост-артефактные шаги (ласточка, hermes); scope каждого кадра; явный список roots кита, остающихся без отдельного фрейма.

**Участники:** Teamlead (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Музыкант → Teamlead → Математик → Структурщик → Верстальщик (циклически)

---

[Музыкант]: По касательной к аудио — но маршрут утра я слышу как тракт записи с автопуском. Замер 22.07 даёт десять скриптов в yarn-цепочке плюс три root'а кита вне неё: `morning-gate`, `telegram-swallow`, `hermes-brief`. Мой давний флаг из `angelina-hostess-m3-gates`: ласточка (swallow) — это рекордер, который сам жмёт «старт». Она **не** в yarn-цепочке, вызывается руками после артефакта. Значит инвентарь обязан честно показать: пост-артефактные шаги — отдельный участок тракта, а не хвост основной цепочки.

[Teamlead]: Рамку фиксирую сразу, чтобы M1 не сполз в M2/M3. Мы **не** ранжируем `morning-wiring` («первый ли он»), **не** назначаем holder'ов, **не** проводим границу «файл кита vs pin» — это M2/M3/M4. Наш выход ровно один: полная опись id + scope каждого + явный список того, что фрейма не получает. По «форма следует функции»: фрейм оправдан, если он — самостоятельная единица сценария с собственными дверями-документами или отдельным артефактом. Root кита без своих дверей и без артефакта — не фрейм, а пин.

[Математик]: Формализую, иначе «инвентарь» останется вкусовым. Пусть R — множество roots кита, |R| = 13 (факт по замеру). Определяю предикат `needsFrame(r)`: root получает отдельный фрейм ⟺ он производит **именованный артефакт** ИЛИ владеет **≥1 дверью-документом**. Остальные — пин внутри кита или внутри чужого кадра. Требую разбиения R на два непересекающихся класса: `F` (фреймы) и `P` (пины), `F ∪ P = R`, `F ∩ P = ∅` — покрытие и непересечение, чтобы ни один из 13 root'ов не потерялся и не задвоился.

[Структурщик]: Начну с термина, иначе опись поплывёт. «Фрейм утра» — это лемма о **самостоятельной единице сценария**, у которой есть свой scope и свой diffable-выход. Не путать с root'ом кита (лемма о физическом скрипте-движке). Один фрейм может пином тянуть несколько roots — как одна словарная статья ссылается на несколько форм. `morning-wiring` уже существует как id (вердикт m3, три двери) — в опись вносим его **как факт существования**, без ранга. Соглашусь с предикатом Математика: артефакт ИЛИ дверь — иначе это пин.

[Верстальщик]: Со стороны потребителя — холодная сессия агента утром и человек в PR. Критерий полноты T6 на моём холсте значит: очередь видна **как список со стрелками**, а не десять строк из `package.json`. Каждый id несёт краткий scope одной строкой, provisional-имена помечены бейджем. Пост-артефактные шаги (swallow/hermes) — отдельная секция «после артефакта», визуально отбита от основной цепочки, иначе читатель решит, что они в автопрогоне. Норма из `main-day-accuracy`: если шаг есть в ките, но не в описи — это мёртвая дверь.

[Музыкант]: Тогда группирую по клоку тракта. Первый клок — **подготовка среды**: `morning-care` → `worktree-sync` → `repo-clean` → `deps-watch`. Все с `|| true` (факт) — это как прогрев и заземление: гудит, но не роняет концерт. Один фрейм на весь блок, `provisional: morning-hygiene` — четыре root'а пином, свои двери у них не просматриваются. Флаг: `|| true` означает мягкий отказ, и это надо показать в scope честно, иначе решат, что блок обязателен.

[Teamlead]: Принимаю группировку прогрева в один кадр — но требую проверить предикат Дынина на каждом из четырёх. `morning-care` производит артефакт? Если да — он тянет на отдельный фрейм. По фактуре замера — это точка входа кита (`engines[]` = `morning-care.mjs`), но артефакт-документа у него нет; это оркестратор среды. Значит `morning-hygiene` как единый кадр держится. Идём дальше по цепочке.

[Математик]: Проверяю блок прогрева предикатом. `morning-care`, `worktree-sync`, `repo-clean`, `deps-watch` — ни один не пишет именованный документ-артефакт, ни один не владеет дверью. `needsFrame = false` для всех четырёх → они P (пины) внутри одного контейнерного кадра `morning-hygiene`. Это не нарушает покрытие: кадр существует, но его наполнение — четыре пина, а не четыре фрейма. Дальше — блок планирования: `plan-week-if-monday` условный (факт: только понедельник), `strategy-day`, `daily-standup` — вот эти уже пахнут артефактами.

[Структурщик]: Развожу блок планирования на леммы. `plan-week-if-monday` — условный движок (предикат «понедельник»), артефакт — недельный план, но только по условию; фрейм `provisional: weekly-plan-gate`, помечен как условный. `strategy-day` → артефакт стратегии дня — самостоятельный фрейм `strategy-day`. `daily-standup` → артефакт стендапа — фрейм `daily-standup`. Три отдельных id: у каждого свой diffable-выход, значит `needsFrame = true` по предикату. Не сливать — разные словарные статьи.

[Верстальщик]: По подаче условного кадра: `weekly-plan-gate` обязан на холсте нести бейдж «условный: понедельник» — иначе читатель в среду увидит id и решит, что кадр пропущен по ошибке (мёртвая дверь наоборот). Честный экран показывает: кадр в очереди присутствует всегда, но исполняется по предикату. Для `strategy-day` и `daily-standup` — обычные кадры основной линии, без бейджей.

[Музыкант]: Теперь мастер-секция сведения — `main-day-probe` → `main-day-issue` → `angelina`. Probe собирает замеры (артефакт-зонд), issue чеканит MAIN_DAY_ISSUE (главный артефакт дня), angelina — точка входа диалога. Флаг из `angelina-hostess-m4`: `angelina` — это ведущий, выходящий на сцену. Он производит артефакт? Встречу утра и журнал — да. Значит три отдельных фрейма: `main-day-probe`, `main-day-issue`, `angelina-greeting`.

[Teamlead]: Согласен с тремя, но уточняю границу с `morning-wiring`. Кадр `morning-wiring` (id из вердикта m3, три двери: CLAUDE.md · зеркало developer-rhythm · AGENTS.md) — существующий id, вносим в опись как **зарезервированный слот**, ранг не решаем (M2). Он НЕ дублирует `main-day-issue`: тот чеканит issue дня, а wiring разводит три двери-документа. Разные функции — разные кадры. Фиксирую: в описи `morning-wiring` = существующий id, позиция «первый/не первый» отложена в M2.

[Математик]: Считаю баланс R. Основная yarn-цепочка: 10 движков (факт). Разбились так: `morning-hygiene` = 4 пина (1 кадр), `weekly-plan-gate` = 1, `strategy-day` = 1, `daily-standup` = 1, `main-day-probe` = 1, `main-day-issue` = 1, `angelina-greeting` = 1. Это 4+6 = 10 движков, покрыто в 7 кадрах. Плюс 3 root'а вне yarn: `morning-gate`, `telegram-swallow`, `hermes-brief`. Проверяю предикат на них — вот это ключевой вопрос комнаты.

[Структурщик]: Развожу три пост-цепочечных root'а. `morning-gate.mjs` — гейт (предикат прохода утра), артефакта-документа не пишет, но управляет прохождением — это **не** отдельный фрейм сценария, а страж, встроенный в существующий кадр (вероятно `main-day-issue` или `angelina`). Помечаю: `morning-gate` = пин внутри гейт-кадра, не самостоятельный фрейм — но точную привязку к кадру не решаем здесь (это ближе к M3 holder). `telegram-swallow` и `hermes-brief` — производят внешние артефакты (отправка в канал, brief). У них есть выход → нужны фреймы.

[Верстальщик]: Стоп по видимости — здесь мой главный флаг. `telegram-swallow` и `hermes-brief` в yarn-цепочке **нет** (факт замера), вызываются руками после артефакта. На холсте это ОБЯЗАНА быть отдельная секция «пост-артефактные шаги (ручной вызов)», визуально отбитая. Иначе холодная сессия прочитает очередь и решит, что ласточка летит в автопрогоне — а это ровно тот риск отправки-в-эфир, о котором Музыкант бил тревогу в m3. Два фрейма: `swallow-send` и `hermes-brief`, оба с бейджем «ручной, после артефакта».

[Музыкант]: Поддержу и заострю. `swallow-send` как фрейм — да, но флаг из `swallow-delivery-idempotency`: таймаут ≠ недоставка. Scope фрейма обязан назвать, что отправка идемпотентна и вне автоцепочки — механика гейта не этой комнаты (M3), но **факт отдельности фрейма** мы фиксируем здесь. `hermes-brief` — тоже свой кадр, отдельный артефакт (brief). Итого пост-секция: два фрейма + `morning-gate` как пин-страж.

[Teamlead]: Свожу и выношу вердикт формы. Опись: **7 кадров основной линии** (`morning-hygiene`, `weekly-plan-gate` условный, `strategy-day`, `daily-standup`, `main-day-probe`, `main-day-issue`, `angelina-greeting`) + **зарезервированный слот** `morning-wiring` (существующий id, ранг → M2) + **2 пост-артефактных фрейма** (`swallow-send`, `hermes-brief`, ручной вызов). Без фрейма: 4 root'а прогрева (пины в `morning-hygiene`) и `morning-gate` (пин-страж). Все имена, кроме `morning-wiring`, — provisional. Принимаю.

[Математик]: Финальная проверка инварианта. R = 13 (факт). F ∪ P: фреймы-держатели артефактов/дверей = {weekly-plan-gate, strategy-day, daily-standup, main-day-probe, main-day-issue, angelina-greeting, swallow-send, hermes-brief} — 8 root'ов → 8 фреймов; плюс контейнер `morning-hygiene` держит 4 пина; плюс `morning-gate` — 1 пин-страж. 8 + 4 + 1 = 13. Покрытие полное, пересечений нет. `morning-wiring` — не root кита (это кадр из вердикта m3 с дверями-документами, не движок), потому в баланс 13 не входит и вносится как отдельный слот. Инвариант держится. Принимаю.

[Структурщик]: Термины сведены, дублей нет, `morning-wiring` не слит с `main-day-issue`. Опись — не «фрейм по мере боли», а полная декомпозиция T6. Принимаю с одной пометкой в DoD: provisional-имена не канонизируются до M2/M3.

[Верстальщик]: Холст честный: основная линия отбита от пост-секции, условный кадр несёт бейдж, ручные шаги помечены. Холодная сессия читает очередь без раскопок в `package.json` — T6 выполнен. Принимаю.

[Музыкант]: Тракт слышен целиком, ласточка не в автопрогоне, дыры показаны. Принимаю.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Полный инвентарь фреймов утра | **9 фреймов + 1 зарезервированный слот**; 5 root'ов остаются пинами |
| Основная линия (по yarn-цепочке) | `morning-hygiene` (контейнер, 4 пина), `weekly-plan-gate` (условный, понедельник), `strategy-day`, `daily-standup`, `main-day-probe`, `main-day-issue`, `angelina-greeting` |
| Существующий id | `morning-wiring` — вносится как **зарезервированный слот** (id из вердикта m3, три двери); ранг «первый/не первый» → **M2** |
| Пост-артефактные (ручной вызов, вне yarn) | `swallow-send`, `hermes-brief` — отдельные фреймы, бейдж «ручной, после артефакта» |
| Без отдельного фрейма (пины) | `morning-care`, `worktree-sync`, `repo-clean`, `deps-watch` → пины в `morning-hygiene` (нет своего артефакта/двери, `|| true`); `morning-gate` → пин-страж (гейт, не единица сценария) |
| Критерий полноты T6 | Очередь читается как список со стрелками; основная линия отбита от пост-секции; условный кадр и ручные шаги помечены бейджами |
| Провенанс имён | Все id кроме `morning-wiring` — **provisional**, не канонизируются в этой комнате |

### Список посылок

- **Факт** — `package.json` `"ritual:day"` содержит 10 движков в порядке `&&`: `morning-care` → `worktree-sync` (`|| true`) → `repo-clean` (`|| true`) → `deps-watch --mode morning` (`|| true`) → `plan-week-if-monday` → `strategy-day` → `daily-standup` → `main-day-probe` → `main-day-issue` → `angelina` (замер 22.07).
- **Факт** — кит `kits/angelina-morning` = 13 roots: 10 скриптов цепочки + `morning-gate.mjs` + `telegram-swallow.mjs` + `hermes-brief.mjs`.
- **Факт** — swallow / hermes в yarn-цепочке отсутствуют; вызываются вручную после артефакта (BRIEF / CLAUDE.md).
- **Факт** — процедура `docs/procedures/ritual-day/`: `leadPersona: angelina`, `kitVersion: kits/angelina-morning`, `engines[]` = только `morning-care.mjs`; `frames[]` ещё нет.
- **Факт** — `plan-week-if-monday` исполняется по условию (понедельник).
- **Факт** — `morning-wiring` существует как id (вердикт m3 / EPIC `procedure-frames`), три двери: CLAUDE.md · зеркало developer-rhythm · AGENTS.md; это кадр с дверями-документами, не root-движок кита.
- **Норма** — вердикт M0 (`1 → {2,3,4} → 5`): фундамент-порядок принят, посылка-предшественник этой комнаты.
- **Норма** — предикат `needsFrame(r)`: root получает отдельный фрейм ⟺ производит именованный артефакт ИЛИ владеет ≥1 дверью-документом; иначе — пин.
- **Норма** — разбиение roots на непересекающиеся классы F (фреймы) и P (пины) с полным покрытием.

**Definition of Done (для I1):**
- Опубликована упорядоченная опись id фреймов утра (9 фреймов + слот `morning-wiring`), каждый с однострочным scope и пометкой provisional, где применимо.
- Для каждого id указан краткий scope (шаги/артефакты кадра) без holder и без механики pin.
- Явно перечислены 5 root'ов без отдельного фрейма (`morning-care`, `worktree-sync`, `repo-clean`, `deps-watch`, `morning-gate`) с причиной (нет артефакта/двери либо роль стража).
- `morning-wiring` присутствует в описи как существующий зарезервированный id без назначения ранга.
- Проверен инвариант покрытия: 13 roots кита распределены между фреймами и пинами без потерь и пересечений; `morning-wiring` учтён отдельно как кадр-с-дверями.
- Холодная сессия читает очередь и видит весь сценарий утра без раскопок в `package.json` (T6), пост-артефактные шаги визуально отбиты.

---

*Реплик в диалоге: 21; каждый участник высказался не менее одного раза.*
