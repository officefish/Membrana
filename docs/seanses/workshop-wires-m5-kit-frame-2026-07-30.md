<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-30T17:02:42.362Z |
| Команда | `yarn consilium` |
| Модель | xai/grok-4.5 |
| Файл | `docs/seanses/workshop-wires-m5-kit-frame-2026-07-30.md` |
| Порядок ролей | Математик → Структурщик → Teamlead → Верстальщик → Архитектор → Музыкант |
| Повестка | `docs/meeting/workshop-wires/M5_AGENDA.md` |

**Вход сеанса** (что комната действительно получила):

| Вход | Носитель | Размер | Отпечаток | Доставка |
|------|----------|-------:|-----------|----------|
| повестка (эхо в голове) | `docs/meeting/workshop-wires/M5_AGENDA.md` | 4416 | `fa7a1ef45748` | полностью |
| инструкция консилиума | `docs/prompts/CONSILIUM_PROMPT.md` | 4494 | `b6bd1a8dc127` | полностью |
| координация ролей | `docs/VIRTUAL_TEAM_PROMPT.md` | 8034 | `75959c7f060d` | **обрезан** |
| контекст: Архитектура | `docs/ARCHITECTURE.md` | 6034 | `33f0886d9bac` | **обрезан** |
| контекст: Дизайн | `docs/DESIGN.md` | 5998 | `c0c614192971` | полностью |
| контекст: Сервисы | `docs/SERVICES.md` | 6034 | `cf5d799f7980` | **обрезан** |
| повестка | `docs/meeting/workshop-wires/M5_AGENDA.md` | 4416 · 1 п. | `fa7a1ef45748` | **обрезан** |

> ⚠ Часть входа не доехала до комнаты целиком. Утверждения протокола о своём входе
> сверять с этой таблицей, а не с текстом реплик.

**Вопрос:**

Становится ли кит отдельным видом служебного фрейма процедуры, и если да — что фрейм объявляет, кто держатель, чем проверяется: вердикт — вид фрейма или обоснованный отказ

---

# Консилиум: кит как вид служебного фрейма процедуры (M5 / workshop-wires)

**Повестка:** F5 — становится ли кит отдельным видом служебного фрейма процедуры (тем, которым процедура отдаёт сессии набор инструментов); если да — что объявляет, кто держатель, чем проверяется. Вердикт: вид фрейма или обоснованный отказ. Список посылок обязателен. Предмет — вид фрейма, не состав китов.

**Участники:** Teamlead (Tarasov), Архитектор (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Математик → Структурщик → Teamlead → Верстальщик → Архитектор → Музыкант

---

[Математик]: Формализую без UI. Паттерн даёт служебные виды V_svc = {провода, времянки, доставка}. Вопрос комнаты — существует ли расширение V' = V_svc ∪ {кит} либо провизия инструментов вырази́ма проекцией в уже существующий вид. Предикат отдельности вида: kind(k) ⇔ ∃ обязанность O_k, не сводимая к O_j для j≠k, и ∃ проверяемый носитель ссылки. Кит как объект уже есть: MANIFEST с id, leadPersona, roots, pins; audit в pre-push. Это не доказывает, что он — *фрейм процедуры*, но задаёт носитель, на который фрейм может ссылаться.

[Структурщик]: Леммы, иначе снова синонимы. **Фрейм процедуры** — элемент полосы `frames` манифеста: `{id, holder, pins?}` + тег рода. **Кит** — `kits/<id>/MANIFEST.json`, отдельный артефакт с собственным leadPersona. **Фрейм кита** (кандидат) — не сам кит, а объявление процедуры: «сессии на прогоне отдаю вот этот набор». Дом может заказать один кит — это другой слой (§1–§7); здесь только контракт *процедуры*. Смешивать `leadPersona` кита и `holder` фрейма — нарушение словаря: разные леммы.

[Teamlead]: Планка матча узкая. Не заводим киты, не назначаем leadPersona конкретных наборов, не переписываем контракт MANIFEST. Решаем одно: появляется ли в контракте процедуры четвёртый служебный вид. Вход владельца прям: дополнительные инструменты — через вызов процедур; это фрейм *кита*, не фреймов проводов; набор не обязан лежать в одной мастерской. Связка: Ожегов — леммы и паттерн #900; Веснин — граница вида; Дынин — предикат отдельности и проверяемость. Тренировка «поговорить про семь китов» — не матч.

[Верстальщик]: С витрины автора процедуры и ревьюера манифеста полоса `frames` должна читаться без легенды. Если кит спрятан внутри тега `провода`, оператор видит «провода» и думает LLM-канал — а на деле там ещё pins/roots инструментов. Это фальшь ярлыка, хуже пустоты. Либо честный тег `кит` рядом с тремя служебными, либо явное «кита во фреймах нет» и другое место в манифесте. Декор «как-нибудь в pins проводов» — отвергаю.

[Архитектор]: Конструкция: служебный фрейм — ответственность прогона, не сюжет. Провода — канал и связи (прежде всего LLM). Времянки — lifecycle временных файлов. Доставка — выход к серверу/соседям. Провизия инструментов сессии — другая ось: *какой набор допустим на вызове*, не *как подключён канал*. Сводить кит к расширению `провода` — цена: расползание ратифицированного смысла тега. Соседний вид дешевле, чем overloaded tag. Рекомендация формы: отдельный вид, тонкая ссылка на kit id, без поглощения контракта кита.

[Музыкант]: На слух заседание началось с контраста: киты — единственная оснастка, которую сессия зовёт исправно, audit на pre-push. Провода — это шина канала; кит — rack с приборами, который процедура выкатывает на сцену. Разные партии. Склеить в один fader — клиппинг смысла: «канал есть», а pins набора гнилые, или наоборот. Держу отдельный take: вид `кит` как четвёртый служебный, не insert внутрь проводов.

[Математик]: Предикат сводимости к `провода`: O_wires ⊇ «объявить LLM-канал и связи входов». O_kit ⊇ «сослаться на набор tools (kit id) и зафиксировать ожидание доступности набора на прогоне». Пересечение непусто (оба про «что нужно сессии»), но ядра разные: channel vs toolset. Сведение даёт ложноположительный pass: фрейм проводов зелёный, kit id битый или отсутствует — и наоборот. Отдельность kind обоснована: ¬(O_kit ⊆ O_wires).

[Структурщик]: Поля кандидата, если вид принимаем. Элемент frames: тег `кит` (или `kit` — один термин в каноне паттерна; в PROCEDURE_FRAMES сейчас кириллические теги — держаться ряда: `кит`). Ссылка: `kitId` / pins на `kits/<id>/MANIFEST.json` — не дублировать roots/pins кита во фрейме. `holder` — персона *процедуры*, ответчик за то, что объявление уместно и ссылка жива; это не `leadPersona` кита (тот отвечает за содержимое набора). При недоступном ките: fail-closed на валидации манифеста/аудите, не silent skip — иначе вид беззубый.

[Teamlead]: Держим: вид ≠ MUST немедленно. Паттерн сейчас SHOULD; комната готовит решение, ратификация — frames-alive / стол, не контрабанда MUST через валидатор в обход. Если скажем «вид есть», путь: правка PROCEDURE_FRAMES (рекомендация) → потом зуб по отдельному решению. Иначе получим MUST мимо стола — BLOCK по процессу. Матч сегодня — форма вида и отказ от расширения `провода`.

[Верстальщик]: Таблица в паттерне — четыре строки служебных, не три. Колонка «что несёт» для кита: «набор инструментов, который процедура отдаёт сессии (ссылка на kit id)». Пустота честная: процедуры без фрейма кита — норма, если инструменты не провизятся; не рисовать обязательную галочку на каждой карточке. Как у времянок: есть вид, нет долга иметь его всегда.

[Архитектор]: Отношение к `провода`: **сосед, не расширение и не пересечение-синоним**. Слой B из §7 — поштучный провод инструмента — остаётся в зоне проводов/прибора; фрейм кита не подменяет поштучную прокладку, он объявляет *набор*. Дом-заказ одного кита и фрейм процедуры — разные этажи: дом заказывает, процедура на вызове отдаёт. Вердикт не трогает неймспейс без `kit` и не заводит новые kits/*.

[Музыкант]: Семь китов в дереве и две мастерские с `kit: non-null` — это фактура состава, не аргумент против вида. Вид может существовать при редком употреблении: как `доставка` не обязана висеть на каждой процедуре. Редкость — сигнал очереди заведения китов (вне скоупа), не отказ от полки в паттерне.

[Математик]: Проверяемость. Пусть F — элемент frames с tag=кит, ref=kitId. Предикаты: (1) `declared(F)` — тег и ref присутствуют; (2) `resolves(ref)` — существует `kits/ref/MANIFEST.json`; (3) `audit_ok(ref)` — уже живой `kits:audit`. Минимальный зуб вида: (1)∧(2) в `validate-procedure.mjs` *после* ратификации; (3) уже на pre-push и не дублируется фреймом. Если «ничего» — вид декоративен: отвергаю как математически пустой. Если только документ без схемы — допустимо на фазе SHOULD, с явным «зуба нет».

[Структурщик]: Эпик #900, зона ozhegov — да. Вношу в словарь паттерна, не в JSON-схему кита. Контракт кита (id, leadPersona, roots, pins) не переопределяем. Имя тега фиксирую одним словом `кит` в ряду служебных; латиница `kit` в полях id пути — как сейчас в filesystem. Синоним «фрейм инструментов» без слова «кит» — запрещён: неймспейс заседания и прибор уже считают *китами*.

[Teamlead]: Положение: отдельность доказана, владелец назвал фрейм кита, цена overload `провода` высока. Решение креню к **виду**. Отказ принял бы только если провизия уже закрыта другим контрактом процедуры без дыр — её нет: полоса frames кит не знает, namespace kit не имеет, дом/мастерская — другой этаж. Связка на ратификацию: Ожегов — текст паттерна; Дынин — предикаты validate; Тарасов — не пускать MUST без frames-alive.

[Верстальщик]: Holder на витрине: строка `holder` у фрейма кита — кто отвечает за *объявление* («эта процедура отдаёт такой набор»), читается рядом с id фрейма. leadPersona из MANIFEST кита — на витрине *кита*, не дублировать во frames. Иначе два имени «ответчик» на одной карточке — фальшивое присутствие, как assigned/participated в sprint-honest.

[Архитектор]: При недоступном kit id: на уровне рекомендации — автор чинит ссылку; на уровне будущего зуба — reject манифеста процедуры (не кита). Кит может быть «здоровым» по audit, но не тем, что процедура назвала — ловит фрейм. Обратное: процедура без фрейма кита при этом живой kit в мастерской — не ошибка вида; мастерская ≠ процедура. Граница модулей: procedure manifest → ref; kits/* → SoT набора.

[Музыкант]: Хронический дефект паттерна — «провода не подведены». Четвёртый вид не отменяет его и не размывает: наоборот, снимает соблазн засунуть toolset в провода «чтобы хоть куда-то». Партитура чище: три старых дефекта живут, новый вид закрывает отдельную дыру провизии, с которой начался workshop-wires.

[Математик]: Посылки для вердикта фиксирую списком (черновик, потом в таблицу): P1 паттерн = 3 служебных SHOULD; P2 кит = отдельный артефакт с audit; P3 owner: провизия через процедуры = фрейм кита ≠ провода; P4 набор не обязан быть одной мастерской; P5 слой B = поштучный провод, не набор; P6 тег frames сейчас без схемного зуба; P7 расширение смысла `провода` = отдельная цена; P8 вердикт не заводит киты и не множит MUST. Из P3∧P5∧P7 → ¬расширение проводов; из P2∧P3 → носитель ссылки есть → вид допустим.

[Структурщик]: Отказной ветки «выражается иначе» не вижу сильной формы. Альтернативы: (a) поле `kit` в корне манифеста процедуры вне frames — плодит второй механизм рядом с полосой frames; (b) pins сюжета — смешение родов; (c) только дом/мастерская — не покрывает «на вызове процедуры» и чужой набор. Значит «иначе» хуже по слабой связанности. Принимаю вид в frames.

[Teamlead]: Вердикт формулирую жёстко: **вид фрейма — да**. Не отложено. Условие внедрения: SHOULD в паттерне, зуб — отдельной ратификацией. Кто держит паттерн — ozhegov. Кто не имеет права в этом PR назначить leadPersona китов — никто из комнаты, вне скоупа. Принимаю свой же критерий: ценность = ясная полка и отказ overload, не семь новых MANIFEST.

[Верстальщик]: DoD для текста паттерна, не для UI продукта: строка таблицы служебных; абзац «что объявляет / holder / не leadPersona кита / сосед проводов»; пример элемента frames; фраза «не MUST». Без этого PR паттерна — пустая галочка. Принимаю вид.

[Архитектор]: Форма элемента (рекомендация): `{ id, holder, tag: "кит", kitId }` или pins на путь манифеста кита — один способ ссылки, не два. Предпочтение: явное поле `kitId` = MANIFEST.id, чтобы не парсить pins как potpourri. Модули: `docs/patterns/PROCEDURE_FRAMES.md`; опционально позже `validate-procedure.mjs`. ADR не обязателен, если правка паттерна и протокол M5 — достаточный след; при споре о MUST — вынести на frames-alive.

[Музыкант]: На слух финал: rack (кит) и cable (провода) — две шины. Count-in для сессии: процедура может выкатить rack по kitId; кабель по-прежнему фрейм проводов. Молчу про witcher/tasks-master — состав вне скоупа. Принимаю.

[Математик]: Инвариант holder ≠ leadPersona(kit) — проверяем документально на SHOULD; машинно — только если появится зуб «holder обязателен» (уже общая механика frames). Инвариант resolves(kitId) — первый кандидат в validate-procedure. Статус «ничего не проверяется» на фазе только-документа — допустим, но в вердикте сказать прямо: сегодня зубá вида нет; audit китов проверяет кит, не фрейм.

[Структурщик]: Согласую с Дыниным: в вердикте явно «проверяемость сейчас = 0 машинных предикатов на tag=кит; путь зуба = validate + exists kitId, без MUST до frames-alive». Иначе секретарь запишет «проверяется audit’ом» — ложь: audit не смотрит полосу frames процедуры. Словарь держим честным.

[Teamlead]: Сводка к приёмке: вид **да**; поля — id, holder, tag `кит`, kitId; сосед проводов; holder процедуры ≠ leadPersona кита; недоступный kitId — ошибка объявления (когда зуб появится); ратификация — паттерн SHOULD под ozhegov/#900, MUST/зуб — отдельно. Кто против вида по существу — слово сейчас или «принимаю».

[Верстальщик]: Принимаю. На витрине паттерна честная четвёрка служебных.

[Архитектор]: Принимаю. Конструкция соседа, не расширения.

[Музыкант]: Принимаю.

[Математик]: Принимаю. Посылки P1–P8 — в итог.

[Структурщик]: Принимаю. Зона паттерна — моя; текст строки таблицы и леммы holder/kitId подготовлю пост-вердиктом, без заведения китов.

[Teamlead]: LGTM по матчу комнаты. Вид фрейма, не отказ. Исполнение текста паттерна — связка Структурщик; зуб не открываем этим вердиктом.

---

## Итоговое решение консилиума

### Вердикт

**Кит становится четвёртым служебным видом фрейма процедуры** (тег `кит` в ряду `провода` · `времянки` · `доставка`).  
**Отказ от вида — нет.**  
**Расширение тега `провода` — нет** (сосед, не overload).

### Список посылок

| # | Посылка |
|---|--------|
| P1 | `PROCEDURE_FRAMES.md` — SHOULD; служебные виды сейчас: провода, времянки, доставка; держатель паттерна `ozhegov`, эпик #900 |
| P2 | Кит — отдельный артефакт `kits/<id>/MANIFEST.json` (id, leadPersona, roots, pins); audit в pre-push |
| P3 | Вход владельца: доп. инструменты сессии — через вызов процедур; это **фрейм кита**, не фрейм проводов; набор не обязан лежать в одной мастерской |
| P4 | §1–§7: namespace без `kit`; дом может заказать один кит; прибор считает наборами-китами; слой B — поштучный провод инструмента |
| P5 | Полоса `frames`: `{id, holder, pins?}`; тег вида — рекомендация; схемного зуба на тег нет |
| P6 | Провизия toolset ⊄ «LLM-канал и связи»; сведение к `провода` даёт ложный pass/fail |
| P7 | Альтернативы вне frames (корневое поле, pins сюжета, только дом) — второй механизм или неверный этаж |
| P8 | Вердикт не заводит киты, не назначает leadPersona наборов, не переопределяет контракт кита, не вводит MUST мимо frames-alive |

### Ответы на обязательные вопросы комнаты

| Вопрос | Решение |
|--------|---------|
| Появляется ли вид? | **Да** — четвёртый служебный: `кит` |
| Что объявляет фрейм? | Что процедура на прогоне отдаёт сессии набор инструментов по ссылке на кит. Поля элемента (SHOULD): `id`, `holder`, тег `кит`, **`kitId`** (= id манифеста кита). Содержимое roots/pins кита **не** дублировать во фрейме |
| Недоступный кит | Объявление некорректно: при будущем зубе — reject манифеста **процедуры**; на фазе SHOULD — дефект автора. Здоровый audit другого кита не оправдывает битый `kitId` |
| Отношение к `провода` | **Сосед.** Не расширение, не синоним. Поштучный слой B остаётся у проводов/прибора; фрейм кита — набор целиком |
| Держатель фрейма | `holder` элемента frames — ответчик за **уместность объявления и живость ссылки** у процедуры. **Не** `leadPersona` кита (тот — за содержимое набора). Разные леммы |
| Проверяемость | **Сейчас — ничего машинно на tag=`кит`.** `kits:audit` проверяет кит, не фрейм. Путь зуба (после ратификации, не этим вердиктом): `validate-procedure.mjs` — exists `kits/<kitId>/MANIFEST.json`. Пока зуб не принят — статус SHOULD/документ |
| Цена и ратификация | Низкая цена паттерна: строка таблицы + абзац лемм + пример. Держатель правки — `ozhegov` / #900. MUST и схемный зуб — только через `frames-alive` S0 / отдельный стол, не контрабандой валидатора |

### Definition of Done (на след паттерна, не на состав китов)

- [ ] В `docs/patterns/PROCEDURE_FRAMES.md` — четвёртая строка служебных и явная формула «сосед проводов, не расширение»
- [ ] Зафиксированы: `kitId`, holder ≠ leadPersona(kit), опциональность фрейма (не долг каждой процедуры)
- [ ] Явная фраза: машинного зуба на вид пока нет; кандидат зуба описан, не включён
- [ ] Нет новых `kits/*`, нет смены leadPersona, нет MUST в validate-procedure из этого вердикта
- [ ] Протокол M5 принят как след решения комнаты; ратификация ужесточения — отдельным порядком

---

*Реплик в диалоге: 33; каждый из шести советчиков высказался не менее одного раза.*

---

## Полное эхо вопроса

Становится ли кит отдельным видом служебного фрейма процедуры, и если да — что фрейм объявляет, кто держатель, чем проверяется: вердикт — вид фрейма или обоснованный отказ

---

## Полное эхо повестки (docs/meeting/workshop-wires/M5_AGENDA.md)

# M5 — кит как вид фрейма процедуры (заседание `workshop-wires`)

**F5 —** Становится ли **кит** отдельным видом служебного фрейма процедуры — тем, которым
процедура отдаёт сессии свой набор инструментов, — и если да, что этот фрейм объявляет, кто
его держатель и чем он проверяется? Вердикт — **вид фрейма или отказ от него, с обоснованием**.
**Список посылок обязателен.**

> **ПРЕДМЕТ — ВИД ФРЕЙМА, НЕ СОСТАВ КИТОВ.** Девятая комната; по ратифицированному порядку
> стоит **вне цепи** — её вход не зависит от §1–§8 и она не задерживала очередь. Вне скоупа:
> предикат принадлежности · глаголы прибора · зуб · пол сессии · норма в документах ·
> какие киты завести и кому.
>
> Вердикт **не должен** заводить киты, назначать держателей конкретных китов или
> переопределять контракт кита — он решает, появляется ли в контракте **процедуры** новый вид
> служебного фрейма.

**Оснований комнате не предлагаю** — только факты и вопросы.

**Зона держателя.** Паттерн фреймов процедуры — зона `ozhegov`, эпик `procedure-frames` #900;
ужесточение контракта фреймов идёт ратификацией под `frames-alive` S0. Комната готовит
решение, ратификация — отдельным порядком.

---

## Входы, которые не пересматриваются

**§1–§7** контракта заседания. Ключевое для этой комнаты: неймспейс не имеет `kit`; дом может
заказать один кит; прибор мастерской скриптов считает наборами **киты**; слой B из §7 —
поштучный провод инструмента.

Тезисы владельца (входы): профиль сессии неизвестен, дополнительные инструменты приходят
**через вызов процедур** — это один из фреймов процедуры. Это **фрейм кита**, а не фрейм
проводов; набор кита **не обязан** лежать в одной мастерской.

## Факты (замер 30.07)

**Паттерн `docs/patterns/PROCEDURE_FRAMES.md`** — статус рекомендации (SHOULD), держатель
`ozhegov`, эпик #900. Объявляет фреймы двух родов: серию **сюжетных** и до трёх **служебных**:

| вид | тег | что несёт |
|---|---|---|
| сюжетный | `сюжетный` | основная линия работы, последовательность |
| служебный | `провода` | входы и связи прогона, **в первую очередь LLM-канал** |
| служебный | `времянки` | где живут временные файлы, как убираются |
| служебный | `доставка` | довести результат до сервера или соседей |

Там же первым из трёх хронических дефектов назван: «**Провода не подведены** — у процедуры
нет фрейма, отвечающего за провода, некому было объявить канал».

**Кит** — `kits/<id>/MANIFEST.json`, поля `id`, `leadPersona`, `roots`, `pins`. Китов в дереве
**7**: `angelina-morning`, `angelina-bridge`, `tasks-master`, `tests-master`, `dream-master`,
`containerization-master`, `witcher`. Имена — процедуры и персоны, **не дома**.

**Киты стоят на пути:** `kits:audit --mode latest` и строгая сверка запиненных файлов идут в
pre-push. Это единственный род оснастки в проекте, который сессия зовёт исправно, — контраст,
с которого началось всё заседание.

**Из 12 мастерских кит объявляют две:** бестиарий (`witcher`) и задачи (`tasks-master`);
у десяти `kit: null`.

**Механика фреймов сегодня:** полоса `frames` очереди манифеста процедуры, элемент
`{id, holder, pins?}`, валидатор `scripts/lib/validate-procedure.mjs`. Виды объявляются тегом
на элементе; тег — рекомендация, схемного зуба на него нет.

## Что комната обязана решить

1. **Появляется ли вид** — становится ли кит четвёртым служебным видом фрейма рядом с
   проводами, времянками и доставкой, или провизия инструментов выражается иначе.
2. **Что объявляет фрейм** — если вид появляется: какие поля несёт элемент, чем ссылается на
   кит, что происходит при недоступном ките.
3. **Отношение к тегу `провода`** — сегодня он про входы и связи «в первую очередь LLM-канал».
   Кит — расширение этого тега, сосед или пересечение. Расширение смысла ратифицированного
   тега — отдельная цена.
4. **Держатель фрейма** — кто отвечает за то, что процедура объявила свой кит, и чем эта
   ответственность отличается от `leadPersona` самого кита.
5. **Проверяемость** — чем проверяется наличие и корректность фрейма: валидатор процедуры,
   зуб китов, ничего. Если ничего — сказать прямо.
6. **Цена и путь ратификации** — какой ценой вид добавляется в паттерн со статусом
   рекомендации, и что должно произойти, чтобы это не превратилось в MUST мимо стола.

## Форма вердикта

Появляется вид или нет; если да — поля, отношение к соседним тегам, держатель, проверяемость,
путь ратификации. Числа выше — материал проверки, не то, что вердикт обязан воспроизвести.
