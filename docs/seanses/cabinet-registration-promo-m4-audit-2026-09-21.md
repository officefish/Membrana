# Аудит M4 «Регистрация в кабинете по промокоду панели» — форма, фраза, частота, лендинг — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Задание: `docs/meeting/cabinet-registration-promo/AUDIT_M4_BRIEF.md`. Предмет: повестка `M4_AGENDA.md` и протокол `docs/seanses/cabinet-registration-promo-m4-form-landing-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `8827e42d`; 38 реплик, модель `xai/grok-4.5`, `--min-replies 36`). Ствол для сверки: `origin/main` = `f989dbb6`. Протокол и повестка не правились. Секции после посылок («Повестка (полное эхо)», «Данности», «Фактура», «Вопрос на консилиум (полное эхо)», стр. 193–227) — добавка инструмента, не аудируются.

## Итог

**ОТКЛОНЕНО** по пунктам **3** и **8**.

- **3.** Из 28 посылок четыре не держатся на дословной реплике названной роли: П9 — стык двух реплик [Структурщика] (стр. 50 и 62) без многоточия; П11 — слово «запрещён» приписано [Архитектору], у которого его нет (стр. 76 «обход»), совпадает со строкой таблицы 133; П25 — «`useAuth()` даёт только `login`» — текст фактуры повестки, в репликах нет (стр. 102 говорит «`login | register`»); П27 — «201 — без фразы, вход в сессию» — строка таблицы 134, реплика [Математика] стр. 56 звучит иначе. Остальные 24 дословны у названных ролей.
- **8.** Расширение границ D названо словами (стр. 133, 140, 142): `AuthContext.tsx` и два файла тестов. Но тест формы, который вердикт требует — «disabled кнопки при submit и после 429» с подменой времени (стр. 140, 58, 96) — неисполним текущей инфраструктурой `apps/cabinet`: в `vite.config.ts` нет блока `test` с DOM-окружением, `jsdom`/`@testing-library` нет ни в `apps/cabinet/package.json`, ни в корне, все 23 теста веб-кабинета — чистые функции или `renderToStaticMarkup` (`NodeOverflowHoldLine.test.tsx:1,16`). Интерактивный тест формы требует правки `apps/cabinet/package.json` и `apps/cabinet/vite.config.ts` — файлы вне D, вердикт о них молчит. Урок M2 (`app.module.ts`) в новой одежде.

Остальные шесть пунктов — PASS: контракт назван буква в букву (функция, тип ошибки, шесть русских фраз, 600 секунд, две карточки лендинга, подпись), 34 строки фактуры сходятся, данности целы, заголовки по-русски, 38 реплик без пустых.

## Таблица

| # | Пункт | Вердикт | Цитата / команда |
|---|---|---|---|
| 1 | Один вопрос | PASS | Контракт двери, коды и английские фразы сервера не тронуты: «Клиент шлёт только `{login,password,code}`. 201 тело сессии „как у входа“» ([Архитектор], стр. 104); «Не тронуты контракты M1–M3 (дверь сервера, office client, английские message сервера)» (стр. 152). Выкатка — «слово владельца» (стр. 64, 98, 150). Утечек в таблице как решений нет. |
| 2 | Вердикт про контракт | PASS (замечание) | Функция и тело: «`registerRequest(login, password, code)` … → `POST /v1/auth/register`, тело `{ login, password, code }`, успех 201 → `{ token }` (как login). Ошибки — `AuthApiError` со `status` и русской `message`» (стр. 132; сигнатура стр. 86–90). Сессия после 201: «`AuthContext.tsx` — метод `register(...)` по образцу `login` (request → `setStoredToken` → me)» (стр. 133). Фразы буква в букву — стр. 134: 401 «Регистрация сейчас закрыта.», 403 «Регистрация не принята.», 429 «Слишком много попыток. Подождите 10 минут.», 503 «Сервис временно недоступен. Попробуйте ещё раз.», иной статус «Не удалось выполнить регистрацию. Попробуйте ещё раз.», сеть «Нет связи с сервером. Проверьте интернет и попробуйте снова.», 201 — без фразы; выбор «Только HTTP-статус (не English message)». Секунды — «disabled **600 секунд** (`retryBlockedUntil = now + 600000`)» (стр. 136). Лендинг сейчас/после дословно с href — стр. 138–139. Подпись — стр. 137. Замечание: тип успеха назван `{ token }`, в стволе `LoginResponse` = `{ token, user }` (`api/auth.ts:11–14`); «как login» это покрывает, но литерал в таблице у́же типа. |
| 3 | «Список посылок» | **FAIL** | Секция есть (стр. 156), 28 посылок, тип у каждой. Скрипт: жирное и кавычки нормализованы, каждая часть цитаты (по « / » и многоточию) — точная подстрока по репликам стр. 44–122, роль по ближайшему маркеру `[Роль]:` выше; спорные разобраны глазами. **Дословны у названных ролей — 24**: П1–П5, П7, П8, П10, П12–П24, П26, П28 (П6 — с точностью до знака на границе: в реплике стр. 44 после `message` двоеточие, в посылке точка; П13 — многоточие без пропуска, стр. 56; П26 — две реплики одной роли, стр. 96 и 68, под одним многоточием; П28 — регистр первой буквы, стр. 64). **Не держатся — 4:** **П9** «`AuthApiError` с `status: number` и `message: string`, плюс `mapAuthErrorStatus` / `mapRegisterErrorStatus`» — стр. 50 кончается на «`mapAuthErrorStatus(status): string`», а «`mapRegisterErrorStatus`» появляется на стр. 62; склейка без многоточия; **П11** вторая часть «запрещён» — [Структурщик], [Архитектор]: у [Архитектора] стр. 76 «`setStoredToken` со страницы — обход», у [Структурщика] стр. 114 «запрет»; «запрещён» — строка таблицы 133; **П25** «`useAuth()` даёт только `login`» — текст фактуры повестки, в репликах отсутствует (стр. 102: «`useAuth` даёт `login \| register`»); подписано «фактура; подтверждено решением» — по повестке «посылка „из фактуры повестки“ без реплики — не посылка»; **П27** «201 — без фразы, вход в сессию» — строка таблицы 134; реплика [Математика] стр. 56: «201 — успех, фраза в UI не нужна: сразу сессия» (вторая часть «успешный такт = вход в кабинет» — [Музыкант] стр. 78 ✓). |
| 4 | Фактура против ствола | PASS | Проверено 34 строки `git show origin/main:<путь> \| sed -n '<строка>p'`. `LoginPage.tsx:5` `const { login } = useAuth();`, `:6–9` `loginName`/`password`/`error`/`submitting`, `:16` `await login(loginName, password)`, `:60` `{error}`, `:73–76` DEV-подпись, `:77–80` «Prod: учётная запись выдаётся администратором при деплое»; `api/auth.ts:4` `getApiBase`, `:28` `setStoredToken`, `:36` `async function parseError(res: Response): Promise<string>`, `:40` `return body.message`, `:44` `return res.statusText \|\| 'Request failed'`, `:47–54` `loginRequest` → `/v1/auth/login`, `:57` `fetchMe`; `registerRequest` — 0 вхождений; `AuthContext.tsx:26` `login: (login: string, password: string) => Promise<void>`, `:48–49` `useCallback(async …) → loginRequest`; `index.html:153–157` карточка «Кабинет — регистрация» с href `https://cabinet.membrana.space`; `<Route` в `App.tsx`/`main.tsx` — 0; тесты `apps/cabinet/src` — 23 файла, страницы входа и `api/auth.ts` среди них нет; `scripts/_ssh-root-site-setup.mjs` есть. Утверждения вердикта о стволе (стр. 44 «строки 36–45 статус теряет», 52 «`grep "<Route"` пуст», 54 «`submitting` уже есть (LoginPage 6–9)», 64 «`index.html:153–157`») — все верны. |
| 5 | Данности и M1–M3 | PASS | `grep -n -iE "check\|причин\|истёк\|исчерпан\|OFFICE_API_TOKEN"` → 9 попаданий, все в смысле запрета: стр. 46 «`check` запрещён, причины 403 не различаем», 66 и 98 «Не строить: pre-check кода, подсказки „истёк/исчерпан“ … различение причин 403», 141 «Что не строить: check кода до отправки; ветвление причин 403», 151, 161, 180. `OFFICE_API_TOKEN` — 0; «Не тащить office key» (стр. 102). Сервер новых полей и кодов не получает (стр. 104). |
| 6 | Словарь | PASS (замечание) | Разделы: «Итоговое решение консилиума», «Критерии готовности», «Список посылок» — по-русски; урок M2/M3 усвоен. Заголовки строк с латиницей: «Вызов API» (132), «Чинить parseError» (135), «Подпись внизу (prod)» (137), «Зубы D» (140), «Расширение границ D» (142) — имена функций, буквы сессий и аббревиатура; «prod» в скобках — англицизм вместо «в проде», замечание. В ячейках: «guard», «mock», «fake timers», «countdown», «i18n-фреймворк» — термины, в заголовках их нет. |
| 7 | Реплик не меньше тридцати | PASS | `grep -cE "^\[(Архитектор\|Верстальщик\|Математик\|Teamlead\|Структурщик\|Музыкант)\]"` → **38**; по ролям: Teamlead 7, Математик 7, Архитектор 6, Верстальщик 6, Музыкант 6, Структурщик 6. Условия `--min-replies 36` (стр. 26), футер «Реплик в диалоге: 38» (189). «Принимаю» без содержания — 0: пять заключительных (стр. 112–120) несут по фразе существа. Вход «координация ролей — обрезан» (стр. 20) — шестой прогон подряд. |
| 8 | Исполнимость в границах D | **FAIL** | Названо явно: «Сессия после 201 — Расширение **вне M0-D**: `apps/cabinet/src/context/AuthContext.tsx`» (133); «Файлы тестов — **расширение границ D**» (140); строка «Расширение границ D — Явно: `AuthContext.tsx`; `auth.register.test.ts`; `LoginPage.register.test.tsx`. В границах M0: `LoginPage.tsx`, `api/auth.ts`, `deploy/root-site/index.html`» (142); реплики [Teamlead] 70, [Структурщик] 74, [Архитектор] 76, 92. Новых страниц и маршрутизатора нет. **Молчание:** вердикт требует «`LoginPage.register.test.tsx` — disabled кнопки при submit и после 429» (140), «После 429 — disabled истинно при fake timers +600000» ([Математик], стр. 96), «mock register rejects … DOM показывает „Регистрация не принята.“» (96). По стволу: `apps/cabinet/vite.config.ts` — блока `test` нет (`grep -n "test\|environment\|jsdom"` → пусто); `jsdom`, `happy-dom`, `@testing-library` — нет ни в `apps/cabinet/package.json`, ни в корневом `package.json`; корневого `vitest.workspace` нет; единственный `.tsx`-тест кабинета `components/nodes/NodeOverflowHoldLine.test.tsx` рендерит через `renderToStaticMarkup` (`react-dom/server`, стр. 1, 16) — без DOM, без событий и таймеров. Тест «нажал → disabled → через 600 с снова активна» так не пишется: нужна DOM-среда (`environment: 'jsdom'` в `vite.config.ts`) и библиотека рендера в `package.json` — два файла вне D, о которых вердикт не сказал. Тест `api/auth.register.test.ts` (подмена `fetch`, статус → фраза) исполним без правок. Как расширять D — решение владельца при ратификации; аудитор способ не выбирает. |

## Что аудитор не делает

Своих решений по форме и по границам сессии D не предлагает; протокол и повестку не правит; повторный прогон — решение председателя после ратификации владельца.
