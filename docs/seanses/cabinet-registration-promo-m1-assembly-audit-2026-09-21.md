# Аудит сборки председателя — M1 «Регистрация в кабинете по промокоду панели», грант и дверь офиса — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Предмет: сборка `docs/seanses/cabinet-registration-promo-m1-grant-door-assembly-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `972a1759`) против протокола `…-m1-grant-door-2026-09-21.md` и аудита `…-m1-audit-2026-09-21.md` (PR #2378, влит `0a2fe79d`). Три проверки по заданию председателя: (а) цитаты трёх починенных посылок дословны и у заявленной роли; (б) сборка не меняет ни одной ячейки решения и определения готовности; (в) по пункту 8 председатель ничего не решил сам. Ствол для сверки: `origin/main` = `0a2fe79d`. Сборка, протокол и повестка не правились.

## Итог

**ЧИСТО** по всем трём проверкам. Одно уточнение к самой сборке: цитата «Тело отказа: …» отнесена к стр. 85, но роль на этой строке не стоит — стр. 85 есть продолжение реплики [Структурщика], начатой на стр. 74 (между ними таблица отказов, стр. 76–83); авторство верно, номер строки физический. Замечание, не расхождение.

## Таблица

| # | Проверка | Вердикт | Цитата / команда |
|---|---|---|---|
| а | Три посылки на дословных репликах | PASS | Одиннадцать цитат в П5/П7/П9 (сборка стр. 14, 16, 18) и две в разделе пункта 8 (стр. 24) проверены скриптом: жирное снято, кавычки нормализованы, поиск точной подстроки на **заявленной** строке протокола и проверка маркера роли. Все 11 найдены на заявленных строках: [Архитектор] 58, [Музыкант] 95, [Архитектор] 93, [Архитектор] 70 (дважды — Q3 и «Успех redeem»), [Teamlead] 124, [Структурщик] 62 (дважды), [Математик] 66, [Структурщик] 85, [Структурщик] 130. Роль на строке совпадает у 10 из 11; стр. 85 — внутри реплики, начатой `[Структурщик]:` на стр. 74, следующий маркер роли — стр. 87 `[Верстальщик]` (`awk 'NR>=74 && NR<=87 && /^\[/'`). П5 теперь стоит на трёх репликах вместо строки таблицы 170; П7 — на репликах 70 и 124 вместо строки 178; П9 — на четырёх репликах вместо «путь, охрана, тела». Девять остальных посылок в сборке не воспроизведены — «остаются как в протоколе» (стр. 12), их дословность подтверждена аудитом #2378. |
| б | Решение и готовность не изменены | PASS | Сборка не воспроизводит таблицу: строк вида `\| Вид кода \|`, `\| Дверь \|`, `\| Отказы \|`, `\| Порядок предикатов \|`, `\| Атомарность \|` в сборке — 0; «Definition of Done» — 1 упоминание, как ссылка (стр. 8). Протокол на ветке побайтно равен копии, снятой при аудите #2378 (`git show <ветка>:<протокол> \| diff -q - <копия>` → без различий). Ссылка сборки «строки 162–192» покрывает заголовок (164), таблицу из 16 строк (166–183) и шесть пунктов готовности (187–192). Изменения на ветке от `4ae98a6d` до `972a1759`: только новый файл сборки и одна строка статуса M1 в `MEETING_ACTIVE.md` (`git diff --stat` → 2 files, +40 −1). |
| в | По пункту 8 председатель не решил сам | PASS | Сборка стр. 26: «Председатель границы не двигает — это правило M0, ратифицированное владельцем. Два выхода, решение при ратификации». Вариант 1 помечен «рекомендация председателя», вариант 2 — повторный прогон; ни один не объявлен принятым. Раздел «Сессии (ратифицированы четыре)» в `MEETING_ACTIVE.md` не менялся (`git diff … \| grep -c "Сессии (ратифицированы"` → 0); новая строка статуса M1 говорит «пункт 8 — развилка владельца при ратификации (… — рекомендация; либо повторный прогон). Ждёт аудита сборки». Опора развилки — реплики стр. 62 и 130, обе дословны (см. а). |
| — | Факты сборки, сверенные попутно | PASS | «14 файлов офиса используют `ApiTokenGuard`» — `git grep -l ApiTokenGuard origin/main -- packages/background-office/src \| wc -l` → 14 (из них 12 контроллеров). «snapshot-first … `panel-users.controller.ts:91–92`» — строки 91–92 и есть комментарий «Snapshot-first: между snapshot() и mutate() нет await …» (в аудите #2378 я назвал 89–91 — точнее у сборки). «`panel-users.store.ts:77–95`» — стр. 77 `mutate(fn: …)`, стр. 94–95 `writeFileSync(tmp, …)` / `renameSync(tmp, path)`. Раздел «Замечания аудита, которые уходят в задание A как факты ствола» решения не трогает: имя стража, сериализация и словарь названы как уточнения задания. |

## Что аудитор не делает

Своих решений по контракту и по границам сессии A не предлагает; сборку, протокол и повестку не правит. Ратификация M1 и выбор выхода по пункту 8 — слово владельца.
