<!-- канал: код — yarn team-memory:report (детерминированный, без LLM); база: c41637df826f -->

# Память команды — 2026-08-10

> Форма — кристалл token 121: записал / утонуло / всплывало. v2 (02.08): вытеснение и
> всплытие — поимённо, с причиной персоны (op-log: `transfer_to_archive`, `emerge`, `reject`).

## SYSTEM
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## angelina
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## dynin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (7): dynin-2026-07-18-evening-auditor-doc2-report-generator ← архив (та же форма: аудит как чистая функция audit(state)→{passed,failed,evidence} с детерминированным входом. Здесь ровно тот же скелет: check_liveness(registry)→{alive[],dead[],evidence… (полностью — в журнале)) · dynin-2026-07-09-insight-hermes-liaison-agent ← архив (детерминизм сбора: одинаковый вход → одинаковый выход, байт в байт. Проверка живости обязана быть детерминированной по id, флейк-риск изолируется в отдельный глагол с фиксированным… (полностью — в журнале)) · dynin-2026-08-01-evening-review-predicate-m4-memoization ← архив (там я уже разбирал коллизию двух предикатов на одном органе; здесь та же форма: required(record) и satisfied(record, forecasts) должны быть определены на одной open-записи, иначе а… (полностью — в журнале)) · dynin-2026-08-03-one-shot-manifest-m3-volume ← архив (оттуда беру привычку явно называть предикат и reason ∈ {…} вместо строк; ниже применяю к problems.) · dynin-2026-07-29-tariff-grid-m7-teeth ← архив (прямо использована формализация зуба (predicate, locus, effect) для оценки нового зуба в v3 и для формулировки оговорки про инъективность cutBlockId.) · dynin-2026-08-03-static-mmbrn-container-m1-boundary ← архив (предикат принадлежности класса ровно про то, что делает v3: расширение множества валидных состояний рода без мутации предиката.) · dynin-2026-08-08-static-mmbrn-container-m7-migration-delivery ← архив (там я уже фиксировал: inventory-gate это не «прогнали и хорошо», а fenced snapshot с per-object предикатами. Ровно этот контракт сейчас переезжает в lib — субъект «collection-bson-… (полностью — в журнале))

## farrell
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## kuryokhin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## ozhegov
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: облако пустое — архив кандидатов не дал

## rodchenko
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня: лифт не звали

## tarasov
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (6): tarasov-2026-08-03-one-shot-manifest-m1-frames ← архив (там я сам ставил рамку: пока держатель не назначен и прогон не зафиксирован, шот не запускается. Здесь ровно обратная ситуация — держатель ты, прогон уже сделан в scratchpad, остал… (полностью — в журнале)) · tarasov-2026-07-26-team-evening-feedback ← архив (оттуда правило: MAIN_DAY_ISSUE = единственный достоверный фокус, план дня — вещдок. Веха secret-parser-built — из стендапа-магистрали, и последнее сомнение по ней снимается ровно э… (полностью — в журнале)) · tarasov-2026-08-03-one-shot-manifest-m3-volume ← архив (прямо по делу: там я разделял гейт (forecast) и стоп (fact) как разные силы. Здесь ровно этот случай: forecast шота №3 красный — гейт не пускает, значит шот не штампуется, маршрут … (полностью — в журнале)) · tarasov-review-oversized-queue-cut-1 ← архив (прямо в тему: там я уже фиксировал «предсказание ↔ исход» по нарезке (110/200, 200/258 и т.д.). Это тот самый род записи, которую B2-гейт хочет сделать обязательной; значит ключ ам… (полностью — в журнале)) · tarasov-2026-08-01-evening-review-predicate-m4s-existing-organs ← архив (там ровно та же логика «куда встаёт новый шаг в существующий контур»: здесь incompleteCollections переезжает в protocolChecks, и вопрос совместимости старых манифестов = операциона… (полностью — в журнале)) · tarasov-evening-order-and-trunk-cut-1 ← архив (напоминание: замер без прогона в мерку не входит. Здесь прогон есть, манифест приложен дословно — приёмка засчитывается, в отличие от тех несделанных нарезок.)

## vesnin
- записал в оперативку: изменений нет
- утонуло в подсознание: ничего
- всплывало сегодня (3): vesnin-2026-08-01-evening-review-predicate-m4s-existing-organs ← архив (прецедент о том, что новые проверки надо ставить в уже существующие органы, а не плодить точки. Прямо про выбор (а) execution-gate против (б)/(в): гейт живёт там, где уже есть закр… (полностью — в журнале)) · vesnin-2026-07-19-insight-archive-lifecycle-c2-state-axes-r4 ← архив («форма следует функции, декор без конструкции не существует». Здесь — против соблазна дублировать гейт в task:archive и sprint-cut: одна функция «нельзя закрыть без записи прогноза… (полностью — в журнале)) · vesnin-2026-08-03-static-mmbrn-container-m1-boundary ← архив (образец «форма контракта = положительная граница + отрицательная граница + инвариант смены версии»; ровно этот каркас нужен для amnesty-by-schema, чтобы @1/@2 держались в одной лен… (полностью — в журнале))

**Итог:** записано 0 · вытеснено 0.
