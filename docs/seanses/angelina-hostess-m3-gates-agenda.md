# Заседание «Ангелина — хозяйка утра» — M3 (G: гейты утра в код)

**Объединяющее задание:** утро не воспроизводится старым; гейты — механика, не дисциплина;
ведёт хозяйка. Порядок (ратифицирован): `C → B → G → H → GC`. C и B закрыты (M1, M2
ратифицированы). Сейчас — G. Вердикты ritual-refactor 20.07 не переоткрываются. Киты вне
скоупа.

## Вопрос M3 (ровно один)

**G1 — Как провести двухгейтовое утро в код: снять telegram-ружьё из цепочки,
гейт magistral (owner-choice из топ-3) и гейт swallow-send — механикой, а не дисциплиной?**

⚠️ **Обязательное эхо в вердикте:** строки «Ружьё Д-1: <решение>», «Гейт magistral:
<решение>», «Гейт swallow-send: <решение>». Вердикт обязан нести секцию-заголовок
**«Список посылок»** (иначе не состоялось, S-M2; задним числом запрещено).

Решаем только G. Не залезать в сборку (B — закрыт), хозяйку фоном (H), затирание (GC).
Ратифицирует владелец.

## Грани вопроса G (простым текстом)

- **Д-1 — ружьё:** шаг `telegram-digest` в цепочках `ritual:day`/`ritual:evening` после
  починки экстрактора стал способен реально отправить без «ок» владельца. Решение 18.07
  («автоотправка снята») так и не доведено до package.json. Снять — одна строка; вопрос
  комнаты: чем гарантировать, что шаг не вернётся (гейт против регрессии).
- **Гейт magistral:** магистраль дня легитимна только через явный owner-choice из топ-3
  (тезис Т-2 шторма; рецидивы 16–17.07 и 21.07). `buildTop3` уже в main. Как цепочка
  СТОИТ до выбора: где пауза (до генерации main-day-issue?), как выбор фиксируется
  (перечеканка `main-day-assertions.json` sources[0]?), как продолжается (`--decide`-механика?
  — но абсорбция фронтира конкурента = предмет H, здесь только контракт гейта). Скрипт
  без владельца рядом (ночной прогон?) — поведение.
- **Гейт swallow-send:** отправка партнёрам только после «ок». Что уже есть в main:
  `canPublish` (structuralIntact ∧ tokensKept ∧ noDead — R-ядро), чек живых ссылок.
  Чего нет: механической связки «canPublish прошёл → показ владельцу → явное „ок“ →
  telegram:swallow»; сейчас «ок» живёт в чате дисциплиной. След «ок» (кто/когда) для
  аудита.
- **Общий контракт двух гейтов:** единая форма «стоп → предъявление → решение владельца →
  след решения → продолжение» или два разных механизма? (Подсказка: фронтир-механика
  конкурента ровно об этом — но её абсорбция в H; здесь зафиксировать ТРЕБОВАНИЯ гейтов
  к ней.)

## Посылки-подсказки (для вердикта, не сам вердикт)

- Т-2 шторма (#741, ратифицирован владельцем): ровно два стопа утра — magistral и
  swallow-send; probe-resolve и day-anchors гейтами НЕ становятся.
- `buildTop3` (балансировка зон) — в main; M2 этого заседания: сборка per-slot, слот
  «Список посылок» в frame.
- R-ядро в main: `canPublish`, `classifyLinkStatus`, protectedTokens; чек живых ссылок —
  норма (память 20.07: #660 не PR, #599 CLOSED — ловится активной проверкой).
- Прецеденты рецидива магистрали без owner-choice: 16.07, 17.07, 21.07 (холодная сессия).
- Решение 18.07: автоотправка дайджеста снята (CLAUDE.md); package.json это не отразил.
