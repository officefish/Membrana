<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-08-21T14:22:18.268Z |
| Команда | `yarn consilium` |
| Модель | xai/grok-4.5 |
| Файл | `docs/seanses/duty-node-detection-m1-slovar-2026-08-21.md` |
| Порядок ролей | Верстальщик → Архитектор → Teamlead → Музыкант → Математик → Структурщик |
| Повестка | `docs/meeting/duty-node-detection/M1_AGENDA.md` |

**Вход сеанса** (что комната действительно получила):

| Вход | Носитель | Размер | Отпечаток | Доставка |
|------|----------|-------:|-----------|----------|
| повестка (эхо в голове) | `docs/meeting/duty-node-detection/M1_AGENDA.md` | 1906 | `57516da602c2` | полностью |
| инструкция консилиума | `docs/prompts/CONSILIUM_PROMPT.md` | 4494 | `b6bd1a8dc127` | полностью |
| координация ролей | `docs/VIRTUAL_TEAM_PROMPT.md` | 8034 | `75959c7f060d` | **обрезан** |
| повестка | `docs/meeting/duty-node-detection/M1_AGENDA.md` | 1906 · 1 п. | `57516da602c2` | полностью |

> ⚠ Часть входа не доехала до комнаты целиком. Утверждения протокола о своём входе
> сверять с этой таблицей, а не с текстом реплик.

**Условия прогона:** `--no-context --no-rag --min-replies 30`

**Вопрос:**

Комната M1 «Словарь» заседания duty-node-detection. Единственный вопрос O1 — в повестке. Вердикт обязан назвать список посылок.

---

# Консилиум: M1 «Словарь» — режимы работы узла (O1)

**Повестка:** O1 — Какой словарь режимов работы узла принимается как обязательный: какие леммы существуют, чем каждая отличается от соседних, и где словарь живёт носителем?

**Участники:** Teamlead (Tarasov), Архитектор (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Верстальщик → Архитектор → Teamlead → Музыкант → Математик → Структурщик

---

[Верстальщик]: С витрины оператора три режима должны читаться без сноски. Если на экране «наблюдение», а в коде `field:capture` — это ложь формы: кнопка врёт о действии. Мне нужны три леммы с различителями, которые можно посадить на badge и фильтр списка записей. Судьба «наблюдения» — убрать из UI-метки режима, иначе 116 чужих треков снова смешаются с сеансом глазами оператора.

[Архитектор]: Конструкция комнаты узкая: только словарь, не форма дежурства и не граница узел/сервер. Три леммы со шторма Т4 уже даны Ожеговым и не переоткрываются: съёмка, сеанс, дежурство. Различители — цель и отношение к человеку/времени, не «как устроен поллер». Носитель словаря — не абзац в `ARCHITECTURE.md` alone: канон ссылается, типы держат норму. Рекомендую тип-дискриминант в `@membrana/core` + короткая отсылка в каноне.

[Teamlead]: Положение: сегодня съёмка и сеанс были, дежурства — ни разу; режима в продукте нет, слова в словаре — обязаны быть, иначе соседние комнаты пишут в пустоту. Решение комнаты — зафиксировать три леммы и запрет «наблюдения» как режима. Связка после вердикта: Ozhegov — носитель типов, Vesnin — строка в каноне. Планка: без списка посылок и без явного owner’а словаря — BLOCK на приёмку протокола.

[Музыкант]: С полевого тракта: `field:capture` — это съёмка, калибровочные пробы усиления в корпус не идут — цель не корпус, а настройка тракта; лемма всё равно «съёмка», а судьба материала — не здесь. Сеанс борда (670 треков, 55,4 мин) — живой диалог с оператором. Поллер с очередью ADR-0027 звучит как «почти дежурство», но человек кладёт задания — это не непрерывный режим без человека. Дежурство пока слышу только как партитуру будущего, не как текущий звук.

[Математик]: Формализую различители предикатами, без UI. Пусть запись `r` лежит в буфере устройства. Режим — не выводим из наличия файла: 116 чужих треков доказывают неразличимость по сырому содержимому. Нужен явный признак. Предлагаю закрытое множество `NodeWorkMode = 'capture' | 'session' | 'duty'`. Различители: (1) cardinality/time-bound: capture — конечный штучный акт; session — интервал с оператором; duty — open-ended без оператора; (2) goal: corpus sample | hypothesis check | stats/event windows. «Наблюдение» ∉ множества.

[Структурщик]: Леммы принимаю как словарные статьи, не как README-синонимы. Один термин — один смысл: `capture` / `session` / `duty` в коде; в русской прозе — съёмка / сеанс / дежурство. Синоним «наблюдение» как режим — нарушает норму словаря, его надо изъять из технического контура (код, ADR, UI-строки режима, манифесты заданий). Носитель: discriminated union / string-union в `@membrana/core`, экспорт через `index.ts`; `ARCHITECTURE.md` — ссылка «словарь режимов узла → core», не вторая копия определений.

[Верстальщик]: Тогда на карточке записи — обязательный badge режима из трёх значений, плюс окно времени, если оно есть. Без режима фильтр «только сеанс» врёт: подмешает июльские чужие треки. Empty-state дежурства честный: «режим ещё не выполнялся», не маскируем сеансом. Метка «наблюдение» в UI — не использовать.

[Архитектор]: Цена альтернатив носителя. (A) Только `ARCHITECTURE.md` — дешево писать, дорого проверять компилятором: строки разъедутся. (B) Отльный документ-словник — третий источник истины, риск рассинхрона с типами. (C) Типы в `@membrana/core` + абзац-указатель в каноне — один source of truth, канон не раздувается. Беру (C). Owner словаря как типа — Структурщик; owner канон-ссылки — Архитектор.

[Teamlead]: Согласен с (C). Не тащим в DoD этой комнаты форму дежурства и lifecycle материала — это чужие комнаты. Здесь DoD: леммы, запрет «наблюдения», носитель, признак на записи. Дежурство в словаре = лемма-резерв под будущий режим, не обещание, что поллер уже duty. Кто напишет «поллер = дежурство» после сегодняшнего материала — получит BLOCK.

[Музыкант]: Калибровка усиления сегодня — съёмка по акту (разовые пробы), даже если в корпус не кладём. Иначе появится четвёртая лемма «калибровка» без нужды: цель и жизненный цикл плода — соседние комнаты. Мне важно, чтобы sidecar/capture-протокол перестал говорить «наблюдение» там, где имеется capture.

[Математик]: Признак режима на записи — необходим статистически: P(перепутать сеанс и чужой хвост | нет mode и window) на живом буфере уже реализована (116 файлов). Минимальный контракт: `mode: NodeWorkMode` на артефакте буфера; окно `(startedAt, endedAt)` для session/duty — отдельное поле, не подмена mode. Capture может нести точку времени акта, не обязательно интервал. Не смешивать mode и disposition (в корпус / не в корпус) — разные оси.

[Структурщик]: Ось disposition (корпус/отход/калибровка) действительно не режим: сегодняшние калибровочные пробы — capture + disposition≠corpus. Если смешаем в одно поле — снова ожеговский грех синонима. Словарь O1 = только mode. Поле mode на записи буфера — да; адаптеры `field:capture` → `capture`, сценарий борда → `session`; поллер заданий — не маппить в `duty` автоматически, пока оператор — источник заданий.

[Верстальщик]: Для a11y бейджа: не цвет единственный носитель смысла — текст «съёмка|сеанс|дежурство». Фильтр списка — enum из core, не free-text. Если mode отсутствует у старых 116 треков — явная метка «режим неизвестен», не угадывать session.

[Архитектор]: Легаси без mode: не ретро-фит фантазией. Норма вперёд — mode обязателен для новых плодов; backfill — отдельное решение не этой комнаты (иначе колонизируем lifecycle). В каноне одна фраза: «режимы узла: capture/session/duty; см. `@membrana/core`; слово „наблюдение“ не режим».

[Teamlead]: Фиксирую дисциплину языка для связок: в task-брифах и ADR дальше только три леммы. «Наблюдение» в разговорной речи капитана не запрещаем — запрещаем в коде, UI режима, каноне и контрактах. Это снимает конфликт шторма без полицейского над чатом.

[Музыкант]: Подтверждаю конфликт шторма с полевой стороны: «наблюдение» тянули и на разовую съёмку, и на мечту о круглосуточном слушании. В DSP-логах и capture-protocol оставлю capture/session; duty — когда появится непрерывный контур без человека, не раньше.

[Математик]: Таблица различителей (для вердикта, не новый режим):  

| Лемма | Время | Агент | Цель |
| съёмка / capture | акт, штучный результат | может быть с оператором | корпус (или проба тракта; disposition отдельно) |
| сеанс / session | ограниченный интервал | оператор в контуре | проверка гипотезы |
| дежурство / duty | непрерывно / open-ended | без человека | статистика и событийные окна |

Поллер ADR-0027 ∉ duty по предикату «без человека как источника заданий».

[Структурщик]: Имена в TS: `NodeWorkMode` с литералами `'capture' | 'session' | 'duty'`. Русские ярлыки — словарь презентации у Верстальщика (map в UI), не дублировать union. Запрещённые алиасы режима: `observation`, `watch`, `monitor`, `наблюдение` — не экспортировать. Жильё: `packages/core` (или актуальный путь `@membrana/core`), баррель `index.ts`.

[Верстальщик]: Map для UI: capture→«Съёмка», session→«Сеанс», duty→«Дежурство», undefined→«Режим неизвестен». Никакого «Наблюдение» в map. Принимаю.

[Архитектор]: Итоговая форма решения O1: (1) три леммы с различителями таблицы Дынина; (2) «наблюдение» снять с роли режима; (3) носитель — union в `@membrana/core`, указатель в `ARCHITECTURE.md`; (4) новым записям буфера — обязательный `mode`. Соседние комнаты не предрешаем. Нужен ADR только если канон сейчас молчит о режимах узла — короткий ADR «словарь NodeWorkMode» допустим как след, но source of truth всё равно тип.

[Teamlead]: ADR — по цене: если тип + абзац в ARCHITECTURE закрывают расхождение — отдельный ADR не обязателен; если в репо уже есть ADR-0027 про поллер, достаточно перекрёстной ссылки «поллер ≠ duty (M1)». Вердикт комнаты — словарь, не простыня процессов. Принимаю форму Веснина. Связка исполнения после заседания: Ozhegov (тип) + Vesnin (канон-строка); Rodchenko — map UI когда дойдёт до экрана буфера, не блокирует словарь.

[Музыкант]: Принимаю три леммы и запрет «наблюдения». Прошу в DoD не включать «реализовать дежурство» — только словарь и признак, иначе снова сыграем партитуру, которой нет на пульте.

[Математик]: Принимаю. Добавляю в норму: mode — дискриминант для последующих чистых функций статистики (event windows считаются на duty-подмножестве; смешение с session ломает метрики). Это мотивация признака, не выход за O1.

[Структурщик]: Принимаю. Посылка «дежурства не было» не вычёркивает лемму: словарь описывает допустимые режимы, не journal выполненных. Лемма duty существует; экземпляр режима — нет. Это важно для соседей: они проектируют duty, опираясь на пустой journal, не на отсутствие слова.

[Верстальщик]: Принимаю. DoD UI не раздуваю: для O1 достаточно обязательства «не вводить метку Наблюдение как режим» в новых строках; полный экран фильтров — когда будет задача на буфер.

[Архитектор]: Принимаю. Единый вердикт без ветвления «зависит».

[Teamlead]: Ещё раз по посылкам — только входы, без выводов комнаты. Шторм Т4 и живой день — в «Список посылок». Вердикт ниже. LGTM на словарь при наличии секции посылок и DoD без колонизации соседей.

[Музыкант]: Короткая реплика согласия: калибровка сегодня не плодит лемму. Принимаю.

[Математик]: Согласен с леммой-резервом duty. Принимаю.

[Структурщик]: Финально: owner типа `NodeWorkMode` — Структурщик (ozhegov); правка канон-ссылки — Архитектор (vesnin); запрет синонима — норма M1 для всех ролей. Принимаю.

[Верстальщик]: Принимаю. Три бейджа, без четвёртого «наблюдение».

[Архитектор]: Принимаю сводную таблицу решения.

[Teamlead]: Консенсус шести есть. Закрываю обсуждение — к вердикту.

[Музыкант]: Принимаю.

[Математик]: Принимаю.

[Структурщик]: Принимаю.

---

## Список посылок

1. **норма (шторм Т4, ozhegov, не переоткрывается):** съёмка — разовое действие, результат один–несколько сэмплов, цель корпус; сеанс — ограниченное по времени взаимодействие с оператором, цель проверка гипотезы; дежурство — непрерывный режим без человека, цель статистика и событийные окна.
2. **факт (шторм Т4):** слово «наблюдение» сегодня служит синонимом съёмки и одновременно им же называют дежурство — конфликт именования зафиксирован до комнаты.
3. **факт (живой день):** сегодня прошли съёмка (калибровочные пробы усиления, в корпус не идут) и сеанс (сценарий борда с оператором: 670 треков, 55,4 минуты).
4. **факт (живой день):** дежурства не было ни разу — экземпляра режима в эксплуатации нет.
5. **факт (код):** носители в коде: `field:capture` соответствует съёмке; сценарий борда — сеансу; поллер узла с очередью заданий (ADR-0027) ближе всего к дежурству по непрерывности опроса, но задания даёт оператор.
6. **факт (сессия Б, 21.08):** в буфере устройства рядом с сеансом лежат 116 чужих треков (2 июля – 20 августа); без окна (и без режима) они неотличимы от материала сеанса.
7. **норма (повестка M1):** вердикт комнаты — только словарь; форма дежурства, граница узел/сервер, жизненный цикл материала — вне O1.
8. **норма (канон команды):** при конфликте приоритет у `ARCHITECTURE.md` и границ пакетов; публичные термины домена не плодят синонимы-слои.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Принимаемые леммы | Ровно три: **съёмка** (`capture`), **сеанс** (`session`), **дежурство** (`duty`). Четвёртых режимов (в т.ч. «калибровка» как mode) нет. |
| Различители | **Время:** акт / ограниченный интервал / open-ended. **Агент:** допускается оператор на акте и в сеансе; duty — без человека в контуре управления. **Цель:** корпус (или штучная проба тракта) / проверка гипотезы / статистика и событийные окна. Ось disposition (корпус/отход/калибровка) **не** входит в mode. |
| Судьба «наблюдение» | **Не режим.** Запрещено как значение mode и как техническая метка режима в коде, каноне, ADR, UI-enum. Разговорное употребление вне контрактов не нормируется. |
| Носитель словаря | Source of truth — тип `NodeWorkMode = 'capture' \| 'session' \| 'duty'` в **`@membrana/core`** (экспорт барреля). В **`ARCHITECTURE.md`** — указатель на тип и запрет синонима, без второй полной копии статьи. Отдельный документ-словник **не** заводится. |
| Владелец | Тип и пакетная граница термина — **Структурщик (Ozhegov)**; строка-указатель в каноне — **Архитектор (Vesnin)**; UI-map подписей — **Верстальщик (Rodchenko)** при задаче на экран. |
| Признак режима на записи | **Да, обязателен для новых** плодов в общем буфере: поле `mode: NodeWorkMode`. Легаси без mode → «неизвестно», без угадывания. Окно времени — отдельное поле, не замена mode. Автомаппинг поллера ADR-0027 → `duty` **запрещён** настоящим словарём. |
| Лемма duty при нуле экземпляров | **Остаётся в словаре** как допустимый режим; отсутствие прогонов не удаляет лемму. |

**Definition of Done (только O1):**

1. В `@membrana/core` опубликован закрытый `NodeWorkMode` и (при необходимости) type-guard; нет экспорта алиасов `observation` / «наблюдение» как mode.
2. В `ARCHITECTURE.md` — короткий указатель: три режима, ссылка на core, явное «наблюдение ≠ режим».
3. Зафиксировано правило: новые записи общего буфера узла несут `mode`; документирован запрет считать поллер ADR-0027 дежурством без смены предиката агента.
4. Протокол содержит секцию **«Список посылок»** и не содержит вердиктов по форме duty, границе узел/сервер и lifecycle материала.

---

*Реплик в диалоге: 36; каждый участник высказался не менее одного раза.*

---

## Полное эхо повестки

# Повестка M1 — комната «Словарь»

**O1 — Какой словарь режимов работы узла принимается как обязательный: какие леммы
существуют, чем каждая отличается от соседних, и где словарь живёт носителем?**

Единственный вердикт этой комнаты — словарь. Вердикты по существу режимов (форма
дежурства, граница решения узел/сервер, жизненный цикл материала) выносятся в своих
комнатах и здесь НЕ выносятся.

## Список посылок обязателен

Вердикт обязан назвать свои посылки в том же прогоне: что взято как данное со шторма,
что — из живого материала дня. Посылка, названная задним числом, не посылка.

## Дано со шторма (Т4, ozhegov — не переоткрывается)

Съёмка — разовое действие, результат один-несколько сэмплов, цель корпус · сеанс —
ограниченное по времени взаимодействие с оператором, цель проверка гипотезы · дежурство —
непрерывный режим без человека, цель статистика и событийные окна. Конфликт назван:
сегодняшнее «наблюдение» служит синонимом съёмки, но им же называют дежурство.

## Живой материал дня (проверка словаря на реальности)

- Сегодня прошли **съёмка** (калибровочные пробы усиления, в корпус не идут) и **сеанс**
  (час сценарием борда с оператором: 670 треков, 55,4 минуты). **Дежурства не было ни
  разу** — режима не существует.
- Носители в коде: `field:capture` — съёмка; сценарий борда — сеанс; поллер узла с очередью
  заданий (ADR-0027) ближе всего к дежурству, но им не является: задания даёт оператор.
- В буфере устройства рядом с сеансом лежат **116 чужих треков** со 2 июля по 20 августа —
  без окна они неотличимы от материала сеанса (находка сессии Б, 21.08).

## Что вердикт обязан покрыть внутри своего единственного решения

Принимаемые леммы и их различители · судьба слова «наблюдение» · носитель словаря
(канон `ARCHITECTURE.md` · отдельный документ · типы в `@membrana/core`) и его владелец ·
нужен ли записи признак режима, раз плоды всех режимов лежат в одном буфере.
