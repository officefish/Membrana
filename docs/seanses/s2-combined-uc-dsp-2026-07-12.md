# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-12T04:44:29.287Z |
| Команда | `yarn consilium` |
| Модель | claude-opus-4-8 |
| Файл | `docs/seanses/s2-combined-uc-dsp-2026-07-12.md` |
| Порядок ролей | Структурщик → Математик → Верстальщик → Музыкант → Teamlead |

**Вопрос:**

Собрать S2 combined UserCase (DSP-only, вариант A выбран владельцем 2026-07-12) поверх готового loop-transition-policy (#357). ОБЯЗАТЕЛЬНО: разбери КАЖДУЮ пронумерованную точку отдельным вердиктом (эхо агенды — иначе дрейф как в #355). Факты разведки: combinedScore сейчас DSP-only везде (yamnet только для sample-library, не для live/combined — оформлено инсайтом deferred); make-detection-fusion фьюзит DetectionAnalysisRef (trends+ensemble); scaffold usercase-free-combined-alarm ПУСТОЙ (loadDocument отдаёт пустой валидный документ); Beta/Gamma/Alpha — рабочие detection-alarm сценарии (fusion→branch→combined→alarm-loop).

---

# Консилиум: S2 combined UserCase (DSP-only, вариант A) поверх loop-transition-policy #357

**Повестка:** Собрать S2 combined UserCase в scaffold `usercase-free-combined-alarm` (сейчас пустой) на базе DSP-only combinedScore и готового loop-transition-policy (#357). Владелец выбрал вариант A (2026-07-12). Требование: отдельный вердикт по КАЖДОЙ пронумерованной точке агенды — эхо во избежание дрейфа (урок #355).

**Участники:** Teamlead (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Структурщик → Математик → Верстальщик → Музыкант → Teamlead

---

[Структурщик]: Прежде чем строить — зафиксируем нумерацию агенды, чтобы каждая точка получила отдельный вердикт. Читаю задачу так: **(1)** наполнить пустой scaffold `usercase-free-combined-alarm` (loadDocument сейчас отдаёт пустой валидный документ); **(2)** цепочка fusion→branch→combined→alarm-loop как в Beta/Gamma/Alpha; **(3)** combinedScore DSP-only — yamnet НЕ подключаем (deferred-инсайт); **(4)** make-detection-fusion фьюзит DetectionAnalysisRef (trends+ensemble); **(5)** интеграция loop-transition-policy #357; **(6)** отличие S2 от существующих detection-alarm сценариев; **(7)** DoD/тесты/доки. Возражения по нумерации есть? Если нет — идём по точкам, иначе дрейф.

[Математик]: Нумерация принимается. Сразу по **точке (3)**: combinedScore DSP-only — численно это корректно и честно. yamnet в live/combined дал бы латентность модели и недетерминизм на окне; для S2 combined мы остаёмся на DSP-детекторах (harmonic, spectral-flux, cepstral) плюс template-match в trends. Мой вердикт по (3): **да, DSP-only**, yamnet остаётся deferred-инсайтом для sample-library. Никаких скрытых веток «если провайдер модели» в S2 — это ровно тот дрейф, что убил #355.

[Верстальщик]: По **точке (6)** — отличие S2 от Beta/Gamma/Alpha должно читаться в UI без объяснений. Если S2 — это тот же fusion→branch→combined→alarm-loop, то владельцу нужен ясный маркер «почему это отдельный usercase». Мой предварительный вопрос к агенде: S2 — это «free-combined» (свободная композиция combined-репорта) против «alarm-scenario» пресетов? Тогда карточка usercase в каталоге должна нести бейдж семейства и одну строку назначения. Держу это для (6), пока слушаю остальных.

[Музыкант]: По **точке (4)** и (3) вместе — с аудио-контура: make-detection-fusion фьюзит DetectionAnalysisRef из trends+ensemble, и это две DSP-ветви на одном окне 48 kHz. Ключевое требование от меня — окно и hop должны быть **одни и те же** для обеих ветвей, иначе combinedScore фьюзит рассогласованные кадры и agreement врёт. Это не эксперимент, это гигиена потока. Вердикт по (4): **фьюз trends+ensemble принимаю**, при условии единого окна — фиксируем в DoD.

[Teamlead]: Хорошо, рамка задана. Напоминаю границу: S2 собирается **поверх готового** #357 и **готовых узлов** — мы НЕ переписываем make-detection-fusion, НЕ трогаем ядро loop-transition-policy, НЕ подключаем yamnet. Это сборка UserCase из существующих кирпичей в пустой scaffold. Любое «а давайте заодно улучшим fusion» — BLOCK, это отдельная задача. Идём по точкам под протокол. Структурщик, **точка (1)** — где физически живёт документ и как он перестаёт быть пустым.

[Структурщик]: **Точка (1) — наполнение scaffold.** `usercase-free-combined-alarm` — это usercase device-board, его `loadDocument` должен вернуть валидный документ с узлами и связями exec/value, а не пустышку. Граница чёткая: документ — это данные (граф узлов), а не новый код. Никаких новых node-типов не создаём — используем зарегистрированные basn-узлы (make-ensemble-analysis, make-detection-fusion, branch-on-detection, make-combined-report, alarm-loop). Вердикт по (1): **наполнить loadDocument графом из существующих узлов**; если потребуется новый узел — это отдельный Issue, не эта задача.

[Математик]: Поддержу (1) с численной стороны: документ должен фиксировать **пороги** явно — `threshold` для branch-on-detection и параметры agreement для fusion. «На глаз» пороги в S2 недопустимы; берём значения из рабочих Beta/Gamma как baseline и помечаем их как параметры документа, а не магические константы в коде узла. Это делает S2 воспроизводимым и тестируемым. Вердикт: пороги — часть документа (1), значения — из проверенных сценариев.

[Верстальщик]: **Точка (7) в части UI** (доки/каталог): карточка usercase должна следовать DESIGN.md — badge семейства (`dsp` → `info`), имя `text-sm font-medium`, одна строка назначения `text-xs text-muted`. Никакого нового визуального языка. Live-репорт combined использует уже готовую demo-раскладку по LIVE_DETECTION_UI.md — сглаживание combinedScore обязательно (EMA + гистерезис), сырой combined на индикатор не вешаем. Вердикт по UI-части: **переиспользуем существующие рендереры**, новый UI не пишем.

[Музыкант]: **Точка (5) — loop-transition-policy #357.** Это как раз то, ради чего S2 отличается от статичного репорта: alarm-loop живёт по политике переходов (ближе/дальше/lost из proximity-trend basn-4). combined-репорт кормит loop, а политика решает, длится тревога или гаснет. Моё требование: переходы должны быть **озвучены** — молчаливая остановка loop запрещена (это уже канон captureAlertToasts и DESIGN device capture). Вердикт по (5): **интегрируем #357 как есть**, S2 не переопределяет политику, только подаёт ей combinedScore + proximity-trend.

[Teamlead]: Принимаю (5) с уточнением границы: #357 — готовый, S2 его **потребляет**, не конфигурирует внутренности. Теперь **точка (2) — форма цепочки**. Структурщик, подтверди топологию: ensemble + trends → fusion → branch → combined → alarm-loop(#357) с proximity-trend как условием жизни loop. Это Beta-паттерн?

[Структурщик]: **Точка (2) — топология.** Да, это канонический detection-alarm граф: `make-ensemble-analysis` (DSP-ансамбль) + trends-детектор → `make-detection-fusion` (DetectionFusion: combinedScore, agreement) → `branch-on-detection` (detected/not по threshold) → detected-ветвь: `make-combined-report` + `alarm-loop` под loop-transition-policy, где `make-proximity-trend` (per-runId host-store) — условие жизни loop. Связи exec отдельно от value — не смешивать. Вердикт по (2): **топология Beta-класса подтверждена**, S2 = свободная параметризация этого графа.

[Математик]: По (2) добавлю инвариант: fusion получает **два** DetectionAnalysisRef (ensemble + trends) — это минимум для осмысленного agreement. Один источник → agreement вырождается, combinedScore = scoreединственного детектора, и весь смысл fusion теряется. Вердикт: **ровно 2 ветви на входе fusion** (расширяемо до 3–4, но не 1). Это численная граница корректности точки (2) и (4).

[Верстальщик]: **Точка (6) — отличие S2.** Слышу консенсус: S2 = «free-combined» — свободная композиция того же графа против фиксированных пресетов Beta/Gamma/Alpha. Для каталога это значит: одна карточка, назначение «свободный combined-alarm сценарий (DSP)», семейство `dsp`. Пользователь понимает: это шаблон для собственной сборки, а не готовый именованный пресет. Вердикт по (6): **S2 = свободный шаблон combined-alarm**, визуально маркируется как `dsp`-семейство, назначение в одну строку.

[Музыкант]: Согласен с (6) — «free» значит владелец докручивает пороги и источники, а не мы за него. Но по аудио-контуру одно предостережение к (2)/(4): если S2 позволяет свободно менять источники fusion, документ по умолчанию должен идти с **проверенной парой** (ensemble+trends на едином окне), иначе владелец соберёт рассогласованную конфигурацию и combinedScore будет мусором. Default = рабочий, свобода = осознанная. Вердикт: **default-документ = проверенная DSP-пара**, свобода поверх.

[Teamlead]: Фиксирую (6): S2 — свободный DSP combined-alarm шаблон с рабочим default. Это чётко отличает его от именованных пресетов и не плодит синонимов. Теперь **точка (7) — DoD**. Собираю требования ролей. Математик — тесты ядра.

[Математик]: **Точка (7), тесты.** Unit: (а) fusion на паре ref даёт combinedScore и agreement в [0,1]; (б) branch срабатывает ровно на threshold-границе (детерминизм); (в) при одном источнике — явная ошибка/деградация, не тихий мусор. Плюс snapshot документа: loadDocument возвращает НЕ пустой валидный граф с зарегистрированными узлами. Без DOM, без yamnet — чистые проверки. Вердикт: тесты покрывают fusion-инварианты + непустоту документа.

[Структурщик]: **Точка (7), структура.** DoD со стороны границ: (а) документ не создаёт новых node-типов вне реестра; (б) usercase-catalog получает карточку через `UserCaseCatalogPort` (никаких прямых импортов из adapter); (в) ноль прямых импортов между детекторами и между усердными сервисами — только через detector-base и engine-типы; (г) `catalog:verify-client` зелёный. Вердикт: границы пакетов не нарушены — иначе BLOCK.

[Верстальщик]: **Точка (7), UI/доки.** DoD: (а) карточка каталога по DESIGN.md (badge `dsp`, назначение одной строкой); (б) live combined-индикатор через сглаживание (LIVE_DETECTION_UI.md), `aria-live="polite"` на стабильный статус; (в) переходы loop озвучены (canon captureAlert). Никакого нового визуала. Вердикт: UI = переиспользование, доки синхронизированы.

[Музыкант]: **Точка (7), аудио-DoD.** (а) единое окно/hop для обеих fusion-ветвей; (б) default-документ = проверенная DSP-пара; (в) переход loop по #357 всегда слышен/виден, молчаливой остановки нет; (г) латентность combined приемлема (DSP-only это гарантирует — модели нет). Вердикт: аудио-гигиена в DoD зафиксирована.

[Teamlead]: Всё сходится. **Сводный вердикт по 7 точкам — LGTM с условиями DoD.** Владелец Ozhegov по документу и границам, Dynin по порогам и тестам, Kuryokhin по единому окну и default, Rodchenko по карточке. yamnet — остаётся deferred, любое его подмешивание в S2 = отдельный Issue, не эта задача. Никакого дрейфа: собираем ровно S2 combined DSP-only поверх готового #357 из существующих узлов. Принимаю.

[Структурщик]: Принимаю. Документ из реестровых узлов, порты каталога, ноль cross-импортов.

[Математик]: Принимаю. DSP-only, пороги в документе, инварианты fusion под тестами.

[Верстальщик]: Принимаю. Карточка `dsp`, сглаженный combined, озвученные переходы.

[Музыкант]: Принимаю. Единое окно, рабочий default, слышимый loop.

[Teamlead]: Единый консенсус зафиксирован. LGTM.

---

## Итоговое решение консилиума

| # | Точка агенды | Решение |
|---|--------------|---------|
| 1 | Наполнить пустой scaffold `usercase-free-combined-alarm` | **Да.** `loadDocument` возвращает непустой валидный граф из **зарегистрированных** basn-узлов; новых node-типов не создаём (иначе отдельный Issue). Пороги — часть документа. |
| 2 | Топология цепочки | **Да, Beta-класс:** ensemble + trends → fusion → branch → combined-report + alarm-loop(#357), proximity-trend как условие жизни loop. exec/value связи не смешивать. |
| 3 | combinedScore DSP-only | **Да, DSP-only.** yamnet НЕ подключаем в live/combined — остаётся deferred-инсайтом для sample-library. Никаких скрытых веток «если провайдер модели». |
| 4 | make-detection-fusion фьюзит DetectionAnalysisRef (trends+ensemble) | **Да.** Минимум 2 источника (расширяемо до 3–4, но не 1). Обязательно единое окно/hop для ветвей. |
| 5 | Интеграция loop-transition-policy #357 | **Да, как есть.** S2 потребляет #357, не переопределяет политику; подаёт combinedScore + proximity-trend. Переходы loop озвучены, молчаливая остановка запрещена. |
| 6 | Отличие S2 от Beta/Gamma/Alpha | **S2 = свободный DSP combined-alarm шаблон** (free-combined) против именованных пресетов; default-документ = проверенная DSP-пара; каталожный бейдж `dsp`. |
| 7 | DoD / тесты / доки | **Определён** (см. ниже). |

**Definition of Done:**
- `loadDocument` возвращает непустой валидный граф из зарегистрированных узлов (snapshot-тест).
- Fusion-инварианты под unit-тестами: combinedScore/agreement ∈ [0,1]; branch детерминирован на границе threshold; один источник → явная деградация, не тихий мусор.
- Ровно 2 источника fusion в default (ensemble+trends), единое окно/hop.
- Пороги (`threshold`, agreement-параметры) вынесены в документ, значения — baseline из рабочих Beta/Gamma.
- DSP-only: ни одного пути подключения yamnet в S2.
- Ноль прямых импортов между детекторами / сервисами; каталог через `UserCaseCatalogPort`; `catalog:verify-client` зелёный.
- Карточка каталога по DESIGN.md (badge `dsp`, назначение одной строкой); live combined через сглаживание (EMA+гистерезис, LIVE_DETECTION_UI.md), `aria-live="polite"` на стабильный статус.
- Переходы loop (#357) озвучены/видимы (canon captureAlert); молчаливой остановки нет.
- Доки device-board синхронизированы; НЕ трогаем ядро #357, make-detection-fusion, не создаём новые узлы.
- Владельцы: Ozhegov (документ/границы) · Dynin (пороги/тесты) · Kuryokhin (окно/default/loop) · Rodchenko (карточка/live-UI) · Vesnin (LGTM).

---

*Реплик в диалоге: 25; каждый участник высказался не менее одного раза.*
