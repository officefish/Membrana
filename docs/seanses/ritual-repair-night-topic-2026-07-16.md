# Повестка: ночной рефакторинг контура ритуала — что чинить и в каком порядке

> Вопросы размечены ID (**A1**…**D2**) — гейт `rt-6` проверит покрытие.
> Оговорка: `rt-6` даёт ложные срабатывания (#558) — читать его вывод как
> «ID не проставлены», а не «вопросы уронены».

## Что случилось сегодня (факты, проверены командами)

Вечерний дайджест едва не ушёл партнёрам с ложью: «День стратегически провальный, **S2 не стартовал в коде третий день подряд**, дедлайн FREE под угрозой», балл 6.0/10. Владелец остановил отправку вручную.

| Факт | Проверка |
| --- | --- |
| S2 (fusion) слит **13.07** | `5e42c937`, PR #417; `fuseDetectorConfidences` живёт в `@membrana/core` |
| `MAIN_DAY_ISSUE.md` (05:01) **до сих пор несёт ложную развилку** | 1 ложная посылка в `origin/main`; законсервирован ещё и в `docs/archive/daily-day/2026-07-16/` |
| `DAILY_CODE_REVIEW.md` = **15.07 15:54** | вечерний code-review упал транзиентно, проглочен `\|\| true` |
| Дайджест приложил вчерашнее под именем `-16.07.md` | у payload проверка свежести **есть**, у вложения (`attachDocument`) — **нет** |
| `team-evening-feedback-ritual.mjs:27` читает `MAIN_DAY_ISSUE.md` как «канон дня» | он честно пересказал ложь из канона |
| Гейт `main-day-probe` смёржен **09:46**, план сгенерирован **05:01** | гейт построен на 4ч45м позже плана, который должен был поймать |
| `rt-9` (гвард свежести) — `active` | консилиум `rt8-loose-ends` сказал **«чинить сейчас»**; отложен |

**Общий класс:** ритуал читает документы и статусы, но не читает код. Эхо-камера от 06.07 отразилась ещё раз — теперь в оценке дня.

**Память сегодня не заражена** (проверено): `persona-memory-extract` — шаг 4 цепочки, `team-evening-feedback` вообще не в `ritual:evening`. Но см. **A2**.

## Вопросы

**A1 — источник лжи.** `MAIN_DAY_ISSUE.md` (05:01) лжёт и лежит в main + в архиве дня. Завтрашний стендап читает вчерашний MAIN_DAY_ISSUE как канон. Исправлять задним числом (переписать/пометить) или архив — история, которую не трогают? Если пометить — где именно, чтобы пометка не потерялась при чтении downstream?

**A2 — отложенное заражение памяти.** `collectSeansesCandidates` берёт **первую реплику роли** и кладёт `excerpt(first.text)` в журнал персоны. Первая реплика Teamlead в артефакте фидбека — «…план требовал S2, а день ушёл в другую сторону». Пометка `ПОСЫЛКА НАРУШЕНА` стоит **снаружи** реплики (отдельный `###` и блок). Значит завтра `vesnin.md` получит ложную посылку **без** поправки, а память подмешивается в консилиумы по умолчанию (#451). Переносить маркер внутрь реплики до мёржа #568 — или менять экстрактор?

**B1 — `|| true`.** Консилиум `rt8-loose-ends` уже решил: явный список `NON_CRITICAL_STEPS`, всё вне списка падает громко (`rt-9`). Сегодня это выстрелило ровно как предсказано. Поднимать `rt-9` в первую фазу ночи — или сначала A1/A2, потому что они уже в проде?

**B2 — свежесть вложения.** У дайджеста проверка payload есть, у `attachDocument` — нет. Отдельная задача или часть `rt-9`?

**C1 — гейты приходят позже факта.** `main-day-probe` не прогонялся против сегодняшнего плана: его смёржили после. Нужен ли шаг «прогнать гейт против УЖЕ сгенерированного плана» — или гейт должен стоять внутри `ritual:day` до генерации?

**C2 — почему не сработала защита от эха.** Предупреждение о протухании снимка (`fdc05d05`) уходит в промпт, но `MAIN_DAY_ISSUE` 05:01 сгенерирован раньше. Достаточно ли предупреждения в промпте, или снимок обязан ронять генерацию, пока не обновлён (`rt-7`)?

**D1 — форма ночи.** Эпик #539 сейчас `kind: epic`; регламент требует `sprintKind: night-build`, epic-промпт с блоком «Night Build — промпт целиком» и таблицу фаз NB0…NBn. Перерегистрировать #539 под ночь — или завести отдельный ночной эпик под починку ритуала, оставив #539 дневным?

**D2 — границы ночи.** Регламент запрещает ночью: prod-deploy/smoke, архитектурные правки `@membrana/core`/`agenda` (только ветка `vesnin` + LGTM Vesnin), `task:close-github`, `--force` push, triage новых Issue. Что из состава попадает под запрет и должно ждать утра?

## Ограничение

Дедлайн FREE ~17.07 — завтра. Stop rule регламента: два падения CI подряд — стоп, handoff с блокером. Решения с размером и фазой (NB0…NBn). Если тема не готова — сказать прямо.
