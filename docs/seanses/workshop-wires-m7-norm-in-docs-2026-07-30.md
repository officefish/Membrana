<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-30T16:42:15.132Z |
| Команда | `yarn consilium` |
| Модель | xai/grok-4.5 |
| Файл | `docs/seanses/workshop-wires-m7-norm-in-docs-2026-07-30.md` |
| Порядок ролей | Музыкант → Верстальщик → Архитектор → Teamlead → Математик → Структурщик |
| Повестка | `docs/meeting/workshop-wires/M7_AGENDA.md` |

**Вход сеанса** (что комната действительно получила):

| Вход | Носитель | Размер | Отпечаток | Доставка |
|------|----------|-------:|-----------|----------|
| повестка (эхо в голове) | `docs/meeting/workshop-wires/M7_AGENDA.md` | 4031 | `9462a4b62c9a` | полностью |
| инструкция консилиума | `docs/prompts/CONSILIUM_PROMPT.md` | 4494 | `b6bd1a8dc127` | полностью |
| координация ролей | `docs/VIRTUAL_TEAM_PROMPT.md` | 8034 | `75959c7f060d` | **обрезан** |
| контекст: Архитектура | `docs/ARCHITECTURE.md` | 6034 | `33f0886d9bac` | **обрезан** |
| контекст: Дизайн | `docs/DESIGN.md` | 5998 | `c0c614192971` | полностью |
| контекст: Сервисы | `docs/SERVICES.md` | 6034 | `cf5d799f7980` | **обрезан** |
| повестка | `docs/meeting/workshop-wires/M7_AGENDA.md` | 4031 · 1 п. | `9462a4b62c9a` | **обрезан** |

> ⚠ Часть входа не доехала до комнаты целиком. Утверждения протокола о своём входе
> сверять с этой таблицей, а не с текстом реплик.

**Вопрос:**

Куда записывается норма «сначала инструмент мастерской, греп последним» и чем она подпирается, раз хук грепа не видит: вердикт — носитель, дословная формулировка, механизм подпорки с ценой, измеримый признак жизни

---

# Консилиум: носитель и опора нормы «сначала инструмент мастерской, греп — последним»

**Повестка:** M7 / N7 — куда записывается норма «сначала искать инструмент мастерской, греп — последним» и чем она подпирается, если хук грепа не видит нарушение. Вердикт: носитель, дословная формулировка, механизм подпорки с ценой, измеримый признак жизни; отношение к §6; судьба граблей.

**Участники:** Teamlead (Tarasov), Архитектор (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Музыкант → Верстальщик → Архитектор → Teamlead → Математик → Структурщик

---

[Музыкант]: На слух три take подряд (21.07, 29.07, 30.07) — одна и та же фальшь: норма «зови мастерскую» висела в партитуре текстом, а в миксе — разведочный греп и ноль вызовов инструмента. Хук грепа слышит только удар по струне грепа, не тишину мастерской. Значит опора не может быть «гейт в момент»: машина не слышит невызов. Нужен post-bounce meter по ленте сессии, не mute на первом grep.

[Верстальщик]: С витрины агента плакат без индикатора — декор. Если норма живёт только абзацем, через месяц её не отличить от мёртвого текста. Требую в вердикте **читаемый** признак жизни: число, бейдж, строка отчёта — не «верим, что читают AGENTS.md». Пустота вызовов мастерской должна быть видна так же честно, как `participated: none` в sprint-honest.

[Архитектор]: Предмет — где норма живёт и чем держится, не механика выдачи хука и не новый зуб вместо нормы. §6 уже закрепил: пол несёт строку «греп — последний», источник — проекция, `AGENTS.md` snapshot не держит. Конструкция: **два слоя носителя** без dual-write смысла — (1) канон-предписание в фундаментальном документе, который агент читает в холодной сессии; (2) проекция той же политики в пол — уже решена M6. Не плодим третий SoT.

[Teamlead]: Планка матча узкая. Три вещдока — норма без опоры = ноль. Не принимаю «запишем ещё раз и заживёт». Связка: Архитектор — носитель и границы с §6; Структурщик — леммы «грабля / правило / регламент»; Математик — измеримый признак и post-factum предикат; Верстальщик — чтобы признак читался; Музыкант — цена клиппинга процесса. Вердикт без «ничего» в подпорке — иначе BLOCK по факту судьбы норм.

[Математик]: Формализую. Машина в момент t видит вызов инструмента; невызов ¬visible(t). Постфактум по транскрипту S: `viol(S) ⇔ ∃ exploratory_grep(S) ∧ workshop_calls(S)=0` (или слабее: доля сессий с grep-first). Признак жизни за окно W (месяц): `live ⇔ rate(workshop_call | session_with_tooling_need) ≥ θ` либо `count(viol(S))/|S| ≤ ε`. Без θ/ε плакат не отличим от нормы. Гейт-в-момент исключён фактом видимости и ценой раннего зуба (§5).

[Структурщик]: Леммы, иначе синонимы. **Норма-порядок** — предписание последовательности действий (не запрет грепа навсегда). **Грабля** в `AGENTS.md` — рукописная статья «врёт или кусает», не полный устав. **Регламент** — `docs/prompts/*` / skill, процедура на класс задач. **Пол** (§6) — проекция политики в выдачу холодной сессии, не SoT формулировки. Норма поиска инструмента — **не грабля**: она не про «инструмент X врёт», а про порядок поиска. Грабля может *ссылаться* на укус (греп вместо `yarn neighbors`), но home нормы — не таблица граблей.

[Музыкант]: Согласен с леммой: грабля — это take «вот здесь соврало», норма — count-in до любого соло «сначала тюнер мастерской». Если запихнуть норму только в грабли, её сожрут как ещё один bullet протухшего списка 08.07 (11 из 253). Слышу носитель: короткий канон в зоне Agent tooling + эхо в полу через уже решённую проекцию.

[Верстальщик]: Тогда на витрине `AGENTS.md` §Agent tooling сейчас уже есть честная строка «Инвентарь — не здесь: `yarn tooling:overview`» — это согласуется с §6. Норму порядка надо рядом, **одной исполняемой лестницей**, не эссе. Не дублировать инвентарь. Скиллы и `.cursorrules` — вторичные зеркала с отсылкой, иначе разойдутся формулировки как два бейджа на один статус.

[Архитектор]: Варианты носителя с ценой.  
A) Только `AGENTS.md` — цена: не все контуры читают его одинаково; плюс: один канон рядом с «инвентарь не здесь».  
B) Только `.cursorrules` / `.claude/CLAUDE.md` — цена: размножение, drift.  
C) Только `docs/prompts/` — цена: холодному агенту легко не открыть.  
D) Канон в `AGENTS.md` + однострочное зеркало-отсылка в `.cursorrules`/CLAUDE + пол (§6) как delivery — цена: дисциплина «не переписывать текст, только pointer».  
Рекомендую D: SoT формулировки — `AGENTS.md`; остальное — pointer, не вторая редакция.

[Teamlead]: D принимаю как форму. Не размазываем устав по четырём файлам полными абзацами — это проигранный матч drift. Кто пишет зеркала — Структурщик по словарю «одна лемма — один home». Подпорку «ничего» снимаю с стола. Дальше спорим только механизм опоры и θ.

[Математик]: Подпорка-кандидаты: (1) runtime-gate на grep — отвергнут повесткой и §5/видимостью невызова; (2) post-factum счётчик по транскриптам `viol(S)`; (3) ритуал ревью человека без числа; (4) периодический отчёт в seanses/ops. (3) одних вещдоков уже мало — три эпизода пойманы человеком post-factum и не изменили частоту. Минимум: (2) с явным ε и владельцем прогона. Цена (2): скрипт/ритуал разбора ленты, false positive если «греп» был по делу после мастерской — предикат надо сузить.

[Структурщик]: Уточняю предикат леммами. **Разведочный греп** — поиск инструмента/команды/«как сделать X по репо», не греп по делу в коде задачи после `tooling:overview` / `neighbors`. Машинно-грубо: grep-вызовы *до* первого workshop-call в сессии, где цель = discovery tooling. Ложное покрытие §7: «строка есть в полу» ≠ «проведено». Подпорка обязана мерить **вызовы**, не наличие текста в AGENTS.md.

[Музыкант]: Цена счётчика — как компрессор после сведения: не душит take в момент игры, но на утреннем прослушивании слышно, что снова mono-grep. Ритуал: раз в неделю (или на team evening) прогон `viol` по холодным сессиям с tooling-need. Если ε пробит — не «написать норму четвёртый раз», а разбор take и правка грабли-укуса, если конкретный инструмент снова обошли.

[Верстальщик]: Признак жизни на витрине капитана через месяц: одна строка метрики, не роман. Например: `workshop-first sessions: 12/14 · grep-first violations: 1 · ε=0.15`. Фальшь — зелёный «норма в AGENTS» без числа. Empty state: «нет размеченных сессий» честнее, чем «всё ок».

[Архитектор]: Отношение к §6 — **подтвердить**: `AGENTS.md` не держит snapshot инвентаря; несёт указатель на генератор **и** (уточнение M7) каноническую **политику порядка** поиска — это не snapshot, это норма. Пол по-прежнему получает политику «греп — последний» только проекцией, без ручной вставки в текст выдачи. M7 не отменяет M6 и не переписывает выдачу хука.

[Teamlead]: Фиксирую: §6 confirmed + clarify. Грабли: норма ≠ новая вечная строка-устав внутри таблицы граблей. В грабли — только укусы с вещдоком (как 16.07: греп вместо `yarn neighbors`). Связка на DoD: Математик — предикат и ε; Структурщик — место строки в AGENTS и pointer-файлы; без нового гейта в CI «запретить grep».

[Математик]: Предлагаю измеримый контракт. Окно W = 28 дней. Сессия в знаменателе: cold/agent session, где был ≥1 tooling-discovery attempt (grep по scripts/package.json/docs tooling **или** вызов workshop).  
`grep_first(S) ⇔ first_tooling_act(S) ∈ exploratory_grep`.  
`live ⇔ mean(grep_first) ≤ 0.2` (ε=0.2) **и** `mean(workshop_calls≥1 | tooling_need) ≥ 0.5`.  
Двойной порог: одного «мало нарушений» мало, если мастерскую не зовут никогда (нулевая частота §7). Цена: ручная/полуавто разметка borderline-сессий ~N минут на недельный batch.

[Структурщик]: Дословная формулировка для SoT — без толкования, лестница:

1. Нужен инструмент/команда/обзор репо-инструментария → сначала `yarn tooling:overview` (и/или узкий workshop-глагол: `neighbors`, `orphans`, `sets-of`, … по задаче).  
2. Узкий прибор мастерской — до любого разведочного поиска по дереву.  
3. Grep/search по репозиторию для **discovery инструмента** — **последним**, если мастерская не дала ответа.  
4. Grep по коду **задачи** после известного инструмента — не запрещён.

Запрет абсолютный на grep не вводим — только порядок для discovery.

[Музыкант]: Лестница на слух честная: не «grep banned», а «тюнер раньше импровизации по файловой системе». Иначе клиппинг полезной работы — греп по делу в модуле. Поддерживаю формулировку Структурщика.

[Верстальщик]: В `AGENTS.md` это блок из 4 пунктов + ссылка «инвентарь не здесь» — иерархия: сначала порядок, потом pointer на overview, потом грабли-укусы отдельно. В `.cursorrules` / CLAUDE — **одна** строка: «Порядок инструментария — `AGENTS.md` §Agent tooling (workshop first, grep last)». Без копипаста лестницы.

[Архитектор]: Расхождение зеркал = дефект конструкции. Правило синхрона: меняется лестница → только PR в `AGENTS.md`; зеркала трогаем лишь если pointer сломан. Skills `.cursor/skills/` могут цитировать норму в procedural form, но home остаётся AGENTS. Пол — delivery layer (§6/§7), не редакция нормы.

[Teamlead]: Подпорка — post-factum счётчик + недельный ритуал разбора при ε-breach. Цена названа: ложные срабатывания и труд разметки; выигрыш — три эпизода «11 часов / ноль мастерских» больше не невидимы системе. Владелец ритуала в контуре исполнения — Teamlead/секретарь дня, считает Математик-скрипт когда появится; до скрипта — ручной checklist по транскрипту, но предикат тот же. Это не зуб-гейт.

[Математик]: Явно: подпорка ≠ gate. `effect` = `report_finding`, не `reject_op`. Инвариант §5 про ранний зуб не нарушаем. Признак жизни через месяц — выполнение двойного порога на W=28d; если сессий с tooling_need < 5 — статус `insufficient_sample`, не green.

[Структурщик]: Грабли: правило ведения без изменения — «только то, что врёт или кусает». Статья-пример допустима: «разведочный grep вместо `yarn neighbors` → ложный вывод (вещдок 16.07/30.07)». Это **укус**, не home нормы. Третье: норма-порядок в каноне; грабля — экземпляр inciden'а; регламент prompts — optional playbook «как искать инструмент», pointer на канон.

[Музыкант]: Итого на слух: канон в AGENTS как count-in; пол шепчет то же проекцией; раз в неделю meters по ленте; грабли — только шрамы. Без нового эффекторного «запрета grep» в цепи — иначе выключат как ранний зуб.

[Верстальщик]: DoD витрины: после мержа строки капитан открывает AGENTS и видит лестницу без скролла по роману; через 28 дней — одна метрическая строка в отчёте ритуала. Иначе норма снова плакат.

[Архитектор]: Вне скоупа оставляем: состав выдачи хука, предикат принадлежности домам, зуб как CI-gate, заведение домов. Вердикт M7 их не трогает. Форма закрыта: D + post-factum report_finding + ε-пара.

[Teamlead]: Сводка на приёмку. Носитель SoT — `AGENTS.md` §Agent tooling. Формулировка — лестница 1–4 Структурщика. Подпорка — post-factum dual-threshold, не gate. Признак — live на 28d или insufficient_sample. §6 — confirm+clarify. Грабля — не home нормы. Спор исчерпан — на явные «Принимаю».

[Математик]: Принимаю. Предикат, ε=0.2, coverage≥0.5, insufficient_sample <5 — в DoD как числа по умолчанию; сдвиг θ только явно, не «на глаз».

[Структурщик]: Принимаю. Home леммы «норма-порядок» = AGENTS; грабля и пол не синонимы нормы.

[Музыкант]: Принимаю.

[Верстальщик]: Принимаю.

[Архитектор]: Принимаю. Конструкция D и граница с M6 держатся.

[Teamlead]: LGTM по форме вердикта. Матч M7 — норма с опорой и метром, не четвёртый плакат. Исполнение текста в файлах — отдельная задача после ратификации; в этой комнате оснований под код-гейт не даём.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| **Носитель** | **SoT:** `AGENTS.md`, §Agent tooling — каноническая норма-порядок. **Зеркала-pointer (не вторая редакция):** одна строка-отсылка в `.cursorrules` и `.claude/CLAUDE.md`; skills/prompts — procedural cite с pointer. **Delivery:** пол холодной сессии несёт политику «греп — последний» **только проекцией** (§6), не ручной правкой выдачи. |
| **Почему не соседний** | Таблица граблей — про укусы, не устав порядка. Только cursorrules/CLAUDE/prompts — drift и разный coverage чтения. Пол ≠ SoT (§7: «есть в полу» ≠ проведено). |
| **Дословная формулировка (канон)** | **(1)** Нужен инструмент, команда или обзор инструментария репо → сначала `yarn tooling:overview` и/или узкий глагол мастерской по задаче (`neighbors`, `orphans`, `sets-of`, …). **(2)** Узкий прибор мастерской вызывается **до** разведочного поиска по дереву. **(3)** Grep/search по репозиторию для **discovery инструмента** — **последним**, если мастерская не дала ответа. **(4)** Grep по коду текущей задачи при уже известном инструменте **не запрещён**. Абсолютного ban на grep нет. |
| **Подпорка + цена** | **Не gate / не зуб в момент нарушения** (невызов машине не виден; §5 — цена раннего зуба). **Механизм:** post-factum разбор транскриптов, `effect = report_finding`. Предикаты (по умолчанию): сессия с tooling-need; `grep_first(S)`; за W=28d — `mean(grep_first)≤0.2` и `mean(workshop_calls≥1 \| tooling_need)≥0.5`; если таких сессий &lt;5 → `insufficient_sample`, не green. **Ритуал:** недельный (или evening) прогон; при breach — разбор take + грабля-укус при повторе, не перепись нормы. **Цена:** труд разметки borderline, риск FP «греп по делу»; выигрыш — нарушения 29.07/30.07 становятся видимы системе. |
| **Признак жизни (измеримый)** | Через ≥28 дней: двойной порог выше **или** честный `insufficient_sample`. Плакат «строка есть в AGENTS/полу» **не** признак жизни. |
| **Отношение к §6** | **Подтвердить** и **уточнить:** snapshot инвентаря в `AGENTS.md` по-прежнему запрещён; указатель на `yarn tooling:overview` остаётся. M7 добавляет: в том же §Agent tooling живёт **политика порядка** (норма), это не snapshot. Выдачу хука M7 не переписывает; ручная строка политики в текст выдачи запрещена. |
| **Грабли** | Норма поиска — **не грабля**, а **норма-порядок** в каноне. Таблица граблей без смены правила: только «врёт или кусает»; допустима статья-укус со ссылкой на вещдоки (греп вместо `yarn neighbors`), home нормы не переезжает в таблицу. |

**Список посылок:**

1. Три эпизода (21.07, 29.07, 30.07): текст нормы без опоры → ноль вызовов мастерской; пойманы только post-factum человеком.  
2. Вызов инструмента машине виден; **невызов — нет** ⇒ moment-gate по «не позвал мастерскую» невозможен.  
3. Post-factum машинно: транскрипт с разведочным grep ∧ без workshop-calls.  
4. §6: пол = проекция политики; `AGENTS.md` ≠ snapshot; ручная запись в выдачу запрещена.  
5. §7: пол ≠ провод; «есть в полу» = ложное покрытие; нулевая частота вызовов полом не лечится.  
6. §5: ранний зуб выключают; опора нормы ≠ новый абсолютный gate.  
7. Вещдок протухшего снимка 08.07/16.07: рукописный инвентарь и grep вместо `yarn neighbors`.  
8. Грабли ведутся руками по правилу «врёт или кусает» — другой род статьи, чем порядок поиска.  
9. Вне скоупа M7: выдача хука, предикат домов, зуб-гейт, кит-фрейм, заведение домов.  
10. Вердикт не вводит dual-write формулировки нормы в несколько полных копий.

**Definition of Done (если ведёт к задаче в репо):**

1. В `AGENTS.md` §Agent tooling — лестница 1–4 + сохранение «Инвентарь — не здесь: `yarn tooling:overview`»; без вставки snapshot команд.  
2. В `.cursorrules` и `.claude/CLAUDE.md` — pointer-строка на канон, без полной копии лестницы.  
3. Опционально: грабля-укус с вещдоком 16.07/30.07 (не вместо п.1).  
4. Зафиксирован ритуал/чеклист (или черновик скрипта) post-factum: предикаты, ε=0.2, coverage=0.5, W=28d, `insufficient_sample`.  
5. Нет CI-gate «forbid grep» и нет правок состава выдачи хука в рамках M7.  
6. Через 28 дней после появления трафика — отчёт признака жизни (числа или `insufficient_sample`).

---

*Реплик в диалоге: 34; каждый участник высказался не менее одного раза.*

---

## Повестка (полное эхо)

# M7 — норма в фундаментальных документах (заседание `workshop-wires`)

**N7 —** Куда именно записывается норма «сначала искать инструмент мастерской, греп —
последним», и чем она подпирается, если хук грепа не видит? Вердикт — **носитель нормы,
её формулировка, механизм подпорки и признак, по которому видно, что норма живёт**.
**Список посылок обязателен.**

> **ПРЕДМЕТ — НОРМА И ЕЁ ОПОРА, НЕ МЕХАНИКА ВЫДАЧИ.** Восьмая комната ратифицированного
> порядка, ждёт §4 и §6, идёт после §7. Вне скоупа: состав выдачи хука · предикат
> принадлежности · глаголы прибора · зуб · кит как вид фрейма · заведение домов.
>
> Вердикт **не должен** переписывать выдачу хука и **не должен** вводить новый гейт вместо
> нормы — он решает, где норма живёт и чем держится.

**Оснований комнате не предлагаю** — только факты и вопросы.

---

## Вопрос на консилиум (полное эхо)

Куда записывается норма «сначала инструмент мастерской, греп последним» и чем она подпирается, раз хук грепа не видит: вердикт — носитель, дословная формулировка, механизм подпорки с ценой, измеримый признак жизни
