# Аудит M2 «Регистрация в кабинете по промокоду панели» — связь и ключ — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Задание: `docs/meeting/cabinet-registration-promo/AUDIT_M2_BRIEF.md`. Предмет: повестка `M2_AGENDA.md` и протокол `docs/seanses/cabinet-registration-promo-m2-link-key-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `cfecc15b`; 45 реплик, модель `deepseek/deepseek-chat`, `--min-replies 36`). Ствол для сверки: `origin/main` = `3cc3228f`. Протокол и повестка не правились.

## Итог

**ОТКЛОНЕНО** по пунктам **8** и **6**.

- **8.** Два файла вне границ B без слова о границе в вердикте. (1) `packages/background-cabinet/src/app.module.ts` — новый `OfficeRegistrationModule` должен быть подключён в `imports` (стр. 47–62 ствола: все одиннадцать модулей кабинета регистрируются там; `HealthDeepModule` импортирует только `PrismaModule`); ни вердикт, ни готовность не говорят, кто и где подключает модуль. (2) `packages/background-cabinet/src/modules/health-deep/` — вердикт велит расширить его чекером живости (стр. 155, 185), но принадлежность к B названа только в репликах (стр. 76, 78, 128), не в таблице; для `config.module.ts` комната сделала это словами (стр. 147), для `health-deep` — нет. Урок M1 повторён наполовину.
- **6.** По букве задания: англицизм в заголовке строки вердикта — «Маппинг HTTP → класс» (стр. 153); заголовок раздела «Definition of Done» (стр. 178). Формальный провал; содержание строки безупречно.

Остальное — контракт назван по всем частям с числами и типами, все 12 посылок дословны и у заявленных ролей, 26 строк фактуры сходятся со стволом, данности целы, реплик 45 без пустых.

## Таблица

| # | Пункт | Вердикт | Цитата / команда |
|---|---|---|---|
| 1 | Один вопрос | PASS (замечание) | Решения M1/M3/M4 в таблицу не вошли; «Что НЕ строить» (стр. 157) перечисляет дверь, порядок «погасить → создать пользователя» (M3), форму (M4). Утечки в репликах ([Верстальщик] стр. 52 «форма должна различать „код неверный“ и „офис недоступен“ … Это M4») решениями не стали. Замечание к строке «Обязательность» (стр. 145) «флаг регистрации выведен как производный и регистрация выключена» и [Архитектор] стр. 94 «`officeRegistrationEnabled = office !== null`. Отдельный `REGISTRATION_ENABLED` — лишняя сущность»: повестка сама спросила «стартует с выключенной регистрацией — и почему», так что это ответ на Q2, не утечка; но связь производного флага с существующим `ALLOW_REGISTRATION` (`env.schema.ts:37,47`, файл C) вердикт не называет — два условия включения регистрации остаются на M3/C. |
| 2 | Вердикт про контракт | PASS (замечания) | Имена пары — стр. 144; обязательность с причиной — стр. 145 и [Архитектор] стр. 46; файл-схема — стр. 146; способ загрузки без `env.schema.ts` — стр. 147; какой ключ и почему — стр. 148 (страж :18/:22); кто выдаёт и доставляет — стр. 149; отличие от пусковика — стр. 150; модуль и класс — стр. 151; метод с типизированным исходом из четырёх ветвей — стр. 152; HTTP → класс — стр. 153; таймаут числом — стр. 154 «`5000` мс»; живость и где зовётся — стр. 155; зубы — стр. 156; что не строить — стр. 157. «Примерно» нет. Замечания: (а) тип `AppConfig` объявлен в `env.schema.ts:51` (`export type AppConfig = z.infer<…>`) — «`APP_CONFIG` несёт поле `office`» без правки `env.schema.ts` возможно только через новый составной тип в `config.module.ts` или `office-env.schema.ts`, вердикт об этом молчит; (б) «кто выдаёт» — значение рождается в `deploy/generate-office-env.sh:21,29` на VDS офиса, вердикт называет только перенос; (в) тесты названы `*.spec.ts` (стр. 116), прецедент в пакете — `media-bridge.service.test.ts`. |
| 3 | «Список посылок» | PASS (замечание) | Секция есть (стр. 161), 12 посылок, типы у всех, у каждой «реплика [Роль]: «…»». Скрипт: жирное и кавычки нормализованы, точная подстрока по репликам стр. 44–136, роль по маркеру. **12 из 12 дословны и у заявленной роли**: П1/П2 [Структурщик] 44; П3 [Структурщик] 80; П4 [Музыкант] 62; П5 [Математик] 66 (три части через многоточие); П6 [Структурщик] 68; П7 [Архитектор] 70; П8 [Teamlead] 84 (две части); П9 [Музыкант] 86 (две части); П10 [Teamlead] 72; П11 [Teamlead] 60; П12 [Музыкант] 114 (две части). Строк таблицы вместо реплик — ноль. Замечание: П5 «норма (M1, дверь и отказы)» несёт путь двери, тела и пять причин, а цитата [Математика] говорит о ветвях исхода клиента и `mode: 'check'` — цитата дословна, но содержание посылки (данность M1) в ней не звучит; по букве пункта — PASS. |
| 4 | Фактура против ствола | PASS (замечание) | Проверено 26 строк `git show origin/main:<путь> \| sed -n '<строка>p'`. Совпали: `config.module.ts:4` `import { parseEnv } from './env.schema'`, `:11` `useFactory: (): AppConfig => parseEnv(process.env)`; `config.tokens.ts:1`; `env.schema.ts:24` `API_INTERNAL_TOKEN: z.string().min(1, …)`, `:31` `MEDIA_API_URL`, `:35` `MEDIA_API_TOKEN … optional()`, `:44` `MEDIA_API_TOKEN: data.MEDIA_API_TOKEN ?? data.API_INTERNAL_TOKEN`; `media-bridge.service.ts:97` `export class MediaBridgeService`, `:100` `@Inject(APP_CONFIG)`, `:102` `mediaHeaders`, `:110` `mediaBase`, `:207–209` разбор `{ ok, reason }` → `MediaContextRefusedError`; `incident-sentry.ts:75,82` `AbortSignal.timeout(10_000)`; `api-token.guard.ts:18` `req.headers['x-membrana-token']`, `:22` `!== this.config.API_INTERNAL_TOKEN`; офис `env.schema.ts:8,20–21`; `generate-cabinet-env.sh:24` `API_INTERNAL_TOKEN=${api_token}`, `:32` `MEDIA_API_URL=http://media-api:3010`; `generate-office-env.sh:21` `openssl rand -hex 32`, `:29`; `night-hunt-office-trigger.yml:26–27,34`; `_sync-office-env-from-root.mjs:3,19`; `.claude/CLAUDE.md:91` `https://office.mmbrn.tech/health`. Таймаута в мосте нет (`grep -c "AbortSignal\|timeout"` → 0). Замечание: `mediaFetch` начинается на стр. 145, `assertOk` — на 155; ссылки «144–152», «154–159» и «`media-bridge.service.ts:144`» (стр. 102, 114, 174) указывают на строку-закрытие комментария и пустую строку — сдвиг на одну, файл и функции существуют. |
| 5 | Данности, M1 и границы M0 | PASS | `grep -n -i "apps/cabinet\|вторая система\|/v1/panel/register"` → стр. 52 «ключ в браузер не попадает (M0: `apps/cabinet` — браузер)», стр. 112 «`apps/cabinet` — браузер, ключ туда не идёт», стр. 190 «В браузерный бандл `apps/cabinet` ключ офиса не попадает — проверка границы M0» — все в смысле запрета. Дверь M1 не изменена: путь, `mode`, `409 { ok: false, reason }` с пятью причинами воспроизведены как данность (стр. 155, 167); «Отдельный ключ гостя-кабинета не вводим — … расширение стража — дверь M1, закрыта» (стр. 148). Сессии не пересмотрены. |
| 6 | Словарь | **FAIL** (формально) | Заголовки строк вердикта: «Маппинг HTTP → класс» (стр. 153) — англицизм вместо «соответствие HTTP → класс исхода»; заголовок раздела «Definition of Done» (стр. 178) вместо «Определение готовности». По заданию: «в заголовке строки — FAIL». В ячейках: «ретраи/бэкофф» (стр. 157), «мок» (стр. 98), «размеченное объединение», «payload/detail/kind» — последние три имена полей, законны. Остальные заголовки — русские слова повестки: «Имена пары», «Обязательность», «Файл-схема пары», «Способ загрузки», «Какой ключ», «Доставка…», «Клиент», «Таймаут», «Проверка живости», «Зубы B», «Что НЕ строить». |
| 7 | Реплик не меньше тридцати | PASS (замечание) | `grep -cE "^\[(Архитектор\|Верстальщик\|Математик\|Teamlead\|Структурщик\|Музыкант)\]"` → **45**; по ролям: Teamlead 8, Архитектор 8, Структурщик 8, Верстальщик 7, Математик 7, Музыкант 7. Реплик «Принимаю» без содержания — 0: четыре заключительные (стр. 130–136) несут по фразе существа. Условия `--min-replies 36`. Замечание: футер «(по 6 реплик)» (стр. 194) не совпадает со счётом 7–8; вход «координация ролей — обрезан» (стр. 20) — четвёртый прогон подряд. |
| 8 | Исполнимость в границах B | **FAIL** | Файлы, которые вердикт велит создать или править, и их граница: `config/office-env.schema.ts` — новый, «собственный файл-схема» B ✓; `modules/office-registration/` (4 файла + тест) — новый модуль B ✓; `config/config.module.ts` — расширение названо словами: «в M0 не закреплён; по решению комнаты его правит сессия B» (стр. 147) ✓; `deploy/generate-cabinet-env.sh` — B ✓; `env.schema.ts` не трогается ✓ (стр. 147, 181). **Вне границ без слова:** (1) **`packages/background-cabinet/src/app.module.ts`** — модуль надо подключить: `git show origin/main:…/app.module.ts \| sed -n '47,62p'` — `imports: [AppConfigModule, PrismaModule, …, HealthDeepModule]`, все модули кабинета регистрируются здесь; `health-deep.module.ts:9` `imports: [PrismaModule]` — `HealthDeepModule` чужие модули не тянет; `AuthModule` — файл C. Ни вердикт, ни DoD не говорят, где появится `OfficeRegistrationModule`; по заданию «регистрация модуля в `app.module.ts` без слова о границах — FAIL». (2) **`packages/background-cabinet/src/modules/health-deep/`** — «Зовётся из расширенного `health-deep`» (стр. 155), DoD «`…/health-deep/` расширен чекером живости» (стр. 185): расширение модуля названо, расширение **границы** — нет; в репликах есть ([Верстальщик] стр. 76 «Это тоже граница B, если `health-deep` не закреплён за другой сессией»; [Математик] стр. 78 «Считаю расширение `health-deep` границей B»; стр. 128 «фиксирую»), в таблицу не перенесено. Для `config.module.ts` комната сделала ровно то, что требует задание; для `health-deep` и `app.module.ts` — нет. Расширение границ B на эти два файла — решение владельца при ратификации, не аудитора. |

## Что аудитор не делает

Своих решений по контракту и по границам сессии B не предлагает; протокол и повестку не правит; повторный прогон — решение председателя после ратификации владельца.
