<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-27T17:26:12.981Z |
| Команда | `yarn consilium` |
| Модель | xai/grok-4.5 |
| Файл | `docs/seanses/bridge-command-post-m4-improvisation-cases-2026-07-27.md` |
| Порядок ролей | Teamlead → Музыкант → Математик → Верстальщик → Структурщик → Архитектор |
| Повестка | `docs/meeting/bridge-command-post/M4_AGENDA.md` |

**Вопрос:**

V4: восстановление импровизаций — форма кейса с подвалом метаданных и путь кейс → системная инструкция мостика

---

```text
# Консилиум: V4 — форма кейса и путь кейс → системная инструкция мостика

**Повестка:** V4 — восстановление импровизаций: форма кейса (тело + обязательный подвал метаданных), разделение сырья и вывода, критерий «несущий / музейный», путь «кейс → системная инструкция процедуры мостика» и провенанс инструкции к породившему кейсу. Список посылок обязателен. Существо M5/M6 и иерархия контейнеров case-store — вне комнаты.

**Участники:** Teamlead (Tarasov), Музыкант (Kuryokhin), Математик (Dynin), Верстальщик (Rodchenko), Структурщик (Ozhegov), Архитектор (Vesnin).

**Порядок реплик:** Teamlead → Музыкант → Математик → Верстальщик → Структурщик → Архитектор

---

[Teamlead]: Положение: M1–M3 ратифицированы; эта комната — C3 из M0, раньше пульта. Не проектируем дом кейсов и не трогаем M5/M6. Нужны три закрытия: (1) форма кейса = тело + подвал метаданных; (2) критерий «несущий vs музей»; (3) путь кейс → инструкция с провенансом. Связка: Архитектор — форма и границы; Структурщик — словарь полей и шов к homes/инструкции; Математик — предикаты и тотальность меток; Музыкант — слух на «живые» импровизации тракта; Верстальщик — честная витрина подвала без декора. Вердикт без «зависит».

[Музыкант]: На слух кейс — это зафиксированный take, не партитура. Сырьё (скрин, цитата, реплика) — room tone сессии; вывод — «что играть дальше», если take несущий. Четыре кандидата 25.07 слышу сразу: имя Фаррелла; «капитан в диалоге / владелец в механике»; представление попугая; «я детектор, он сирена». Это не ностальгия — это разводка шин, без которой мостик снова сыграет фальшь. Статика кейса — правильно: take не переписывают mid-set.

[Математик]: Формализую объект. Кейс = пара `(body, meta)`, meta — конечный набор меток с закрытыми доменами. Статика: после commit meta и body иммутабельны (append-only история версий — отдельный id, не mute in place). Предикат несущего: `bearing(case) ∈ {bearing, museum, candidate}` — тотальная функция от наблюдаемых полей meta + явного штампа, без «кажется важным». Без тотальности агент не сделает расчёт по «целевым меткам».

[Верстальщик]: С витрины: подвал — не сноска «для разработчиков», а несущая полоса карточки. Оператор и агент видят одни и те же метки: id, дата сессии, статус (candidate|bearing|museum), ссылки на сырьё, вывод одной строкой, provenance→instruction (пусто = ещё не вшит). Честная пустота: нет инструкции — бейдж «museum/candidate», не спрятанный null. Декор иконок «для души» — вне DESIGN; tabular-nums для дат и sha.

[Структурщик]: Словарь до реализации. Леммы: `case` — статическая единица архива импровизации; `body` — narrative + блоки raw/conclusion; `meta` — подвал, публичный контракт идентификации и расчёта; `instruction` — текст/жест в homes процедуры мостика (M2), не новый mandatory-шаг без основания M1–M3. Граница: case-store (контейнер) сегодня не проектируем; проектируем **форму записи** и **шов** `case.id → instruction.ref`. Запрет: инструкция без `derivedFrom: case-id` — дыра провенанса.

[Архитектор]: Конструкция: кейс — гранула памяти комнаты; инструкция — гранула нормы фрейма. Путь однонаправленный: case (статика) → номинация → stamp → patch инструкции в homes/system prompt мостика → обратная ссылка в meta. Обратного «инструкция переписывает кейс» нет. Цена смешать body и meta: агент считает по прозе — невоспроизводимо. Рекомендация: жёсткий подвал с закрытым schema version; body свободнее, но с обязательными секциями Raw / Conclusion.

[Teamlead]: Принимаю однонаправленность и schema version. Дальше режем мясо формы. Требую: без подвала кейс не существует как объект комнаты (отклонение на входе, как trash без имени в leveling). Кто штампует bearing — не «любой голос», а явный гейт: предложение ведущей/секретаря + слово капитана или машинный критерий из meta, если капитан делегировал. Иначе музей расползётся в норму.

[Музыкант]: Про детектор/сирену: в body.raw — цитата/скрин «я детектор, он сирена»; в conclusion — правило разведения: Фаррелл находит один раз, попугай повторяет по порогу, не дублирует hunt. Это несущий: без него side-chain снова сольётся с основной шиной. «Представление попугая» — тоже несущий вводный take. Имя Фаррелла — несущий для состава (C1 уже дал слот; кейс фиксирует *как* имя родилось, чтобы не потерять жест).

[Математик]: Закрытые домены meta (минимум v1): `id` (stable kebab/slug), `schemaVersion`, `sessionDate`, `sourceBridge` (ref сессии), `status ∈ {candidate, bearing, museum}`, `rawRefs[]` (path/url/vechdok id + optional sha256), `conclusionDigest` (короткая строка ≤ N символов), `tags[]` из контролируемого словаря (не free tags врассыпную), `bearingScore` опционально численный 0..1 только если есть явная формула — иначе поле отсутствует, не NaN, `derivedInstructionId` | null, `stampedBy` | null, `stampedAt` | null. Агент считает только по meta; body — для человека и для цитирования.

[Верстальщик]: В карточке две зоны визуально: (1) тело — Raw цитатой/блоком, Conclusion — одним абзацем контрастом; (2) подвал `bg-base-200/60 border-t` — dl-список меток, не paragraph. Статус — badge semantic (warning=candidate, success=bearing, neutral=museum). Ссылка на инструкцию — если null, явный текст «не вшит», не пустая ячейка. Для агента та же структура в JSON-front/companion; markdown-карточка = presentation of the same fields.

[Структурщик]: Raw vs Conclusion — словарно: Raw = неизменённые наблюдаемые (скрин, цитата реплики, id вещдока); Conclusion = интерпретация «какой жест/запрет/разводка следует». Смешение в одном абзаце — `нарушена слабая связанность` смысла: агент не отличит факт от нормы кейса. Теги v1 предлагаю закрытым enum черновиком: `naming`, `role-split`, `signal-route`, `refusal`, `provenance`, `gate-language`, `other` — expansion только PR, не freeform.

[Архитектор]: Путь в пять шагов, без UI пульта: (P1) capture — секретарь/архивариус кладёт case со status=candidate, body+meta; (P2) review — сопоставление с M1–M3: инструкция-кандидат не ломает состав/фреймы/граф; (P3) stamp — status→bearing | museum; (P4) weave — текст инструкции вшивается в systemic instruction homes мостика как *жест/разводка*, не как новый обязательный step без основания; (P5) linkback — meta.derivedInstructionId + в инструкции блок `provenance: case:<id>`. Museum останавливается на P3.

[Teamlead]: P1–P5 принимаю как каркас исполнения. Уточняю ответственность: capture — scribe/секретарь комнаты; review — ведущая готовит brief «вписывается ли в M1–M3»; stamp — капитан (слово) или делегированный предикат, но не молчаливый auto-bearing по красивой цитате; weave — Структурщик/Архитектор формы текста в homes, Teamlead принимает LGTM что не колонизирует M5/M6. Красный: auto-stamp без капитана в v1 — BLOCK.

[Музыкант]: Свежие 26–27.07: «команда отказалась имитировать на пустом входе» — на слух bearing: это anti-clipping процесса, честный silence вместо фальшивого матча. Ozon-receipt field-kit — скорее museum/reference вещдока, пока не рождает жест мостика. Пять вечерних 26.07 — в candidate пачку, не штампуем оптом: каждый take отдельно, иначе side-chain забьёт master.

[Математик]: Критерий stamp без вкуса. Минимальный предикат `mayBecomeBearing(c)`: (1) meta полна по schema; (2) rawRefs непуст или явно `rawRefs: []` + reason `oral-only` с цитатой в body; (3) conclusionDigest непуст; (4) `fitsFrame(c) = true` — ручная/чеклист-проверка против M2 homes (не вычисляю UI); (5) не дублирует already-bearing с тем же conclusionDigest (коллизия → museum или merge-ref, не второй bearing). Stamp = mayBecomeBearing ∧ captainStamp. Музей: ¬(4) или явный выбор «оставить экспонатом».

[Верстальщик]: Для человека в PR/протоколе: шаблон markdown кейса фиксируем как конструкцию, не как «напиши эссе».

```md
# Case: <id>
## Raw
…
## Conclusion
…
## Meta
- status: candidate
- …
```

Подвал всегда в конце; агент парсит Meta как ключ→значение. Без Meta — карточка невалидна (как компонент без aria на критичном контроле).

[Структурщик]: Шов к инструкции: instruction document/segment MUST содержать:
`<!-- provenance: case:<id> sha:<optional> -->` или YAML-поле `derivedFrom: [{caseId, sha256?}]`.
Один case → 0..1 primary instruction segment в v1 (не fan-out на пять мест без ADR). Много кейсов → одна составная секция «Жесты комнаты» со списком provenance. Имя файла инструкции — зона M2 homes; здесь только контракт ссылки. Запрет: вшивать в код apps/* — инструкция живёт в docs/procedure bridge, не в React.

[Архитектор]: Жест свободного тела — норма M0/M4 вводных: bearing-кейс даёт *разрешённое/запрещённое поведение* в существующих steps/gates, не новый mandatory frame. Пример: «капитан vs владелец» — языковая норма гейта, не step. «Детектор/сирена» — разводка ролей C1, не новый фрейм. Если кейс требует нового step — это не weave в C3, а эскалация на ADR/консилиум фреймов; status остаётся candidate до отдельного вердикта.

[Teamlead]: Фиксирую эскалацию: weave только intra-frame. Новый step = другой митинг. Идём по списку кандидатов 25.07 — nominal bearing shortlist, не stamp (stamp — за капитаном), но комната номинирует.

[Музыкант]: Номинация bearing-shortlist 25.07: (1) имя Фаррелла в разговоре — naming + C1; (2) капитан≠владелец — gate-language; (3) детектор/сирена — signal-route; (4) представление попугая — signal-route / role-split. Все четыре — в P1 как candidate с raw из скринов владельца, conclusion по одному жесту. Не номинирую «все 8 скринов» пачкой — только те, где слышен жест.

[Математик]: Идентификаторы: `case-2026-07-25-farrell-naming`, `case-2026-07-25-captain-vs-owner`, `case-2026-07-25-detector-siren`, `case-2026-07-25-parrot-intro` — стабильные, не от display title. schemaVersion: `case-meta/1`. Хэш raw: если вещдок уже в index — ref id+sha; если только скрин вложение — sha256 файла в rawRefs. Детерминизм linkback: instruction patch меняет derivedInstructionId атомарно с merge.

[Верстальщик]: Museum-экспонат тоже виден в реестре (когда дом кейсов появится — не сегодня): dimmed badge, conclusion читаем, кнопки «вшить» нет. Сейчас, без контейнер-иерархии: museum = status в meta + файл в том же плоском контракте формы. Не прячем museum — иначе повторно номинируют тот же take.

[Структурщик]: Отказ имитировать пустой вход (26–27.07) — лемма `refusal`: conclusion = «на пустом входе комната не играет матч; честный stop/пустота». tags: [`refusal`]. mayBecomeBearing привязан к anti-pattern «галлюцинация повестки». Ozon field-kit: tags [`other`] или отдельный domain later; default museum, пока conclusion не формулирует жест *мостика*.

[Архитектор]: Цена переусложнить meta v1: агент не поедет. Минимальный обязательный подвал: id, schemaVersion, sessionDate, status, rawRefs, conclusionDigest, tags, derivedInstructionId, stampedBy, stampedAt, sourceBridge. Остальное (bearingScore, free notes) — optional extension с тем же schemaVersion только через version bump. Body: обязательны заголовки Raw и Conclusion; прочий narrative — optional Context.

[Teamlead]: Сверяю с бестиарием процесса: «ностальгия без id» — зверь; «инструкция без provenance» — зверь; «кейс переписан после stamp» — зверь. Планка: статика после commit; правка = новый case id или explicit supersedes, не edit bearing in place. Вердикт комнаты должен это назвать нормой.

[Музыкант]: Провенанс решений секретарём (бриф M0) — не отдельный кейс-тип, а правило capture: в meta.sourceBridge + optional `capturedBy: secretary`. Имена append-only — уже в id policy Математика. Не плодим типы caseType в v1: один тип, различие — tags + status.

[Математик]: Supersedes: `meta.supersedes: caseId | null`, `meta.supersededBy: caseId | null`. Инвариант: не более одной head-версии в status=bearing для одного logicalSlot (logicalSlot optional string, e.g. `role-split.detector-siren`). Если logicalSlot пуст — коллизии ловятся equality conclusionDigest. Численно проверяемо в линтере кейсов (ops later, не в этой комнате кодить).

[Верстальщик]: В тексте инструкции для человека — сноска-конструктивизм: «Источник: case:<id>». Не прятать в HTML-comment only: comment для машин, видимая строка для капитана в diff. Двойная запись provenance (машина + человек) — как dual labeling на пульте, без третьего декора.

[Структурщик]: Путь P4 weave — конкретный артефакт-цель: сегмент в system instruction мостика / homes (как решит M2), публичный якорь `## Жесты (bearing cases)` со списком:
`- <conclusionDigest> (case:<id>)`.
Секретарь при закрытии дня может номинировать candidate, но не weave без stamp. Слабая связанность: case files не импортируют apps; instruction не импортирует UI.

[Архитектор]: Форма решения итогом: (A) schema case-meta/1 + body Raw/Conclusion; (B) status machine candidate→bearing|museum; (C) path P1–P5; (D) shortlist номинации 25.07 четыре id + refusal 26–27 как candidate; (E) запрет нового mandatory step из кейса. Дом кейсов, пульт, долги попугая — не здесь. Рекомендация: принять, DoD на документ формы + 4 каркаса candidate из скринов, без auto-stamp.

[Teamlead]: Спорим по stamp-делегированию. Математик дал mayBecomeBearing — хорошо как necessary, не sufficient. Sufficient в v1 = слово капитана. Точка. Исключений «агент сам вшил» нет до отдельного ADR.

[Музыкант]: Согласен: иначе сирена начнёт сама писать партитуру. Кейс — take; мастер — капитан.

[Математик]: Уточняю: `bearing ≡ mayBecomeBearing ∧ captainStamp`; museum ≡ stamp museum ∨ (timeout policy later — **не в v1**). Timeout auto-museum не вводим сегодня — нет числа дней в посылках.

[Верстальщик]: Принимаю. На витрине candidate без stamp висит янтарным сколько угодно — честная незавершёнка, не trash.

[Структурщик]: Принимаю. Словарь status закрыт тремя леммами; синонимы draft/final/archived — запрещены в v1.

[Архитектор]: Принимаю. Конструкция замкнута на M1–M3: инструкция — жест в существующей рамке. Список посылок — только входы до комнаты.

[Teamlead]: Номинация shortlist — часть вердикта как *рекомендация к capture*, не как stamp. Капитан штампует отдельно. DoD без «закрыть M5».

[Музыкант]: Ещё раз по сирене: conclusion обязан содержать разведение «find once / repeat by threshold», иначе кейс — anecdote museum.

[Математик]: conclusionDigest ≤ 160 chars; полная conclusion в body без лимита жёсткого, но одна мысль — один case (single responsibility).

[Верстальщик]: Принимаю single thought per case: иначе карточка — простыня, иерархия плывёт.

[Структурщик]: Принимаю. Один case — один primary tag из enum + 0..n secondary из того же enum.

[Архитектор]: Финальная рекомендация без ветвления: **да**, принимаем форму и путь как выше; shortlist номинируем; stamp — за капитаном; контейнеры case-store — после 01.08.

[Teamlead]: Вердикт связки: LGTM на форму+путь+номинацию. BLOCK на любое weave без captainStamp и на колонизацию пульта. Исполнение после протокола: каркас docs формы case-meta/1 и четыре candidate-файла (секретарь), review ведущей против M1–M3, stamp — капитану. Матч = капитан принял shortlist stamp'ами, не зелёный CI шаблона.

[Музыкант]: Принимаю.

[Математик]: Принимаю. Предикаты mayBecomeBearing и status machine — в DoD как спецификация, не код.

[Верстальщик]: Принимаю. Шаблон md — в DoD.

[Структурщик]: Принимаю. Шов provenance двусторонний — в DoD.

[Архитектор]: Принимаю. Граница «жест ≠ новый step» — в DoD и в список норм вердикта.

---

## Список посылок

| # | Посылка | Тип |
|---|---------|-----|
| 1 | Вердикты M1–M3 (состав комнаты, фреймы мостика, контракт мостик↔граф) ратифицированы; инструкция из кейса обязана вставать в их рамку | норма |
| 2 | M0: ребро-порядок C1→C2→C4→C3→C5→C6; C3 (импровизации/кейсы) разбирается до пульта (M5) | норма |
| 3 | Закон брифа: фрейм без исполнителя — процедура не готова; инструкция адресует носителей из C1 и homes из C2 | норма |
| 4 | Слово владельца: у кейса всегда подвал метаданных для идентификации и целевых меток агента; кейсы — статика | норма |
| 5 | Слово владельца: дом кейсов (контейнер-архитектура, токен-экономика) — отдельное заседание после 01.08; V4 только форма и путь | норма |
| 6 | Владелец показал 8 скриншотов первого мостика 25.07 как кандидатов восстановления (имя Фаррелла; капитан vs владелец; попугай; детектор/сирена и др.) | факт |
| 7 | Реестр/кандидаты 26–27.07 (#1298 и связанное): отказ имитировать пустой вход; вечерние кандидаты 26.07; ozon-receipt-3765-field-kit в вещдоках | факт |
| 8 | Наблюдение/норма брифа импровизаций: провенанс решений секретарём; имена append-only; «капитан в диалоге — владелец в механике»; разделение детектор/сирена | норма |
| 9 | Жест из кейса — свободное тело в существующих steps/gates, не новый mandatory-шаг без отдельного основания | норма |
| 10 | Существо M5 (пульт) и M6 (долги/попугай) этой комнатой не проектируется | норма |
| 11 | Капитан — гейт stamp несущего в механике владения; ведущая/секретарь готовят capture и review, не подменяют stamp | норма |

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Форма кейса | **Да:** `body` (обязательно `## Raw`, `## Conclusion`; опционально Context) + обязательный подвал `meta` по schema `case-meta/1`. Без meta объект невалиден. |
| Статика | **Да:** после commit body+meta иммутабельны; изменение = новый id или `supersedes`/`supersededBy`, не mute bearing in place. |
| Обязательный подвал meta v1 | `id`, `schemaVersion`, `sessionDate`, `sourceBridge`, `status`, `rawRefs[]`, `conclusionDigest`, `tags[]`, `derivedInstructionId`, `stampedBy`, `stampedAt`; опционально `logicalSlot`, `supersedes`, `supersededBy`, `capturedBy`, `sha` сырь |
| Raw vs Conclusion | Raw — наблюдаемые (скрин, цитата, vechdok ref+sha); Conclusion — один жест/запрет/разводка; не смешивать в одном абзаце. |
| status | Закрытый enum: `candidate` \| `bearing` \| `museum`. Синонимы draft/final/archived запрещены в v1. |
| tags v1 | Закрытый enum: `naming`, `role-split`, `signal-route`, `refusal`, `provenance`, `gate-language`, `other`; expansion только PR. |
| Критерий несущего | `bearing ≡ mayBecomeBearing(c) ∧ captainStamp`; `mayBecomeBearing` = schema complete ∧ conclusionDigest ∧ (rawRefs∨oral-only) ∧ fits M1–M3 frame check ∧ нет коллизии logicalSlot/digest с другим bearing. Museum — явный stamp или отказ weave; auto-timeout museum **не** вводим в v1. |
| Кто stamp | **Только капитан** (слово). Auto-weave/auto-bearing агентом в v1 — запрет. |
| Путь кейс → инструкция | **P1 capture** (candidate) → **P2 review** (fits M1–M3) → **P3 stamp** (bearing\|museum) → **P4 weave** (жест в homes/system instruction мостика) → **P5 linkback** (`derivedInstructionId` + видимый provenance + машинный маркер). Museum стоп на P3. |
| Тип вшивания | Только *жест/разводка/языковая норма* в существующих фреймах. Новый mandatory step → эскалация ADR/консилиум фреймов, не weave C3. |
| Провенанс | Двусторонний: в instruction — `derivedFrom: case:<id>` (машина + видимая строка человеку); в meta — `derivedInstructionId`. v1: один case → 0..1 primary segment. |
| Номинация shortlist (не stamp) | **Bearing-candidates к capture:** `case-2026-07-25-farrell-naming`, `case-2026-07-25-captain-vs-owner`, `case-2026-07-25-detector-siren`, `case-2026-07-25-parrot-intro`; плюс `refusal` пустого входа 26–27.07 как candidate. Ozon field-kit — default museum, пока нет жеста мостика. Вечерние 26.07 — поштучно candidate, не оптом. |
| Дом кейсов / пульт / M6 | **Вне комнаты** (после 01.08 / M5 / M6). |
| Вердикт | **Принято** единогласно шестью советчиками. |

**Definition of Done (V4 only):**

1. Документ спецификации `case-meta/1` + markdown-шаблон кейса (Raw / Conclusion / Meta) в зоне docs процедуры мостика (без реализации case-store hierarchy).
2. Зафиксированы status machine, tags enum, path P1–P5, запрет weave без captainStamp, запрет нового mandatory step из кейса, двусторонний provenance.
3. Созданы **четыре** (или пять с refusal) файла-каркаса status=candidate по shortlist с заполненным meta-каркасом и черновиком Raw/Conclusion из скринов/цитат владельца — **без** stamped bearing.
4. Review-чеклист «fits M1–M3» одним абзацем-приложением к спецификации (не код).
5. Нет обязательств/ID задач на M5 пульт, M6 долги, контейнер-иерархию case-store, UI-панель кейсов.

---

*Реплик в диалоге: 48; каждый участник высказался не менее одного раза.*
```
