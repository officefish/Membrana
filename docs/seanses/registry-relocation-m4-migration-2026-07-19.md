<!-- канал: llm — протокол произведён yarn consilium, гейт структуры вердикта пройден -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-19T09:56:00.849Z |
| Команда | `yarn consilium` |
| Модель | claude-opus-4-8 |
| Файл | `docs/seanses/registry-relocation-m4-migration-2026-07-19.md` |
| Порядок ролей | Teamlead → Верстальщик → Структурщик → Математик → Музыкант |
| Повестка | `docs/meeting/registry-relocation/M4_AGENDA.md` |

**Вопрос:**

Вопрос H1: как существующий реестр переезжает и как включается инвариант законности. Шесть вещей, все обязательны:

---

# Консилиум: миграция реестра и включение инварианта законности (M4)

**Повестка:** H1 — как существующий реестр переезжает и как включается инвариант законности: критерий «правильно заархивировано», судьба 411 записей без паспорта, форма включения инварианта, защита от фиктивного долга, порядок переноса, обращение с четвёртой категорией.

**Участники:** Teamlead (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант (Kuryokhin), Верстальщик (Rodchenko).

**Порядок реплик:** Teamlead → Верстальщик → Структурщик → Математик → Музыкант (циклически)

---

[Teamlead]: Рамку фиксирую сразу, чтобы H1 не расползся на шесть отдельных заседаний. У нас шесть обязательных выходов, но они образуют одну несущую конструкцию, а не список независимых стен. Корень — критерий «правильно заархивировано»: без него ни судьба 411, ни порядок переноса не определены. По конструктивизму «форма следует функции»: инвариант законности существует, чтобы незакрытое не исчезало, а превращалось в **законный технический долг**. Значит миграция обязана быть проекцией долга в видимое состояние, а не его стиранием. Начинаем с определения, дальше — план с проверяемыми состояниями.

[Верстальщик]: С точки зрения потребителя реестра — а потребитель это агент утром и человек в PR — моя первая боль это **мёртвая дверь**: карточка помечена «заархивировано», а паспорта под ней нет. Дефект `db-h1b`…`db-h4` ровно про это: `active`, `archivedAt: null`, но отчёт в архиве и формат объявлен `closed`. Экран соврал бы трижды. Требую: критерий «правильно заархивировано» обязан быть **видимым в самой карточке** — не выводимым логикой постфактум, а полем, которое читатель видит без раскопок. Иначе долг записи и долг работы сольются на холсте в одну зелёную галочку.

[Структурщик]: Начну с термина, иначе DAG поплывёт. «Заархивировано» — омоним двух лемм, слипшихся под одним словом. Первая: **перенесено в холодный контур** — запись физически в `archive.jsonl`, движение состоялось. Вторая: **законно закрыто** — у записи есть паспорт (GitHub-issue) и `archivedAt`. Дефект хакатона — ровно расщепление этих лемм: контент в архиве (лемма 1), но карточка `active` без `archivedAt` (лемма 2 не выполнена). «Правильно заархивировано» = **обе леммы истинны одновременно**: `record ∈ archive.jsonl ∧ hasPassport(record) ∧ archivedAt ≠ null`. Три предиката, все проверяемые локально.

[Математик]: Формализую, иначе «правильно» останется вкусовым. Определим предикат `wellArchived(r) = inCold(r) ∧ hasPassport(r) ∧ (archivedAt(r) ≠ null)`. Это чистая функция, вход — запись, выход — bool, без сети (снимок-контракт из M2). Но здесь скрыта ловушка ложной точности, о которой предупреждал ресёрч. Из фактуры: 411 без Issue, при этом пилот 20 старейших дал **17 сделано, 3 частично, 0 не сделано**. Значит `hasPassport = false` **не влечёт** `workDone = false`. Если мы механически прогоним `wellArchived` и всё без паспорта объявим незакрытым долгом работы — мы синтезируем ~258 архивных фиктивных незакрытых работ из ничего. Это `precision → 0` на оси «долг работы». Категории долга обязаны быть **ортогональны в предикатах**.

[Музыкант]: По касательной, аудио тут не задет, но ритм я узнаю — это как чистить старую сессию перед архивом проекта. У меня в DAW сотни файлов: часть — готовые мастера без метаданных, часть — недосведённые куски. Если я свалю всё в «недоделано» по признаку «нет тега» — я потеряю готовые мастера в куче мусора. Мой флаг из `code-review-honesty`: индикатор «сделано» должен мерить **содержание файла**, а не наличие ярлыка. Поддерживаю Математика: **отсутствие паспорта — это долг законности, а не долг работы**, и смешивать их — тот самый клиппинг, который экспорт покажет зелёным.

[Teamlead]: Принимаю разделение и фиксирую его как несущую. Долг законности и долг работы — **разные оси, разные владельцы** (из фактуры: законность → владельцу/регламенту, работа → исполнителю). Теперь критерий. `wellArchived(r) = inCold ∧ hasPassport ∧ archivedAt≠null` — принимаю как определение «правильно заархивировано». Но у нас 411 без паспорта, и мы не можем задним числом заставить `hasPassport` стать истиной для всех. Значит нужен **легальный класс для записи без паспорта**, иначе критерий отбрасывает 411 записей в «потеряно». Переходим ко второму выходу.

[Верстальщик]: Со стороны холста — три варианта судьбы 411 (выписка задним числом · рубеж законности с датой · класс «архивная запись без паспорта») должны отличаться **видимо**. Если запись легализована рубежом, в карточке обязано стоять поле `legalizedBy: "cutoff-2026-07-19"` — читатель отличит «был паспорт всегда» от «принят рубежом». Моя норма: легализация без явной пометки — это подделка провенанса на экране. Читатель утром обязан видеть, что запись законна **по решению**, а не по факту наличия issue.

[Структурщик]: По судьбе 411 предложу словарь без синонимов-слоёв. Три пути — это не альтернативы «или-или», а **функция от того, что мы можем узнать про запись**. Выписка задним числом (создать GitHub-issue ретроспективно) дорога и оправдана только для **активных**, которые продолжают жить — 153 активных без Issue. Для **258 архивных без паспорта** выписка бессмысленна: работа уже в истории, issue задним числом — это синоним-слой без функции. Им — **рубеж законности**: класс `archived-pre-passport`, легализованный датой. Термин один, смысл один: «запись зафиксирована до включения инварианта».

[Математик]: Посчитаю цену каждого пути, иначе выбор вкусовой. Выписка задним числом: стоимость `O(1)` человеко-действий на запись × 411 = неприемлемо для архивных, но `precision` провенанса = 1 (настоящий issue). Рубеж: стоимость `O(1)` **на весь класс** (одно решение владельца с датой), провенанс = «легализовано нормой», честно помечен. Класс «без паспорта» без рубежа: стоимость 0, но оставляет `hasPassport = false` без объяснения — это невидимый долг, худший вариант, `наблюдаемость = 0`. Вывод по числам: **153 активных → выписка задним числом** (они дорогие, но живые и малочисленные); **258 архивных → рубеж законности с датой** (дёшево, честно, помечено `legalizedBy`).

[Музыкант]: Поддержу с живой стороны разделение 153 / 258. Активная запись без паспорта — это трек, который ещё в работе без имени файла: назвать обязан, иначе потеряешь при следующем сведении. Архивная без паспорта — это старый релиз до того, как ты завёл систему тегов: переименовывать задним числом каждый — сизифов труд, честнее поставить штамп «до-каталожная эпоха, дата X». Один флаг: рубеж обязан нести **дату переключения инварианта**, иначе через месяц никто не вспомнит, что было «до», а что «после», и класс расползётся.

[Teamlead]: Принимаю. Судьба 411: **153 активных без Issue → выписка задним числом (создать паспорт, легализовать через PR)**; **258 архивных без Issue → рубеж законности с датой, класс `archived-pre-passport`, поле `legalizedBy`**. Цена альтернатив названа: выписка для всех 411 — неподъёмная ручная работа; молчаливый класс без рубежа — невидимый долг. Теперь форма включения инварианта. Из наследства первого заседания: **мягко → миграция → жёстко**, не ломать несущую стену на ходу. Дынин, дай измеримое событие переключения.

[Математик]: Формализую переключатель, иначе «готовность» вкусовая. Три режима гейта законности:
- **Мягко** (warn): `wellArchived = false` → замечание, exit-код 0. Работа не встаёт.
- **Миграция**: применяем выписку к 153, рубеж к 258. Инвариант измеряет `N_illegal = |{r : ¬wellArchived(r) ∧ ¬legalized(r)}|`.
- **Жёстко** (block): `wellArchived = false ∧ ¬legalized` → отказ, exit-код ≠ 0.

Событие переключения мягко→жёстко: **`N_illegal = 0`**. Это проверяемый предикат: все записи либо законны, либо явно легализованы рубежом. Пока `N_illegal > 0` — жёсткий гейт ломал бы несущую стену. Отказ отличается от замечания **кодом возврата** — наследство соблюдено.

[Верстальщик]: На форму включения — потребитель обязан видеть, в каком режиме гейт **сейчас** и сколько до переключения. Мой экран: шапка отчёта миграции несёт `mode: soft|migrating|hard`, счётчик `N_illegal: 41` и дельту за прогон. Иначе переключение мягко→жёстко случится «вдруг» и уронит чей-то PR без предупреждения — мёртвая дверь наоборот, зелёное вчера, красное сегодня без объяснения. Готовность к жёсткому = `N_illegal = 0` **видимая на холсте**, не только в exit-коде.

[Структурщик]: По защите от фиктивного долга — это вопрос **границы предиката**, и он критичен. Долг записи (сделано, карточка открыта) не имеет права приехать в новый стек как долг работы. Механизм: при миграции каждая запись получает **явное поле класса долга** — `debtClass ∈ {record, work, legality, owner-knowledge}` — вычисленное **до** переноса, по сверке с кодом (для record/work) и по наличию паспорта (для legality). Метрика «незакрытых работ» в новом стеке читает **только** `debtClass = work`. Тогда 258 архивных с `debtClass = legality` физически не могут раздуть счётчик работ. Один термин — один смысл, счётчик не врёт.

[Математик]: Уточню предикат `debtClass`, чтобы он был функцией, а не эвристикой. `debtClass(r)`:
- `¬hasPassport ∧ ¬wellArchived` → **legality** (нет паспорта);
- `hasPassport ∧ issueClosed ∧ ¬archivedAt` → **record** (сделано, карточка открыта);
- `hasPassport ∧ ¬issueClosed ∧ ¬workDone` → **work** (не сделано);
- статус неустановим по коду и докам (ручные фазы) → **owner-knowledge**.
Важно: `debtClass` вычисляется на снимке-контракте (M2), детерминирован, без сети. Пилот подтверждает распределение: 17/20 старейших → record, не work. Значит счётчик `work` будет **малым и честным**, а не «сотни фиктивных». Ложная точность обезврежена конструкцией.

[Музыкант]: Поддержу аналогией — это как раскладка сессии по папкам перед архивом: «готово-без-тега», «недоделано», «нет метаданных», «спроси автора что это». Четвёртая папка — `owner-knowledge` — самая опасная, потому что туда сваливается всё непонятное и очередь пухнет. Мой живой флаг из `ritual-inputs-echo`: если очередь к владельцу длинная, он перестаёт отвечать, и класс превращается в свалку `n=много` под видом `n=1`. Очередь к владельцу **обязана быть короткой** — из фактуры это прямо названо. Значит нужен лимит и default-состояние, пока он молчит.

[Teamlead]: Принимаю `debtClass` как несущий механизм защиты от фиктивного долга. Теперь четвёртая категория — `owner-knowledge`. Из запретов: подробный процесс разбора — **отдельный процесс**, здесь только вход и выход. Вход: запись, чей статус неустановим ни по коду, ни по докам. Выход: она **не блокирует** миграцию и **не считается** ни долгом работы, ни законности — она в отдельной очереди с default-состоянием `pending-owner`. Rodchenko, Kuryokhin — как ограничить очередь.

[Верстальщик]: Очередь `pending-owner` на холсте — **короткий список с датой запроса**, не растущая стена. Default-состояние до ответа: запись держится в текущем стеке как `parked`, видимо помечена «ждёт владельца с DD.MM», из счётчиков долга **исключена**. Норма: если запись `parked` дольше N дней — она поднимается в шапку как просроченный вопрос, но всё равно не красит метрику работы. Владелец видит ровно очередь, а не весь реестр — иначе он не ответит никогда.

[Структурщик]: Соглашусь и добавлю границу термина `parked`. Это **не** пятый класс долга — это **состояние ожидания** внутри `owner-knowledge`. Лемма одна: «статус неустановим машинно, ждёт человека». Она не мигрирует в холодный контур (не заархивирована) и не блокирует активную работу (не в жёстком гейте). Ограничение очереди — **норма процесса**: одновременно в `pending-owner` не более, скажем, разумного порога; сверх него — миграция активных продолжается, новые кандидаты в `owner-knowledge` ждут разбора предыдущих. Очередь короткая по конструкции, а не по надежде.

[Математик]: Порядок переноса формализую как топологию состояний, чтобы работа не вставала. Инвариант между шагами: **на каждом шаге система в законном промежуточном состоянии, гейт в режиме, соответствующем шагу**.
1. Мягкий гейт включён (warn) — снимок сделан, `N_illegal` измерен, ничего не блокируется.
2. Классификация: каждой записи присвоен `debtClass` на снимке-контракте.
3. Легализация: 153 активных → выписка; 258 архивных → рубеж с датой; `owner-knowledge` → `parked`.
4. Перенос архивных в холодный контур (единственный производитель — office-батч, из M3), движение не дублируется.
5. Проверка `N_illegal = 0` → переключение мягко→жёстко.
Между шагами 1–4 работа идёт как обычно, гейт мягкий. Жёсткий включается **только** после шага 5.

[Музыкант]: Ритм переноса слышу как правильный — сначала опись, потом сортировка, потом архив, и только в конце запираешь дверь. Один финальный флаг по дефекту `db-h1b`…`db-h4`: эти четыре — живой пример оборванного шага 4 (контент в архиве, карточка `active`). Их нельзя чинить вслепую — «какой шаг сорвался, по репозиторию не восстановить». Значит они кандидаты в `owner-knowledge` / `parked`: спросить владельца, а не домыслить `archivedAt`. Иначе мы задним числом впишем дату, которой не было — подделка мастера, ровно то, против чего я весь сеанс.

[Teamlead]: Принимаю и закрываю. Дефект хакатона → `parked`, вопрос владельцу, не машинное досочинение даты. Все шесть выходов есть: критерий, судьба 411, форма включения, защита от фиктивного долга, порядок, четвёртая категория. Конструкция цельная: критерий `wellArchived` — фундамент; `debtClass` — несущая, что не даёт долгу записи стать долгом работы; рубеж — дверь для 258 архивных; выписка — для 153 живых; `parked` — короткая очередь к владельцу; переключение по `N_illegal = 0`. Стена не ломается на ходу. **LGTM.** Свожу решение.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Критерий «правильно заархивировано» | `wellArchived(r) = inCold(r) ∧ hasPassport(r) ∧ archivedAt(r)≠null` — три проверяемых предиката на снимке-контракте, без сети. Видимы в карточке. |
| Судьба 411 без паспорта | **153 активных** → выписка задним числом (ретро GitHub-issue, легализация через PR). **258 архивных** → рубеж законности с датой, класс `archived-pre-passport`, поле `legalizedBy`. Цена альтернатив: выписка для всех 411 — неподъёмна; молчаливый класс без рубежа — невидимый долг. |
| Форма включения инварианта | **мягко (warn, exit 0) → миграция → жёстко (block, exit≠0)**. Событие переключения: `N_illegal = |{r: ¬wellArchived ∧ ¬legalized}| = 0`. Режим и счётчик видимы в шапке отчёта. |
| Защита от фиктивного долга | Поле `debtClass ∈ {record, work, legality, owner-knowledge}`, вычисляется до переноса. Счётчик «незакрытых работ» читает **только** `debtClass=work`. 258 архивных получают `legality`, а не `work`. |
| Порядок переноса | 1) мягкий гейт + снимок; 2) классификация `debtClass`; 3) легализация (выписка/рубеж/`parked`); 4) перенос архивных office-батчем (M3); 5) `N_illegal=0` → жёсткий. Работа между шагами не встаёт. |
| Четвёртая категория (`owner-knowledge`) | Состояние `parked`: не мигрирует, не блокирует, из счётчиков долга исключена, помечена «ждёт владельца с DD.MM». Очередь короткая по норме процесса. Подробный разбор — отдельный процесс. Дефект `db-h1b…db-h4` → `parked`, вопрос владельцу, без машинного досочинения `archivedAt`. |

**Definition of Done (H1):**
- Реализован чистый предикат `wellArchived(record) → bool` на снимке-контракте, детерминированный, без сети; unit-тесты.
- Реализована чистая функция `debtClass(record) → {record|work|legality|owner-knowledge}`; unit-тесты покрывают все четыре ветки; пилот 20 старейших даёт `record`, не `work`.
- Гейт законности поддерживает три режима (`soft|migrating|hard`) с различием **по коду возврата**; переключение управляется предикатом `N_illegal = 0`.
- Отчёт миграции несёт видимую шапку: `mode`, `N_illegal`, дельта за прогон; класс `archived-pre-passport` с полем `legalizedBy`; список `parked` с датой запроса владельцу.
- Метрика «незакрытых работ» нового стека читает исключительно `debtClass=work` (тест: 258 архивных без паспорта не увеличивают счётчик).
- 153 активных без Issue проведены через выписку задним числом; 258 архивных — через рубеж с датой; перенос архивных в холодный контур — единственным производителем office-батч.

---

## Список посылок

- **факт** — Всего записей 779 (активных 221 · архивных 551 · отложенных 7); замер 19.07.
- **факт** — Без Issue всего 411 из 779: 153 активных + 258 архивных; замер 19.07.
- **факт** — Пилот 20 старейших активных: 17 сделано · 3 частично · 0 не сделано (долга работы в выборке нет).
- **факт** — Архивных без `archivedAt` — 0; замер 19.07.
- **факт** — Дефект `db-h1b`…`db-h4`: `active`, `archivedAt: null`, отчёты в `docs/tasks/archive/`, `HACKATHON_ACTIVE.md` объявляет `closed`; какой шаг сорвался — по репозиторию не восстановить.
- **норма** — Задач без Issue не бывает; задача без Issue незаконна; паспорт — GitHub-issue (решено владельцем).
- **норма** — Незаконно сделанное легализуется через PR, не выбрасывается (решено владельцем).
- **норма** — Цепочка в GitHub нужна против исчезновения: незакрытое обязано превращаться в законный технический долг (решено владельцем).
- **норма** — Отказ отличается от замечания кодом возврата (из первого заседания).
- **норма** — Включение жёсткого гейта на живой практике идёт мягко → миграция → жёстко, не ломать несущую стену на ходу (из первого заседания).
- **норма** — Холодный контур `archive.jsonl`, единственный производитель — office-батч; хранит содержание и доменные поля; движение не дублирует (из M3).
- **норма** — Вход гейтов — снимок-контракт с провенансом, сеть в теле вердикта запрещена (из M2).
- **норма** — Четвёртая категория (`owner-knowledge`) — отдельный процесс разбора; очередь к владельцу обязана быть короткой (решено владельцем / повестка).
- **факт** — `dependsOn` заполнен в 413 записях; замер 19.07.

---

*Реплик в диалоге: 22; каждый участник высказался не менее одного раза.*
