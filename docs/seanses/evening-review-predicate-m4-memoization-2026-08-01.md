<!-- канал: llm — протокол произведён yarn consilium -->

# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-08-01T07:17:39.070Z |
| Команда | `yarn consilium` |
| Модель | anthropic/claude-sonnet-4-6 |
| Файл | `docs/seanses/evening-review-predicate-m4-memoization-2026-08-01.md` |
| Порядок ролей | Teamlead → Архитектор → Структурщик → Математик → Верстальщик → Музыкант |
| Повестка | `docs/meeting/evening-review-predicate/M4-topic.md` |

**Вход сеанса** (что комната действительно получила):

| Вход | Носитель | Размер | Отпечаток | Доставка |
|------|----------|-------:|-----------|----------|
| повестка (эхо в голове) | `docs/meeting/evening-review-predicate/M4-topic.md` | 4994 | `c696f43ebaa5` | полностью |
| инструкция консилиума | `docs/prompts/CONSILIUM_PROMPT.md` | 4494 | `b6bd1a8dc127` | полностью |
| координация ролей | `docs/VIRTUAL_TEAM_PROMPT.md` | 8034 | `75959c7f060d` | **обрезан** |
| повестка | `docs/meeting/evening-review-predicate/M4-topic.md` | 4994 · 1 п. | `c696f43ebaa5` | полностью |

> ⚠ Часть входа не доехала до комнаты целиком. Утверждения протокола о своём входе
> сверять с этой таблицей, а не с текстом реплик.

**Вопрос:**

R1 — Что означает «вечерняя мемоизация в голове вечера»: пересматривается ли требование 3 из #569 (в память идёт только показанное партнёрам), или мемоизация в голове это другие органы, а извлечение памяти персон остаётся на ласточке? СЛОВО ВЛАДЕЛЬЦА 01.08: «сначала нужно производить вечернюю мемоизацию, собирать информацию со всех сессий, архивировать их, собирать информацию о памяти виртуальной команды по дню и генерировать мемо дей». ЧТО СТОИТ НА ПУТИ: persona-memory:extract вынут из цепочки ritual:evening СОЗНАТЕЛЬНО 18.07 — по требованию 3 из #569 в память попадает лишь показанное партнёрам, поэтому извлечение едет на ласточке после sent=true, а цепочка обрывается на черновике и ждать одобрения владельца не умеет; побочно тем же решением чинился ВТОРОЙ порядковый дефект — раньше извлечение шло 4-м шагом, ДО того как team-evening-feedback породит сегодняшний протокол, и сегодняшний фидбек попадал в память лишь на следующие сутки. ТРИ РАЗНЫХ ОРГАНА, слипающиеся в слове «мемоизация», развести поимённо: team-memory-report (шаг 6, читатель поверх журналов персон, git-дифф БЕЗ LLM, exit 3 = вытеснение найдено); day-memo (шаг 12, производный документ дня — факты, инсайты, след); persona-memory:extract (ВНЕ цепочки, на ласточке, связан требованием 3). ФАКТ, МЕНЯЮЩИЙ ВЕС: память команды сейчас только пишется и не читается ни разу — отчёт 31.07 записано 90, вытеснено 99, у КАЖДОЙ персоны строка «всплывало сегодня: контур не поставлен»; усиление извлечения при неработающем вспоминании увеличит давление вытеснения — писать будем больше, помнить меньше. ПОСЫЛКИ ОТ ПРЕДШЕСТВЕННИКОВ, не предмет спора — из M1: единица суждения вечера карточка реестра, область определения множество карточек дня, вход ревью тройка (карточка, коммиты, контракт), форма вердикта boundary/checks/safety; из M2: порядок day-memo(12) → audit-evening(13) → code-review(14) → archive-code-review(15), мемо и хроника становятся critical, падение любого останавливает цепочку и вечер = incomplete; ОГОВОРКА владельца 01.08 — раздел вердикта M2 про судьбу вердикта M4 чужого заседания переквалифицирован в РЕКОМЕНДАЦИЮ, комната права отзывать чужую норму не имеет. ФАКТ из дерева 01.08: живой scripts/audit-evening.mjs потребляет репозиторий, docs/tasks/registry.json и docs/truth/registry.json и НЕ читает DAILY_CODE_REVIEW.md вовсе — тройка построенного прибора не совпадает с тройкой вердикта 18.07. ТРЕБУЕТСЯ: (а) судьба требования 3 из #569 — остаётся, отменяется или сужается до конкретного органа, и если остаётся, что тогда означает мемоизация в голове вечера; (б) поимённый состав головы вечера — какие из трёх органов встают перед day-memo, какие остаются, и что происходит с порядковым дефектом «сегодняшний фидбек попадает в память на следующие сутки»; (в) что делать с тем, что память пишется и не читается — усиливать запись при неработающем чтении или сначала ставить контур вспоминания. Соседние вопросы упоминать можно, вердикт по ним выносить НЕЛЬЗЯ: опора аудита на память сессий — M5, ворота качества документа — M6. Назвать полный список посылок в этом же прогоне; посылки из вердиктов M1 и M2 помечать источником «вердикт предшественника M1» / «M2».

---

# Консилиум: мемоизация в голове вечера против требования 3 из #569

**Повестка:** M4 — судьба требования 3 из #569, поимённый состав головы вечера, порядковый дефект сегодняшнего фидбека, условие «память пишется и не читается».

**Участники:** Teamlead (Тарасов), Архитектор (Веснин), Структурщик (Ожегов), Математик (Дынин), Верстальщик (Родченко), Музыкант (Курёхин).

**Порядок реплик:** Teamlead → Архитектор → Структурщик → Математик → Верстальщик → Музыкант (циклически, ≥30 реплик)

---

[Teamlead]: Матч узкий. Три органа слипаются в одном слове — наша задача их развести и сказать, что именно владелец потребовал вернуть в голову вечера, а что уже там стоит или стоять не должно. Требование 3 из #569 — не абстрактная норма, это конкретное архитектурное решение с причиной. Прежде чем отменять, надо назвать, что именно отменяем и чем расплачиваемся.

[Архитектор]: Конструкция сейчас такая: `team-memory-report` стоит на шаге 6 — он читает журналы, не пишет, LLM не задействован, первоисточник не трогает. `day-memo` стоит на шаге 12 и потребляет уже собранное. `persona-memory:extract` стоит вне цепочки, на ласточке. Слово владельца 01.08 называет три действия: «собирать информацию со всех сессий, архивировать их, собирать информацию о памяти виртуальной команды по дню и генерировать мемо дей». Первые два действия — это не `persona-memory:extract`. Это архивирование сессий и отчёт по памяти команды. Они уже в цепочке или их там нет — это надо проверить отдельно от судьбы требования 3.

[Структурщик]: Разведу термины, чтобы не ехать на синонимах. «Мемоизация в голове вечера» — это не тип операции, это временно́й слот: то, что происходит ДО `day-memo`. Орган `team-memory-report` (шаг 6) — читатель, не писатель; он не мемоизирует, он отчитывается о состоянии памяти. Орган `persona-memory:extract` — единственный, кто пишет в журналы персон. Архивирование сессий — отдельная операция, которая в таблице трёх органов вообще не названа. Слово владельца говорит об архивировании сессий как об отдельном действии — это не `persona-memory:extract` и не `team-memory-report`.

[Математик]: Формализую предикат конфликта. Требование 3 из #569 задаёт инвариант: `admit(e) ⇔ shown_to_partners(content(e))`. Слово владельца 01.08 задаёт другой предикат для головы вечера: `in_head(e) ⇔ occurred_today(e)`. Эти два предиката несовместимы ровно для одного органа — `persona-memory:extract`, который пишет в журналы персон. Для `team-memory-report` конфликта нет: он читает, не пишет, инвариант `admit` к нему неприменим. Для `day-memo` конфликта нет: он производный документ, не журнал персон. Значит, требование 3 надо проверять только как ограничение на `persona-memory:extract`, а не на всю «мемоизацию» скопом.

[Верстальщик]: С витрины агента ситуация выглядит так: `team-memory-report` стоит на шаге 6 и уже виден в цепочке — его не надо «возвращать». `day-memo` стоит на шаге 12 — тоже уже в цепочке. Единственный орган, который НЕ в цепочке и который имеет отношение к слову владельца — это `persona-memory:extract`. Но слово владельца буквально говорит «архивировать сессии» — этого органа в таблице нет вообще. Прежде чем двигать `extract`, надо ответить: архивирование сессий — это уже покрытый шаг или пробел?

[Музыкант]: На слух слово владельца — это четыре такта, не один. «Собирать информацию со всех сессий» — это read-операция над сессиями дня. «Архивировать их» — это write-операция: сессии уходят в архив. «Собирать информацию о памяти виртуальной команды по дню» — это то, что делает `team-memory-report`. «Генерировать мемо дей» — это `day-memo`. Из четырёх тактов три уже в цепочке или уже существуют. Четвёртый — архивирование сессий — звучит как отсутствующий шаг, не как перенос `persona-memory:extract`.

[Teamlead]: Хорошо. Тогда уточняю постановку: слово владельца 01.08 описывает «голову вечера» как четыре операции. Три из них покрыты существующими органами. Четвёртая — архивирование сессий — не покрыта. Вопрос о `persona-memory:extract` остаётся отдельным: его возврат в цепочку прямо отменяет оба следствия решения 18.07 — и порядковый дефект чинился именно им. Надо решить: требование 3 остаётся в силе или нет, и если остаётся — `persona-memory:extract` на ласточке, а архивирование сессий встаёт в голову отдельным шагом.

[Архитектор]: Требование 3 из #569 решало реальную задачу: не пускать в долгосрочную память содержимое, которое ещё не прошло через владельца. Это не произвол — это контроль качества входа в журнал персоны. Если мы его отменяем, журналы персон начинают принимать всё подряд, включая черновики, которые владелец отклонил. Цена отмены — потеря инварианта качества. Цена сохранения — порядковый дефект остаётся: сегодняшний фидбек в память попадёт завтра. Это известная цена, и она была принята сознательно 18.07. Предлагаю: требование 3 сужается, а не отменяется — оно остаётся как ограничение на `persona-memory:extract`, но не распространяется на архивирование сессий.

[Структурщик]: Поддерживаю сужение. Лемма: «мемоизация в голове вечера» = архивирование сессий + `team-memory-report` + `day-memo`. Все три не пишут в журналы персон. Требование 3 из #569 регулирует только то, что пишет в журналы персон — то есть только `persona-memory:extract`. Следовательно, требование 3 не конфликтует со словом владельца 01.08, если мы правильно определяем область его действия. Слово владельца требует поставить архивирование сессий в голову вечера — это новый шаг, не отмена старого правила.

[Математик]: Порядковый дефект формализую. Пусть `T_feedback` — момент порождения протокола `team-evening-feedback`, `T_extract` — момент выполнения `persona-memory:extract`. Дефект: `T_extract < T_feedback` при расположении extract на шаге 4. Решение 18.07 устранило дефект переносом на ласточку: `T_extract > T_sent`, где `T_sent > T_feedback`. Если требование 3 сохраняется только для `persona-memory:extract` — порядковый дефект не возникает заново: extract по-прежнему на ласточке. Новый шаг архивирования сессий не несёт этого дефекта — сессии дня завершены к вечеру, их содержимое не изменится после отправки фидбека.

[Верстальщик]: Картина становится чёткой. В голове вечера стоят: (1) сбор и архивирование сессий — новый шаг, которого сейчас нет, встаёт перед `team-memory-report`; (2) `team-memory-report` — уже на шаге 6, остаётся; (3) `day-memo` — уже на шаге 12, остаётся. `persona-memory:extract` остаётся на ласточке. Требование 3 сужается: оно действует только на `persona-memory:extract`, не на архивирование сессий.

[Музыкант]: Добавлю про порядковый дефект в свете нового шага. Если архивирование сессий встаёт ДО `day-memo`, то `day-memo` потребляет уже заархивированные сессии — это корректно. `team-memory-report` на шаге 6 читает журналы персон, которые на этот момент содержат данные до ласточки — это тоже корректно, потому что `extract` ещё не отработал. Цепочка не ломается.

[Teamlead]: Теперь к третьему вопросу — память пишется и не читается. Отчёт 31.07: записано 90, вытеснено 99, «всплывало сегодня: контур не поставлен» у каждой персоны. Это диагноз: система записи работает, система чтения не построена. Вопрос не в том, усиливать ли запись — а в том, разумно ли усиливать то, чего никто не читает. Моя позиция: сначала контур вспоминания, потом усиление записи.

[Архитектор]: Здесь конструктивная проблема. Запись без чтения — это склад без выдачи. `persona-memory:extract` — механизм склада. Нет ни одного места в цепочке, где журнал персоны читается как вход. `team-memory-report` читает журналы через git-дифф без LLM — это статистика по складу, не выдача. Контур вспоминания — это отдельный орган, которого нет. Он не является предметом этой комнаты (M5 — опора аудита на память сессий), но как условие он меняет рекомендацию: усиливать запись сейчас означает увеличивать давление вытеснения при том, что выход со склада не построен.

[Структурщик]: Формулирую лемму для вердикта. «Контур вспоминания» — именованная операция чтения из журналов персон с передачей контента в активный контекст. Сейчас такой операции нет ни в одном шаге цепочки. Её отсутствие — не вопрос этой комнаты, но её отсутствие является условием, при котором любое усиление записи не даёт ценности. Вердикт этой комнаты должен зафиксировать: архивирование сессий встаёт в голову вечера, запись усиливается только до уровня, который не увеличивает вытеснение сверх существующего базиса.

[Математик]: Числа: записано 90, вытеснено 99. Это означает, что на 31.07 бюджет памяти уже переполнен — вытеснений больше, чем новых записей. Добавление нового шага (архивирование сессий) создаёт новые записи, если его результаты пишутся в журналы. Но архивирование сессий — не запись в журналы персон, это запись сессий в архив. Тип хранилища другой. Следовательно, новый шаг не увеличивает давление вытеснения в журналах персон — он пишет в другое хранилище.

[Верстальщик]: Уточнение для вердикта. `persona-memory:extract` — единственный орган, который пишет в журналы персон. Архивирование сессий пишет в архив сессий — другое хранилище. `day-memo` пишет производный документ — тоже другое хранилище. Усиление давления вытеснения возможно только при расширении частоты или объёма вызовов `persona-memory:extract`. Если он остаётся на ласточке, давление вытеснения остаётся тем же. Новый шаг архивирования сессий не меняет давление вытеснения в журналах персон.

[Музыкант]: Картина звучит так. Две системы записи: журналы персон (один канал, управляется `extract` на ласточке, бюджет 14 400 единиц, уже у порога) и архив сессий (другой канал, сейчас не заполняется в голове вечера). Слово владельца говорит: «архивировать сессии» — это про второй канал. Контур вспоминания нужен для первого канала. Они не конкурируют за одно хранилище.

[Teamlead]: Хорошо, картина собирается. Промежуточный итог: требование 3 из #569 сужается — оно действует только на `persona-memory:extract` и только как ограничение на вход в журналы персон. Архивирование сессий — отдельный новый шаг, который встаёт в голову вечера перед `team-memory-report`. `persona-memory:extract` остаётся на ласточке — порядковый дефект не возвращается. По чтению памяти: эта комната фиксирует факт и выносит рекомендацию не усиливать запись в журналы персон до построения контура вспоминания — но контур вспоминания как таковой не является предметом этой комнаты.

[Архитектор]: Хочу зафиксировать границу. Слово владельца 01.08 — это требование к ритуалу вечера. Оно не отменяет требование 3 из #569 явно. Оно описывает желаемое поведение. Наша интерпретация — что слово владельца покрывается тремя существующими органами плюс одним новым (архивирование сессий) — является разрешением конфликта без отмены нормы. Если владелец имел в виду именно перенос `persona-memory:extract` в тело вечера — это потребует явного отзыва требования 3 самим владельцем, не этой комнатой.

[Структурщик]: Поддерживаю. Лемма о полномочиях: эта комната вправе интерпретировать слово владельца. Она не вправе отменять требование 3 из #569 — это Issue-норма, не вердикт заседания. Она вправе сузить область действия нормы до конкретного органа, потому что сужение не противоречит букве нормы — «в память идёт только показанное партнёрам» остаётся истиной для `persona-memory:extract`.

[Математик]: Формализую решение по порядковому дефекту. Пусть шаги в голове вечера — это множество S = {s_archive_sessions, s6_team_memory_report, ..., s12_day_memo}. Новый шаг `s_archive_sessions` вставляется до s6. `persona-memory:extract` не входит в S. Предикат «сегодняшний фидбек попадает в память сегодня» для `persona-memory:extract`: `T_extract > T_sent > T_feedback`. Инвариант выполнен: дефект не возвращается.

[Верстальщик]: Финальная картина головы вечера с витрины: шаг `archive-sessions` (новый, сбор и архивирование сессий дня) → шаг 6 `team-memory-report` (читает журналы, git-дифф, отчёт) → ... → шаг 12 `day-memo` (производный документ дня). Вне цепочки — `persona-memory:extract` на ласточке после `sent=true`. Три органа из таблицы: `team-memory-report` — в цепочке, остаётся; `day-memo` — в цепочке, остаётся; `persona-memory:extract` — вне цепочки, остаётся вне. Требование 3 — действует только на `persona-memory:extract`.

[Музыкант]: Один оставшийся вопрос: что именно делает шаг `archive-sessions`? Слово владельца говорит «собирать информацию со всех сессий, архивировать их». Это read + write: читаем сессионные файлы дня, пишем в архив сессий. Это не то же самое, что `team-memory-report` (который читает журналы персон через git-дифф). Это другой первоисточник — сессионные файлы. Если шаг уже существует под другим именем — надо проверить. Если не существует — новый шаг, владелец: Ангелина или Математик как ops.

[Teamlead]: Для вердикта: `archive-sessions` — новый шаг, его существование или отсутствие в текущей цепочке выходит за рамки этой комнаты с точки зрения реализации, но вердикт должен зафиксировать его позицию: перед `team-memory-report`, в голове вечера. Definition of Done по этому пункту — отдельная задача реализации, не предмет этого заседания.

[Архитектор]: Подтверждаю полноту вердикта. Три вопроса закрыты: (а) требование 3 сужается до `persona-memory:extract`, не отменяется; (б) голова вечера = `archive-sessions` + `team-memory-report` + `day-memo`, extract вне цепочки, порядковый дефект не возвращается; (в) усиление записи в журналы персон не производится до построения контура вспоминания — факт зафиксирован, вопрос контура вспоминания выходит за рамки этой комнаты. Список посылок — в обязательной секции.

[Структурщик]: Добавлю уточнение по формулировке вердикта (а). «Сужение требования 3 до конкретного органа» означает следующее: норма «в память идёт только показанное партнёрам» применяется исключительно к операции записи в журналы персон, то есть к `persona-memory:extract`. Архивирование сессий, `team-memory-report` и `day-memo` не подпадают под норму, потому что они не пишут в журналы персон. Сужение логически согласовано с буквой нормы.

[Математик]: Финальный предикат вердикта (в). Пусть `W(t)` — объём записей в журналы персон за день t, `V(t)` — объём вытесненных записей. На 31.07: W = 90, V = 99. Усиление записи при постоянном бюджете увеличивает V пропорционально. Условие торможения: не добавлять новых источников записи в журналы персон до построения операции чтения с передачей в активный контекст. Новый шаг `archive-sessions` пишет в другое хранилище — условие не нарушается.

[Верстальщик]: Принимаю. Все три вопроса закрыты, витрина корректна. Бейдж «требование 3 отменено» не ставится — ставится бейдж «требование 3 сужено до органа `persona-memory:extract`».

[Музыкант]: Принимаю. Четыре такта слова владельца покрыты: два существующих органа остаются, один новый встаёт в голову, один остаётся на ласточке. Микс не врёт.

[Teamlead]: Принимаю. Вердикт сформирован. Передаю в секции ниже.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| **(а) Судьба требования 3 из #569** | **Сужается** до конкретного органа: норма «в память идёт только показанное партнёрам» действует исключительно на `persona-memory:extract` — единственный орган, записывающий в журналы персон. Требование 3 не отменяется и не пересматривается. |
| **(а) Что означает «мемоизация в голове вечера»** | Три операции: (1) сбор и архивирование сессионных файлов дня (`archive-sessions`, новый шаг); (2) `team-memory-report` (уже на шаге 6, читатель, не писатель); (3) `day-memo` (уже на шаге 12, производный документ). Ни одна из трёх не пишет в журналы персон — требование 3 к ним не применяется. |
| **(б) Поимённый состав головы вечера** | `archive-sessions` (новый, встаёт перед шагом 6) → `team-memory-report` (шаг 6, остаётся) → … → `day-memo` (шаг 12, остаётся). `persona-memory:extract` остаётся **вне цепочки**, на ласточке после `sent=true`. |
| **(б) Порядковый дефект «сегодняшний фидбек в память на следующие сутки»** | **Не возвращается.** `persona-memory:extract` остаётся на ласточке: `T_extract > T_sent > T_feedback`. Инвариант решения 18.07 сохранён. |
| **(в) Память пишется и не читается** | Усиление записи в журналы персон **не производится** до построения контура вспоминания (операции чтения из журналов персон с передачей в активный контекст). Факт зафиксирован: W=90, V=99 на 31.07, строка «всплывало сегодня: контур не поставлен» у каждой персоны. Новый шаг `archive-sessions` пишет в архив сессий — другое хранилище — давление вытеснения в журналах персон не увеличивает. Вопрос построения контура вспоминания выходит за рамки этой комнаты. |

---

## Список посылок

| # | Посылка | Тип | Источник |
|---|---------|-----|---------|
| 1 | Единица суждения вечера — карточка реестра, активная за день; область определения предиката вечера — множество карточек дня; вход ревью — тройка `(карточка, коммиты, контракт)`; форма вердикта — `boundary` / `checks` / `safety` | норма | вердикт предшественника M1 |
| 2 | Порядок в исполняемом источнике: `day-memo` (12) → `audit-evening` (13) → `code-review` (14) → `archive-code-review` (15); мемо и хроника — `critical`; падение любого останавливает цепочку, вечер = `incomplete` | норма | вердикт предшественника M2 |
| 3 | Раздел вердикта M2 «Судьба вердикта M4 заседания `meeting-evening-auditor`» переквалифицирован владельцем в рекомендацию; комната M2 права отзывать норму чужого заседания не имеет | норма | оговорка владельца 01.08, зафиксирована в посылках повестки |
| 4 | `persona-memory:extract` вынут из цепочки `ritual:evening` сознательно 18.07: причина — требование 3 из #569 («в память идёт только показанное партнёрам»), extract едет на ласточке после `sent=true` | факт | `.claude/CLAUDE.md`, решение 18.07 |
| 5 | Тем же решением 18.07 устранён порядковый дефект: до него extract шёл шагом 4, до порождения `team-evening-feedback`, и сегодняшний фидбек попадал в память лишь на следующие сутки | факт | `.claude/CLAUDE.md`, решение 18.07 |
| 6 | Слово владельца 01.08: «сначала нужно производить вечернюю мемоизацию, собирать информацию со всех сессий, архивировать их, собирать информацию о памяти виртуальной команды по дню и генерировать мемо дей» | норма | слово владельца 01.08 |
| 7 | `team-memory-report` — читатель поверх журналов персон, git-дифф без LLM, первоисточник не трогает, exit 3 = вытеснение найдено; стоит на шаге 6 | факт | повестка M4, таблица органов |
| 8 | `day-memo` — производный документ дня (факты, инсайты, след), потребляет вещдоки, мостик, память команды, сеансы; стоит на шаге 12 | факт | повестка M4, таблица органов |
| 9 | `persona-memory:extract` — единственный орган, пишущий в журналы персон; стоит вне цепочки, на ласточке | факт | повестка M4, таблица органов |
| 10 | На 31.07: записано 90, вытеснено 99; у каждой персоны строка «всплывало сегодня: контур не поставлен» | факт | отчёт 31.07, повестка M4 |
| 11 | Живой `scripts/audit-evening.mjs` потребляет репозиторий, `docs/tasks/registry.json` и `docs/truth/registry.json` — `DAILY_CODE_REVIEW.md` не читает; тройка прибора не совпадает с тройкой вердикта 18.07 | факт | проверка дерева 01.08, зафиксирована в посылках повестки |

---

## Definition of Done

1. В `.claude/CLAUDE.md` зафиксировано: требование 3 из #569 сужено — область действия ограничена органом `persona-memory:extract`; архивирование сессий, `team-memory-report` и `day-memo` под норму не подпадают.
2. В цепочку `ritual:evening` добавлен шаг `archive-sessions` перед шагом 6 (`team-memory-report`): сбор и запись сессионных файлов дня в архив сессий.
3. `persona-memory:extract` остаётся вне цепочки на ласточке — изменений в его позиции нет.
4. Зафиксировано в документации ритуала: до построения операции чтения из журналов персон с передачей в активный контекст (контур вспоминания) новые источники записи в журналы персон не добавляются.
5. Протокол содержит секцию «Список посылок» с разметкой факт/норма и источниками M1/M2 — машинный гейт проходит.

---

*Реплик в диалоге: 31; каждый участник высказался не менее пяти раз.*
