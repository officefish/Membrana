# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-18T03:51:16.455Z |
| Команда | `yarn consilium` |
| Модель | claude-opus-4-8 |
| Файл | `docs/seanses/night-build-format-m0-order-run2-clean-2026-07-18.md` |
| Порядок ролей | Teamlead → Музыкант → Структурщик → Математик → Верстальщик |
| Повестка | `docs/meeting/night-build-format/M0-topic.md` |

**Вопрос:**

O1 — порядок зависимостей (DAG) РОВНО пяти доработок формата night-build: (1) edit-class taxonomy тест-гейта, (2) гранулярность кванта + каденс чекпоинта, (3) переформулировка guardrails (hard scoped+worktree первичны, мёрж-утром вторичен), (4) night:open ветка от origin/main в параллельном режиме, (5) night:close авто-handoff из коммитов ветки. ОГРАДА: не импортировать кандидаты заседания scripts-boundary (версионирование схемы, ретрай, реестр процессов, журнал прогонов) — это другой контур. Единственный вердикт — ПОРЯДОК этих пяти. Полный список посылок. Вход — topic-file.

---

# Консилиум: DAG порядка пяти доработок формата night-build (M0)

**Повестка:** O1 — определить порядок зависимостей ровно пяти доработок формата night-build: (1) edit-class taxonomy тест-гейта, (2) гранулярность кванта + каденс чекпоинта, (3) переформулировка guardrails (hard scoped+worktree первичны), (4) night:open ветка от origin/main в параллельном режиме, (5) night:close авто-handoff из коммитов ветки. Вынести единственный вердикт: корень-фундамент, что параллельно, что без консилиума, порядок M1..Mn.

**Участники:** Teamlead (Vesnin), Структурщик (Ozhegov), Математик (Dynin), Музыкант, Верстальщик (Rodchenko).

**Порядок реплик:** Teamlead → Музыкант → Структурщик → Математик → Верстальщик (циклически)

---

[Teamlead]: Открываю M0. Ограда из topic-file обязательна: кандидатов ровно пять, номера 1–5. Кандидаты соседнего заседания scripts-boundary (версионирование схемы, ретрай, реестр процессов, гейт свежести, журнал прогонов) — другой контур, в этот DAG не входят. Мой первый тезис: фундамент — кандидат 3 (guardrails). Пока не зафиксировано, что hard-профиль (scoped + изоляция worktree + ветка от origin/main) первичен, а «мёрж утром» вторичен, остальные четыре правки висят в воздухе. Research Q3 прямо говорит: процесс без hard-изоляции — театр.

[Музыкант]: Согласен по духу, но добавлю слух практика. Кандидат 4 (`night:open` советует ветку от origin/main в параллельном режиме) — это не отдельная симфония, это одна нота внутри guardrails. Эпизод 17.07 (локальный main залочен) — частный случай того же самого hard-требования «ветка origin/main». Я бы 4 не разводил с 3 концептуально, но как тулинг-фикс он тривиален и играется сразу.

[Структурщик]: Уточню термины, иначе DAG поедет. «Guardrails» (3) — это **норма** (регламент, определение hard vs soft). «night:open ветка» (4) и «night:close handoff» (5) — это **реализация** нормы в тулинге. Норма — лемма, тулинг — ссылка на статью. Значит ребро направленное: 3 → 4 и 3 → 5. Реализация не может опережать определение, иначе получим два синонима-слоя без единого источника истины.

[Математик]: Посчитаю связность как граф, а не «на глаз». Пять узлов. Ребро X→Y ставлю только если Y численно/логически невыводим без X. 3→4: да (ветка origin/main — пункт hard-профиля). 3→5: да (handoff имеет смысл только когда ветка изолирована). 3→1: слабое — taxonomy тест-гейта независима от изоляции worktree. 3→2: нет. 1↔2: нет ребра, ортогональны. Вывод: 3 — корень с out-degree 2 (к 4 и 5), а 1 и 2 — изолированные компоненты графа.

[Верстальщик]: С точки зрения того, кто читает handoff утром: кандидат 5 (`night:close` авто-handoff из коммитов ветки) — это выходной артефакт, его форма зависит от того, что ветка вообще есть и изолирована. То есть визуально/структурно 5 — лист DAG, самый последний. Merge-handoff без гарантии ветки — это отчёт без данных, честности нет. Поддерживаю 3 → 5 как терминальное ребро.

[Teamlead]: Фиксирую консенсус по корню: **3 — фундамент**. Теперь спор о 1 и 2. Математик назвал их изолированными компонентами. Это значит их можно вести параллельно и они не блокируют цепочку 3→4→5. Но у меня вопрос: нужны ли им отдельные заседания M1..Mn или это регламентные правки под LGTM?

[Музыкант]: Кандидат 2 (гранулярность кванта + каденс чекпоинта, различить вес NB-фаз) — вот тут я слышу диссонанс, требующий обсуждения. «45–60 мин или логический блок», «различить вес фаз» — это про ритм, а ритм субъективен и спорен. Это не тривиальный фикс. За 2 надо созывать заседание. А кандидат 1 (edit-class taxonomy) — почти механика: класс правки → тип гейта. Меньше спора.

[Структурщик]: Поддержу разделение. Кандидат 1 — это словарь соответствий (код→юнит, конфиг→линтер, инфра→plan, интеграция→sandbox), однозначная таблица. Такой словарь можно принять регламентно, но само определение классов правки лучше зафиксировать в одном заседании, чтобы «edit-class» не расползся синонимами по скриптам. Кандидат 4 — единственный чисто тулинговый и тривиальный: topic-file сам предполагает его «вероятно без консилиума».

[Математик]: Верно про 4. Оценю «тривиальность» численно: 4 — это одна строка совета в `night:open` (в параллельном режиме предлагать `git branch from origin/main`). Изменение локально, обратимо, риск близок к нулю. Такой квант не требует кворума пяти ролей — это правка под ревью. Но! Оговорка: 4 семантически принадлежит норме 3. Значит порядок: сначала зафиксировать норму (M1 = 3), потом 4 исполнить как её прямое следствие тем же PR или сразу после.

[Верстальщик]: Тогда картинка DAG вырисовывается чистая, без визуального шума. Корень 3. От него вниз — 4 (тулинг-следствие) и 5 (артефакт-лист). Сбоку два независимых блока — 1 и 2, каждый со своей глубиной спорности. Это читается как конструктивистский макет: одна несущая ось (3→4→5) и две автономные колонки (1, 2).

[Teamlead]: Хорошо. Но проверю ограду ещё раз вслух: никто не притащил версионирование схемы, ретрай, реестр процессов, журнал прогонов? Это соседний контур scripts-boundary. Если бы кто-то привязал 5 (handoff) к «журналу прогонов» — это дрейф. Все согласны, что 5 = только генерация merge-handoff из коммитов ветки, теги — follow-up, а журнал прогонов сюда не входит?

[Музыкант]: Согласен, 5 держим узко: коммиты ветки → handoff-текст. Теги — отдельная нота на потом, не в этом DAG. Журнал прогонов не трогаю — это чужая партитура.

[Структурщик]: Подтверждаю границу. 5 читает коммиты своей изолированной ветки и генерирует handoff — это замкнутый контракт, вход (git log ветки) → выход (markdown). Никаких ссылок на реестр процессов или версии схемы. Иначе `нарушена слабая связанность` между контурами заседаний.

[Математик]: Численно закрываю ограду: множество узлов = {1,2,3,4,5}, |V|=5. Любой шестой узел — вне множества, отвергается. Рёбра: E = {3→4, 3→5}. Компоненты связности: {3,4,5} и {1} и {2} — три компоненты. Топологическая сортировка цепочки: 3 ≺ 4, 3 ≺ 5; 4 и 5 между собой без ребра (параллельны после 3).

[Верстальщик]: Значит порядок заседаний по убыванию спорности и по зависимости: сначала несущая ось (норма 3), затем спорный ритм (2), затем словарь (1). Тулинг 4 и артефакт 5 — не заседания, а исполнение: 4 сразу за 3, 5 — как реализация после нормы. Форма следует функции: заседаем там, где есть спор, исполняем там, где есть канон.

[Teamlead]: Собираю порядок M1..Mn. M1 = кандидат 3 (guardrails, фундамент, максимальный спор о первичности hard). После M1 — исполнить 4 без заседания (прямое следствие нормы, тривиальный тулинг). M2 = кандидат 2 (гранулярность/каденс, ритм спорен). M3 = кандидат 1 (edit-class taxonomy, словарь). Кандидат 5 — исполнить после того, как норма 3 зафиксирует изоляцию ветки; заседание не нужно, но реализация терминальна в цепочке.

[Музыкант]: Одна поправка ритма: 5 (handoff) лучше делать сразу после 4, одним тулинг-спринтом «night:open ветка + night:close handoff», потому что это парные концы одной ветки — открыл от origin/main, закрыл в handoff. Не разрывать пару на разные фазы.

[Структурщик]: Принимаю пару 4+5 как единый тулинг-инкремент после M1, при условии что оба ссылаются на норму 3 как единственный источник определения hard-профиля. Тогда нет дублирования смысла. Порядок исполнения: M1(3) → tooling(4,5) → M2(2) → M3(1).

[Математик]: Проверяю, что порядок не нарушает топосортировку: 3 первым — да (корень). 4 и 5 после 3 — да. 1 и 2 в любом месте после 3 или параллельно — да, они изолированы. Предложенная последовательность M1(3)→tool(4,5)→M2(2)→M3(1) валидна как одна из линеаризаций DAG. Возражений нет.

[Верстальщик]: С точки зрения читаемости итога — последовательность честная: сначала несущая, потом парный тулинг, потом две автономные правки по спорности. Принимаю.

[Teamlead]: Фиксирую единый вердикт. Все роли — подтвердите. Полный список посылок: {1 edit-class taxonomy, 2 гранулярность кванта+каденс, 3 guardrails hard-первичны, 4 night:open ветка origin/main, 5 night:close авто-handoff}. Корень — 3. Рёбра 3→4, 3→5. Изолированы — 1 и 2. Без консилиума — 4 и 5 (тулинг). Заседания — M1(3), M2(2), M3(1).

[Музыкант]: Принимаю.

[Структурщик]: Принимаю.

[Математик]: Принимаю.

[Верстальщик]: Принимаю.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Полный список посылок (ровно 5) | 1 — edit-class taxonomy тест-гейта; 2 — гранулярность кванта + каденс чекпоинта; 3 — guardrails (hard: scoped-профиль + изоляция worktree + ветка origin/main первичны, «мёрж утром» вторичен); 4 — `night:open` ветка от origin/main в параллельном режиме; 5 — `night:close` авто-handoff из коммитов ветки |
| Корень (фундамент) | **Кандидат 3** (guardrails). Без фиксации первичности hard-профиля остальные висят в воздухе (research Q3) |
| Рёбра DAG | `3 → 4`, `3 → 5` (реализация нормы не опережает её определение) |
| Изолированные компоненты (параллельны) | Кандидат 1 и кандидат 2 — ортогональны цепочке 3→4→5 и друг другу |
| Кандидаты без консилиума | **4 и 5** — тулинг; исполнить парным инкрементом после M1 (открыл ветку от origin/main → закрыл handoff), оба ссылаются на норму 3 |
| Порядок заседаний M1..Mn | **M1 = 3** (фундамент, спор о первичности hard) → tooling(4,5) → **M2 = 2** (ритм/каденс спорен) → **M3 = 1** (словарь edit-class) |
| Ограда | Узлы соседнего контура scripts-boundary (версионирование схемы, ретрай, реестр процессов, гейт свежести, журнал прогонов) — **не входят**; 5 держим узко: коммиты ветки → handoff, теги — follow-up |

**Definition of Done (для дальнейших заседаний/задач):**
- M1 (кандидат 3) закрыт регламентной правкой: в night-build-регламенте hard-guardrails (scoped-профиль + изоляция worktree + ветка origin/main) явно объявлены первичными, «мёрж утром» — вторичным следствием.
- Тулинг 4+5 реализован одним инкрементом после M1: `night:open` в параллельном режиме советует ветку от origin/main; `night:close` генерирует merge-handoff из коммитов ветки; оба без прямых ссылок на контур scripts-boundary.
- M2 (кандидат 2) и M3 (кандидат 1) созваны отдельными заседаниями в указанном порядке; edit-class taxonomy зафиксирована как единый словарь (без синонимов-слоёв).
- Топологическая валидность порядка подтверждена (Математик): линеаризация не нарушает рёбра `3→4`, `3→5`.

---

*Реплик в диалоге: 25; каждый участник высказался не менее одного раза.*
