# Аудит сборки председателя — M4 «Регистрация в кабинете по промокоду панели», форма, фраза, частота, лендинг — 2026-09-21

Аудитор: отдельный агент, только чтение (S-M5), сессия Г. Предмет: сборка `docs/seanses/cabinet-registration-promo-m4-form-landing-assembly-2026-09-21.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`, вершина `1e0b5827`) против протокола `…-m4-form-landing-2026-09-21.md` и аудита `…-m4-audit-2026-09-21.md` (PR #2384). Три проверки по заданию председателя: (а) цитаты дословны и у заявленных ролей на заявленных строках; (б) сборка не меняет ни одной ячейки и ни одного критерия; (в) по пункту 8 председатель ничего не решил сам. Ствол для сверки: `origin/main` = `f989dbb6`. Сборка, протокол и повестка не правились.

## Итог

**ЧИСТО** по всем трём проверкам. Уточнений к самоописанию нет. Одно замечание по существу починки: П25 сменила тип с «факт» на «план» и содержание — с «`useAuth()` даёт только `login`» на «контекст расширяется методом регистрации»; это правка посылки, не решения, и сборка называет её прямо (стр. 18), факт вынесен в фактуру. По букве пункта 3 повестки так и надо: посылка без реплики — не посылка.

## Таблица

| # | Проверка | Вердикт | Цитата / команда |
|---|---|---|---|
| а | Цитаты дословны, роль и строка | PASS | Восемь адресных цитат (сборка стр. 14, 16, 18, 20, 24) проверены скриптом: жирное снято, кавычки нормализованы, точная подстрока на **заявленной** строке протокола, роль — по ближайшему маркеру `[Роль]:` выше. Все восемь на заявленных строках и у заявленных ролей: [Структурщик] 50 «Предлагаю `AuthApiError`…», [Структурщик] 62 «`parseError` переписать…», [Архитектор] 76 «Согласен: `setStoredToken` со страницы — обход.», [Структурщик] 114 «Прямой setStoredToken со страницы — запрет.», [Структурщик] 102 «Импорты LoginPage: `useAuth` даёт `login \| register`.», [Архитектор] 76 «Форма решения: D правит LoginPage + auth.ts + index.html…», [Математик] 56 «201 — успех, фраза в UI не нужна: сразу сессия.», [Математик] 96 «disabled истинно при fake timers +600000». Безадресная цитата «DOM показывает „Регистрация не принята.“» (сборка стр. 24) — тоже стр. 96, [Математик]. П9 теперь на двух отдельных репликах вместо склейки; П11 — «обход» и «запрет» вместо «запрещён»; П27 — реплика вместо строки таблицы 134. |
| б | Ни одна ячейка и ни один критерий не изменены | PASS | Сборка не воспроизводит ни таблицу решения, ни критерии готовности: строк таблицы — 0, маркеров критериев — 0. Протокол на ветке побайтно равен копии аудита #2384 (`git show <ветка>:<протокол> \| diff -q - <копия>` → без различий). Диапазоны сборки совпадают с протоколом: 126 `## Итоговое решение консилиума` … 142 `\| Расширение границ D`; 144 `### Критерии готовности` … 154 `---`, критериев семь (стр. 146–152). Изменения ветки `8827e42d` → `1e0b5827`: сборка (+39) и одна строка статуса M4 в `MEETING_ACTIVE.md` (`git diff --stat` → 2 files, +40 −1). Замечание: П25 — тип «факт» → «план», содержание переписано; это починка посылки по аудиту, сборка её объявляет (стр. 18: «Факт … из фактуры повестки, в репликах не произнесён; остаётся фактурой, не посылкой»). |
| в | Председатель ничего не решил сам | PASS | Сборка стр. 26: «Председатель границы не двигает и содержание теста не переписывает. Два выхода, решение при ратификации»; вариант 1 помечен «рекомендация председателя» с оговоркой «сверх этого в этих файлах ничего не менять», вариант 2 — повторный прогон с исполнимым тестом. Раздел «Сессии (ратифицированы четыре)» в `MEETING_ACTIVE.md` не менялся (`git diff … \| grep -c "Сессии (ратифицированы"` → 0); новая строка статуса M4: «пункт 8 — развилка владельца (расширить D на `package.json` и `vite.config.ts` веб-кабинета ровно на тестовое окружение DOM — рекомендация; либо повторный прогон). Ждёт аудита сборки». Факты ствола в сборке (стр. 4) сходятся с аудитом: `vite.config.ts` без `test`, `jsdom`/`happy-dom`/`testing-library` нет, `NodeOverflowHoldLine.test.tsx:1` `renderToStaticMarkup`. Раздел «Замечания аудита, которые уходят в задание D» решения не трогает: `{ token, user }`, «(в проде)», порядок правки лендинга — уточнения задания. |

## Что аудитор не делает

Своих решений по форме и по границам сессии D не предлагает; сборку, протокол и повестку не правит. Ратификация M4 и выбор выхода по пункту 8 — слово владельца.
