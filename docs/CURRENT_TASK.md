# CURRENT_TASK — точка входа для холодной сессии

**Эпик:** `detector-scoreboard` — витрина качества детекции
**Фаза сейчас:** Ф1 `scoreboard-plugin-shell` (плагин на измеренных числах)
**Ветка:** `feat/product` · worktree `Membrana-product`

## Стартовая строка (copy-paste в новую сессию)

> Работаем по `docs/prompts/DETECTOR_SCOREBOARD_EPIC_PROMPT.md`, фаза Ф1.
> Изделие — ПЛАГИН в модуле микрофона с таблицей качества детекции, не стенд.
> Числа уже измерены и лежат в `docs/tasks/research/dads-lab-report-2026-07-18.md`
> и `data/detectors-benchmark/v0.2/reports/latest.json` — новых замеров в Ф1 нет.
> Запреты эпика — в промпте, они проголосованы. Первым делом покажи владельцу,
> что именно будет видно в таблице, и только потом код.

## Фазы и паузы

| фаза | что появляется | пауза безопасна, когда |
|---|---|---|
| Ф1 `scoreboard-plugin-shell` | плагин + таблица на измеренном | таблица открывается, числа сходятся с отчётом |
| Ф2 `scoreboard-spectral-ladder` | лестница спектрального из наших 253 | видно, стабильны ли цифры с ростом выборки |
| Ф3 `scoreboard-dataset-ladder` | четыре набора с дедупом обеих сторон | наборы в репозитории, провенанс виден |
| Ф4 `scoreboard-neural-ladder` | лестница нейросети | виден прирост от обучения |
| Ф5 `scoreboard-panel-publish` | таблица на `mmbrn.tech` | по слову владельца |

Ф1 и Ф2 не требуют внешних данных. Ф3 — единственная с внешним риском (~900 МБ).

## Что уже измерено (материал для витрины)

| детектор | обнаружено дронов | ложных тревог |
|---|---|---|
| стоковая нейросеть, порог наш | 55 из 60 | 22 из 60 |
| спектральный (train/val) | 19 из 20 | 6 из 20 |
| нейросеть после обучения | 62 из 63 | 6 из 74 |

Потолок стоковой модели: при обнаружении ≥54 из 60 ни одно из 120 положений порога
не даёт меньше 21 ложной тревоги.

## Незакрытое из прошлого спринта

`dads-benchmark-bridge` — волны 2 (извлечение) и часть 3 остались; они частично
поглощены Ф3/Ф4 этого эпика. Волны 1 и 4 сделаны и смёржены в ветку.

## Что ждёт владельца

- Подтверждение DoD витрины **до кода** (урок 18.07).
- Слово на Ф5 — публикация внешняя.
- Мёрж в main — только по слову владельца.
