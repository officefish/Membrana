# Expectations — Block `snapshot-cold-migration`

> Односторонний документ (регламент §Hard rules): чужие EXPECTATIONS не читались,
> об интерфейсах не договаривались. Живой — дописывается по ходу Phase 2,
> запечатывается freeze-тегом в точке интеграции.

---

## Что мне нужно от соседей

| От блока | Что | Форма (сигнатура/схема) — как я её жду | Инварианты (единицы, частоты, порядок, владелец времени) |
|---|---|---|---|
| `units-trace-measure` | **Словарь единиц**: что именно является записью снимка и записью холода | Таблица «величина → единица счёта» + правило биекции «центральная под-задача ↔ GitHub-issue». Мне нужно однозначное «поле X снимка = единица Y словаря» | Единица ответственности = центральная задача = GitHub-issue (вердикт M1 — закрыт, на него опираюсь); контейнер и дополнительные — вне счёта; двойной счёт исключён биекцией |
| `units-trace-measure` | **Потребность батча измерения**: какие поля движения нужны пяти величинам, чтобы producer снимал достаточно | Список полей записи снимка (state? assignee? completedAt? blockedBy?) — иначе снимаю по допущению A1/A6 и на консилиуме сверяем | «Два снимка» батча = два материализованных pull-снимка Linear (уточнение M2 — закрыто); частота — вечерний батч; окно отчёта задаёт измерение, момент съёмки — мой `capturedAt` |
| `units-trace-measure` | **Форма honest-шапки отчёта** — чтобы свести её с honest-шапкой снимка/холода в один словарь терминов | Поля шапки отчёта (окно, ревизии обоих снимков, множество артефактов) | Шапка отчёта обязана уметь нести мои `capturedAt` и `sourceRevision` обоих снимков — провенанс самодостаточен независимо от места хранения (M2 DoD) |
| `units-trace-measure` | **Артефакт закрытия**: где в нём приёмка и ревизия — для `hasPassport`/`issueClosed`/`workDone` в `debtClass` | Схема артефакта закрытия (кто дал LGTM + ревизия head) | До консилиума `debtClass` живёт на фикстурном артефакте закрытия (стаб S3); отказ = `exit ≠ 0`, не тон — общий закон |
| `lead-persona` | **Формат имени персоны, принявшей выход** — значение поля `leadPersona`, которое холод хранит, а снимок несёт | Строка-аватар (как в текущем реестре: `kuryokhin`, не имя модели); подпись субагента не нужна (тот безымянен по контракту) | Одно имя на запись; имена `assignee`/`delegatedAgent` не склеиваются (M1) |
| оба | **Единая таблица exit-кодов** спринта | Моя пятистрочная таблица (`0/10/11/20/21`, CONCEPT §4) — вход; жду встречных кодов отказа-I/II | Конвенция блока: 0 чисто · 1–19 мягкие · ≥20 жёсткие; известное напряжение M2↔M4 о «мягкости» вынесено в CONCEPT §4 — сводить на консилиуме |
| оба | **Владелец времени** на швах | Решение консилиума: чьи часы каноничны | Моя позиция односторонне: `capturedAt` ставит производитель (office, UTC); окно отчёта и «день» журнала — потребители моего времени, не наоборот |

## Что я готов отдать

| Блоку | Что | Форма | Инварианты |
|---|---|---|---|
| `units-trace-measure` | **Снимок-контракт** — вход чистой функции измерения | JSON `linear-snapshot@1`: `header{capturedAt, sourceRevision, source, trigger, recordCount}` + `records[]` (CONCEPT §1.1) | Адресуем; провенанс обязателен; тот же снимок → тот же вердикт бит-в-бит; сеть только в производителе |
| `units-trace-measure` | **Предикат свежести** | `fresh(snapshot, sourceCursor) → bool`, курсор инъецируется | O(1), один дешёвый запрос, вне тела гейта; свежесть — аннотация, не валидность |
| `units-trace-measure` | **Коды снимка** | `0 OK / 10 SNAPSHOT_STALE / 20 SNAPSHOT_NO_INPUT` | Отказ ≠ замечание по коду |
| оба | **Офлайн-reader холода** | `cold-reader.mjs`: выборка записей (содержание + `dependsOn`/`strategicWave`/`leadPersona`) | Без сети; `archive.jsonl` append-only; единственный писатель — office-батч; движение не дублируется |
| оба | **`wellArchived` / `classifyDebt` / счётчик** | Чистые функции `(record, closureArtifact?) → bool | class`; счётчик читает только `work` | Детерминизм на снимке; `parked` и `archived-pre-passport` счётчик не поднимают |
| координатору | **Шапка отчёта миграции** | `mode: soft|migrating|hard`, `N_illegal`, дельта за прогон, список `parked` с датами | Переключение режимов по предикату `N_illegal = 0`, не по дате |

## Мои стабы (исполняемые, в моей зоне)

| Стаб | Замещает | Где живёт |
|---|---|---|
| S1. Фикстуры Linear GraphQL-ответов (2 страницы issues полного pull с пагинацией) | живой Linear API | `stubs/linear-issues-page-1.fixture.json`, `stubs/linear-issues-page-2.fixture.json`; потребитель — `linear-snapshot.service.test.ts` (fetch застаблен, сети в тестах нет) |
| S2. Фикстурная копия реестра (представители всех классов M4: DRU-93, active-no-issue, archived-pre-passport, дефект db-h1b, record/work/owner-knowledge; массовые наборы 153/258 тесты строят программно от этих форм) | живой `docs/tasks/registry.json` (его блок не читает и не трогает) | `stubs/registry.fixture.json`; потребитель — `scripts/lib/debt-classify.test.mjs` |
| S3. Стаб артефакта закрытия (`{lgtmBy, headRevision}`) | форму следа блока `units-trace-measure` | `stubs/closure-artifact.fixture.json` |
| S4. Стаб провайдера курсора источника | сетевой запрос курсора Linear | `stubs/linear-cursor.fixture.json` + инъекция `getSourceCursor` в `snapshot-freshness.test.mjs` |
| S5. Стаб триггера (вебхук-payload; сигналы webhook/evening-signal/manual) | боевую вебхук-подписку и вечерний ритуал | `stubs/linear-webhook-issue-update.fixture.json` + тесты коалесценции в `linear-snapshot.service.test.ts` |

Стабы в интеграционную ветку не мёржатся; стаб, доживший до прода, — дефект интеграции.

## Явные допущения (на них блок живёт до Interface Consilium)

- **A1.** Запись снимка = Linear-issue; проекция на единицу счёта — через
  `githubIssueRefs` (биекция центральной под-задачи ↔ GitHub-issue, вердикт M1).
- **A2.** `blockedBy`/`blocking` видны в GraphQL, но **вебхуками не приходят** —
  из ресёрча 19.07; проверка по живому API — Phase 2 (см. «Пробелы»).
- **A3.** Двухслойность `assignee` (ответственная) / `delegatedAgent` (исполнитель)
  существует в API как два поля — факт проверяет сосед (`units-trace-measure`).
  До проверки производитель поле делегата **не запрашивает** (неверифицированное
  имя поля уронило бы живой GraphQL) и честно отдаёт `delegatedAgent: null`;
  имена не склеиваются.
- **A4.** Прецедент `linearId = DRU-93` — единственная заполненная связка в архивной
  записи `issue-178-async-v2-reconciliation`; репозиторная половина проверена
  Phase 2 (см. «Пробелы»), Linear-половина — нет.
- **A5.** `leadPersona` — строка-аватар в терминах текущего реестра.
- **A6.** `sourceRevision` = курсор синхронизации Linear (кандидаты: org-level
  sync id / max(`updatedAt`) выборки) — точную форму даст проверка API Phase 2;
  контракт от выбора не зависит (строка-курсор, сравнение на равенство).

## Пробелы (обновлено Phase 2, 19.07)

- **Живой Linear через office недоступен — пробел остаётся, не блокер.**
  Проверка выполнена 19.07: office жив (`/health` 200), аутентификация
  `X-Membrana-Token` проходит, но `v1/linear/issue/DRU-93` и `DRU-1` стабильно
  отвечают 400 `Linear API request failed` — отказ на плече office→Linear
  (вероятно, ключ Linear на VDS). Следствия:
  - **A2** («blockedBy/blocking вебхуками не приходят») по живому API не
    подтверждён — остаётся допущением из ресёрча 19.07; контур снимко-центричный,
    вебхук — только триггер в любом случае, поэтому на конструкцию не влияет.
  - **A4** проверен наполовину: в реестре карточка `issue-178-async-v2-reconciliation`
    несёт `linearId: "DRU-93"` простой строкой (заполнена вручную в day-sprint
    29.06, `archiveNotes: PR #200`) — связка существованием, без механизма
    автосинка. Как выглядит DRU-93 со стороны Linear — не проверено.
  - **A6** (форма курсора; кандидат `organization.updatedAt`) по живому API не
    подтверждён; контракт от выбора не зависит (строка-курсор, равенство).
- **Код миграционного режима гейта комнатой не назван** — блок назвал `11 =
  LEGALITY_MIGRATING` сам (CONCEPT §3.3, §4); это решение блока, подлежит
  сведению в единую таблицу exit-кодов на Interface Consilium.
- **Семантика «мягкого» кода расходится между M2 (ненулевой различимый) и M4
  (exit 0)** — не сглаживал, исполняю оба буквально; напряжение вынесено
  в CONCEPT §4 на консилиум.
