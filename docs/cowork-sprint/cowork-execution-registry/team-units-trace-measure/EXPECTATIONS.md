# Expectations — Block `units-trace-measure`

> Односторонний документ (Phase 1, дописывается в Phase 2). Чужие ветки и
> EXPECTATIONS не читались; всё ниже — допущения этого блока, не договорённости.

## Что мне нужно от соседей

| От блока | Что | Форма (сигнатура/схема) — моё допущение | Инварианты (единицы, частоты, порядок, владелец времени) |
|---|---|---|---|
| `snapshot-cold-migration` | **Форма снимка реестра** — вход `computeTeamMetrics` и гейта отказа-I | Материализованный JSON: `{ header: { capturedAt, sourceRevision }, cards: [...] }`; карточка несёт минимум `id`, `status`, `leadPersona`, `createdAt`, `closedAt/archivedAt` | Снимок адресуем и версионирован, читается офлайн; «два снимка» батча = два **материализованных pull-снимка** (вердикт Р2 эпика 2); тело моих функций в сеть не ходит; владелец времени — timestamps ИЗ снимка, не мои часы |
| `snapshot-cold-migration` | Признак движения для `wip` (какая колонка/статус считается «движется») | поле `status ∈ { open, in-progress, ... }` на карточке/под-задаче | биекция «центральная под-задача ↔ GitHub-issue» сохраняется в снимке — иначе `wip` и `throughput` считают разные множества |
| `snapshot-cold-migration` | Ревизии снимков для honest-шапки отчёта | `header.sourceRevision` + `header.capturedAt` обоих снимков | шапка отчёта обязана назвать ревизии обоих срезов — без них отчёт не собирается |
| `lead-persona` | **Имя персоны, принявшей выход** — значение для `leadPersona` (карточка) и `acceptedBy` (артефакт закрытия) | строковый идентификатор персоны из канона (`vesnin`, `angelina`, ...) | Ровно одна персона на единицу работы; подпись субагента **не существует и не требуется** (безымянен по контракту); смена значения между снимками = событие эскалации для `escalationRate` |
| координатор / интеграция | Точка вшивания гейтов (`pre-push`, `task:archive`, closure review) | адаптер зовёт `checkLeadPersona` / `checkAcceptance` и транслирует `{code}` в exit процесса | вшивание — адаптер Phase 4, блок хуки не трогает |

## Что я готов отдать

| Блоку | Что | Форма | Инварианты |
|---|---|---|---|
| всем | **Словарь единиц** `docs/tasks/UNITS_DICTIONARY.md` | словарные статьи + таблица «величина → единица счёта» + правило биекции | центральная задача = GitHub-issue; «закрыто» = закрытие центральной; контейнер и дополнительные вне счёта; двойной счёт исключён |
| `snapshot-cold-migration` | Требования потребителя к снимку (какие поля обязаны быть, чтобы пять величин считались) | список полей карточки из строки 1 таблицы выше | если поле отсутствует — величина честно не считается, а не домысливается |
| `lead-persona` | Форма поля приёмки в артефакте закрытия | `{ acceptedBy: <персона>, headRev: <sha> }` | подтверждение декоррелировано от назначения фазой (регистрация ≠ LGTM на ревизии) |
| интеграции | Чистые функции гейтов + локальная таблица exit-кодов | `checkLeadPersona(card)`, `checkAcceptance(artifact, {mode})` → `{code, verdict, reason}`; `0` = пройден (pass/soft), `1` = отказ (hard) | отказ отличается **кодом, не тоном**; функции без сети/git/Date.now; готов свести коды в единый глоссарий на Interface Consilium |
| интеграции / ритуалу | `computeTeamMetrics(prev, curr, artifacts, window)` + рендер отчёта с honest-шапкой | детерминированный отчёт: окно, ревизии обоих снимков, множество артефактов, подпись «wip = очередь намерений», секция неизмеримого | тот же вход → тот же отчёт бит-в-бит; UI/ритуал только читает |
| аудиту дня | `computeReworkRhetoric(prev, curr, artifacts)` — риторическая шестая | отдельный экспорт, в батч пяти величин не входит | вход назван явно (два снимка дня + артефакты закрытия окна); хранимого события нет |

## Мои стабы (исполняемые, в моей зоне)

| Стаб | Замещает | Где живёт |
|---|---|---|
| Фабрика фикстурных снимков реестра (пара prev/curr с провенансом) | producer снимка блока `snapshot-cold-migration` | `scripts/lib/measure-fixtures.mjs` + инлайн в `scripts/*.test.mjs` |
| Фикстурные артефакты закрытия с `{acceptedBy, headRev}` (по мотивам схемы `docs/reviews/*/manifest.json`) | closure-артефакт живого lifecycle | те же тесты |
| Фикстура «213 карточек, 212 с `leadPersona`» | живой `registry.json` (трогать запрещено) | `scripts/trace-lead-persona.test.mjs` |
| Стаб имени персоны (`'vesnin'`-строка) | контракт ведущей персоны блока `lead-persona` | инлайн в тестах |

Стабы в интеграционную ветку не мёржатся; стаб, доживший до прода, — дефект интеграции.

## Пробелы (фиксируются, не блокеры)

- **Проверка по живому Linear API не выполнена** (Phase 1 — только доки). Факты
  «двухслойность `assignee`/`delegatedAgent`» и «под-задача — полноценная сущность
  со своим статусом» взяты из выжимки ресёрча. План: Phase 2, office
  `v1/linear/issue/:id`; при недоступности API пробел остаётся здесь — контур
  снимко-центричный, на DoD блока не влияет.
- Форма снимка соседа — допущение (см. первую таблицу); сведение — Interface
  Consilium, шов №2 и №3 из brief.
- Код миграционного режима гейта снимка (три режима) — зона соседа; мой отказ-II
  оперирует только своими режимами soft/hard, переключение — параметром `mode`.
