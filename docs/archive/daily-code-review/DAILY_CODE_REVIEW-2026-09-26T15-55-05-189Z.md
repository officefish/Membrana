<!-- Сгенерировано: 2026-09-26T15:54:36.222Z (yarn code-review; daily, llm-anthropic) -->

> Контур ревью (rt-8):
> Режим: работа дня
> Precision: exact
> Период: 590c1bdfcc502e0b29b482e662f6c4c26c3045b5^..36e88e71687d663eea491ae2547683b73bc62fd9 (7 коммит(ов))
> ⚠ Oversized (>400 строк, дифф не развёрнут — ревьюить отдельно): 590c1bdf #2467 (601), 79d5afa2 #2461 (544), 36e88e71 (656)

---

Tier: T2

---

## [Архитектор — Vesnin] (ведущий ревью, T5-бестиарий)

Дифф охватывает 4 изменённых пакета/скоупа (`scripts/`, `packages/background-office`, `docs/`, `apps/`), поэтому — полный формат.

**Бестиарий (проход по всем 10 зверям):**

- **B6 («Молчаливый зелёный»)** — `docs/procedure-runs/trail/2026-09-26.jsonl` фиксирует три прогона `ritual-day` за утро, два из которых закрыты `fail`, третий открыт без закрытия. Это не антипаттерн в диффе, это данные; сам агрегатор читает `status:"fail"` честно. Однако `ritual-day-2026-09-26-r2` закрыт как `orphaned` через `orphanedBy`, а корень фрикций (`morning-care`, `angelina`) в обоих `fail` не раскрыт (`root:null, fix:null`) — агрегатор видит день без диагноза. P2 / opportunity.
- **B3 («DoD-на-механику»)** — не замечен: DoD MAIN_DAY_ISSUE содержит продуктовые критерии (офис-смоук шаг 6, `/health/deep` healthy, лендинг обновлён, #2369 закрыт). Чисто.
- **B4, B7, B8, B9, B10** — не обнаружены в диффе.
- **B1, B2, B5** — не применимы к данному диффу (скрипты/docs, не генерация).

**Архитектурные наблюдения:**

- `#2469` (`pr-wait.mjs`): экспорт `explainMergeBlock` выделен из `explainNoChecks` — правильное разделение ответственности; проверка конфликта вынесена до цикла ожидания, что устраняет класс «ждём CI на уже конфликтующем PR». Контракт чист.
- `#2471` (`pr-ship.mjs`): замена `@{u}` на `refs/remotes/origin/<branch>` — устраняет зависимость от настроенного upstream после `land-rebase`. Удаление шага `land-upstream` корректно: `--no-track` не переназначает upstream, поведение предсказуемо. ADR-0023 не нарушен.
- `#2470` (`night-hunt-graph.ts`): `assertWorkspaceGraphIntegrity` добавлена в хвост `parseWorkspaceGraph` — fail-fast на неполном разборе. Составной Berry-ключ (`@membrana/core@npm:*, @membrana/core@workspace:…`) теперь парсится через `workspaceDescriptor`. Граф не может вернуть пустой результат молча — верная архитектура для доверенного вещдока.

Контракты между пакетами (`scripts/` → `packages/background-office`) — через публичный API, прямых импортов через границы монорепо в диффе нет. C1 — чисто.

---

## [Teamlead — Tarasov]

**PR size:** `#2469` ~67 строк OK · `#2470` ~112 строк OK · `#2471` ~75 строк OK. Три oversized-PR (`#2467`/601, `#2461`/544, коммит `36e88e71`/656) — помечены скриптом как «не развёрнуты»; ревьюить отдельными слотами, P1 ревью-долга фиксируется третий день.

**C8:** `console.error` в `pr-wait.mjs:516` (`[pr:wait] ОТКАЗ: …`) — это диагностика tooling-скрипта, не production runtime; допустимо, не блокирует.

**C9:** секретов и deploy-логов в коммите нет. `docs/security/deps-watch-snapshot.json` — только дата обновлена, findings не изменились.

**C10:** `docs/field/buffer-watch-2026-09-06.jsonl` → `docs/field/buffer-watch-2026-09-06-trail.jsonl` — переименование согласует суффикс `-trail` с форматом `2026-09-25-trail.jsonl`. Catalog/device-board не затронут.

**Ключевой риск на завтра:** rollout-магистраль (`cabinet-registration-rollout`, #2369 OPEN) третий день не продвинута — сегодняшний дифф целиком в tooling/docs. Образ офиса не собран, `cabinet.env` не дополнен — это не code review, это операционный блокер.

**Утренние команды:**

```bash
# 1. Убедиться, что tooling-фиксы живы
yarn turbo run typecheck test --filter=@membrana/background-office
node --test scripts/pr-wait.test.mjs
node --test scripts/pr-ship.test.mjs

# 2. Санитария (P1, четвёртый день)
# sequence-контракт trail-агрегатора — синтетический тест
yarn turbo run test --filter=@membrana/background-office -- --reporter=verbose

# 3. Oversized PR — выделить архитектурный слот (не попутно)
yarn code-review:pr 2467
yarn code-review:pr 2461
```

---

## [Структурщик — Ozhegov]

**C4 (сервисы без React):** `packages/background-office/night-hunt-graph.ts` — чистый TS без фреймворков. Все новые функции (`entryKey`, `workspaceDescriptor`, `countWorkspaceEntries`, `assertWorkspaceGraphIntegrity`) — чистые, без побочных эффектов. Слабая связанность соблюдена.

**C7 (тесты рядом):** все три PR несут тесты рядом с кодом (`pr-wait.test.mjs`, `pr-ship.test.mjs`, `night-hunt-graph.test.ts`). Красные входы присутствуют (`classifyChecks(pr.statusCheckRollup).state, 'green'` — намеренный контрастный кейс). Критические ветки покрыты: `CONFLICTING/DIRTY`, `MERGEABLE/CLEAN`, составной Berry-ключ, пересекающиеся `runId`.

**Замечание (P2 / opportunity):** `countWorkspaceEntries` и `assertWorkspaceGraphIntegrity` — приватные функции в том же модуле, не экспортированы; тест проверяет их только через `parseWorkspaceGraph`. Это верное сокрытие, но если понадобится изолированный юнит-тест — придётся идти через публичный вход. Не блокирует.

**C1:** `readFileSync(new URL('../../../../../yarn.lock', import.meta.url), …)` в тесте `night-hunt-graph.test.ts` — тест читает живой `yarn.lock` монорепо. Это интеграционный тест по природе; путь хрупкий при переносе пакета. P2 / opportunity: зафиксировать фикстуру или пометить тест тегом `integration`.

---

## [Математик — Dynin]

**C6 (чистые функции, границы/NaN):** `entryKey` возвращает `null` при несовпадении — безопасно. `workspaceDescriptor` возвращает `null` при отсутствии `@workspace:` спецификации — безопасно. `countWorkspaceEntries` итерирует строки без накопления побочных состояний. `assertWorkspaceGraphIntegrity` бросает `Error` при нарушении инварианта — поведение детерминировано, не молчит.

Инвариант монотонности `sequence` в trail (`2026-09-26.jsonl`): два прогона `ritual-day-2026-09-26` и `ritual-day-2026-09-26-r2` оба стартуют с `sequence:1` — это ровно тот P1, который фиксируется четвёртый день. Дифф не чинит агрегатор, только добавляет данные с тем же нарушением. Синтетический тест с пересекающимися `runId` в `night-summary.test.mjs` не написан — блокер для гейта `secret-parser-built`.

---

## [Музыкант — Kuryokhin]

По моей зоне (Web Audio, DSP, edge-захват, IoT) дифф пустой — ни одного касания аудио-контура. C2 — «—». Стендап честно запрещает DSP-бенчмарки (`FFT_METRICS_POTENTIAL_AND_LIMITS.md §6`); правило соблюдено.

---

## [Верстальщик — Rodchenko]

По UI-зоне (`apps/panel`, `apps/cabinet`) в развёрнутом диффе правок нет — только обновление дат в `DAILY_STANDUP.md`, `DAY_PLAN.md`, `MAIN_DAY_ISSUE.md`. C5 — «—». Lint-warning `titleOf` (`react-hooks/exhaustive-deps`) в `apps/cabinet` третий день не закрыт; XS-правка, не блокирует, но копит технический долг.

---

## Итоговый артефакт

`docs/DAILY_CODE_REVIEW.md` (режим daily, вечер 2026-09-26)

## Definition of Done (утро)

```bash
yarn turbo run typecheck test --filter=@membrana/background-office
node --test scripts/pr-wait.test.mjs
node --test scripts/pr-ship.test.mjs
# После: выделить слот на sequence-контракт и три oversized-PR
```

## Риски

- **P1** — `sequence`-контракт trail-агрегатора не починен (четвёртый день); данные `2026-09-26.jsonl` подтверждают нарушение в проде (`sequence:1` дважды в двух разных `runId`). Синтетический тест не написан. Гейт `secret-parser-built` технически непроходим.
- **P1** — три oversized-PR (`#2467`/601, `#2461`/544, `36e88e71`/656) влиты без архитектурного разбора; ревью-долг накапливается третий день.
- **P1** — магистраль `cabinet-registration-rollout` (#2369 OPEN) не продвинута: образ офиса не собран, `cabinet.env` не дополнен.
- **P2** — `root:null, fix:null` в `friction`-записях trail за 26.09: агрегатор видит день fail без диагноза.
- **P2** — интеграционный тест читает живой `yarn.lock` без тега; хрупко при переносе пакета.
- **P2** — lint `titleOf` в `apps/cabinet` висит третий день.