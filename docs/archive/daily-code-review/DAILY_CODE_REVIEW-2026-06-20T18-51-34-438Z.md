<!-- Сгенерировано: 2026-06-20T18:50:12.780Z (yarn code-review) -->

# Code Review: Realtime Observation Pipeline P0–P3

## Контекст
Ветка **vesnin** (Teamlead), сегодня доделана эпика `DEVICE_BOARD_REALTIME_OBSERVATION_EPIC` (DBJ0–DBJ6): интеграция журнала, репортера, сценариев с runtime-обсервацией. Текущее состояние: **build + lint отказывают**, есть неотслеженные файлы для recording-gate (DB-RECORDING-GATE-V07).

---

## [Teamlead]

### Стратегическая оценка

✅ **Выполнено**:
- Контракты `JournalRef`, `ReporterRef`, `PublishReport` инкапсулированы в `packages/core/contracts/device-board/`
- Runtime-хост (`scenarioMicJournalBridge.ts`) корректно делегирует:
  - `GetJournal` → `JournalService`
  - `GetReporter` → `ReporterService`  
  - `PublishReport` → `appendReport(…)`
- Цепочка `MakeReportFromTrack` → `MakeReportFromAnalysis` → `PublishReport` логически завершена
- Сценарные переменные расширены: `VariableType.Reporter`, `VariableType.Journal`

⚠️ **Риски архитектуры**:

1. **Build-break в `@membrana/device-board`** — критично:
   - Линтер выбрасывает ошибку (не видно деталей)
   - Неотслеженные файлы `recording-*.ts*` предполагают **незавершённую фичу** в том же пакете
   - Две эпики по-видимому переплелись (realtime observation + recording-gate)

2. **Граница между device-board и core-контрактами**:
   - `recording-policy.ts`, `recording-slice-ref.ts` лежат в `packages/core/contracts/device-board/`
   - Это правильно (контракты), но **录制逻辑 (recording-gate-executor.ts) в device-board** — нужно убедиться, что нет обратной зависимости от runtime → core → runtime

3. **Смешивание эпик**:
   - **DBJ0–DBJ6** завершены (observation-pipeline)
   - **DB-RECORDING-GATE-V07** стартует (recording, gate, flush)
   - На ветке `vesnin` они **не разделены** — это затрудняет код-ревью и управление задачей

### Требования перед LGTM

- [ ] Развязать две эпики по разным веткам / блокам коммитов (или явно в `CURRENT_TASK.md`)
- [ ] Зафиксировать все новые файлы в git (`git add packages/device-board/src/graph/start-recording-node.ts` и т.д.)
- [ ] Пройти `yarn build` и `yarn lint` до нуля
- [ ] Обновить `docs/tasks/README.md` и `registry.json` (если DB-RECORDING-GATE-V07 стартует)

---

## [Структурщик]

### Проверка слабой связанности

**Граница пакетов**: ✅ OK в большинстве.

```
packages/core/
  └─ contracts/device-board/
       ├─ device-board.ts (node, port, graph) ✅
       ├─ journal + reporter contracts ✅
       ├─ recording-policy.ts (NEW) ✅ контракт, не реализация
       └─ recording-slice-ref.ts (NEW) ✅

packages/device-board/
  ├─ graph/
  │  ├─ make-track-node.ts ✅ (VariableType.Reporter support)
  │  ├─ publish-report-node.ts ✅
  │  ├─ start-recording-node.ts (NEW) ? нужна проверка
  │  ├─ stop-recording-node.ts (NEW) ? нужна проверка
  │  └─ flush-spectral-analyser-node.ts (NEW) ? нужна проверка
  ├─ runtime/
  │  ├─ block-executor.ts ✅
  │  ├─ scenario-runtime.ts ✅
  │  ├─ recording-gate-executor.ts (NEW) ? нужна проверка
  │  └─ recorder-recording-session.ts (NEW) ? нужна проверка
  └─ context/
     └─ device-board-graph-context.tsx ✅
```

⚠️ **Потенциальные нарушения**:

1. **`recording-gate-executor.ts`** — проверить:
   - Зависит ли от чего-то снаружи device-board (журнал, репортер, медиа-сервис)?
   - Если да — использует ли `JournalRef` / `ReporterRef` через контракты? (допустимо)
   - Или напрямую импортирует `JournalService`? (нарушение слабой связанности)

2. **`start-recording-node.ts`, `stop-recording-node.ts`** — граница:
   - Похоже, это **UI-ноды** для графа (как `MakeTrackNode`)
   - Они **не должны** содержать бизнес-логику recording'а — только параметризацию
   - Recording-логика → `recording-gate-executor.ts` (runtime)

3. **`scenarioMicJournalBridge.ts`** (модифицирован):
   - Расширен на `GetJournal` + `GetReporter`
   - **Нет прямых импортов между плагинами** ✅
   - Использует `JournalRef`, `ReporterRef` из контрактов ✅

### Команда на Структурщика

```bash
/refactor device-board "убедиться, что recording-gate не импортирует JournalService,
ReporterService прямо. Должны быть JournalRef + ReporterRef из контрактов."
```

---

## [Математик]

—

---

## [Музыкант]

⚠️ **Recording-related изменения** (новые файлы):

Потенциально затрагивает потоки:
- `FlushSpectralAnalyserNode` → выброс спектра из анализатора
- `RecordingGateExecutor` → запуск / остановка записи, управление буфером
- `RecorderRecordingSession` → сессионное состояние записи

**Вопросы перед реализацией**:

1. Как `FlushSpectralAnalyserNode` **выбирает** спектральный анализатор?
   - Через `VariableType.SpectralAnalyser` (новая переменная)?
   - Через глобальный контекст?
   - Проверить контракт `scenario-variables.ts`

2. **Recording-gate**: когда триггерить старт/стоп?
   - `IsRecordingWindowFull` (проверка буфера) — это отдельный узел или часть `RecorderRecordingSession`?
   - Если отдельный узел → должен вернуть `boolean` и затем вызвать `StopRecordingNode`

3. **24-bit / 48 kHz целевое качество** — где проверяется?
   - В `RecorderRecordingSession` при инициализации WebAudio контекста?

**Статус**: не видно реализации в текущем ревью (файлы есть, но линтер отказывает). После пропуска lint должен проверить **очередь**.

---

## [Верстальщик]

**Новые UI-ноды** (по сути, данные-node параметризаторы):

- `StartRecordingNode` — ui-params для инициирования сессии
- `StopRecordingNode` — параметры завершения
- `IsRecordingWindowFullNode` — условный узел (возвращает `boolean`)
- `FlushSpectralAnalyserNode` — параметры выброса спектра

**Требования по DESIGN.md**:

- [ ] Каждый узел должен иметь иконку, заголовок, подсказку (tooltip)
- [ ] Palette-ночу должны быть добавлены в `variable-constructor-palette.ts`
- [ ] Socket-типы указаны в контрактах (`socket-type.ts`)?

**Проверка в коде**:

```typescript
// packages/device-board/src/types/variable-constructor-palette.ts (модифицирован)
// ? Добавлены ли стартовые ноды recording?
```

**Статус**: lint отказывает, нельзя проверить конечное состояние.

---

## Итоговый артефакт

### Diagnose & Blockers

| Проблема | Статус | Зависит от | Действие |
|----------|--------|-----------|----------|
| **@membrana/device-board#lint ошибка** | 🔴 CRITICAL | Неизвестно | Запустить `yarn lint --filter device-board` и показать detalь |
| **@membrana/device-board#build ошибка** | 🔴 CRITICAL | lint | Вероятно, TypeScript синтаксис или импорты |
| **Неотслеженные файлы** (recording-*.ts) | 🟡 WARNING | Контроль | `git add packages/device-board/src/{graph,runtime}/*recording*.ts` |
| **Две эпики на одной ветке** (observation + gate) | 🟡 WARNING | Структура | Разделить или явно задокументировать в CURRENT_TASK.md |

### Pre-requisites перед следующей итерации

1. **Исправить lint/build**:
   ```bash
   yarn lint --filter device-board
   # Показать ошибку
   yarn build --filter device-board
   ```

2. **Зафиксировать новые файлы**:
   ```bash
   git add packages/core/src/contracts/device-board/recording-*.ts
   git add packages/device-board/src/graph/{start,stop,flush,is-recording}*.ts*
   git add packages/device-board/src/runtime/{recorder,recording-gate}*.ts*
   ```

3. **Завершить или заморозить recording-gate**:
   - Если готово → обновить `registry.json`, архивировать DBJ-этапы
   - Если в work → перейти на отдельную ветку `druid/db-recording-gate-v07`

4. **Запустить тесты**:
   ```bash
   yarn test --filter device-board
   yarn test --filter core
   ```

---

## Definition of Done

- [ ] `yarn lint --filter device-board` → 0 errors
- [ ] `yarn build --filter device-board` → успешно
- [ ] Все новые файлы в git (не untracked)
- [ ] `yarn test --filter device-board` → зелёный (если есть новые тесты)
- [ ] Recording-gate отделён от observation-pipeline (либо явно объединены в одной эпике)
- [ ] Проверка **слабой связанности**: нет прямого импорта `JournalService` / `ReporterService` из recording-gate
- [ ] Проверка **контрактов**: все новые socket-типы и variable-типы описаны в `device-board.ts`
- [ ] **Teamlead LGTM**: да / нет

---

## Команды к выполнению (priority order)

1. **Vesnin** (Teamlead): `yarn lint --filter device-board` → диагностика
2. **Ozhegov** (Структурщик): проверить импорты в recording-gate-executor
3. **Druid** (агент): исправить lint, закоммитить untracked файлы
4. **Rodchenko** (Верстальщик): проверить palette-ноды after build passes
5. **Vesnin** (Teamlead): финальный ревью + LGTM / отправить на рефактор

---

## Evening handoff — 2026-06-20 (уточнение после автоматического ревью)

> Авто-ревью выше частично устарело (контекст collector не видел свежий diff). Фактическое состояние на конец дня:

### Сделано сегодня (Recording Gate v0.7, ветка `vesnin`)

| Фаза | Статус |
|------|--------|
| R0 contracts | ✅ archived |
| R1 graph nodes | ✅ archived |
| R2 host bridge | ✅ archived |
| R3 executor + PublishReport exec-out | ✅ archived |
| R4 smoke JSON | 🟡 partial (`main-v07.json`, E2E крутится) |
| `@membrana/device-board` tests | ✅ **234/234** |
| Interim fix GetSample → PCM buffer | ✅ (bridge); качество звука — **не OK** |

### Блокер продукта (не граф)

Tick-chunk concat (`GetSample` ~100 ms) vs **continuous Worklet** в `mic-buffer-recorder` → треск, «ускоренный» preview.

### 🎯 Фокус завтра (MAIN_DAY_ISSUE)

```
db-recording-parity-mic-r5
→ docs/prompts/DEVICE_BOARD_RECORDING_PARITY_MIC_PROMPT.md
→ Issue #133 (комментарий, эпик не закрывать)
```

**DoD дня:** перенести capture path из `apps/client/src/plugins/mic-buffer-recorder/` в bridge; A/B smoke 60 s без артеfactов.

### Утро

```bash
yarn morning-care
yarn plan:day
yarn standup
yarn main-day-issue --focus db-recording-parity-mic-r5
```

### Перед PR

- Закоммитить незакоммиченное на `vesnin` (recording gate + bridge + v07 JSON)
- `yarn turbo run lint typecheck test --filter=@membrana/device-board --filter=@membrana/core --filter=@membrana/client`