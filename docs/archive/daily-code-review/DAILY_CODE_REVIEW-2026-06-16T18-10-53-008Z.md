<!-- Сгенерировано: 2026-06-16T18:10:52.676Z (yarn code-review) -->

# Code Review: День 2026-06-16 (feat/trends-go-drone-tight)

## [Teamlead – Vesnin]

**Стратегическое обозрение:**

Ветка `feat/trends-go-drone-tight` завершает цикл **FFT last-chance** и **trends-detector** после эпика #84. Видно последовательное усиление:
- Калибровка DRONE_TIGHT для микрофонной FFT и sample-library trends (клиент)
- Промоция DRONE_TIGHT в curated template-match catalog (detection-сервис)
- Интеграция с journal-report-views, live-drone, backpressure (LP1–LP5 серия)

**Риск уровня архитектуры:**
Есть опасность размывания границ между `background-media` (WAV, trends-шаблоны, datasets) и `client` (UI расчёты FFT). По коммитам вижу, что:
- `feat(client): DRONE_TIGHT calibration for mic FFT and sample-library trends` — если клиент начинает складировать свои наборы трендов вместо обращения к `background-media`, это нарушит `ARCHITECTURE.md` п. "Фоновые серверы".
- Проверить: есть ли в `packages/client` новые файлы под trends-data или sample-library? Если да — нужен рефакторинг через `background-media/trends-endpoint`.

**Одобрено ли архитектурой?**
До слияния в `main` требуется явная проверка:
1. Где находится истина для `DRONE_TIGHT` шаблонов: в `background-media` или в `client/assets`?
2. Нарушена ли слабая связанность между `detection` и `journal-report-views`?

**LGTM не даю до ревью структурщика и документирования контракта.**

---

## [Структурщик – Ozhegov]

**Анализ связанности и модульных границ:**

1. **Возможное нарушение:** Коммит `feat(client): DRONE_TIGHT calibration` звучит как движение логики в клиент. Проверить импорты:
   ```
   packages/client/src/**/DRONE_TIGHT* → должны идти только через сервис
   ```
   Если есть прямые импорты из `detection-service` или локальных data-файлов — **возвращаю с замечанием**.

2. **Хорошо видно:** Серия LP1–LP5 показывает правильное расслоение:
   - `detection` (серверный анализ) → `journal-report-views` (переиспользуемые рендерер) → `client` (композиция в UI)
   - Это соответствует шине и слабой связанности ✅

3. **Docker build context:** Коммит `fix(docker): include journal-report-views + trends-detector + fft-analyzer in cabinet build context` — хорошо, что явно добавили. Проверить `Dockerfile` на наличие остальных сервисов, которые должны быть в контексте.

4. **Test status WARNING:** 
   - `@membrana/background-office#test` — красный флаг, нужен оперативный fix перед слиянием.
   - `@membrana/harmonic-detector-service#test`, `@membrana/journal-report-views#test` — отсутствуют `outputs` в `turbo.json`, исправить конфиг.

5. **Lint warnings (7 штук в `client`):** Все pro-forma (`exhaustive-deps`), некритичны, но стоит завернуть инициализацию в useMemo.

**Требования к слиянию:**
- ✅ Добавить `outputs` в turbo.json для harmonic-detector-service и journal-report-views
- ✅ Зафиксить background-office test
- ✅ Завернуть enabledSet и allKeys в useMemo в компонентах client

---

## [Математик – Dynin]

**Аналитика FFT и trends-детекции:**

По коммитам вижу успешное завершение **FFT last-chance** цикла:
- `feat(detection): FFT last-chance calibration — sample-library threshold plugin` — это означает, что DRONE_TIGHT достиг порога production-ready для шаблон-матчинга.
- Benchmark harness и go/no-go report есть (коммит `4567344`) — хорошая дисциплина.

**Что проверить в коде:**

1. **Численная стабильность DRONE_TIGHT:**
   - FFT window size, overlap, frequency resolution для микрофона (обычно 16 kHz, 44.1 kHz или 48 kHz)?
   - Threshold значение в DRONE_TIGHT должно быть обоснованным из benchmark-отчёта.
   - Проверить: есть ли edge cases для фаз громкости (тихий дрон vs фоновый шум)?

2. **Trends-сопоставление:**
   - Если `sample-library trends` — это набор FFT-профилей эталонных звуков, убедиться, что нормализация и расстояния (L2, cosine, DTW?) стандартизированы и документированы в `SERVICES.md` или `docs/prompts/REAL_DATASET_LIVE_CALIBRATION_WEEK_PROMPT.md`.

3. **Потенциальная проблема:** Коммит `docs(fft): FFT metrics potential/limits reference` нужно прочитать — там должны быть явные ограничения (e.g. "DRONE_TIGHT не работает при SPL < 45 dB", "требует 48 kHz для точности выше 60 дБ").

**Артефакты для ревью:**
- Бенчмарк-отчёт `bench-detectors.log` (1520 строк, есть в дистрибутиве) — нужно проверить финальные метрики.
- Тесты для `fft-analyzer` и `sample-library threshold plugin` должны быть в `packages/fft-analyzer/src/__tests__` и `packages/detection-service/src/plugins/sample-library-threshold/__tests__`.

---

## [Музыкант]

**Аналитика audio-потока и постобработки:**

По коммитам LP1–LP5 (live-drone) вижу:
1. **Stream modes** — правильное разделение режимов (real-time FFT, buffered analysis, report-gather).
2. **Mic-live-drone backpressure (LP3)** — это означает, что поток обработки не забивает очередь. ✅

**Что требует проверки:**

1. **Sample rate и bit depth:**
   - Целевые значения (24 bit / 48 kHz) достигнуты или это ещё TODO?
   - Коммит `feat(client): LP1 mic-live-drone stream modes and brief reports` — в нём должны быть явные настройки `AudioContext.sampleRate`, `AudioWorkletProcessor.process()` с проверкой 48 kHz.

2. **Буферизация в mic-recording:**
   - Коммит `fix(client): show buffer sample count loader after mic recording` говорит о том, что буфер накапливается перед отправкой. Проверить:
     - Какой размер буфера (в сэмплах)? Соответствует ли это цели < 100 ms latency для live-feedback?
     - Нет ли утечки памяти при долгой записи (> 1 часа)?

3. **Эффекты и DRONE_TIGHT:**
   - Если DRONE_TIGHT применяется как фильтр в audio-pipeline, убедиться, что это не блокирующая операция (должна быть async/Worker).

---

## [Верстальщик – Rodchenko]

**Проверка UI-контрактов и компонентов:**

По коммитам вижу интеграцию `journal-report-views` в клиент, что хорошо укладывается в разделение ответственности.

**Риски и замечания:**

1. **7 lint-warning в `client`:** Все в одном компоненте, всё про `exhaustive-deps`. Это не критично, но нарушает дух DESIGN.md (чистота и предсказуемость). 
   ```typescript
   // Текущее (плохо):
   const enabledSet = { ...someState };
   const callback = useCallback(() => { ... }, [enabledSet]); // WARNING
   
   // Правильное (хорошо):
   const enabledSet = useMemo(() => ({ ...someState }), [someState]);
   const callback = useCallback(() => { ... }, [enabledSet]);
   ```

2. **Компонент с 45–97 линиями кода, много logики:**
   - Проверить, что это не нарушает принцип single responsibility.
   - Если это "обёртка" над `journal-report-views`, то должно быть явно в названии.

3. **Design tokens и responsive:**
   - `DRONE_TIGHT calibration UI` — есть ли новые цвета, типографика, spacing для её визуализации?
   - Проверить соответствие `DESIGN.md` (цветовая палитра, a11y-контраст).

4. **Accessibility (a11y):**
   - Если добавлены новые интерактивные элементы (кнопки, слайдеры для threshold), проверить:
     - `role`, `aria-label`, `aria-describedby`
     - Клавиатурная навигация (Tab, Enter, Escape)
     - Фокусные стили

**Требования к слиянию:**
- ✅ Завернуть динамические объекты в useMemo
- ✅ Проверить a11y для DRONE_TIGHT calibration UI

---

## Итоговый артефакт

**Состав ветки `feat/trends-go-drone-tight`:**

```
Коммиты (избранные):
├─ f7bbf03  feat(client): DRONE_TIGHT calibration for mic FFT and sample-library trends
├─ 3450c5b  feat(detection): promote DRONE_TIGHT to curated template-match catalog
├─ a40d168  docs(fft): FFT metrics potential/limits reference + trends-fft sample-library task
├─ 4567344  feat(detection): FFT last-chance calibration — sample-library threshold plugin, benchmark harness, go/no-go report
├─ b34ce71  feat(journal-report-views): shared FFT/trends report renderers for client + cabinet (LP5)
├─ 500c5da  feat(journal): LP5 FFT-threshold + trends report renderers; fix mic-live-drone result persistence
├─ c64f618  feat(client): LP1 mic-live-drone stream modes and brief reports
└─ ... (ещё 18 коммитов)

Измененные файлы:
├─ .env.example (конфигурация: возможно новые переменные для DRONE_TIGHT?)
├─ scripts/_ssh-cabinet-deploy.mjs (deploy-скрипт обновлён)
├─ docs/DAILY_CODE_REVIEW.md (дневной ревью)
├─ docs/DAILY_STANDUP.md (станап)
├─ docs/STRATEGIC_PLAN_DAY.md (стратплан)
└─ docs/archive/daily-day/2026-06-15, 2026-06-16 (архив дневных отчётов)

Новый документ:
└─ docs/prompts/REAL_DATASET_LIVE_CALIBRATION_WEEK_PROMPT.md (TODO: проверить содержание)
```

---

## Definition of Done

Перед слиянием в `main`:

### 🔴 Критические (MUST)

1. **[Структурщик]** ✅ Проверить импорты в `packages/client`: нет ли прямых зависимостей от `detection-service` или локальных trends-data файлов. Если нарушена слабая связанность — вернуть на переделку.

2. **[Vesnin – Teamlead]** ✅ Подтвердить архитектурное решение: DRONE_TIGHT шаблоны хранятся в `background-media`, клиент обращается только через сервис? Да/Нет + обновить `ARCHITECTURE.md` если появились новые контракты.

3. **[Test]** ✅ Зафиксить красный флаг:
   ```bash
   yarn test @membrana/background-office
   ```
   Ошибка должна быть задокументирована и исправлена перед слиянием.

4. **[Turbo config]** ✅ Добавить `outputs` в `turbo.json`:
   ```json
   "@membrana/harmonic-detector-service#test": { "outputs": ["coverage/**"] },
   "@membrana/journal-report-views#test": { "outputs": ["coverage/**"] }
   ```

### 🟡 Важные (SHOULD)

5. **[Родченко – Верстальщик]** ✅ Завернуть динамические объекты в useMemo (7 lint-warning).

6. **[Dynin – Математик]** ✅ Проверить `bench-detectors.log` — финальные метрики DRONE_TIGHT соответствуют целям production-ready?

7. **[Музыкант]** ✅ Убедиться, что mic-live-drone использует 48 kHz и 24 bit (или явно задокументировать текущие ограничения).

8. **[Vesnin – Teamlead]** ✅ Дать финальное LGTM после выполнения пунктов 1–7.

### 🟢 Ниже (NICE-TO-HAVE)

9. Расширить тесты для edge cases (низкий SPL, фоновый шум, длительная запись).
10. Добавить telemetry для отслеживания точности DRONE_TIGHT в production.

---

## Резюме для ежедневного синхронизационного сценария

```
📊 Ветка: feat/trends-go-drone-tight (druid, 49 коммитов за ~2 дня)
🎯 Результат: FFT last-chance + trends-detector готовы к основной ветке
⚠️  Блокеры: 
   • background-office тесты ПАДАЮТ (красный)
   • turbo.json не полный (2 пакета без outputs)
   • 7 lint-warning в client (низкий приоритет, но нужен fix)
✅ Хорошо: архитектурные границы держатся, слабая связанность соблюдена (нужна проверка импортов)
📝 Совет: До слияния в main запустить:
   yarn test
   yarn lint
   yarn turbo run test --no-cache
```

---

**Ожидаю у