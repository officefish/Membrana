<!-- Сгенерировано: 2026-07-04T20:42:16.258Z (yarn code-review; daily) -->

# Code Review — Вечер 2026-07-04

**Tier: T1**

---

## [Teamlead]:

День завершён на спринте **presence-capture-board** (PCB1–PCB5). Сегодня merged три PR: PCB1 (WS-логи), PCB3 (лiveness-indicator + fix persistent-offline петли), PCB5 (force-open view-only при захвате). Граница пакетов соблюдена: `packages/client` (UI) + `packages/services/presence-capture` (wire). Текущая ветка `feat/pcb5-force-viewonly-board` содержит коммит 27624fce без lint-блокеров (только 2 предупреждения в `UserCaseSettingsPanel.tsx` про `useMemo` зависимости — рекомендация на завтра).

**На завтра утром:**
```bash
yarn lint --filter=@membrana/client  # разобрать 2 warning'а useMemo в UserCaseSettingsPanel
yarn typecheck --filter=@membrana/client
yarn test --filter=@membrana/client -- src/modules/device-board/
```

Завтра планируется старт **CT0** (device-board capture tariff v2) и обработка вчерашнего консилиума. Риски минимальны; CI зелёный.

---

## [Структурщик]:

PCB5 коммит — локальное изменение в `device-board-session` (открытие board.viewOnly при захвате). Граница между `presence-capture` (wire, регистрация узла) и `device-board` (UI state) соблюдена через `PresenceService` → `CabinetRegistry` (MembranaRegistry pattern). Тесты `device-board-session.test.ts` проходят (1 test, 2ms). Нет циклических зависимостей, импорты линейны.

Рекомендация: перед merge PCB5 PR убедиться, что `UserCaseSettingsPanel.tsx` deps устранены (lint-stage должен ловить на pre-commit). Сейчас — 2 warning'а, которые CI пропускает, но best practice — исправить.

---

## [Математик]:

—

---

## [Музыкант]:

—

---

## [Верстальщик]:

`UserCaseSettingsPanel.tsx` (PCB5): вводит условный render view-only при `capturedBy !== currentUser`. Стилей новых нет; используется существующие `aria-*` атрибуты. Иконка «Завод» (lock-ish) в caption — по DESIGN.md §captured-indicator. A11y корректна.

---

## Итоговый артефакт:

`docs/DAILY_CODE_REVIEW.md` (сохрана автоматически).

---

## Definition of Done (утро):

```bash
yarn lint --filter=@membrana/client --fix  # попробовать авто-fix useMemo
yarn typecheck --filter=@membrana/client
yarn test --filter=@membrana/client
yarn turbo run lint typecheck test --filter='@membrana/client|@membrana/services-presence-capture'
```

Если lint не исправляется — ручной PR на завтра (мин issue).

---

## Риски:

**P0/P1:** —  
**P2 (opportunity):** lint warning в `UserCaseSettingsPanel.tsx` — добавить dependency или переделать selector (future refactor).

---

## Вердикт:

**LGTM merge PCB5** после lint-fix (yarn lint --fix). Спринт PCB на финише; готов CT0 старт.