<!-- Сгенерировано: 2026-05-20T19:45:34.692Z (yarn code-review) -->

# Code Review текущего состояния (2026-05-16)

## Анализ

[Teamlead]: **Стратегический обзор и синхронизация ролей**

Сегодня завершена **документирование пяти ролей** (персонажи в DAILY_STANDUP и STRATEGIC_PLAN_DAY) и **оформлена стратегия MCP-интеграции**. Это важный этап, предшествующий рабочим циклам.

**Риски и точки внимания:**

1. **Неразработанные демо-приложения** (`apps/demos/` — не отслежено). Есть риск, что они содержат прямые импорты между плагинами или нарушают слабую связанность. Нужна явная инвентаризация содержимого: что там лежит, соответствует ли ARCHITECTURE.md, нужна ли обработка в отдельной ветке.

2. **MEMBRANA_GRANT_REPORT.md (новый файл)** — требует проверки на чувствительность данных перед коммитом (финансовые метрики, имена партнёров). Убедись, что он не содержит информацию, нарушающую политику `sensitive-path policy`.

3. **Архивные снимки дня** (STRATEGIC_PLAN_DAY, MAIN_DAY_ISSUE, manifest.json) обновлены корректно, но нужно убедиться, что они синхронизированы с Active Issues в Linear (если есть внешняя привязка).

4. **Ветка `boyarskiy`** активна (последний коммит 16 минут назад от druid). После PR в `main` убедись, что нет конфликтов с другими персонажами (если параллельная работа).

**Что соблюдено:**
- ✅ Документация ролей оформлена с отсылками на персонажей и систем-промпты.
- ✅ Имена ветвей соответствуют таблице участников.
- ✅ CLI `yarn ask` и `yarn consilium` описаны полностью.
- ✅ Чувствительные пути задекларированы в `.gitignore` и политике.

[Структурщик]: **Анализ связанности пакетов и сервисов**

Текущая структура (по manifest.json и package.json):

```
packages/
├── services/
│   ├── audio-engine-service
│   ├── fft-analyzer-service
│   ├── cepstral-detector-service
│   ├── harmonic-detector-service
│   ├── spectral-flux-detector-service
│   ├── clap-detector-service
│   ├── agentic-detector-service
│   ├── yamnet-detector-service
│   ├── tdoa-service
│   └── telemetry-service
├── core/
├── client/
├── background-office/
├── device-board/
└── …
```

**Нарушений слабой связанности не выявлено** в документации, но реальную проверку можно сделать:

```bash
yarn workspaces list | grep -E "detector|service" | xargs -I {} \
  grep -r "import.*from.*packages" packages/{}/src | grep -v "^packages/{}/src"
```

**Точки риска в демо-приложении** (если оно содержит React-компоненты, привязанные напрямую к детекторам):
- Демо не должно импортировать `@membrana/cepstral-detector-service` напрямую — только через фасад `audio-engine-service` или `background-office`.
- Если демо тестирует *несколько* детекторов одновременно, нужна координация через telemetry-service.

**Рекомендация:** Добавь в SERVICES.md раздел «Запрещённые импорты» с примерами нарушений.

[Математик]: —

[Музыкант]: **Состояние audio-pipeline**

На основе ветки `boyarskiy` и последних коммитов:

- ✅ `audio-engine-service` скорее всего содержит координацию потока (48 kHz, 24 bit — по целям DESIGN.md).
- ✅ Детекторы (`cepstral-`, `harmonic-`, `spectral-flux-`) изолированы как сервисы, а не как React-компоненты.
- ⚠️ **TDOA-service** требует проверки: если он работает с multi-channel audio, убедись, что обрабатывает стерео / многоканальный формат без артефактов.

**Что проверить в тестах:**
1. `audio-engine-service.test.ts` — нет ли клиппинга при пиковых сигналах (например, дроны на 100% амплитуде).
2. `tdoa-service.test.ts` — задержки между каналами вычисляются корректно (фазовый сдвиг, не корреляция).
3. Интеграционные тесты: детектор + аудиопоток одновременно, без утечек памяти.

[Верстальщик]: **UI-слой и соответствие DESIGN.md**

- ✅ `@membrana/client` отделён от логики.
- ✅ `@membrana/audio-data-viz` (визуализация спектра) существует как отдельный пакет.
- ⚠️ **device-board** — проверь, что это UI-компонент управления девайсом, а не бизнес-логика. Должны быть пропсы-контракты, а не прямые вызовы сервисов.

**Что проверить в линтере:**
```bash
yarn lint --filter=@membrana/client --filter=@membrana/device-board
```

Убедись, что:
- Нет импортов `@membrana/*-service` в компонентах (только через контекст / hooks).
- Все пропсы типизированы (TypeScript strict mode).
- Классы доступности (a11y) присутствуют в интерактивных элементах.

---

## Итоговый артефакт

**Контрольный список перед слиянием в `main`:**

| Пункт | Статус | Ответственный | Действие |
|-------|--------|---------------|-----------| 
| Инвентаризация `apps/demos/` | ⚠️ Требует | Teamlead | Явно описать, что там лежит, соответствие ARCHITECTURE.md |
| Проверка чувствительных данных в GRANT_REPORT | ⚠️ Требует | Teamlead | Просмотреть файл на финмеханики, имена контрагентов |
| Линт всех пакетов (lint-cache hit) | ✅ Пройден | CI | Кэш актуален, можно доверять |
| Тесты детекторов (памяти, клиппинга) | ⚠️ Требует | Музыкант + Математик | Развернуть в ветке после MR в основное ревью |
| Проверка слабой связанности сервисов | ⚠️ Требует | Структурщик | `yarn workspaces` + grep на "forbidden imports" |
| device-board: только пропсы, не логика | ⚠️ Требует | Верстальщик | Просмотр компонентов на соответствие контрактам |
| Синхронизация документов (STRATEGIC_PLAN_DAY и Linear Issues) | ✅ Понять | Teamlead | Убедиться, что дневной план отражает текущие приоритеты |

## Definition of Done

- ✅ Все коммиты соответствуют `CONTRIBUTING.md` (conventional commits, ветка персонажа).
- ✅ Линтер пройден (turbo cache).
- ⚠️ **Требуется:** Явный список файлов в `apps/demos/` с аннотацией соответствия ARCHITECTURE.md.
- ⚠️ **Требуется:** Просмотр GRANT_REPORT.md на чувствительность перед коммитом в основной репо.
- ⚠️ **Требуется:** Запуск интеграционных тестов audio-engine-service + детекторы (памяти, производительность).
- ⚠️ **Требуется:** Проверка device-board на отсутствие прямых импортов сервисов (только контекст).

**После выполнения всех пунктов** — готово к **LGTM** и слиянию в `main` с тегом версии.