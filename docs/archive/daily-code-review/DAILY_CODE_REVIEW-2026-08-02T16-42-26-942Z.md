<!-- Сгенерировано: 2026-08-02T16:42:26.767Z (yarn code-review; daily, llm-anthropic) -->

> Контур ревью (rt-8):
> Режим: работа дня
> Precision: exact
> Период: 35c0e9378b7f667e5babf29d6f8d2c4fc5052222^..1672ffc6f96912556b213ccdd7a51196133b3cd2 (16 коммит(ов))
> ⚠ Oversized (>400 строк, дифф не развёрнут — ревьюить отдельно): 9dc167d0 #1630 (1034), 3bc7cda5 #1634 (763), f7337aec #1635 (1028), a4766257 #1636 (1094), 618ca3d5 #1639 (948), 4388f415 #1642 (5369), e6c04420 #1640 (1620), 387e35e3 #1645 (1136), 40468d1d #1650 (2602), 1672ffc6 (815)

---

Tier: T1

---

## Живые состояния PR (сверено с таблицей фактов)

#1630 MERGED · #1631 MERGED · #1632 MERGED · #1633 MERGED · #1634 MERGED · #1635 MERGED · #1636 MERGED · #1639 MERGED · #1640 MERGED · #1642 MERGED · #1644 MERGED · #1645 MERGED · #1646 MERGED · #1650 MERGED · #1652 MERGED

---

**[Vesnin — Архитектор (ведущий ревью)]:**
Скоуп дня — исключительно `docs/`, `scripts/lib/`, `kits/` и корневой `turbo.json`; пути `packages/core`, `MembranaRegistry`, `audio-engine` не затронуты → T2 не нужен. Три архитектурных события заслуживают фиксации. Первое (P1): `PERSONA_ROLE_LABELS` шесть дней держала тимлида вне карты после ротации ролей — дефект обнаружен и закрыт в #1644, зуб `persona-memory-roster.test.mjs` поставлен; это образцовый сценарий «обнаружение через сбой → зуб → закрытие», держать как прецедент. Второе (#1632): вынос `prisma:generate` в отдельную turbo-задачу с `dependsOn` — корректное решение гонки; убедиться, что `background-cabinet/turbo.json` и `background-media/turbo.json` не расходятся со стратегией кэширования корневого `turbo.json` (сейчас визуально идентичны — ок). Третье: два новых инсайта (`insight-owner-intervention-ledger`, `insight-server-only-ritual-run`) и одна запись реестра правды (`depot-tasks-are-a-subset-not-a-second-registry`) — все три добавлены как `draft`/`active` без заполненных research-блоков; это норма для черновиков, но `insight-server-only-ritual-run` называет #1649 предусловием server-only прогона — если задача #1649 не имеет открытого иссью или карточки, пропасть останется невидимой. C1 — пересечений между пакетами в диффе нет. C10 — `docs/insights/registry.json` синхронизирован с папками инсайтов. Ведущий вердикт: **пропуск** (блокеров нет).

**[Tarasov — Teamlead]:**
PR size: день oversized суммарно, но это известное поведение 02.08 — десять из шестнадцати коммитов помечены как самостоятельные для отдельного ревью; в сегодняшнем ревью они не развёрнуты и не оцениваются. По развёрнутым шести: C8 — `console.error` в `code-review.mjs` и `review-referenced-states.mjs` используется штатно как диагностический поток (не production-log), нарушения нет. C9 — секретов в диффе нет; `env.snapshot.json` содержит только задержки и `outcome`, ключей нет. C10 — реестры (`insights/registry.json`, `tasks/registry.json`, `tasks/README.md`) синхронизированы. Незакрытое: lint и test CI красные по нескольким пакетам (`@membrana/panel`, `@membrana/core`, `@membrana/rag-service` и другие) — это не следствие сегодняшнего диффа (все затронутые файлы — docs/scripts), но красный CI остаётся красным. Утром: прочитать `DAILY_CODE_REVIEW.md`; проверить статус #1641 (open) и #1649 (open) — оба упомянуты в инсайтах как блокирующие следующий шаг.

**[Ozhegov — Структурщик]:**
`review-referenced-states.mjs` — чистая инъекция `fetchStates` без импорта сети в ядро; контракт синхронный с явной защитой от Promise; тесты покрывают живой случай 01–02.08 дословно. Один нит (P2): в `buildReferencedStatesBlock` потолок `MAX_REFERENCED` жёстко зашит внутри `referencedNumbers`, но передаётся через `opts` в `buildReferencedStatesBlock` без проброса в `referencedNumbers` — `opts` до `referencedNumbers` доходит, но вызывающий `buildReferencedStatesBlock` не может снизить потолок ниже `MAX_REFERENCED`, только выше (что `Math.min` обрезает). Поведение корректное, но сигнатура `opts.max` создаёт ложное ожидание управляемости. C4 — сервисная логика без React, хуки не задействованы.

**[Dynin — Математик]:**
`referencedNumbers` — regex `/(^|[\s(«"'`,;:])#(\d{2,6})\b/gu` корректно отсекает однозначные номера (`#7`) и URL-пути (`/pull/1562`). Граница `\d{2,6}` исключает шестизначные `#100000` — если реестр иссью когда-либо превысит 99999, функция молча перестанет их находить; для текущего масштаба (~1650) не критично, но стоит задокументировать как осознанное ограничение (P2, opportunity). Арифметика potолка: `Math.max(1, Math.min(Number(max) || MAX_REFERENCED, MAX_REFERENCED))` — устойчива к `NaN` и `Infinity`, тест это подтверждает.

**[Kuryokhin — Музыкант]:**
— (аудио-путь не затронут)

**[Rodchenko — Верстальщик]:**
— (UI не затронут; `docs/` — только markdown, a11y не применимо)

---

**Итоговый артефакт:** `docs/DAILY_CODE_REVIEW.md`

**Definition of Done (утро):**
```bash
# Убедиться, что fix prisma-гонки не сломал кэш:
yarn turbo run build --filter=@membrana/background-cabinet --filter=@membrana/background-media

# Проверить зуб ростера памяти:
node --test scripts/persona-memory-roster.test.mjs

# Проверить зуб review-referenced-states:
node --test scripts/lib/review-referenced-states.test.mjs

# Посмотреть статус открытых иссью-предусловий:
# #1641 (honest_pair при одном следе) — open
# #1649 (журнал прогонов без производителя) — open; предусловие server-only опыта
```

**Риски:**
- **P1** — #1649 (журнал прогонов без производителя) открыт и назван предусловием `insight-server-only-ritual-run`; пока не закрыт, server-only опыт не имеет наблюдаемости и запускать его бессмысленно.
- **P2** — красный CI (`@membrana/panel`, `@membrana/core`, `@membrana/rag-service` и ещё пять пакетов lint/test) — происхождение не в сегодняшнем диффе, но завтра нужно установить, чьи это хвосты.
- **P2** — граница `\d{2,6}` в `referencedNumbers` — осознанное ограничение, требует комментария в коде.