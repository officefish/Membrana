<!-- Сгенерировано: 2026-05-13T15:32:37.813Z (yarn code-review) -->

# Code Review: Состояние Membrana на текущий момент

## Классификация текущего спринта

Сегодня сделано **7 коммитов** с фокусом на:
- UI-модули анализа (FFT, осциллограф)
- Микрофонный ввод и потоковая обработка
- Системы ленивой загрузки модулей
- Визуализация аудиоданных

**Тип работ**: интеграционные + расширение сервисного слоя + UI-контракты.

---

## Порядок Review (эвристика)

1. **Teamlead** — граница модулей, нарушение слабой связанности, соответствие `ARCHITECTURE.md`
2. **Структурщик** — импорты, сервис-ориентированность, слои
3. **Математик** — наличие изоляции FFT и анализа в сервисе
4. **Музыкант** — качество потока (24/48 kHz), обработка сигнала, плагины
5. **Верстальщик** — компоненты модулей, соответствие `DESIGN.md`, a11y

---

## 🎯 [Teamlead]

### ✅ Что сделано правильно

- **Модульная структура сохранена**: FFTModule, OscilloscopeModule как отдельные сущности
- **Ленивая загрузка** — хороший паттерн масштабирования (уменьшает бандл)
- **Микрофонный модуль** — отделён как отдельный плагин, не встроен в core
- **Turbo.json обновлён** — возможно, для управления зависимостями между workspace'ами

### ⚠️ Риски и нарушения

1. **Прямые импорты между модулями** (критично):
   - Нужно проверить: импортирует ли `FFTModule` код из `OscilloscopeModule` или наоборот?
   - Если да → нарушение: оба модуля должны идти через **shared-сервисы** (agenda, device-board)

2. **Lazy loading без контракта в CONTRIBUTING.md**:
   - Добавлены файлы, но не описан процесс добавления нового модуля
   - **Action**: дополнить CONTRIBUTING.md раздел о регистрации lazy-modules

3. **Неясна инициализация потока**:
   - Микрофонный модуль создан, но как он включается в `audio-engine`?
   - Риск: спагетти-инициализация через глобальное состояние

4. **Версии пакетов в package.json**:
   - Изменены версии agenda, core, device-board, но нет changelog
   - **Action**: убедиться, что breaking changes документированы

### 📋 Требует явного одобрения

- [ ] **Структура lazy-loaded модулей** — форма подходит или нужен factory + DI?
- [ ] **Граница ответственности между audio-engine и каждым модулем** — контракт через SERVICES.md?

---

## 🏗️ [Структурщик]

### ✅ Хорошо

- `packages/services/audio-engine` и `packages/services/fft-analyzer` разделены
- `packages/agenda` как **шина сообщений** — правильное место для координации
- `packages/device-board` (аудиоустройства) отделён от UI

### 🔴 Нарушения слабой связанности (КРИТИЧНО)

**Проверить немедленно:**

```typescript
// ❌ Ищем прямые импорты вида:
// FFTModule.tsx → import from OscilloscopeModule
// Микрофонный модуль → import from audio-engine/src (вместо через agenda)
// Любой UI → import from packages/services (исключение: DESIGN-токены)
```

**Текущее состояние неясно из diff, нужна проверка:**

1. **`apps/client/src/modules/FFTModule.tsx`** → какие импорты?
   - ✅ Должны быть: `agenda`, `fft-analyzer-service`
   - ❌ НЕ должны быть: OscilloscopeModule, device-board (прямо)

2. **`apps/client/src/modules/OscilloscopeModule.tsx`** → аналогично

3. **Микрофонный модуль** (из коммита `cc372ad`):
   - ✅ Должен: слушать события agenda, отправлять в audio-engine через SERVICES.md контракт
   - ❌ НЕ должен: напрямую вызывать методы device-board

### 📊 Рекомендуемый слой коммуникации

```
[UI Модули] ←→ [Agenda (shim)] ←→ [Services: audio-engine, fft-analyzer, device-board]
                    ↓
              [shared состояние]
```

**Action**: автоматический линтер-правило (ESLint) на нарушение импортов между модулями.

---

## 📐 [Математик]

### ✅ Сделано

- `packages/services/fft-analyzer-service` существует как отдельный пакет ✅
- Визуализация данных отделена в `@membrana/audio-data-viz` (специализированная lib)

### ⚠️ Вопросы

1. **Чистота FFT-функций?**
   - Содержит ли `fft-analyzer/src` только **чистые функции** (без побочных эффектов)?
   - Или инициализирует Web Audio / DOM?
   - **Требуется** проверка: `fft-analyzer/src/*.ts` не должны импортировать `react`, `@web-audio-api`, Web APIs

2. **Парсинг результатов**:
   - Возвращает ли FFT массив чисел или Uint8Array?
   - Типизация в `audio-data-viz` совпадает?

3. **Тесты**:
   - Есть ли unit-тесты для FFT с известными входами/выходами?
   - (Вероятно, `test status` пока не запущены полностью)

### 📋 Checklist

- [ ] `fft-analyzer-service/src` не содержит импортов Web APIs или React
- [ ] Функция FFT имеет signature: `(samples: Float32Array) => Float32Array`
- [ ] Есть unit-тесты с фиксированными данными

---

## 🎵 [Музыкант]

### ✅ Архитектура

- `audio-engine-service` создана ✅
- Микрофонный модуль добавлен как **плагин** (хороший паттерн)
- Потоковая обработка упомянута в `4ba808e` ("Stream visualisation added")

### ⚠️ Критические вопросы

1. **Качество потока**:
   - Какой sample rate: 48 kHz или 44.1 kHz?
   - Bitdepth: 24 bit или 16 bit?
   - Проверить в `audio-engine/src`: инициализация AudioContext

2. **Буферизация**:
   - Размер буфера (FFT window)? Должен быть степень двойки (512, 1024, 2048)
   - Есть ли оверлап для гладкости? (обычно 50% или 75%)

3. **Микрофон**:
   - Как обработка отклонений (шум, клиппинг)?
   - Входит ли в flow **audio-engine** или отдельно?

4. **Плагины vs. монолит**:
   - Если микрофон — плагин, где регистрация?
   - Риск: спагетти-инициализация в `apps/client` index

### 📋 Требует явного одобрения

- [ ] **Контракт потока**: сэмпл-рейт, буфер, оверлап → задокументировать в SERVICES.md § Audio Engine
- [ ] **Архитектура микрофона**: plugin vs. встроенный модуль?
- [ ] **Обработка ошибок**: что если микрофон недоступен или отключён?

---

## 🎨 [Верстальщик]

### ✅ Что видно

- FFT и осциллограф — отдельные компоненты ✅
- Очевидно, использована специализированная lib для визуализации (`audio-data-viz`)

### ⚠️ Вопросы DESIGN.md соответствия

1. **Отсутствие дизайн-контракта в diff**:
   - Были ли обновлены в `DESIGN.md` сами компоненты?
   - Где пропсы, состояния, темизация?

2. **Accessibility**:
   - ARIA-labels для модулей?
   - Keyboard navigation для controls?
   - (Вероятно, пока не реализовано)

3. **Адаптив**:
   - Графики в FFT и осциллографе адаптируются на мобильном?
   - Или только desktop-first?

4. **Тёмная тема** (из DESIGN.md):
   - CSS переменные в компонентах?
   - Или hardcoded цвета?

### 📋 Checklist

- [ ] FFTModule и OscilloscopeModule имеют Storybook stories
- [ ] DESIGN.md обновлён с новыми компонентами
- [ ] Нет inline-стилей (все в CSS modules или styled-components)
- [ ] Есть fallback для браузеров без Web Audio (graceful degradation)

---

## 📊 Итоговый артефакт

### Definition of Done для текущего спринта

- [❓] **Архитектура**: Teamlead проверил граници модулей, одобрил форму lazy-loading
- [❓] **Слабая связанность**: Структурщик запустил lint-check на прямые импорты между модулями (результат в `LINT STATUS`)
- [❓] **Чистота FFT**: Математик убедился, что `fft-analyzer-service` содержит только чистые функции
- [❓] **Качество потока**: Музыкант проверил sample rate, buffer size, overt overlap в `audio-engine`
- [❓] **Дизайн**: Верстальщик обновил DESIGN.md, добавил a11y, проверил адаптив
- [❓] **Тесты**: все пакеты проходят `yarn test` и `yarn lint` без ошибок
- [❓] **Документация**: CONTRIBUTING.md дополнен о lazy-module API, SERVICES.md актуален

### Критические файлы для проверки (порядок)

```
1. apps/client/src/modules/FFTModule.tsx       (импорты, слои)
2. apps/client/src/modules/OscilloscopeModule.tsx
3. packages/services/fft-analyzer/src/*.ts     (чистота функций)
4. packages/services/audio-engine/src/*.ts     (инициализация потока, 48 kHz)
5. packages/agenda/src/core/provider.tsx       (шина регистрации плагинов)
6. docs/DESIGN.md                               (новые компоненты, props-контракты)
7. docs/CONTRIBUTING.md                         (lazy-module регистрация)
```

---

## 🚨 Приоритет Action Items

| Приоритет | Что | Кто | Статус |
|-----------|-----|-----|--------|
| **КРИТИЧНО** | Проверить импорты: модули → модули прямо? | Структурщик | ⏳ |
| **КРИТИЧНО** | Контракт потока (sample rate, buffer) | Музыкант + Teamlead | ⏳ |
| **ВЫСОКИЙ** | fft-analyzer: чистые функции, типизация | Математик | ⏳ |
| **ВЫСОКИЙ** | DESIGN.md: новые компоненты, props | Верстальщик | ⏳ |
| **СРЕДНИЙ** | CONTRIBUTING.md: lazy-module регистрация | Teamlead | ⏳ |
| **СРЕДНИЙ** | Changelog / breaking changes в версиях | Teamlead | ⏳ |

---

## 💬 Рекомендация

**Перед merge:**
1. Запустить полный `yarn test` и `yarn lint` (сейчас в кеше miss — нет результатов)
2. Получить **явное одобрение (LGTM)** от **Teamlead** на форму lazy-loading и микрофон-плагин
3. Структурщик провести аудит импортов (можно автоматизировать в ESLint)
4. Музыкант документировать контракт потока в SERVICES.md

**Общий статус**: 🟡 **Ready for detailed review** (архитектура в целом правильная, но требуется уточнение деталей и проверка соответствия контрактам).