<!--
  archive-role: archive-snapshot
  archive-day: 2026-08-29
  archived-at: 2026-08-29T15:32:57.521Z
  source: docs/DAY_PLAN.md
  canonical: docs/DAY_PLAN.md (перезаписывается yarn plan:day / standup / main-day-issue)
  Не использовать как основной документ дня — побочный снимок для ретроспективы и анализа.
-->

<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: 75eca5fa9c48c4247dd3f2e56a90e25e3549f09b9fd67ff71d8bf66692a3b597 · signedAt: 2026-08-29T08:24:13.248Z -->
# План дня — 2026-08-29

## Магистраль
- **angelina-hostess-impl** (L) — закрывает разрыв «заявленная команда / нет субъекта действия»: hostess как вызываемый носитель дневного контура; L-размер и guard `angelina` на горизонте дня делают её естественным хребтом сессии, а не боковым шотом.
- **assets-container** (L) — собирает хранение в одно продуктовое обещание (детекция первична, записи вспомогательны, буфер живёт сортировкой); после вчерашнего `buffer-control` годится магистралью, потому что без контейнера активов квота и ротация остаются разрозненными хвостами.
- **batch-collection-run-contour** (L) — замыкает контур пакетного сбора/прогона коллекций вместо разовых ручных проходов; тянет на магистраль как L-контур с измеримым «один датированный проход», сонаправленный вехе `secret-parser-built` (манифест/проход, не точечный фикс).

Выбор магистрали — слово владельца (owner-choice)

## Подкрепление
- Черновик датированного манифеста ротации засвеченных ключей (критерий гейта `secret-parser-built`) — усиливает магистраль к вехе: резак без прохода с манифестом гейт не закрывает.
- Контур «резак до бэкапа»: сверка, что `night-triage-secret-scan` режет до архива сессий и не читает бэкап как вход (кристаллы `secret-parser-cuts-aggressively`, `session-backup-requires-secret-redaction`) — снимает риск нарушить амнистию/архив при агрессивном cut.

## Перспективные
- Прохождение гейта `secret-parser-built` (резак + датированный манифест ротации) снимает амнистию на правку архива и открывает безопасный бэкап сессий с вырезкой секретов на следующие дни.
- Доказанный стоп буфера по remaining (#2229 ↔ DoD п.3) открывает следующее дежурство без риска переполнения квоты record-сценарием.
- Закрытие калибровки сторожа и разгрузки буфера до дежурства открывает ночной контур записи с живыми порогами, а не опорными константами.

## Экспериментальные
- Прогон `night-triage-secret-scan` на одном синтетическом фикстуре с заведомо ложными ключами: узнаем, режет ли резак контекст вокруг секрета или только паттерн, не трогая прод-архив.
- Сверка `buffer-stop.ts` → сигнал в `MicBufferRecorderPanel` одним чтением графа импортов: узнаем, доходит ли remaining-стоп до UI без нового кода или разрыв уже есть.
- Одна фейковая тревога сторожа диска в личный чат с поддельным remaining: узнаем, хватает ли текущего канала уведомлений для «активной тревоги сверх журнала» или нужен отдельный род.

## Санитарные
- media-тесты `#2204`: residual RED с 27.08 — `turbo test` media-library-service + background-media → green или fail-log в issue
- сверка `#2229` / `buffer-stop.ts` с DoD п.3: remaining + порог + сигнал наружу; иначе gap-таблица в `#2204`
- живое состояние `#2199`: в таблице ещё OPEN при влитом `#2217` — `gh pr view`, закрыть хвост учёта
- oversized-ревью: `#2221` (707), 531, 609 — очередь P1, не разобраны
- помеха прогонов: зонтик `#2204` размыл носитель стоп-предиката — вынести факт «есть/нет» до любых L-кандидатов

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
