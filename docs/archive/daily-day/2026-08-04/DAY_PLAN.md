<!--
  archive-role: archive-snapshot
  archive-day: 2026-08-04
  archived-at: 2026-08-04T23:23:53.536Z
  source: docs/DAY_PLAN.md
  canonical: docs/DAY_PLAN.md (перезаписывается yarn plan:day / standup / main-day-issue)
  Не использовать как основной документ дня — побочный снимок для ретроспективы и анализа.
-->

<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: e9d3cab952b8eae9263a8c6dc05ff50a15f7649af0074500426a1d96aeb60693 · signedAt: 2026-08-04T04:46:14.682Z -->
# План дня — 2026-08-04

## Магистраль
- **agent-tooling-night-build** — закрывает боль «воспоминание выдаёт себя за факт»: ночной прогон сборки тулинга даст честный снимок сети и CI-зелень до старта сессии; размер L и инструментальный характер совпадают с вчерашним днём, когда такой же слой дал максимум.
- **angelina-hostess-impl** — закрывает боль «процедура разворачивания не имеет владельца»: имплементация hostess-роли Angelina делает текущую сессию субъектом, способным исполнить магистраль дня, а не только спланировать; L-размер уместен после плотного инструментального дня.
- **archivarius-sessions-container** — закрывает боль «сырые транскрипты на сервер не уходят»: контейнер архивации — прямой разблокер после гейта `secret-parser-built`, у него полная предусловная цепочка, и вечерний фидбек называет его единственной задачей с готовым входом.
- Выбор магистрали — слово владельца (owner-choice).

## Подкрепление
- **Проверка `assertions[]` через `yarn main-day-probe`** — закрывает открытый пункт DoD, не давая вердикту дня остаться честным наполовину; подкрепляет магистраль `deploy-procedure-survey`, чей официальный статус зависит от чистоты этого массива.
- **Верификация гейта `secret-parser-built` по факту** (наличие `redact` как отдельного модуля поверх сканера, а не внутри `night-triage-secret-scan.mjs`) — снимает риск четвёртого переноса, даёт утру определённость до любой новой работы; это прямой разблокер `archivarius-sessions-container`, который без этого не имеет полной предусловной цепочки.

## Перспективные
- **Утренний верификатор DoD (`yarn main-day-probe`)**: единый проход по двум открытым пунктам — `assertions[]` и след резака `redact` — закрывает вопрос третьего переноса `secret-parser-built` и разблокирует `archivarius-sessions-container` как единственную задачу с полной цепочкой.
- **Ревью #1648 (`detectors-window-single-carrier`, 2328 строк)**: единственный продуктовый коммит за 02–03.08, не прошедший развёрнутое ревью; проверка инвариантов окна при сведении четырёх копий даёт следующий шаг для завтрашнего эшелона.
- **Заведение P1 «монотонность `sequence` на `runId`»**: дубль `sequence: 1` в `2026-08-03.jsonl` тихо создаст коллизию при агрегации — issue до того, как долг утонет, открывает чистую доску завтра.
- **`detector-scoreboard` / `media-library-a3` возвращаются в фокус**: обе имеют полную предусловную цепочку и измеренные числа — после закрытия инструментального контура это первый кандидат на продуктовый день.

## Экспериментальные
- **Узнаем, сдержит ли `gate-honest-pair-completeness` (#1661) обещание: упадёт ли он на историческом дубле `sequence: 1` из журнала 03.08, когда агрегатор начнёт честно проверять пары.**
- **Узнаем, отзывается ли резак `redact` как чистая функция на вход без файловой системы: сможет ли `redact(transcript)` дать `{redacted, manifest}` на синтетическом следе, не трогая диск.**
- **Узнаем, виден ли разрыв «контейнеризация есть, акт выкладки — нет» на практике: поднимется ли один из 41 глагола деплоя до состояния «процедура», если смотреть на него через линзу survey-карты.**

## Санитарные
- **Хвост DoD с вечера: `assertions[]` в `main-day-assertions.json` — статус перечеканки неясен, `yarn main-day-probe` не зафиксирован.**
- **Гейт `secret-parser-built`: след `redact` в логе не найден; задача переносится третий день — проверить наличие резака как отдельного модуля `scripts/lib/secret-redact.mjs`, а не внутри сканера.**
- **Ревью-долг: 10 oversized PR не развёрнуты; начать поимённо с #1648 (2328 строк), затем #1661, #1662.**
- **Завести issue на P1: монотонность `sequence` в procedure-run journal (дубль `sequence: 1` в `2026-08-03.jsonl`).**

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
