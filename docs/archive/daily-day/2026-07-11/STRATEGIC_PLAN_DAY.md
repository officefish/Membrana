<!--
  archive-role: archive-snapshot
  archive-day: 2026-07-11
  archived-at: 2026-07-11T18:10:53.678Z
  source: docs/STRATEGIC_PLAN_DAY.md
  canonical: docs/STRATEGIC_PLAN_DAY.md (перезаписывается yarn plan:day / standup / main-day-issue)
  Не использовать как основной документ дня — побочный снимок для ретроспективы и анализа.
-->

<!-- Сгенерировано: 2026-07-11T03:54:45.227Z (yarn plan:day) -->
<!-- Период: последние сутки (since="1 day ago"); горизонт: следующий день -->
<!-- Источник цели: WHITE_PAPER.md -->

# План на следующий день

## 1. Что сделано за период (последние сутки (since="1 day ago"))

- **`device-board` (рантайм и семантика) — основной блок работы.** Закрыт Competition Sprint `comp-detection-alarm` (победитель Team Beta, 213.5). Три сценария детекция→alarm (alpha/beta/gamma) собраны на basn-палитре и внедрены в пикер вместо async-v2 (#336/#337/#339). Ключевые семантические фиксы рантайма (#340/#341): fusion — единственный писатель `lastDetection` (вход в alarm по `combinedScore >= 0.5`, не по trends-соло — дух ND3); коэрсия invalid-ref к целевому kind до первого flush; пустое окно анализаторов = молчащий skip, не throw; порог `fusion→lastDetection` читается из branch-узла подграфа.
- **`device-board` + `apps/client` (live-wiring) — отладка живого прогона.** Проброс fusion/ensemble/proximity stores во все точки исполнения (`exec-subgraph`, `event-dispatch`, `function-call`) — basn-узлы теперь работают во всех ветках рантайма (#338/#346, находки L29–L33). Host `startAsyncJob` поддержал kind `report-build`. Стабилизирован singleton `LiveJournalService` (`replaceBackend` меняет storage на месте) + рестарт записи из хвостов latent-веток gamma (#347, L34/L35).
- **`packages/services/telemetry-journal`.** `LiveJournalService.replaceBackend` + гард `storageMode !== remote-server` (−1.7 с латентности на publish); покрыто тестами.
- **Корневая инфра / тулинг.** `pr:ship --no-commit` для готовых коммитов (#335); дефолт тега `cabinet:deploy:image:prod` → `main` вместо `latest` (#334, фикс отката прода); commit-msg хук допускает тип `comp(<sprint>/<team>)`.
- **Документация и процесс.** Пилот routine «Ночной триаж реестра» (#344/#345) — заморожен по стоп-условию (у облачного рана нет канала ошибок, только git-успех). Скилл `membrana-usercase-lessons` (журнал недочётов, 3 слоя, #343). Журнал L24–L35 в `USERCASE_COMPETITION_LESSONS.md`, closure-артефакты дня 2026-07-09 (10 задач LGTM).

## 2. Привязка к стратегической цели

**Где мы на дорожной карте (WHITE_PAPER §8).** Формально — Этап 1.A/1.B (детекция на одном узле). Фактически детекционный потолок эшелона 0 (DSP/FFT) зафиксирован (`FFT_METRICS §0`, §6), а эшелон 2 де-факто открыт: yamnet в prod-бенчмарке (F1 0.803, лучший на free-v1). Текущая продуктовая магистраль (форсайт 2026-07-06) — **финализация FREE-тарифа**: S2 combined UC → S3 упаковка UserCases → S4 студия-download → S5 лендинг. Работа суток шла именно по S2: собран и отлажен на живом борде полный сценарий детекция+alarm (fusion спектр+нейро → branch → combined → alarm-loop).

**Что приближает к цели:**
- Три detection-alarm UserCase на basn-палитре с fusion как единственным писателем `lastDetection` — прямая реализация S2 (fusion спектр+нейро + alarm-loop). Соответствует принципу WP §6 «слияние модальностей в одной шине» и заметке ND3 (сырой confidence yamnet, не бинарный вердикт).
- Live-wiring basn-узлов во всех ветках рантайма + стабильный журнал — снимает блокеры живой демонстрации S2 (WP §4.6 ситуационный слой, §11 задержка отображения).

**Что нейтрально:** тулинг-фиксы (#334/#335), скилл-журнал, closure-гигиена — необходимая гигиена, но напрямую к этапу не двигают.

**Что отвлекало / завершено правильно:** пилот night-triage заморожен по стоп-условию — верное решение, не тянуть R&D-рутину без канала наблюдаемости.

**Недостающие сервисы.** По коммитам **не пора** начинать `tdoa-service` / `localizer-service` / `tracker-service` / `transport-service` — это Этапы 2–4, замороженные до прохождения hard-gate; фокус остаётся на single-node + продуктовой упаковке. `detection-ensemble-service` и `fusion` уже существуют (basn-1/basn-2 merged). Открыть новые сервисы на этом горизонте — не требуется.

## 3. Риски и долг

- **Долг живого smoke S2 (Задачи A/B).** По вечернему ритуалу 2026-07-10 хвост на завтра: перепрогон Gamma на новой топологии, Beta/Alpha live, живой smoke S2 (browser-smoke Задача A + combined+alarm Задача B). Unit-тесты зелёные (708/710 device-board, 287 client), но **живого подтверждения S2 на борде ещё нет** — это главный незакрытый риск магистрали.
- **Wiring-хрупкость рантайма (L24, L29–L35).** Серия находок «сценарий молчит» вскрыла системный долг: точки вызова `executeScenarioBlock` (exec-subgraph / event-dispatch / function-call) исторически не пробрасывали stores единообразно. Профилактика L24 «grep все точки» была записана, но не выполнена сразу — риск, что остались непокрытые сайты. Latent-ветки ранее глотали исключения молча (L31) — отладка вслепую.
- **Наблюдаемость live-журнала.** L34 (подмена инстанса сервиса) и L35 (гонка рестарта записи) — оба всплыли только на живом Run, не в unit. Значит покрытие live-сценариев тестами недостаточно; каждая находка живого Run обязана давать L-запись (скилл #343 это кодифицирует).
- **Границы пакетов — нарушений по diff не видно.** Изменения в `device-board`, `telemetry-journal`, `apps/client` в своих слоях; `scenarioMicJournalBridge` (клиент) корректно посредничает между бордом и журналом. Правило «сервисы не зависят друг от друга горизонтально» не нарушено по видимым файлам.
- **Ограничения из WHITE_PAPER, релевантные сейчас.** Точность детекции (WP §9, precision trends ≈0.76 на val — ниже hard-gate 85/90). Fusion-стратегия: профили ошибок DSP/нейро слабо коррелированы (ND3) — правильный путь, но качество combined на free-v1 ещё не измерено одной таблицей. TDOA/синхронизация/многолучёвость (WP §5.2, §9) — вне горизонта (Этап 2 заморожен).

## 4. План на следующий день

### Задача 1 — Живой smoke S2: combined+alarm UserCase на борде
- **Цель** — подтверждённый живой прогон Beta (winner) + Alpha на борде с непустым журналом и корректным alarm-loop.
- **Пакет / слой** — device-board + client (интеграция сценария и live-журнала).
- **Связь с WHITE_PAPER** — S2 combined UC (форсайт); WP §4.6 ситуационный слой, §11 задержка отображения ≤3 с.
- **Definition of Done**:
  1. Beta прогнан live: ensemble → fusion → branch → combined → publish-done, журнал непустой, alarm срабатывает по `combinedScore >= 0.5`.
  2. Alpha прогнан live с тем же чек-листом.
  3. Каждая находка живого Run → L-запись (симптом с runId → корень → Fix #PR → профилактика).
- **Роль** — Верстальщик (ведёт), Teamlead (приёмка live).
- **Размер** — M.

### Задача 2 — Перепрогон Gamma на новой топологии
- **Цель** — Gamma отработает на актуальной топологии после фиксов L34/L35 без «пустого журнала / одноразовой детекции».
- **Пакет / слой** — device-board (graph + runtime).
- **Связь с WHITE_PAPER** — S2 combined UC; принцип WP §5 «локальная автономность» / стабильность цикла записи.
- **Definition of Done**:
  1. Gamma live-run: цепочка полная, запись рестартует из хвостов обеих веток решения.
  2. Тест `default-usercase-detection-alarm-gamma` зелёный на новой топологии.
  3. Обнаруженные несоответствия зафиксированы L-записью.
- **Роль** — Структурщик (ведёт runtime), Верстальщик (live-проверка).
- **Размер** — M.

### Задача 3 — Аудит точек вызова `executeScenarioBlock` (закрытие долга L24)
- **Цель** — единый инвентарь всех сайтов исполнения блока с гарантией проброса fusion/ensemble/proximity stores.
- **Пакет / слой** — device-board (runtime).
- **Связь с WHITE_PAPER** — надёжность fusion-слоя (WP §4.4, §7 открытый контракт наблюдений).
- **Definition of Done**:
  1. Grep/инвентарь всех точек вызова `executeScenarioBlock` в документе аудита.
  2. Регресс-тест, покрывающий проброс stores в каждой ветке (initial/main/alarm/handlers/functions/event/latent).
  3. Если найден непокрытый сайт — фикс + тест.
- **Роль** — Структурщик.
- **Размер** — S.

### Задача 4 — Сравнительная таблица trends `DRONE_TIGHT` vs yamnet на val (долг объяснимости)
- **Цель** — одна таблица: кто основной кандидат в hard-gate, кто объяснимый бэкап (P/R/FPR/F1 на held-out val).
- **Пакет / слой** — analyzer (`fft-analyzer` / `template-match-detector` + neural-детектор), артефакт в `DETECTOR_BENCHMARK.md`.
- **Связь с WHITE_PAPER** — stage-gate 1→2 (WP §8), метрики WP §11; поддерживающая полоса детекции.
- **Definition of Done**:
  1. Таблица на существующем held-out val (без нового прогона DSP-тюнинга).
  2. Явно указан основной путь (yamnet F1 0.803) и объяснимый бэкап (trends `DRONE_TIGHT` 95%/30%).
  3. Ссылка из `FFT_METRICS §6` и `DETECTOR_BENCHMARK.md`.
- **Роль** — Математик.
- **Размер** — S.

### Задача 5 — Подготовка S3: инвентарь UserCases под упаковку (3+1 в device-board)
- **Цель** — список готовых UserCase-сценариев (детекция-alarm + сопутствующие) и структура их упаковки в пикер device-board.
- **Пакет / слой** — device-board (catalog) + docs.
- **Связь с WHITE_PAPER** — S3 упаковка UserCases (форсайт); WP §6 device-board.
- **Definition of Done**:
  1. Документ-инвентарь: какие entry идут в bundled, какие в community, где живёт winner Beta как эталон S3.
  2. `catalog:verify-client` + `user-case-catalog.test` зелёные.
  3. Открыт вопрос «где живут шаблоны» (`template-match` vs `background-media` vs client) как явный пункт архитектурного долга.
- **Роль** — Структурщик (ведёт), Teamlead (LGTM состава).
- **Размер** — M.

## 5. Что НЕ делаем на этом горизонте

- **Не** запускаем повторный unified benchmark harmonic/cepstral/spectral-flux на free-v1 без нового датасета/алгоритма/fusion — эшелон 0 исчерпан, вердикт зафиксирован (`FFT_METRICS §0`, §6). DSP-детекторы остаются объяснимостью и ранним индикатором, не магистралью.
- **Не** ставим магистралью «довести Этап 1.A» или stage-gate 85/90 через одиночные DSP — их FPR 88–100% на free-v1 (WP §8 hard-gate требует другого пути).
- **Не** начинаем `tdoa-service` / `localizer-service` / `tracker-service` / `transport-service` — Этапы 2–4 заморожены до прохождения hard-gate на пилотном корпусе (WP §8, VDR-железо ~17.07).
- **Не** переизобретаем yamnet-детектор/плагин/бенчмарк — уже в prod (#266/#268, F1 0.803); никакой «разведки» эшелона 2.
- **Не** реанимируем night-triage routine — заморожена по стоп-условию до восстановления канала наблюдаемости облачного рана (чеклист разблокировки у владельца).

## 6. Проверки в конце периода

- **Живой S2 подтверждён:** Beta и Alpha прогнаны на борде, журнал непустой, alarm-loop по `combinedScore >= 0.5` работает — снят долг «живого smoke S2» (Задачи A/B).
- **Gamma стабильна:** перепрогон на новой топологии без пустого журнала / одноразовой детекции; тест gamma зелёный.
- **Долг L24 закрыт:** аудит-документ точек `executeScenarioBlock` + регресс-тест проброса stores во всех ветках; новых непокрытых сайтов нет.
- **Таблица объяснимости готова:** trends `DRONE_TIGHT` vs yamnet на val в `DETECTOR_BENCHMARK.md`, явно назван основной путь и бэкап.
- **S3-инвентарь готов:** документ состава UserCases + зелёные `catalog:verify-client` / `user-case-catalog.test`; вопрос «где живут шаблоны» вынесен как явный пункт.
- **Гигиена процесса:** все находки живых Run зафиксированы L-записями (append-only, скилл #343); тесты device-board / client зелёные, границы пакетов не нарушены.