<!--
  archive-role: archive-snapshot
  archive-day: 2026-08-30
  archived-at: 2026-08-30T17:29:13.453Z
  source: docs/DAY_PLAN.md
  canonical: docs/DAY_PLAN.md (перезаписывается yarn plan:day / standup / main-day-issue)
  Не использовать как основной документ дня — побочный снимок для ретроспективы и анализа.
-->

<!-- angelina {"author":"llm","guard":"angelina","readAt":{}} -->
<!-- canon-digest: b6d44d1f95e053bed883ee36dc5964f367bc4681a4c9d638975ad78eafbb84ef · signedAt: 2026-08-30T08:46:34.477Z -->
# План дня — 2026-08-30

## Магистраль
- **angelina-hostess-impl** (L) — закрывает боль «хозяина контура без воплощения»: guard `angelina` уже стоит на горизонте, а реализации hostess нет; годится магистралью как L-ствол, который превращает именной контур в работающий носитель дня.
- **assets-container** (L) — закрывает разрыв «хранение как продуктовое обещание» (буфер, библиотека, витрины): без контейнера активов отбор и разбор проб остаются россыпью; тянет на магистраль как общий каркас под кабинет/Studio и квоты.
- **batch-collection-run-contour** (L) — закрывает боль пакетного контура сбора (прогон коллекции целиком, а не штучные пробы): нужен, чтобы ночные/полевые уловы шли управляемым контуром, а не разовыми актами; магистрален как L-обвязка рутины сбора.

Выбор магистрали — слово владельца (owner-choice)

## Подкрепление
- **резак в `night-triage-secret-scan.mjs` + фикстура «сырой хвост не уходит»** — без агрессивного вырезания (не только детекта) гейт `secret-parser-built` и кристалл `session-backup-requires-secret-redaction` остаются недостижимы, а бэкап сессий снова тащит секреты на сервер.
- **один датированный проход + манифест ротации засвеченных ключей** — закрывает второй критерий вехи и кристалл `credential-rotation-biweekly`, снимая риск «амнистия архива висит на дате, а не на предикате».

## Перспективные
- Прохождение гейта `secret-parser-built` (резак + датированный проход с манифестом ротации) снимет амнистию на правку архива и откроет безопасные бэкапы сессий с вырезкой секретов до отправки.
- Оформленный контракт ночной процедуры (фреймы → утренний читатель сверх test-report) откроет следующие дни на честный предикат «охоты» и ритуальный контур без optional-маскировки.
- Манифест ротации засвеченных ключей из прохода парсера откроет би-weekly перевыпуск доступов там, где это возможно, без ручного разбора архива вслепую.

## Экспериментальные
- Прогнать `night-triage-secret-scan.mjs` на синтетической фикстуре с 3 заведомо ложными ключами (разные паттерны) → узнаем, резак реально вырезает payload или пока только детектит.
- Собрать черновик манифеста ротации на одном датированном «засвеченном» токене-заглушке (без живых секретов) → узнаем, хватает ли полей манифеста, чтобы гейт `secret-parser-built` читался как предикат, а не как дата.
- Сверить один архивный бэкап сессии «до/после» redaction на копии → узнаем, уходит ли сырой фрагмент в артефакт или парсер уже режет до записи.

## Санитарные
- красный `@membrana/background-media#test` из снимка гигиены — диагноз или issue, не повторный прогон ритуала
- schedule drift GitHub 10–12 ч — закрыть числом или явно загейпить
- #2204 держать в санитарии, не в стволе (риск подмены `night-procedure-frames`)
- неразвёрнутый `53b60caf` (~932 строки) — хвост выкладки после вчерашних #2231–#2236
- claims-probe: в реестре нет карточек `night-procedure-frames` / `declared-imports` / `workspace-dirs` / `ritual-artifacts` / `buffer-delete-modal-2218` @53b60caf

## Список посылок
- реестр задач: кандидатов магистрали 3 (детерминированный ранг, зоны по разметке)
- горизонт: docs/STRATEGY_DAY.md прочитан (живой генератор #592)
- рука: docs/HANDOFF.md прочитан
- фидбек: вчерашний протокол команды прочитан
- вчерашний день ЗАКРЫТ (вычислено): протокол команды есть, ласточка отправлена — в санитарные это НЕ писать
- магистраль НЕ назначена планом — top-3 кандидатов, выбор словом владельца (Q1 17.07 + гейт утра)

_слотов пусто/бито: 0 из 5_
