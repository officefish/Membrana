# MEETING ACTIVE — `tariff-single-truth`

> Протокол **контейнера**, отдельный от протоколов консилиумов.
> Открыто: 2026-09-08 · Ветка: `angelina/chore/ritual-day-20260908` · Worktree: `../Membrana-tooling`
> Председатель: ведущая сессия (Ангелина) · Аудитор: отдельный агент, read-only (S-M5) — не председатель

---

## СТАТУС: ОТКРЫТО — M0 отработал (37 реплик, 10 посылок, гейт посылок пройден); аудит M0 (первый прогон): ОТКЛОНЕНО по HR5 — посылка «линейка квот / наборы вне квоты» не использована репликами; порядок и рёбра не оспорены. Председатель выбрал строгий путь аудитора: M0 перепрогнан с требованием «каждая посылка — со ссылкой на реплику, только использованные»; первый протокол остаётся следом. **Прогон 2 отработал** (36 реплик, 11 посылок, каждая со ссылкой на реплику) — порядок ИЗМЕНИЛСЯ: R → P → U → D → S (наборы — последними); аудит прогона 2 ЧИСТ; порядок РАТИФИЦИРОВАН владельцем 08.09 («Ратифицирую»); M1 отработал (42 реплики, 12 посылок со ссылками); **аудит M1 прогона 1: ОТКЛОНЕНО** (13 замечаний, границы комнаты чисты) — повестка M1 дополнена поправками фактуры и требованиями аудитора, **M1 перепрогоняется** (прогон 2); первый протокол остаётся следом

## Задание

См. [`MEETING_BRIEF.md`](MEETING_BRIEF.md). Вход — закрытый шторм
[`storm-tariff-single-truth-2026-09-08`](../../storm/storm-tariff-single-truth-2026-09-08/REPORT.md):
2 вдоха по машине, 16 тезисов; развилка «в заседание» — слово владельца 08.09 («Да заканчиваем… можно сначала через собрание»).
Раздача владельца: матрица — Б (#2331); адаптация сервера — В после заседания; фриз STOP — Г (#2328); всё спринтами с командой.

## Порядок (M0)

Вердикт M0 прогона 2 (протокол `docs/seanses/tariff-single-truth-m0-order-r2-2026-09-08.md`), **ратифицирован владельцем 08.09** («Ратифицирую»):

```text
M1 = (R) Пересев производных из матрицы
 → M2 = (P) Право и объём
   → M3 = (U) Повышение тарифа
   → M4 = (D) Понижение тарифа и холодное хранилище как носитель
 → M5 = (S) Наборы звуков при переходе   (зависит только от R; в календаре последним)
```

Рёбра (6): R→P, R→U, R→D, R→S, P→U, P→D. Не рёбра: U↔D, S↔D (холод), P↔S; наборы не требуют U/D.
Прогон 1 (след, отклонён по HR5): R → P → S → U → D с рёбрами S→U, S→D — во втором прогоне комната сняла S→U/S→D как не необходимые.

## Аудит

**Аудит M0, прогон 1 (08.09, отдельный read-only агент): ОТКЛОНЕНО.** 18 проверок PASS; одно нарушение HR5 — посылка «линейка 512 МБ / 2 ГБ / 4 ГБ; системные наборы вне квоты» ни разу не использована в 37 репликах (лишняя посылка «лжёт об отзыве»); не несущие: клауза о self-переходе в посылке 7 не использована; таблица одновременно зовёт S→D ребром и «D↔S по холоду» не-ребром (снято репликой: S→D — про каталог, не про носитель недели); основания посылок — источником, не репликой. Решение председателя: перепрогон M0 на той же повестке с требованием в строке вопроса «каждая посылка — со ссылкой на реплику, где сработала; только использованные».

**Аудит M0, прогон 2 (08.09, отдельный read-only агент): ЧИСТО.** 26 проверок PASS: 30/30 ссылок посылок на реплики верны, лишних нет; DAG ацикличен, 6 рёбер ⊆ подсказок; сдвиг наборов в M5 обоснован (#16, #17, #19, #30 — рёбра S→U/S→D сняты как не необходимые, R→S сохранено); задание не поехало (эхо повестки r1↔r2 отличается одной строкой требования аудита). Не несущие: посылки 8 и 10 работают ограничительно; в #22 названы три внешние данности, в список попали две; на M1–M5 в строку вопроса добавить «запрет на подмену словаря».

**Аудит M1, прогон 1 (08.09, отдельный read-only агент, дерево `Membrana-tooling`): ОТКЛОНЕНО.** Границы комнаты чисты (залётов в M2–M5 нет, все четыре подпункта Q1 отвечены). Замечания (13): HR5 — P11 и P12 стоят со ссылками на реплики, которые их не используют («фон» ≠ использование), P9 использована наполовину; P6 и P3 помечены «факт», но реестр квот в рантайм не проведён, а `yarn tariff:reseed` в стволе нет (живёт в невлитом #2331); переезд не называет судьбу сегодняшнего посева (`seed.mjs` + `tariff-scalars.json`, две строки тарифов без автора в дереве, мёртвый `ensureFreeTariff`); флаг `TARIFF_GRID_MODE` и предикаты S9 `tariff-cutover.mjs` проигнорированы (два переключателя правды); DoD 6 про несуществующий клиентский кэш — настоящий кэш серверный, один на процесс; зуб 2 без отображения колонок `Tariff` на ячейки сетки, у холода колонки нет; сетка уже несёт `version: 1`; у записи прибора нет `updatedAt`; словарь нарушен (records 26×, contractVersion 21×, reconcile/rolling/upsert/disperse); DoD 5/7/8 непроверяемы; ни у одного зуба нет порчи и файла-предмета; зуб 3 раздвоен (сверка D↔B у Математика против счётчика доставки в вердикте). Подтверждено кодом: `contractVersion` отсутствует; сетка в образе кабинета, сервер записей её не читает; разноска со счётом и её три вызова; приём на сервере записей; витрина — пересечение; расхождение free 512 МиБ ↔ 1 ГБ. Решение председателя: поправки фактуры и требования аудитора — в повестку M1, прогон 2 на той же строке вопроса с расширенным словарём.

**Аудит M1, прогон 2 (08.09, отдельный read-only агент): ОТКЛОНЕНО по форме, содержание принято.** 46 реплик, 19 посылок, 9 DoD; повестка с поправками доехала полностью. Требования 2 (судьба посева, `ensureFreeTariff` удалить), 3 (флаг режима сетки подчинить зубам, затем снять; один рубильник), 5 (версия — то же поле `version`, `G.version := R.version`), 7 (словарь: «сервер записей» везде, «пересев» = R→G→B→D объявлен явно, чужих слов 0 содержательных) — выполнены; фактура сошлась с кодом по 12 позициям; залётов в M2–M5 нет. Остаток: HR5 — 8 из 19 посылок со ссылками, сдвинутыми на единицу или на несуществующие номера (та же болезнь, что в прогоне 1, теперь механическая); зуб 3 без файла-предмета и пар полей (у записи прибора нет `maxNodesPerMembrane`, политика — поле мембраны, не тарифа); DoD 8 и 9 — регламент, не предикат; DoD 2/3/7 без имени теста. Решение председателя: содержание прогона 2 объявлено данностью, прогон 3 — только форма (цитата первых слов реплики в каждой ссылке, пары полей зуба 3, DoD предикатами).

**Аудит M1, прогон 3 (08.09, отдельный read-only агент): ОТКЛОНЕНО по форме.** 30 реплик (по 5 у роли). Содержание прогона 2 сохранено по всем осям, кроме одной потери: версия контракта на строке `Tariff` (зуб 2 «version B=G», DoD 2, «поля версии на B и D») из прогона 3 выпала. Зуб 3 починен (файл-предмет, пять пар по именам колонок, `maxNodesPerMembrane` остаётся в базе кабинета, политика — поле мембраны/прибора). Остаток: P13 снова со сдвинутым номером (цитата от Музыканта #4, номер #3); DoD 2/3/5 без имени теста; DoD 9 потерял «ноль записи в `Tariff` на старте»; в повестке и протоколе — несуществующая колонка `Membrane.bufferPolicy` (ошибка председателя; политика — `MembraneBufferPolicy.mode` / `Device.bufferPolicy`); строки типа «план» нет; P16/P7 шире реплик; `rg TARIFF_GRID_MODE` в DoD 8 не ловит `scripts/`. **Решение председателя:** три прогона одной комнаты — каждый чинит одно и роняет другое; четвёртый не созывать. Вердикт M1 собирается председателем из прогона 2 (содержание) и прогона 3 (форма) с поправками аудита, каждая поправка помечена; сборка — на аудит (прецедент эпика «полный буфер»: сборка не готова, пока аудитор не принял).

**Аудит сборки M1, прогон 1 (08.09, отдельный read-only агент): ОТКЛОНЕНО по форме, «четвёртый прогон не нужен».** Источники всего непомеченного содержания найдены в прогонах 2/3; пометки аудита r3 подтверждены записью аудита, кроме зуба 1 (возврат законен, атрибуция неверна); фактура против кода сошлась по всем колонкам, ячейкам сетки и скриптам; имена [председатель] в стволе не существуют и не конфликтуют; ссылки P13/P16/P7/«план» по r3 верны; границы и словарь чисты. Шесть редакционных замечаний: имя колонки `Tariff.version` без пометки против словаря (два имени одного поля); атрибуция возврата версии в зубе 1; третья клауза DoD 7 прогона 2 (тест прибора-потребителя) снята без объяснения; правило «эффективная политика = привязка ? мембрана : прибор» взято из комментария схемы без пометки; в списке посылок не исправлена P9 с `Membrane.bufferPolicy`; P16 шире реплики Структурщика #2. Наблюдение: «cutover не импортирует скаляры» — проверка без предмета, опора на скаляры — строка `S0_scalars`. Все семь внесены в сборку; сборка — на повторном аудите.

**Аудит сборки M1, прогон 2 (08.09, тот же read-only агент): ЧИСТО.** Все семь правок закрыты с цитатами; нового непомеченного содержания нет; принятое содержание прогона 2 на месте; границы и словарь чисты. **Вердикт M1 принят.**

**Аудит M2, прогон 1 (08.09, отдельный read-only агент): ОТКЛОНЕНО по существу.** 37 реплик (Teamlead 7, остальные по 6). Границы Q2 чисты, данности не переоткрыты, фактура повестки по коду верна, все 16 ссылок посылок по номерам и началам реплик верны, регламент вне DoD. Замечания (13): DoD 4 — ложный предикат «свободно = 0 после отказа» (отказ при `used + incoming > limit` даёт `available ≠ 0`; комната в реплике знала «или», в DoD уехала эквивалентность); носитель функции не назван — словарь отказа в `plugin-contracts` ESM-only, сервер записей CJS берёт его только `import()`; расширение контракта отказа полем «свободно» не объявлено; клиент уже считает «свободно» сам (`overflow-window/viewModel.ts:82`, `media-library/quota-status.ts`) — не названо ни фактом, ни красным входом; носитель режима «окно» расщеплён (сервер числа эпизода не хранит, снимок на приборе); предикаты без путей; DoD без имён; HR5 — клаузы посылок не покрыты цитируемыми репликами (`resolveDeviceLimits` 0 упоминаний, «stop/200» 0); словарь — records ×4, occupied ×5. Решение председателя: держащееся содержание объявлено данностью прогона 2, повестка дополнена шестью фактами по коду и требованиями 6–13, M2 перепрогоняется.

**Аудит M2, прогон 2 (08.09, отдельный read-only агент): ОТКЛОНЕНО по форме, содержание принято.** 36 реплик (6×6), порядок ролей соблюдён. Требования 6 (предикат «свободно < входящее»), 7 (носитель — `plugin-contracts`, ESM, `import()` из CJS, без нового пакета и без core), 8 (расширение `BufferOverflowRefusal` полем «свободно» — явно в «Строить»), 9 (вторая арифметика клиента — факт и красный вход), 12 (DoD — одно имя, красный случай, регламент вне) — выполнены; границы и данности чисты, держащееся содержание прогона 1 воспроизведено; фактура сходится с кодом. Остаток формы: HR5 — 11 из 19 цитат не по пять слов, клаузы P2/P10/P11 без реплики (`resolveDeviceLimits` и `sizeBytes` — 0 упоминаний; `maxNodesPerMembrane` — в Архитектор #2, а цитирован #1; stop — Музыкант #1, 200 — Teamlead #2, не цитированы; T8/T9/T12/T13 — 0); режим окна оставлен дизъюнкцией («либо читает, либо зовёт»), правило есть у Архитектора #4; ложный прецедент «как reasons» (там литералы с `satisfies`, рантайм не импортируется; настоящий прецедент `import('@membrana/plugin-contracts')` — `collections/plugin-host.service.ts:63`; `buildBufferOverflowRefusal` синхронна, `:104-122`; подпуть-импорт невозможен — экспорт пакета только `"."`); C8 без файла, C2/C3 с многоточием; словарь — records ×5, hold ≈10, freely ×2. **Решение председателя (прецедент M1):** третий прогон не созывать; сборка из прогона 2 с поправками аудита, каждая помечена, — на аудит.

**Аудит сборки M2 (08.09, тот же read-only агент): прогон 1 — ОТКЛОНЕНО по форме (цитаты не выписаны, одна клауза P10 без реплики, D2/D3 размещение без пометки); прогон 2 — ЧИСТО** (30/30 цитат совпали с первыми пятью токенами реплик механически, переадресация P3 → Математик #4 верна, ничего нового непомеченного, словарь чист). **Вердикт M2 принят.**

**Аудит M3, прогон 1 (08.09, отдельный read-only агент): ОТКЛОНЕНО по существу.** 33 реплики (Структурщик/Верстальщик/Teamlead по 6, Архитектор/Математик/Музыкант по 5). Границы Q3 (а–г) и данности чисты: событие перехода с версией (T7), `/quota` — канал объёма; смена тарифа не `release`, удержание до «Возобновить»; окно — снимок старых чисел; откат права запрещён, «право ждёт + повтор». Три замечания по существу: (1) версия контракта использована как счётчик переходов («применять если `v_in > v_cur`») — по принятому M1 версия принадлежит релизу и при смене тарифа не меняется, так что вторая смена в одном релизе — пустая разноска, а ответ на (г) о защите от обратного порядка пуст; (2) «локальный контракт лимита прибора» рядом со «свободно» M2 не разведён — вторая арифметика или пустой предикат; (3) хедж «`/quota` может нести version» — второй канал без решения. Форма: 0 из 15 цитат по пять слов; посылки P4/P6/P8 шире реплик; проверки K3/K4/K6/K7 без файла-предмета, нет «кто краснеет»; DoD-1/DoD-7 с двумя именами и дизъюнкцией; словарь — hold ×6, freely ×2, fanout, reconcile, success/retry/rollback/raise. Фактура: путь проверки K1 указывает в кабинет, а применение на сервере записей (`syncMembraneContext`); `hold.held` — нет, есть `isHeld()`; в повестке `:132` → `:133`, `wiring.ts:35` → `:53` (исправлено). Решение председателя: держащееся содержание — данность прогона 2, требования 7–13 в повестку, M3 перепрогоняется.

**Аудит M3, прогон 2 (08.09, отдельный read-only агент): ОТКЛОНЕНО, содержание годится в сборку.** 37 реплик (Teamlead 7, остальные по 6). Требования 7 (носитель порядка отдельно от версии, обоснован против второго носителя правды), 8 (прибор лимит не считает), 9 (один носитель «принял», `/quota` — диагностика) — выполнены по существу; границы и данности прогона 1 держатся, `HoldReleaseBy` не расширен. Дефекты: (1) предикат приёма `v_in ≥ v_cur ∧ ord_in > ord_cur` ломает принятый M1 — пересев без смены тарифа несёт новую версию при том же порядке и по правилу «ord равен → пусто» не ляжет на запись прибора; нужен порядок пары; (2) носитель порядка привязан к `TariffChangeLog.id` как к монотонному числу — по стволу `id` uuid, `at` не строго монотонен; порядок перехода — новая колонка-последовательность; (3) локальный страж описан не по коду (`judgeLocalGuard` судит по живому чтению квоты, снимка не держит, при снижении молчит); форма — 1 из 28 цитат по пять слов, три номера сдвинуты (реплики «Принимаю» не сосчитаны), путь проверки 1 без `modules/`, контракты проверок 7–8 не именованы, словарь (hold ×12, rollback ×3). **Решение председателя (прецедент M1/M2):** сборка из прогона 2 с поправками аудита, каждая помечена, — на аудит.

**Аудит сборки M3 (08.09, тот же read-only агент): прогон 1 — ОТКЛОНЕНО узко (три пометки: отправитель события, «кто чинит» домами, роли регламента); прогон 2 — ЧИСТО** (28/28 цитат механически, нового непомеченного нет, пометки [председатель] совместимы с кодом). **Вердикт M3 принят.**

## Вердикт M3 (U) — сборка председателя (источник: прогон 2 — содержание; поправки аудита прогона 2; аудит сборки 1–2), ПРИНЯТ аудитом 08.09

Пометка **[аудит r2]** — поправка по замечанию аудита прогона 2 с источником; **[председатель]** — решение, которого комната не приняла, назначено председателем и подлежит замене исполнителем при первом коммите.

**Словарь:** право — тариф мембраны в базе кабинета (`TariffChangeLog` + `Membrane.tariffId`); лимиты — числа на записи прибора; версия контракта — версия релиза (M1); **порядок перехода** — монотонный номер записи журнала смен, курсор доставки, не право; удержание — `DeviceOverflowHold`; событие перехода — сообщение прибору с версией и порядком; **отправитель — сервер записей** (он принял лимиты и порядок на запись) **[председатель: комната отправителя не назвала]**; сигнал «принял» — поле состояния узла в кабинете.

**(а) Порядок событий.** 1) Право: транзакция кабинета `selectTariff` + строка `TariffChangeLog` с **порядком перехода — новой монотонной колонкой-последовательностью** (`id` — uuid, `at` не строго монотонен — `schema.prisma:284, 293`) **[аудит r2: вместо «id/at журнала»]**. 2) Разноска: `PATCH /v1/devices/:id/membrane` с лимитами + версия контракта (релиз) + порядок перехода. 3) Приём на сервере записей `syncMembraneContext` (`packages/background-media/src/modules/devices/devices.service.ts:174-197`): лимиты, версия и порядок ложатся на запись прибора **⇔ пара `(версия, порядок)` входящая строго больше хранимой в лексикографическом порядке; равная пара — повтор, пусто без ошибки** **[аудит r2: вместо конъюнкции «версия ≥ и порядок >» — иначе пересев без смены тарифа (новая версия, тот же порядок) на запись не ляжет и зуб 3 M1 покраснеет навсегда]**. 4) «Прибор принял»: событие перехода на прибор с версией и порядком (T7), не `/quota`. 5) Кабинет узнаёт «принял» по сигналу состояния узла, не по счётчику доставки. `/quota` — канал объёма и жизни; версия в нём, если появится, — диагностика без права на переход.

**(б) Удержание.** Смена тарифа удержание не снимает; выход только `release('human' | 'cleanup')` (`types.ts:62`, вызовы `wiring.ts:53` cleanup и `overflow-window/controller.ts:106` human); новой причины нет. Окно оператора и плашка — снимок старых чисел эпизода (M2), снимок держит удержание (`deviceOverflowHold.ts:82`). **Локальный страж** (`device-overflow-hold/localGuard.ts:16-34 judgeLocalGuard`) судит по живому чтению `/quota`, снимка не держит, ничего не показывает; при снижении заполнения отвечает `ignored` и удержание не трогает; отправку режут ворота `wiring.ts:46 setSampleUploadGate(() => hold.isHeld())`. После повышения страж увидит новые числа, промолчит, удержание и снимок останутся до «Возобновить» **[аудит r2: вместо «страж держит снимок и отображает»]**.

**(в) Окно синхронизации.** Полуинтервал `[право в базе, лимиты + порядок на записи прибора и событие на приборе)`. Прибор показывает снимок удержания, лимит не считает, отправку не решает (решает сервер записей по записи, M2). `/quota` после приёма — новые лимиты. **Витрина права** (какой тариф у мембраны) — новый тариф сразу; **витрина принятия** («прибор принял») — только по сигналу прибора; расщепление принято как уточнение T7, не его переоткрытие: «успех перехода» показывается не раньше прибора, право — сразу **[председатель: подтверждение по замечанию 8 аудита]**. Длительность — до приёма и события; закрывают сервер записей (приём) и прибор (событие); при отказе разноски — повтор.

**(г) Отказ разноски и гонки.** Откат права запрещён; право ждёт; повтор разноски с тем же порядком — идемпотентен (равная пара — пусто). Версия контракта порядок доставки не защищает (принадлежит релизу); защищает порядок перехода в полезной нагрузке и на записи прибора по правилу пары. Два запроса подряд — как сегодня `tariff_moved_concurrently` (`tariff-transition.service.ts:294`), победитель — новая строка журнала с большим порядком. Порядок перехода — не вторая правда: правда о тарифе — матрица и версия релиза (M1), порядок — курсор журнала доставки уже существующего `TariffChangeLog`.

**Носители.** Кабинет: право, журнал с порядком, повтор разноски, витрина права и витрина принятия. Сервер записей: запись лимитов + версия + порядок, `/quota`, событие перехода прибору. Прибор: приём события, удержание, сигнал «принял». Разноска: транспорт база → сервер записей.

**Строить:** колонка порядка перехода на `TariffChangeLog` (последовательность) **[аудит r2]**; версия + порядок в полезной нагрузке разноски и на записи прибора; предикат приёма по паре; событие перехода прибору — контракт `tariff-transition/v1` `{version, transitionOrder, limits}` **[председатель: имя]**; сигнал «принял» — поле `tariffTransition {version, transitionOrder, acceptedAt}` в `RuntimeNodeState` рядом с `overflowHold` (`packages/core/src/contracts/node-realtime/events.ts:192`) **[председатель: имя]**; повтор разноски; разведение витрины права и витрины принятия; удержание без авто-снятия; зуб 3 M1 дополняется равенством порядка.
**Не строить:** откат права; версию как счётчик смен; `release` по тарифу; «успех» по счётчику разноски; вторую арифметику «свободно» на приборе; переход через `/quota`; компенсацию `Membrane.tariffId`; M4/M5/оплату/промокод/пересев/форму гранул/умную очистку.

**Проверки (предмет · предикат · красный вход · кто краснеет · кто чинит; «кто чинит» — дом носителя вместо ролей комнаты, роли — при раздаче [председатель]):**
1. `packages/background-media/src/modules/devices/devices.service.ts` `syncMembraneContext` **[аудит r2: путь]** — при паре ≤ хранимой запись не меняется; красный: две разноски в обратном порядке → на записи более новая; CI `sync-membrane-order-guard`; сервер записей.
2. `packages/background-cabinet/src/modules/pair/membrane-context-fanout.service.ts` — полезная нагрузка несёт версию и порядок; красный: разноска без порядка при наличии журнала; CI `fanout-payload-has-order`; кабинет.
3. `packages/background-cabinet/src/modules/tariff/tariff-transition.service.ts` — при `failed > 0` право и журнал сохранены, повтор возможен; красный: отказ разноски откатил `tariffId`; CI `tariff-right-waits-on-fanout-fail`; кабинет.
4. `apps/client/src/lib/device-overflow-hold/deviceOverflowHold.ts` — после события перехода `isHeld()` остаётся истиной без human/cleanup; красный: событие снимает удержание; CI `hold-persists-on-tariff-event`; клиент.
5. `apps/client/src/lib/overflow-window/viewModel.ts` (+ `OverflowWindow.tsx`) — числа окна и плашки = снимок эпизода после повышения; красный: лимит в окне стал новым без «Возобновить»; CI `overflow-window-snapshot-stable`; клиент.
6. `apps/client/src/lib/device-overflow-hold/localGuard.ts` + `wiring.ts:46` **[аудит r2: ворота]** — страж не считает «свободно» и ворота не открываются по новым лимитам до `release`; красный: при удержании отправка пошла из-за роста лимита; CI `local-guard-no-quota-arith`; клиент.
7. `packages/core/src/contracts/node-realtime/events.ts` — поле `tariffTransition` состояния узла **[председатель: имя]** отдельно от `overflowHold`; красный: кабинет ставит «принял» по одному `POST /tariff 200`; CI `tariff-vitrine-waits-device`; кабинет + core.
8. приём события `tariff-transition/v1` на приборе — модуль рядом с `device-overflow-hold/wiring.ts` **[председатель: место]** — событие с парой ≤ известной отбрасывается; красный: старое событие после нового перетёрло состояние; CI `transition-event-applies-order`; клиент.

**Definition of Done (одно имя, красный случай):** 1 `sync-membrane-order-guard` — красный: обратный порядок двух разносок оставил на записи старую пару. 2 `fanout-payload-has-order` — красный: тело без порядка. 3 `tariff-right-waits-on-fanout-fail` — красный: `failed > 0` вернул старый `tariffId`. 4 `hold-persists-on-tariff-event` — красный: событие сняло удержание. 5 `overflow-window-snapshot-stable` — красный: окно показало новый лимит без «Возобновить». 6 `local-guard-no-quota-arith` — красный: страж открыл отправку по росту лимита. 7 `tariff-vitrine-waits-device` — красный: «принял» без поля `tariffTransition` от прибора. 8 `transition-event-applies-order` — красный: событие со старой парой перетёрло новую.

**Регламент (вне DoD):** словарь в коде и UI — право, лимит, запись прибора, разноска, версия контракта, порядок перехода, удержание, окно оператора, окно синхронизации, сервер записей, витрина; «принял», «повтор», «откат» (запрещён), «возобновить». Витрину права и витрину принятия не смешивать. Повтор разноски — тот же порядок; равная пара — пусто без ошибки. Приоритет приёмки — Teamlead по DoD 1–8; контракт полей — Архитектор и Структурщик до кода UI (регламент прогона 2, [Teamlead] #6); роли исполнения — при раздаче спринта.

**Список посылок** — по протоколу прогона 2 (10 посылок) с поправками аудита: три номера исправлены (реплики «Принимаю» комната не сосчитала) **[аудит r2]**; P10 сужена: предикат — пара, порядок — новая колонка **[аудит r2]**; цитаты — первые пять слов реплик прогона 2:

| # | Тип | Посылка | Где сработала |
|---|---|---|---|
| P1 | норма | T7: переход — событие с версией; витрина не раньше прибора; не перечитывать квоту как переход | [Архитектор] #1 «Конструкция (а). Цепочка носителей: 1)»; [Музыкант] #2 «Канал `/quota` после стопа ≥»; [Teamlead] #1 «Планка прогона 2 жёсткая. Данности» |
| P2 | норма | Удержание снимает только human/cleanup; авто по месту нет; тариф ≠ release | [Teamlead] #2 «Фиксирую развилку (б) закрытой данностью:»; [Верстальщик] #1 «С витрины: после повышения окно»; [Музыкант] #1 «С тракта прибора: отправку проб» |
| P3 | план | M1: версия принадлежит релизу/сетке; разноска база → сервер записей; зуб — поля `Tariff ↔ Device` | [Структурщик] #1 «Лемма порядка. Версия контракта —»; [Архитектор] #2 «Порядок перехода — не вторая»; [Математик] #6 «Норма T7/T11 и вердикты M1/M2» **[аудит r2: был #5]** |
| P4 | план | M2: «свободно» считает сервер записей; «тариф» в функции = лимиты записи прибора; окно = снимок без второго запроса | [Математик] #3 «Уточняю зазор. В окне синхронизации»; [Структурщик] #2 «Окно синхронизации — полуинтервал `[право»; [Верстальщик] #1 «С витрины: после повышения окно» |
| P5 | факт | `TariffChangeLog` пишется в транзакции `selectTariff`; гонка `tariff_moved_concurrently` | [Математик] #2 «Отказ разноски: право в `TariffChangeLog`»; [Архитектор] #1 «Конструкция (а). Цепочка носителей: 1)» |
| P6 | факт | Счётчик разноски — доставка, не подтверждение; версии в полезной нагрузке нет; при `failed` право уже записано | [Структурщик] #6 «Уточнение фактуры: сегодня version в» **[аудит r2: был #5]**; [Архитектор] #1 «Конструкция (а). Цепочка носителей: 1)»; [Teamlead] #3 «(г) закрываем без отката. Строить:» |
| P7 | факт | `syncMembraneContext` копирует лимиты; `/quota` читает строку заново | [Структурщик] #2 «Окно синхронизации — полуинтервал `[право»; [Структурщик] #3 «Носители по домам. Кабинет: `tariff-transition.service.ts`» |
| P8 | факт | удержание: выход human/cleanup; смена тарифа — не release; окно/плашка — снимок | [Верстальщик] #6 «Факт UI: wiring release только» **[аудит r2: был #5; в реплике «human» — из `overflow-window/controller.ts:106`, не `wiring.ts`]**; [Музыкант] #1 «С тракта прибора: отправку проб»; [Верстальщик] #3 «Плашка после повышения: тот же» |
| P9 | норма | Данность прогона 1: право ждёт + повтор разноски; откат запрещён | [Teamlead] #3 «(г) закрываем без отката. Строить:»; [Математик] #2 «Отказ разноски: право в `TariffChangeLog`»; [Архитектор] #4 «DoD кабинета: `tariff-right-waits-on-fanout-fail` — при» |
| P10 | план | Носитель порядка перехода — `transitionOrder` из `TariffChangeLog` в полезной нагрузке и на записи прибора; предикат — пара `(версия, порядок)` **[аудит r2: комната писала конъюнкцию и «id журнала»]** | [Математик] #1 «Формализую предикат приёма на записи»; [Структурщик] #1 «Лемма порядка. Версия контракта —»; [Структурщик] #4 «Имя поля: `transitionOrder` (число/bigint =» |

## Вердикт M2 (P) — сборка председателя (источник: прогон 2 — содержание и форма; поправки аудита прогона 2; аудит сборки 1–2), ПРИНЯТ аудитом 08.09

Пометка **[аудит r2]** — поправка по замечанию аудита прогона 2 с источником в протоколе; **[председатель]** — решение, которого комната не приняла, назначено председателем и подлежит замене исполнителем при первом коммите.

**Словарь:** право — что даёт тариф (матрица); объём — сколько занято в доме ресурса; доступная квота (свободно) — `max(0, лимит − занято)` на оси; окно — снимок чисел эпизода отказа; «тариф» в функции = лимиты с записи прибора (переопределение комнаты, прогон 1); удержание — `DeviceOverflowHold`; сервер записей — media.

**(а) Носитель функции и кто зовёт.** Чистая функция «доступная квота» живёт в `@membrana/plugin-contracts`, файл `packages/plugin-contracts/src/buffer-overflow/available-quota.ts`, экспорт через индекс пакета (экспорт пакета только `"."` — подпуть-импорт невозможен **[аудит r2]**). Пакет ESM-only; сервер записей (CJS) берёт функцию динамическим `import()` по прецеденту `packages/background-media/src/modules/collections/plugin-host.service.ts:63` — **не** «как reasons»: словарь причин в `buffer-overflow-refusal.ts:5-11,47` чеканится литералами под `satisfies`, рантайм оттуда не импортируется **[аудит r2]**. `buildBufferOverflowRefusal` синхронна (`buffer-overflow-refusal.ts:104-122`), поэтому функция грузится один раз при инициализации сервиса и кэшируется, сборка отказа остаётся синхронной **[председатель: способ; комната цену не назвала]**. Нового пакета не создавать; `@membrana/core` в сервер записей не подключать. Зовёт: сервер записей при сборке `/quota` (`devices.service.ts:223-278 getQuota`) и при отказе; клиент в живом контуре только читает готовое «свободно» из ответа; клиент в режиме окна: **«свободно» из снимка отказа в удержании, если отказ его нёс; иначе та же функция на `used/limit` снимка удержания** **[аудит r2: правило Архитектора #4 вместо дизъюнкции]**. Третьей арифметики нет.

**(б) Оси и сложение.** Оси независимы, между собой не складываются: буфер — байты (`kind === 'buffer'`); пользовательское хранилище — байты (`user` + `system` с ключом ≠ `TARIFF_DATASET_SYSTEM_KEY`); тарифный системный набор — вне байтов, только `sampleCount` + `catalogId`; рабочие места — счётная ось; узлы (`maxNodesPerMembrane`) — ось кабинета, не функция сервера записей. Предикат отказа байтов: **свободно < размер входящего** ⇔ `used + incoming > limit`; «свободно = 0» — частный случай, не эквивалент. Отказ мест: `used >= maxUserWorkspaces`.

**(в) Окно.** Снимок эпизода — `used/limit/свободно` в миг отказа; не неделя холода и не момент перехода (M3/M4). Автор чисел — сервер записей в теле отказа; носитель снимка — удержание на приборе (сервер числа эпизода не хранит: `overflow-episode-registry.ts:12-14`); режим окна исполняет клиент без второго запроса `/quota` (данность M5 «полный буфер»).

**(г) Единый источник числа.** Истина объёма в рантайме — сервер записей + функция. Витрина объёма и библиотека кабинета — `media-bridge.service.ts:224 getQuota` → `sample-library.service.ts:26-41 mapQuota`, прокидывает «свободно», из `Tariff` не вычисляет. Окно оператора — отказ + удержание, то же «свободно». Витрина мембраны `/v1/tariffs` — только право (лимиты), «свободно» не подменяет. **Факт и красный вход:** клиент и библиотека сегодня считают «свободно» сами — `apps/client/src/lib/overflow-window/viewModel.ts:82` (`freeBytes = max(0, limit − used)`) и `packages/services/media-library/src/quota-status.ts:19-24, 44-50`; после переезда локальное `limit − used` там — красный.

**Строить:** 1) чистая функция в `plugin-contracts`; 2) расширение `BufferOverflowRefusal` (`packages/plugin-contracts/src/buffer-overflow/refusal.ts:44`) полем «свободно» по отказавшей оси — носитель `plugin-contracts`, добавка к телу, транспорт 200 не меняется; 3) `/quota` отдаёт «свободно» по осям буфера и хранилища; 4) `getQuota` и сборка отказа зовут функцию; 5) `mapQuota`, `viewModel`, `quota-status` потребляют «свободно» из ответа или функцию на снимке; 6) удалить локальный вычет в клиенте и библиотеке.
**Не строить:** новый пакет; зависимость сервера записей от core; проводку `domain/quota-ledger.ts` в рантайм; вторую арифметику на клиенте; вычисление «свободно» из `Tariff` в кабинете; пересев и версию (M1); повышение (M3); понижение и холод (M4); наборы при переходе (M5); гранулы; умную очистку; оплату.

**Проверки (предмет · предикат · красный вход · чинит):**
- C1 `packages/plugin-contracts/src/buffer-overflow/available-quota.ts` — `f(limit, used) = max(0, limit − used)`, при `used > limit` → 0; красный: отрицательное «свободно»; чинит автор функции.
- C2 `packages/background-media/src/modules/samples/buffer-overflow-refusal.ts` **[аудит r2: путь]** — отказ ⇔ `used + incoming > limit`, тело несёт «свободно» = функция по оси; красный: отказ без поля или поле ≠ функции; чинит сервер записей.
- C3 `packages/background-media/src/modules/devices/devices.service.ts:223-278 getQuota` **[аудит r2: путь]** — `/quota` несёт «свободно» по буферу и хранилищу = функция; красный: поля нет; чинит сервер записей.
- C4 `apps/client/src/lib/overflow-window/viewModel.ts` — нет локального `limit − used`; красный: строка `:82`; чинит клиент.
- C5 `packages/services/media-library/src/quota-status.ts` — уровень full/warning из «свободно», не из своего вычета; красный: `:19-24, :44-50`; чинит библиотека.
- C6 `apps/client/src/lib/device-overflow-hold/` — снимок удержания хранит `used/limit` и «свободно» с отказа; красный: удержание без чисел оси при открытом окне; чинит клиент.
- C7 `packages/background-cabinet/src/modules/sample-library/sample-library.service.ts:26-41 mapQuota` — прокидывает «свободно» с моста, из `Tariff` не считает; красный: вычет из `Tariff` или потеря поля; чинит кабинет.
- C8 `devices.service.ts:247-253` **[аудит r2: путь вместо «оси getQuota»]** — тарифный набор не в байтах хранилища, прочие системные — в байтах; красный: `sampleCount` тарифа в `usedBytes`; чинит сервер записей.

**Definition of Done (одно имя, красный случай):**
- D1 unit на `available-quota.ts` (имя даёт исполнитель): таблица (100,0)→100; (100,100)→0; (100,150)→0; (100,40)+50 → отказ, свободно 60 ≠ 0; красный: тест ждёт «свободно = 0» на любом отказе.
- D2 тест сборки отказа (имя даёт исполнитель; размещение рядом с `buffer-overflow-refusal.ts` — **[председатель]**): `BufferOverflowRefusal` несёт «свободно» по отказавшей оси = функция; красный: поля нет.
- D3 контракт `/quota` (имя даёт исполнитель; размещение рядом с `devices.service.quota-freshness.test.ts` — **[председатель]**): JSON несёт «свободно» на буфере и хранилище; красный: только `used/limit`.
- D4 `apps/client/src/lib/overflow-window/viewModel.ts`: нет `limit − used` в расчёте; красный: строка `:82`.
- D5 `packages/services/media-library/src/quota-status.ts`: уровень не из своего вычета; красный: full через свой вычет.
- D6 `sample-library.service.ts mapQuota`: «свободно» проходит в модель библиотеки; красный: поле потеряно или расчёт из `Tariff`.

**Регламент (вне DoD):** витрину права (`/v1/tariffs`) с витриной объёма не смешивать; чтение «свободно» оператором — после влития полей; загрузка функции в CJS — один `import()` при инициализации сервиса.

**Список посылок** — по протоколу прогона 2 с поправками аудита (сужения и переадресации помечены **[аудит r2]**; цитаты — первые пять слов реплик прогона 2, номера по ролям пересчитаны):

| # | Тип | Посылка | Где сработала |
|---|---|---|---|
| P1 | норма | Оси независимы; тарифный системный набор вне байтов; занято считает только сервер записей; окно = снимок эпизода; «тариф» в функции = лимиты записи прибора (держание прогона 1 + данности) | [Архитектор] #1 «Конструкция: право в матрице →»; [Музыкант] #2 «Окно как «неделя холода» или»; [Архитектор] #2 «Оси (б), без сложения между»; [Музыкант] #6 «Уточнение с края: system≠TARIFF_DATASET_SYSTEM_KEY внутри» |
| P2 | факт | `getQuota` считает занято по виду коллекции; тарифный каталог вне байтов — `devices.service.ts:223-278` **[аудит r2: клаузы `resolveDeviceLimits`/`sizeBytes` сняты — в репликах не сработали]** | [Архитектор] #1 «Конструкция: право в матрице →»; [Музыкант] #6 «Уточнение с края: system≠TARIFF_DATASET_SYSTEM_KEY внутри» |
| P3 | факт | `axisRefuses`: `used + incoming > limit`; инвариант `used ≤ limit` — `buffer-overflow-refusal.ts:100-102` | [Математик] #1 «Формализую. Оси независимы (данность прогона»; [Математик] #4 «Инвариант: ∀ оси байтов `used» **[аудит r2: в протоколе стоял #5 — инвариант в #4]** |
| P4 | факт | `plugin-contracts` ESM-only; сервер записей (CJS) — динамический `import()`, статически только типы — `buffer-overflow-refusal.ts:5-8`; зависимости сервера записей от core нет | [Структурщик] #1 «Лемма пакета. Словарь отказа уже»; [Структурщик] #3 «Место в пакете: `packages/plugin-contracts/src/buffer-overflow/available-quota.ts` (имя» |
| P5 | факт | Тело отказа «свободно» не несёт — `buildBufferOverflowRefusal` `:104-122` **[аудит r2: строки]**; расширение — явное «Строить» | [Teamlead] #2 «Расширение контракта отказа полем «свободно»»; [Структурщик] #2 «Кто зовёт функцию. (1) Сервер» |
| P6 | факт | Вторая арифметика: `viewModel.ts:82`, `quota-status.ts:19-24,44-50` | [Верстальщик] #1 «С витрины: окно (#2316) уже»; [Верстальщик] #3 «Красный вход зафиксировать явно: после» |
| P7 | факт | Сервер числа эпизода не хранит — `overflow-episode-registry.ts:12-14`; снимок в удержании — `device-overflow-hold/types.ts:11-12` | [Музыкант] #2 «Окно как «неделя холода» или»; [Математик] #2 «Кто задаёт окно: при отказе»; [Структурщик] #2 «Кто зовёт функцию. (1) Сервер» |
| P8 | факт | `mapQuota` копирует `used/limit`, `Tariff` не трогает — `sample-library.service.ts:26-41` | [Teamlead] #3 «Кабинет: `sample-library.service.ts:26-41 mapQuota` копирует used/limit»; [Верстальщик] #2 «Единый источник числа для витрины» |
| P9 | факт | Рабочие места — своя ось, отказ `used >= max` — `device-workspaces.service.ts:113` | [Архитектор] #2 «Оси (б), без сложения между»; [Математик] #6 «Рабочие места: availableWorkspaces = max(0,» |
| P10 | план | M1: лимиты на записи прибора — производная разноски; `maxNodesPerMembrane` — ось кабинета **[аудит r2: клауза «функцию M1 не решал» снята — в репликах не сработала; узлы → Архитектор #2]** | [Архитектор] #1 «Конструкция: право в матрице →»; [Архитектор] #2 «Оси (б), без сложения между»; [Структурщик] #6 «Версия контракта отказа: поле available» |
| P11 | норма | Квота ≥ 0; свободно = функция(тариф, занято, окно) **[аудит r2: сужена; ссылки на T8/T9/T12/T13 сняты]** | [Математик] #1 «Формализую. Оси независимы (данность прогона»; [Математик] #3 «Сигнатура чистой f (без UI):» |
| P11а | норма | Умолчание политики — stop **[аудит r2: выделено из P11]** | [Музыкант] #1 «С тракта прибора: при stop» |
| P11б | норма | Отказ — 200 + причина; «свободно» — добавка к телу, не смена транспорта **[аудит r2: выделено из P11]** | [Teamlead] #2 «Расширение контракта отказа полем «свободно»» |
| P12 | норма | Окно оператора без второго запроса квоты (M5 «полный буфер») | [Верстальщик] #1 «С витрины: окно (#2316) уже»; [Архитектор] #3 «(г) Единый источник: **сервер записей**» |

Правило режима окна — [Архитектор] #4 «Цена альтернативы «кабинет зовёт f» **[аудит r2]**. Типы «факт» сверены аудитом со стволом.

## Вердикт M1 (R) — сборка председателя (источники: прогон 2 — содержание; прогон 3 — форма; аудиты 1–3; аудит сборки 1–2), ПРИНЯТ аудитом 08.09

Пометка **[аудит r3]** — поправка по замечанию аудита прогона 3; **[председатель]** — имя, которого комната не дала, назначено председателем и подлежит замене исполнителем при первом коммите.

**Словарь (объявлен комнатой до употребления):** *пересев* — вся цепочка релиз → сетка → база кабинета → записи приборов; *разноска* — звено база кабинета → сервер записей по мембране; *версия контракта* — число, которое издаёт релиз; `contractVersion` — допустимое имя поля в коде рядом с термином.

**(а) Цепочка пересева и вызов.** S1 — закрепление релиза матрицы (человек, матрица в strategic-docs). S2 — `yarn tariff:reseed`: релиз → сетка (план, после влития Б #2331); зовёт человек по слову или CI на мердж релиза. S3 — `yarn tariff:grid`: форма сетки + «сетка = релиз» (CI). S4 — сетка едет в образ кабинета (Dockerfile). S5 — кабинет **при старте только читает** сетку в память, один раз за процесс, **без записи** в базу и записи приборов. S6 — проекция сетка → строки `Tariff` базы кабинета вместе с версией контракта; зовёт человек или ритуал наката, не HTTP-трафик. S7 — разноска база → записи приборов существующим `membrane-context-fanout` (привязка, смена тарифа, политика) плюс проход по всем мембранам на накате; кабинет зовёт, сервер записей принимает. Порядок строгий: S6 только после зелёного S3; S7 после S6. Сервер записей сетку не читает.

**(б) Версия контракта.** Номер издаёт релиз. Поле `version` сетки (сейчас `1`) — **то же поле**, при пересеве `G.version := R.version`, второго канона нет. Путь: релиз → сетка → **колонка версии на `Tariff`** (S6) **[аудит r3: возвращено из прогона 2]** → новое поле записи прибора на сервере записей (разноска кладёт версию в ту же полезную нагрузку, что квоты) → прибор отличает новую правду от старой сравнением с последней известной. `updatedAt` у записи прибора нет — не использовать.

**(в) Ровно три зуба.**
- **Зуб 1, релиз ↔ сетка.** Предмет: релиз `tariff-matrix` в контейнере strategic-docs (после Б) + `docs/tariffs/tariff-grid.json`. Предикат: слепок сетки равен релизу **и `version` равен** **[возвращено из прогона 2: зуб 1 «G.version = R.version», DoD 1]**. Краснеет CI (`yarn tariff:grid`); чинит автор релиза через reseed. Красный вход: ручная правка сетки.
- **Зуб 2, сетка ↔ база кабинета.** Предмет: сетка + `Tariff` (cabinet `schema.prisma`). Пары: `userStorageQuotaBytes ↔ storage.hot`, `bufferQuotaBytes ↔ storage.buffer`, `maxNodesPerMembrane ↔ nodes.max`, `maxUserWorkspaces ↔ workspaces.user.max`, **колонка версии контракта на `Tariff` = `version` сетки** **[аудит r3: возвращено; имя колонки комната не выбирала — исполнитель даёт одно имя, второго канона нет]**; `storage.cold` вне предиката — колонки нет, судьба в M4. Краснеет тест кабинета; чинит S6. Красный вход: ручной SQL по `Tariff` или возврат посева из скаляров.
- **Зуб 3, база кабинета ↔ записи приборов.** Предмет: cabinet `Tariff` (+ политика) ↔ media `Device` (`packages/background-media/prisma/schema.prisma`). Пары: `Tariff.userStorageQuotaBytes ↔ Device.userStorageQuotaBytes`, `Tariff.bufferQuotaBytes ↔ Device.bufferQuotaBytes`, `Tariff.maxUserWorkspaces ↔ Device.maxUserWorkspaces`, `Tariff.datasetCatalogId ↔ Device.datasetCatalogId`, версия контракта ↔ новое поле `Device`. `maxNodesPerMembrane` у записи прибора нет — в зуб не входит, остаётся в базе кабинета. Политика переполнения входит как поле мембраны/прибора, не тарифа: `MembraneBufferPolicy.mode` при привязке, иначе cabinet `Device.bufferPolicy` ↔ media `Device.bufferPolicy` **[аудит r3: вместо несуществующей `Membrane.bufferPolicy`; правило «эффективная = привязка ? мембрана : прибор» — председатель, по комментарию cabinet `schema.prisma:138-139`]**. Предикат — равенство полей, **не** счётчик «обновлено/не удалось» разноски. Краснеет проверка после разноски; чинит повторная разноска. Красный вход: прямой PATCH или SQL по записи прибора в обход кабинета. Запрет ручной правки производных = эти три красных входа, не просьба.

**(г) Переезд без остановки прода.** Сетка: с рукописной на след релиза через reseed, дальше только reseed. База: (1) `checkpoint-v1` и `observatory-v1` получают автора в дереве через релиз → сетка → база (конец пути #2297); (2) `tariff-scalars.json` и тарифный блок `prisma/seed.mjs` **выводятся из авторов квот** — иначе повторный сид перепишет free 1 ГБ на 512; (3) мёртвый `ensureFreeTariff()` удалить; (4) S6 пишет free и остальные id значениями релиза. Записи приборов: S7 накатом, без общей остановки; зуб 3 ловит окна расхождения. `TARIFF_GRID_MODE` и предикаты `tariff-cutover` (S9): на переезд подчинить зубам как зеркало готовности, **затем снять** как переключатель правды; `tariff-cutover` не источник чисел и не импортирует скаляры; два рубильника истины запрещены; накатная сверка = зубы 1–3 плюс версия, не новый режим «matrix mode».

**Носители.** Матрица: релиз, номер версии, гранулы (форма — Б). Кабинет: сетка в образе, чтение при старте, база, S6, вызов разноски, переход тарифа. Сервер записей: запись прибора, приём контекста мембраны, отказы «полон» (вне цепи пересева). Прибор: читает запись и версию; тариф не авторствует.

**Строить:** reseed (после Б); расширение зуба сетки до «сетка = релиз»; **поля версии контракта на `Tariff` и на `Device`** **[аудит r3: «на B и D» возвращено]**; тесты зубов 2 и 3; ритуал S6/S7 с проходом по мембранам; нейтрализация seed/скаляров; удаление `ensureFreeTariff`; подчинение и снятие cutover/флага.
**Не строить:** форму гранул (#2331); колонку холода; M2–M5; оплату, умную очистку; клиентский кэш каталога; второй флаг правды; новый протокол вместо разноски; рантайм-реестр квот «заодно»; опору на `updatedAt`; экран «статус пересева».

**Вызов цепочки (регламент, не DoD):** влить Б → на мердж релиза CI или человек: reseed → grid → S6 → S7 с версией → выкатка кабинета (процесс при старте читает новую сетку); при красном зубе 3 — повтор разноски.

**Definition of Done (предикаты):**
1. Зуб 1: `yarn tariff:grid` на порче `tariff-grid.json` (число или `version` ≠ релиз) — exit ≠ 0; на сетке из релиза — 0.
2. Зуб 2: тест `packages/background-cabinet/src/domain/tariff-grid-base.test.ts` **[председатель]** — красный при расхождении любой из четырёх пар или колонки версии контракта `Tariff` ≠ `version` сетки; фикстура без колонки холода не краснеет.
3. Зуб 3: команда `yarn tariff:devices-check` (`scripts/tariff-devices-check.mjs` + тест рядом) **[председатель]** — красный при неравенстве любой из пяти пар или политики; счётчик разноски критерием не является.
4. Старт кабинета: тест на `domain/tariff-grid-source.ts` — ровно одно чтение сетки за процесс и **ноль записей в `Tariff` на старте** **[аудит r3: возвращено]**.
5. Посев: `prisma/seed.mjs` не читает `tariff-scalars.json` для квот (grep в тесте seed); прогон seed не меняет free в обход сетки.
6. `rg ensureFreeTariff packages/background-cabinet` — пусто.
7. Версия: миграции базы кабинета и сервера записей несут поле версии; тест разноски рядом с `membrane-context-fanout.service.ts` **[председатель: файл]** — красный, если поле версии на записи прибора ≠ `version` сетки после пересева. Третья клауза DoD 7 прогона 2 (тест прибора-потребителя «новая правда отличима») **в сборке снята**: чтение версии прибором — деталь прошивки после появления поля на записи (прогон 2, Музыкант; прогон 3 [Музыкант] #3 «На приборе M1 ничего не играет» — опрос версии на MCU не M1), а прибор о переходе не узнаёт до #2323.
8. Второй переключатель: `rg TARIFF_GRID_MODE packages/ scripts/` **[аудит r3: + scripts/]** пуст после переезда; `scripts/lib/tariff-cutover.mjs` не опирается на скаляры — предикат целить в строку-носитель `S0_scalars` в `CUTOVER_REQUIREMENTS` (`tariff-cutover.mjs:18-21`): импорта там нет и сегодня, так что «не импортирует» — проверка без предмета **[аудит сборки]**.

**Список посылок** — по протоколу прогона 3 (18 посылок с цитатами) с поправками аудита: P13 → [Музыкант] #4 «Флаг TARIFF_GRID_MODE и tariff-cutover»; P16 сужена до «разноска — `membrane-context-fanout`» → [Структурщик] #2 «Вызов цепочки: после влития» (вторая половина, «сервер записей сетку не читает», — прогон 2 P4, [Архитектор] #4); P9 — вместо `Membrane.bufferPolicy` читать `MembraneBufferPolicy.mode` / `Device.bufferPolicy` (та же поправка, что в зубе 3); P7 сужена до «Tariff ↔ Device» ([Математик] #2 «Три зуба, не больше»), мембрана — из [Структурщик] #3; добавлена строка типа **план**: спринт Б (#2331) даёт релиз `tariff-matrix`, `yarn tariff:reseed`, зуб «сетка = релиз» ([Структурщик] #2). Типы «факт» сверены аудитом со стволом.

## Вердикт M1 (R), прогон 1 — кратко (ОТКЛОНЁН аудитом, след; полный: протокол)

- **Цепочка пересева:** релиз матрицы → (CI на мердж) `yarn tariff:reseed` → сетка → проекция сетки в базу кабинета (CI и/или кабинет при старте сверяет базу к сетке образа) → событие разноски кабинет → записи приборов на сервере записей (существующий канал + пакетный переезд). Человек по слову — только повтор той же команды; сервер записей сетку не читает.
- **Версия контракта:** `contractVersion` — монотонный атрибут релиза, копируется в сетку, базу, событие и запись прибора; применять только если входящая версия больше хранимой, иначе идемпотентный no-op; `updatedAt` версией не считать; запись прибора хранит версию уже в M1, прибор-край — позже (#2323).
- **Зубы:** релиз↔сетка (CI, чинит пересборка), сетка↔база (сверка/гейт кабинета, чинит повторная проекция), база↔записи (счётчик разноски, чинит повтор события); равенство — по каноническому дампу квотных полей + id тарифов + версия; запрет ручной правки производных — предикат гейта, не просьба.
- **Переезд без остановки:** пересев сетки → накатная сверка кабинета (free 1 ГБ уходит значением из релиза, не SQL) → пакетное событие на все записи с новой версией → мониторинг зубов; середину руками не правят; поведение при переполнении уже занятого — M2/M4.
- **Не строить:** форму гранул (#2331), «доступную квоту» (M2), повышение (M3), понижение/холод (M4), наборы (M5), чтение сетки сервером записей, UI-редактор квот, версию эпизода переполнения как тарифную правду.

## Вопросы и статусы

| M | Вопрос | Статус | Протокол |
|---|---|---|---|
| M0 | Порядок пяти кандидатов (DAG) | ратифицирован (прогон 2), аудит чист | `docs/seanses/tariff-single-truth-m0-order-2026-09-08.md` |
| M1 | (R) Пересев производных из матрицы | прогон 1 отклонён (13 замечаний, фактура); прогон 2 — содержание принято, форма отклонена; прогон 3 — форма отклонена по остатку; сборка председателя из r2+r3 с поправками аудита — **принята аудитом (второй прогон сборки)** | `docs/seanses/tariff-single-truth-m1-reseed-2026-09-08.md` (след), `…-m1-reseed-r2-…` (содержание), `…-m1-reseed-r3-…` |
| M2 | (P) Право и объём | прогон 1 (37 реплик) отклонён по существу; прогон 2 (36 реплик) — содержание принято, форма отклонена; сборка председателя — **принята аудитом (второй прогон сборки)** | `docs/seanses/tariff-single-truth-m2-rights-volume-2026-09-08.md` (след), `…-m2-rights-volume-r2-…` |
| M3 | (U) Повышение тарифа | прогон 1 (33 реплики) отклонён по существу (версия контракта как счётчик переходов против M1; локальный лимит прибора против M2; хедж по носителю); прогон 2 (37 реплик) — содержание годится, дефект предиката и форма; сборка председателя — **принята аудитом (второй прогон сборки)** | `docs/seanses/tariff-single-truth-m3-upgrade-2026-09-08.md` (след), `…-m3-upgrade-r2-…` |
| M4 | (D) Понижение тарифа и холодное хранилище | прогон 1 (39 реплик) **отклонён аудитом**: границы чисты и второго носителя правды нет, но две ошибки фактуры (у ячейки `storage.cold` четыре читателя, а не один; понижение сегодня поднимает удержание само) плюс форма; повестка дополнена, **прогон 2 отложен** — панельная цепочка LLM исчерпана | `docs/seanses/tariff-single-truth-m4-downgrade-cold-2026-09-08.md` (след) |
| M5 | (S) Наборы звуков при переходе | повестка готова, фактура сверена по коду; **созыв отложен** — панельная цепочка LLM исчерпана (все три звена overlay), развилка у владельца | `docs/meeting/tariff-single-truth/M5_AGENDA.md` |
