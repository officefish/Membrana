# Read-only аудит заседания static-mmbrn-container

| Поле | Значение |
|---|---|
| дата | 2026-08-03 |
| аудитор | Codex S-M5 Read-Only Auditor |
| agent | `019fc720-8fec-7c63-8d9f-28a52e2fb5c2` |
| режим | read-only; файлов не изменял; в обсуждении не голосовал |
| председатель | отдельная родительская сессия Codex |
| вердикт до M0 | **PASS** |

## Проверки

- В M0 ровно один ID-вопрос: `P1`.
- Все семь кандидатов перечислены непосредственно в строке вопроса.
- Предмет совпадает с `MEETING_BRIEF.md`; запреты сохранены.
- M0 требует только DAG и порядок зависимостей.
- Решений по существу, выбора технологий или реализации в M0 нет.
- Председатель и аудитор разделены, S-M5 соблюдён.
- Требование списка посылок присутствует.

Административное замечание аудитора выполнено председателем: placeholder в
`MEETING_ACTIVE.md` заменён удостоверенным именем аудитора до запуска M0.

## Повторный предаудит после доставки фактуры

Перед запуском M0 председатель добавил в `M0_AGENDA.md` фактическую подложку из
`MEETING_BRIEF.md`, потому что ссылка из `topic-file` не раскрывается автоматически.
Тот же read-only аудитор повторно дал **PASS**:

- по-прежнему ровно один ID-вопрос `P1`;
- все семь кандидатов остаются в несущей строке;
- новых утверждений и расхождений с брифом нет;
- технологии, архитектура и реализация не выбраны;
- S-M5 и требование списка посылок сохранены.

## Постаудит M0, прогон 1

Вердикт аудитора: **BLOCK**, к владельческой ратификации не допущен.

1. Заявленный минимальный DAG содержал ребро `1→3`, транзитивное через `1→2→3`.
2. Порядок `3` перед `4` был обоснован ложной посылкой: пути `3→5→7` и `4→6→7`
   имеют одинаковую длину.

Остальное выдержано: семь кандидатов сохранены, восьмого нет, список посылок есть,
существо M1..M7 не решено. Сам линейный порядок допустим, но его основание непригодно.
Протокол сохранён в `docs/seanses/rejected/` и исключён из корпуса состоявшихся комнат.

## Предаудит M0, прогон 2

Вердикт аудитора: **PASS**.

- минимальный DAG теперь явно означает транзитивную редукцию;
- равные ветви запрещено объявлять разными, tie-break требует проверяемого факта;
- в повестке по-прежнему ровно один `P1` и те же семь кандидатов;
- нового вопроса и решения по существу M1..M7 не появилось;
- `MEETING_ACTIVE.md` честно исключает первый прогон и запрещает M1 до ратификации.

## Постаудит M0, прогон 2

Вердикт аудитора: **BLOCK**, к владельческой ратификации не допущен.

1. Протокол отверг `5→6`, но удалил `5→7` как будто существует путь `5→6→7`.
   Роль Affine перестала быть предшественником финального переезда.
2. Tie-break `3/4` исправлен, но линейный порядок не назвал отдельные основания выбора
   между одновременно доступными `4/5`, а затем `5/6`.

Записанный порядок `1, 2, 3, 4, 5, 6, 7` топологически допустим, но причинный DAG
неполон, а два выбора в его линейном расширении остались без основания. Протокол
сохранён в `docs/seanses/rejected/` и исключён из корпуса состоявшихся комнат.

## Предаудит M0, прогон 3

Вердикт аудитора: **PASS**.

- причинная полнота закреплена правилом `5→7`, когда `5→6` отсутствует;
- каждый шаг линейного расширения с несколькими ready-узлами требует назвать набор и
  проверяемый tie-break;
- при отсутствии содержательного различия разрешена только объявленная детерминированная
  конвенция;
- один `P1`, семь кандидатов, S-M5 и запрет M1 до ратификации сохранены.

## Постаудит M0, прогон 3

Вердикт аудитора: **BLOCK**, к владельческой ратификации не допущен.

Содержательная часть прошла: причинно полный минимальный DAG содержит семь рёбер,
порядок `1, 2, 4, 3, 5, 6, 7`, ready-наборы и tie-break названы, роль Affine достигает
переезда по пути `5→6→7`. Блокирующий дефект один: DoD протокола заявил
«протокол ратифицирован владельцем», хотя владельческого акта ещё не было.

Протокол сохранён в `docs/seanses/rejected/`. Совпадение будущего состояния с ложным
утверждением задним числом не лечит подделку гейта.

## Предаудит M0, прогон 4

Вердикт аудитора: **PASS**.

- повестка требует точную формулу «передано владельцу, ратификация ожидается»;
- преждевременное «ратифицировано» явно запрещено;
- один `P1`, семь кандидатов, причинная редукция, ready/tie-break и S-M5 сохранены;
- запрет процедурный и не решает существо будущих комнат.

## Постаудит M0, прогон 4

Вердикт аудитора: **BLOCK**, к владельческой ратификации не допущен.

Содержательная часть прошла: DAG `1→2, 2→3, 2→4, 3→5, 4→6, 5→6, 6→7`, порядок
`1, 2, 3, 4, 5, 6, 7`, полные ready-наборы и честные tie-break. Владельческий гейт
назван ожидающим.

Блокирующий дефект: DoD потребовал создать `docs/meeting/static-mmbrn-container/
M0_PROTOCOL.md` и продублировать туда DAG и порядок. Регламент назначает носителем
протокол `docs/seanses/`, а `MEETING_ACTIVE.md` — указателем контейнера. Второй носитель
вердикта запрещён. Протокол сохранён в `docs/seanses/rejected/`.

## Предаудит M0, прогон 5

Вердикт аудитора: **PASS**.

- единственный носитель M0 — инструментальный протокол `docs/seanses/`;
- `MEETING_ACTIVE.md` остаётся только указателем;
- `M0_PROTOCOL.md` и иной второй носитель явно запрещены;
- один `P1`, семь кандидатов, причинная редукция, ready/tie-break, владельческий гейт
  и S-M5 сохранены.

## Постаудит M0, прогон 5

Вердикт аудитора: **BLOCK**, к владельческой ратификации не допущен.

Формальная часть прошла: минимальный DAG, ready/tie-break, владельческий гейт и один
носитель выдержаны. Блокирующая посылка П-3 объявила фактом, что Panel-грант привязан
к конкретному материалу с адресом. Бриф говорит лишь о существующих грантах разделов,
а гранулярность `контейнер/коллекция/документ` оставляет вопросу 3.

Ложная посылка обосновала `2→3` и предрешила часть будущей комнаты доступа. Протокол
сохранён в `docs/seanses/rejected/`.

## Предаудит M0, прогон 6

Вердикт аудитора: **PASS**.

- существующие Panel-гранты честно названы разделовыми;
- гранулярность `контейнер/коллекция/документ` оставлена открытым существом вопроса 3;
- выводить `2→3` из документной гранулярности запрещено;
- один `P1`, семь кандидатов и все прежние процедурные ограничения сохранены.

## Постаудит M0, прогон 6

Вердикт аудитора: **BLOCK**, к владельческой ратификации не допущен.

Протокол снова обосновал `2→3` тем, что доступ защищает конкретный материал. Это
предположение не вынесено в список посылок и опровергается допустимым разделовым или
контейнерным доступом. Поэтому строгая зависимость тождества отдельного материала от
доступа не доказана для всех допустимых ответов вопроса 3.

Формальная часть прошла: минимальный DAG, полный путь роли Affine к переезду,
ready-наборы, tie-break, владельческий гейт и единственный носитель выдержаны. Ребро
`2→5` допустимо, но его основание о каноническом тождестве и устойчивых адресах также
должно быть явно включено в список посылок. Протокол сохранён в `docs/seanses/rejected/`.

## Предаудит M0, прогон 7

Вердикт аудитора: **PASS**.

- один `P1` и ровно семь кандидатов сохранены;
- гранулярность доступа вопроса 3 явно остаётся нерешённой;
- разделовый или контейнерный доступ закреплён как контрпример автоматическому `2→3`;
- для `2→3` требуется независимое от гранулярности основание, иначе ребро опускается;
- основания всех оставленных рёбер, включая `2→5`, должны войти в список посылок;
- причинная полнота, транзитивная редукция, ready-наборы и tie-break сохранены;
- владельческая ратификация ожидается, носитель протокола остаётся единственным.

## Постаудит M0, прогон 7

Вердикт аудитора: **PASS**, протокол допущен к владельческой ратификации.

- гранулярность доступа не предрешена: `2→3` отсутствует с разделовым и контейнерным
  доступом как явным контрпримером;
- `2→5` держится на устойчивом каноническом тождестве, нужном контракту Affine, а не
  на месте прикрепления гранта;
- у каждого оставленного ребра есть явная посылка;
- транзитивно редуцированный DAG:
  `1→2, 1→3, 1→4, 2→5, 3→6, 4→6, 5→6, 6→7`;
- роль Affine достигает переезда путём `5→6→7`;
- ready-наборы и tie-break проверены, точный порядок: `1, 2, 3, 4, 5, 6, 7`;
- существо M1–M7 не решено, владельческая ратификация ожидается;
- второго носителя нет, аудитор файлов не изменял.

## Владельческий гейт M0

Владелец ратифицировал порядок сообщением «ратифицирую» 2026-08-03 после предъявления
точного порядка и ссылки на прошедший аудит протокол. Председатель не подменял этот акт.
M1 разрешено открыть; M2–M7 остаются закрыты до вердиктов своих предшественников.

## Предаудит M1

Вердикт аудитора: **PASS**.

- повестка несёт ровно один ID-вопрос `B1`, а вся необходимая рамка стоит в его строке;
- вопрос относится только к границе контейнера, M2–M7 явно исключены;
- шесть примеров даны как незаполненные проверки классификации, а не готовые ответы;
- список посылок требует только использованные входы с метками `слово владельца`,
  `измеренный факт` или `ратифицированный порядок M0`;
- вердикты будущих комнат запрещены как посылки;
- владельческая ратификация M0 записана до созыва, M1 является корневым узлом DAG.

Машинный предаудит также подтвердил один вопрос и отсутствие структурных нарушений;
неотслеживаемость повестки была ожидаемой до этого коммита.

## Постаудит M1, прогон 1

Вердикт аудитора: **BLOCK**, комната не закрыта.

1. DoD потребовал несуществующий `M1_VERDICT.md`, создавая второй носитель рядом с
   инструментальным протоколом `docs/seanses/`.
2. Архив исследования был не только классифицирован, но объявлен атомарным при приёме;
   это преждевременно выбрало единицу тождества вопроса M2.
3. Три законных входа получили ложную родословную `ратифицированный порядок M0`, хотя
   M0 ратифицировал только DAG и порядок: публичность и отдельный редактор пришли из
   общего брифа, запреты комнаты — из повестки M1.

Остальное прошло: шесть примеров разобраны, доступ, хранилище, Affine API, ingest и
миграционный план не выбраны. Допустимость потери состояния старого движка признана
границей актива контейнера, а не решением судьбы 82 страниц: M7 всё ещё может сохранить
их по иным основаниям. Протокол сохранён в `docs/seanses/rejected/`.

## Предаудит M1, прогон 2

Вердикт аудитора: **PASS**.

- один вопрос `B1`, предмет остаётся границей контейнера;
- единственный носитель — инструментальный протокол `docs/seanses/`, второй запрещён;
- архив можно классифицировать, но единица его тождества оставлена M2;
- происхождение посылок разведено на слово владельца, измеренный факт, норму брифа,
  ограничение M1 и ратифицированный порядок M0;
- метка M0 явно ограничена DAG и порядком;
- решения M2–M7 запрещены, канонического протокола M1 пока нет.

## Постаудит M1, прогон 2

Вердикт аудитора: **BLOCK**, комната не закрыта.

1. Инструментальная сверка насчитала 29 реплик при заявленном минимуме 30; условие
   исполнения не выполнено, хотя структурный аудит это не ловит.
2. Протокол определил записи управления как невосстановимые из байтов и признал индекс
   `docs/evidence` сутью, но затем разрешил потерять и пересоздать эти записи при смене
   движка. Без источника реконструкции это противоречие и преждевременный ответ M2.
3. Вывод «наличие в Affine не доказывает принадлежность контейнеру» был ложно помечен
   как измеренный факт; измерено только фактическое содержимое Affine.

Прежние дефекты второго носителя и атомарности архива исправлены. Решений M3–M7 не
обнаружено. Протокол сохранён в `docs/seanses/rejected/`.

## Предаудит M1, прогон 3 — первый проход

Вердикт аудитора: **BLOCK до созыва**, кредит не потрачен.

Вывод «наличие в Affine не доказывает принадлежность контейнеру» был запрещён как
входная посылка, но всё ещё оставался в разделе `Фактура`. Он удалён из фактической
подложки; там осталось только измеренное содержимое живого Affine.

## Предаудит M1, прогон 3 — повтор

Вердикт аудитора: **PASS**.

- один `B1`, несущая строка полна;
- фактическая подложка больше не содержит выводов B1;
- единственный носитель, отложенная единица тождества архива и честная родословная
  посылок сохранены;
- контейнерные записи управления нельзя потерять или объявить восстановимыми до M2;
- после инструментальной сверки требуется не менее 30 фактических реплик;
- M2–M7 остаются за границей комнаты.

## Постаудит M1, прогон 3

Вердикт аудитора: **BLOCK**, комната не закрыта.

1. Формула «изменение содержания создаёт новый объект» выбрала семантику тождества и
   версий до M2; M1 вправе лишь запретить мутацию принятого содержания.
2. Архив был назначен «единицей фиксации», хотя оговорка рядом оставляла тождество M2.
   M1 может классифицировать его принадлежность, но не назначать никакую единицу.
3. Все семь пунктов DoD остались `[ ]`, и протокол сам объявил работу незавершённой,
   несмотря на явный вердикт и LGTM в репликах.

Условный класс хранимых производных не признан отдельным дефектом: конкретные
производные и механизм включения оставлены M6. Фактические 30 реплик и родословная
посылок прошли. Протокол сохранён в `docs/seanses/rejected/`.

## Предаудит M1, прогон 4

Вердикт аудитора: **PASS**.

- неизменяемость ограничена запретом мутации без семантики нового объекта или версии;
- архиву не назначается единица фиксации, приёма, тождества или версии;
- выполненный DoD обязан иметь `[x]`, любой `[ ]` блокирует закрытие;
- единственный носитель, защита `docs/evidence`, честная родословная и минимум 30
  фактических реплик сохранены;
- один `B1`, решения M2–M7 запрещены, канонического протокола M1 пока нет.

## Постаудит M1, прогон 4

Вердикт аудитора: **BLOCK**, комната не закрыта.

1. Формула «последующее содержание не является новым объектом или версией» выбрала
   отрицательный ответ M2 вместо воздержания от классификации.
2. В реплике архив был назван «одной записью поступления», то есть получил скрытую
   единицу приёма вопреки финальным оговоркам.
3. Этот отрицательный вывод был внесён в список посылок как ограничение повестки, хотя
   повестка требовала только не решать вопрос.
4. Два `[x]` DoD стали фактически ложными: протокол утверждал отсутствие решения M2.

Фактически было 30 реплик, по пять у каждой из шести ролей. Футер объяснил число неверно.
Единственный носитель и защита `docs/evidence` прошли. Протокол сохранён в
`docs/seanses/rejected/`.

## Предаудит M1, прогон 5

Вердикт аудитора: **PASS**.

- отношение последующего содержания к прежнему полностью нейтрально до M2;
- архив не получает единицу фиксации, приёма, поступления, учёта, тождества или версии;
- `[x]` разрешён только для фактически выполненного пункта DoD;
- футер обязан честно показать фактическое число и раскладку по шести ролям;
- единственный носитель, защита `docs/evidence`, честные посылки и минимум 30 реплик
  сохранены;
- один `B1`, решения M2–M7 запрещены, канонического протокола M1 нет.

## Постаудит M1, прогон 5

Вердикт аудитора: **BLOCK**, комната не закрыта.

Механический подсчёт дал 38 реплик: Teamlead 6, Архитектор 6, Верстальщик 7,
Математик 6, Музыкант 6, Структурщик 7. Футер заявил по 7 каждой роли, что равно 42,
и одновременно написал итог 38. Ложная раскладка нарушила явный гейт исполнения, а
пункт DoD о честном подсчёте стал ложным `[x]`.

Ранняя путаница вывода с измеренным фактом была явно исправлена в том же прогоне и не
попала в список посылок. Нейтральность тождества, отсутствие единицы архива, защита
`docs/evidence`, единственный носитель и границы M2–M7 прошли. Протокол сохранён в
`docs/seanses/rejected/`.

## Предаудит M1, прогон 6

Вердикт аудитора: **PASS**.

- гейт требует ровно 36 ролевых строк: шесть полных циклов, по шесть каждой роли;
- после строки 36 дополнительные ролевые подтверждения запрещены;
- футер может честно сообщить только проверяемое `6 × 6 = 36`;
- нейтральность тождества, отсутствие единицы архива, истинные `[x]`, единственный
  носитель, защита `docs/evidence`, родословная и границы M2–M7 сохранены;
- один `B1`, канонического протокола M1 пока нет.

## Постаудит M1, прогон 6

Вердикт аудитора: **BLOCK**, комната не закрыта.

Механический подсчёт дал ровно 30 реплик, по пять у каждой из шести ролей. Протокол
ложно заявил `6 × 6 = 36` и отметил два числовых пункта DoD `[x]`. Содержательно
нейтральность тождества, отсутствие единицы архива, защита `docs/evidence`, единственный
носитель, посылки и границы M2–M7 прошли.

Повторный дефект показывает, что модель не может быть источником истины о собственном
счёте. Начиная с run7, она не пишет числовой DoD и раскладку; число удостоверяют
механический счётчик председателя и отдельный аудитор. Протокол сохранён в
`docs/seanses/rejected/`.

## Предаудит M1, прогон 7

Вердикт аудитора: **PASS**.

Вынос счёта из модели признан процедурно корректным: порог остаётся `≥30` фактических
ролевых строк, председатель даёт механический замер, а отдельный read-only аудитор выносит
вердикт. S-M5 не нарушен. Модельный числовой DoD и раскладка запрещены; расхождение
автоматического футера с механикой по-прежнему даёт BLOCK.

Возврат от экспериментальных ровно 36 к исходным `≥30` не является ослаблением: снята
только ненадёжная самосертификация. Нейтральность M2, отсутствие единицы архива, защита
`docs/evidence`, истинные ненумерические `[x]`, родословная, единственный носитель и
границы M2–M7 сохранены.

## Постаудит M1, прогон 7

Вердикт аудитора: **BLOCK**, комната не закрыта.

1. Архив был назван «единым поступлением байтов», то есть получил запрещённую единицу.
2. Весь PDF был объявлен одним каноническим материалом относительно встроенных
   изображений, что выбрало границу объектов до M2.
3. Финал безусловно постановил хранение производных контейнером, хотя это принадлежит
   M6, и одновременно противоречил раннему безусловному отвержению производных.
4. Три пункта DoD о классификации, отсутствии единиц и границах M2–M7 стали ложными.

Внешний счёт прошёл: 35 реплик (пять ролей по 6, Музыкант 5). Модель чисел не заявляла.
Нейтральность последующих поступлений, защита `docs/evidence`, единственный носитель и
родословная посылок прошли. Протокол сохранён в `docs/seanses/rejected/`.

## Предаудит M1, прогон 8

Вердикт аудитора: **PASS**.

- никакой составной формат или часть не получает единицу тождества, фиксации, приёма,
  поступления, учёта или версии;
- архив нельзя называть одной записью, PDF — единым объектом относительно вложений;
- превью/OCR только классифицируются как производные, судьба хранения полностью M6;
- внешний счёт `≥30`, нейтральность тождества, защита `docs/evidence`, единственный
  носитель, честные посылки и истинные ненумерические `[x]` сохранены;
- один `B1`, канонического протокола M1 пока нет.

## Постаудит M1, прогон 8

Вердикт аудитора: **BLOCK**, комната не закрыта.

1. В нецелях механизм ingest был ошибочно отнесён к M2, хотя M2 держит тождество и
   источник истины, а загрузка/ingest принадлежит M6. Рядом сам протокол говорил M6.
2. Строка носителя назвала шаблон `static-mmbrn-container-m1-<дата>.md`, которого нет,
   вместо фактического файла протокола.

Внешний счёт прошёл: 33 реплики (Teamlead, Архитектор, Музыкант по 6; Верстальщик,
Математик, Структурщик по 5). Составные форматы остались без единиц, хранение
производных отложено M6, тождество нейтрально, `docs/evidence` защищена, посылки прошли.
Протокол сохранён в `docs/seanses/rejected/`.

## Предаудит M1, прогон 9

Вердикт аудитора: **PASS**.

- единственный носитель обязан быть назван точным реальным путём
  `docs/seanses/static-mmbrn-container-m1-boundary-2026-08-03.md`;
- шаблонный путь, `M1_VERDICT.md` и второй носитель запрещены;
- M2 держит тождество, источник истины, хеши, версии и адреса; M6 — загрузку/ingest,
  выдачу и судьбу хранения производных;
- составные форматы остаются без единиц, производные только классифицируются;
- внешний счёт, нейтральность тождества, защита `docs/evidence`, родословная и истинные
  ненумерические `[x]` сохранены;
- один `B1`, канонического протокола M1 пока нет.

## Постаудит M1, прогон 9

Вердикт аудитора: **PASS**, M1 закрыта.

- внешний счёт: ровно 30 ролевых реплик, по пять каждой из шести ролей;
- точный носитель:
  `docs/seanses/static-mmbrn-container-m1-boundary-2026-08-03.md`;
- M2 держит тождество, источник истины, хеши, версии и адреса; M6 — ingest, выдачу и
  судьбу хранения производных;
- архивы, PDF, члены и вложения не получили единицы или гранулярности;
- последующее содержание нейтрально, превью/OCR получили только класс;
- `docs/evidence` защищена как контейнерная запись управления;
- все посылки имеют честное происхождение, каждый `[x]` DoD истинен;
- второго носителя и скрытых решений M2–M7 нет.

Машинный аудит также прошёл; единственная оговорка была ожидаемой до коммита: протокол
ещё не отслеживался git.

## Предаудит M2 — первый проход

Вердикт аудитора: **BLOCK до созыва**, кредит не потрачен.

Повестка подала `id + sha256 + location + source` как полную живую форму записи, хотя
валидатор также обязательно требует `bytes` и `addedAt`, а `about` и `measured`
опциональны. Фактура исправлена: полный `EvidenceRecord` перечислен, четыре прежних поля
названы только ядром имени, тождества, адреса и провенанса.

## Предаудит M2 — повтор

Вердикт аудитора: **PASS**.

- один `I1`, несущая строка полна;
- обязательные поля `id`, `sha256`, `bytes`, `addedAt`, `source`, `location` и
  опциональные `about`, `measured` названы честно;
- четыре поля ядра не выданы за полную запись;
- текущая схема, цепочки ревизий, дубли, чувствительные записи и шесть случаев остаются
  проверками, которые M2 вправе ратифицировать или заменить;
- M2 держит тождество, версии, источник истины, провенанс, составные материалы и адрес;
- M3-M7, точный носитель, родословная, внешний счёт и истинный DoD соблюдены;
- M1 закрыта как прямой предшественник, канонического протокола M2 пока нет.

## Постаудит M2 — run1

Вердикт аудитора: **BLOCK**.

Механический счёт председателя: Teamlead — 7, Архитектор — 6, Верстальщик — 6,
Математик — 6, Музыкант — 7, Структурщик — 6; всего **38**, порог исполнен.
Собственный футер модели ошибочно объявил 36 и сам нарушил запрет на самосчёт.

- протокол придумал `location.kind = sensitive` и допустил пустой `location.ref`, хотя
  валидатор требует `local|affine|url|archivarius` и непустой ref, а чувствительность
  хранится отдельно в `sensitive.reason`;
- запись, байтовое содержание и логическая линия смешаны: поправка одновременно получает
  новый `id` и объявляется сохраняющей `id`;
- `source` назван обязательным, но затем сделан необязательным в поправках и без основания
  сохранённым у новой редакции;
- sensitive-поведение отсутствует в единственной несущей пропозиции;
- из-за этих противоречий несколько отмеченных `[x]` ложны, а вариант отдельной описи
  преждевременно решает M3-M4.

Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run1-false-sensitive-identity.md`.
Повестка усилена фактическими kind/ref/sensitive, тремя уровнями тождества, обязательным
`source` каждой записи и запретом собственного числового футера.

## Предаудит M2 после run1

Вердикт аудитора: **PASS**.

- в повестке ровно один полный `I1`;
- фактура `location.kind`, обязательного ref и отдельного `sensitive` точна;
- разведены `sha256` байтов, неизменяемый record `id` и корень логической линии;
- `source` обязателен для каждой записи и не обязан совпадать по цепочке;
- sensitive-кейс не определяет хранение, раскрытие адреса или круг доступа M3-M4;
- собственный счёт модели, числовой DoD и числовой футер запрещены;
- единственный точный carrier и границы M3-M7 сохранены;
- run1 остаётся только в `rejected/`, канонического carrier перед созывом нет.

## Постаудит M2 — run2

Вердикт аудитора: **BLOCK**.

Механический счёт председателя: по 7 реплик каждой из шести ролей, всего **42**;
порог исполнен, собственного числового футера результата нет. Машинный аудит структурных
нарушений не нашёл, но смысловой аудит опроверг контракт:

- живая вершина определена одновременно через `addedAt`, отсутствие входящей и отсутствие
  исходящей ссылки; это три несовместимых правила;
- четыре существующие `<id>-rN` не имеют `supersedes`, поэтому новый контракт ошибочно
  превратил бы каждую из них в отдельный корень;
- одиночный `supersedes` не умеет сливать две линии и не разрешает fork, хотя протокол
  объявил обратное;
- `source` местами подменён автором операции переноса, а не провенансом материала;
- из-за этого `[x]` о честном `source`, точных посылках и непротиворечивом контракте ложны.

Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run2-chain-source.md`.
Повестка требует линейную цепь без `addedAt`, fork и merge, ограниченное legacy-чтение
четырёх живых `-rN` и провенанс материала в `source`.

## Предаудит M2 после run2

Вердикт аудитора: **PASS**.

- один полный `I1`, `addedAt` исключён из порядка;
- живая запись — единственный record `id`, не встречающийся значением `supersedes` у
  продолжателя линии;
- цепь линейная и ациклическая, fork и merge запрещены;
- legacy fallback ограничен четырьмя названными записями и точно обрабатывает пропуски N;
- для новых поправок обязателен `supersedes`;
- `source` остаётся провенансом материала или байтов, не автором операции;
- три уровня тождества, sensitive, точный carrier, внешний счёт и границы M3-M7 сохранены;
- run1-run2 лежат только в `rejected/`, канонического carrier перед созывом нет.

## Постаудит M2 — run3

Вердикт аудитора: **BLOCK**.

Механический счёт: по 6 реплик каждой роли, всего **36**; порог исполнен. `meeting:audit`
не нашёл структурных нарушений; предупреждение rt-6 о метке `I1` признано ограничением
канала, потому что метка есть в заголовке и машинный аудит связал протокол с повесткой.

- embedded PDF не получил решения, а член архива объявлен извлекаемым и отдельно не
  хранимым, что преждевременно решает M6;
- `about/measured` одновременно названы изменяемыми без новой записи и неизменяемыми в
  append-only строке;
- простой PDF смешал запись и линию: новый location помещён рядом с прежним `id`, хотя
  поправка обязана создать новый record id;
- четыре legacy-записи ошибочно названы четырьмя линиями, а `-r1` — возможным корнем;
- неэкранированный `|` разорвал строку Markdown-таблицы полей;
- поэтому `[x]` о границе M3-M7 и согласованных инвариантах ложны.

Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run3-composites-metadata.md`.
Повестка теперь условно привязывает отдельную запись композита к факту поступления от M6,
проводит все метаданные через новую запись, исправляет три legacy-линии и форму таблиц.

## Предаудит M2 после run3

Вердикт аудитора: **PASS**.

- один полный `I1`;
- архив/PDF регистрируется целиком, компонент — только после независимого поступления либо
  предоставления M6; извлечение и хранение не предрешены;
- любое изменение поля создаёт новую запись с новым `id` и `supersedes`;
- адресная поправка сохраняет `sha256`, root и `canonicalRef`, но меняет record `id`;
- четыре legacy-записи правильно образуют три линии, `r1` не может быть корнем;
- защита Markdown-таблиц, линейная цепь, три уровня, source, sensitive, внешний счёт,
  точный carrier и границы M3-M7 сохранены;
- run1-run3 находятся только в `rejected/`, канонического carrier перед созывом нет.

## Постаудит M2 — run4

Вердикт аудитора: **BLOCK**.

Механический счёт: Teamlead, Архитектор, Верстальщик и Музыкант — по 5, Математик и
Структурщик — по 6; всего **32**, порог исполнен. Машинный аудит структурных нарушений
не нашёл.

- fallback ошибочно требовал точный `r(N-1)`, а не максимальную существующую меньшую
  ревизию либо root; при отсутствующем `r1` результаты расходятся;
- live tip смотрел только физические `supersedes`, поэтому legacy `bpla-r2` и `bpla-r3`
  одновременно становились живыми;
- число строк без физического `supersedes` ошибочно приравнено к числу линий;
- root и `canonicalRef` обычной линии произвольно сокращали `report-pdf-a1b2` до
  `report-pdf`, чего не доказывает ни имя, ни цепочка;
- `[x]` о точном fallback и непротиворечивом root поэтому ложны.

Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run4-effective-predecessor.md`.
Повестка вводит единый effective predecessor для физических и legacy-связей, а root
обычной линии теперь равен точному `id` записи без predecessor.

## Предаудит M2 после run4

Вердикт аудитора: **PASS**.

- один полный `I1`;
- effective predecessor единообразно покрывает явный `supersedes` и четыре legacy-записи;
- live tip учитывает физические и вычисленные связи;
- root получается следованием до точного `id` без predecessor, canonicalRef — от root;
- точный `r(N-1)` и подсчёт линий по отсутствующим полям запрещены;
- append-only метаданные, conditional composites, source, sensitive, три уровня,
  линейность, таблицы, внешний счёт и границы M3-M7 сохранены;
- run1-run4 находятся только в `rejected/`, канонического carrier перед созывом нет.

## Постаудит M2 — run5

Вердикт аудитора: **BLOCK**.

Механический счёт: Teamlead и Архитектор — по 6, остальные четыре роли — по 5;
всего **32**, порог исполнен. `meeting:audit` и rt-6 прошли.

Единственный смысловой дефект: протокол назвал физические байты и `location`
«производными от реестра». Байты — самостоятельный предмет регистрации и доказательство
`sha256`, а не восстанавливаемая из JSONL производная; `location` — поле реестра,
физическая достижимость — внешнее состояние склада. Формулировка противоречила M1 и
делала `[x]` о полном ответе I1 ложным.

Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run5-bytes-not-derivative.md`.
Повестка точно ограничивает область истины реестра и независимость фактических байтов.

## Предаудит M2 после run5

Вердикт аудитора: **PASS**.

- один полный `I1`;
- область истины реестра ограничена фактом регистрации, identity, заявленными полями и
  историей;
- байты — независимый предмет и доказательство, не производная JSONL;
- `location`-заявление отделено от внешней фактической достижимости;
- effective predecessor, legacy, roots, metadata, composites, source, sensitive,
  линейность, таблицы, счёт, carrier и границы M3-M7 сохранены;
- run1-run5 находятся только в `rejected/`, канонического carrier перед созывом нет.

## Постаудит M2 — run6

Вердикт аудитора: **BLOCK**.

Механический счёт: Teamlead, Архитектор, Верстальщик, Математик и Музыкант — по 7,
Структурщик — 6; всего **41**, порог исполнен. rt-6 и `meeting:audit` прошли.

- `canonicalRef` оставлен абстрактным `det(root_id)` рядом с несколькими альтернативами,
  инъективность и отсутствие коллизий не доказаны;
- единственность root/live tip переутверждена из одной ацикличности или запрета fork;
- `source` новой редакции ошибочно обязан отличаться, хотя честный провенанс может совпасть;
- таблица шести случаев поместила новый `id` в «record: то же», а root/canonicalRef — в
  record-колонку;
- sensitive-формула поручила M3-M4 место хранения `location.ref`, хотя ref уже обязан
  находиться в реестре; M3 решает видимость, M4 — склад байтов;
- связанные `[x]` поэтому ложны.

Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run6-canonicalref-levels.md`.
Повестка выбирает точный URN, конечную последовательность линии, честный source без
обязательного различия и строгие колонки трёх уровней.

## Предаудит M2 после run6

Вердикт аудитора: **PASS**.

- один полный `I1`;
- точный инъективный `urn:mmbrn:static:<rootId>`, альтернативы запрещены;
- линия — конечная связная последовательность, root и tip уникальны по определению;
- source новой редакции может совпасть или различаться;
- таблица случаев строго разделяет bytes, record и lineage;
- ref хранится в реестре, M3 решает видимость, M4 — склад байтов;
- effective predecessor, legacy, metadata, composites, sensitive, счёт, carrier и
  границы M3-M7 сохранены;
- run1-run6 находятся только в `rejected/`, канонического carrier перед созывом нет.

## Вход run7 — fail-closed до API

`consilium` отказал до внешнего вызова: накопленная повестка обрезалась во входе комнаты.
Кредит не потрачен, протокол не создан, номер содержательного прогона остаётся run7.

Шесть хронологических секций поправок свёрнуты в один набор ограничений. Полная
родословная не удалена: она остаётся в этом аудите и rejected-протоколах run1-run6.
Компактная повестка требует нового предаудита до созыва.

## Предаудит компактной M2 — первый проход

Вердикт аудитора: **BLOCK до созыва**.

При свёртке исчезли три прежних зуба: байты должны верифицировать `sha256/bytes`, не
восстанавливаться из JSONL и не заменять реестр; число линий нельзя выводить из числа
физических строк без `supersedes`; адресная поправка PDF обязана получить новый record
`id` и `supersedes`, сохранив `sha256`, root и `canonicalRef`. Все три формулы возвращены.

## Предаудит компактной M2 — повтор

Вердикт аудитора: **PASS**.

- три утраченные формулы восстановлены точно;
- остальные ограничения run1-run6 сохранены без смысловой потери;
- один `I1`, exact carrier, внешний счёт и границы M3-M7 на месте;
- run1-run6 остаются в rejected, первая попытка run7 не дошла до API и протокола;
- канонического carrier перед созывом нет.

## Постаудит M2 — run7

Вердикт аудитора: **BLOCK**.

Механический счёт: ровно 6 реплик каждой роли, всего **36**. rt-6, полный вход и
`meeting:audit` прошли. Два оставшихся дефекта:

- простой PDF ошибочно объявил три разных уровня «совпадающими в одном объекте»;
- его строка таблицы поместила root в record-колонку.

Все остальные инварианты прошли аудит. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run7-initial-levels.md`.
Повестка run8 явно сохраняет различие уровней одноэлементной линии и место root.

## Предаудит M2 — run8

Вердикт аудитора: **PASS**.

- одноэлементная линия сохраняет три разных предмета, даже если root равен record `id`;
- record-колонка простого PDF не содержит root/canonicalRef;
- один `I1`, допустимый размер, прежние ограничения, carrier и границы сохранены;
- run1-run7 находятся в `rejected/`, канонического carrier перед созывом нет.

## Постаудит M2 — run8

Вердикт аудитора: **BLOCK**.

- фактически 26 реплик при обязательных 30;
- record-ячейка PDF снова содержала root/lineage;
- протокол не вынес запрет считать линии по физическим строкам без `supersedes`;
- секция «полное эхо» была кратким резюме и ложно утверждала полноту;
- закрытые нормы M1 и README ошибочно названы фактами;
- SHA-256 объявлен математически уникальным доказательством равенства байтов;
- «каждый материал получает ровно одну запись» противоречило append-only поправкам.

Машинный аудит структурных нарушений не нашёл, что не снимает эти дефекты. Кандидат
отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run8-count-echo-premises.md`.
Run9 требует не менее 30 фактических реплик после авто-перепрогонов, честные типы посылок,
контрактное толкование SHA-256 и начальную плюс последующие записи линии.

## Предаудит M2 — run9

Вердикт аудитора: **PASS**.

- финальный carrier обязан фактически содержать не менее 30 реплик;
- начальная и последующие записи разведены;
- SHA-256, типы посылок и запрет ложного эха сформулированы точно;
- строгая таблица, line-count guard и все прежние ограничения сохранены;
- один `I1`, допустимый размер, run1-run8 только в rejected, canonical отсутствует.

## Постаудит M2 — run9

Вердикт аудитора: **BLOCK**.

Финальный carrier содержал ровно 30 реплик, по 5 каждой роли; rt-6 и машинный аудит
прошли. Три смысловых дефекта:

- record-ячейка PDF снова содержала `root = id`;
- live tip определён отсутствием среди физических `supersedes`, а не всех effective
  predecessor, поэтому legacy `bpla-r2` становился вторым tip;
- точный legacy-root назван «предположительным».

Остальные инварианты прошли. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run9-tip-pdf-root.md`.
Повестка run10 запрещает эти три формулировки буквально.

## Предаудит M2 — run10

Вердикт аудитора: **PASS**.

- PDF record-ячейка, effective predecessor tip и точные legacy-root заданы буквально;
- финальный carrier обязан иметь не менее 30 фактических реплик;
- прежние ограничения, один `I1`, размер, carrier и границы сохранены;
- run1-run9 только в rejected, canonical отсутствует.

## Постаудит M2 — run10

Вердикт аудитора: **BLOCK**.

Финальный carrier: 33 реплики; все роли участвовали; машина прошла. Два дефекта:

- протокол допустил численное совпадение всех трёх уровней одноэлементной линии, хотя
  совпадают только root и record `id`, но не `sha256`;
- запись без predecessor (root) отождествлена с `id`, отсутствующим среди значений
  predecessor (tip); мощности множеств равны, сами записи различны.

Остальное прошло. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run10-root-tip-values.md`.
Повестка run11 разводит значения и множества буквально.

## Предаудит M2 — run11, первый проход

Вердикт аудитора: **BLOCK до созыва**.

Повестка ошибочно объявила root и tip разными записями во всех случаях. Исправлено:
это разные роли и критерии; в одноэлементной линии их исполняет одна запись, в более
длинной — разные записи. Внешний API не вызывался, протокол run11 не создавался.

## Предаудит M2 — run11, повтор

Вердикт аудитора: **PASS**.

- root/tip — разные роли и критерии;
- в линии длины 1 их исполняет одна запись, в более длинной — разные;
- совпадают только значения root и record `id`, не SHA-256;
- один `I1`, допустимый размер, canonical отсутствует.

## Постаудит M2 — run11

Вердикт аудитора: **BLOCK**.

Финальный carrier: 34 реплики; rt-6 и машина прошли. Два дефекта:

- таблица полей пропустила обязательный `bytes`;
- `ep` типизирована как возвращающая запись, но фактически возвращала и сравнивала `id`.

Остальное прошло. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run11-bytes-ep-type.md`.
Повестка run12 требует строку `bytes` и единый тип `ep: R -> Id(R) ∪ {⊥}`.

## Предаудит M2 — run12

Вердикт аудитора: **PASS**.

- обязательная строка `bytes` сформулирована точно;
- `ep` возвращает только predecessor `id` либо `⊥`;
- root/tip сравниваются в том же типе;
- прежние ограничения, порог, размер и carrier сохранены;
- run1-run11 только в rejected, canonical отсутствует.

## Intake M2 — run12, первый вызов

Вердикт механизма: **FAIL-CLOSED ДО API**.

`consilium` остановил созыв из-за обрезания входа: внешний API не вызывался, кредит не
тратился, протокол не создавался. Чтобы убрать дублирование, вопрос `I1` уплотнён до
перечня шести обязательных результатов; все накопленные ограничения run1-run11 оставлены
ниже как binding constraints. Компактная редакция направлена тому же read-only аудитору
на повторный предаудит до нового созыва.

## Предаудит M2 — run12, компактная редакция

Вердикт аудитора: **PASS**.

- ровно один вопрос `I1`;
- все шесть результатов и binding constraints run1-run11 сохранены;
- сохранены `bytes`, `ep: R -> Id(R) ∪ {⊥}`, root/tip, четыре legacy-записи,
  canonicalRef, source, registry/bytes/location, sensitive и шесть случаев;
- порог не менее 30 фактических реплик, один carrier и границы M3-M7 сохранены;
- размер снижен до 10 045 символов при 136 строках;
- canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run12

Вердикт аудитора: **BLOCK**.

Служебная «координация ролей» была обрезана, но предметная повестка доставлена полностью;
в carrier осталось 37 фактических ролевых реплик (Teamlead — 7, остальные пять ролей —
по 6), а `meeting:audit` не нашёл структурных нарушений. Обрезка сама по себе carrier не
дисквалифицировала. Обнаружены пять содержательных дефектов:

- три места забыли дверь независимого поступления компонента и оставили только M6;
- новая редакция безусловно получила «новый `bytes`», хотя размер может совпасть;
- равенство root и record `id` объяснено в прозе под таблицей, а не только в lineage-ячейке;
- финальная формула не донесла полный контракт root/tip для линий длины 1 и больше 1;
- `about` и `measured` названы не влияющими на тождество без уточнения, что любое их
  изменение создаёт новый record `id`.

Из-за ложных строк таблицы шести случаев её `[x]` не принят. Остальные требования прошли.
Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run12-five-contradictions.md`.
Повестка run13 получила отдельный запрет для каждого дефекта.

## Предаудит M2 — run13

Вердикт аудитора: **PASS**.

- все пять новых зубьев точны и не противоречат прежним;
- один `I1`, все ограничения run1-run12, порог не менее 30, один carrier и границы
  M3-M7 сохранены;
- фактический размер — 10 837 символов и 146 строк;
- возможная обрезка служебной координации ролей не является содержательным BLOCK;
  предметная обрезка по-прежнему должна дать fail-closed до API;
- run1-run12 находятся в `rejected/`, canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run13

Вердикт аудитора: **BLOCK**.

Механика прошла: 37 фактических реплик, полный состав ролей и структурный PASS; обрезанная
служебная координация ролей не была причиной отказа. Три содержательных дефекта:

- две реплики объявили новую редакцию новой линией с новым root/canonicalRef, а финальная
  таблица правильно продолжила прежнюю линию;
- после DoD создано запрещённое полное эхо `M2_AGENDA.md`;
- равенство root и record `id` обсуждалось в репликах, но не было буквально сформулировано
  в единственно разрешённой lineage-ячейке простого PDF.

Остальные требования прошли. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run13-lineage-echo-cell.md`.
Повестка run14 запрещает каждый дефект во всём carrier, включая реплики, и требует конец
файла сразу после DoD.

## Предаудит M2 — run14

Вердикт аудитора: **PASS**.

- новая редакция однозначно продолжает ту же линию, сохраняет root/canonicalRef и
  становится новым tip; противоположное запрещено во всём carrier;
- равенство root и record `id` разрешено ровно один раз, только в lineage-ячейке простого
  PDF;
- carrier обязан закончиться после DoD без echo, appendix или копии повестки;
- все прежние ограничения, один `I1`, порог не менее 30, один carrier и границы M3-M7
  сохранены;
- размер — 11 266 символов и 149 строк; предметная обрезка обязана остановить созыв
  fail-closed до API;
- run1-run13 находятся в `rejected/`, canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run14

Вердикт аудитора: **BLOCK**.

Механика прошла: 34 фактические реплики, все роли, структурный PASS, конец файла сразу
после DoD. Новая редакция везде сохранила lineage/root/canonicalRef. Три дефекта:

- отношение root и record `id` кроме разрешённой lineage-ячейки повторилось в паспорте,
  репликах, формулах и DoD;
- carrier цитировал запрещённые формулировки про M6, поэтому DoD об их отсутствии был ложен;
- финальный root/tip-контракт не сказал буквально, что в линии длины больше 1 роли исполняют
  разные записи.

Остальные требования прошли. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run14-meta-repetition.md`.
Повестка run15 исключает метаотчёт о запретах, оставляет single-place проверку внешнему
аудитору и требует полный root/tip-контракт в финале.

## Предаудит M2 — run15

Вердикт аудитора: **PASS**.

- single-place tooth исполним: отношение допускается только в целевой lineage-ячейке,
  instruction-паспорт и остальной carrier исключены; метаотчёт запрещён;
- компонент описывается положительным контрактом двух дверей без цитирования guards;
- финальный root/tip-контракт обязан включить разные записи при длине линии больше 1;
- все прежние ограничения, один `I1`, порог не менее 30, один carrier, конец после DoD и
  границы M3-M7 сохранены;
- размер — 11 860 символов и 157 строк; предметная обрезка обязана остановить запуск
  fail-closed до API;
- run1-run14 находятся в `rejected/`, canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run15

Вердикт аудитора: **BLOCK**.

Механика прошла: 36 фактических реплик, все роли, структурный PASS, конец после DoD.
Повестка признана исполнимой; дефекты принадлежат carrier:

- единственное отношение root/record-id отсутствовало в разрешённой PDF lineage-ячейке,
  но его эквиваленты появились в четырёх других местах;
- после положительного контракта двух дверей реплика пересказала TD-guard;
- таблица полей/инвариантов пропустила отдельные derived-строки root и canonicalRef;
- уникальность `id` названа входным инвариантом схемы, хотя это решение M2, а не
  заявленная измеренная посылка.

Остальное прошло, включая полный root/tip-контракт. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run15-four-carrier-defects.md`.
Повестка run16 требует точную PDF lineage-ячейку, только положительный TD-контракт,
derived-строки и правильный эпистемический статус уникальности `id`.

## Предаудит M2 — run16, первый проход

Вердикт аудитора: **BLOCK до созыва**.

Содержательно повестка согласована, но имела 12 469 символов при машинном лимите topic-файла
12 000. `readBounded` гарантированно обрезал бы её, а meeting-gate остановил бы запуск до
API. Внешний вызов не делался. Повторения в I1 и посылках сокращены без снятия зубьев.

## Предаудит M2 — run16, компактная редакция

Вердикт аудитора: **PASS**.

- все шесть outputs и ограничения run1-run15 сохранены;
- один `I1`, порог не менее 30, один carrier, конец после DoD и границы M3-M7 сохранены;
- Node считает 11 653 символа; при `MAX_TOPIC_CHARS = 12 000` остаётся 347 символов;
- `readBounded` обрезает только при строгом превышении, launch instruction не расходует
  лимит topic-файла;
- run1-run15 находятся в `rejected/`, canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run16

Вердикт аудитора: **BLOCK**.

Механика прошла: 31 фактическая реплика, все роли, структурный PASS. Три дефекта:

- целевая PDF lineage-ячейка не содержала буквального single-place отношения, а прямой и
  эквивалентный повторы появились в реплике и derived-строке root;
- ozon/day ошибочно названы одноэлементными при фактических линиях `base + r2`;
- положительный контракт двух дверей повторён пять раз и получил отрицательный хвост.

Derived-строки и эпистемический статус уникальности `id` прошли. Остальное тоже прошло.
Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run16-single-place-legacy-td.md`.
Повестка run17 закрепляет два однократных утверждения за конкретными case-ячейками,
derived-root только за ролью `ep = ⊥` и точную длину трёх legacy-линий.

## Предаудит M2 — run17, первый проход

Вердикт аудитора: **BLOCK до созыва**.

Новые зубья были согласованы, но при компактировании выпал старый value-level зуб:
`sha256` первичного PDF должен быть отличен от record- и lineage-идентификаторов; нельзя
утверждать совпадение всех трёх значений. API не вызывался. Зуб возвращён в PDF bytes-cell
без повторения single-place отношения.

## Предаудит M2 — run17, повтор

Вердикт аудитора: **PASS**.

- старый зуб восстановлен без конфликта с single-place;
- все ограничения run1-run16, один `I1`, порог не менее 30, один carrier, конец после DoD
  и границы M3-M7 сохранены;
- Node-размер — 11 206 символов, запас до лимита — 794;
- run1-run16 находятся в `rejected/`, canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run17

Вердикт аудитора: **BLOCK**.

Механика прошла: 36 фактических реплик, ровно по шесть каждой роли, структурный PASS.
Пять дефектов:

- single-place relation отсутствовала в PDF lineage-ячейке и повторялась эквивалентами
  в пропозиции и derived-строке;
- PDF bytes-cell различила `sha256` только с record `id`, но не с lineage identifier;
- TD-контракт отсутствовал в archive/member record-cell и размножился метатекстом;
- fallback legacy назван измеренным фактом вместо нормы M2;
- реплика сама объявила число реплик достаточным.

Остальное прошло. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run17-cells-epistemics-selfcount.md`.
Повестка run18 задаёт точный предметный текст трёх целевых ячеек, разводит факт и норму
legacy и запрещает метаотчёт, включая самооценку реплик.

## Предаудит M2 — run18

Вердикт аудитора: **PASS**.

- три целевые ячейки определены точно и раздельно, вне них запрещены пересказы и meta;
- proposition и derived-root используют только роль записи с `ep = ⊥`;
- legacy-факт и fallback-норма разведены, состав линий точен;
- все ограничения run1-run17, один `I1`, внешний порог, один carrier, конец после DoD и
  границы M3-M7 сохранены;
- Node-размер — 11 151 символ, запас до лимита — 849;
- run1-run17 находятся в `rejected/`, canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run18

Вердикт аудитора: **BLOCK**.

Механика прошла: 36 фактических реплик, все роли, полный topic, структурный PASS. Все три
целевые ячейки были заполнены правильно, но действовавший глобальный guard отклонил
необходимые формальные определения и обсуждение. Кроме того:

- пропозиция объявила три значения различными, хотя различны тождества, а rootId и record
  `id` одноэлементной линии совпадают;
- six-case table не дала отдельного доказательства каждому случаю и не описала условные
  bytes/lineage компонента.

Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run18-overconstraint-and-case-proof.md`.

Аудитор отдельно установил: глобальные single-place/TD-once запреты были ошибочным
расширением исходного placement-дефекта. Для run19 scope возвращён к структурированному
выходу: строгие PDF bytes/record/lineage и archive-member record/bytes/lineage ячейки;
вне таблицы разрешены предметные formal definitions и обсуждение без meta. Это исправление
контракта повестки, не принятие дефектного carrier.

## Предаудит M2 — run19

Вердикт аудитора: **PASS**.

- scope placement-guards ограничен case-table; предметные определения вне неё разрешены;
- пропозиция различает тождества, не требует попарного различия значений;
- six-case table требует доказательство в каждой строке и полный условный компонент;
- все прежние содержательные ограничения, один `I1`, внешний порог, один carrier, конец
  после DoD и границы M3-M7 сохранены;
- размер — 11 267 символов, запас до лимита — 733;
- run1-run18 находятся в `rejected/`, canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run19

Вердикт аудитора: **BLOCK**.

Механика прошла: 36 фактических реплик, все роли, полный topic, структурный PASS. Три
дефекта:

- record-cell дубля содержала lineage и root, уже присутствующие в соседней ячейке;
- четыре реплики сузили две двери компонента до одной или назвали «решение M6» вместо
  фактического предоставления компонента;
- sensitive-факты валидатора и существующего дома были маркированы нормами M2.

PDF-ячейки, доказательства шести случаев, legacy, ep и поля прошли. Кандидат отведён в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run19-duplicate-component-sensitive.md`.
Повестка run20 запрещает lineage во всех record-cells, сохраняет обе компонентные двери
во всём обсуждении и явно разводит sensitive-факты с нормами M2.

## Предаудит M2 — run20

Вердикт аудитора: **PASS**.

- новые record-cell, component и sensitive guards согласованы;
- шесть случаев с доказательствами, PDF placement, ep/root/tip, legacy, canonicalRef,
  source, registry/bytes/location и границы M3-M7 сохранены;
- один `I1`, внешний порог, один carrier и конец после DoD сохранены;
- размер — 11 675 символов, запас до лимита — 325;
- run1-run19 находятся в `rejected/`, canonical carrier и `M2_VERDICT.md` отсутствуют.

## Постаудит M2 — run20

Вердикт аудитора: **BLOCK**.

Механика прошла: 40 фактических ролевых реплик, все шесть ролей, полный предметный вход и
структурный PASS. Четыре группы дефектов не позволили принять carrier:

- реплика Архитектора объявила три значения различными, хотя в одноэлементной линии
  rootId совпадает с record `id`;
- итоговая пропозиция не включила единицу регистрации, две двери компонента архива и
  полный sensitive-контракт с границей M3/M4;
- в таблице случаев record-ячейка редакции содержала lineage-фразу, а bytes-ячейка архива
  содержала record-факт;
- четыре реплики обсуждали устройство carrier, placement и полноту выдачи вместо предмета.

Остальные требования прошли. Сырой carrier сохранён без правок в
`docs/seanses/rejected/static-mmbrn-container-m2-identity-2026-08-03-run20-final-external.md`.

## Лимит внешних попыток и локальная сборка

Владелец установил правило заседаний: на каждую комнату допускается не более пяти внешних
попыток; после пятого BLOCK новые вызовы прекращаются, а председатель локально собирает
носитель из добытых материалов. Для M2 правило принято после двадцатого внешнего carrier,
поэтому run21 запрещён. Два дополнительных intake-события перед run7 и run12 были
fail-closed до API и протокола и внешними вызовами не считаются.

Корпус всех вызовов и отказов зарегистрирован отдельным прецедентом
[`2026-08-03-static-mmbrn-m2-twenty-consilium-calls`](../../precedents/2026-08-03-static-mmbrn-m2-twenty-consilium-calls.md).
Канонический M2 carrier собран председателем из run20 и замечаний постаудита, явно помечен
`local-synthesis` и ожидает независимого постаудита. Это не новый вызов консилиума.

## Постаудит M2 — локальная сборка 1

Вердикт независимого аудитора: **BLOCK**.

- две формулировки компонента использовали запрещённую исключительность `только`, одна из
  них не называла обе двери буквально;
- bytes-ячейка простого PDF не развела `sha256` с lineage-идентификатором `canonicalRef`;
- реплика Архитектора обсуждала полноту выдачи и сборку carrier вместо предмета.

Прецедент, сохранность сырого run20 и непреждевременное состояние `MEETING_ACTIVE.md`
получили PASS. Четыре дефекта исправляются локально; внешнего вызова нет.

## Постаудит M2 — локальная сборка 2

Вердикт независимого аудитора: **PASS**.

- предметная реплика больше не обсуждает сборку или полноту carrier;
- обе двери компонента названы без исключающего `только`;
- bytes-ячейка PDF явно разводит `sha256` с record `id` и lineage-идентификатором
  `canonicalRef`;
- регрессий относительно `M2_AGENDA.md` не найдено.

Механический `meeting:audit` также завершился без структурных нарушений; в carrier остаётся
40 фактических ролевых реплик. M2 содержательно прошла и ожидает ратификации владельца.

## Ратификация M2

Владелец ратифицировал M2 сообщением «ратифицирую» 2026-08-03. Пропозиция тождества и
источника истины закрыта; гейт M2 открыт. По утверждённому порядку M0 следующая комната —
M3 «Доступ». Её повестка должна пройти независимый предаудит до первого внешнего вызова;
лимит — не более пяти внешних попыток.

## Предаудит M3 — первый проход

Вердикт независимого аудитора: **BLOCK до созыва**.

Повестка оставляла выбор канонического источника авторизации открытым, но в двух более
поздних требованиях заранее называла решение Panel. Это исключало допустимый альтернативный
вердикт. Обе формулировки заменены на «решение выбранного канонического авторизатора»;
Panel остаётся измеренным кандидатом. Внешнего вызова и carrier M3 ещё не было.

## Предаудит M3 — повтор

Вердикт независимого аудитора: **PASS**.

- один вопрос A1 и один будущий carrier;
- выбор единственного канонического авторизатора открыт, Panel не предрешён;
- восемь обязательных случаев, прямой обход, granular policy, sensitive ref, отзыв и
  audit contract сохранены;
- границы M4–M7 не нарушены;
- объём — 7 650 символов, предметная повестка укладывается в лимит 12 000;
- carrier M3 отсутствует, внешних попыток M3 использовано 0 из 5.

Повестка готова к первому созыву после разрешения владельца.

Продолжение пересекло полночь Europe/Moscow. До первого вызова ожидаемый carrier M3
переназван с даты подготовки `2026-08-03` на фактическую дату созыва `2026-08-04`;
предмет и ограничения повестки не менялись.

## Постаудит M3 — run1

Вердикт независимого аудитора: **BLOCK**. Использована попытка 1 из 5.

Внешний carrier произведён через `openrouter/anthropic/claude-sonnet-4.6`, 33 фактические
ролевые реплики; предметная повестка доставлена полностью, координация ролей обрезана.
Сырой результат сохранён в
`docs/seanses/rejected/static-mmbrn-container-m3-access-2026-08-04-run1-seven-contract-defects.md`.

Дефекты:

- неверный заголовок списка посылок, запрещённое полное эхо после DoD и две meta-реплики;
- session id выдан за stable identity, `subjectId` не определён;
- смешаны role-grant и permission-grant; контейнерный grant и `manage-access` одновременно
  разрешены и запрещены; конфликт ограничений не вычислим;
- обещан немедленный отзыв при cache TTL 60 секунд и delegation TTL 5 минут; текущий gate
  не сверяет snapshot grants/pv из cookie с актуальным users store;
- mapping `ally -> Affine reader` раскрывает больше, чем разрешённое ally metadata;
  делегация не связана с canonicalRef, action и policy version;
- обязательный case 5 доказывает deny manage-access, но не allow metadata;
- нормы M1/M2 маркированы фактами, а решение M2 о registry приписано M1.

Direct bypass, sensitive ref/bytes separation и границы M4-M7 в остальном прошли.
Повестка run2 получает семь точных поправок и новый предаудит до внешнего вызова.

## Предаудит M3 — run2, первый проход

Вердикт независимого аудитора: **BLOCK до созыва**.

Единственный дефект: измеренный snapshot `grants/pv` был ошибочно приписан любой сессии.
Он присутствует только у зарегистрированной партнёрской сессии `sub=user:*`; обычная
сессия несёт `kind/role/sub/exp`. Факт уточнён. Остальные семь поправок run1, один A1,
carrier, лимит размера и границы M4-M7 прошли. Внешнего вызова run2 ещё не было.

## Предаудит M3 — run2, повтор

Вердикт независимого аудитора: **PASS**.

- факт о двух формах session payload и cookie-derived gate совпадает с кодом;
- семь поправок run1 сохранены без преждевременного выбора реализации;
- один A1, один отсутствующий carrier, точный список посылок и конец после DoD;
- объём — 9 822 символа, порог — не менее 30 внешне считаемых реплик;
- M4-M7 остаются нерешёнными.

Использовано 1 из 5 внешних попыток M3. Run2 ожидает отдельного разрешения владельца на
отправку обновлённой внутренней повестки во внешний OpenRouter API.

## Постаудит M3 — run2

Вердикт независимого аудитора: **BLOCK**. Использовано 2 из 5 внешних попыток.

Carrier произведён через `openrouter/anthropic/claude-sonnet-4.6`; предметная повестка
доставлена полностью. Сырой результат сохранён в
`docs/seanses/rejected/static-mmbrn-container-m3-access-2026-08-04-run2-grant-revocation-threshold.md`.

Дефекты:

- 28 фактических ролевых реплик при обязательном пороге не менее 30;
- role-grant перевёрнут: `grant.minRole < baselineRole` не позволяет public-гранту ally
  сработать, поэтому case 3 не следует из алгоритма;
- обычная сессия не имеет `pv`, но алгоритм требует его у любой non-anonymous session;
  переиздание токена само по себе старый не инвалидирует, а policy-version не определена;
- реплика назвала будущую store-проверку свойством live gate, хотя текущий gate её не делает;
- Affine native roles шире Panel actions, а proxy не проверяет requested action против
  `allowedActions`;
- public metadata и requiredRole case 5 противоречат таблице полномочий;
- одна реплика обсуждает полноту случаев и carrier.

Stable principal, direct bypass, sensitive separation, посылки и границы M4-M7 прошли.
Повестка run3 консолидирует поправки run1-run2 и повторно проходит предаудит.

## Предаудит M3 — run3, первый проход

Вердикт независимого аудитора: **BLOCK до созыва**.

При консолидации потерялись два зуба: не требовался канонический источник stable principal,
а Affine-поправки безусловно предполагали delegation+proxy при открытом выборе механизма.
Источник/mapping principal возвращён; action/version/object guards сформулированы для любой
выбранной границы, а token/proxy-проверки сделаны условными. Carrier run3 отсутствует.

## Предаудит M3 — run3, повтор

Вердикт независимого аудитора: **BLOCK до созыва**.

Четыре старые формулировки вне консолидированной секции всё ещё безусловно требовали
delegation. Они заменены проверкой устаревшего полномочия выбранного Affine-механизма;
token/delegation требования теперь действуют только при выборе delegation. Stable principal
и остальные поправки прошли. Carrier run3 отсутствует.

## Предаудит M3 — run3, финальный

Вердикт независимого аудитора: **PASS**.

- все Affine-проверки привязаны к выбранному механизму, delegation/proxy не предрешены;
- stable principal требует поле, канонический источник/mapping, sentinel и credential;
- grant, revocation, policy version, action enforcement и cases 1/3/4/5 проверяемы;
- один A1, carrier отсутствует, объём — 10 313 символов, порог — не менее 30;
- список посылок, конец после DoD и границы M4-M7 сохранены.

Использовано 2 из 5 внешних попыток M3. Run3 ожидает отдельного разрешения владельца на
отправку консолидированной повестки во внешний OpenRouter API.

## Постаудит M3 — run3

Вердикт независимого аудитора: **BLOCK**. Использовано 3 из 5 внешних попыток.

Carrier произведён через `openrouter/anthropic/claude-sonnet-4.6`; предметная повестка
доставлена полностью, машинный аудит не нашёл структурных нарушений, фактически записано
36 ролевых реплик — по шесть от каждой роли. Сырой результат без правок сохранён в
`docs/seanses/rejected/static-mmbrn-container-m3-access-2026-08-04-run3-inheritance-revocation-affine.md`.

Семантические дефекты:

- правило «наиболее специфичная политика» противоречит `min` всех политик; более узкое
  ограничение не может повысить порог;
- Case 1 вручную вводит отсутствующий в функции floor `ally` и потому не следует из
  заявленного единого алгоритма;
- hard floor `manage-access` равен `operator` в формуле, но `owner` в таблице и Cases 5–6;
- отзыв subject grant меняет `pv_subject`, тогда как Affine proxy проверяет только object
  `policyVersion`; старое Affine-полномочие не инвалидируется этим отзывом;
- ordinary session без `pv` честно stale до `exp` в таблице, но fail-closed раздел обещает
  невозможный немедленный deny;
- статическая таблица `Panel role -> Affine role` спорит с последующим action-based
  понижением и может расширить полномочия;
- две ролевые реплики обсуждают переход к carrier, несмотря на прямой запрет meta.

Stable principal, direct bypass, sensitive `read-ref`/`read-bytes`, точный список посылок,
конец после DoD и границы M4–M7 прошли. Повестка run4 фиксирует одну role-elevation
математику, обе version axes и per-request proxy без статического role mapping; до нового
внешнего вызова требуется независимый предаудит и отдельное разрешение владельца.

## Предаудит M3 — run4

Вердикт независимого аудитора: **PASS**.

- в повестке ровно один A1, один canonical carrier, сам carrier отсутствует;
- run1–run3 честно отклонены, использовано 3 из 5 внешних попыток;
- объём topic — 10 161 символ при лимите 12 000;
- оставлена одна role-elevation модель: наиболее специфичная политика, отдельный
  `actionFloor`, затем одна функция сравнения ролей;
- Cases 1/3/5 согласованы, `manage-access` owner-only и исключён из grant-scope;
- partner, ordinary и object versions разведены; staleness ordinary до `exp` не скрыта;
- proxy проверяет requested action, binding, subject version и object-version vector,
  статический mapping ролей запрещён;
- новых ложных фактов и решений M4–M7 нет.

Run4 ожидает отдельного разрешения владельца на отправку обновлённой повестки во внешний
OpenRouter API.

## Внешняя попытка M3 — run4 без carrier

Владелец разрешил run4. Вызов с `--min-replies 36`, `--seed 71`, разреженным входом и
прошедшей предаудит повесткой дошёл до panel overlay, но все три звена завершились без
контента: `anthropic/claude-sonnet-4-6`, `xai/grok-4.5`,
`openrouter/anthropic/claude-sonnet-4.6`. Инструмент вернул `chain exhausted`; canonical
carrier не создан, поэтому содержательного постаудита run4 нет.

По владельческому лимиту событие засчитано как внешняя попытка 4 из 5. Автоматический
fallback `LLM_NO_OVERLAY=1` не включался: инструмент прямо требует отдельного слова
владельца. Фактура вызова зарегистрирована прецедентом
[`2026-08-04-static-mmbrn-m3-run4-overlay-chain-exhausted`](../../precedents/2026-08-04-static-mmbrn-m3-run4-overlay-chain-exhausted.md).
Повестка run4 содержательно не менялась и сохраняет предаудит PASS; последняя попытка run5
ждёт владельческого выбора маршрута. После пятого внешнего отказа новый вызов запрещён и M3
собирается локально из сохранённого корпуса.

## Постаудит M3 — run5

Владелец отдельно разрешил последнюю попытку с `LLM_NO_OVERLAY=1`. Default-chain вернул
carrier через `openrouter/anthropic/claude-haiku-4.5`; повестка доставлена полностью,
машинный аудит не нашёл структурных нарушений, фактически записана 41 ролевая реплика.
Вердикт независимого аудитора: **BLOCK**. Использовано 5 из 5 внешних попыток.

Сырой carrier без правок сохранён в
`docs/seanses/rejected/static-mmbrn-container-m3-access-2026-08-04-run5-final-external.md`.
Дефекты:

- `max(grantedRoles...)` не ограничен объектной цепочкой и не исключает grants из
  `manage-access`;
- request path требует `pv_subject` у ordinary session, хотя её текущий stale-доступ живёт
  до `exp`;
- object-vector не сравнивается с текущим после разрешения `canonicalRef`, binding
  `canonicalRef <-> affineDocId` отсутствует;
- роль Panel снова статически сопоставлена роли Affine;
- Cases 4, 5 и 8 потеряли соответственно stale Affine-state, metadata allow и
  sensitive-metadata allow + bytes deny;
- видимость обязательного M2-поля `location.kind` не определена;
- список посылок включает выводы M3 как посылки и не несёт полный sensitive-контракт M2;
- после DoD оставлен запрещённый числовой самосчёт.

Внешний бюджет M3 исчерпан. Run6 запрещён. Председатель локально собирает carrier из
сохранённых run1-run5 и замечаний постаудита, с явной меткой `local-synthesis`; до
владельческой ратификации он обязан пройти новый независимый постаудит.

## Постаудит M3 — локальная сборка 1

Вердикт независимого аудитора: **BLOCK**.

Все десять содержательных дефектов run5 были закрыты: object-scoped grants, исключение
`manage-access`, ordinary/partner revocation, subject/object versions, fail-closed cache,
binding, отсутствие статического Affine mapping, полные Cases 4/5/8, sensitive-контракт M2,
посылки и конец после DoD. Остались два формальных дефекта:

- не было отдельной таблицы соответствия решения Panel и механизма Affine;
- таблица Cases объединяла обязательные колонки и не имела отдельной колонки `Почему`.

Это локальная редакция, не внешний run6. Добавлены только две требуемые таблицы; контракт и
36 ролевых реплик не менялись.

## Постаудит M3 — локальная сборка 2

Вердикт независимого аудитора: **PASS**.

- отдельная таблица отображает `Panel decision -> proxy/Affine enforcement`, а не роли;
- пользовательские Affine identity/token не выдаются, статический role mapping не вернулся;
- Cases 1–8 имеют отдельные `Субъект`, `Объект`, `Действие`, `Решение`,
  `Где проверяется`, `Почему`; Cases 4/5/8 сохранили полную семантику;
- scope-grants, `manage-access`, версии, ordinary/partner, binding, cache и M2 sensitive
  contract прошли без регрессий;
- ровно 36 ролевых реплик, по шесть от каждой роли;
- последняя непустая строка — DoD, M4–M7 остаются открыты.

Машинный `meeting:audit` также не нашёл структурных нарушений. M3 допускается к
владельческой ратификации; M4 до этого акта остаётся закрыта.

## Ратификация M3

Владелец ратифицировал M3 сообщением «ратифицирую» 2026-08-04 после предъявления
прошедшего независимый постаудит local-synthesis carrier. Контракт доступа закрыт; гейт M3
открыт. По утверждённому порядку M0 следующая комната — M4 «Хранение и живучесть».
До независимого предаудита её повестки и отдельного владельческого разрешения внешний
созыв запрещён; лимит M4 — не более пяти внешних попыток.

## Предаудит M4

Вердикт независимого аудитора: **PASS**.

- один вопрос `S1`, один отсутствующий carrier, объём 7 263 символа;
- M4 следует четвёртой в ратифицированном порядке M0;
- topology остаётся открытым предметом решения, а office VDS честно сохранён как NO-GO;
- primary/backup failure domains, M2 integrity, M3 access, quota/watermarks,
  restore/RPO/RTO и retention/deletion заданы исполнимо;
- десять случаев покрывают capacity, corruption, sensitive, restore, ложный backup PASS,
  hold/deletion, direct bypass и office NO-GO;
- vendor/account/secret/region, API, миграция и M5–M7 не решены.

Машинный аудит также подтвердил один `S1` и отсутствие структурных нарушений. Внешних
попыток M4 использовано 0 из 5. Первый созыв ждёт отдельного разрешения владельца.

## Постаудит M4 — run1

Вердикт независимого аудитора: **BLOCK**. Использована попытка 1 из 5.

Carrier произведён через `openrouter/anthropic/claude-sonnet-4.6`; повестка доставлена
полностью, машинный аудит не нашёл структурных нарушений, записано 42 ролевые реплики — по
семь от каждой роли. Сырой результат сохранён в
`docs/seanses/rejected/static-mmbrn-container-m4-storage-2026-08-04-run1-m2-retention-restore-boundaries.md`.

Дефекты:

- FD-3 оставлен альтернативой «Git или append-only FS», поэтому topology не одна;
- object key использует отсутствующие в M2 `container_id/lineage_id/revision_seq` и только
  32-битный префикс hash;
- dedup сливает physical key разных records, не объясняя сохранность разных lineages;
- придуманные `status/hold/tombstone` мутируют immutable M2 record и не соответствуют schema;
- integrity проверяет `sha256`, но не обязательное `bytes`;
- абсолютный минимум 12 GiB расходится с ratio-only RG-1, hard threshold записан двумя
  несовместимыми числами;
- срок retention superseded originals не назначен;
- restore не восстанавливает согласованную checkpoint-пару registry + bytes;
- HTTP-коды, proxy ingest/hash pipeline и scrape endpoint предрешают M6;
- `Список посылок` расположен после DoD.

Failure domains, sensitive/direct-access и 10 случаев дали полезный материал. Повестка run2
добавляет десять точных поправок; до нового внешнего вызова нужны независимый предаудит и
отдельное разрешение владельца.

## Предаудит M4 — run2

Вердикт независимого аудитора: **PASS**.

- все десять дефектов run1 покрыты непротиворечивыми поправками;
- будущий carrier обязан выбрать одну topology, один FD-3 и один lifecycle mechanism;
- key ограничен реальными M2-полями и полным `sha256`, dedup не сливает identities;
- lifecycle ledger не мутирует M2 record; integrity проверяет `sha256 + bytes`;
- capacity predicate, retention и checkpoint-pair restore обязаны быть едиными;
- HTTP-коды, endpoints, scrape protocol и hash pipeline M6 прямо запрещены;
- один `S1`, объём 9 750 символов, canonical carrier отсутствует;
- run1 сохранён в rejected, счётчик 1 из 5, M5–M7 открыты.

Run2 ожидает отдельного разрешения владельца на отправку обновлённой повестки во внешний
LLM API.

## Постаудит M4 — run2

Вердикт независимого аудитора: **BLOCK**. Использована попытка 2 из 5.

Carrier произведён после владельческого разрешения, машинно прошёл структурный аудит и
содержит 43 ролевые реплики: по семь у пяти ролей и восемь у Teamlead. `Список посылок`
стоит до последней секции DoD. Сырой результат без редакторских исправлений сохранён в
`docs/seanses/rejected/static-mmbrn-container-m4-storage-2026-08-04-run2-dedup-capacity-checkpoint-rpo-m6.md`.

Пять содержательных дефектов:

- declared key `objects/{sha256}/{record_id}` создаёт разные objects, но dedup одновременно
  объявляет один физический `objects/{sha256}/`; реальный bytes key и семантика ref расходятся;
- capacity formula допускает ingest до `< 0.90`, а Case 1 и readiness G1 требуют `< 0.85`;
  реплика с `free_after / total >= 0.10` расходится с `< 0.90` на точной границе;
- manifest FD-1 и snapshot FD-3 снимаются последовательно без write fence, high-water mark
  или общего checkpoint id; перенос самих перечисленных bytes в FD-2 не определён;
- RPO 24 часа не защищён возрастом последнего успешного checkpoint: G3 проверяет лишь его
  наличие, поэтому старый checkpoint может ложно дать PASS;
- temporary signed URL с конкретным TTL проектирует download-механизм M6 и позволяет
  storage-действие после единственного решения вместо per-action enforcement M3.

Run2 дал пригодные topology, FD-3, lifecycle ledger, integrity и retention, но storage
contract остаётся внутренне противоречивым. Повестка run3 добавляет пять узких поправок;
до нового внешнего вызова обязательны независимый предаудит и отдельное разрешение владельца.

## Предаудит M4 — run3

Вердикт независимого аудитора: **PASS**.

- пять дефектов run2 закрыты однозначными требованиями к physical key/dedup, единой
  capacity formula, consistent cut, возрасту checkpoint и границе M6;
- поправки run2 не отменяют ограничения run1 и ратифицированные M2/M3;
- agenda оставляет носителю выбор key-модели и checkpoint mechanism, поэтому не подменяет
  заседание готовым storage-контрактом;
- signed URL, TTL, endpoint, transport и download workflow запрещены, остаётся только
  per-action invariant M3;
- один `S1`, объём 11 499 символов, canonical carrier отсутствует;
- rejected run2 существует, счётчик 2 из 5, M5–M7 открыты.

Машинный `meeting:audit` также подтвердил один `S1` и отсутствие структурных нарушений.
Run3 ждёт отдельного разрешения владельца на внешний LLM-вызов.

## Постаудит M4 — run3

Вердикт независимого аудитора: **BLOCK**. Использована попытка 3 из 5.

Внешний вызов завершился без транспортной ошибки; машинный аудит не нашёл структурных
нарушений. Сырой carrier сохранён без редакторских исправлений в
`docs/seanses/rejected/static-mmbrn-container-m4-storage-2026-08-04-run3-count-location-dedup-checkpoint-quota-m6.md`.

Подтверждены девять дефектов:

- записано 34 ролевые реплики вместо требуемых 36; Архитектор и Музыкант дали по пять;
- для новой topology не выбран допустимый M2 `location.kind`, поэтому `{kind, ref}` неполна;
- global physical dedup одного hash конфликтует с отдельными sensitive credential/encryption;
- удаление blob по authorization одной record не защищает другие records того же hash;
- FD-2 bundle не несёт собственного complete marker: snapshot сделан до live complete-event;
- write fence не drain-ит in-flight writes и не закрывает lifecycle/registry mutations;
- RPO age считается от completion, а не от более ранней точки фактического cut;
- container/collection quota не имеет числа или алгоритма, Case 2 проверяет только FD-1;
- обсуждение снова назначает запрещённый HTTP-код `403`, хотя итог его уже не повторяет.

Run3 исправил topology/FD-3, полный hash, immutable M2, двойную integrity, глобальную
capacity formula, перенос bytes в FD-2, age-gate, retention, signed URL/TTL и форму конца.
Повестка run4 сводит ограничения run1-run3 в один непротиворечивый раздел и дополнительно
требует доказуемый RTO. До внешнего run4 обязательны независимый предаудит и новое
разрешение владельца.

## Предаудит M4 — run4

Вердикт независимого аудитора: **PASS**.

- все 24 дефекта run1-run3 покрыты единым разделом без взаимных коллизий;
- M2 требует допустимые `location.kind/ref`, immutable rows и только реальные поля;
- standard/sensitive dedup разделён по policy class, shared blob quarantine/deletion
  учитывают все refs;
- capacity, container budget, collection quota, logical dedup charge и G1 должны стать
  вычислимыми;
- checkpoint требует drain всех writers, high-water/cut_at, bytes copy, FD-2 complete marker
  и снятие fence после двух commit-свидетельств;
- RPO считается от cut, RTO доказывается throughput и bound корпуса;
- M6 запрещена даже HTTP-кодами, ролевой порог закреплён как 36 и минимум шесть на роль;
- один `S1`, объём 10 333 символа, canonical carrier отсутствует, счётчик 3 из 5;
- rejected run3 существует, M5-M7 остаются открытыми.

Машинный аудит также подтвердил один `S1` и отсутствие структурных нарушений. Run4 ждёт
отдельного разрешения владельца на внешний LLM-вызов.

## Внешний M4 — run4: транспортный BLOCK

После отдельного разрешения владельца run4 запущен через panel overlay с `--min-replies 42`
и `--seed 137`. Все три звена завершились `rate_limit`:
`anthropic/claude-sonnet-4-6`, `xai/grok-4.5`,
`openrouter/anthropic/claude-sonnet-4.6`. Инструмент вернул `chain exhausted`; canonical
carrier не создан.

Попытка 4 из 5 использована. Автоматический fallback в непробованный default
`deepseek/deepseek-chat` не запускался. Повестка содержательно не менялась и сохраняет
предаудит PASS. Событие записано штатным генератором в прецедент
[`2026-08-05-static-mmbrn-m4-run4-overlay-chain-rate-limit`](../../precedents/2026-08-05-static-mmbrn-m4-run4-overlay-chain-rate-limit.md).

Run5 является последней внешней попыткой M4. Он требует отдельного владельческого выбора:
исправленный panel overlay либо разовое `LLM_NO_OVERLAY=1`. После пятого BLOCK/no-carrier
run6 запрещён, следующий носитель может быть только локальной сборкой с новым постаудитом.

## Постаудит M4 — run5

Владелец отдельно разрешил последнюю попытку через `LLM_NO_OVERLAY=1`. Default chain вернул
carrier моделью `deepseek/deepseek-chat`; 44 ролевые реплики, не меньше семи у каждой роли.
Сырой файл имел неверную дату `2026-08-05`, получил машинный BLOCK по отсутствию heading
`Список посылок` и сохранён в
`docs/seanses/rejected/static-mmbrn-container-m4-storage-2026-08-05-run5-final-external.md`.

Независимый постаудит подтвердил ещё 15 дефектов: противоречивый FD-3, возврат вымышленных
M2 полей, невычислимые quotas и reserve, неполный checkpoint final, недоказанный RTO,
отсутствующие schedule/retention, неполные tables Cases/readiness, решения UI из M5,
неправильные premises/DoD headings и meta-заявления. Полезный материал: class-scoped dedup,
shared-ref safety, полный hash, RPO от cut, независимые FD-1/FD-2 и отсутствие M6 transport.

Внешний бюджет M4 закрыт: 5 из 5. Run6 запрещён.

## Постаудит M4 — локальная сборка 1

Председатель локально собрал canonical carrier из повестки, M1-M3, run1-run5 и всех
постаудитов. Машинный аудит PASS; записано ровно 36 реплик, по шесть у каждой роли;
посылки стоят до последней секции DoD. Независимый аудитор дал **BLOCK** по трём пунктам:

- пропущен ратифицированный M3 action `download` при наличии `read-ref/read-bytes`;
- последняя строка premises была немаркирована и циркулярно ссылалась на rejected runs;
- Case 8 доказывал hold, но не отдельный запрет deletion до 365-дневного retention.

Это не внешний run6. Контракт storage не менялся; исправлены только три названных места.

## Постаудит M4 — локальная сборка 2

Вердикт независимого аудитора: **PASS**.

- `read-ref`, `read-bytes`, `download` сохранены как отдельные M3 actions; flow остаётся M6;
- последняя premise маркирована как норма M4 agenda и не выводится из rejected corpus;
- Case 8 разделён на hold и retention branches с отдельными вещдоками;
- независимые FD-1/FD-2/FD-3, M2 kind/ref, class derivation, dedup/quarantine/deletion,
  capacity/quota, checkpoint marker/abort, RPO/RTO и retention прошли без регрессий;
- Cases/readiness оформлены таблицами, unknown честно остаются NO-GO;
- 36 ролевых реплик распределены 6x6, M5-M7 открыты, DoD заканчивает файл.

Машинный `meeting:audit` также не нашёл структурных нарушений. M4 допускается к
владельческой ратификации; M5 до этого акта остаётся закрыта.

## Ратификация M4 и открытие подготовки M5

Владелец ратифицировал M4 сообщением «ратифицирую» 2026-08-05. Ратификация относится к
canonical local-synthesis carrier после независимого PASS и закрывает комнату M4; внешний
бюджет остаётся зафиксированным как 5 из 5 и не обнуляется.

По ратифицированному порядку M0 следующая комната — M5 «роль Affine». Подготовлена одна
повестка `M5_AGENDA.md` с единственным вопросом F1 и единственным ещё не существующим
carrier. Внешний запуск M5 запрещён до независимого предаудита повестки и отдельного
разрешения владельца.

## Предаудит M5 — редакция 1

Вердикт независимого аудитора: **BLOCK** по одной ложной формулировке статуса. Повестка
утверждала, что M1–M4 ратифицированы, хотя M1 имеет PASS и закрыта без отдельного
владельческого акта; ратифицированы M2–M4. Остальные проверки прошли: один F1, объём меньше
12 000 символов, carrier отсутствует, границы M1–M4 сохранены, binding/state/cases/readiness
покрыты, M6–M7 и стратегический редактор не предрешены.

Исправлена только строка статуса: «M1–M4 закрыты; M2–M4 ратифицированы». Это локальная
правка повестки до созыва и не расходует внешний бюджет M5. До повторного независимого
PASS внешний запуск остаётся запрещён.

## Предаудит M5 — редакция 2

Вердикт независимого аудитора: **PASS**.

- статус теперь точно различает закрытую M1 и ратифицированные M2–M4;
- M5 следует пятой в порядке M0; ровно один F1 и один отсутствующий canonical carrier;
- объём повестки 9 519 символов;
- инварианты M1–M4 сохранены: Affine не стал источником identity, bytes или authority;
- binding, классы состояния, переносимость, десять cases и readiness gates достаточны,
  но центральный выбор модели оставлен заседанию;
- M6 endpoints/transport и M7 DNS/migration/rollout не решены; роль стратегического
  редактора Affine не возвращена;
- машинный `meeting:audit` подтвердил один F1, шесть повесток, пять протоколов и отсутствие
  структурных нарушений.

Внешний бюджет M5 остаётся 0 из 5. Run1 допускается только после отдельного разрешения
владельца; автоматического запуска по акту ратификации M4 нет.

## Постаудит M5 — run1

Владелец отдельно разрешил run1. Повестка F1 доставлена полностью; координация ролей
обрезана. Panel-chain ответила через `anthropic/claude-sonnet-4-6`; после premise follow-up
и единственного verdict retry инструмент всё равно дал **BLOCK** «нет секции “Список
посылок”» и сохранил результат только в rejected. Внешняя попытка 1 из 5 использована.

Сырой файл без редакторских исправлений переименован в
[`run1-m3-actions-m6-binding-readiness`](../../seanses/rejected/static-mmbrn-container-m5-affine-role-2026-08-05-run1-m3-actions-m6-binding-readiness.md).
В нём 36 реплик, ровно по шесть каждой роли, один F1, пять требуемых таблиц и визуально
присутствующая секция посылок; процедурный BLOCK от этого не превращается в PASS.

Независимый постаудит подтвердил 14 дефектов:

- четыре capabilities и Case 8 используют вымышленные M3 actions вместо закрытого словаря;
- `IMPORT`, ingest queue, file/bytes flow, JSON response и Affine API колонизируют M6;
- `canonicalRef` ошибочно превращён в пользовательский URL;
- M4 topology искажена: bytes приписаны FD-3, где живут registry/lifecycle;
- absent binding оставляет IMPORT, обходя M3 fail-closed;
- append-only binding одновременно мутирует старые status/`supersededBy`; version scope,
  stale и reducer semantics не определены;
- readiness по counts допускает пропуски, не проверяет уникальность обоих концов binding и
  не доказывает отсутствие native-only пользователей;
- annotation gate не сравнивает живой engine export с portable store, использует
  неопределённое `author_panel_id` и не задаёт engine-neutral identity/version;
- собственные выводы M5 циркулярно внесены в посылки;
- после DoD оставлен meta-footer с self-count и carrier path.

Полезное ядро run1 сохранено как материал: Affine отделён от M2 identity, M4 storage и M3
authority; portable annotations разведены с disposable navigation/cache; есть десять cases,
намерение binding history и rehydration/access gates. Повестка run2 получила точечные
ограничения для закрытия всех дефектов, но внешний run2 запрещён до повторного независимого
предаудита и отдельного разрешения владельца.

## Предаудит M5 — run2

Вердикт независимого аудитора: **PASS**.

- все 14 дефектов run1 покрыты единым непротиворечивым разделом;
- capability может иметь своё имя, но M3 action берётся только из закрытого словаря;
- IMPORT, ingest/API/URL/response/transport M6 запрещены; `canonicalRef` не URL;
- распределение M4 сохранено: bytes FD-1, complete backup FD-2, registry/lifecycle FD-3;
- binding обязан иметь однозначные history/version/stale/reconciliation semantics, но
  конкретная append-only либо иная модель остаётся выбором комнаты;
- readiness проверяет множества, оба конца binding, двустороннюю уникальность и полный
  allowlist service identities;
- annotations требуют engine-neutral identity/version и triple-equality без unresolved diff;
- посылки не содержат выводов M5, DoD заканчивает carrier без meta/self-count;
- один F1, canonical carrier отсутствует, run1 только в rejected, объём 11 277 символов;
- M6–M7 и стратегический редактор открыты; машинный аудит не нашёл структурных нарушений.

Внешний бюджет остаётся 1 из 5. Run2 можно созывать только после отдельного разрешения
владельца.

## Постаудит M5 — run2

После отдельного разрешения владельца run2 произведён `anthropic/claude-sonnet-4-6` и
прошёл встроенные premise/verdict gates. Машинный аудит подтвердил один F1, 36 реплик 6x6,
посылки до последней секции DoD и структурный PASS. Независимый смысловой аудит дал
**BLOCK** по 15 пунктам; использована попытка 2 из 5.

Сырой carrier без исправлений перенесён в
[`run2-m3-semantics-m6-fd3-annotations`](../../seanses/rejected/static-mmbrn-container-m5-affine-role-2026-08-05-run2-m3-semantics-m6-fd3-annotations.md).

Основные дефекты:

- VIEW склеивает разные floors `read-metadata`/`read-ref`; ANNOTATE/REVIEW изобретают
  policy object, MANAGE-PROJECTION переопределяет `upload-revision`;
- Case 8 изобретает strategic class/M2 record, хотя strategic docs вне контейнера;
- вернулись bytes-through-Proxy, response codes, API evidence и готовый
  export/import/deploy/rebuild workflow M6–M7; projection rebuild решает preview/rendering;
- binding потребован для всего registry вместо `requiredProjectionSet`, обратная
  уникальность engine object отсутствует;
- reducer противоречив при conflict, а evidence смешивает immutable events со status output;
- `binding.jsonl`/`annotations.jsonl` назначены FD-3 stores без M4 backup/retention/restore;
- annotation schema не имеет stable version, canonical serialization и Panel principal;
  engine-neutral anchor не доказан, а no-loss допускает operator waiver;
- readiness labels расходятся с state table и не доказывают post-replacement binding
  coverage; API evidence запрещён;
- meta/self-count вернулись, часть DoD `[x]` содержательно ложна.

Полезное ядро сохранено: точный M3-словарь, правильные FD, append-only намерение,
per-lineage version, fail-closed binding, service allowlist, portable/disposable split,
десять cases и set-based readiness. Повестка run3 консолидирует ограничения run1–run2;
внешний запуск запрещён до независимого предаудита и нового разрешения владельца.

## Предаудит M5 — run3

Вердикт независимого аудитора: **PASS**. Полный проход дважды превысил окно ожидания без
вердикта; председатель прервал дальнейшее чтение и запросил конечный ответ только по уже
собранному материалу. Аудитор подтвердил, что оставшихся blocking contradictions нет.

Механическая сверка до этого акта подтвердила: 11 684 символа, один F1, canonical carrier
отсутствует, run1/run2 находятся только в rejected, внешний счётчик 2 из 5, M6–M7 открыты,
machine `meeting:audit` не нашёл структурных нарушений. Run3 допускается только после
отдельного разрешения владельца.

## Intake M5 — run3 после смены даты

Владелец отдельно разрешил run3 уже 2026-08-06 Europe/Moscow. До внешнего вызова ожидаемый
carrier механически переназван с даты подготовки `2026-08-05` на фактическую дату созыва
`2026-08-06`. Предмет F1, ограничения, предаудит PASS и внешний счётчик 2 из 5 не менялись.

## Постаудит M5 — run3

Run3 произведён через `anthropic/claude-sonnet-4-6`, но после premise follow-up и verdict
retry получил процедурный **BLOCK** «нет секции “Список посылок”» и остался rejected.
Фактически 38 реплик: Teamlead/Музыкант по семь, остальные по шесть. Использована попытка
3 из 5. Сырой файл сохранён как
[`run3-false-m3-circular-set-fd3`](../../seanses/rejected/static-mmbrn-container-m5-affine-role-2026-08-06-run3-false-m3-circular-set-fd3.md).

Независимый постаудит подтвердил: вымышленный набор M3 с `delete-ref`, `grant-access`,
`revoke-access`, `list-refs`, `check-policy`; annotation write на новом policy object;
циркулярный `requiredProjectionSet`; reducer без `engineDocId` в key и уникального `seq`;
ложное покрытие новых FD-3 stores гарантиями M4; неполный annotation contract и no-loss;
смешанные access gates, M6/M7 детали и самозакрытый ролевой DoD.

Полезны явная биекция, переходный граф reducer, stable annotation intent и no-waiver.
Повестка run4 несёт точный словарь M3 прямо в F1, независимо назначает projection set и
консолидирует проверки run1–run3. Внешний run4 запрещён до предаудита и нового разрешения.

## Предаудит M5 — run4

Вердикт независимого аудитора: **PASS**. Повестка покрывает дефекты run1–run3 без
подстановки готового вердикта. Один F1, 11 395 символов, canonical carrier отсутствует,
run1–run3 находятся только в rejected, внешний бюджет 3 из 5; машинный аудит имеет
структурный PASS. Run4 допускается только после отдельного разрешения владельца.

## Постаудит M5 — run4

После отдельного разрешения владельца run4 произведён
`anthropic/claude-sonnet-4-6`. Встроенный premise gate и машинный структурный аудит дали
PASS, но независимый смысловой аудит дал **BLOCK**. Использована попытка 4 из 5. Сырой
carrier без исправлений сохранён как
[`run4-m3-owner-stale-m6-durability`](../../seanses/rejected/static-mmbrn-container-m5-affine-role-2026-08-06-run4-m3-owner-stale-m6-durability.md).

Фактически carrier содержит 36 предметных реплик, ровно 6x6; сообщение инструмента о
«35» было ложным. Это не снимает дефекты:

- carrier сам закрыл внешний ролевой DoD и оставил meta-footer после DoD;
- `manage-access` допускает grants вместо owner-only baseline;
- absent/stale binding не fail-closed: разрешены Panel read/read-only paths;
- спроектированы M6: bytes-through-Proxy, download-file, storage flow, UI и network trace;
- новый durable boundary не исполним: нет численных retention/RPO/RTO и доказанного restore;
- immutable event rows одновременно меняют status, нет `eventType`, а delete/conflict не
  задают точные engine ids и однозначный seq;
- inventory не доказывает равенство independently required refs, active ledger и live
  engine objects в обе стороны;
- annotation contract не задаёт canonical serialization/version/anchor полностью, а gate
  не доказывает тройное двустороннее set+hash equality;
- cases/readiness наследуют эти разрывы и содержат ложные `[x]`.

Полезное ядро сохранено: точный M3 action/object vocabulary, annotation write disabled,
независимый `requiredProjectionSet`, правильный deny стратегических документов, отдельные
no-forward/service-allowlist gates и portable/disposable state. Повестка run5 получила
точечные требования ко всем дефектам run1–run4.

Run5 — последняя внешняя попытка M5. Она запрещена до независимого предаудита и отдельного
разрешения владельца. После пятого BLOCK внешний run6 запрещён; следующий carrier может
быть только локальной сборкой из накопленного корпуса с новым постаудитом.

## Предаудит M5 — run5

Вердикт независимого аудитора: **PASS**.

- все смысловые дефекты run1–run4 покрыты непротиворечивыми требованиями;
- один F1, 11 987 символов; canonical carrier отсутствует, runs1–run4 только в rejected;
- внешний бюджет корректен: 4 из 5, run5 явно последняя внешняя попытка;
- M6–M7 не решены, готовый verdict не предписан;
- role-count и соответствующий DoD оставлены внешнему аудиту, footer после DoD запрещён;
- `MEETING_ACTIVE`, повестка, аудит и сырой run4 согласованы.

Машинный `meeting:audit` также имеет структурный PASS. Run5 допускается только после
отдельного разрешения владельца. При его BLOCK внешний run6 не допускается.

## Постаудит M5 — run5

После отдельного разрешения владельца последняя внешняя попытка произведена
`anthropic/claude-sonnet-4-6`. Carrier содержит 43 ролевые реплики: Верстальщик 8,
остальные роли по 7. DoD оставлен внешнему аудиту, footer после него отсутствует.

Машинный `meeting:audit` дал **BLOCK**: визуальный heading `Список посылок` оказался перед
вложенными bold-heading и не образовал принимаемого телом валидатора списка. Независимый
смысловой постаудит также дал **BLOCK** по 11 группам:

- `read-ref` возвращает `canonicalRef`, а не только `location.ref`;
- engine fetch, downloadable package, Panel/M4 write flow, native API и пошаговый drill
  проектируют M6;
- canonical originals ошибочно приписаны одновременно FD-1/FD-2/FD-3;
- event stream и create/replace/stale semantics неполны, conflict не согласован с key;
- Case 3 смешивает replace/stale/create без однозначной immutable последовательности;
- set equalities не доказывают обратную уникальность engine id;
- annotation hash не покрывает version/author/anchor, grammar anchor не ограничена;
- triple equality не связана одним snapshot;
- нет отдельного readiness gate `Panel deny -> no-forward`;
- Case 2 не доказывает неизменность policy versions и audit history.

Полезное ядро: owner-only `manage-access`, annotation-write deny, fail-closed binding,
strategic out-of-container deny, независимый `requiredProjectionSet`, Panel-owned durable
boundary, численные thresholds с честным NO-GO без evidence, state split и portable
annotation model.

Внешний бюджет M5 закрыт: 5 из 5. Внешний run6 запрещён. По правилу владельца следующий
carrier собирается локально из run1–run5 и всех постаудитов, затем проходит новый
независимый смысловой и машинный аудит.

## Постаудит M5 — local synthesis 1

Председатель локально собрал canonical carrier из ратифицированных M2-M4, повестки M5,
run1-run5 и всех независимых постаудитов. Нового внешнего вызова не было. Сырой run5
сохранён отдельно в rejected; внешний бюджет остаётся закрытым 5 из 5.

Механический результат: один carrier, ровно 36 предметных реплик по 6 каждой роли, один
heading посылок, DoD заканчивает файл. Машинный `meeting:audit` не нашёл структурных
нарушений.

Вердикт независимого смыслового аудитора: **PASS**.

- точная семантика M3 сохранена: `read-ref -> location.ref`, `manage-access` owner-only;
- M6-M7 не спроектированы, результаты остаются логическими;
- FD-1/FD-2/FD-3 разведены, portable-state boundary не выдан за FD-3;
- численные durability requirements имеют честный NO-GO без measured evidence;
- immutable reducer задаёт seq, cause/group links и fail-closed invalid state;
- same-snapshot равенства и уникальность обоих концов доказывают биекцию;
- annotation hash покрывает всю record, anchor ограничен, triple equality связано snapshot;
- десять cases и отдельные no-forward/service-allowlist gates присутствуют;
- посылки не содержат выводов M5, DoD последний, ролевой пункт оставлен внешнему аудиту.

Смысловых BLOCK не найдено. M5 допускается к владельческой ратификации; M6 до этого акта
остаётся закрыта.

## Ратификация M5 и открытие подготовки M6

Владелец ратифицировал M5 сообщением «ратифицирую» 2026-08-06. Акт относится к canonical
local-synthesis carrier после независимого PASS. Он закрывает комнату M5, но не обнуляет
её внешний бюджет 5 из 5 и не превращает rejected runs в канон.

По ратифицированному порядку M0 следующая комната — M6 «Приём и выдача». До подготовки
повестки штатный прибор мастерской вещдоков показал 12 строк реестра, все с текущим
`location.kind=local`; `verify` дал 3 расхождения и отдельно два класса content duplicates.
Это измеренный вход M6, не приговор отдельным legacy rows.

Подготовлена одна повестка `M6_AGENDA.md` с одним вопросом E1 и одним отсутствующим
carrier. Внешний запуск M6 запрещён до независимого предаудита повестки и отдельного
разрешения владельца.

## Предаудит M6 — редакция 1

Вердикт независимого аудитора: **PASS**.

- M5 корректно открывает M6, Affine остаётся сменной projection;
- один E1, 9 179 символов, canonical carrier отсутствует;
- immutable M2 identity и разделение record/bytes/lineage сохранены;
- M3 metadata/ref/bytes/download и Panel authority разведены;
- M4 FD-1/FD-2/FD-3, class/quota/integrity gates не изменены;
- commit, atomicity, idempotency, recovery, sensitivity, verification, preview и archive
  safety обязательны, но готовые authority/state/verdict не подставлены;
- M7 DNS/migration/rollout запрещены, текущие 12 rows остаются evidence;
- classification не противоречит M4: M6 выбирает authority, но carrier обязан получить
  fail-closed class decision до durable write.

Машинный `meeting:audit` также имеет структурный PASS. Внешний бюджет M6 остаётся 0 из 5;
run1 допускается только после отдельного разрешения владельца.

## Постаудит M6 — run1

После отдельного разрешения владельца run1 произведён
`anthropic/claude-sonnet-4-6`. `meeting:audit` дал structural PASS, но фактически carrier
содержит только 30 реплик, ровно по 5 каждой роли, вместо 36/6x6. Внешняя попытка 1 из 5
использована. Сырой carrier сохранён как
[`run1-30-replies-m3-preview-idempotency`](../../seanses/rejected/static-mmbrn-container-m6-intake-delivery-2026-08-06-run1-30-replies-m3-preview-idempotency.md).

Независимый смысловой аудит дал **BLOCK** по 12 группам:

- operations не замкнуты на существующие M3 actions/objects; status/verify обходят gate;
- `GetRef` возвращает лишний canonicalRef, а recordId не разрешается до policy lineage;
- preview одновременно использует `read-metadata` и требует `read-bytes`;
- state machine смешивает address/record lifecycle с `manage-access`;
- sensitivity unknown/resolution и malware/format gate неисполнимы;
- commit ложно требует FD-2 до M4 checkpoint и не защищает JSONL append;
- idempotency key конфликтует с failed retry и не доказывает one intent/one record;
- cleanup может удалить shared blob и склеивает class namespaces по одному hash;
- historical unreachable одновременно назван нормой и нарушает universal atomicity;
- quota readiness сравнивает record counts вместо M4 byte predicates;
- archive component выдаётся до обязательной record, provenance положен в `about`;
- audit/readiness теряют M3/M4 поля, допускают leaks и spot-check как universal proof.

Полезное ядро: quarantine/staged intent, раздельные delivery operations, six-dimensional
verification, explicit outcomes, 16 cases, archive safety intent и честный M7 запрет.
Повестка run2 получила единый раздел обязательных поправок. Внешний run2 запрещён до
повторного независимого предаудита и отдельного разрешения владельца.

## Предаудит M6 — run2

Вердикт независимого аудитора: **PASS**.

- один E1, 11 636 символов, canonical carrier отсутствует, run1 только в rejected;
- внешний бюджет корректен: 1 из 5; машинный `meeting:audit` имеет structural PASS;
- operation/action/object, recordId resolution, exact ref и preview bytes gate покрыты;
- attempt state отделён от record/access lifecycle;
- pre-write class, malware/format и authorised resolution исполнимы;
- commit/FD-2, durable fingerprint/CAS idempotency согласованы с M4;
- cleanup class-aware/shared-safe, quota выражена byte predicates;
- archive provenance, audit redaction и full-corpus readiness обязательны;
- 36/6x6 оставлены внешнему аудиту, готовый verdict и M7 не предписаны.

Run2 допускается только после отдельного разрешения владельца.

## Постаудит M6 — run2

После отдельного разрешения владельца run2 произведён внешним провайдером
`anthropic/claude-sonnet-4-6`. Механика прошла: 37 предметных реплик, у каждой из шести
ролей не меньше шести; `meeting:audit` дал structural PASS. Внешняя попытка 2 из 5
использована. Сырой carrier без исправлений сохранён как
[`run2-m2-source-canonicalref-m3-atomicity-rpo`](../../seanses/rejected/static-mmbrn-container-m6-intake-delivery-2026-08-06-run2-m2-source-canonicalref-m3-atomicity-rpo.md).

Первый независимый аудитор не вернул вердикт за ограниченное окно и был остановлен; это
отказ канала аудита, не PASS и не дополнительный consilium run. Повторный независимый
аудитор дал **BLOCK** по семи группам:

- M2 `source`-строка заменена объектом, а `bytes` местами назван `bytes_size`;
- revision ошибочно получает новый `canonicalRef`, хотя продолжает прежнюю lineage;
- `archive-extract-component` объединяет два M3 actions в одной public operation;
- `verify` под `read-metadata` проверяет ref/bytes dimensions и превышает authority;
- G6-success/G7-fail ведёт к удалению уже referenced FD-1 object вместо восстановления
  durable intent binding;
- degraded FD-2 разрешает intake без проверки обязательного M4 RPO `now-cut_at <= 24h`;
- lifecycle описан мутационными состояниями без обязательных append-only events.

Полезное ядро сохранено: durable idempotency fingerprint/CAS, class-aware quota и cleanup,
preview через `read-bytes`, численные archive bounds, full-corpus readiness и запрет решать
M7. Повестка run3 получила единый раздел поправок run1-run2. Внешний бюджет M6 — 2 из 5;
run3 запрещён до нового независимого предаудита и отдельного разрешения владельца.

## Предаудит M6 — run3

Первый проход предаудита дал **BLOCK** по одному формальному пробелу: расход бюджета 2/5
был в активном протоколе и журнале, но отсутствовал в самой повестке. После добавления
этой строки тот же независимый аудитор дал **PASS**:

- один E1, 11 957 символов, canonical M6 отсутствует;
- внешний бюджет прямо зафиксирован как 2 из 5;
- все семь BLOCK run2 и обязательства run1 перенесены без готового verdict;
- M2-M5 не переопределены, M7 запрещена;
- 36/6x6 оставлено внешнему постаудиту.

Машинный `meeting:audit` также имеет structural PASS. Run3 допускается только после
отдельного разрешения владельца.

## Постаудит M6 — run3

После отдельного разрешения run3 произведён `anthropic/claude-sonnet-4-6`. Ролевой корпус
полон: 42 реплики, ровно 7 каждой из шести ролей. Внешняя попытка 3 из 5 использована.
Сырой carrier сохранён как
[`run3-surface-authority-m2-recovery-readiness`](../../seanses/rejected/static-mmbrn-container-m6-intake-delivery-2026-08-06-run3-surface-authority-m2-recovery-readiness.md).

`meeting:audit` дал structural BLOCK: заголовок посылок существует, но следующая жирная
строка распознана как новый heading, поэтому тело секции для валидатора пусто. Независимый
смысловой аудит также дал **BLOCK**:

- operation table не имеет authority object/resolution и оставляет list/preview/verify вне
  закрытого surface;
- verify dimensions не имеют полного exact action/object mapping;
- M2 `about` ошибочно стал object, revision не защищена expected-tip CAS от fork;
- recovery после append не имеет durable pre-append связи intent с proposed recordId, а
  uniqueness не запрещает два recordId одному fingerprint;
- readiness смешивает registry/lifecycle scopes и ослабляет M4 quota/capacity predicates;
- archive numbers даны как примеры, а не выбранные constants;
- audit хранит raw sensitive ref и предлагает redaction только при чтении вопреки M4.

Полезное ядро: точные `source/bytes/canonicalRef`, RPO NO-GO, append-only lifecycle,
class-aware cleanup, preview `read-bytes`, 19 cases и честный запрет M7. Повестка run4
получила единый раздел поправок run1-run3. Бюджет M6 — 3 из 5; run4 запрещён до нового
независимого предаудита и отдельного разрешения владельца.

## Предаудит M6 — run4

Первый проход дал **BLOCK** по одной внутренней коллизии: ранняя строка требовала class
quota, хотя M4 определяет collection quota `U_c/Q_c`, а class относится к storage key.
После замены на collection quota при сохранённом предшествующем class resolution тот же
независимый аудитор дал **PASS**:

- один E1, 11 993 символа, canonical M6 отсутствует;
- бюджет прямо зафиксирован как 3 из 5;
- closed action/object surface, exact M2 fields/CAS и durable recovery обязательны;
- M4 readiness/RPO, fixed archive constants и write-time redaction сохранены;
- M2-M5 не переопределены, M7 запрещена, 36/6x6 оставлено внешнему аудиту.

`meeting:audit` без canonical carrier имеет structural PASS. Run4 допускается только после
отдельного разрешения владельца.

## Постаудит M6 — run4

Run4 произведён после отдельного разрешения. Механика PASS: 46 реплик, у ролей 8/8/8/8/7/7;
`meeting:audit` structural PASS. Внешняя попытка 4 из 5 использована. Сырой carrier:
[`run4-m2-bytes-m3-audit-m4-capacity-reconcile`](../../seanses/rejected/static-mmbrn-container-m6-intake-delivery-2026-08-06-run4-m2-bytes-m3-audit-m4-capacity-reconcile.md).

Независимый смысловой аудит дал **BLOCK**:

- M2 size `bytes` использован как payload, а размер назван запрещённым `bytes_size`;
- `manage-access` имеет два authority objects в одной operation;
- verify audit пишет `verify-*` как action вместо точного M3 action;
- M4 capacity `free_after>=12GiB && utilisation<0.90` заменена новым резервом 1GiB;
- reconciliation доказывает только равенство counts, не exact recordId join;
- archive component flow отсутствует в closed surface, а max bytes выпал из case/readiness;
- список посылок содержит выбранные самой M6 constants/state/retention/CAS.

Полезное ядро: durable proposedRecordId до append, recoverable idempotent commit, tip CAS,
immutable history, shared-safe cleanup, exact RPO, fixed archive intent и write-time
redaction. Повестка run5 получила единый раздел поправок run1-run4. Бюджет 4 из 5; run5 —
последняя внешняя попытка, запрещённая до независимого предаудита и отдельного разрешения.

## Предаудит M6 — run5

Независимый аудитор дал **PASS** повестке последней внешней попытки:

- один E1, 11 894 символа, canonical M6 отсутствует;
- бюджет 4 из 5, run5 прямо названа последней внешней;
- все BLOCK run1-run4 перенесены: M2 content/bytes, split authority objects, exact M3
  audit actions, M4 capacity, exact reconciliation, archive flow и чистые premises;
- legacy ledger gap обязан давать uncovered/NO-GO;
- M2-M5 не переопределены, M7 запрещена;
- ролевой DoD оставлен внешнему аудиту.

`meeting:audit` без carrier имеет structural PASS. Run5 допускается только после отдельного
разрешения владельца; при её BLOCK внешний бюджет закрывается и M6 верстается локально.

## Постаудит M6 — run5

После отдельного разрешения владельца run5 произведён `anthropic/claude-sonnet-4-6`.
Механика PASS: 42 реплики, ровно 7 каждой из шести ролей; `meeting:audit` structural PASS.
Внешняя попытка 5 из 5 использована, бюджет комнаты закрыт. Сырой carrier сохранён без
исправлений как
[`run5-final-external-surface-fingerprint-readiness`](../../seanses/rejected/static-mmbrn-container-m6-intake-delivery-2026-08-06-run5-final-external-surface-fingerprint-readiness.md).

Независимый смысловой аудит дал **BLOCK** по восьми группам:

- operation surface не имел authority object и операции управления доступом;
- metadata correction была спрятана в content revision;
- fingerprint не покрывал все влияющие поля;
- write inputs не требовали positive integer `bytes`;
- verify dimensions были сопоставлены неверным M3 actions;
- archive constants не повторялись во всех проверяемых carriers;
- readiness hash/size проверял sample вместо полного корпуса;
- authorization/durability evidence и текущий NO-GO не образовывали замкнутый контракт.

Пятая внешняя попытка не принята. По правилу владельца шестой внешний вызов запрещён;
председатель собрал canonical carrier локально из M2-M5, повестки M6 и полезного материала
run1-run5.

## Локальная сборка M6

Canonical carrier:
[`static-mmbrn-container-m6-intake-delivery-2026-08-06.md`](../../seanses/static-mmbrn-container-m6-intake-delivery-2026-08-06.md).
Он содержит 36 предметных реплик, ровно 6 каждой роли, один E1, закрытые operations,
outcomes, cases и full-corpus readiness. Машинный `meeting:audit` дал structural PASS;
`git diff --check` чист.

Независимый смысловой аудит прошёл четыре последовательных проверки без внешнего
consilium-вызова:

- первый BLOCK: ослабленный FD-2 RPO, неполный crash corpus, неразведённые metadata/address/
  class transitions и лишняя посылка;
- второй BLOCK: FAILED выпадал из `L_proposed`, stale reachable FD-2 не имел outcome,
  `move-address` допускал разрушительный move;
- третий BLOCK: orphan без M2 row ошибочно подчинялся deletion chain зарегистрированной
  записи, которая требует существующий recordId;
- после исправлений final verdict — **PASS**: all-state state-indexed reconciliation,
  безусловный RPO, copy-only address/class transitions, раздельные orphan cleanup и
  registered deletion, точные M3 action/object и M4 predicates согласованы.

M6 допускается к ратификации владельцем. M7 не созывалась и остаётся закрыта до этой
ратификации и отдельного разрешения.

## Ратификация M6 и открытие гейта M7

Владелец ратифицировал M6 сообщением «ратифицирую» 2026-08-06. Акт относится к canonical
local-synthesis carrier после независимого PASS. Он закрывает комнату M6, не обнуляет её
внешний бюджет 5 из 5 и не превращает rejected run1-run5 в канон.

По ратифицированному порядку M0 следующая и последняя комната — M7 «Переезд и доставка».
Её гейт теперь допустим к подготовке, но сама комната не созвана: повестка и внешний вызов
запрещены до отдельного разрешения владельца.

## Предаудит M7 — редакция 1

После отдельного разрешения владельца подготовлена одна повестка `M7_AGENDA.md`. Внешний
consilium не вызывался; бюджет M7 остаётся 0 из 5. Машинный `meeting:audit` подтвердил один
D1 и отсутствие структурных нарушений; объём повестки 11 316 символов, canonical carrier
отсутствует, DoD является последней секцией.

Независимый read-only аудитор дал **PASS**:

- посылки M1-M6 соответствуют закрытым carriers и не переопределяют их;
- inventory/disposition, migration ledger, route/access, rollout, consistency, rollback,
  readiness и slicing #1303/#1305 заданы как вопросы решения, а не готовый verdict;
- семь обязательных таблиц и двадцать cases покрывают measured corpus и failure paths;
- пример rollout допускает иной более строгий эквивалент и не навязывает carrier маршрут;
- storage/intake NO-GO и unknown=NO-GO сохранены;
- code, DNS, Caddy, certificates, Panel, issues, `LIVE_SERVICES` и production не меняются;
- ролевой критерий 36/6x6 оставлен внешнему постаудиту.

M7 run1 допускается только после отдельного разрешения владельца.

## Постаудит M7 — run1

Первый запуск после разрешения отказал **до API**: input manifest показал, что повестка не
доехала из-за общего context/RAG/persona budget. Кредит не потрачен. Dry-run с
`--no-context --no-rag --no-memory` подтвердил полную доставку повестки; после этого тем же
разрешением произведён единственный внешний run1 через `anthropic/claude-sonnet-4-6`.

Механика run1 PASS: 42 предметные реплики, ровно 7 каждой из шести ролей; один D1, DoD
заканчивает файл, `meeting:audit` structural PASS. Внешняя попытка 1 из 5 использована.
Сырой carrier без исправлений сохранён как
[`run1-m3-actions-rollout-cycles-inventory-retirement-predicates`](../../seanses/rejected/static-mmbrn-container-m7-migration-delivery-2026-08-06-run1-m3-actions-rollout-cycles-inventory-retirement-predicates.md).

Независимый смысловой аудит дал **BLOCK** по пяти группам:

- route matrix изобрела `static.*` actions и оставила legacy routes без exact M3 action/object;
- rollout поставил freeze после snapshot и потребовал M5 rehydration parity до rebuild;
- inventory сравнивал counts вместо exact sets, зафиксировал baseline 82/57 как вечный
  corpus и усёк M6 state-indexed reconciliation;
- unresolved `manual_review` мог считаться terminal и не блокировать retirement;
- redirect/canary/rollback/retirement оставили альтернативы и слова `default/proposed/long`
  вместо одного machine-checkable набора constants.

M1/M2 boundary, engine identity separation, текущий NO-GO, чистые посылки, семь таблиц и
отсутствие production-изменений прошли. Повестка run2 получила единый раздел обязательных
поправок. Из-за перехода календарной даты отсутствующий canonical carrier перенесён с
`2026-08-06` на `2026-08-07`; rejected run1 остаётся под фактической датой создания.
Внешний run2 запрещён до повторного независимого предаудита и отдельного разрешения.

## Предаудит M7 — run2

Первый dry-run исправленной повестки отказал до API: после добавления пяти поправок её
13 962 символа не доехали целиком. Кредит не потрачен. Повторяющиеся формулировки сжаты без
удаления требований; новая редакция имеет 11 120 символов.

Повторный dry-run с `--no-context --no-rag --no-memory` завершился успешно: один D1,
повестка доставлена полностью, canonical carrier назначен на фактическую дату
`2026-08-07` и отсутствует. Обрезан только дополнительный блок координации ролей, не agenda.

Независимый read-only аудитор дал **PASS**:

- бюджет 1/5 и rejected-only статус run1 записаны;
- exact M3 surface, ацикличные stage gates, fence-derived set equality, полный M6 corpus,
  запрет unresolved retirement и требование одного набора constants перенесены;
- поправки не выбирают готовые routes, constants, states или rollout DAG;
- M1-M6, current NO-GO, production ban, 36/6x6 и последняя секция DoD сохранены.

Машинный `meeting:audit` также имеет structural PASS. Внешний run2 допускается только после
отдельного разрешения владельца.

## Постаудит M7 — run2

Первая эскалация run2 была остановлена до API политикой внешней передачи: повестка содержит
внутренние сведения о routes/access/storage/migration, а chain включает Anthropic, xAI и
OpenRouter. Владелец был явно уведомлён о payload и destinations и после этого повторно дал
разрешение. Этот отказ не потратил кредит. Фактический run2 произведён
`anthropic/claude-sonnet-4-6`; внешний бюджет стал 2 из 5.

Механика PASS: 37 реплик (Teamlead 7, остальные роли по 6), семь таблиц, один D1,
`meeting:audit` structural PASS. Все DoD boxes оставлены `[ ]`; аудитор признал это честной
позой до внешней проверки, но заявленный verdict содержательно не прошёл. Сырой carrier
сохранён без исправлений как
[`run2-m3-objects-gate-cycles-m6-corpus-rollback-retirement`](../../seanses/rejected/static-mmbrn-container-m7-migration-delivery-2026-08-07-run2-m3-objects-gate-cycles-m6-corpus-rollback-retirement.md).

Независимый смысловой аудит дал **BLOCK** по шести группам:

- actions исправлены, но authority objects снова вымышлены вместо container/collection/lineage;
- provision требует M4 PASS собственного ещё не созданного target, а M5 parity — до rebuild;
- source corpus присвоил имена M6 и выкинул FAILED/reconciliation/state cardinalities;
- rollback purge нарушает retention/append-only и вводит необъявленное состояние ROLLBACK;
- assets проходят по ref-count без M1 qualification/M6 intent, legacy slice предлагает
  synthetic registry rows;
- constants расходятся между tables, а cutover/retirement объединены до окончания redirect.

Повестка run3 получила объединённые обязательные поправки run1-run2. Внешний run3 запрещён
до нового независимого предаудита и отдельного разрешения владельца.

## Предаудит M7 — run3

Повестка run3 имеет один D1, бюджет 2/5 и отсутствующий carrier
`static-mmbrn-container-m7-migration-delivery-2026-08-07.md`; run1-run2 находятся только в
`rejected`. Dry-run с `--no-context --no-rag --no-memory` подтвердил полную доставку.

Независимый read-only аудитор дал **PASS**:

- exact M3 actions и только container/collection/lineage objects обязательны;
- provision/M4 и rebuild/M5 gates разведены без причинных циклов;
- source sets отделены по именам от полного неизменённого M6 corpus;
- rollback не удаляет committed bytes и не вводит необъявленные states;
- pages/assets требуют M1 qualification и отдельного M6 intent; synthetic registry rows
  запрещены;
- unresolved блокирует retirement, а cutover/retirement и единичные constants разведены;
- M1-M6, current NO-GO, production ban и внешний ролевой аудит сохранены.

Машинный `meeting:audit` имеет structural PASS. Неблокирующий риск: повестка занимает
11 965 из лимита 12 000 символов; любое изменение требует нового dry-run. Текущая неизменная
редакция доставляется. Внешний run3 допускается только после отдельного разрешения владельца.

## Постаудит M7 — run3

Первый вызов был остановлен до API: внутренний payload routes/access/storage/migration требовал
явного согласия на destinations. После уведомления владелец разрешил цепочку Anthropic Claude
Sonnet 4.6 → xAI Grok 4.5 → OpenRouter/Claude Sonnet 4.6. Отказ не потратил кредит. Run3
произведён `anthropic/claude-sonnet-4-6`; внешний бюджет стал 3 из 5.

Механика PASS: 36 реплик, ровно шесть от каждой роли, семь таблиц, 20 cases, один D1/carrier,
DoD — последняя секция; `meeting:audit` structural PASS. Сырой carrier без исправлений сохранён
как [`run3-cyclic-dag-readiness-ledger-inventory-routes-retirement`](../../seanses/rejected/static-mmbrn-container-m7-migration-delivery-2026-08-07-run3-cyclic-dag-readiness-ledger-inventory-routes-retirement.md).

Независимый смысловой аудит дал **BLOCK** по семи группам:

- S1 требует уже provisioned FD-1/2/3, а S4 — результаты M5, производимые лишь S5: DAG цикличен;
- вместо exact M4 G1-G10 и M5 G1-G10 даны 6+3 иных gates; полный M6 corpus и reconciliation
  отсутствуют, а rule без evidence получил PASS;
- ledger использует необъявленные discard/parity/canary/cutover/rollback states и обратные
  transitions, хотя объявлена append-only machine;
- validation требует будущий M6 commit до S6, который только создаёт bytes/row/binding;
- representative classes не дают per-object disposition 82+57, а page-based id не покрывает
  самостоятельные assets; baseline 139 выдан за доказанную cardinality;
- route matrix вводит `pass-through`, multi-action API и WS без exact M3 action/object;
- engine copies удаляются post-cutover до retirement; 14-day observation обещает rollback при
  окне 72h.

Повестка run4 требует точные M4/M5 gates, полный M6 contract, причинный DAG, закрытый ledger,
честный per-object NO-GO, точный M3 route classification и совместимые rollback/retirement.
Внешний run4 запрещён до нового dry-run, независимого предаудита и отдельного разрешения.

## Предаудит M7 — run4

После возобновления 2026-08-08 независимый read-only аудитор дал **PASS**. Предыдущая попытка
предаудита не состоялась из-за лимита агента и не является внешним прогоном M7.

- canonical carrier отсутствует; run1-run3 находятся только в `rejected`; бюджет 3/5;
- один D1/carrier, повестка занимает 11 931 из 12 000 символов;
- все семь групп BLOCK run3 перенесены без обнаруженной коллизии;
- точные M3 actions/objects, M4 G1-G10, M5 G1-G10 и полный M6 сохранены;
- закрытая append-only machine, причинный DAG, M6 commit order и per-object page/asset evidence
  обязательны;
- rollback, observation, cutover и retirement разведены;
- 36/6x6 и последняя секция DoD сохранены; готовый verdict не подставлен.

Dry-run с `--no-context --no-rag --no-memory` доставил повестку полностью. Машинный
`meeting:audit` имеет structural PASS. Внешний run4 допускается только после отдельного
разрешения владельца с явным согласием на передачу внутреннего payload указанной LLM-chain.

## Постаудит M7 — run4

После явного разрешения run4 произведён `anthropic/claude-sonnet-4-6`; внешний бюджет стал
4 из 5. Инструмент записал `2026-08-08`, хотя agenda и итог модели продолжили называть carrier
с датой `2026-08-07`. Сырой файл без исправлений сохранён как
[`run4-carrier-replies-cyclic-gates-m6-ledger-tables`](../../seanses/rejected/static-mmbrn-container-m7-migration-delivery-2026-08-08-run4-carrier-replies-cyclic-gates-m6-ledger-tables.md).

Механический **BLOCK**: фактически 29 реплик — роли 5/5/5/5/5/4 вместо 36 и 6x6. DoD последняя,
20 cases и structural `meeting:audit` PASS этого не исправляют. Независимый смысловой аудит
подтвердил ещё пять групп:

- S2 требует M4 до manifest/post-import evidence, S7 — M5 до rehydration: DAG цикличен;
- M4/M5 predicates переопределены: random sample вместо full restore, RPO 1h вместо 24h,
  сокращённые M5 predicates и потерян семилетний retention;
- M6 сведён к одной строке без canonical sets/FAILED/cardinalities/full diff; `COMMITTED` не
  требует verified FD-1 и durable binding;
- ledger использует необъявленные `HOLD`/`ANNOTATION_PARITY_FAIL`, а asset снова проходит по
  ref-count/parent без отдельного M6 intent;
- inventory/disposition и rollout DAG даны prose, а не двумя из семи обязательных tables.

Повестка run5 назначает фактический carrier `2026-08-08`, буквально несёт predicates M4/M5,
полный M6 commit, запрет циклов/необъявленных states, семь реальных tables и ролевой минимум.
Это последняя внешняя попытка 5/5; до dry-run, независимого предаудита и нового разрешения она
запрещена.

## Предаудит M7 — run5

Первый независимый проход дал **BLOCK до созыва** по двум пробелам: M4 G1 был назван без
формулы `free_after/utilisation/quota`, а M6 sets — без определений и crash-cardinalities.
Внешний кредит не потрачен. Повестка дополнена буквальными predicates и снова сжата.

Повторный read-only предаудит дал **PASS**:

- carrier `2026-08-08` отсутствует; run1-run4 только в `rejected`; бюджет 4/5;
- один D1/carrier, размер 11 825 из 12 000, dry-run доставил topic полностью;
- M4 G1 formula, M4 G1-G10, M5 G1-G10 и полный M6 sets/cardinalities/diff сохранены;
- причинный DAG, closed append-only ledger и отдельный intent для каждого page/asset обязательны;
- cutover/rollback/observation/retention/retirement разведены;
- требуются >=36 реплик, минимум шесть каждой роли и семь реальных Markdown tables;
- DoD последняя, production ban сохранён, готовый states/constants/DAG/verdict не подставлен.

Машинный `meeting:audit` имеет structural PASS. Внешний run5 — последняя попытка 5/5 —
допускается только после отдельного разрешения владельца на передачу внутреннего payload
внешней LLM-chain.

## Постаудит M7 — run5 и local synthesis

После отдельного разрешения run5 произведён `xai/grok-4.5`; внешний бюджет исчерпан 5/5.
Механика впервые прошла полностью: 42 реплики, ровно семь каждой роли, семь обязательных
Markdown tables, exact carrier/date и последняя секция DoD. Structural `meeting:audit` PASS.
Сырой carrier сохранён как
[`run5-ledger-routes-m5-m6`](../../seanses/rejected/static-mmbrn-container-m7-migration-delivery-2026-08-08-run5-ledger-routes-m5-m6.md).

Независимый смысловой аудит дал **BLOCK**:

- `PARITY_FAILED -> PROJECTION_EXPORTED` был reverse edge;
- route rows содержали `one of 8`, aggregate auth и `download/read-bytes`;
- readiness сворачивал M5 G1-G10 в одну ссылочную строку;
- M6 был одной строкой без полного корпуса и predicates.

По owner rule шестого внешнего вызова нет. Canonical собран локально из run5 и помечен
`local synthesis`; исходник неизменён в `rejected`. Первый local audit подтвердил исправление
четырёх дефектов, но дал BLOCK по трём остаткам: неполная M4 G1 formula, stale pre-write M4 G6
и M6 без `A_all`/state-linked cardinalities. Они исправлены: G1 буквальный, G6 повторяется после
S8, M6 связывает состояния с `0/0`, `0/1`, `1/1/1` и full bidirectional diff.

Финальный независимый recheck завис без вердикта; повторный короткий dispatch не стартовал.
Это не semantic PASS. Локальная сверка подтверждает устранение всех семи перечисленных BLOCK,
а machine audit до последних точечных правок имел structural PASS. Canonical остаётся
**local-synthesis candidate** до машинной перепроверки и owner ratification. Пять внешних
вызовов и отказ финального аудитора сохранены в
[`прецеденте`](../../precedents/2026-08-08-static-mmbrn-m7-five-external-runs-local-synthesis.md).

## Ратификация M7 local synthesis

Владелец ответил «ратифицирую» 2026-08-08 после явного доклада об ограничении: финальный
независимый recheck не дал вердикта из-за зависания. Ратификация принимает canonical local
synthesis и весь сохранённый audit trail; зависший вызов не переименовывается в PASS.

Definition of Done carrier отмечен `[x]` по owner-act и проверенной механике 42/7x6/7 tables.
Состояние комнаты — **PASS local-synthesis; РАТИФИЦИРОВАНО**. Machine recheck и git delivery
остаются отдельными действиями; отсутствие shell не отменяет акт, но запрещает докладывать
коммит или доставку до их фактического выполнения.
