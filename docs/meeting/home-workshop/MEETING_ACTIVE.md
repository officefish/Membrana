# MEETING ACTIVE — `home-workshop`

> Протокол **контейнера**, отдельный от протоколов консилиумов.
> Открыто: 2026-07-22 · Основание: форк «а» шторма
> [`storm-home-workshop-2026-07-22`](../../storm/storm-home-workshop-2026-07-22/REPORT.md)

---

## СТАТУС: ЗАСЕДАНИЕ ЗАКРЫТО 2026-07-22 · эпик РАТИФИЦИРОВАН владельцем

Вердикты собраны и ратифицированы: [`EPIC.md`](./EPIC.md). Финальный аудит (отдельный
агент) — процедура соблюдена, посылка 6 M4 вычеркнута эрратумом. Дальше — фаза
исполнения паттерна «Домашняя мастерская».

## Задание

**Возвести паттерн «Домашняя мастерская»: контракт мастерской, словарь инструментов,
полиморфизм/рекурсия в 2D-доме, обобщение двух живущих мастерских, правило
принадлежности + починка #915.**

Полностью: [`MEETING_BRIEF.md`](./MEETING_BRIEF.md). Изменение только через
`BRIEF_AMENDMENT.md` + LGTM владельца (S-M4).

## Роли

| Роль | Кто | Ограничение |
|---|---|---|
| Председатель | агент сессии (ветка `meeting/home-workshop`) | не судит соблюдение процедуры |
| Аудитор | **отдельный** агент, read-only | не пишет повестку, не голосует, не чинит (S-M5) |
| Владелец | druid | задание, ратификация порядка M0 |

## Порядок (вердикт M0 — **РАТИФИЦИРОВАН владельцем 2026-07-22**)

DAG (не линейная цепь): есть независимая пара на третьем слое.

```
L0: 1 контракт паттерна и дом   (фундамент)
L1: 2 словарь инструментов
L2: 3 полиморфизм/рекурсия 2D  ⟂  5 принадлежность + #915   (независимы — антицепь)
L3: 4 обобщение двух мастерских (сток; нужен внешний замер потребления кита)
```

Рёбра: 1→2, 1→3, 1→4, 1→5, 2→3, 2→5, 2→4(слабое). Обратных нет.
Протокол M0: [`m0-order`](../../seanses/home-workshop-m0-order-2026-07-22.md).
Риск обратного ранга: контракт паттерна из определения превращается в рационализацию
задним числом. Блокер по фактуре: кандидат 4 требует замера — потребляют ли ветки/задачи
кит или зовут скрипты напрямую (снять до комнаты 4).

## Фазы (в порядке из M0; независимую пару беру 3, затем 5)

| Фаза | Вопрос (кандидат) | Статус |
|------|--------|--------|
| M0 | порядок зависимостей пяти кандидатов | ✅ вердикт DAG + ратификация 22.07 |
| M1 | контракт паттерна и дом (1) | ✅ вердикт (`m1-contract`): мастерская = именованный набор для ОДНОГО дома, `worksOn: House` (обяз., 1) + `orders: Kit[]` (опц.); сторона спроса; третья ось «операции» рядом с GC (пространство) и PSV (время); `HOME_WORKSHOP.md` (SHOULD); ссылки без дубля правил (`owner(r)` единствен) |
| M2 | словарь инструментов (2) | ✅ вердикт (`m2-vocabulary`): минимум ровно 3 глагола (`audit`/`decompose`/`inspectElement`, разные арности); стек-слой — SHOULD-гомоморфизм `φ:M→StackOps` (не изоморфизм, не интерфейс); `domainPreserved` — декомпозиция не переписывается через примитивы; открытый класс `domain` (каждый с `worksOn`); зуб `validateWorkshop` |
| M3 | полиморфизм/рекурсия в 2D-доме (3) | ✅ вердикт (`m3-polymorphism`): общий интерфейс (не свитч) — род приносит реализацию, живёт в ките процедуры; база рекурсии = якорный документ, конечность через фундированность; `anchor-missing` = находка (не гейт), `kit-missing` = жёсткий стоп; `decompose` полиморфен по этажам; операции обзора идемпотентны (чтение). _rt-6 споткнулся на метке F3 (#558), вердикт в теле есть — сверить финальным аудитом_ |
| M4 | принадлежность инструмента + #915 (5) | ✅ вердикт (`m4-belonging`): правило `owner(tool)=workshop(worksOn(tool))`; зуб `checkOwnership` (приписка + уникальность), блокирующий CI; #915 → процедурная мастерская ПОСЛЕ её создания, до того — видимый Allowlist с `expiresWhen`; переезд — отдельная задача. _rt-6 на метке F4 (#558); посылка 6 ссылается на независимый M3 — кандидат в лишнюю посылку, на финальный аудит_ |
| M5 | обобщение двух живущих мастерских (4) | ✅ вердикт (`m5-generalize`): `workshop.manifest.json` обеим (поля M2 + `worksOn` + `kit:null` явно); `inspectElement` нет у обеих → SHOULD/⚠; точка истины правил только в `HOME_WORKSHOP.md`, манифест ссылается; CI-зуб на совпадение чеклиста; переписывание скриптов — BLOCK |
