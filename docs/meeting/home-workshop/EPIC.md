# ЭПИК заседания `home-workshop` — паттерн «Домашняя мастерская»

> Собран 2026-07-22 из пяти вердиктов (порядок M0 — DAG, ратифицирован владельцем 22.07).
> Основание: форк «а» шторма `storm-home-workshop-2026-07-22` (10 тезисов + стек-словарь).
> **Статус: РАТИФИЦИРОВАН владельцем 2026-07-22** (посылка 6 M4 вычеркнута эрратумом).

---

## Вердикты (порядок исполнения = слои DAG M0)

| Слой | Вердикт | Протокол | Суть |
|------|---------|----------|------|
| L0 · Ф1 | `m1-contract` | [`m1`](../../seanses/home-workshop-m1-contract-2026-07-22.md) | Мастерская = именованный набор инструментов для **одного** дома; `worksOn: House` (обяз., кратность 1) + `orders: Kit[]` (опц.); **сторона спроса** (заказчик). Третья ось паттернов: `GROUP_CONTAINERIZATION` = пространство, `PINNED_SUBGRAPH_VERSIONING` = время, «Домашняя мастерская» = **операции**. Документ `docs/patterns/HOME_WORKSHOP.md` (SHOULD). Правила без дубля: `owner(r)` единствен, соседи держат reference. Недоступен кит → `unequipped` (видимо, без тихой подмены) |
| L1 · Ф2 | `m2-vocabulary` | [`m2`](../../seanses/home-workshop-m2-vocabulary-2026-07-22.md) | Минимум **ровно три глагола**: `audit` (`House→Report`), `decompose` (`House→Element[]`), `inspectElement` (`Element→Detail\|recurse`, терминатор — якорь). Стек-слой — **SHOULD-гомоморфизм** `φ:M→StackOps`, не изоморфизм и не обязательный интерфейс. `domainPreserved` — доменная декомпозиция **не** переписывается через примитивы. Открытый класс `domain` (каждый с `worksOn`). Зуб `validateWorkshop` |
| L2 · Ф3 | `m3-polymorphism` | [`m3`](../../seanses/home-workshop-m3-polymorphism-2026-07-22.md) | Диспетчер — **общий интерфейс** (не свитч): род процедуры приносит реализацию, живёт в **ките процедуры**; мастерская зовёт, не зная чей кит. База рекурсии — **якорный документ**, конечность через фундированность (`inspectElement` тотальна). `anchor-missing` = находка обзора (не гейт, T9), `kit-missing` = жёсткий failover-стоп. `decompose` полиморфен по этажам. Операции обзора — **чтение**, идемпотентны |
| L2 · Ф4 | `m4-belonging` | [`m4`](../../seanses/home-workshop-m4-belonging-2026-07-22.md) | Правило `owner(tool) = workshop(worksOn(tool))`. Зуб `checkOwnership` (приписка `expected=actual` + уникальность инструмента в одном манифесте), блокирующий CI. #915 → процедурная мастерская **после её создания**; до того — видимый `Allowlist` с `reason`+`expiresWhen`, без тихого pending. Переезд — **отдельная задача** |
| L3 · Ф5 | `m5-generalize` | [`m5`](../../seanses/home-workshop-m5-generalize-2026-07-22.md) | Обеим живущим (ветки, задачи) — `workshop.manifest.json` (поля M2 + `worksOn` + `kit:null` явно) + ссылка на `HOME_WORKSHOP.md` + копия чеклиста. `inspectElement` нет у обеих → **⚠** (SHOULD). Точка истины правил только в паттерне, манифест ссылается; CI-зуб на совпадение чеклиста. Переписывание живых скриптов — **BLOCK** |

## Родовой узор (сведён)

```
Контейнер скриптов ──supplies──▶ Кит ──orders◀── Мастерская ──worksOn──▶ Дом
   [поставка]                                    [спрос, заказчик]      [группа]
                        три глагола: audit · decompose · inspectElement (+ domain, +stackLike SHOULD)
   плоский дом → inspectElement на лист · процедурный дом (2D) → полиморфизм по роду,
     рекурсия decompose/inspect по этажам до ЯКОРНОГО документа
```

## Маппинг на мандат заседания

- Формальный контракт мастерской — Ф1.
- Словарь (три глагола + стек-ориентир, декомпозиция не поглощается) — Ф2.
- Полиморфизм/рекурсия в 2D-доме до якоря — Ф3.
- Правило принадлежности + починка #915 — Ф4.
- Обобщение двух живущих мастерских — Ф5.

## Хвосты заседания (в фазу исполнения / отдельные задачи)

1. **Создание процедурной мастерской** (три глагола над процедурным домом) — предусловие
   переезда #915; достройка 2D-дома (T10) отдельной задачей, не в этом эпике.
2. **Переезд #915** в процедурную мастерскую — отдельная задача с гейтом «дом-приёмник
   существует»; до того #915 в `Allowlist` с `expiresWhen`.
3. **Реализация зубов**: `validateWorkshop` (Ф2), `checkOwnership` (Ф4), CI-зуб чеклиста
   (Ф5), тест конечности `inspectElement` (Ф3) — фаза исполнения паттерна.

## Финальный аудит (отдельный агент, read-only, S-M5) — 2026-07-22

**Вердикт аудитора: процедура соблюдена с одной точечной правкой.** Пять из пяти
проверок проходят по всем фазам, кроме проверки 4 в M4.

- **Ф3/Ф4 — rt-6 опровергнут глазами.** Метка-греп дал ложный отрицательный (#558):
  вердикты по существу в протоколах M3 и M4 фактически вынесены. Проверка 2 — PASS.
- **Ф4, посылка 6 — ЛИШНЯЯ (эрратум, вычеркнута).** Посылка 6 M4 регистрировала как
  вход вывод M3 (`checkLayerDirection`), а M3 и M4 **независимы** по DAG (антицепь);
  агенда M4 прямо запрещала тащить M3. Носитель-манифест на деле введён в **M2**, а не
  M3 (посылка её мис-атрибутировала); вердикт M4 самодостаточен на M1/M2 + правило
  Веснина + факт #915. По правилу 5 заседаний лишняя посылка лжёт об отзыве — **посылка 6
  M4 настоящим вычеркнута из оснований** (протокол движка сохранён как есть с пометкой
  канала; корректность M4 не затронута). Спрятанных посылок аудит не нашёл.
- EPIC, задание (=BRIEF, поправок нет), пометки канала (все 6 — движок), границы комнат
  (колонизации нет) — чисты.

## Ратификация

Порядок M0 (DAG) ратифицирован владельцем 22.07. Эпик (пять вердиктов) **ратифицирован
владельцем 22.07** (посылка 6 M4 вычеркнута эрратумом финального аудита). Дальше — фаза
исполнения: паттерн-документ `HOME_WORKSHOP.md`, манифесты двух мастерских, зубы
(`validateWorkshop`, `checkOwnership`, CI-чеклист), затем отдельными задачами —
процедурная мастерская (три глагола над 2D-домом) и переезд #915.
