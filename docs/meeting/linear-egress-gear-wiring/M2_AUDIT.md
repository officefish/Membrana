# Аудит M2 — `linear-egress-gear-wiring`

> **Аудитор:** отдельный read-only агент (S-M5)  
> **Дата:** 2026-07-20  
> **Команда:** `yarn meeting:audit --id linear-egress-gear-wiring` → **exit 2**  
> **Вердикт машины:** **FAIL** (3 нарушения; вердикты комнат не правил)

Машинный прогон (worktree `Membrana-meeting-egress`):

```
meeting:audit — linear-egress-gear-wiring (повесток: 4, протоколов: 4)

  ✓ [1] M0-topic.md
      один вопрос: E1
  ✓ [1] M1-topic.md
      один вопрос: E2
  ✓ [1] M1b-topic.md
      один вопрос: E3
  ✓ [1] M2-topic.md
      один вопрос: E4
  ✓ [2] linear-egress-gear-wiring-m0-order-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [2] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [2] linear-egress-gear-wiring-m1b-sprint-scaffold-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [2] linear-egress-gear-wiring-m2-secrets-trust-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [3] порядок протоколов
      4 протокол(ов) по возрастанию
  ✖ [4] linear-egress-gear-wiring-m0-order-2026-07-20.md
      DoD несёт обязательства по чужим вопросам (E1) — hard rule 2
  ✓ [4] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      структурных дефектов не найдено (НЕ «чист»)
  ✖ [4] linear-egress-gear-wiring-m1b-sprint-scaffold-2026-07-20.md
      DoD несёт обязательства по чужим вопросам (E3) — hard rule 2
  ✖ [4] linear-egress-gear-wiring-m2-secrets-trust-2026-07-20.md
      DoD несёт обязательства по чужим вопросам (E4) — hard rule 2
  ✓ [6] linear-egress-gear-wiring-m0-order-2026-07-20.md
      произведён инструментом (канал помечен) — гейт посылок отработал при записи
  ✓ [6] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      произведён инструментом (канал помечен) — гейт посылок отработал при записи
  ✓ [6] linear-egress-gear-wiring-m1b-sprint-scaffold-2026-07-20.md
      произведён инструментом (канал помечен) — гейт посылок отработал при записи
  ✓ [6] linear-egress-gear-wiring-m2-secrets-trust-2026-07-20.md
      произведён инструментом (канал помечен) — гейт посылок отработал при записи
  ✓ [5] артефакты в git
      история есть — правку после ратификации видно

Нарушений: 3.
```

## Ручная допроверка (то, что машина не увидела / не судит)

| Проверка | Результат | Заметка |
|---|---|---|
| 1 — ровно один ID в повестке M2 | PASS | `M2-topic.md` → `E4` only |
| 2 — секция вердикта M2 | PASS | «Итоговое решение консилиума» + DoD |
| 3 — порядок протоколов | PASS | m0 → m1 → m1b → m2 по датам UTC в метаданных |
| 4 — структура вердикта / посылки M2 | PASS | «Список посылок» есть; выводы комнаты в посылки не внесены; DoD без ID соседей E1/E2/E3 |
| 4′ — FAIL машины на M0 DoD/`E1` | **ложный sibling FAIL** | DoD M0 несёт «для E1» — это **свой** вопрос M0. `extractAgendaIds` на протоколе не видит `E1` (нет формы `**E1`), поэтому E1 в `siblingIds`. Вердикт M0 **не правил**. Класс из `M1_AUDIT.md` §4′ / `M1b_AUDIT.md` §4′. |
| 4″ — FAIL машины на M1b DoD/`E3` | **ложный sibling FAIL** | DoD помечен `(E3, …)` — **свой** вопрос комнаты M1b. Та же механика. Вердикт M1b **не правил**. |
| 4‴ — FAIL машины на M2 DoD/`E4` | **ложный sibling FAIL** | DoD: «только вопрос E4 — trust boundary…» — **свой** вопрос комнаты M2, не сосед. Та же механика sibling-ложного FAIL. Вердикт M2 **не правил**. |
| 5 — артефакты в git | PASS | все четыре повестки + четыре протокола tracked; история видна |
| 6 — канал инструмента M2 | PASS | `<!-- канал: llm … -->` |
| S-M2 — M2 состоялся | PASS | вердикт trust boundary зафиксирован |
| S-M5 — аудитор ≠ председатель | PASS | этот файл пишет отдельный агент; BRIEF/ACTIVE/протоколы/повестки не правил; Mn не созвал |

## Содержание процедуры M2 (не существо схемы / не оценка политики)

- Вопрос комнаты — ровно trust boundary / секреты (E4 / К2); в ACTIVE помечен CLOSED.
- Вердикт: кандидат 1 — `LINEAR_API_KEY` только media-NL; egress инициирует только процесс на media; office→media через существующий `X-Membrana-Token` / `API_INTERNAL_TOKEN`; агент РФ без ключа и GraphQL.
- Отложено явно: узкий egress-trigger при `k≥2` (кандидат 2); cold-only pull при on-demand+полной изоляции (кандидат 3).
- Уход в К3 одной строкой: провенанс `producedBy: media-NL` + регион (контракт, не секрет); К3/К5/К4b явно «не решались».
- Посылки: нормы владельца + M0/M1 + факты кода/BRIEF/пилота — без выводов этой комнаты.
- Mn (К3) аудитором **не созвано**.

Неблокирующие оговорки:

- все три машинных FAIL check 4 — **ложные sibling** на своих ID в DoD (E1/M0, E3/M1b, E4/M2); зубы ловят ярлык без «own» ID в теле протокола;
- футер M2: «Реплик в диалоге: 20» — семантику счётчика машина не сверяет (известный класс #558 / reconcile).

## Итог для владельца

| Слой | Результат |
|---|---|
| `yarn meeting:audit` | **exit 2 · FAIL** (3 structural) |
| Процедура M2 (ручная) | годна: вердикт есть, посылки на месте, S-M5 соблюдён |
| Машинные FAIL | все три на **своих** ID в DoD (E1/M0, E3/M1b, E4/M2) — класс ложного sibling; **не** на чужих вопросах |
| Вердикты | **не менялись** |

Содержательную оценку выбранной trust-политики аудитор не выдаёт.
