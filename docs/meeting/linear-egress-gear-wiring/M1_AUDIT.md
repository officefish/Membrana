# Аудит M1 — `linear-egress-gear-wiring`

> **Аудитор:** отдельный read-only агент (S-M5)  
> **Дата:** 2026-07-20  
> **Команда:** `yarn meeting:audit --id linear-egress-gear-wiring` → **exit 2**  
> **Вердикт машины:** **FAIL** (1 нарушение; вердикты комнат не правил)

Машинный прогон (worktree `Membrana-meeting-egress`):

```
meeting:audit — linear-egress-gear-wiring (повесток: 3, протоколов: 2)

  ✓ [1] M0-topic.md
      один вопрос: E1
  ✓ [1] M1-topic.md
      один вопрос: E2
  ✓ [1] M1b-topic.md
      один вопрос: E3
  ✓ [2] linear-egress-gear-wiring-m0-order-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [2] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [3] порядок протоколов
      2 протокол(ов) по возрастанию
  ✖ [4] linear-egress-gear-wiring-m0-order-2026-07-20.md
      DoD несёт обязательства по чужим вопросам (E1) — hard rule 2
  ✓ [4] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      структурных дефектов не найдено (НЕ «чист»)
  ✓ [6] linear-egress-gear-wiring-m0-order-2026-07-20.md
      произведён инструментом (канал помечен)
  ✓ [6] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      произведён инструментом (канал помечен)
  ? [5] артефакты в git
      вне git: docs/meeting/linear-egress-gear-wiring/M1b-topic.md — проверка без зубов

Нарушений: 1.
```

## Ручная допроверка (то, что машина не увидела / не судит)

| Проверка | Результат | Заметка |
|---|---|---|
| 1 — ровно один ID в повестке M1 | PASS | `M1-topic.md` → `E2` only |
| 2 — секция вердикта M1 | PASS | «Итоговое решение консилиума» + DoD |
| 3 — порядок протоколов | PASS | m0 → m1 по датам UTC в метаданных |
| 4 — структура вердикта / посылки M1 | PASS | «Список посылок» есть; выводы комнаты в посылки не внесены; DoD без ID соседей E1/E3 |
| 4′ — FAIL машины на M0 DoD | **оговорка аудитора** | DoD M0 содержит «для E1» — это **свой** вопрос M0, не сосед. `extractAgendaIds` на протоколе не видит `E1` (нет формы `**E1`), поэтому E1 попадает в `siblingIds` из повесток. Вердикт M0 **не правил** (аудитор ≠ председатель). |
| 5 — артефакты в git | НЕЧЕМ | `M1b-topic.md` untracked; BRIEF/ACTIVE/M1-topic/протоколы — tracked |
| 6 — канал инструмента M1 | PASS | `<!-- канал: llm … -->` |
| S-M2 — M1 состоялся | PASS | вердикт формы egress зафиксирован |
| S-M5 — аудитор ≠ председатель | PASS | этот файл пишет отдельный агент; BRIEF/ACTIVE/протоколы/повестки не правил; Mn не созвал |

## Содержание процедуры M1 (не существо DAG / не оценка топологии)

- Вопрос комнаты — ровно форма egress-пути (E2 / К1); в ACTIVE помечен CLOSED.
- Вердикт: media-NL = инициатор + клиент Linear; office = потребитель `linear-snapshot@1`; кандидат 2.
- Уход в К2/К3 назван одной строкой каждый; stub/К4a/К4b явно «не решалось».
- Посылки: факты замера/кода/пилота + нормы владельца + ратифицированный M0 — без выводов этой комнаты.
- `M1b-topic.md` (E3 / К4a) лежит в дереве **вне git** и **без протокола** — параллельная повестка подготовлена, заседание M1b аудитором не созвано и не оценивается как состоявшееся.

Неблокирующие оговорки:

- машинный FAIL check 4 на M0 при повторном аудите после появления M1/M1b-повесток — см. 4′ выше (зубы ловят ярлык в DoD без «own» ID в протоколе);
- check 5 = НЕЧЕМ из‑за untracked `M1b-topic.md`, не из‑за отсутствия истории M0/M1;
- футер M1: «Реплик в диалоге: 20» — семантику счётчика машина не сверяет (известный класс #558 / reconcile).

## Итог для владельца

| Слой | Результат |
|---|---|
| `yarn meeting:audit` | **exit 2 · FAIL** (1 structural) |
| Процедура M1 (ручная) | годна: вердикт есть, посылки на месте, S-M5 соблюдён |
| Машинный FAIL | на **M0** DoD/`E1` (оговорка 4′), не на протоколе M1 |
| Вердикты | **не менялись** |

Содержательную оценку выбранной топологии egress аудитор не выдаёт.
