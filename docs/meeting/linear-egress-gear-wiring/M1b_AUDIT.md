# Аудит M1b — `linear-egress-gear-wiring`

> **Аудитор:** отдельный read-only агент (S-M5)  
> **Дата:** 2026-07-20  
> **Команда:** `yarn meeting:audit --id linear-egress-gear-wiring` → **exit 2**  
> **Вердикт машины:** **FAIL** (2 нарушения; вердикты комнат не правил)

Машинный прогон (worktree `Membrana-meeting-egress`):

```
meeting:audit — linear-egress-gear-wiring (повесток: 4, протоколов: 3)

  ✓ [1] M0-topic.md
      один вопрос: E1
  ✓ [1] M1-topic.md
      один вопрос: E2
  ✓ [1] M1b-topic.md
      один вопрос: E3
  ✓ [1] M2-topic.md
      один вопрос: E4
  ✓ [2] linear-egress-gear-wiring-m0-order-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [2] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [2] linear-egress-gear-wiring-m1b-sprint-scaffold-2026-07-20.md
      секция вердикта есть (семантику машина не судит — #558)
  ✓ [3] порядок протоколов
      3 протокол(ов) по возрастанию
  ✖ [4] linear-egress-gear-wiring-m0-order-2026-07-20.md
      DoD несёт обязательства по чужим вопросам (E1) — hard rule 2
  ✓ [4] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      структурных дефектов не найдено (НЕ «чист»)
  ✖ [4] linear-egress-gear-wiring-m1b-sprint-scaffold-2026-07-20.md
      DoD несёт обязательства по чужим вопросам (E3) — hard rule 2
  ✓ [6] linear-egress-gear-wiring-m0-order-2026-07-20.md
      произведён инструментом (канал помечен) — гейт посылок отработал при записи
  ✓ [6] linear-egress-gear-wiring-m1-egress-path-2026-07-20.md
      произведён инструментом (канал помечен) — гейт посылок отработал при записи
  ✓ [6] linear-egress-gear-wiring-m1b-sprint-scaffold-2026-07-20.md
      произведён инструментом (канал помечен) — гейт посылок отработал при записи
  ? [5] артефакты в git
      вне git: docs/meeting/linear-egress-gear-wiring/M2-topic.md — проверка без зубов

Нарушений: 2.
```

## Ручная допроверка (то, что машина не увидела / не судит)

| Проверка | Результат | Заметка |
|---|---|---|
| 1 — ровно один ID в повестке M1b | PASS | `M1b-topic.md` → `E3` only |
| 2 — секция вердикта M1b | PASS | «Итоговое решение консилиума» + DoD |
| 3 — порядок протоколов | PASS | m0 → m1 → m1b по датам UTC в метаданных |
| 4 — структура вердикта / посылки M1b | PASS | «Список посылок» есть; выводы комнаты в посылки не внесены; DoD без ID соседей E1/E2/E4 |
| 4′ — FAIL машины на M0 DoD/`E1` | **повтор известного ложного FAIL** | Тот же класс, что в `M1_AUDIT.md` §4′: DoD M0 несёт «для E1» — это **свой** вопрос M0. `extractAgendaIds` на протоколе не видит `E1` (нет формы `**E1`), поэтому E1 попадает в `siblingIds` из повесток. Вердикт M0 **не правил**. |
| 4″ — FAIL машины на M1b DoD/`E3` | **тот же ложный класс** | DoD помечен `(E3, …)` — **свой** вопрос комнаты M1b, не сосед. Та же механика sibling-ложного FAIL, что у E1 на M0. Вердикт M1b **не правил**. |
| 5 — артефакты в git | НЕЧЕМ | `M2-topic.md` untracked; M1b-topic / m1b-протокол / ACTIVE — tracked |
| 6 — канал инструмента M1b | PASS | `<!-- канал: llm … -->` |
| S-M2 — M1b состоялся | PASS | вердикт каркаса процедур зафиксирован |
| S-M5 — аудитор ≠ председатель | PASS | этот файл пишет отдельный агент; BRIEF/ACTIVE/протоколы/повестки не правил; Mn не созвал |

## Содержание процедуры M1b (не существо схемы / не оценка кода)

- Вопрос комнаты — ровно каркас процедур спринта (E3 / К4a); в ACTIVE помечен CLOSED.
- Вердикт: `task:start` = событие старта (Issue → биекция → acceptance → stub `deferred-egress` → leadPersona); `task:register` = содержание; зуб `active ∧ ¬issueExists → reject`; посев на паспорт+словарь+`task:start`; три носителя: Issue=счёт, registry=содержание, Linear=движение(stub); override вне MAIN_DAY с Issue владельца.
- Уход в К2/К4b/К5 назван одной строкой каждый; форма egress (M1) и секреты явно «не решались».
- Посылки: нормы владельца + паспорт/словарь + факты пилота/workflow + предшественники M0/M1 — без выводов этой комнаты.
- `M2-topic.md` (E4 / К2) лежит в дереве **вне git** — параллельная подготовка председателя; заседание M2 аудитором **не созвано** и не оценивается как состоявшееся.

Неблокирующие оговорки:

- оба машинных FAIL check 4 — **ложные sibling** на своих ID в DoD (E1 на M0, E3 на M1b); зубы ловят ярлык без «own» ID в теле протокола;
- check 5 = НЕЧЕМ из‑за untracked `M2-topic.md`, не из‑за отсутствия истории M0/M1/M1b;
- футер M1b: «Реплик в диалоге: 22» — семантику счётчика машина не сверяет (известный класс #558 / reconcile).

## Итог для владельца

| Слой | Результат |
|---|---|
| `yarn meeting:audit` | **exit 2 · FAIL** (2 structural) |
| Процедура M1b (ручная) | годна: вердикт есть, посылки на месте, S-M5 соблюдён |
| Машинные FAIL | оба на **своих** ID в DoD (E1/M0, E3/M1b) — класс ложного sibling из M1_AUDIT §4′; **не** на чужих вопросах |
| Вердикты | **не менялись** |

Содержательную оценку выбранного каркаса `task:start` аудитор не выдаёт.
