# M6 — рефакторинг extractor: исполнение вердиктов контура

**C6 —** Как существующий extractor памяти персон перестраивается под ратифицированные
контракты C1–C5: границы правки (что меняется, что не трогается), миграция —
как существующие журналы становятся архив+проекция и восстанавливается ли уже
вытесненное из git-истории, какие зубы держат каждый контракт (erase-запрет,
pinned fail-closed, фикстура потери 27.07, forge_done, forge_metrics), и каков
порядок стройки — фазы/блоки, пригодные для оркестрации несколькими исполнителями
без коллизий файлов? **Список посылок обязателен.**

---

## Вводные (терминальная комната; существо C1–C5 ратифицировано — НЕ переоткрывать, только исполнение)

- Рамки (все ратифицированы 28.07): C1 — архив per-persona jsonl первичен,
  md = проекция, transfer с importanceSnapshot, erase отсутствует; C2 —
  selectOperational(candidates, importance, budget) → {retained, transferred,
  report}, точка вызова — extractor; C3 — один retrieval-порт, лифт ≠ судья;
  C4 — леммы шагов memory.evening_compress / morning_warmup / surface_invoke,
  квитанции, матрица miss; C5 — op-log verbs → таблица эмиттеров, предикаты
  n/a-vs-zero и запретов forge.
- Существующий extractor: scripts/persona-memory-extract.mjs — перезаписывает
  md целиком, отбирает «33 из 239 кандидатов» под 5K, кандидатов собирает из
  docs/seanses; importance.json объявлен, не читается; вызов — шаг persona-memory
  вечерней цепочки (после ласточки, #569).
- Миграция-вопрос: текущие md-журналы — единственный носитель; вытесненное
  27–28.07 живёт ТОЛЬКО в git-истории файлов (диффы восстановимы — отчёт памяти
  их уже считает). Комнате решить: backfill архива из git-истории (глубина?
  провенанс восстановленного?) или архив начинается с miграционного снапшота
  «нулевого дня» с честной пометкой.
- Зубы-требования из рамок (собрать в план, не изобретать новых норм): C2 DoD п.9 —
  фикстура «routine дня вытеснила position мастерской (27.07)» не воспроизводится;
  pinned overflow → ошибка, не truncate; C4 — forge_done ловится; C5 — forge_metrics
  и n/a-vs-zero ловятся; C1 — append-монотонность архива в фикстуре.
- Образец оркестрации (вчера-сегодня, работает): фазы с контрактом стыка
  «build<X>(root, date) → {markdown, stats, problems}», блоки без общих файлов,
  сборщик-оркестратор отдельным тактом; агенты в своих worktree, pr:ship.
- Существующие потребители extractor-выхода: team-memory-report (дифф md),
  day-memo persona-trace (читает md + отчёт), консилиум --with-memory /
  persona-RAG (читают md). Проекция обязана остаться читаемой ими без правок
  ИЛИ правки потребителей — явной частью плана.
- Ограничение: пороги/веса/эталоны — v2; UI — вне; новые нормы контрактов —
  запрещены (обнаружен пробел в C1–C5 → зафиксировать находкой к сшивке,
  не решать здесь).
