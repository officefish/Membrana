# M5 — повестка: критерий готовности к спринту исполнения

> M5 · кандидат 9 · терминал DAG · зависит от всех вердиктов · secretary

## Вход (ратифицировано — не переоткрывать)

P1 реестр · S1 scope · C1 hybrid SoT · X1 thin catalog · T1 events · F1 chain ·
U1 sync emit · V1 panel owner page.

## **R1 — Что обязательно до первой строки кода спринта и что есть DoD эпика v1?**

Комната фиксирует: минимальный набор deliverables / фаз спринта; порядок вертикальных
срезов; явный out-of-scope; критерий «можно открывать исполнение».

### Кандидаты нарезки (текст)

1. Один вертикальный срез: transport+resolve+emit в code-review → затем consilium → затем office API → затем panel.
2. Слои: (A) registry+defaults+lib transport (B) wire code-review+consilium (C) office overlay+usage API (D) panel page.
3. Panel-first mock → потом агент — нет (нет SoT).
4. Cowork три команды — нет, это заседание→обычный day-sprint.

### Форма вердикта + **список посылок обязателен.**
Запреты: не пересматривать P1…V1; только сборка в план исполнения.
