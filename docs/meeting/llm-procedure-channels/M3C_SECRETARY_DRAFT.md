# Метаданные сеанса

| Поле | Значение |
|------|----------|
| Дата (UTC) | 2026-07-23T05:57:00.000Z |
| Канал | secretary |
| Повестка | `docs/meeting/llm-procedure-channels/AGENDA_M3C.md` |
| Заседание | `llm-procedure-channels` · M3c |

**Вопрос:**

F1 — Что делает процедура при отказе effective-канала?

---

# Консилиум: F1 — fallback / STOP

**Повестка:** Цепочка vs STOP; кто меняет; ollama-молчание. Список посылок обязателен.

**Участники:** Teamlead, Структурщик, Математик, Музыкант, Верстальщик.

---

[Структурщик]: C1 дал один effective channel. F1 — что если он мёртв. Кандидат 4 (тихий ollama) — худший: LGTM от слабой модели без метки. Кандидат 1 (только STOP) честен, но не лечит вечер при лимите Anthropic, если overlay не успели сменить.

[Teamlead]: Нужна **явная** цепочка в defaults/overlay: упорядоченный список provider попыток. Исчерпание → STOP с кодом. Админ меняет цепочку тем же overlay write, что и single channel (расширяем форму: `channel` | `channelChain`). Кандидат 2.

[Математик]: Форма: `effective.chain: ProviderId[]` непустой; try i=0..n-1; emit event per attempt; успех → break; все fail → exit≠0. Single provider = chain длины 1 (совместимо с C1).

[Музыкант]: Кандидат 3 (цепочка только code-review): consilium дороже — тем важнее не жечь кредит впустую на мёртвом anthropic, а сразу openrouter если в chain. Одна политика для обоих id v1. Не 3.

[Верстальщик]: На панели цепочка видна как список, не скрытый magic. Бейдж attempt/provider в usage (T1 events).

[Структурщик]: Ollama: только если явно в chain как provider `ollama`; никогда автоподмешивать. Secretary/hand — вне transport chain (человек), не путать.

[Teamlead]: Default git для code-review v1 предложение существа спринта: `[openrouter, anthropic]` или наоборот — **не фиксируем порядок здесь**, только механизм; порядок = содержимое defaults/overlay (админ/PR defaults).

[Математик]: Инвариант: нет попытки provider ∉ ritual enum (X1).

[Музыкант]: Принимаю 2. Флаг: каждая попытка = событие T1 (ok false) — иначе экономика врёт «один запрос».

[Верстальщик]: Принимаю 2. STOP текст = последний errorClass.

[Структурщик]: Отвергаем тихий ollama и STOP-only как единственную стратегию.

[Teamlead]: Голосуем кандидат 2 + явный chain + emit per attempt?

[Структурщик]: Принимаю.

[Математик]: Принимаю.

[Музыкант]: Принимаю.

[Верстальщик]: Принимаю.

[Teamlead]: Зафиксировано.

[Структурщик]: Совместимость: overlay с одним provider = chain[0].

[Математик]: Пустая chain после merge = builtin-fail (C1).

[Музыкант]: Не маскировать rate_limit под unknown.

[Верстальщик]: UI цепочки — комната 3; контракт данных уже есть.

---

## Итоговое решение консилиума

| Вопрос | Решение |
|--------|---------|
| Политика | **Кандидат 2**: упорядоченная `chain` провайдеров в defaults/overlay; перебор; исчерпание → STOP. |
| Единообразие | Одна политика для `code-review` и `consilium` в v1 (не candidate 3). |
| Ollama | Только если явно в chain; запрет тихого авто. |
| Учёт | Каждая попытка → событие T1. |
| Кто меняет | Тот же write overlay (админ) / git defaults (PR), что C1. |
| Порядок в default | Не фиксирован в F1 — содержимое конфига; механизм обязателен. |
| Отвергнуто | STOP-only без chain (1); тихий ollama (4); разная политика CR vs consilium (3). |

**Definition of Done (только F1):** механизм chain/STOP/emit назван; не UI и не шов ключей.

## Список посылок

- **факт** — C1: effective channel + source; overlay write админом.
- **факт** — 22.07 Anthropic limits; нужен уход на другой API без IDE-only.
- **факт** — S1: оба id v1 — code-review и consilium.
- **факт** — OpenRouter уже доступен через `.env.llm-proxy` / experimental (не ритуал).
- **норма** — Нет тихого снижения качества ревью (ollama без метки).
- **норма** — M3c не проектирует UI панель.

---

*Реплик в диалоге: 21; каждый участник высказался не менее одного раза.*
