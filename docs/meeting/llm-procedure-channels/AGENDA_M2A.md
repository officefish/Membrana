# M2a — повестка: scope v1

> Заседание `llm-procedure-channels` · M2a · кандидат 7  
> Зависит от: M1 (P1). Параллельно с: M2b (control plane).  
> Канал: secretary.

---

## Вход (не переоткрывать)

- P1 ратифицирован: реестр процедур, id kebab, первые примеры `code-review` / `consilium`.
- BRIEF: «интересует code-review и consilium» — как первый скоуп, не обязательно «навсегда».

## **S1 — Какова граница scope v1 контура «процедура → канал»?**

Комната решает: в v1 каркас сразу на все ритуальные LLM-скрипты с двумя живыми
проводами, или реестр+маршруты только для двух id, а остальные — follow-up.
Также: что считается «ритуальной LLM» для будущего расширения (не список id — принцип).

### Кандидаты (просто текст)

1. Только два id в реестре и в панели v1; каркас кода общий, но новые id не обещаем до v2.
2. Каркас (реестр+transport+meters API) рассчитан на N процедур; в v1 meters/routing
   включены только у code-review и consilium; остальные id можно добавить PR без редизайна.
3. В v1 сразу подключаем все скрипты из CREDIT_FALLBACKS / ритуала evening+day, что зовут Anthropic.
4. Scope = «всё с `anthropicPost` в scripts/» автоматически — без ручного реестра жильцов.

### Форма вердикта

- Выбранный вариант границы v1.
- Принцип, кто попадает в следующий PR (не полный список всех будущих id).
- Что сознательно out of v1.
- **Список посылок обязателен.**

### Запреты

- Не решать SoT маршрутов (M2b), телеметрию, UI, fallback.
- Не отменять P1 (реестр остаётся; не возвращать id≡yarn без реестра).
