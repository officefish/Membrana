# Аудит C2

## Раунд 1 — BLOCK

Протокол: `docs/seanses/insight-archive-lifecycle-c2-state-axes-2026-07-18.md`.

Независимый аудитор подтвердил heading, archive non-implications и сохранение immutable
scope, но заблокировал модель:

- общий `D×L×O×V` смешивает оси разных subjects без typed join-context;
- `transcribed` превращён в delivery coverage вместо relation Task→Mandate;
- `reopened` одновременно state и inter-revision event;
- X1–X3 и terminal/monotonic semantics не следуют из посылок;
- outcome overwrite угрожает истории C4;
- источники premises атрибутированы неверно.

Раунд не засчитывается; `ial-c2-state-axes` остаётся active.

## Раунд 5 — SUBSTANCE PASS / FORMAL EXTRACTION BLOCK → PASS

Протокол: `docs/seanses/insight-archive-lifecycle-c2-state-axes-r5-2026-07-19.md`.

- C2R4 однозначно замкнул exact L/O enums и выбрал event-only supersede.
- Председатель механически собрал `C2_VERDICT.md`, исправив source attribution,
  C3–C7 map и footer без новых решений.
- Финальный independent extraction audit: **PASS**.

Узел `ial-c2-state-axes` допускается к архиву как завершённая meeting-фаза.

## Раунд 4 — SUBSTANCE + EXTRACTION BLOCK

Протокол: `docs/seanses/insight-archive-lifecycle-c2-state-axes-r4-2026-07-19.md`.

Канонические D/L/O/V, subjects, visibility, no-inference и projection приняты.
Остались два содержательных решения:

- замкнуть exact L/O enums, явно назвав positive и negative assessments при `None`
  вне enum;
- выбрать одну supersede-модель вместо «assessment или event-link».

После substantive repair нужны механический source/C3–C7 extraction и исправление footer
(24 реплики; Teamlead 5).

Раунд не засчитывается; `ial-c2-state-axes` остаётся active.

## Раунд 3 — SUBSTANCE BLOCK

Протокол: `docs/seanses/insight-archive-lifecycle-c2-state-axes-r3-2026-07-19.md`.

Optional assertions, `deferred`, sources, C6/C7 и reopen без transcription приняты,
но обсуждение само разрушило каноническую типологию:

- D/L/V переименованы в delivery/lifecycle/validation;
- потеряна archive/visibility axis;
- delivery/outcome/decision назначены неверным subjects;
- создана лишняя lifecycle-axis;
- matrix больше не соответствует tuple mandate/slice/representation;
- supersede привязан к generic assertion вместо decision/MANDATE.

Раунд не засчитывается; `ial-c2-state-axes` остаётся active.

## Раунд 2 — SUBSTANCE BLOCK

Протокол: `docs/seanses/insight-archive-lifecycle-c2-state-axes-r2-2026-07-19.md`.

Typed subjects, separate `transcribed`, reopen event и удаление X1–X3 приняты, но
модель заблокирована:

- отсутствие assertion закодировано синтетическими `pending/unassessed` values;
- потерян `deferred`, а `superseded` смешан как value и event;
- archive invariants неполны в обе стороны;
- immutable scope перепутан с изменяемыми assertions;
- sources неверно приписывают C1 отсутствие prerequisites;
- перепутаны C6/C7, reopen автоматически создаёт transcription;
- linked tuple не имеет проверяемых abstract join predicates;
- append-only constraint сформулирован как преждевременная history/storage model.

Раунд не засчитывается; `ial-c2-state-axes` остаётся active.
