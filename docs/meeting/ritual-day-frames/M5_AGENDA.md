# Повестка M5 — приёмка раскладки и миграция на живой `ritual-day`

> Заседание `ritual-day-frames`, фаза M5 (терминал по M0).
> Задание: [`MEETING_BRIEF.md`](./MEETING_BRIEF.md) — прочесть целиком до реплик.
> Предшественники: M0–M4 — посылки этой комнаты.

---

## Вопрос заседания

**A1 — какова приёмка раскладки утреннего ритуала на фреймы и план миграции
на живой `ritual-day`: что считать DoD заседания, что можно объявить в MANIFEST
декларативно до зубьев спринта «фреймы» (#900), что ждёт validateProcedure /
auditPins в main, и какой стоп-критерий «раскладка готова / ещё нет»?**

Фактура: Issues #927–#929 (фазы исполнения контракта) ещё **OPEN**, код зубьев
в `origin/main` отсутствует — это посылка-барьер M0, не сюрприз. Комната
обязана назвать барьер явно, а не притворяться, что зубы уже есть.

---

## Фактура

- **M1** инвентарь: 9 кадров + слот wiring; 5 roots → пины.
- **M2** ранг: `preflight` / `frames` / `post`; wiring ∈ preflight.
- **M3** holders: ozhegov · vesnin · angelina по таблице.
- **M4** граница: кит = файл (SHA); кадр = segment|command; `whole-file` в
  кадре запрещён; `kitVersion` инвариантен к декларациям; `engines[]` =
  `{morning-care}`.
- **Спринт #900**: фазы исполнения контракта (Issues #927 frames-contract,
  #928 segment-pin/auditPins, #929 morning-wiring в ritual-day) — OPEN;
  OPEN-файл day-sprint `procedure-frames-2026-07-22` — чекбоксы приёмки
  не закрыты.
- Живой `docs/procedures/ritual-day/MANIFEST.json` сегодня: без `frames` /
  `preflight` / `post`.

## Что комната должна выдать

- DoD **этого заседания** (раскладка как решение): какие артефакты обязаны
  лежать в контейнере `docs/meeting/ritual-day-frames/` (EPIC, таблицы),
  чтобы холодная сессия читала очередь без раскопок.
- Правило миграции в живой MANIFEST: что писать декларативно **сейчас**
  (если что-то), что **запрещено** до зубьев #927–#929 в main, что пишется
  только исполнением спринта #900 (не этим заседанием).
- Стык с M2-ключами `preflight` / `frames` / `post`: как они входят в
  контракт #900 (потреблять EPIC `procedure-frames`, не пересобирать зубы) —
  достаточно нормы «куда класть» и «что ждёт машины».
- Стоп-критерий: раскладка «закрыта как решение» vs «жива на прогоне утра».
- Явный список follow-up вне заседания (Issues / фазы #900), без открытия
  нового скоупа кода здесь.

## Список посылок — обязателен

Вердикт обязан нести секцию с заголовком ровно **«Список посылок»**: полный список
входов, у каждого пометка **факт** или **норма**. Только входы.
Вердикты M0–M4 — легальные посылки-предшественники.

## Запреты этой комнаты

- Не реализовывать validateProcedure / auditPins / segmentHash — это #900.
- Не пересматривать M1–M4 по существу (только потреблять).
- Не менять `kitVersion` и состав roots кита.
- Не подменять `leadPersona: angelina`.
- Не открывать новый day-sprint и не писать прод-код в этой комнате.
