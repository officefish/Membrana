# Повестка M2 — ранг кадра `morning-wiring` в очереди утра

> Заседание `ritual-day-frames`, фаза M2 (кандидат 3 по M0; ритм после M1).
> Задание: [`MEETING_BRIEF.md`](./MEETING_BRIEF.md) — прочесть целиком до реплик.
> Предшественники: M0 (порядок) · M1 (инвентарь I1) — посылки этой комнаты.

---

## Вопрос заседания

**W1 — какое место занимает уже решённый кадр `morning-wiring` в очереди
фреймов утра: всегда ли он первый, как соотносится «подводка до ритуала» с
шагами внутри `yarn ritual:day`, и куда в описи M1 он встаёт относительно
`morning-hygiene` … `hermes-brief`?**

Не переоткрывать состав трёх дверей (m3) — только ранг в цепочке.

---

## Фактура

- **Вердикт m3 / EPIC `procedure-frames`**: id `morning-wiring`, holder ozhegov,
  три двери (CLAUDE.md · зеркало developer-rhythm · AGENTS.md); аудит —
  инъекция в `auditPins` при старте ritual-day; missing = STOP; ремонт руками
  holder. Состав дверей — **не трогать**.
- **Инвентарь M1** ([`m1-inventory`](../../seanses/ritual-day-frames-m1-inventory-2026-07-22.md)):
  основная линия provisional —
  `morning-hygiene` → `weekly-plan-gate` → `strategy-day` → `daily-standup` →
  `main-day-probe` → `main-day-issue` → `angelina-greeting`;
  пост-секция — `swallow-send`, `hermes-brief` (ручной);
  `morning-wiring` внесён как **зарезервированный слот без ранга**.
- **BRIEF**: «подводка к агенту — фрейм»; дом — `ritual-day` MANIFEST как
  `frames[…]`; не путать с `leadPersona: angelina` процедуры.

## Что комната должна выдать

- Позицию `morning-wiring` в упорядоченной очереди относительно кадров M1
  (индекс / «перед X» / «после Y»).
- Явный вердикт: всегда ли первый; если нет — при каких условиях сдвигается.
- Разведение лемм: «подводка до ритуала» vs «шаг внутри `ritual:day`» —
  одна позиция в `frames[]` или две роли одного id.
- Обновлённый набросок порядка всей очереди (только порядок id; holder’ы и
  pin — не этой комнаты).

## Список посылок — обязателен

Вердикт обязан нести секцию с заголовком ровно **«Список посылок»**: полный список
входов, у каждого пометка **факт** или **норма**. Только входы.
Вердикты M0 и M1 — легальные посылки-предшественники.

## Запреты этой комнаты

- Не пересматривать три двери m3 и holder ozhegov.
- Не назначать holder’ов остальным кадрам — M3.
- Не проводить границу кит↔`frames[].pin` — M4.
- Не писать DoD миграции / #900 — M5.
- Не переименовывать provisional-id из M1 без необходимости для ранга.
