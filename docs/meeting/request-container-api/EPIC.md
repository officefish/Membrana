# ЭПИК заседания `request-container-api` — серверное API для контейнера запросов

> Вход: конспект шторма [`storm-request-container-api-2026-07-23`](../../storm/storm-request-container-api-2026-07-23/REPORT.md)
> (закрыт потолком семи вдохов; развилка названа владельцем — **в заседание**).
> **Порядок M0 РАТИФИЦИРОВАН владельцем 2026-07-23.**

## Что заседание НЕ переоткрывает

Семь тезисов шторма — вход, а не предмет спора: пара «модель+провайдер» выясняется у сервера · агент в процедурах без ключей · вызов есть заявка · ответственность переезжает на сервер · кабинет = зона полномочий · серверную механику держит Фаррелл · контейнер регулирует очередь и считает токен-экономику.

## Цепь комнат (вердикт M0, ратифицирован)

| # | Комната | Место | Зависит от | Питает |
|---|---------|-------|------------|--------|
| **K1** | Контракт кабинета: что перечисляется в зоне полномочий, кто выдаёт, чем проверяется граница, что при выходе за неё | 1-я | — | M2 |
| **M2** | Границы мастерской контейнера запросов: какие глаголы несёт, что Фаррелл делает её глаголами, достройка третьего глагола | 2-я | K1 | A3 |
| **A3** | Второй актор на сервере: где живёт `countersign`, кто подписывает приёмку, раз у клиента ключей нет | 3-я | M2 | E4 |
| **E4** | Токен-экономика: единица счёта, кто читает, чем гейтит, что при исчерпании | 4-я | A3 | финал |
| **B5** | Счёт вдохов на сервере (хвост шторма после потолка) | **параллельно**: старт с K1, финиш до M2 | независим | M2 — только при подтверждённом влиянии |

**Логика цепи:** полномочия задают, что мастерская вправе → мастерская задаёт, чем оперирует актор → актор задаёт, кто отвечает за счёт → счёт закрывает. Вдохи вынесены в параллель: они про формат, а не про сервер.

## Протоколы

| Комната | Протокол | Статус |
|---------|----------|--------|
| M0 — порядок | [`request-container-api-m0-order-2026-07-23.md`](../../seanses/request-container-api-m0-order-2026-07-23.md) | вердикт есть, **ратифицирован владельцем** |
| K1 · M2 · A3 · E4 · B5 | — | не созывались |

## Фактура на входе (замерено)

- Контейнер запросов существует: `docs/audit/llm-calls` (семья `audit-family` атласа), оснащён на **два глагола из трёх** — `inspectElement` отсутствует.
- Слой каналов частично живой: `consilium` резолвит пару через `llm-procedure-resolve`, дефолт — цепочка `anthropic → openrouter`. **Проверено делом:** при лимите Anthropic комната M0 заговорила запасным каналом; лимит перестал быть смертью процедуры.
- Дыра актора не закрыта: полномочия кабинета отличимости субъекта не дают — предмет A3.

## Замечание о ведении (не рушит вердикт)

Гейты S-M1 дважды отказали до прогона: повестка без ID-вопроса, затем повестка без требования списка посылок. Оба отказа по делу — вердикт не встал бы на фундаменте. Первые три попытки прогона шли **кодом с боковой ветки**, где `consilium` не знает слоя каналов и бьётся в лимит насмерть; комната пошла только со свежего дерева.

Гейт `rt-6` предупредил, что метка `O1` не найдена в теле протокола. Сверено глазами: вопрос отвечён по существу под собственным заголовком — «уронено молча» не произошло.
