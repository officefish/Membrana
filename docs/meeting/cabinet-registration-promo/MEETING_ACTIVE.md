# MEETING ACTIVE — `cabinet-registration-promo`

> Протокол **контейнера**, отдельный от протоколов консилиумов.
> Открыто: 2026-09-21 · Ветка: `angelina/docs/meeting-cabinet-registration-20260921` · Worktree: `../Membrana-tooling`
> Председатель: ведущая сессия (Ангелина) · Аудитор: отдельный агент, read-only (S-M5) — не председатель

---

## СТАТУС: ВСЕ КОМНАТЫ РАТИФИЦИРОВАНЫ — M0, M1, M2, M3, M4 приняты владельцем 21.09 (каждая: протокол → аудит → сборка председателя → аудит сборки → слово владельца в чате ведущей); границы всех четырёх сессий расширены по ратификациям (см. «Сессии»); задания A, B, C, D — на ветке `angelina/docs/session-tasks-20260921`, раздаёт владелец; заседание переходит в исполнение, контейнер остаётся открытым до приёмки второго кабинета владельца на `free-v1`

**Путь M0:** прогон 2 (созыв словом владельца 21.09; 57 реплик, 6 ролей по 9–10; первое живое звено — DeepSeek: OpenRouter мёртв по ключу, xAI не отдал, Anthropic до этого дважды обрывался на 158-й секунде — #2374) → аудит прогона 2 (PR #2376): PASS 1/2/4/5/6/7/8, FAIL 3 (посылки цитировали повестку) → сборка председателя (посылки приведены к дословным репликам, решение по существу без изменений) → аудит сборки (PR #2377): ЧИСТО, с поправкой самоописания сборки → ратификация.

**Прогон 1 (27 реплик) отклонён аудитом** (#2370): клиент офиса в несуществующем `apps/cabinet/src/services/`, дверь сервера не отнесена, утечки содержания как «нормы», посылка без реплики, недобор реплик, общий файл. Повестка дополнена уроками; прогон 2 созван по слову владельца.

**Замечание председателя к аудиту прогона 2 (не вердикт):** вердикт даёт сессии A/B/C параллельно и D после C — четыре, а не три; часть посылок-фактов подписана «[Роль] (цитата из фактуры повестки)» — аудитору решить по пункту 3, дословная ли это цитата реплики.

## Задание

См. [`MEETING_BRIEF.md`](MEETING_BRIEF.md). Вход — слово владельца 21.09 (условно открытая регистрация по промокоду панели; промокоды — те же, что у партнёров, новой системы не заводить). Билет #2369.

## Порядок (M0) — ратифицирован владельцем 21.09

Вердикт прогона 2 (сборка `docs/seanses/cabinet-registration-promo-m0-order-r2-assembly-2026-09-21.md`, аудит сборки ЧИСТО):

```text
Лендинг (deploy/root-site/index.html:154) — вне графа, сразу в задание
M1 (К1) грант и дверь офиса
 → M2 (К2) связь и ключ: клиент офиса в сервере кабинета
   → M3 (К3) порядок гашения и режим отказа
     → M4 (К4) форма, фраза ошибки, частота, лендинг
```

Рёбра: 1→2 (форма запроса кабинета к офису определяет, что запрашивать и как предъявлять ключ), 2→3 (клиент офиса — инструмент транзакции гашения), 3→4 (тело двери сервера и правило одной фразы ошибки определяют форму). Не-рёбра: 1→3, 1→4, 2→4, 4→1, 4→2, 4→3.

Сессии (ратифицированы четыре): **A** (К1) — `packages/background-office/src/modules/panel-users/panel-users-core.ts`, `panel-users.controller.ts`, `scripts/_ssh-panel-smoke.mjs`; **расширение по ратификации M1 (владелец, 21.09):** новый файл контроллера внутренней двери в `packages/background-office/src/modules/panel-users/` (имя — за сессией), `panel-users.module.ts`, новые тесты в том же каталоге по образцу `panel-users-core.test.ts`; **B** (К2) — новый модуль клиента офиса в `packages/background-cabinet/src/modules/`, собственный файл-схема пары «адрес + ключ офиса», `deploy/generate-cabinet-env.sh`; **расширение по ратификации M2 (владелец, 21.09):** `packages/background-cabinet/src/config/config.module.ts`, `packages/background-cabinet/src/modules/health-deep/` (модуль, сервис, тесты), `packages/background-cabinet/src/app.module.ts` — только строка подключения нового модуля в `imports`; **C** (К3) — `packages/background-cabinet/src/modules/auth/auth.controller.ts:21`, `auth.service.ts:15`, `config/env.schema.ts:37,47`, тесты `auth`; **расширение по ратификации M3 (владелец, 21.09):** `packages/background-cabinet/src/modules/auth/auth.dto.ts`, `auth.module.ts`, новый файл-помощник ограничителя скользящего окна в том же каталоге (имя — за сессией) с тестом; `env.schema.ts:37,47` — только чтение; **D** (К4, после C) — `apps/cabinet/src/pages/LoginPage.tsx:79`, `apps/cabinet/src/api/auth.ts`, `deploy/root-site/index.html:154`; **расширение по ратификации M4 (владелец, 21.09):** `apps/cabinet/src/context/AuthContext.tsx` (метод `register`), новые `apps/cabinet/src/api/auth.register.test.ts` и `apps/cabinet/src/pages/LoginPage.register.test.tsx`, `apps/cabinet/package.json` и `apps/cabinet/vite.config.ts` — ровно на объявление тестового окружения DOM и его dev-зависимостей. Каждый путь — ровно одной сессии; `env.schema.ts` — только C. Приёмка второго кабинета владельца на `free-v1` (`membrane.service.ts:157`) — сквозная проверка, не сессия.

## Вопросы и статусы

| M | Вопрос | Статус | Протокол |
|---|---|---|---|
| M0 | Порядок кандидатов (DAG) | прогон 1 (21.09, 27 реплик) — **ОТКЛОНЁН аудитом** (PR #2370): FAIL 1/3/4/7/8 — вердикт кладёт клиент офиса и интеграцию в несуществующий `apps/cabinet/src/services/` (браузерное приложение), дверь сервера `auth.controller.ts:21` не отнесена ни к одной сессии, утечки содержания К2/К3/К4 стали посылками-«нормами», посылка П1 без реплики, 27 < 30 реплик, общий файл `config/env.schema.ts` у двух сессий; PASS 2/5/6. Повестка дополнена уроками прогона 1; прогон 2 — по слову владельца | прогон 1: `docs/seanses/cabinet-registration-promo-m0-order-2026-09-21.md`; аудит: `docs/seanses/cabinet-registration-promo-m0-audit-2026-09-21.md` |
| M0 | Порядок кандидатов (DAG) | прогон 2 (57 реплик) → аудит FAIL 3 → сборка → аудит сборки ЧИСТО → **ратифицирован 21.09** | `…-m0-order-r2-2026-09-21.md`, `…-m0-audit-r2-…`, `…-m0-order-r2-assembly-…`, `…-m0-assembly-audit-…` |
| M1 | (К1) Грант и дверь офиса | прогон 1 отработал 21.09 (созыв словом владельца; 49 реплик, 6 ролей по 8–9; модель xai/grok-4.5); вердикт: тот же панельный код с грантом `cabinet-register`; дверь `POST /v1/internal/cabinet/registration-codes/consume` под внутренней охраной (в офисе уже есть `api-token.guard.ts`, заголовок `X-Membrana-Token`), `mode: check | redeem`, отказы 409 с причиной (`not_found` → `revoked` → `expired` → `grant_mismatch` → `exhausted`), Q3 только наружу; атомарность — сериализованная запись состояния панели; аудит-запись `redeem-cabinet-code`; отзыв не откатывает погашенное. Аудит (PR #2378, влит `0a2fe79d`): PASS 1, 2, 4, 5, 6, 7; **FAIL 3** (три посылки из 12 без дословной реплики) и **FAIL 8** (дверь не помещается в `panel-users.controller.ts` — класс под `@Controller('v1/panel')` и `PanelAuthGuard`; нужен новый контроллер и запись в `panel-users.module.ts` — вне границ A по M0). Сборка председателя: решение не изменено, три посылки поставлены на реплики (стр. 58, 62, 66, 70, 85, 93, 95, 124), пункт 8 — развилка владельца при ратификации (расширить границы A на новый контроллер, `panel-users.module.ts` и тесты модуля — рекомендация; либо повторный прогон). Аудит сборки — ЧИСТО (PR #2379). **M1 РАТИФИЦИРОВАН владельцем 21.09** с выходом 1 по пункту 8: границы A расширены (см. «Сессии» выше). Решение — по протоколу, строки 162–192; посылки — по сборке | `docs/seanses/cabinet-registration-promo-m1-grant-door-2026-09-21.md`, `…-m1-audit-2026-09-21.md`, `…-m1-grant-door-assembly-2026-09-21.md`, `…-m1-assembly-audit-2026-09-21.md` |
| M2 | (К2) Связь и ключ | повестка `M2_AGENDA.md` и бриф аудита `AUDIT_M2_BRIEF.md` готовы 21.09 (фактура по стволу `3cc3228f`; ловушка для B: отдельная схема пары никем не читается — `config.module.ts:4,11` грузит только `env.schema.ts`, файл C); прогон 1 отработал 21.09 (созыв словом владельца; 45 реплик, 6 ролей по 6; модель deepseek/deepseek-chat); вердикт: пара `OFFICE_URL` / `OFFICE_API_TOKEN`, обе необязательны (без пары регистрация выключена); схема `config/office-env.schema.ts` → `parseOfficeEnv`, загрузка через `config.module.ts` (комната отдала его B — расширение границ, решать при ратификации); ключ общий `API_INTERNAL_TOKEN` офиса (страж знает один ключ), доставка переносом из root `.env` (план-скрипт по образцу `_sync-office-env-from-root.mjs`); модуль `modules/office-registration/`, `OfficeRegistrationBridgeService`, один выход `officeFetch`, таймаут 5000 мс; поверхность для C — `redeemRegistrationCode(code)` с исходом ok / refused(reason) / office-unavailable / config-invalid (401/403 → config-invalid); живость — `probeOfficeConfig` (check несуществующего кода → 409 not_found) из `health-deep`. Аудит (PR #2380): PASS 1, 2, 3, 4, 5, 7; **FAIL 8** (три файла вне границ B: `config.module.ts` — комната назвала в таблице; `modules/health-deep/` — только в репликах 76/78/128; `app.module.ts` — не называла, а подключение модуля в `imports` неизбежно) и **FAIL 6** (заголовки «Маппинг HTTP → класс», «Definition of Done»). Сборка председателя: решение не изменено, два заголовка переведены с явным указанием, пункт 8 — развилка владельца при ратификации (расширить B на `config.module.ts`, `modules/health-deep/`, строку `imports` в `app.module.ts` — рекомендация; либо повторный прогон). Аудит сборки — ЧИСТО (PR #2381; два уточнения самоописания внесены). **M2 РАТИФИЦИРОВАН владельцем 21.09** с выходом 1 по пункту 8: границы B расширены (см. «Сессии» выше). Решение — по протоколу, строки 140–190, с двумя словарными заголовками по сборке | `docs/seanses/cabinet-registration-promo-m2-link-key-2026-09-21.md`, `…-m2-audit-2026-09-21.md`, `…-m2-link-key-assembly-2026-09-21.md`, `…-m2-assembly-audit-2026-09-21.md` |
| M3 | (К3) Порядок гашения и режим отказа | повестка `M3_AGENDA.md` и бриф аудита `AUDIT_M3_BRIEF.md` готовы 21.09 (фактура по стволу `1c5a0499`; ловушки для C, вшитые в повестку: `auth.dto.ts` и `auth.module.ts` в M0 не названы; два условия выключения — `ALLOW_REGISTRATION` и `office === null`; ограничителя частоты у кабинета нет; тарифный `PromoCode` кабинета — чужой); прогон 1 отработал 21.09 (созыв словом владельца; 42 реплики; модель xai/grok-4.5); вердикт: порядок — выключено (флаг ИЛИ office null → 401) → ограничитель IP 10/600000 мс → 429 → длины → код trim → логин занят (403 без офиса) → хеш → `redeemRegistrationCode` → только при `ok` создать пользователя и сессию; refused/локальный отказ → 403 «Registration was not accepted», reason в лог; office-unavailable/config-invalid → 503 «Please try again later»; полуудача «код сгорел, create упал» → 403 + лог `registration_redeem_orphaned`, un-redeem не строим; DTO `code` обязателен; `auth.module.ts` импортирует модуль M2; расширение границ C названо явно (`auth.dto.ts`, `auth.module.ts`); Аудит (PR #2382): PASS 1, 2, 3, 4, 5, 7, 8; **FAIL 6 формально** (заголовок «Definition of Done»). Сборка председателя: решение не изменено, один заголовок переведён; замечание аудита для ратификации — ограничитель скользящего окна есть только в офисе (`panel-auth-core.ts:237`), в кабинете нет → новый файл-помощник в `modules/auth/` неизбежен (комната ветвь произнесла, стр. 99, файл не назвала); Аудит сборки — ЧИСТО без уточнений (PR #2383). **M3 РАТИФИЦИРОВАН владельцем 21.09** с расширением границ C (`auth.dto.ts`, `auth.module.ts`, помощник ограничителя — см. «Сессии» выше). Решение — по протоколу, строки 177–228, заголовок готовности по сборке | `docs/seanses/cabinet-registration-promo-m3-order-failure-2026-09-21.md`, `…-m3-audit-2026-09-21.md`, `…-m3-order-failure-assembly-2026-09-21.md`, `…-m3-assembly-audit-2026-09-21.md` |
| M4 | (К4) Форма, фраза, частота, лендинг | повестка `M4_AGENDA.md` и бриф аудита `AUDIT_M4_BRIEF.md` готовы 21.09 (фактура по стволу `f7ee1a43`; ловушки для D, вшитые в повестку: `context/AuthContext.tsx` в M0 не назван, а метода `register` в контексте нет; `parseError` в `api/auth.ts` теряет HTTP-статус и отдаёт английскую фразу сервера как есть; маршрутизатора нет; тестов страницы входа нет; лендинг обещает регистрацию — «лендинг — сразу»); прогон 1 отработал 21.09 (созыв словом владельца; 38 реплик; модель xai/grok-4.5); вердикт: та же `LoginPage` с режимом вход/регистрация, поля «Логин», «Пароль», «Код приглашения», без повтора пароля; `registerRequest(login, password, code)` в `api/auth.ts`, ошибки как `AuthApiError` со статусом, фраза выбирается по статусу (401 «Регистрация сейчас закрыта.», 403 «Регистрация не принята.», 429 «Слишком много попыток. Подождите 10 минут.», 503 «Сервис временно недоступен. Попробуйте ещё раз.», сеть, иное); сессия после 201 — через `AuthContext.register` (расширение D названо явно, вместе с двумя файлами тестов); после 429 кнопка заблокирована 600 с; подпись внизу «Учётная запись открывается по коду приглашения. Код выдаёт владелец.»; лендинг сейчас «Кабинет — вход по приглашению», после формы «Кабинет — регистрация по приглашению»; заголовки по-русски. Аудит (PR #2384): PASS 1, 2, 4, 5, 6, 7; **FAIL 3** (четыре посылки из 28: склейка, слово из таблицы, фактура повестки, строка таблицы) и **FAIL 8** (тест формы «disabled после 429», «DOM показывает фразу» неисполним: у `apps/cabinet` нет DOM-окружения тестов и библиотек рендера — нужны `apps/cabinet/package.json` и `vite.config.ts`, вне D, вердикт молчит). Сборка председателя: решение не изменено, четыре посылки поставлены на реплики (стр. 50, 56, 62, 76, 102, 114), пункт 8 — развилка владельца (расширить D на `package.json` и `vite.config.ts` веб-кабинета ровно на тестовое окружение DOM — рекомендация; либо повторный прогон). Аудит сборки — ЧИСТО без уточнений (PR #2385). **M4 РАТИФИЦИРОВАН владельцем 21.09** с выходом 1 по пункту 8: границы D расширены (см. «Сессии» выше). Решение — по протоколу, строки 126–154; посылки — по сборке | `docs/seanses/cabinet-registration-promo-m4-form-landing-2026-09-21.md`, `…-m4-audit-2026-09-21.md`, `…-m4-form-landing-assembly-2026-09-21.md`, `…-m4-assembly-audit-2026-09-21.md` |

## Исполнение (с 22.09) — сессии и разъяснения ведущей

**Раздача 22.09 (владелец, руками):** A — сессия `membrana-3f`, дерево `C:\Users\user190825\practice\Membrana-office-door`, ветка `feat/office-cabinet-register-door` от `e0accf31`; C — сессия `membrana-b3`, дерево `C:\Users\user190825\practice\Membrana-orphans`, ветка `feat/cabinet-register-by-code` от `e0accf31` (дерево прежде держал аудитор S-M5, отпустил 22.09 по слову ведущей, чистое); B — сессия Codex (вне канала сообщений, отчёт через владельца или PR). Ведущая — контрольный модератор: приёмка по факту, слияние по приёмке; ответы на вопросы исполнителей — только в рамках ратифицированных решений и с записью здесь.

**Разъяснения при исполнении** (не новые решения — чтение ратифицированных; каждое с опорой на протокол):

| # | Сессия | Вопрос | Разъяснение | Опора |
|---|---|---|---|---|
| Р1 | A | Писать ли аудит-запись на `mode: "check"` | Нет. Запись `redeem-cabinet-code` пишется только на `redeem` — на успех и на каждый отказ; `check` следа не оставляет. Поля записи — код (или id), mode (всегда `redeem`), результат ok/reason, usedCount после | M1, стр. 180 «Запись `redeem-cabinet-code` (успех и отказ)»; [Математик], стр. 101 «На redeem после прохождения — inc usedCount, persist, audit success»; [Музыкант], стр. 95 (поля). Сверх буквы: по M1 П12 продуктовый путь `check` не зовёт, а по M2 `check` зовёт чекер живости `health-deep` регулярно — запись на `check` вытеснила бы ленту (кап 500, `panel-users-core.ts:53`) |
| Р2 | A | Стор в деградации: `PanelUsersStore.mutate` вернул `null` | Дверь отвечает **503** без тела `{ ok:false, reason }` (это недоступность, не бизнес-отказ из пяти); аудит не пишется; для `check` при деградации — тоже 503 (не отдавать `redeemable` по недостоверному состоянию). Зуб на оба режима | M2, стр. 153: «прочие не-2xx → office-unavailable»; M3, карта двери: `office-unavailable` → 503 «Please try again later», пользователь не создан, код не сгорел |
| Р3 | A | Замок `createStateLock` живёт в экземпляре контроллера — в одном процессе; при двух репликах офиса над одним стором не свяжет | Ограничение принято как есть — это позиция комнаты; записано как **долг к будущему ADR о репликах офиса**, не к M2/M3. В коде не менять | M1, [Архитектор], стр. 93: «Если позже несколько инстансов офиса — отдельный ADR; в M1 фиксируем: один writer-контур state, как нынешняя панель» |
| Р4 | A | Отозванный код без гранта `cabinet-register` даёт `revoked`, а не `grant_mismatch` — так ли задумано | Да, порядок предикатов численно однозначен и ратифицирован; оставить | M1, [Математик], стр. 101: «(1) not_found (2) revoked (3) expired (4) grant_mismatch (5) exhausted (6) else ok» |
| Р5 | A | Тело 409 — ровно `{ ok: false, reason }` без `statusCode`; добавлять ли | Нет, тело ровно по таблице; клиент M2 читает `body.reason`, статус — из HTTP | M1, стр. 176 (строка «Отказы»); M2, стр. 153 («409 { ok: false, reason } → refused») |
| Р6 | C | Ключ ограничителя за обратным прокси: `main.ts` без `trustProxy`, Caddy `reverse_proxy 127.0.0.1:3020` → `req.ip` = 127.0.0.1 у всех, окно 10/600000 стало бы общим | Ключ — адрес **клиента**, каким его видит прокси: последний адрес в `X-Forwarded-For` (тот, что дописал Caddy; первый элемент клиент может подделать), иначе `req.ip`, иначе `unknown`. В границах C (`auth.controller.ts` + помощник), `main.ts` не трогать. Зубы: подделка первого элемента не обходит окно | M3, стр. 191 «ключ = IP»; повестка M3: «адрес клиента за обратным прокси проверить… если нет — вопрос ведущей»; панель офиса берёт первый адрес — её долг, не повторять |

**Ход исполнения:**
- **A — PR #2393 ПРИНЯТ ведущей 22.09** (вершина `a354806f`): прогон ведущей в своём дереве — два новых файла 26/26, пакет офиса 51 файл / 333 теста, `tsc -b` чистый; шесть файлов, все в границах; контракт по коду сверен (маршрут под `ApiTokenGuard`, режим → 400, порядок предикатов, 409 `{ok:false, reason}`, 503 на деградацию, аудит только на redeem — Р1/Р2 исполнены); ревью команды LGTM на `a354806f`, гейт опубликован; размер 703 строки с причиной в теле PR; smoke-шаг 6 живьём не гонялся — двери на проде нет, выкатка — слово владельца. Слияние — сессией A из её дерева после зелёного CI (`pr-ship --merge-only`), по приёмке. **ВЛИТ 22.09 — `ad9abd13`** (первый прогон CI покраснел в чужом пакете `journal-report-views#typecheck` TS7006 — файл в стволе не менялся, соседние PR зелёные; перезапуск упавшей работы — зелёный: класс «гонка порядка сборки под turbo --continue»); карточка дерева `Membrana-office-door` заведена (per-tree, в ствол не едет), `repo:clean` → sprint-open. **A закрыта.** Долг записан: замок двери — в одном процессе; реплики офиса — будущий ADR (Р3).
- **C — ПРИНЯТА ЛОКАЛЬНО 22.09** (вершина `e90228c9`, не запушена по замыслу — ветка красна по typecheck до слияния B: `auth.module.ts` импортирует модуль B): ведущая забрала ветку из дерева C (`git fetch <путь> feat/cabinet-register-by-code`) и прогнала у себя — `vitest run src/modules/auth` 4 файла / 40 тестов, eslint чисто; на стволе 26 красных / 9 зелёных (по отчёту C); файлы в границах (`auth.controller.ts`, `auth.service.ts`, `auth.dto.ts`, `auth.module.ts`, `register-rate-limiter.ts` + тест, `auth.service.test.ts`, `auth.controller.test.ts`); порядок M3, карта двери 401/429/403/503, полуудачи с логами, Р6 исполнена; связь с B — порт `RegistrationCodeRedeemer` + токен, `auth.module.ts` импортирует `OfficeRegistrationModule` и привязывает к `OfficeRegistrationBridgeService` (имена по M2 — сверить с PR B). Дальше: после слияния B — сверка имён с его `index.ts`, typecheck, пуш через крюк, `pr:ship` без слияния, приёмка. **PR #2397 ПРИНЯТ и ВЛИТ 22.09 — `2dbc1ae2`** (вершина `e7454f03`, перебазирована на `03e61bb5`, три коммита; пуш через крюк честно: wire-sync, affected-typecheck 9/9): прогон ведущей на голове — `vitest run src/modules/auth` 40/40, eslint 0, пакет кабинета 533/534 (один упавший — плавающий, см. ниже); имена с B совпали (`import type` от B, порт `RegistrationCodeRedeemer` + `useExisting` на `OfficeRegistrationBridgeService`); CI 3/3; ревью LGTM с меткой на `e7454f03` (первый прогон ревью остался без метки — gh не прочитал голову PR, сетевой сбой; перепрогон дал метку); размер 843 строки с причиной принят (помощник без двери — мёртвый код). Слияние — сессией C из её дерева, подтверждено стволом. Карточка дерева `Membrana-orphans` перевыдана, `repo:clean` → sprint-open. **C закрыта.** Долги ревью (не в PR): рост `Map` ключей ограничителя без чистки по времени; `prune` внутри `count()`.
- **B — ПРИНЯТ ведущей по стволу 22.09** (PR #2396, Codex, влит `03e61bb5`, голова `930535c1`, +444 −5, 14 файлов — все в границах B с расширением M2: модуль `office-registration/` (`index.ts`, `modes.ts`, `office-registration-bridge.service.ts` + тест, `office-registration.module.ts`), `config/office-env.schema.ts` + тест, `config.module.ts`, `health-deep/` (контроллер, модуль, сервис, тест), одна строка импорта и одна строка подключения в `app.module.ts`, комментарии секции офиса в `generate-cabinet-env.sh`; `env.schema.ts` не тронут): контракт по коду — `RegistrationOutcome` из четырёх ветвей ровно по M2, `RegistrationRefusalReason`, `ConfigProbeOutcome`, константы пути и таймаута; прогон ведущей на стволе — 3 файла / 36 тестов зелёные; первый BLOCK ревью (`process.exit` в `parseOfficeEnv`) исправлен до слияния (вызова нет); второй BLOCK (`undici`) — ложный: `undici ^6.21.0` объявлен прямой зависимостью `background-cabinet`, слияние через `REVIEW_GATE_OVERRIDE` с причиной, владелец в курсе. Отчёт Codex — через владельца (канала сообщений нет).
- **Дефект ствола (22.09, не PR):** `packages/background-cabinet/src/modules/journal/selection/chain-rehearsal.test.ts` — плавающий: на одной вершине `e7454f03` 9/9 за 3,4 с и 8/9 за 31 с при холодной сборке (тест у границы своего лимита); в CI зелёный. Билет — санитарный слот.
- **Блокер среды C снят 22.09:** владелец выполнил `yarn install` в `Membrana-orphans` из своей оболочки (ссылки созданы; `cpu-features` — необязательный отказ). Замер сессии A: в её дереве `yarn install` прошёл из сессии — ограничение на ссылки не общее для всех сессий, зависит от того, как запущена сессия.
- **C — блокер среды (22.09):** в `Membrana-orphans` `yarn install` падает на `EPERM symlink/junction` (среда агентов junction не создаёт — то же у ведущей; существующие junction созданы 06.08 из оболочки владельца). Решение: C пишет код и зубы без прогона (вариант 3); прогон её файлов на стволе и на ветке делает ведущая в своём дереве (приём #2313); владельцу вынесена команда `yarn install` из его оболочки. В чужое дерево никто не пишет.
- **B (Codex) — блокер сети (22.09):** в `~/.codex/.env` стояли `HTTP_PROXY/HTTPS_PROXY/NO_PROXY` на мёртвый локальный прокси (ни один порт не слушает, VPN системный) — сняты ведущей с копией `.env.bak-20260922`; второй слой — вход по подписке ChatGPT (`auth.openai.com`, `chatgpt.com/backend-api`) отвечает 403 с текущего выхода VPN (DE/FRA), API OpenAI доступен (401). Дальше — узел VPN или API-ключ, слово владельца. **Уточнение по журналу Codex (`~/.codex/logs_2.sqlite`):** отказы шли на `POST https://api.openai.com/v1/responses` «error sending request» — то есть Codex ходит на API, не на `chatgpt.com`, и второй слой (403) ему не мешает; после полного перезапуска приложения Codex поднялся (слово владельца 22.09). **Внимание 22.09:** на снимке владельца Codex открыл вкладку «Сессия C» и собирался завести дерево и ветку `feat/cabinet-register-by-code` — то же имя, что у принятой C на Claude; ведущая попросила владельца не одобрять и передать Codex задание B (и лендинг из D первым шагом). Решение — за владельцем. **Итог 22.09:** владелец остановил вкладку C у Codex и отдал ему B (влит `03e61bb5`) и D; лендинг — PR #2398 (`9f535e92`, `deploy/root-site/index.html`, +2 −2, тексты буква в букву по M4; CI зелёный, ревью LGTM с меткой, гейт опубликован — ПРИНЯТ ведущей, слияние — Codex из своего дерева `.worktrees/cabinet-register-form`); форма D — в работе у Codex, сливается после C (C влита `2dbc1ae2`).

## Выкатка и сквозная приёмка (слово владельца; порядок подготовлен ведущей 22.09)

Предпосылки: в стволе A (`ad9abd13`), B (`03e61bb5`), C (`2dbc1ae2`); влиты лендинг (#2398) и форма D; ствол зелёный на голове. Выкатка — только по слову владельца; секреты — только руками владельца, в чат и лог не печатать.

1. **Офис (VDS `office.mmbrn.tech`).** Туннель `node scripts/_ssh-office-local-forward.mjs` (держать), смоук доступа `_ssh-office-inventory.mjs`; сборка на хосте `DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0 ./deploy/office-stack.sh build && ./deploy/office-stack.sh up`; `curl -fsS http://127.0.0.1:3000/health`. Затем смоук панели живьём `node scripts/_ssh-panel-smoke.mjs` — шаг 6 сессии A: чеканка кода с грантом `cabinet-register` → без ключа 401 → `check` → `redeem` → повтор 409 `exhausted` → иной `mode` 400 → отзыв кода за собой. Красный шаг 6 = дверь не работает, кабинет не выкатывать.
2. **Кабинет (хост media, `cabinet.membrana.space`).** Владелец вносит в `/etc/membrana/cabinet.env` пару `OFFICE_URL=https://office.mmbrn.tech` и `OFFICE_API_TOKEN=<API_INTERNAL_TOKEN офиса из /etc/membrana/office.env на VDS>` (маркер секции — `generate-cabinet-env.sh:34`), и `ALLOW_REGISTRATION=true` (сегодня `false`; по M3 регистрация включена только при флаге **И** паре). Выкатка `CABINET_IMAGE_TAG=main node scripts/_ssh-cabinet-deploy-image.mjs` (гейты DR0 дерево = origin/main, DR1 CI зелёный на голове; метки отката ставятся до сборки, откат — `_ssh-cabinet-rollback.mjs` предыдущим тегом). Проверка живьём, не по /health: `GET https://cabinet.membrana.space/health/deep` → поле `office_registration: ok` (чекер B бьёт дверь офиса несуществующим кодом и ждёт 409 `not_found`; `config-invalid` = ключ не тот, `office-unavailable` = адрес/сеть).
3. **Лендинг.** После #2398 — `node scripts/_ssh-root-site-setup.mjs`; проверить карточку «Кабинет — вход по приглашению» на `membrana.space` глазами.
4. **Сквозная приёмка (владелец, руками):** в панели чеканит код с грантом `cabinet-register` (1 использование, срок 1 день) → в кабинете переключатель «Создать учётную запись» → логин, пароль, код → 201 и вход → мембрана нового пользователя на `free-v1`, квота прибора 536870912 (`membrane.service.ts:157`) → повтор того же кода → «Регистрация не принята.» → отзыв кода в панели → снова «Регистрация не принята.». Дверь без пары в env → «Регистрация сейчас закрыта.» (401). Всё это — критерий закрытия эпика #2369; контейнер заседания закрывается после него.
5. **Откат:** кабинет — предыдущий тег образа; офис — предыдущий образ `office-stack`; пара в `cabinet.env` остаётся (без неё регистрация выключена по M2/M3, это безопасное состояние).
