# Аудит M4 «Регистрация в кабинете по промокоду панели» — форма, фраза, частота, лендинг

**Кто:** отдельная сессия, только чтение (S-M5: председатель ≠ аудитор). Протокол и повестку не править; своих решений по контракту не предлагать.
**Предмет:** повестка `docs/meeting/cabinet-registration-promo/M4_AGENDA.md` и протокол `docs/seanses/cabinet-registration-promo-m4-*.md` (ветка `angelina/docs/meeting-cabinet-registration-20260921`; вершину назвать в отчёте). Ствол для сверки — `origin/main` (sha назвать).

## Чек-лист (каждый пункт — PASS или FAIL с цитатой или командой)

1. **Один вопрос.** Решался ровно Q4 — место формы, поля, вызов и получение сессии, тексты на каждый статус, частота, подпись внизу, лендинг «сейчас» и «после», зубы D. Контракт двери сервера и его коды (M3), клиент (M2), дверь офиса (M1), выкатка — утечки, не решения; в таблице как решение — FAIL.
2. **Вердикт про контракт, а не описание.** Имя новой функции в `api/auth.ts` и её тело; способ получения сессии после 201 с названием файла; русская фраза для каждого из 201, 401, 403, 429, 503 и сетевого сбоя — буква в букву; выбор фразы по статусу; секунды блокировки после 429 числом; текст карточки лендинга «сейчас» и «после» дословно и адрес ссылки; подпись внизу дословно. «Примерно» — FAIL по пункту.
3. **«Список посылок».** Секция есть; у каждой посылки тип и **дословная** цитата реплики с ролью. Проверка: `grep -cF` по репликам с нормализацией жирного и кавычек → 1, роль по ближайшему маркеру `[Роль]:` выше строки. Посылка без реплики, пересказ или строка таблицы — FAIL по посылке.
4. **Фактура против ствола.** Не менее пяти утверждений по `origin/main` командой `git show origin/main:<путь> | sed -n '<строка>p'`. Особо: `LoginPage.tsx:5–9,16,60,73–80`, `api/auth.ts:36–45,47–54`, `context/AuthContext.tsx:26,48–49`, `deploy/root-site/index.html:153–157`, отсутствие `<Route` в `App.tsx`/`main.tsx`.
5. **Данности и M1–M3 не переоткрыты.** Форма не зовёт `check`, не различает причины 403, не просит сервер новых полей или кодов; ключ офиса в браузер не попадает. `grep -n -iE "check|причин|истёк|исчерпан|OFFICE_API_TOKEN"` — каждое попадание в смысле запрета или фактуры.
6. **Словарь.** Заголовки строк таблицы и разделов вердикта — по-русски (англицизм в заголовке — FAIL, как в M2 и M3); латиница только в именах файлов, функций, полей и статусах.
7. **Реплик не меньше тридцати**, шесть советчиков; счёт `grep -cE "^\[(Архитектор|Верстальщик|Математик|Teamlead|Структурщик|Музыкант)\]"`; число по ролям; «Принимаю» без содержания — отдельно.
8. **Исполнимость в границах D.** Границы D по M0: `apps/cabinet/src/pages/LoginPage.tsx:79`, `apps/cabinet/src/api/auth.ts`, `deploy/root-site/index.html:154`. Проверить каждый файл, который вердикт велит создать или править: `context/AuthContext.tsx`, новая страница или компонент, тесты (`*.test.ts(x)`) — либо в границах, либо вердикт **явно** называет расширение и почему для решения владельца при ратификации; молчание — FAIL (уроки M1–M3).

## Форма отчёта

`docs/seanses/cabinet-registration-promo-m4-audit-<дата>.md`: шапка (аудитор, предмет, вершины), итог **ЧИСТО / ОТКЛОНЕНО по пунктам N**, таблица `| # | Пункт | Вердикт | Цитата / команда |`, раздел «Что аудитор не делает». Реплики не нумеруются — цитировать роль и дословный текст с номером строки протокола. Отчёт — отдельным PR без слияния; слияние — по приёмке председателя.
