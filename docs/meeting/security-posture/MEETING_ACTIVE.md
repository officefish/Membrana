# Заседание `meeting-security-posture` — протокол контейнера

**Задание:** [`MEETING_BRIEF.md`](./MEETING_BRIEF.md)
**Фаза:** ✅ **ЗАСЕДАНИЕ ЗАВЕРШЕНО 19.07.** Все фазы M0–M3 с вердиктами,
ратифицированы владельцем (M0/M1/M2/M3 — словом «ратифицирую» в сессии 19.07,
в хронологическом порядке фаз) и аудированы отдельными S-M5 агентами:
M0 — 6 PASS/1 NA, M1 — 7/7, M2 — 7/7, M3 — 8/8 «заседание закрыто корректно».
Компонента A замкнута; открытыми остаются только четыре апгрейд-карточки
`sec-upgrade-*` (обычные спринты, вне контейнера заседания).
**Вход:** аудит `yarn npm audit` 19.07 (96 advisories) + инцидент patchhog

## Статус фаз

| Фаза | Вопрос | Статус | Протокол |
| --- | --- | --- | --- |
| **M0** | O1 — порядок (DAG) 5 security-кандидатов | ✅ вердикт (run2) + ратифицирован 19.07 + аудит S-M5 6/7 | `security-posture-m0-order-run2-2026-07-19.md` |
| **M1** | A1 — дозор (Dependabot + audit в ритуале + порог severity) | ✅ вердикт + **ратифицирован 19.07** + аудит S-M5 **7/7 PASS** + DoD исполнен | `security-posture-m1-watch-2026-07-19.md` |
| **M2** | B1 — стратегия/нарезка апгрейд-спринта №1b | ✅ вердикт + **ратифицирован 19.07** (в чате, до созыва M3) + аудит S-M5 **7/7 PASS**; карточки `sec-upgrade-*` (4) в реестре | `security-posture-m2-upgrade-2026-07-19.md` |
| **M3** | C1 — единый регламент реагирования (№3 + №4-правило + №5-дисциплина, M-lite) | ✅ вердикт + **ратифицирован 19.07** + DoD исполнен (регламент + карточки) | `security-posture-m3-response-2026-07-19.md` |

## Вердикт M3 (кратко; ратифицирован 19.07, DoD исполнен)

Один документ `docs/security/INCIDENT_RESPONSE_REGULATION.md` (3 секции) и
каталог карточек `docs/security/incidents/` (эталон — patchhog-19.07).

- (а) Входящие: презумпция недостоверности; чек-лист сверки; ЗАПРЕЩЕНО —
  токен-ссылки, Sign in with GitHub на чужих отчётах, установка предлагаемого
  тулинга.
- (б) Доступы: deny-by-default, write — только владелец; таблица активных
  доступов; аккаунт-ревизия квартал и после инцидента.
- (в) Секреты: `.env` per-worktree глазами; ротация первой; baseline gitleaks —
  только с явной санкцией владельца.
- Сквозной принцип: человек-в-контуре перед необратимым. Владельцы: Teamlead —
  процесс/LGTM, Dynin — редактор, владелец проекта — санкции.
- rt-6 «метка C1» — ложный греп-класс #558, вердикт в теле есть, сверено глазами.

## DoD M1 — исполнен 19.07 (после ратификации)

- Dependabot alerts включены (monitoring-only, auto-PR — за M2-вердиктом «не включать»);
- `yarn deps:watch` (`scripts/deps-watch.mjs`): утро — сводка, разрез рантайм/dev,
  корзина «СРАЗУ», снапшот; вечер — diff. Встроен в `ritual:day` /
  `ritual:evening` (мягко, `|| true` — дозор информирует, не роняет ритуал);
- порог/владельцы/каденс — `docs/security/DEPS_WATCH_REGULATION.md`;
- прогон 19.07: 84 advisories, корзина «СРАЗУ» = 28 (почти вся → M2).

## Вердикт M2 (кратко; ратифицирован 19.07)

Четыре отдельные задачи, не монолит: **№1 backend-runtime-major** (Nest 11 +
fastify 5 + middie 9 + fast-uri 3 — неделимый атом, оба сервера синхронно) ∥
**№2 dev-tooling-major** (vite 7 + vitest 3, фазы: foundation → детекторы →
client), мерж поочерёдно (общий lockfile) → **№3 tar 7** (канареечная сборка
desktop, без слепых resolutions) → **№4 electron-major** (последним, ручной
smoke). №1 первым по модели угроз (auth bypass достижим извне). rt-6 «метка B1
не найдена» — ложный греп-класс #558, вердикт в теле есть, сверено глазами.

## Листы без консилиума — исполнены 19.07

| Лист | Результат |
| --- | --- |
| №1a гигиена-патчи | ✅ **96 → 84 advisories** (high 34→27): undici 6.27.0, ws 8.21.1, multer 2.2.0, form-data 4.0.6, fastify 4.29.1, fast-uri 2.4.3 (resolutions + `yarn up -R`) |
| critical рантайма | ⚠️ **патча в мажоре НЕТ**: @fastify/middie фикс только в 9.x (=fastify 5); fastify/fast-uri highs — только в 5.x/3.x. Весь fastify-стек — единый мажор-прыжок → вход M2. Оба critical остаются до M2 |
| ревизия доступов (фаза №4) | ✅ репо-уровень чист: webhooks 0, deploy keys 0, коллаборатор 1 (officefish/admin), 6 ожидаемых Actions-секретов. Аккаунт-уровень (Apps/OAuth/PAT) API не отдаёт — ручная ревизия владельцем: github.com/settings/installations + /settings/applications |
| gitleaks-гейт (фаза №5) | ✅ полный скан истории (1439 коммитов): живых секретов НЕТ (3 фикстуры → `.gitleaksignore`); pre-commit хук (мягкий) + CI `gitleaks.yml` |
| Dependabot alerts | ✅ были выключены (404) → включены 19.07 (204), monitoring-only без auto-PR (посылка 3 и DoD-1 вердикта M1) |

**run1 завернул гейт правила 6** (вердикт без секции посылок) → вещдок
`docs/seanses/rejected/security-posture-m0-order-2026-07-19.md` (НЕ протокол).
run2 — повестка усилена обязательной формой «## Список посылок», прошла.

## Вердикт M0 (единственный — ПОРЯДОК)

```
корни-параллели:  №2 дозор (ФУНДАМЕНТ правил: порог severity + каденс)
                  №3 протокол входящих   ┐
                  №4 политика доступов   ├ ортогональны, рёбер между ними нет
                  №5 секрет-гигиена      ┘
единственное ребро:  №2 → №1b (порог/каденс определяют нарезку апгрейд-спринта)
изолированный лист:  №1a гигиена-патчи — исполнять, не заседать
```

**Расщепление узлов:** №1 = {№1a гигиена-лист, №1b апгрейд-M}; №4 = {ревизия-лист,
правило-M-lite}; №5 = {gitleaks-лист, .env-дисциплина-M-lite}.
**Мнимое ребро №3→№4 разорвано:** инцидент patchhog — общая причина, не зависимость.

**Без консилиума (исполнять до заседаний):**

- №1a гигиена-патчи: tar, multer, ws, undici, form-data, fast-uri;
- critical рантайма: @fastify/middie auth bypass + 6 high middleware-обходов;
- ревизия уже выданных OAuth/GitHub Apps (лист-фаза №4);
- gitleaks в pre-commit/CI (лист-фаза №5).

**Порядок созыва:** M1 = №2 (дозор) → M2 = №1b (апгрейд-спринт) → M3 = регламенты
(№3 + №4-правило + №5-дисциплина, один M-lite).

## Аудит M3 и закрытие (S-M5 соблюдён)

Аудитор M3 — отдельный read-only агент: **«процедура M3 действительна, заседание
закрыто корректно» — 8/8 PASS** (включая построчную сверку DoD-артефактов с
вердиктом: регламент ↔ таблица решения, шаблон + эталонная карточка patchhog).
Замечания аудитора (не влияют на вердикт, приняты председателем):

1. Фиксация ратификаций словом владельца была постфактум и без цитат — исправлено
   в шапке этого файла; на будущие заседания: фиксировать в момент получения.
2. Гигиена контейнера отставала от статусов (⏳ после ратификаций) — исправлено.
3. Формулировка проверки 4 аудитора в MEETING_REGULATION.md («посылки ⊆ закрытые
   вердикты») уже фактической практики (факты/листы/память — законные входы по
   правилу 6) — **кандидат в правку при ратификации регламента консилиумом**.
4. MEETING_REGULATION.md — черновик; аудиты проведены по нему как по действующей
   договорённости.

## Аудит M0 (S-M5 соблюдён)

Аудитор — отдельный read-only агент (созван по ратификации владельца 19.07),
НЕ председатель. Вердикт: **процедура M0 действительна** — 6 PASS, 1 NA
(предшественники: у M0 их нет), 0 FAIL.

- `yarn meeting:audit --id security-posture` — exit 0, «один вопрос: O1»,
  нарушений 0;
- вердикт по O1 в теле есть (rt-6 «метка не найдена» подтверждён ложным —
  греп-класс #558, сверено глазами);
- посылки: 11 входов, маркеров «здесь принято» ноль;
- задание не поехало (BRIEF_AMENDMENT отсутствует, линия бриф→повестка→протокол
  одна);
- дрейфа нет: единственное вхождение чужих узлов — цитата ограды в посылке №9;
- счётчик реплик 25=25 (Teamlead ×6, Ozhegov ×5, Dynin ×5, Kuryokhin ×5,
  Rodchenko ×4).

Примечание аудитора: на момент аудита артефакты были вне git — целостность
машиной не гарантировалась; снято коммитом артефактов сразу после аудита.

## Что M0 НЕ решал (S-M2)

Только порядок. Существо (какие версии ставить, форма порога, текст регламентов) —
за M1..M3 и листами-задачами.

## Блокеры до M1

1. ~~Ратификация DAG владельцем~~ — ✅ ратифицирован 19.07 («ратифицирую»).
2. ~~S-M5 аудит~~ — ✅ процедура действительна (6 PASS / 1 NA / 0 FAIL).

Блокеров нет: листы-задачи «без консилиума» можно исполнять, M1 (дозор) — созывать.

## Блокеры до M2

1. **Ратификация вердикта M1 владельцем** (порог severity, место audit в ритуале,
   каденс — таблица в протоколе M1). После неё: встроить audit-шаг в
   `ritual:day`/`ritual:evening` (DoD M1) и созывать M2.
2. rt-6 на M1: «метка A1 не найдена» — тот же ложный греп-класс #558, вердикт в
   теле есть, сверено глазами председателем.

## Примечания председателя

- rt-6 на run2: «метка O1 не найдена в теле» — известный класс (греп метки, не
  вердикта, #558); секция вердикта есть, отвечает на O1, сверено глазами.
- Реестр: карточку `meeting-security-posture` завести при ратификации M0.
