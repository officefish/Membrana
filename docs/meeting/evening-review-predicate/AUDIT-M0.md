# AUDIT-M0 — подпись аудитора заседания `evening-review-predicate`

**Прогон:** M0 (O1 — порядок зависимостей) · **дата:** 2026-08-01
**Аудитор:** отдельный read-only агент, изолированный подагент сессии; повестку не писал, в
комнате не участвовал, права записи не имел.
**Председатель:** агент Claude Code (основная сессия).
**S-M5:** совпадения ролей нет.

> **Зачем этот файл.** Заверение S-M5 артефактом — решение владельца 01.08 (вариант «файл-подпись
> аудитора»). До него разделение председателя и аудитора опиралось только на разделение сессий и
> ни одним файлом не подтверждалось; аудитор поднял это дважды. Теперь у каждого прогона есть
> подписанный отчёт роли, не смешанный с протоколом контейнера.

## Вывод инструмента

`yarn meeting:audit --id evening-review-predicate` → **нарушений 0, код возврата 0.**
Проверки `[1]`–`[4]`, `[6]`, `[7]` зелёные; `[5]` (артефакты в git) — **НЕЧЕМ**.

## Пять проверок регламента

| # | Проверка | Вердикт | Чем проверено |
|---|---|---|---|
| 1 | Повестка = ровно один ID-вопрос | **PASS** | инструментом, с зубами (`extractAgendaIds` → `O1`); кандидаты в теле простым текстом без жирных ID |
| 2 | Вердикт по вопросу есть в теле | **PASS** | полумашинно + чтением: инструмент подтвердил только наличие секции, существо прочитано аудитором |
| 3 | Порядок соблюдён | **PASS, пустая** | инструментом вхолостую: протокол один, сравнивать не с чем |
| 4 | Посылки — из вердиктов предшественников | **WARN** | структура инструментом, существо чтением — см. находки |
| 5 | Задание не поехало | **НЕЧЕМ** | все артефакты untracked; расхождений `BRIEF` ↔ `ACTIVE` нет, но истории нет |

## Правила 6 и 7

**Правило 6 — PASS (машинно).** Протокол несёт пометку канала `<!-- канал: llm -->`; гейт
`meetingVerdictProblems` отработал **до** записи. Повестка требовала список посылок, список выдан
в том же прогоне (таблица П1–П6). Добора задним числом не производилось.

**Правило 7 — PASS (чтением).** Строка вопроса несёт шесть кандидатов по номерам, запрет вводить
свой словарь и подменять состав, запрет вердиктов по существу, требование посылок. Повестка
доставлена дважды — в голову и в хвост, обе строки «полностью».

## Находки

1. **П6 подозревается лишней.** `OVERSIZED_CHANGED_LINES` = 400 подтверждён фактом кода
   (`scripts/lib/day-work-diff.mjs:25`), но численное содержание П6 ни одного ребра DAG не
   питает: ребро (1)→(6) выведено из того, что мерка есть атрибут предмета ревью. По правилу 5
   лишняя посылка лжёт об отзыве. Комната сама себя проверила и признала П6 использованной —
   аудитор не согласился.
2. **Спрятанная посылка.** Определение «жёсткого ребра» взято из Hard rule 3 регламента
   («Mn не созывается раньше Mn−1»); несущая, в список П1–П6 не внесена.
3. **Посылки П1, П3 подтверждены дословно** по источникам: `workshop-wires-m0-order-2026-07-30.md`
   (предикат ребра) и `evening-auditor-m4-report-form-2026-07-18.md` (строка Lifecycle
   «после `truth.mjs cool` и `code-review`»).

## Итог

**Процедура M0 действительна.** S-M1, S-M2, S-M4, S-M5 не сработали; вердикт по O1 состоялся;
заседание правомерно перешло к ратификации порядка владельцем.

Поправки председателю: внести артефакты в git; разобраться с П6 и спрятанной посылкой **не**
правкой тела протокола; записать флаги прогона в метаданные.
