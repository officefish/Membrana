# AUDIT-M4 — подпись аудитора заседания `evening-review-predicate`

**Прогон:** M4 (R1, вершина (4) — мемоизация в голове вечера против требования 3 из #569)
**Дата:** 2026-08-01 · **Аудитор:** отдельный read-only агент; повестки не писал, в комнате не
участвовал, права записи не имел. **S-M5:** совпадения ролей нет.

## Вывод инструмента

`yarn meeting:audit --id evening-review-predicate` (повесток 5, протоколов 5) → **нарушений 0,
код 0.** `[5]` — НЕЧЕМ по артефактам M4 (untracked); M0–M3 в стволе (PR #1584 смёржен,
squash `d3d1fe15`, за 50 секунд до прогона M4).

## Пять проверок

| # | Проверка | Вердикт | Чем проверено |
|---|---|---|---|
| 1 | повестка = один ID | **PASS** | инструментом (`R1`) |
| 2 | вердикт есть | **PASS** | чтением: (а), (б), (в) закрыты явно, ни одно не умолчанием |
| 3 | порядок | **PASS** | метки: M2 `06:46:46Z` → M4 `07:17:39Z`; ребро (3)→(4) ратифицировано |
| 4 | посылки ⊆ вход | **FAIL** | чтением построчно |
| 5 | задание не поехало | **PASS (задание) / НЕЧЕМ (артефакты M4)** | git: `MEETING_BRIEF.md` внесён `db7f8984` и не менялся, `BRIEF_AMENDMENT.md` нет — **впервые с зубами** |
| — | правило 6 | **PASS** | инструментом `[6]` + чтением |
| — | правило 7 | **PASS с оговоркой** | отпечаток `c696f43ebaa5` сверен с sha256 файла — совпал побайтово |
| — | правило 2 | **FAIL** | чтением |

## Хорошее: рецидива вывода-в-посылках нет

Третий прогон подряд без болезни M1/M2. Все одиннадцать посылок — входы, колонка «Источник»
заполнена честно, решения комнаты (сужение требования 3, шаг `archive-sessions`, мораторий на
усиление записи) в список **не просочились**. Посылки 1, 2, 4, 5, 11 сверены построчно с
источниками и подтверждены дословно, включая факт про `audit-evening.mjs`.

## FAIL по проверке 4 — три спрятанных входа, один ЛОЖНЫЙ

**(а) Ложная несущая посылка «архиватора сессий нет».** Вердикт (б) и DoD 2 целиком стоят на
невысказанном допущении, что органа архивирования сессий не существует. В дереве он есть:

- [`docs/archivarius/README.md`](../../archivarius/README.md) — «Archivarius (#1330, **эпик
  #1229**) — архивариус прожитых сессий Membrana»; контракт `span://<sessionId>/<uuid>`, акт
  изъятия, секретная граница через `scripts/lib/secret-redact.mjs`;
- `scripts/archivarius.mjs` + `scripts/lib/archivarius.mjs` (`ingestJsonlText`, `inspectSession`,
  `auditSpans`, `spanAddress`), команда `yarn archivarius`;
- `scripts/session-scan.mjs` / `scripts/session-extract.mjs` — `yarn sessions:scan` /
  `sessions:extract`, инвентарь и извлечение JSONL-транскриптов дня.

Комната риск **заметила и сознательно не сняла**: «если шаг уже существует под другим именем —
надо проверить» (Музыкант), «его существование или отсутствие выходит за рамки этой комнаты»
(Teamlead). **Норма #1573 председателем для M4 не исполнена** — в отличие от M0, где обзор «что
уже есть» в брифе был. Это дефект председателя.

**(б) Текст Issue #569 в комнату не подавался.** Комната цитировала пересказ из
`.claude/CLAUDE.md`, где **отсутствует целевая клауза первоисточника**: «Цель — исключить
заражение памяти невычитанным/ложным содержимым». Вердикт (а) стоит на аргументе «сужение не
противоречит букве нормы» — буквы на столе не было. Целевая клауза ломает довод «архив сессий —
другое хранилище, норма к нему не применяется»: цель сформулирована по **свойству содержимого**,
а не по имени хранилища. Отягчающее: #569 **открыт**.

**(в) `docs/tasks/evening-ritual-steps.json` в комнату не подавался** — номера шагов взяты из
таблицы органов повестки, то есть из пересказа председателя.

Лишних посылок (правило 5) нет: все одиннадцать питают хотя бы одну строку вердикта.

## FAIL по правилу 2 — занят предмет M5

**Тяжёлое: шаг `archive-sessions` есть вердикт по M5 в чистом виде.** M5 сформулирован в M0 как
«как аудит опирается на память сессий, пока архив не отдаёт отрезок (**#1229**)». M4 предписывает
вставить шаг, который **собирает и пишет в архив сессий** — то есть распорядился наполнением
ровно того хранилища, режим опоры на которое есть предмет вершины (5), не имея на столе ни #1229,
ни существующего Archivarius (а это **тот же тикет**). Ребро (4)→(5) даёт право **питать** M5
посылками, а не **занимать** его предмет.

**WARN: DoD 4 превращает воздержание в норму.** Вердикт (в) говорит «вопрос контура вспоминания
за рамками комнаты», а DoD 4 требует записать в канон мораторий «новые источники записи в журналы
персон не добавляются» — связывающее правило, заранее вычёркивающее часть пространства решений M5.
Противоречие внутри одного протокола.

**Отдельно: DoD 1 предписывает переписать `.claude/CLAUDE.md`** — сузить требование владельца из
**открытого** Issue. Комната сама записала, что отменять требование 3 не вправе; сужение чужой
нормы с записью в канон — то же действие меньшей амплитуды. **Без слова владельца не исполнять.**

## Две нумерации конфликтуют

Фактическое состояние `docs/tasks/evening-ritual-steps.json` на 01.08:

```
6  team-memory-report   noncritical
10 code-review          critical
11 archive-code-review  critical
12 day-memo             noncritical   ← M2 требовал critical
13 audit-evening        noncritical   ← M2 требовал critical
14 deps-watch           noncritical   ← M2 требовал сюда code-review
15 evening-tail         critical      ← M2 требовал сюда archive-code-review
16 deliver-to-main      critical
```

1. **Вердикт M2 в источник не перенесён вовсе** — ни номера, ни критичность. Посылка 2 M4 подана
   как «порядок в исполняемом источнике»: как цитата вердикта M2 верна, как факт о дереве —
   ложна.
2. **DoD M2 дословно неисполним** — 14 и 15 заняты живыми `deps-watch` и `evening-tail`.
3. **M4 смешал две системы координат:** посылка 2 оперирует целевой нумерацией M2 (14/15), вердикт
   (б) и DoD 2 — текущей нумерацией файла («перед шагом 6», «шаг 12 = `day-memo`»). Обе
   одновременно истинными быть не могут.
4. **Вставка не определена операционально:** «перед шагом 6» не говорит, сдвигается ли хвост.

## Контейнер устарел в стволе

`MEETING_ACTIVE.md` уже в git (`7286c6e9`) и уже неверен: шапка утверждает «M4 НЕ созывается»,
статус M4 = `⏸`, ссылки на протокол нет. Регламент требует от контейнера нести текущую фазу,
статус каждого вопроса и ссылки. Ствол лжёт о состоянии заседания сильнее, чем прежде лгало
отсутствие артефактов.

## Итог

**Процедура M4 НЕДЕЙСТВИТЕЛЬНА:** проверка 4 FAIL (ложная несущая посылка про отсутствие
архиватора; не поданы #569 и `evening-ritual-steps.json`) и правило 2 FAIL (`archive-sessions` и
мораторий DoD 4 занимают предмет M5).

**До созыва M5 председатель обязан:** снять стоп-каскад словом владельца и привести
`MEETING_ACTIVE` в соответствие факту; **не исполнять DoD 1 и DoD 2**; снять DoD 4 или явно
передать в M5; развести две нумерации и зафиксировать, что вердикт M2 в источник не перенесён;
закоммитить артефакты M4.
