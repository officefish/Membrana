# Заседание tariff-grid — протокол контейнера

| Поле | Значение |
|---|---|
| id | `tariff-grid` |
| предмет | тарифная сетка + план её интеграции (слово капитана 29.07) |
| вход | шторм `storm-tariff-boundary-dev-2026-07-29` (11 тезисов), конспект мостика 29.07 |
| председатель | сессия мостика (Claude Code) |
| аудитор | **Фаррелл** (`origin: pet`) — назначен владельцем 29.07; read-only, S-M5 соблюдён (не председатель) |
| задание | [`BRIEF.md`](BRIEF.md) — не меняется без `BRIEF_AMENDMENT.md` + LGTM владельца (S-M4) |

## Ход заседания

| Комната | Вопрос | Состояние | Протокол |
|---|---|---|---|
| **M0** | порядок разбора (DAG) | ✅ вердикт вынесен · **РАТИФИЦИРОВАН владельцем 29.07** | [`tariff-grid-m0-order-2026-07-29.md`](../../seanses/tariff-grid-m0-order-2026-07-29.md) |
| **M1** | форма сетки | ✅ вердикт: форма C — versioned контракт `TariffGridDocument` (registry прав + матрица SKU × entitlementId), home истины — сервер, клиент — wire + pure resolve, deny-by-default | [`tariff-grid-m1-form-2026-07-29.md`](../../seanses/tariff-grid-m1-form-2026-07-29.md) |
| **M2** | отношение к рельсу прав сценариев | ✅ вердикт: **проекция** — сетка author, `entitledTariffSkus` выводится из матрицы (catalog-срез), имя поля в wire живёт как носитель проекции; dual-write запрещён | [`tariff-grid-m2-rail-2026-07-29.md`](../../seanses/tariff-grid-m2-rail-2026-07-29.md) |
| **M3** | три исхода вместо двух | ✅ вердикт: третьего enum НЕТ — состояние = `entitled` ∧ `unmetPreconditions≠[]`; условие живёт `preconditionId` в ячейке `gated`, факт считает контур сети (до него stub `stub_unwired`); витрина — soft (притемнение), борд и сервер — hard fail-closed | [`tariff-grid-m3-tri-2026-07-29.md`](../../seanses/tariff-grid-m3-tri-2026-07-29.md) |
| **M4** | две памяти (горячая/холодная) | ✅ вердикт: **две отдельные ячейки** рода quota (cold ← треки, hot ← working-set детекторов); `bufferQuotaBytes` — третья, не сливать; матрица задаёт только limit, occupied — серверное состояние; reserve→commit/release, hard deny при исчерпании, без auto-evict и silent degrade. Основания выведены **заново** — совпадение с записью M1 объявлено независимым | [`tariff-grid-m4-memories-2026-07-29.md`](../../seanses/tariff-grid-m4-memories-2026-07-29.md) |
| **M5** | право производить своё | ✅ вердикт: `produce{enabled, scope?}` — capability, не quota и не call; проверяется на **create/expanding-mutate**, НЕ на существование; при понижении — **retain**: созданное живёт, читается, удаляется владельцем и остаётся вшитым в сценарии, запрещено только новое. «Проверка существования против тарифа» — **запрещена** | [`tariff-grid-m5-produce-2026-07-29.md`](../../seanses/tariff-grid-m5-produce-2026-07-29.md) |
| **M6** | смена тарифа и промокод | ✅ вердикт: меняется **только `Membrane.tariffId`**, снимок прав в мембране запрещён; единственная точка записи `applyTransition(membraneId, target, proof)` + append-only TariffChangeLog; менять вправе admin (любое направление) и redeem промокода (только вверх); промокод = одноразовый объект с `targetTariffId`, atomic check→apply→spend→log, повтор `already_redeemed`; понижение по всем родам — **retain данных**, deny только нового | [`tariff-grid-m6-switch-2026-07-29.md`](../../seanses/tariff-grid-m6-switch-2026-07-29.md) |
| **M7** | зубы сетки | ✅ вердикт: **закрытый реестр зубов** с полями predicate/locus/effect — полнота матрицы, sync проекции, отказ без права, квота, класс памяти на write, produce на create, старший узел на load/clone, unknown id, клиент≠источник истины, запрет снимка прав, единый path смены тарифа, запрет existence-API и silent-evict; эффекты только `reject_op` / `red_ci` / `report_finding`, всегда с `toothId`; **silent запрещён** | [`tariff-grid-m7-teeth-2026-07-29.md`](../../seanses/tariff-grid-m7-teeth-2026-07-29.md) |
| **M8** | план интеграции | ✅ вердикт: шаги **S0→S9** с DoD и сторожевым зубом на каждом; точка cutover — сетка становится единственным источником истины после готовности S0–S8 и зелёных зубов; до неё один adapter «легаси → та же форма провода», двойная запись запрещена; явный список непокрытого | [`tariff-grid-m8-integration-2026-07-29.md`](../../seanses/tariff-grid-m8-integration-2026-07-29.md) |

## Итог заседания

**9 / 9 вердиктов вынесены — заседание состоялось и РАТИФИЦИРОВАНО владельцем 29.07.**
Сводка: [`MEETING_VERDICT.md`](MEETING_VERDICT.md). Каркас тарифа и план S0→S9 — в силе.

## Ратифицированный порядок (вердикт M0, слово владельца 29.07)

```text
1 форма → 2 рельс → 3 три исхода → 4 две памяти → 5 своё → 6 смена тарифа → 7 зубы → 8 интеграция
```

Контрактные рёбра: `F → R,O3,M2,T` · `O3 → P` · `R → P,T,I` · `M2 → C,T` · `P → C` ·
`O3 → C` · `C → T → I`. Обратный ранг запрещён: план интеграции не определяет форму сетки.

## Аудит (Фаррелл, назначен владельцем 29.07)

Отчёт: [`AUDIT_FARRELL.md`](AUDIT_FARRELL.md) — машинный аудит 0 нарушений; **заседание идёт
с оговорками**. Ключевое: M1 вынес два вердикта по чужим комнатам (M4 «два quota id», сужение
выбора M3) — подтверждено исходом M3; протокол M2 несёт ложную посылку «M1 ратифицирован»;
в повестки не доехали ограничение брифа про серию работ и решение владельца об эшелонированной
границе; потеряны пять входов (сид 512 МБ · «плагины физически у пользователя» · «не
клонируются» · нейроузел · долг #1425); все артефакты заседания вне git.

**Поимка председателя (принята):** суждение о ложной тревоге rt-6 на M0 вынес председатель,
письменно признав, что не вправе (S-M5). Вывод верен, роль нарушена — впредь процедурные
вердикты только аудитору.

**Ответы владельца на находки аудита (29.07):**
- *Нейроузел и состав прав* — **содержание набора уточняется отдельно**, вне сегодняшнего контура;
  предмет заседания — общий каркас тарифа. Админский дашборд управления набором — тоже вне контура.
- *Долг #1425 (нет исходящего маршрута у office)* — **сервером занимается отдельный контур
  network**, над ним работает второй агент. Из предмета этого заседания снят.
- *Поправка M4:* комната переспросила вопрос, присвоенный M1, и вывела основания заново.
