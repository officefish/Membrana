# Хвосты санитарии — 18.09.2026 (сессия В)

**Задание:** `docs/prompts/SESSION_V_SANITATION_TAILS_2026-09-18.md` (ветка
`angelina/docs/session-tasks-20260918`), подкрепление магистрали дня. **Ствол на момент
работы:** `bac1180c`. **Дерево:** `Membrana-worktree-cards` (своё). Все команды сноса —
только `repo:clean`, raw `git worktree remove` не применялся.

## Задача 1 — снос освободившихся деревьев

### Шаг 1. Сухой прогон — числа разошлись с карточкой, остановка и доклад

Карточка ждала девять кандидатов: семь `sprint-closed` с 17.09 плюс деревья вчерашних
сессий. Сухой прогон `node scripts/repo-clean.mjs --report …` утром 18.09 дал:

| Класс | 17.09 (после карточек) | 18.09 утром |
|---|---:|---:|
| canon | 5 | 5 |
| sprint-closed | 7 | **5** |
| sprint-open | 48 | 48 |
| unregistered | 5 | 5 |
| деревьев всего | 65 | **63** |

Куда делись два: `.worktrees/buffer-stop-permanent-2204` — каталога нет и записи в git нет;
`.worktrees/session2-tails-20260831` — каталог на диске остался, записи в git нет (мёртвый
каталог, как семь `Membrana-*` из находки 17.09). Кто и когда их снял — в этой сессии не
делалось; по журналу коммитов `docs/evidence/` записи о сносе в стволе нет.

Деревья вчерашних сессий под снос не попали: `Membrana-orphans` занято новой сессией
(ветка `codex/night-hunt-trigger-20260918`, `sprint-open`), `Membrana-sanitation-b` держится
ложным хвостом «PR #2351 MERGED, но работа не доехала» (дефект `unpushedCount`, #2350),
`Membrana-worktree-cards` — текущая сессия (гард инструмента).

Расхождение доложено владельцу вместе со списком пяти; слово владельца 18.09: **«снести все
пять»**. Пятеро:

| Дерево | Ветка | PR |
|---|---|---|
| `Membrana/.worktrees/static-container-meeting-delivery` | `codex/archive-static-registry-cowork` | #1829 MERGED |
| `Membrana-assets-container-recut` | `codex/assets-container-blocker` | #2105 MERGED |
| `Membrana-cw-buffer-integration` | `chore/product-docs-tariffs-from-grid` | #2335 MERGED |
| `Membrana-night-freshness-20260829` | `codex/night-freshness-head-20260829` | #2231 MERGED |
| `Membrana-night-summary-reader-20260830` | `codex/night-summary-reader-20260830` | #2242 MERGED |

### Шаг 2. Исполнение: остановлено инструментом на первом дереве

`node scripts/repo-clean.mjs --execute --worktrees --report …` (локальных мёртвых веток
к удалению прогон не нашёл — 0). Кадры первого дерева
`.worktrees/static-container-meeting-delivery`:

1. снимок живых деревьев — собрался;
2. скан связей наружу — снятых связей **0** (все шесть ссылок `node_modules/@membrana/*`
   вели внутрь того же дерева, в `apps/*`);
3. `git worktree remove` → `--force` — git-запись снята, часть файлов удалена
   (`apps/`, `docs/` и др.);
4. проверка «каталог ушёл» — **каталог остался** (`node_modules`, `packages`, `scripts`,
   `tests`, `tools`, конфиги): Windows не отдал `node_modules`. Инструмент: «Убрать вручную»;
5. **СТОП по ADR-0020: убрано 0, ошибок 1, exit 2.** Пост-чек живых деревьев (кадр 6) не
   выполнялся — до него не дошло.

Проверено после остановки: в оставшемся каталоге шесть ссылок, все ведут внутрь него же и
висят (цели удалены на шаге 3); наружу — ни одной. Ручной снос каталога (снятие ссылок как
ссылок, затем `rmdir /S /Q`) и **повторный запуск самого `repo:clean --execute --worktrees`**
для четырёх оставшихся деревьев в этой сессии выполнить не удалось: оба действия
заблокированы классификатором прав Claude Code как необратимое локальное удаление. Обход
запрета не делался.

### Итог задачи 1

| Дерево | Что произошло |
|---|---|
| `.worktrees/static-container-meeting-delivery` | запись git снята; **каталог на диске** (без `.git`, с висящими внутренними ссылками) — убрать вручную владельцу или инструментом после разрешения прав |
| `Membrana-assets-container-recut` | не тронуто, `sprint-closed`, чистое |
| `Membrana-cw-buffer-integration` | не тронуто, `sprint-closed`, чистое |
| `Membrana-night-freshness-20260829` | не тронуто, `sprint-closed`, чистое |
| `Membrana-night-summary-reader-20260830` | не тронуто, `sprint-closed`, чистое |

Для завершения нужно одно из двух: (а) владелец сам запускает
`yarn repo:clean --execute --worktrees --report <файл>` (четыре прогона, если Windows будет
держать `node_modules` каждого дерева, после каждого — ручной `rmdir` остатка) либо
(б) разрешение на эти команды сессии В правилом прав. Живые деревья после остановки на месте
(см. ниже «Состояние после остановки»).

### Состояние после остановки (проверено на чтение)

- деревьев в `git worktree list`: **62** (было 63); записи `static-container-meeting-delivery` нет, каталог на диске есть;
- главное дерево `Membrana`: 8 незакоммиченных файлов — ровно столько же, сколько до сноса; `apps/` 9 и `packages/` 11 записей — на месте;
- четыре нетронутых кандидата: в git есть, на диске есть, грязи 0;
- локальная ветка снесённого дерева `codex/archive-static-registry-cowork` осталась (до шага удаления веток инструмент не дошёл);
- мёртвые каталоги без git-записи, всего **десять**: семь `Membrana-*` из находки 17.09 (`angelina`, `openrouter`, `procedures-core`, `send-gate`, `tree-hygiene`, `tw-v3-axes`, `tw-v5-validity`) плюс `.worktrees/session2-tails-20260831`, `.worktrees/static-container-meeting-delivery` (сегодняшний) и `.worktrees/strategic-docs`. `repo:clean` их не видит по построению — снос как каталогов, руками владельца.
## Задача 2 — пять «неясных» веток: сами уникальные строки

Метод тот же, что в описи Г (`docs/evidence/orphan-branches-2026-09-17.md`): по каждому файлу,
названному в описи, взяты строки, которые ветка добавила относительно точки расхождения и
которых **нет в текущем файле ствола** `bac1180c`. Всё остальное содержимое этих веток в ствол
вошло другим путём (проверено Г блобом и историей). Копии всех пяти веток лежат на сервере как
`rescue/<ветка>-20260917`, поэтому решение «считать пустой» обратимо. Ничего не удалялось.

| Ветка | Файл | Уникальных строк | Что это | Теряется ли что-то, если считать ветку пустой |
|---|---|---:|---|---|
| `backup/mfcc-rebased-3107` (31.07) | `package.json` | 1 | скрипт `"calibrate:mfcc": "node scripts/calibrate-mfcc-gates.mjs"` | скрипт-цель `scripts/calibrate-mfcc-gates.mjs` в ствол вошёл (Г: блоб в истории), а алиас в `package.json` — нет. Теряется одна строка-алиас; без неё скрипт зовётся `node scripts/…` напрямую. **Решение владельца: нужен ли алиас.** |
| `chore/day-2026-08-15-product` (15.08) | `docs/DAILY_STANDUP.md` | 15 | стендап 15.08 целиком (заголовок, магистраль `secret-parser-built`, риски, пять персон) — сгенерирован `yarn standup@1550bcee` | **ничего**: в архиве дня `docs/archive/daily-day/2026-08-15/DAILY_STANDUP.md` лежит более поздняя генерация того же стендапа (07:56Z, 47 строк); ветка несёт ранний черновик (07:48Z, 42 строки), 10 строк различий — правки между двумя генерациями |
| | `docs/STRATEGY_DAY.md` | 1 | штамп генерации `2026-08-15T07:47:16Z` | ничего: штамп |
| `feat/results-bridge` (19.08) | `docs/tasks/README.md` | 1 | строка каталога задач `plugin-results-bridge` (M) со ссылкой на `PLUGIN_RESULTS_BRIDGE_PROMPT.md` | сам мост (контроллер, DTO, тесты — 631 строка) в ствол вошёл; в реестре задач карточки `plugin-results-bridge` нет — строка каталога без карточки бесполезна. Ничего |
| | `scripts/tasks-decompose.config.json` | 1 | регэксп группировки `"^(server-plugin\|plugin-results-bridge)"` | в стволе группа `server-plugin` без `plugin-results-bridge`; влияет только на раскладку `tasks:decompose`. Ничего существенного |
| `tmp-keep-2107` (24.08) | `docs/network/env.snapshot.json` | 9 | снимок сети 24.08 12:09Z: `generatedAt` + восемь `latencyMs` | ствол хранит более свежий снимок; старые значения — в `history/` |
| | `docs/network/env.snapshot.md` | 2 | два штампа времени того же снимка | ничего |
| | `docs/network/history/2026-08.jsonl` | 2 | две записи истории сети 24.08 (11:58Z и 12:09Z, вердикт «сеть работает», 6 ok / 2 geo_blocked) | в стволе этих двух записей истории нет — **две строки истории сети 24.08 потеряются**; ценность историческая, не рабочая |
| `wip/ritual-31-08` (31.08) | `docs/procedure-runs/trail/2026-08-31.jsonl` | 1 | открывающая запись прогона `ritual-day-2026-08-31` (`started`, 09:31Z) | в трейле ствола за 31.08 (4 записи: deploy-media-vps и ritual-evening) утреннего прогона `ritual-day-2026-08-31` **нет вовсе** — ни открытия, ни закрытия. Ветка несёт только открытие без закрытия; перенос одной открывающей записи сделал бы трейл невалидным (незакрытый прогон). Теряется след, что утро 31.08 запускалось |
| | `docs/tasks/morning-gates-state.json` | 8 | состояние гейта `evening:partner-swallow` вечера 30.08: черновик ласточки, `claimsProbe` = hard по протоколу 30.08, sha `042ad3223429` | состояние гейта — снимок момента, ствол ушёл дальше (в стволе записи о черновике 30.08 нет); теряется след, что ласточка вечера 30.08 прошла пробу претензий «hard» |

Итог для решения: содержательно спорна одна позиция — алиас `calibrate:mfcc` в `package.json`
(скрипт в стволе есть, алиаса нет). Остальное — штампы времени, снимки состояния и строки каталога без
носителя. Удаление любой из пяти обратимо через `rescue/*-20260917`.

## Задача 3 — открытый прогон `ritual-day-2026-09-17-r2`

**Закрывать нечего: прогон уже закрыт, и закрыт писателем.** Проверка по стволу `bac1180c`:

- `docs/procedure-runs/trail/2026-09-17.jsonl` — 6 записей: три `open` (`ritual-day-2026-09-17`,
  `ritual-day-2026-09-17-r2`, `ritual-evening-2026-09-17`) и три `close` (fail · **pass** · fail).
  Закрывающая запись r2: `sequence 2`, `status pass`, `runPhase close`, `2026-09-17T12:09:08Z`.
- `node scripts/procedure-run-journal.mjs check --trail docs/procedure-runs/trail/2026-09-17.jsonl`
  → `ok (6 records)`; `findUnclosedRuns` → пусто.
- Закрывающая запись пришла в ствол коммитом `62bae613` (#2349, 17.09 17:02 MSK). Находка тимлида
  делалась на `af77ed93` (#2348, 15:08 MSK) — тогда в стволе была только открывающая. Находка
  была верна на своей вершине и устарела через два часа.

Дописывать вторую закрывающую запись нельзя: писатель (`closeProcedureRun`) считает
`sequence` от последней записи прогона, и дубль закрытия сделал бы трейл невалидным. Команда
для закрытия у писателя есть (`closeProcedureRun`, CLI `procedure-run-journal.mjs append`), но
применять её не к чему.
