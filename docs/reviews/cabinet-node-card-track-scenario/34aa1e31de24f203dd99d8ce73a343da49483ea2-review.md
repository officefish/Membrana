Tier: T2
Task: cabinet-node-card-track-scenario
Commit: 34aa1e31de24f203dd99d8ce73a343da49483ea2

[Teamlead]: PR #319, ~2 логических коммита (S1 сценарная ячейка, S2 waveform), diff ~8 файлов только в `apps/cabinet` + docs/registry. Scope соответствует prompt/DoD, unrelated/secrets/log artifacts нет. Diff чистый, границы кабинета соблюдены (audio-engine/sample-playback переиспользованы, сырого Web Audio нет). Один потенциальный дефект корректности (revoke object URL при unmount) и ряд P2. P0/P1 не подтверждены — LGTM.

[Структурщик]: Границы целы — `NodeScenarioCell` тянет `ScenarioGroups` из кабинетного `CabinetScenarioPicker`, `computePeakEnvelope`/`SAMPLE_PLAYBACK_WAVEFORM_POINTS`/`loadAudioBuffer` из shared-сервисов без их модификации. csp-5 семантика (`canSelect`, tariff-бейджи, locked-апселл) вынесена в `ScenarioGroups` в одном месте, не дублирована и не сломана. `check:boundaries` pass. Выбор-состояние остаётся в `scenarioList.selectedScenarioId` (`runtime.selectScenario`), «Пуск» не тронут. C1/C3/C4/C7 — ок.

[Математик]: —

[Музыкант]: C2 — декод через `loadAudioBuffer` (audio-engine), моно-канал `getChannelData(0)` → `computePeakEnvelope(..., SAMPLE_PLAYBACK_WAVEFORM_POINTS)`, сырого `AudioContext`/`decodeAudioData` в кабинетном компоненте нет. Загрузка по клику, offline-деградация (`disabled = !deviceLive`, деградация к кнопке) сохранена. Seek/`currentTimeSec`/play синхронизированы через скрытый `<audio>`. Соответствует DoD.

[Верстальщик]: C5 — `NodeScenarioCell`: collapsible через `useState`, `aria-expanded`/`aria-controls`, `role="region"`, `useId()` для стабильного id; выбранный сценарий виден свёрнутым. Порядок node card соответствует канону (кнопки → трек → сценарная ячейка). Waveform compact height=56 как в библиотеке. Мелкое a11y-замечание в P2 (кнопка Play/Пауза меняет `aria-label` от `isPlaying`, но текст кнопки — тоже; дублирование приемлемо).

P0/P1: —

P2:
1. `apps/cabinet/src/components/nodes/NodeLastTrackPreview.tsx` — object URL освобождается только в `useEffect`-cleanup при смене `track/deviceId`, но не при unmount компонента (нет отдельного cleanup-return, revoke идёт внутри `setAudioUrl` при следующем эффекте). Возможна утечка blob URL при размонтировании карточки с загруженным треком.
2. `apps/cabinet/src/components/nodes/NodeScenarioCell.tsx` — `useId().replace(/:/g, '')` костыль; можно использовать id как есть (React `useId` валиден для `id`/`aria-controls`), либо задокументировать причину замены.
3. `apps/cabinet/src/components/nodes/NodeLastTrackPreview.tsx` — при повторном `handleSeek` до загрузки waveform `durationSec<=0` guard есть, но контрол `<audio>` скрыт (`hidden`) без нативных `controls`; убедиться, что нет остаточной зависимости на прежнее поведение `<audio controls>` в smoke.
4. Evidence: ручной smoke на живом узле в DoD помечен «при наличии браузера» — в манифесте отсутствует, browser-check не выполнен (не блокирует).

Checks:
- `git diff --check` — pass (SHA 34aa1e3, 2026-07-09T12:59:07Z, exit 0)
- `yarn turbo run lint typecheck test --filter=@membrana/cabinet && yarn check:boundaries` — pass (SHA 34aa1e3, 2026-07-09T12:59:11Z, exit 0)
- `github-check:optional-review` — skipped
- `github-check:Turbo unit tests` — skipped
- `github-check:Lint, typecheck, test, build` — skipped

Closure readiness: waiting_merge
Verdict: LGTM

Примечание: GitHub metadata показывает PR #319 `remoteState: open` — merge не подтверждён, Issue #318 не закрыт. `finalize` запрещён до merge evidence либо явного `--accepted-branch-only`. Local evidence достаточно (lint/typecheck/test/boundaries pass на reviewed SHA = current SHA).
