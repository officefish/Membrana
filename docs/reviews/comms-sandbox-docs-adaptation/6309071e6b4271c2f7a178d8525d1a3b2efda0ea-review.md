Tier: T0
Task: comms-sandbox-docs-adaptation
Commit: 6309071e6b4271c2f7a178d8525d1a3b2efda0ea

[Teamlead]: Doc-адаптация 4 партнёрских доков + новый индекс `docs/comms/README.md` + cross-link в INSIGHT под реализацию CC1–CC9 (Вариант A, живой канон, переиспользование `tone-guard`). PR среднего размера (9 файлов; 2 из них — удаления и registry-cleanup). Diff соответствует DoD CD1–CD6: топология зафиксирована, канон переведён с копий на живые пути, hook переиспользует `checkTone`, плейсхолдеры Playwright/Replit помечены `[сверить перед запуском]`, Node-версия обоснована по фактам репо (min 20/CI, `.nvmrc` 22). Секретов, `.env`, log-артефактов нет; `git diff --check` чистый. **Одно наблюдение вне DoD (см. ниже).** Нет P0/P1 → LGTM.

[Структурщик]: Границы контура сохранены — доки ссылаются на живой канон через `canon-sources.ts`, инвариант «сток не исток» и запись только в `out/` выдержаны во всех 4 доках; `yarn check:boundaries` pass на текущем SHA. Взаимная сверка (4 сервера, 9 скиллов, канон-пути) внутренне согласована.

[Математик]: Числовая сверка CD5 корректна: Node 22 vs репо-20 разведён явно (min = `engines.node >=20` + CI на 20; локально `.nvmrc` 22), без выдуманных значений; blocked-terms сведён к `GLOSSARY §3` без дублей.

[Музыкант]: —

[Верстальщик]: —

P0/P1: —
P2:
1. Diff удаляет `docs/prompts/PARTNER_TUTORIALS_SPRINT_PROMPT.md`, `docs/seanses/partner-tutorials-design-2026-07-06.md` и вычищает из `docs/tasks/registry.json` эпик `partner-tutorials` + задачи `pt-0..pt-3` — это вне scope CD1–CD6 (unrelated к doc-адаптации песочницы). Не блокирует (P2), но должно быть подтверждено как намеренный откат, а не случайный захват коммита; иначе вынести отдельным изменением с обоснованием.
2. `docs/comms/README.md` ссылается на `BRIEF_COMMS_CONTOUR_TOPOLOGY.md` и `CANON_MINIMUM_FOR_COMMS_CONTOUR.md` — существование этих путей не подтверждено в diff/evidence; сверить наличие или пометить `[TBD]` по правилу DoD «не выдумывать ссылки».

Checks:
- `git diff --check` — pass (exit 0, 6309071, 2026-07-06T05:14:12Z)
- `yarn check:boundaries` — pass (exit 0, 6309071, 2026-07-06T05:14:17Z)
- `github-check:Turbo unit tests` — skipped (6309071)
- `github-check:Comms contour — boundaries, typecheck, lint, test` — skipped (6309071)
- `github-check:Lint, typecheck, test, build` — skipped (6309071)

Closure readiness: waiting_merge
Verdict: LGTM

Примечание: local evidence достаточно (T0, checks на exact SHA, unresolved P0/P1 отсутствуют). `reviewedCommitSha` в манифесте всё ещё `null`, verdict `pending` — при записи immutable artifact зафиксировать SHA. PR #265 `open`, merge не подтверждён → finalize/`merged` запрещён до merge evidence. P2 #1 (unrelated deletions) рекомендуется прояснить до finalize.
