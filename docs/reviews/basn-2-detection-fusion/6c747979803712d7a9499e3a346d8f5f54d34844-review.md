Tier: T2
Task: basn-2-detection-fusion
Commit: 6c747979803712d7a9499e3a346d8f5f54d34844

[Teamlead]: PR #324, ~S задача, но T2 по domain routing (packages/core + device-board, socket-type/scenario-variables contracts, ≥2 packages). Диф связный и в рамках DoD basn-2: core value-тип `DetectionFusion`, SocketType `DetectionAnalysisRef`/`EnsembleAnalysisRef`, union-совместимость входа, node/pins/palette/executor/store и unit-тесты. Unrelated/secret/log артефактов нет; registry+prompt+seance добавлены осмысленно. Одно расхождение title↔реализация (registry говорит "FusionRef", код осознанно даёт value, что соответствует консилиуму т.1) — не блокер, P2. LGTM.

[Структурщик]: C1/C3/C4/C7 — контракты чистые. `DetectionFusion` в VALUE_SOCKET_TYPES, refs в REFERENCE_SOCKET_TYPES; union-исключение `isValidSocketConnection` для `DetectionAnalysisRef` изолировано и документировано (единственное санкционированное). Type guard, variable-value union, applyVariableSetValue расширены консистентно. Границы device-board→core соблюдены (fusion через core, host не заведён — верно по прописи). Сериализация без миграций (новый kind опционален). — ок.

[Математик]: correctness — `fuseDetectorConfidences` вызывается чисто; clamp01 и presentCount trunc корректны. Тестовая матрица покрывает согласие/расхождение/молчащий/все-молчат; combinedScore ожидания (0.875, 0.5, 0.8, 0) согласованы. `emptyDetectionFusionValue` даёт agreement:1 при presentCount:0 — семантически «нет разброса», приемлемо, downstream смотрит на score/present. Арность 2..4 клампится. — ок.

[Музыкант]: — (audio/host-мост в basn-2 не затрагивается; fusion чистый).

[Верстальщик]: — (value/ref визуальное различие — область Rodchenko в basn-1/UI, здесь пины заведены с корректным socketType, a11y вне scope этой карточки).

P0/P1: —
P2:
1. `docs/tasks/registry.json` — title basn-2 упоминает "FusionRef", а реализация (по консилиуму) — value `DetectionFusion` без ref; выровнять формулировку во избежание рассинхрона.
2. `packages/device-board/src/runtime/block-executor.ts` — арность fusion валидируется по `detectionFusionInputCount`, но prompt требует валидацию арности в `isValidConnection`; проверить, что connect-time guard появится в UI-слое (или явно зафиксировать перенос в basn-3/UI).
3. `packages/device-board/src/graph/make-detection-fusion-node.ts` — `makeDetectionFusionSeq` module-level counter: детерминизм id только внутри процесса, для persistence опираться на `options.id` (уже поддержано) — задокументировать.

Checks:
- git diff --check — pass (6c74797, 2026-07-09T15:18:32Z)
- yarn turbo lint/typecheck/test core+device-board && check:boundaries && verify:wire-sync && typecheck client — pass (6c74797, 2026-07-09T15:18:39Z)
- github-check:Lint, typecheck, test, build — skipped
- github-check:optional-review — skipped
- github-check:Turbo unit tests — skipped

Closure readiness: waiting_merge
Verdict: LGTM

Примечание: local evidence достаточно и совпадает с reviewed SHA; P2 не блокируют. GitHub PR #324 remoteState=open, merge не подтверждён — `merged` утверждать нельзя, finalize только после merge evidence либо явного `--accepted-branch-only`.
