# Teamlead closure review — PC-2a (is-window-elapsed, #493, PR #505)

**Ревьюер:** Vesnin (Teamlead) · **Дата:** 2026-07-15 · **Канал:** offline (кредит пуст)
**Вердикт:** ✅ **LGTM** (после фикса, найденного в ревью)

## Найдено и устранено в ревью

| # | Точка | Итог |
|---|-------|------|
| 1 | **Окно не сбрасывалось на scenario-run-start.** `resetProximityHistory()` (де-факто run-start reset) чистил proximity+combined-кэш, но НЕ `windowElapsedStartMs`. Новый ран стартовал бы с протухшей точкой отсчёта → первый гейт мог сработать сразу или не вовремя. | ✅ **ФИКС:** `windowElapsedStartMs.clear()` добавлен в reset + тест (новый ран стартует окна заново). |

## Проверено (реальные точки)

| # | Точка | Вывод |
|---|-------|-------|
| 2 | **Границы: новый узел палитры** | ✅ Санкционировано консилиумом `pc2-periodic-window-gate` (гейт узла пройден). ADR не требуется. |
| 3 | **Backward-compat** | ✅ `is-recording-window-full` не тронут; `windowElapsedMs`/новый вид узла аддитивны; сохранённые графы валидны (device-board 802 зелёный). |
| 4 | **Граница с тиком (риск слить pacing и гейт)** | ✅ Зафиксирована в доке узла + тестах: `onTick`=pacing, `is-window-elapsed`=гейт-потребитель. host-хук поверх существующих часов лупа, новой инфраструктуры времени нет. |
| 5 | **windowMs резолюция** | ✅ провод Integer > поле узла > дефолт (5000); покрыто рантайм-тестом. |
| 6 | **per-node изоляция окон** | ✅ Map<nodeId, startMs>; тест независимости. |
| 7 | **Наглядность (Rodchenko)** | ✅ узел «IsWindowElapsed» без RecorderRef — тест пинов гардит отсутствие рекордера. |
| 8 | **Скоуп-честность** | ⚠️→OK. PC-2 разбит на a (узел, этот PR) и b (пересборка спектр-графа, follow-up). Разбивка обоснована в PR: хирургия деривации отдельным заходом. Узел без потребителя — но это переиспользуемое ядро; спектр-граф ещё тащит рекордер до PC-2b. Явно задокументировано, не скрыто. |

## CI
scoped: core 138 + device-board 802 + client (после фикса — +1 тест). typecheck+test+lint зелёный.

## Вердикт
**LGTM.** Аддитивно, backward-compatible, границы соблюдены, найденный run-start-баг
устранён с тестом. PC-2b (спектр-граф) — обязательный follow-up: пока узел есть, но
целевой сценарий рекордер-часы ещё не потерял.
