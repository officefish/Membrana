```text
Tier: T2
Task: basn-3-branch-on-detection
Commit: 5baa811e7c1bf251984612524c31b7648e665703

[Teamlead]: Чистый exec-ветвитель branch-on-detection: core kind + threshold field, пины, палитра (3 массива синхронно), executor с fail-safe на молчащем входе, полная тестовая матрица. Diff ~10 файлов, S-размер, relevant, без секретов/artifacts. Local evidence зелёное. LGTM.
[Структурщик]: C1/C3/C4/C7 — kind добавлен в core `scenario-node-kind.ts` + `detectionThreshold` в `scenario-graph.ts`; палитра синхронизирована в трёх местах (`palette-node.ts` V04_PALETTE_NODE_KINDS + label + switch, `board-ui.ts` sections, оба теста); device-board→core boundary соблюдён, host не задействован (чистый узел, консилиум т.6). Legacy D0 `branch-on-detection` blockKind корректно разведён с новым nodeKind (комментарий + раздельный диспатч). verify:wire-sync pass.
[Математик]: correctness — порог clamp в [0..1], NaN/undefined→0.5; условие detected = `presentCount > 0 && combinedScore >= threshold`; грань `==` даёт detected (`>=`), presentCount 0 → not-detected. Соответствует DoD basn-3. Тесты покрывают выше/ниже/грань/кастом/пустой/неподключённый.
[Музыкант]: — (host-мост не затронут, аудио-runtime не задействован)
[Верстальщик]: — (UI/inspector-notes вне этого PR; DoD инспектора — эпик-уровень, не блокирует карточку basn-3)

P0/P1: —
P2:
1. `packages/device-board/src/graph/branch-on-detection-node.ts` — module-level `branchOnDetectionSeq` счётчик даёт скрытое состояние на уровне модуля; для генерации id приемлемо, но стоит рассмотреть переиспользуемый id-генератор (совпадает с паттерном других узлов — не регресс).
2. Inspector-notes для узла не входят в этот diff — проверить, что покрыты в рамках эпика перед smoke-gate (DoD эпика, не карточки).

Checks:
- git diff --check — pass (2026-07-09T15:35:25Z, SHA 5baa811)
- yarn turbo lint typecheck test (@membrana/core + @membrana/device-board) && check:boundaries && verify:wire-sync && client typecheck — pass (2026-07-09T15:35:32Z, SHA 5baa811)
- github-check:optional-review — skipped
- github-check:Lint, typecheck, test, build — skipped
- github-check:Turbo unit tests — skipped

Closure readiness: waiting_merge
Verdict: LGTM
```

Примечание: local evidence на exact SHA достаточно для LGTM; GitHub checks помечены skipped (не stale, не fail — не блокируют local review). PR #325 remoteState `open`, merge evidence отсутствует — состояние `merged` не утверждается, finalize запрещён до подтверждения merge.
