```text
Tier: T1
Task: vdr-hg2-mic-validation-plugin
Commit: 91d35800ac0957ccc17f271abca8084860435f8f

[Teamlead]: Рабочая реализация плагина валидации для модуля Микрофон. Diff покрывает регистрацию плагина, UI-панель, аудио-анализ trends, расчёт метрик gate и live-окно 5 с. Размер L оправдан (10 файлов, ~800 строк кода+тестов+docs). Структура следует паттернам проекта (registerPlugin, useSyncExternalStore, createAnalysisFrameFeed). Звук идёт только через audio-engine. Manifest-парсинг и gate-логика (85/80%) соответствуют консилиуму. Тесты vdrMetrics граничных случаев присутствуют. Catalog-запись добавлена. LGTM при условии прохождения Музыканта и Структурщика на audio-routing и C1/C3.

[Структурщик]: ✓ C1 (module boundary): плагин регистрируется через MembranaRegistry.registerPlugin('microphone', ...) в registerClientModules.ts, импорт в MicrophoneModule.tsx условный (activeIds.includes). ✓ C3 (package scope): все файлы внутри apps/client; зависимости на @membrana/* (audio-engine, trends-detector, template-match-detector) валидны. Catalog-запись в registry.json добавлена с parentModuleId='microphone'. Нет пересечений с соседними плагинами. —

[Музыкант]: ✓ Audio C2: analyzeVdrFile() → loadAudioBuffer (audio-engine) + getMonoChannel + analyzeVdrPcm. analyzeVdrPcm вызывает collectMetricSamples + classifyTrends с DRONE_TIGHT шаблонами (паритет с benchmark-харнессом). Live-окно: createAnalysisFrameFeed с bufferSize=2048, smoothing=0.5, окно 5 с на микрофоне (handleFrame собирает samples в Float32Array). MIN_RMS=0.02 совпадает с harness. Нет Web Audio в плагине (только audio-engine-service). ✓ Trends-логика использует getDroneTightEnabledTemplateKeys(), не подмешивает пользовательские шаблоны (паритет со скриптом). —

[Верстальщик]: — (плагин UI на DaisyUI: card, badge, file-input, progress; кнопки, таблица pred-vs-truth; aria-label и aria-live расставлены; нет явных нарушений a11y; детальная review отложена на UI-специалиста если требуется).

P0/P1: —

P2:
- `vdrValidationPanel.tsx`: условие `if (!pluginActive) return null;` проверяет через useMembranaStore, но плагин по умолчанию `active: false` (создан неактивным); пользователь должен активировать вручную (ожидаемо, но документировать в catalog).
- `vdrMetrics.test.ts`: граничный случай «все предсказания not-drone» возвращает `precision: null`, что правильно (TP+FP=0), но UI-отображение `percent(null)` → '—' может быть неочевидно оператору без объяснения в тултипе.
- `analyzeVdrAudio.ts`: `sampleIdFromFileName()` просто удаляет `.wav$`; если имя не содержит расширения или содержит несколько точек, может дать неожиданный id (нет валидации имён). Низкий приоритет если manifest контролирует истину.
- Catalog-запись `vdr-validation.md` статус `draft` — корректно, но no `services` field в registry.json для плагина (в отличие от других); можно добавить для полноты.

Checks:
- `git diff --check` — pass (exit 0, 2026-07-03T09:55:55Z)
- `github-check:verify` — pass (exit null, success, 2026-07-03T09:54:37Z)
- `github-check:optional-review` — skipped (2026-07-03T09:49:21Z)

Closure readiness: waiting_merge
Verdict: LGTM
```
