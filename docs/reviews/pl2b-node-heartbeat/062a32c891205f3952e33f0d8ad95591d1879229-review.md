Tier: T2
Task: pl2b-node-heartbeat
Commit: 062a32c891205f3952e33f0d8ad95591d1879229

[Teamlead]: Повторный closure-review exact squash SHA после merge PR #263. Предыдущий BLOCK был только по evidence-gap вокруг Prisma schema. Evidence снят на этом же SHA: `packages/background-cabinet/prisma/schema.prisma` содержит `Node.membraneId`, связь `Device.node`, `Device.mediaDeviceId @unique`, `Device.pairingStatus @default(paired)` и `Device.lastSeenAt`; `yarn workspace @membrana/background-cabinet prisma validate --schema prisma/schema.prisma` с локальным `DATABASE_URL` завершился успешно. Значит `recentDeviceIdsForMembrane` schema-зависимость подтверждена. DoD PL2b выполнен: клиент отправляет heartbeat при open и каждые 120с; gateway не принимает spoofed deviceId; сервис пишет `Device.lastSeenAt` серверным временем; cabinet bootstrap добавляет recent paired devices в окно 5 минут. PR #263 merged, diff сфокусирован, P0/P1 нет. LGTM.

[Структурщик]: C1/C3/C4/C7 соблюдены. Контракт heartbeat добавлен в core и реэкспортирован. Client импортирует интервал из `@membrana/core`. Background-cabinet использует локальный wire-domain mirror — это существующий паттерн проекта; риск дрейфа оставлен как P2. DI `PrismaService` в `NodeRealtimeService` и async `registerCabinet` покрыты тестами.

[Математик]: Интервал 120000мс и recent window 300000мс соответствуют prompt. Timestamp валидируется, но источником истины является серверное время `new Date()`. `new Set([...live, ...recent])` корректно дедуплицирует snapshot.

[Музыкант]: —

[Верстальщик]: —

P0/P1: —

P2:
1. Дублирование `PresenceHeartbeatPayload` / `parsePresenceHeartbeatPayload` / `NODE_RECENT_PRESENCE_WINDOW_MS` между core и `background-cabinet/src/domain/node-realtime-wire.ts`; стоит унифицировать источник контракта, если package boundary позволит.
2. `recordPresenceHeartbeat` использует `updateMany` и не логирует `count === 0`; приемлемо для heartbeat, но полезно для диагностики.

Checks:
- git diff --check — pass (062a32c, 2026-07-06T05:43:55Z)
- github-check:Lint, typecheck, test, build — pass (062a32c, 2026-07-06T05:34:52Z)
- github-check:Turbo unit tests — pass (062a32c, 2026-07-06T05:31:26Z)
- github-check:Package Membrana Studio (Windows nsis) — pass (062a32c)
- github-check:Decide if studio affected — pass (062a32c)
- github-check:Build and push cabinet images — pass (062a32c)
- schema evidence: `packages/background-cabinet/prisma/schema.prisma` lines 84-108 confirm `Node.membraneId`, `Device.node`, `mediaDeviceId @unique`, `pairingStatus @default(paired)`, `lastSeenAt`
- `DATABASE_URL=postgresql://user:pass@localhost:5432/membrana yarn workspace @membrana/background-cabinet prisma validate --schema prisma/schema.prisma` — pass

Closure readiness: ready
Verdict: LGTM
