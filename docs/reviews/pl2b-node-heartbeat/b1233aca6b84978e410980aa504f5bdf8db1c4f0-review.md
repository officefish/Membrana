Tier: T2
Task: pl2b-node-heartbeat
Commit: b1233aca6b84978e410980aa504f5bdf8db1c4f0

[Teamlead]: PL2b heartbeat узла: клиент шлёт `presence.heartbeat` каждые 120с, сервер валидирует deviceId против аутентифицированного сокета и обновляет `Device.lastSeenAt` серверным временем; bootstrap-снапшот дополняется recent `lastSeenAt`-устройствами. PR среднего размера (11 файлов, ~сфокусировано на DoD, тесты покрывают клиент/gateway/service/contract). Diff чистый, без secrets/log/unrelated. Один потенциальный P1 по несоответствию prompt vs implementation — см. ниже. **BLOCK.**

[Структурщик]: C1 границы соблюдены — константы/типы/parser в `@membrana/core`, wire-дубликат в `background-cabinet/domain`, реэкспорт из `index.ts` корректен. C3: `registerCabinet` стал async, все call-sites (gateway с `await`) и тесты обновлены. C4: DI `PrismaService` в конструктор сервиса — раньше конструктора не было, все тесты через `buildService`. Дублирование `NODE_RECENT_PRESENCE_WINDOW_MS` и `parsePresenceHeartbeatPayload` в core и в background-cabinet/domain — уже существующий паттерн для wire, приемлемо (P2).

[Математик]: correctness heartbeat/window — интервал 120_000, окно 300_000 согласованы с prompt. Валидация timestamp (finite, ≥0) корректна. Дедуп `[...new Set([...live, ...recent])]` верен. Серверное время как источник истины — правильно.

[Музыкант]: —

[Верстальщик]: —

P0/P1:
1. **P1 — DoD-несоответствие: prompt PL2b требует «Prisma-фильтр `pairingStatus: 'paired'`», но `Device.pairingStatus` вводится в PL2 (`pl2-device-pairingstatus`), которого нет в `dependsOn` этой задачи (`dependsOn: ["pl1-presence-snapshot"]`).** `recentDeviceIdsForMembrane` (`node-realtime.service.ts`) и тест (`node-realtime.service.test.ts`) полагаются на `where.pairingStatus: 'paired'`. Если поле/миграция ещё не в схеме на этом SHA — рантайм-запрос упадёт (заглушается `catch → []`, маскируя баг), либо миграция вносится вне scope этой S-задачи. Требуется evidence, что `pairingStatus` уже существует в Prisma-схеме на reviewed SHA, либо убрать зависимость от него в PL2b. Fail-closed до подтверждения.

P2:
1. Двойной guard `if (socket.readyState !== socket.OPEN) return;` в `sendPresenceSnapshot` — первый до `await`, второй после; первый лишний (можно оставить только пост-await, т.к. состояние могло измениться за время запроса). `node-realtime.service.ts`.
2. Дублирование `NODE_RECENT_PRESENCE_WINDOW_MS` / `parsePresenceHeartbeatPayload` между `packages/core` и `background-cabinet/domain/node-realtime-wire.ts` — риск дрейфа; рассмотреть единый импорт из core.
3. `catch { return []; }` в `recentDeviceIdsForMembrane` глушит ошибку молча — стоит логировать (потенциально маскирует P1 выше).

Checks:
- git diff --check — pass (b1233ac, 2026-07-05T18:44:47Z)
- github-check:Turbo unit tests — pass (b1233ac)
- github-check:Lint, typecheck, test, build — pass (b1233ac)
- github-check:Decide if studio affected — pass (b1233ac)
- github-check:Package Membrana Studio (Windows nsis) — pass (b1233ac)
- github-check:optional-review — skipped (non-blocking)

Closure readiness: needs_fix
Verdict: BLOCK
