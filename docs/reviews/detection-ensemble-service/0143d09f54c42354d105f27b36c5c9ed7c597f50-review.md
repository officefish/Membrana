Tier: T2
Task: detection-ensemble-service
Commit: 0143d09f54c42354d105f27b36c5c9ed7c597f50

[Teamlead]: Новый analyzer-сервис `@membrana/detection-ensemble-service` (чистое `fuseDetectorResults` + `EnsembleProducer` с EMA) плюс client-плагин-продюсер `mic-combined-detection`, питающий `combinedScore` в `mic-proximity-alarm`. PR средний (~30 файлов, из них значимая доля — новый пакет + тесты + docs/registry). Scope соответствует Задаче 1 магистрали; unrelated/secrets/log-артефактов в diff нет. Границы соблюдены: сервис зависит только от `@membrana/core` + `@membrana/detector-base`, детекторы инъецируются через контракт `DroneDetector`, слияние считает ядро (без бинарного OR). Correctness/security/architecture претензий уровня P0/P1 нет. LGTM.

[Структурщик]: C1/C4 — границы пакета чистые: `package.json`/`tsconfig.json` references только core + detectors/base; сервис не импортирует другие analyzer/detector-сервисы. Wiring консистентен: alias в `vite.config.ts` + `tsconfig.app.json`, `references`, `yarn.lock`, строка в `packages/services/README.md`, catalog registry. Плагин-продюсер отделён от потребителя (`mic-proximity-alarm` читает shared-store). `check:boundaries` и `verify:wire-sync` — pass. P2: `createCombinedStreamDetectors.ts` импортирует конкретные detector-сервисы (harmonic/cepstral/spectral-flux) — но это client-слой (плагин), не сам сервис, поэтому запрет не нарушен.

[Математик]: `fuseDetectorResults` корректно маппит `DetectorSnapshot`→`FusionSourceInput` (null→present:false, confidence 0). EMA `α·score+(1−α)·ema_prev` корректна; `presentCount===0` не двигает EMA; `normalizeSmoothing` клампит NaN/вне-диапазона→1. Тесты покрывают согласие/расхождение/молчащий/веса/сглаживание/устойчивость к throw + reset + пустой набор — совпадают с DoD-матрицей. Веса из snapshot имеют приоритет над options — задокументировано в types.

[Музыкант]: C2 — alarm-loop переведён с сырой громкости на `combinedScore` через `combinedDetectionState` (`live→smoothedScore`, иначе 0). Аудио только через engine (LiveSampler поверх shared MediaStream), прямого Web Audio нет. Rolling-окна с guard `analyzing` от наложения, `disposed`/teardown корректны, детекторный throw поглощается. Headless-тест `combinedDetection.test.ts` проверяет реальный DSP-путь (presentCount 3, score∈[0,1]).

[Верстальщик]: C5 — `MicCombinedDetectionPanel` использует `role="status" aria-live="polite"`, DaisyUI-классы, состояния «поток остановлен / live / молчит». Индикатор опционален по промпту, реализован аккуратно. Замечаний нет.

P0/P1: —
P2:
1. `apps/client/src/plugins/mic-combined-detection/createCombinedStreamDetectors.ts` — жёсткая связка на три конкретных detector-сервиса в client-слое; при добавлении yamnet стоит вынести формирование набора в конфигурируемую фабрику.
2. DoD-пункт «`combinedScore > 0` на живом входе (demo)» подтверждён только headless-тестом на синтетическом окне; браузерный smoke явно out of scope — ручная проверка микрофоном не в evidence.

Checks:
- `git diff --check` — pass (SHA 0143d09, 2026-07-09T12:11:33Z, exit 0)
- `yarn turbo run lint typecheck test --filter=@membrana/detection-ensemble-service && yarn check:boundaries && yarn verify:wire-sync && yarn turbo run typecheck --filter=@membrana/client` — pass (SHA 0143d09, 2026-07-09T12:11:40Z, exit 0)
- `github-check:Lint, typecheck, test, build` — skipped (не blocker для local review)
- `github-check:Decide if studio affected` — skipped

Closure readiness: waiting_merge
Verdict: LGTM

Примечание: LGTM привязан строго к SHA 0143d09f54c42354d105f27b36c5c9ed7c597f50. GitHub PR #317 — `open`, merge не подтверждён; `finalize`/archive/Issue-closure не запускать до merge evidence. Issue в registry = null — при появлении gh проставить номер.
