Tier: T2
Task: detection-ensemble-service
Commit: a416def120f161dc1ee1d46872ecf7b63d45883d

[Teamlead]: Новый analyzer-сервис `@membrana/detection-ensemble-service` + интеграция combined-продюсера в модуль «Микрофон». PR средний (~30 файлов, но большинство — новый пакет и новый client-плагин, unrelated/секретов нет). Correctness/architecture чистые, границы соблюдены, тесты покрывают DoD. Мешает закрытию только evidence-gap: обязательные `check:boundaries`, `verify:wire-sync` и полный `turbo lint typecheck test` из DoD отсутствуют в manifest, а GitHub-checks помечены skipped. Verdict — BLOCK по недостатку обязательного evidence, не по коду.

[Структурщик]: C1/C3/C4/C7 — границы пакета корректны: `dependencies` только `@membrana/core` + `@membrana/detector-base`; `service.ts` не импортирует другие analyzer/detector-сервисы (детекторы через контракт `DroneDetector`). Client-адаптер `createCombinedStreamDetectors.ts` импортирует конкретные detector-сервисы — но это client-слой, не сам сервис, что допустимо. tsconfig references (core + detectors/base), alias в vite/tsconfig, registry/README-строки согласованы. Замечание: DoD требует `verify:wire-sync` зелёным — evidence нет.

[Математик]: `fuseDetectorResults` корректно маппит `DetectorSnapshot`→`FusionSourceInput`, молчащий (`result===null`)→`present:false, confidence 0`. EMA-логика верна: α∈[0..1] с нормализацией NaN/out-of-range→1; `presentCount===0` не двигает EMA (держит прошлое или 0). Тесты покрывают согласие/расхождение/молчащий/веса/сглаживание/устойчивость к throw + reset + пустой ансамбль. Слияние не переизобретено — считает ядро. Math correctness — OK.

[Музыкант]: C2 — alarm-loop в `micProximityAlarmPlugin.ts` теперь берёт `combinedScore` из `combinedDetectionState.getSnapshot()` (smoothedScore при live, иначе 0), а не из громкости — DoD выполнен. Аудио только через `LiveSampler`/engine, прямого Web Audio нет. Rolling-окна с guard `analyzing`/`disposed`, teardown сбрасывает sampler+collector+producer+state. Живой `combinedScore > 0` заявлен через синтетический headless-тест (реальный DSP-путь, presentCount=3), ручной smoke микрофоном в evidence не подтверждён.

[Верстальщик]: C5 — `MicCombinedDetectionPanel` имеет `role="status" aria-live="polite"`, DaisyUI-классы, состояния live/stopped/молчит обработаны. UI-индикатор опционален по промпту; выполнен корректно, блокеров a11y нет.

P0/P1:
1. P1 — Отсутствует обязательное DoD-evidence: нет сохранённых результатов `yarn check:boundaries` и `yarn verify:wire-sync` (явное требование DoD и раздела «Проверка после PR»). Manifest их не содержит.
2. P1 — Нет evidence полного `yarn turbo run lint typecheck test --filter=@membrana/detection-ensemble-service`; выполнен только `workspace ... test`. lint/typecheck и «регрессов в core/fft-analyzer/client нет» не подтверждены.

P2:
1. `docs/tasks/README.md` — новая строка использует путь `../docs/prompts/...` (двойной `docs/`), тогда как другие ссылки в таблице ведут на `../docs/prompts/...` тоже — проверить корректность относительного пути к `docs/prompts/DETECTION_ENSEMBLE_SERVICE_PROMPT.md`.
2. `createCombinedStreamDetectors.ts` — веса детекторов не задаются, все DSP идут равным голосом; при будущем добавлении yamnet предусмотреть weight через `EnsembleProducerOptions.weights` (сейчас no-op, ремарка на будущее).
3. registry title всё ещё «trends+yamnet», хотя live-набор — DSP (harmonic/cepstral/spectral-flux); yamnet отложен. Уточнить формулировку notes/title.

Checks:
- `git diff --check` — pass (SHA совпадает)
- `yarn workspace @membrana/detection-ensemble-service test` — pass (SHA совпадает)
- `yarn check:boundaries` — missing (обязателен по DoD)
- `yarn verify:wire-sync` — missing (обязателен по DoD)
- `yarn turbo run lint typecheck test --filter=...` — missing (только test выполнен)
- `github-check:Lint, typecheck, test, build` — skipped
- `github-check:Turbo unit tests` — skipped
- `github-check:Decide if studio affected` — skipped
- `github-check:optional-review` — skipped

Closure readiness: needs_fix
Verdict: BLOCK
