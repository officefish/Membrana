```text
Tier: T1
Task: pcb-5-force-open-viewonly-board
Commit: 5f15c0d0f70fca0b4ed8d4373e23fd493f2ec239

[Teamlead]: 
  PCB5 модуль force-open device-board при захвате. Диапазон маленький (3 файла, 
  ~130 строк чистого кода), фокусированно на board.capture → selectedModuleId + флаг 
  isViewOnlyFromCapture. Логика простая, идемпотентна, юнит-тесты покрывают 4 сценария 
  (переключение/no-nav/release). Нет cross-package границ, secrets, env-артефактов. 
  Инвариант §3.3 (emergency stop доступен) явно названисан в комментах, не нарушен. 
  LGTM.

[Структурщик]: 
  — (single package apps/client, нет миграций, нет границ пакетов).

[Математик]: 
  — (нет math/thresholds).

[Музыкант]: 
  — (нет audio/Web Audio, no runtime stream).

[Верстальщик]: 
  — (нет UI/DaisyUI/a11y изменений в диффе).

P0/P1: —

P2: 
  • apps/client/src/lib/boardLeaseBridge.ts:127 — комментарий «[capture] force-open 
    device-board (view-only)» хорош, но сама лог-строка в продакшене может быть 
    слегка многословна; если логи монитормт, рассмотреть сокращение (например, 
    «[pcb5] capture → device-board readonly»).

Checks: 
  • git diff --check — pass (5f15c0d).
  • yarn workspace @membrana/client test --run boardLeaseBridge — pass (5f15c0d).
  • github-check:Decide if studio affected — pass (5f15c0d).
  • github-check:Package Membrana Studio (Windows nsis) — skipped.
  • github-check:optional-review — skipped.
  • github-check:Lint, typecheck, test, build — skipped.
  • github-check:Turbo unit tests — skipped.

Closure readiness: waiting_merge
Verdict: LGTM
```
