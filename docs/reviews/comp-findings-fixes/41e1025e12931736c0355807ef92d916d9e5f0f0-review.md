Tier: T2
Task: comp-findings-fixes
Commit: 41e1025e12931736c0355807ef92d916d9e5f0f0

[Teamlead]: PR средний (~10 файлов: 2 runtime-исходника + 3 test-файла + docs/registry, 2 CONCEPT-архива). Три решения консилиума реализованы согласованно с DoD: kind в invalid-ref (т.1), fusion→lastDetection с graph-local порогом (т.2), молчащий пустой batch (т.3). Границы соблюдены — новой связи runtime↔топология нет (порог читается из подграфа graph-local, не из внешней топологии). Diff чист: секретов, .env, log-артефактов, unrelated нет. Один P2 по evidence (baseline 703 не подтверждён числом), не блокирует. LGTM.

[Структурщик]: C1/C4 — единственный писатель lastDetection = fusion зафиксирован конструктивно: fusion исполняется после анализаторов и перекрывает их запись, legacy fallback (trends→lastDetection) при отсутствии fusion-узла покрыт тестом. `resolveFusionDetectedThreshold` использует graph-local edge lookup по `MAKE_DETECTION_FUSION_OUT_HANDLE` → branch-узел в том же подграфе — это не runtime→топология, граница чистая. Инвариант «invalid-ref всегда несёт целевой kind» реализован сквозно (collect-samples/collect-fft-frames коэрсят kind при !valid). C7: комментарии привязаны к #340, термины не смешаны (valid vs kind ортогональны). Замечаний нет — LGTM по границам.

[Математик]: correctness — combined = (0.62+0.42)/2 = 0.52 проверено против порога branch 0.55 → not-detected, рассинхрона с branch нет; combined 0.9/0.7 → 0.8 detected. Разделение «пусто» (valid:false, без записи) и «сломано» (throw на NaN/kind) сохранено — throw для реального мусора не тронут, убран только empty-window guard. Порог `DEFAULT_DETECTION_THRESHOLD` fallback корректен. Math корректна.

[Музыкант]: audio C2 — вход в alarm теперь честно питается combinedScore через lastDetection (дух ND3 восстановлен), solo-trends не пишет lastDetection в обход fusion при наличии fusion-узла. Пустое окно холодного старта не роняет сценарий (transient n=0). Возражений нет.

[Верстальщик]: — (UI/DaisyUI/a11y в diff не затронуты; skeleton/aria-busy из DoD — отдельный UI-слой, здесь только runtime + docs).

P0/P1: —

P2:
1. `packages/device-board/src/runtime/*` — DoD требует подтверждения «703 базовых теста зелёные + целевые тесты», а local-evidence фиксирует лишь агрегированный pass turbo-команды без числа тестов; желательно приложить точный test count в артефакт.
2. `packages/device-board/src/runtime/block-executor.ts` — соглашение `templateId: 'DRONE_FUSION'` для legacy detection-front держится на строковом префиксе `DRONE`; стоит вынести в именованную константу рядом с legacy `isDroneDetection`, чтобы связь была явной.
3. DoD-пункт «smoke трёх сценариев alpha/beta/gamma (bundled MVP)» — по diff не виден отдельный smoke-прогон в этом коммите; отмечен как обязанность владельца перед живым тестом, но closure это не покрывает.

Checks:
- `git diff --check` — pass (SHA 41e1025, 2026-07-10T11:29:19Z)
- `yarn turbo run lint typecheck test --filter=@membrana/device-board --filter=@membrana/client && yarn check:boundaries && yarn verify:wire-sync` — pass (SHA 41e1025, 2026-07-10T11:30:03Z)
- `github-check:Lint, typecheck, test, build` — skipped (не считаю pass)
- `github-check:optional-review` — skipped
- `github-check:Turbo unit tests` — skipped

Closure readiness: waiting_merge
Verdict: LGTM

Примечание: local evidence достаточно для LGTM на reviewed SHA. GitHub PR #341 remoteState=open — merge не подтверждён, поэтому `merged` не утверждаю; finalize запрещён до merge evidence или явного `--accepted-branch-only`.
