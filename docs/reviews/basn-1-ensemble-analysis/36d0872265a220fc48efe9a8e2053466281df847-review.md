```text
Tier: T2
Task: basn-1-ensemble-analysis
Commit: 36d0872265a220fc48efe9a8e2053466281df847

[Teamlead]: PR #326, ~16 файлов, средний размер. Host-мост EnsembleProducer через клиент, stateless-узел + host-store per nodeId, границы device-board↔core соблюдены (только core-импорты). Local checks зелёные на exact SHA. GitHub CI checks skipped (не pass, но и не блокер для local review). Verdict LGTM с P2-замечаниями.
[Структурщик]: C1/C3/C4/C7 — kind в core + категория-массивы (V04_PALETTE_NODE_KINDS, board-ui, palette-node), новый SocketType handle-prefix, executor+store+resolve wired, тесты graph/executor присутствуют. Опциональность нового kind сохраняет serialization без миграции. OK.
[Математик]: fusion симметрия сохранена — сырой combinedScore в detection.confidence, fusion сливает confidence (0.9+0.7)/2=0.8 подтверждён тестом. Порог detected `>=0.5` и `presentCount>0` захардкожен в host (см. P2). OK для basn-1.
[Музыкант]: host-мост C2 — EnsembleProducer + createCombinedStreamDetectors по образцу mic-combined-detection, scenarioChainLog на start/skip/done, teardown через resetAll на stop/disconnect. Async, не блокирует рантайм. OK.
[Верстальщик]: — (UI-инспектор/a11y карточки не в scope basn-1; палитра-метка добавлена).

P0/P1: —
P2:
  1. `packages/device-board/src/runtime/host.ts` / bridge — порог detected `combinedScore >= 0.5` и `smoothing: 1` захардкожены; промпт basn-1 упоминает policy-опции на узле (детекторы/веса/smoothing) — сейчас узел без policy-инспектора. Уточнить, отложено ли осознанно.
  2. `packages/device-board/src/runtime/block-executor.ts` — при `host.makeEnsembleAnalysisFromSampleRefs === undefined` узел молча логирует `detected:false` без ref; поведение допустимо, но стоит зафиксировать тестом отсутствие host-моста.
  3. Клиентский host-тест с мок-детекторами (упомянут в DoD карточки basn-1: "клиентский host-тест с мок-детекторами") в diff не виден — присутствует только executor-тест с мок-host. Проверить наличие/добавить.

Checks:
  - git diff --check — pass (SHA 36d0872, 2026-07-09T16:30:54Z)
  - yarn turbo lint/typecheck/test core+device-board && check:boundaries && verify:wire-sync && client typecheck — pass (SHA 36d0872, 2026-07-09T16:31:27Z)
  - github-check:optional-review — skipped
  - github-check:Turbo unit tests — skipped
  - github-check:Lint, typecheck, test, build — skipped
  - github-check:Decide if studio affected — skipped

Closure readiness: waiting_merge
Verdict: LGTM
```

Примечание: local evidence достаточно и P0/P1 нет → LGTM. Но GitHub PR #326 в состоянии `open`, merge не подтверждён, поэтому closure readiness — `waiting_merge`, не `ready`. Finalize/archive запрещён до merge evidence или явного `--accepted-branch-only`. P2-3 (клиентский host-тест из DoD карточки) стоит проверить до закрытия эпика, но он не блокирует LGTM данного SHA.
