Tier: T2
Task: basn-5-combined-report
Commit: 6a592ab652f510d358bfeef0fb529f54f341ff85

[Teamlead]: PR #328, ~13 файлов, M-размер, аккуратный N5-узел (make-combined-report): kind в core → пины/фабрика → executor → палитра → host-мост + client-реализация + тесты (executor + эпик-smoke). Диффа чистый, unrelated/secrets/log-артефактов нет. Local evidence зелёное, но есть один расходящийся с DoD момент по async report-build, требующий явного подтверждения — см. P2. P0/P1 не найдено → LGTM.

[Структурщик]: C1/C3/C4/C7 — границы соблюдены: device-board зависит только от core (`fuseDetectorConfidences`, `ScenarioReportSchema`), сервисные вызовы не протекают в пакет; узел stateless, кэш идемпотентности живёт на хосте per-run и чистится в `resetProximityHistory` (C4). Новый kind/schema добавлены опционально, миграции старых сценариев не требуются (C7). wire-sync и boundaries в evidence — pass.

[Математик]: Слияние confidence делегировано core `fuseDetectorConfidences` (не бинарный OR) — соответствует консилиуму т.1. Порог `combinedScore >= 0.5` и `presentCount > 0` для `isDetected` согласован с branch-on-detection default. Замечаний по корректности нет.

[Музыкант]: Host-мост `makeCombinedReport` в `createScenarioRuntimeHost` → bridge; teardown кэша на scenario-run-start. Аудио-runtime не блокируется (синхронный конструктор ref) — ок.

[Верстальщик]: UI/a11y (C5) — DoD требует loading-состояний карточки отчёта (`skeleton` + `aria-busy`); в этом диффе UI-изменений нет. Не блокирует basn-5 как узел, но фиксирую как незакрытый пункт DoD — см. P2.

P0/P1: —

P2:
1. DoD basn-5 предписывает «async report-build запускает host-runtime» + «async-путь не блокирует main loop (по образцу async-pipeline-observability)». В диффе `makeCombinedReport` — синхронный конструктор ReportRef, но реального dispatch тяжёлой упаковки как `report-build` async job в этом коммите не видно; путь публикации через `publish-report` тоже вне диффа. Подтвердить, что async-выгрузка покрыта существующим каналом и не требует изменений в этом PR — `apps/client/src/modules/device-board/scenarioMicJournalBridge.ts`.
2. UI loading-состояние карточки отчёта (`skeleton` + `aria-busy`) из DoD не затронуто в диффе — уточнить, входит ли в basn-5 или переносится на эпик-smoke/отдельную карточку — `apps/client` (UI-слой).
3. `trackId` fallback: при `trackHandle` без ожидаемого префикса возвращается сырой handle, при `null` → `'none'` — поведение корректно, но стоит зафиксировать контракт "none" как sentinel — `scenarioMicJournalBridge.ts`.

Checks:
- git diff --check — pass (exit 0, SHA 6a592ab, 2026-07-09T17:01:35Z)
- yarn turbo lint/typecheck/test @core+@device-board && check:boundaries && verify:wire-sync && typecheck @client — pass (exit 0, SHA 6a592ab, 2026-07-09T17:01:44Z)
- github-check:optional-review — skipped
- github-check:Lint, typecheck, test, build — skipped
- github-check:Turbo unit tests — skipped
- github-check:Decide if studio affected — skipped

Closure readiness: waiting_merge
Verdict: LGTM

Примечание: local evidence достаточно и актуально (reviewed SHA = current SHA). GitHub checks в статусе `skipped`, PR #328 `open`, merge не подтверждён — состояние `merged` утверждать нельзя, finalize только после merge evidence. P2-пункты 1–2 рекомендую подтвердить при эпик-smoke закрытии (#323), они не блокируют basn-5.
