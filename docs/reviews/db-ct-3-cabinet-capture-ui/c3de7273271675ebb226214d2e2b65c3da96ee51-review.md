```text
Tier: T1
Task: db-ct-3-cabinet-capture-ui
Commit: c3de7273271675ebb226214d2e2b65c3da96ee51

[Teamlead]: 447 LOC, well-scoped CT3 phase (cabinet UI + REST capture API + realtime board event handlers). 
Aligns with sprint prompt §3 and canon v2.0. Code is defensive (null-checks, error handling), 
deprecations marked for v1 cleanup (CT7). No P0/P1 found. PR #231 open, checks pass. Ready for merge.
LGTM

[Структурщик]: C1 ✓ (single package `apps/cabinet`; no cross-package boundaries violated). 
C3 ✓ (new `deviceCapture.ts` API layer, integrates cleanly into `useCabinetNodeRuntime` hook). 
C4 ✓ (realtime subscriptions follow existing pattern; handler sets idempotent). 
No file moves or registry touch. —

[Математик]: —

[Музыкант]: —

[Верстальщик]: C5 ✓ (a11y maintained: `sr-only` role=status aria-live="polite" for capture state; 
button titles and aria-labels updated; fade-out 200ms declared in title; capture badge has title attribute 
explaining soft/hard modes). UI simplification (removed pause/mode buttons per v2 tariff) is correct. —

P0/P1: —

P2:
- `deviceCapture.ts` line 27: `parseError()` silently catches JSON parse; consider logging or telemetry for 403/5xx responses (non-blocking, operational insight).
- `useCabinetNodeRuntime.ts` line 96: heartbeat payload updates `expiresAt` but does not validate sessionId mismatch; if stale heartbeat arrives after release, update is skipped (correct). No issue, but document invariant if needed.
- `NodesPage.tsx` line 184: `capture` state lives in component, REST response updates it optimistically before broadcast arrives; idempotent but consider timing edge case if user clicks Release → Capture rapidly (already handled by `captureBusy` flag, no issue).

Checks: 
- `git diff --check` — pass (2026-07-02T18:11:05.163Z, SHA c3de72..ee51)
- `github-check:verify` — pass (2026-07-02T18:08:06Z, SHA c3de72..ee51)
- `github-check:optional-review` — skipped (2026-07-02T18:02:38Z)

Closure readiness: waiting_merge
Verdict: LGTM
```
