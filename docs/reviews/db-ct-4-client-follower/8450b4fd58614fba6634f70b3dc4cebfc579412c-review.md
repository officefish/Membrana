```text
Tier: T2
Task: db-ct-4-client-follower
Commit: 8450b4fd58614fba6634f70b3dc4cebfc579412c

[Teamlead]: CT4 client-side capture v2 enforcement: TTL auto-release, heartbeat loop, soft/hard mode differentiation, optimistic blocking. Scope: 2 packages, ~370 LOC net. Implementation aligns with sprint canon (DEVICE_BOARD_SERVER_FIRST.md v2.0 §3–4). Tests comprehensive (13 new cases), smoke DoD locally satisfied. LGTM — no P0/P1; two minor P2 observables noted.

[Структурщик]: C1 ✓ (package boundary; `apps/client` → serverFirstStore; `packages/device-board` → capture-flags export). C3 ✓ (capture-flags.ts pure utility, zero cross-domain coupling). C7 ✓ (deprecated v1 `editLease`/`captureState` marked with `// Tariff v3`; no orphan removal). Multi-package coordination correct: type import from `@membrana/core` (contracts), export from `device-board` (enforcement flags).

[Математик]: Capture TTL logic sound. `armCaptureTtl(expiresAt)`: millisecond-precision delay calculation, `Number.isFinite()` guard, zero-or-negative delay → immediate release. Heartbeat renewal: session-ID matching prevents replay, `applyCaptureHeartbeat()` immutable update with session guard. Last-write-win timestamp deferred to CT6 (runtime runtime-layer); CT4 scope correct. No precision loss, no off-by-one in test timers (fake-timer advancement 5m+1ms correctly triggers expiry).

[Музыкант]: —

[Верстальщик]: —

P0/P1: —

P2:
  1. **Stale check artifact**: `github-check:optional-review` marked `skipped` (2026-07-02T11:41:17Z) — predates current commit review (12:03:00Z). Evidence chain incomplete; recommend re-run for audit trail. Does not block LGTM (optional), but closure finalization should confirm CI green on exact SHA.
  2. **`applyRuntimeCommand` fade-out pass-through**: Comment notes `// fadeOutMs прокидывается в audio-engine в CT6` but CT4 code ignores it (`controller.stop()` unconditional). Correct per spec (CT4 = selection only), yet runtime-bridge test expects fade recognition. Verify CT6 handler exists before merge; prevent orphan parameter in wire.

Checks:
  - `git diff --check` — pass (2026-07-02T12:03:01.893Z, SHA exact match)
  - `github-check:verify` — pass (2026-07-02T11:46:42Z, success conclusion)
  - `github-check:optional-review` — skipped (2026-07-02T11:41:17Z, predates review; stale but non-blocking)

Closure readiness: ready
Verdict: LGTM
```
