Tier: T2
Task: wire-contract-codegen
Commit: 59df295ba875cb49c6651acdb726f62ba82d09f4

[Teamlead]: PR средний (M): апстрим `parseHealthPongPayload` в core, детерминированный генератор wire-контракта, freshness-гейт вместо частичной эвристики. Diff строго в границах DoD (core → генератор → checked-in артефакт → gate → package.json/registry/docs). Секретов, .env, log-артефактов, unrelated-файлов нет. Local evidence достаточно, все три local-команды pass на reviewed SHA. GitHub checks — skipped/pending (не stale, не fail). Нет P0/P1 → LGTM. Merge не подтверждён.

[Структурщик]: C1/C3/C4/C7 — направление зависимости верное: core = единственный источник, `node-realtime-wire.ts` помечен GENERATED, руки запрещены. `stripInternalImports` fail-closed на внешний импорт (самодостаточность контракта). Дедуп `isRecord` без молчаливого выбора: идентичные дропаются, неидентичные → throw. Границы пакетов не нарушены (`check:boundaries` pass); 12 импортёров не тронуты. `verify:wire-sync` теперь строго сильнее старой эвристики (весь контракт байтово vs. значения событий + одно поле). Замечание по устойчивости — см. P2.

[Математик]: correctness/детерминизм — `generateWireSource` идемпотентен (тест: два прогона байтово идентичны), fixed порядок `WIRE_SOURCE_FILES`, `normalizeDeclaration` whitespace-insensitive. `parseHealthPongPayload` в core эквивалентен прежней wire-копии (non-empty pingId), unit покрывает валид/битые входы. `wireFreshness` корректно нормализует CRLF и репортит первую расходящуюся строку. Апстрим-хелпер сохраняет поведенческий паритет — деплой по обычному окну обоснован.

[Музыкант]: — (audio/runtime stream не затронут; RuntimeCommand/Runtime payloads — только контрактные типы, не аудиотракт).

[Верстальщик]: — (UI/a11y не затронут).

P0/P1: —

P2:
1. `scripts/generate-wire-contract.mjs` `extractTopLevelDeclarations` — скобочный счётчик по символам не игнорирует `{`/`}` внутри строковых/template-литералов и комментариев; сейчас работает лишь потому, что контрактные файлы «простые» (задокументировано в комментарии). При усложнении core возможен тихий сбой границ деклараций — стоит добавить defensive-тест на балансный литерал или явную проверку инварианта.
2. `stripInternalImports` regex `^\.\/[A-Za-z0-9-]+\.js$` жёстко фиксирует flat-layout core-директории; при появлении вложенных путей (`./sub/x.js`) даст ложный fail — приемлемо как guard, но стоит зафиксировать ожидание в тесте.
3. `docs/tasks/registry.json` строка добавлена вручную и дублирует registry entry — консистентно, замечаний по существу нет.

Checks:
- `git diff --check` — pass (59df295, 2026-07-09T14:09:36Z, exit 0)
- `yarn turbo run lint typecheck test --filter=@membrana/core --filter=@membrana/background-cabinet && yarn verify:wire-sync && node --test generate/verify-wire-sync tests && yarn check:boundaries` — pass (59df295, 14:09:42Z, exit 0)
- `yarn test:scripts` — pass (59df295, 14:09:48Z, exit 0)
- `github-check:optional-review` — skipped (не pass, не блокирует)
- `github-check:Turbo unit tests` — skipped/pending
- `github-check:Lint, typecheck, test, build` — skipped/pending

Closure readiness: waiting_merge
Verdict: LGTM
