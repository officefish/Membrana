```text
Tier: T2
Task: sca-s1-ttl-throttling
Commit: ba4b1f16f2e8a33481aad1ec032f517e4666c239

[Teamlead]: Multi-package diff (apps/client + apps/membrana-studio), IPC-контракт, TTL/heartbeat логика. PR #237 размер S, scope точен. Checks зелёные (Turbo unit, typecheck, build, CI). Дополнены unit-тесты (SC1: TTL-разряд ровно один раз; focus-once per acquire). Вёрткой кода нарушений не обнаружено. LGTM при условии Structurer C1 (IPC-контракт + inter-package flow).

[Структурщик]: C1 — IPC boundary: electronStudioShellPort.ts (новый port) → boardLeaseBridge.ts (renderer) → main.ts (IPC listener). Контракт: preload.studioShell.notifyCaptureAcquired() → ipcMain on 'membrana:studio-shell:captureAcquired'. Архитектура: рендерер не вызывает логику main, main не знает о boardLeaseBridge — разделение ответственности соблюдено (STUDIO_HOST_BRIDGE_CONTRACT §Принципы). Inter-package dependency (apps/client → apps/membrana-studio) корректна — студия зависит от клиента, обратно нет. C4 — manifest/preload: studioShell добавлен в contextBridge без утечки node/require, sandbox:true. Структура валидна.

[Музыкант]: —

[Верстальщик]: —

P0/P1: —
P2:
  - boardLeaseBridge.ts:94 — комментарий упоминает "§3.3" (DEVICE_BOARD_SERVER_FIRST); подробность уместна, но в спринте задача ST7 (tray-shortcut) отложена — убедиться, что emergency stop сейчас доступен через UI-кнопку (требование выполнено логикой).
  - main.ts:98 — writeShellLog может быть undefined в раннем контексте; стабильность прослежена в registerLoggingIpc инициализации.

Checks:
  - git diff --check: pass
  - github-check:Turbo unit tests: pass (ba4b1f...)
  - github-check:Lint, typecheck, test, build: pass (ba4b1f...)
  - github-check:Package Membrana Studio (Windows nsis): pass (ba4b1f...)
  - github-check:Decide if studio affected: pass (ba4b1f...)
  - github-check:verify: pass (ba4b1f...)
  - github-check:optional-review: skipped

Closure readiness: waiting_merge
Verdict: LGTM
```
