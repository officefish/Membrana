Tier: T2
Task: basn-4-proximity-trend
Commit: 04488a9e9354dc369abf51bfa32220af5308a124

[Teamlead]: PR #327, ~18 файлов, чистое разделение core-классификатор / host-store / executor / контракты / палитра. Диапазон изменений строго по DoD basn-4; unrelated/secrets/log artifacts отсутствуют. Инварианты эпика соблюдены: узел stateless, состояние на хосте per nodeId с teardown на scenario-run-start, device-board зависит только от core, сервис через host-мост. Local evidence зелёное на exact SHA. P0/P1 нет — LGTM.

[Структурщик]: C1/C3/C4/C7 — kind добавлен в `scenario-node-kind.ts`, SocketType `ProximityRef` в `SOCKET_TYPES` + `REFERENCE_SOCKET_TYPES`, палитра/label/pins/секции синхронизированы (`palette-node.ts`, `board-ui.ts` + тесты). Границы: `check:boundaries` pass, core-лемма чистая (без DOM/Web Audio). Экспорт из `contracts/device-board/index.ts` консистентен. Опциональность нового kind сохраняет сериализацию старых сценариев. — ок.

[Математик]: `classifyProximityTrend` тотальна: NaN/отрицательные фильтруются, тишина (обе половины < minLoudness) → stable без деления на шум, приоритет `lost` (N промахов подряд в хвосте score) над трендом громкости — соответствует консилиуму т.2. `deltaRatio` через `Math.max(baseline, minLoudness)` защищён от деления на ноль. Пороги/окно нормализуются (чётное ≥2, positiveOr). Тесты покрывают approaching/receding/stable/lost/not-ready/кастомные пороги. — корректно.

[Музыкант]: C2 — host-мост `evaluateProximityTrend` копит серии (loudness из `evaluateSoundLevel`, score из fusion) per nodeId, окна ограничены (64/16), `resetProximityHistory()` вызывается на `scenario-run-start`. Stub-host возвращает детерминированный not-ready результат. chain-log `proximity-tick` для наблюдаемости. Рантайм не блокируется (лёгкая синхронная классификация). — ок.

[Верстальщик]: — (UI карточки/инспектор — вне scope basn-4; ProximityRef value/ref-различимость относится к basn-5/эпик-UI).

P0/P1: —

P2:
1. `packages/device-board/src/runtime/proximity-runtime-store.ts` — метод `resetAll()` есть, но host-store истории в `scenarioMicJournalBridge.ts` сбрасывается только по `scenario-run-start` log-хуку; при stop/disconnect без нового run история моста живёт до следующего старта. DoD требует teardown на stop — стоит явно связать `resetProximityHistory` с stop/disconnect путём, а не только с run-start.
2. `block-executor.ts` — при `host.evaluateProximityTrend === undefined` узел молча даёт `trend='stable', ready=false` и не пишет ref в store; поведение корректно, но стоит добавить smoke/лог-ветку на этот случай для наблюдаемости.
3. Эпик-smoke полного сценария (DoD эпика) — вне этой карточки, но alarm-loop exit через is-valid покрыт только executor-тестом; финальный end-to-end smoke ожидается на закрытии эпика.

Checks:
- `git diff --check` — pass (exit 0, 2026-07-09T16:47:02Z, SHA 04488a9)
- `yarn turbo run lint typecheck test --filter=@membrana/core --filter=@membrana/device-board && yarn check:boundaries && yarn verify:wire-sync && yarn turbo run typecheck --filter=@membrana/client` — pass (exit 0, 2026-07-09T16:47:10Z, SHA 04488a9)
- `github-check:optional-review` — skipped
- `github-check:Turbo unit tests` — skipped
- `github-check:Lint, typecheck, test, build` — skipped
- `github-check:Decide if studio affected` — skipped

Closure readiness: waiting_merge
Verdict: LGTM

Примечание: local evidence достаточно и покрывает DoD-проверки basn-4; GitHub checks в состоянии `skipped`, PR #327 `open`, merge не подтверждён — `finalize`/`merged` не заявляю. P2-пункты не блокируют.
