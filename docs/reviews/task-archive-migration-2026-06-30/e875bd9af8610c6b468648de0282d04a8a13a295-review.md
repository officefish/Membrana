# Task archive migration — Teamlead closure review

Tier: T2
Task: task-archive-migration-2026-06-30
Commit: e875bd9af8610c6b468648de0282d04a8a13a295
Branch: codex/task-archive-migration-sprint
Scope: b7064c1..HEAD

[Teamlead]: LGTM. The sprint completes the intended two-tier task storage split: hot registry is compact and archive history moved to `archive.jsonl`. The diff is large but expected because it includes the exact pre-migration backup, migration manifest and the registry/archive data move. No unrelated daily/Research-Tree artifacts are present in the reviewed scope.

[Структурщик]: LGTM. Migration is manifest-driven, clean-tree guarded, checksum-protected and has explicit rollback. Shared helpers now cover legacy row selection, archive loading, pending GitHub close and strict validation. The earlier `cwd` leak in `listPendingGithubClose` was fixed and covered by tests.

[Математик]: LGTM. Counts reconcile: `registry.json` 517 → 162 rows, `archive.jsonl` 1 → 356 rows, moved 355, legacy closed rows after migration 0. Manifest `moveCount` is 355 and preserves per-task checksums.

[Музыкант]: — no audio/runtime surface affected.

[Верстальщик]: — no UI surface affected.

P0/P1: —

P2:

1. Consider adding a future CI script alias such as `task-integrity-check` that wraps `yarn tasks:audit:verify --strict`, so the strict invariant becomes discoverable from CI naming.
2. Consider schema validation via a JSON Schema runner later; current validation is script-level and adequate for this sprint.

Checks:

- `yarn tasks:audit:verify --strict` — pass; `Task registry OK: 162 registry rows, 356 archive.jsonl rows.`
- `node --test scripts/task-registry.test.mjs` — pass; 8/8.
- `yarn test:scripts` — pass; 101/101.
- `yarn main-day-issue --dry-run --no-rag` — pass; generated dry-run context.
- `git diff --check` — pass.
- `git status --short` before review artifact — clean.

Closure readiness: waiting_merge
Verdict: LGTM

Notes:

- Automatic `yarn task:review:*` lifecycle was not used because this migration sprint did not have a live hot-registry task id after the storage split. This review is therefore a manual Teamlead closure artifact bound to exact SHA and diff scope.
- Final archive/merge must still be confirmed separately; this artifact does not claim PR merge, push, or GitHub Issue closure.
