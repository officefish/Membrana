Проверяю tier независимо, прежде чем принять инструкцию. Manifest объявляет T2. Domain routing: diff затрагивает ≥2 packages (`apps/client` + `packages/services/detectors`), плюс UI/DaisyUI (Layout) и math-пресеты порогов (Mathematician). Множественные пакеты → T2 согласно правилу. Независимая оценка совпадает с манифестом, конфликта нет — принимаю T2 не по инструкции, а по регламенту.

```text
Tier: T2
Task: nd-2-neural-plugin-ui
Commit: 1d8dfd92f50f72ebe24540487e6b7d83c8015832

[Teamlead]: PR среднего размера (~19 файлов; новый клиентский плагин neural-drone-analyzer + vite/tsconfig alias + каталог + review-артефакты ND1). DoD ND2 закрыт по коду: плагин по образцу trends-fft-sample-analyzer (offline-прогон + автозапуск по ended), UI с вердиктом/confidence/топ-классами, регистрация через MembranaRegistry, запись в client registry.json. Границы через публичный API audio-engine. Local checks зелёные на exact SHA. Один P1 по scope (см. ниже). Verdict BLOCK → needs_fix.
[Структурщик]: C1/C7 — границы плагина корректны: аудио только через sample-playback-service/audio-engine-service, детектор через @membrana/yamnet-detector-service, без new AudioContext/getUserMedia. check:boundaries pass. Порядок alias в vite.config (assets-подпуть ПЕРЕД пакетным) обоснован и прокомментирован. НО: diff включает docs/reviews/nd-1-yamnet-detector/* (review.md + manifest.json) — это артефакты ЧУЖОЙ задачи (nd-1), unrelated к DoD nd-2. Это нарушение «diff не включает unrelated».
[Математик]: correctness OK для клиента — resolveNeuralDroneAnalyzerConfig клампит threshold в [0,1], NaN/мусор → дефолт пакета; тесты покрывают границы. Пороги некалиброваны (ND3), помечено честно. topClasses сортирует по score убыв. Математики инференса здесь нет (в yamnet-service, ND1).
[Музыкант]: — offline-прогон по буферу сэмпла, без live Web Audio stream; getMonoChannel/loadSampleBufferById — публичный API. Runtime-аудио-тракт не затронут.
[Верстальщик]: C5 — DaisyUI-паттерны корректны (card/badge/progress/alert). alert role="alert" присутствует. Замечания a11y (P2): у progress нет aria-label/текстовой связи с именем класса (визуально рядом, но для screen reader связь неявная); кнопка «Анализировать сэмпл» дизейблится по busy/selectedSampleId — ок. Спиннера при model-loading/analyzing нет, только текст — приемлемо.

P0/P1:
  1. [P1][scope] Коммит смешивает две задачи: файлы docs/reviews/nd-1-yamnet-detector/cd0f43069...-review.md, docs/reviews/nd-1-yamnet-detector/manifest.json и правки packages/services/detectors/yamnet/src/core/{model.ts,yamnet-detector.ts} относятся к nd-1 (review-артефакты и P2-фиксы ND1), а не к DoD nd-2 (клиентский плагин/каталог). Review этой задачи ведётся строго по diff, относящемуся к DoD; unrelated-файлы должны быть исключены из коммита nd-2. Требуется разделить: правки yamnet-core и review nd-1 — в свой коммит/PR, оставить в nd-2 только клиентский плагин + каталог + alias-конфиги.

P2:
  1. apps/client/src/plugins/neural-drone-analyzer/NeuralDroneAnalyzerPanel.tsx — progress-бары топ-классов без явной aria-связи с именем класса; добавить aria-label={`${name}: ${score}%`} для screen reader.
  2. apps/client/src/plugins/neural-drone-analyzer/neuralDroneAnalyzerPlugin.ts — warmUp().catch маппит любую ошибку в failAnalysis(status='error'), из-за чего до первого анализа UI показывает error, хотя сэмпл ещё не анализировался; рассмотреть отдельный статус model-error, чтобы не путать провал прогрева с провалом анализа.
  3. docs/catalog/client/prompts/plugins/neural-drone-analyzer.md заявляет «~16 МБ» весов; та же зависимость от бинарных assets/model/*, что и P2#1 review ND1 — вне diff, проверить присутствие весов в пакете перед merge (иначе browser-провайдер fetch'ит несуществующие ?url в CI/проде).
  4. Ветка feat/nd-1-yamnet-detector — nd-2 публикуется в ветку nd-1; при branch-per-task это смешивает истории задач. Согласовать стратегию ветвления.

Checks:
  git diff --check — pass (SHA 1d8dfd9, 2026-07-06T12:42:26Z)
  yarn workspace @membrana/client test — pass (SHA 1d8dfd9, 12:42:42Z)
  yarn workspace @membrana/client typecheck — pass (SHA 1d8dfd9, 12:42:44Z)
  yarn catalog:verify-client — pass (SHA 1d8dfd9, 12:42:45Z)
  yarn check:boundaries — pass (SHA 1d8dfd9, 12:42:49Z)
  github-check:optional-review — skipped
  github-check:Turbo unit tests — skipped
  github-check:Decide if studio affected — skipped
  github-check:Lint, typecheck, test, build — skipped

Closure readiness: needs_fix
Verdict: BLOCK
```

Примечание: local checks зелёные и код плагина качественный, но BLOCK — по P1 (scope): коммит смешивает DoD nd-2 с review-артефактами и P2-фиксами nd-1. Регламент требует review строго по diff, относящемуся к DoD текущей задачи, без unrelated-файлов. Разделите коммиты — правки `yamnet/src/core/*` и `docs/reviews/nd-1-*` вынесите к nd-1, оставьте в nd-2 только клиентский плагин, каталог и alias-конфиги. После этого — повторный review нового SHA. GitHub PR #266 `remoteState: open` — merge в любом случае не подтверждён; `finalize` запрещён (fail-closed: unresolved P1 + нет merge evidence).
