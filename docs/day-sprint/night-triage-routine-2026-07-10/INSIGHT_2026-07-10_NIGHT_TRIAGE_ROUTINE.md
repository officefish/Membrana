# INSIGHT — пилот routine «Ночной триаж реестра задач» · 2026-07-10

> Фиксация решений и замеров пилота (Issue #344, бриф `SPRINT_BRIEF.md`,
> консилиум `docs/seanses/night-triage-routine-2026-07-10.md`).
> Статус: **ЗАМОРОЖЕН по стоп-условию 2026-07-10 ~16:20 UTC** — раны стартуют,
> но ни один из трёх не оставил наблюдаемых следов (ни ветки, ни PR);
> вероятная причина — репо не подключено к claude.ai/code (клон/push из облака).
> Разблокировка — за владельцем аккаунта (чеклист внизу).

## Канал (Шаг 1)

- Claude Code Routines (research preview) **доступен** на аккаунте: API
  `/v1/code/triggers` отвечает, рутина создаётся/обновляется/запускается программно.
- Рутина: `trig_01B4ugB4DJkc256k2ek9Rduc` «Night triage — Membrana»,
  https://claude.ai/code/routines/trig_01B4ugB4DJkc256k2ek9Rduc
- Расписание `30 23 * * *` UTC (= 02:30 МСК, окно 02:00–04:00 из брифа),
  **enabled: false** до прохождения приёмки.
- MCP-коннекторы аккаунта (Linear, Replit, Mintlify) цеплялись к рутине
  **по умолчанию** — отцеплены (`clear_mcp_connections`); пилоту внешние
  сервисы запрещены. ⚠ Ловушка: проверять `mcp_connections: []` при любом
  пересоздании рутины.
- Верификационный ран (read-only, «перечисли активные задачи»):
  сессия `cse_01Vy3LTQcXmKvXLV2AtPZC7b`, 2026-07-10 12:30 UTC.
- Боевой ран №1 (промпт v1): сессия `cse_01Wisbj6RubfjPKbfBJTaoDb`,
  12:46 UTC — **умер молча**: ~3.5 ч без ветки/PR.
- Боевой ран №2 (промпт v1.1 с ранним git-маркером и бюджетом вызовов):
  сессия `cse_01PjFGKyprHrTpWk4ibhEYjc`, 16:09 UTC — **маркер-ветка не
  появилась за 9.5 мин** → ран умирает ДО анализа; push/clone из облака,
  вероятно, не работает (репо не подключено к claude.ai/code?).
- **Квотная стоимость рана:** _<заполняет владелец по claude.ai UI>_ —
  вывод облачных сессий и расход квоты через API не читаются.
  Израсходовано 3 рана из 5 суточных (12:30 / 12:46 / 16:09 UTC).

## Главный урок пилота

**У облачного рана нет наблюдаемого канала ошибок.** Снаружи (API/git) виден
только успех (ветка/PR); провал неотличим от «ещё работает». Отсюда два
обязательных приёма в ROUTINE_PROMPT v1.1: ранний git-маркер (заглушка+draft PR
до анализа) и бюджет вызовов (запрет по-задачных git/gh — ~160×2 вызовов
на 677-строчном registry, вероятная смерть рана №1). Но и они не помогают,
если сессия падает до первого git push — этот слой проверяется только
глазами владельца в claude.ai UI.

## Секрет-гейт (Шаг 2)

- `yarn night-triage:secret-scan` (`scripts/night-triage-secret-scan.mjs`):
  паттерны ключей (Anthropic/GitHub/AWS/Slack/Google/PEM/Bearer/basic-auth-URL)
  + чувствительные JSON-ключи с непустыми значениями; non-zero exit блокирует.
- Прогон по путям чтения рутины (registry.json, tasks/README.md, ROUTINE_PROMPT.md):
  **OK, секретов нет** (2026-07-10). Тесты 5/5, `test:scripts` 188/188.

## Эталонный срез (для полноты/точности)

`REFERENCE_SNAPSHOT_2026-07-10.txt` (детерминированное вычисление из registry):

- активных: 162 (на ветке пилота; на main — 161, без задачи самого пилота);
- **ghost: 9** — все делят Issue #47 (VDR Hard Gate; #47 сознательно держим
  открытым → ожидаемая рекомендация relink/re-scope, не close);
- **orphan: 110** — масштаб структурного долга подтверждает мотивацию пилота.

`yarn issues:audit` оказался **не** discovery-аудитом (требует manifest,
исполняет закрытия) — эталоном приёмки служит детерминированный срез выше.

## Приёмка боевого рана (НЕ ПРОЙДЕНА — раны не доносят)

- Draft PR: не появился (раны №1, №2).
- Полнота / точность / детерминизм / dwell-time: не измерены — нет отчёта.

## Чеклист разблокировки (владелец аккаунта)

1. Открыть https://claude.ai/code, найти сессии
   `cse_01Vy3LTQcXmKvXLV2AtPZC7b` (12:30), `cse_01Wisbj6RubfjPKbfBJTaoDb`
   (12:46), `cse_01PjFGKyprHrTpWk4ibhEYjc` (16:09) — прочитать, на чём
   именно умерли (клон? push? разрешения? лимит?).
2. Проверить, подключён ли `officefish/Membrana` к claude.ai/code
   (GitHub-интеграция) и есть ли у облака право push в `claude/`-ветки.
3. Записать сюда квотную стоимость одного рана (из UI).
4. После починки: один ручной ран (`RemoteTrigger run` или кнопка в UI) →
   если draft PR с маркером появился, продолжить приёмку по DoD
   task-промпта (полнота ⊇ эталона, TP/FP, второй прогон на детерминизм).
5. Расписание (`enabled: true`) — ТОЛЬКО после полной приёмки.

## Решения (канон пилота)

Смотри таблицу консилиума — N=14 не канон (dwell-time в отчёте), daily draft PR,
отчёты копятся без архивации до приёмки, `issues:audit`-механика не CI-гейт,
секрет-гейт блокирующий, сортировка по id.

## Стоп-условие

**СРАБОТАЛО** (2026-07-10 ~16:20 UTC): preview доступен и раны стартуют, но
end-to-end канал не подтверждён — три сессии подряд без наблюдаемых следов.
По брифу: пилот заморожен, рутина остаётся `enabled: false`, остаёмся на
локальном `night:open`. Отказ дешёвый по построению: артефакты (промпт v1.1,
секрет-гейт, эталонный срез) готовы к моменту разблокировки канала.
