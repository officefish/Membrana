# HANDOFF → ночная сессия (ночной ритуал) · 2026-07-24

> Готовит: дневная сессия 24.07 (дерево Membrana-angelina). Для: новой сессии, которая запускает **ночной ритуал**.
> Точка входа: скилл `membrana-night-sprint`; `yarn night:open` → `night:run` → `dreams:tick` → `night:checkpoint` → `night:close`.

## ⚠️ КРИТИЧНО ПЕРВЫМ ДЕЛОМ — LLM-каналы

- **Anthropic лимитирован до 01.08** (HTTP 400 usage limits). Прямые вызовы Anthropic из скриптов **хардфейлят** — так утром 24.07 упал `ritual:day`.
- **Проводка к панели каналов ПОЧИНЕНА сегодня** (PR #1117): у утреннего ритуала per-stage процедуры под группой `ritual`, у всех цепочек фолбэк `anthropic → openrouter → deepseek → grok`. Провайдер **xAI/Grok добавлен** (PR #1118), ключ `X_AI_API_KEY` живой, `grok-4.5` отвечает.
- **Для ночи:** если ночные LLM-шаги (`dreams`, `night:research`) зовут Anthropic **напрямую** — они упадут так же. Проверь, идут ли они через `invokeProcedureLlm` (office-канал) или напрямую. **Живой путь сегодня — deepseek и grok** (Anthropic/OpenRouter-Sonnet залимичены). Форсировать провайдера можно overlay: `{ chain: [{provider:'xai', model:'grok-4.5'}] }` или `{provider:'deepseek', model:'deepseek-chat'}`.
- **Принцип владельца:** каналы НЕ должны стоять. Стойка = авария → искать иного провайдера/хостинг/VPN, но сперва локализовать: сервер (явный ответ типа usage-limit/rate_limit) vs наш канал (таймаут → возможно гео/egress). Прямой `curl office/health` = 200 (office жив).

## Состояние инфры

- **office редеплойнут** (healthy, uptime свежий) — сервит поле `group` и провайдера `xai`. Панель `panel.mmbrn.tech` → «Каналы LLM» показывает группу «ritual» + Grok.
- **Сны на office:** долг мостика «модуль снов смерджен в main, но не задеплоен» — office держал старый билд, ночь 2 суток пустая. **Проверь маршрут снов на office** (после сегодняшнего редеплоя мог подхватиться, а мог и нет — редеплой был про llm-channels, не про dreams). Если сны 404 — ночь снова будет пустой.

## Что уехало в main сегодня

- Коворк **контейнер стратегических документов** (PR #1141) — 5 фаз целиком на Grok, 33 теста. `scripts/lib/strategic-docs-*.mjs`.
- Проводка ритуала к каналам (#1117), xAI/Grok (#1118), утренние артефакты (#1095), шторм контейнера + инсайт борда сети.
- Шторм `membrana-leveling` и `storm-strategic-docs-container` — в `docs/storm/`.

## Зарегистрированные хвосты (issues)

- **#1142** canon-data.valid падает TypeError без skeleton (баг).
- **#1143** fn-гранулы end-to-end через адаптер.
- **#1144** реальные README/AGENTS/CLAUDE через трубу (пруф на живом дрейфе).
- **#1145** открытые вопросы INTERFACE_CONTRACT.
- **#1146** Cowork Phase 5 не автоархивирует ACTIVE-маркер.
- **#1105** бестиарий: антипаттерн «лиса в курятнике».
- **#1094** мягкий паттерн фреймов процедуры (магистраль дня).

## Открытые гейты / незакрытое

- **Мостик** открыт с 22.07, доска — 13 долгов (`docs/bridge/DEBTS.md`); вечерний `bridge-close` не срабатывал (LLM-лимит).
- **ACTIVE-маркер коворка** (`docs/COWORK_SPRINT_ACTIVE.md`) стоит `open Phase 0` — надо архивировать (#1146), коворк уже merged.
- **Заседание `membrana-leveling`** (ветвь A шторма) — свести регламент, ждёт слова владельца.
- **tar-fix PR #1128** висит красным на ЧУЖОЙ поломке (`device-board`/`@membrana/core` TS2305, не наш дифф) — фикс `--force-local` уже применён локально при редеплое.
- **6 WIP-снимков выравнивания** по деревьям (утро 24.07) — разобрать по T11 (мусор/ready/незавершёнка).
- **Вечерний ритуал** (обязателен): `bridge-close` + `team-evening-feedback` + вечерняя ласточка + code-review — не выполнен, день не закрыт вечерним контуром.

## Дерево/ветки

- Дневная работа шла в worktree **Membrana-angelina**. Коворк-ветки блоков запушены (`cowork/cowork-strategic-docs-container/*`).
- Для ночи — свой worktree (норма параллельных сессий), не коллизить с этим деревом.
