# HANDOFF: поток «тулинг + формат night-build» → новая сессия

**Закрыт:** 2026-07-18 · **Ветка потока:** работа влита в `main` (PR #603, #604, #606, #607, #608, #610)
**Параллельно:** соседняя сессия ведёт поток линз — её бриф в [`../HANDOFF.md`](../HANDOFF.md). Не смешивать.

## Что сделано (не переделывать)

**Заседание `meeting-night-build-format` ЗАВЕРШЕНО** — все пять узлов DAG закрыты,
каждая фаза ратифицирована владельцем и прошла аудит S-M5 отдельным агентом:

| Узел | Итог | Где |
|---|---|---|
| M0 порядок (DAG) | ратифицирован + аудит | `docs/seanses/night-build-format-m0-order-run2-clean-2026-07-18.md` |
| M1 guardrails | инвариант ночи перевёрнут | `NIGHT_SPRINT_REGULATION.md` §Несущий инвариант |
| M2 каденс | вес кванта W=R+C+D | там же §Вес кванта и каденс |
| M3 taxonomy | 5 классов тест-гейта | там же §Тест-гейт по классу правки |
| тулинг 4+5 | `night:open` от origin/main, `night:close` авто-handoff | `scripts/lib/night-build.mjs` |

**Ночь тулинга 17.07** (PR #603): `.markdownlint.json` (MD060-шум), честный rt-6
(`hasVerdictSection`), `reconcileReplyCount` (счётчик реплик), `resolveOfficeToken`
(токен из любого worktree), libuv-фикс `insight review`, мета-гвард `test-list-coverage`.

**Партнёрские активы:** DADS Антона → инсайт adopted 7.4; ВИЗОР Дениса → инсайт
deferred 2.4. Обе ласточки доставлены.

## Владельческие решения, которые гейтят будущее (токены правды)

Читать в `docs/truth/registry.json` — они **сужают** пространство решений:

- `meeting-regulation-is-canon` — формат заседаний **канон** (ратифицирован прогоном).
  Следствие: правки формата идут порядком канона → `meeting-format-edits-are-canon-edits`.
- `meta-work-precedes-product-magistral` — мета-работа **безусловно** впереди
  продуктовой магистрали (выбран не условный вариант). Влияет на `plan:week`.
- `parallel-sessions-standard-fix-driver` — параллельные сессии штатны, union-драйвер
  реестра **обязан чинить** (не «принять как издержку»).
- `membrana-is-sensor-layer` — Membrana сенсор; пеленг/эффектор/C2 — чужой слой.
- `partner-assets-are-deliberate-channel` + `partner-asset-eval-becomes-insight` —
  партнёрский актив идёт через инсайт-цикл.

## Открытые долги (с точными указателями)

1. **Фикс union-драйвера реестра** — `scripts/registry-merge-driver.mjs`. Union
   объединяет НАБОР записей, но не мёржит ПОЛЯ: при расхождении одного токена
   побеждает одна сторона, отзыв откатывается в `active`. Токен
   `registry-union-loses-status-change` (с предикатом). Владелец сказал «чинить».
   Правило разрешения (revoked > active? по дате?) — **не назначено, владельческое**.
2. **Две правки формата заседаний** (теперь правки канона, нужен порядок):
   `--no-rag --no-memory` по умолчанию для M0 (урок run1-дрейфа) и запрет
   процедурных посылок в вердикте (`surplusPremise`, находка аудита M3).
3. **NB6-follow-up:** `telegram.client.sendMessage`
   (`packages/background-office/.../telegram.client.ts:58`) возвращает голый boolean —
   довести до `message_id`, чтобы ласточка показывала след доставки. Код + **деплой**.
4. **`meeting:open/next/audit` не построены** — контейнеры заседаний собираются руками.
5. **Заседание `scripts-boundary`** (17.07): M0 ратифицирован, M1 (S+F: семантика
   отказа + гейт свежести) **не проводился**. Контейнер:
   `docs/meeting/scripts-boundary/MEETING_ACTIVE.md`.
6. **DADS-инсайт adopted** — волна 1 (D1 extraction / D3 дедуп ESC-50 / D4 манифест)
   не начата; D2/D5 ждут M1-заседания по ресэмплу vs рекалибровке.

## Ловушки этой сессии (стоили времени — не наступать снова)

- **Локальный `main` залочен** соседним worktree → `git checkout main` падает.
  Ветку брать **от `origin/main`** (это же G3 регламента ночи).
- **Ветка отстаёт от main** → наивный мёрж откатывает работу соседней сессии.
  Порядок: `git fetch` → `git merge origin/main` В свою ветку → проверить, что
  чужое цело → только потом PR. origin двигался под руками дважды за сессию.
- **`OFFICE_API_TOKEN`** нет в `.env` openrouter-worktree (там плейсхолдер
  `API_INTERNAL_TOKEN`, office даёт 401). С NB4 `resolveOfficeToken` берёт сам —
  но если сломается, токен в `.env` **основного** worktree.
- **S-M1 считает жирные `**M2**`/`**NB1**` за вопрос-ID** → повестка «несёт 4
  вопроса», отказ до прогона. Фазовые ссылки в повестке — не жирным.
- **Консилиум дрейфует в соседнее заседание** через RAG/память (run1 M0 подмешал
  узлы scripts-boundary). Для M0 — `--no-rag --no-memory` + ограда в повестке.
- **`docs/HANDOFF.md` — общий файл**, сейчас у потока линз. Не перезаписывать.

## С чего начать холодной сессии

1. `yarn hermes:brief` — прогреться (читает `HANDOFF.md`, реестр, PR, git дня).
2. `yarn truth:review` — очередь ревизии графа. **Начинать с неё**: 18.07 именно
   ревизия нашла тихую потерю отзыва, которую не поймал ни один гейт.
3. Спросить владельца, какой поток берём: долг №1 (драйвер) / №2 (канон формата) /
   №5 (scripts-boundary M1) / №6 (DADS-волна) — или продуктовую магистраль дня.

> Напоминание из этой сессии: гейты проверяют инварианты графа, но **не спрашивают
> «не потерялось ли уже принятое решение»**. Это делает только ритуал ревизии.
