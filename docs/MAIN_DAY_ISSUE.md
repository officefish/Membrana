<!-- Сгенерировано: 2026-06-10T04:32:54.139Z (yarn main-day-issue) -->
<!-- Тип: центральная задача дня (MAIN_DAY_ISSUE) — обязательный фокус для человека и агентов -->
<!-- Входы: DAILY_STANDUP, STRATEGIC_PLAN_DAY, DAILY_CODE_REVIEW, registry, активные промпты -->
<!-- CURRENT_TASK — только вспомогательный буфер, не канон -->
<!-- active в реестре: single-node-detection-first -->

# MAIN_DAY_ISSUE — 2026-06-10

**Дата:** 2026-06-10 (утро)
**Роль:** Teamlead (Vesnin)
**Статус:** Переход от инфраструктуры к детекторам; stage-gate 1→2 в подготовке

---

## 🎯 Один обязательный фокус дня

### **Закладка фундамента Этапа 1.A: контрактный слой детекторов + бенчмарк-инфраструктура + первый детектор**

**GitHub:** [#47](https://github.com/officefish/Membrana/issues/47) (Single-Node Detection First)
**Реестр:** `single-node-detection-first` (stage-gate 1→2)
**Критерий успеха дня:** `@membrana/detector-base` + `DETECTOR_BENCHMARK.md` + `DATASET.md` + scaffold `@membrana/harmonic-detector-service` + синтетический датасет v0.1 + обновление `ARCHITECTURE.md` §1e с явной заморозкой многоузловой архитектуры до gate.

---

## 📋 Сущность дня

После завершения инфраструктурных работ (Media Library + MCP фаза A) **сегодня — переломный момент: переход от фундамента к первому конкретному детектору**. Находимся между Этапом 0 (завершён) и Этапом 1.A (начинаем сегодня).

**Три стратегических блока:**

1. **Контрактный слой** — типы `DroneDetector`, `DetectionResult`, `AudioWindow` в `@membrana/detector-base`; типы экспортируются из `@membrana/core` для удобства.
2. **Бенчмарк-инфраструктура** — документ `DETECTOR_BENCHMARK.md` (регламент), скрипт `yarn benchmark:detectors`, синтетический датасет v0.1 (9 wav-файлов).
3. **Гармоник-детектор v0.1** — первый детектор (harmonic-анализ 80–250 Гц), unit-тесты ≥70%, регистрация в реестре детекторов.

**Критический результат:** К вечеру имеем **демонстрируемую систему** для тестирования детектора на синтетике, а также ясную архитектурную границу, запрещающую многоузловую разработку до stage-gate.

---

## 📋 Definition of Done

- [ ] Пакет `@membrana/detector-base` создан; типы `DroneDetector`, `DetectionResult`, `AudioWindow`, `DetectionMetrics` экспортируются из `@membrana/core`.
- [ ] `docs/DETECTOR_BENCHMARK.md` написана (регламент, примеры, как добавить детектор).
- [ ] `docs/DATASET.md` написана (требования к данным, схема папок, источники, минимальная квота).
- [ ] Синтетический датасет v0.1 собран: 9 WAV-файлов (3 дрона, 3 дрона+фон, 3 фона) в 48 kHz с metadata.json.
- [ ] Скрипт `yarn benchmark:detectors` запускается без ошибок; выдаёт JSON-отчёт + markdown-таблицу.
- [ ] `@membrana/harmonic-detector-service` полностью реализована: math + core + тесты ≥70%; детектор регистрируется в benchmark-реестре.
- [ ] `ARCHITECTURE.md` §1e обновлена: явный запрет на `tdoa-service`, `localizer-service`, `tracker-service`, `transport-service` до stage-gate; ссылка на `DETECTOR_BENCHMARK.md`; PR-чек-лист.
- [ ] `yarn lint` и `yarn type-check` проходят без ошибок во всех затронутых пакетах; нет циклических зависимостей.
- [ ] Консилиум-memo в `docs/seanses/` (опционально, если потребуется группое обсуждение).

---

## ⏱️ Порядок работы

| Фаза                       | Время       | Роль                    | Содержание                                                          | Выход                               |
| -------------------------- | ----------- | ----------------------- | ------------------------------------------------------------------- | ----------------------------------- |
| **A. План и форма**        | 09:00–10:00 | Teamlead                | Уточнение скоупа, контракт на детектор (1–2 абзаца + список файлов) | ADR-абзац, LGTM на границы          |
| **B. Контракт + scaffold** | 10:00–11:30 | Структурщик + Математик | Создание `detector-base`, типы, mock-реализация                     | Пакет с тестами, типы в core        |
| **C. Ядро гармоник**       | 11:30–14:00 | Математик               | Алгоритм анализа, классификатор, unit-тесты ≥70%                    | `analyzeHarmonic()`, тесты pass     |
| **D. Интеграция + скрипт** | 14:00–16:00 | Структурщик + Музыкант  | Benchmark-скрипт, регистрация детектора, синтетический датасет      | `yarn benchmark:detectors` работает |
| **E. Документация**        | 16:00–17:30 | Структурщик + Музыкант  | `DETECTOR_BENCHMARK.md`, `DATASET.md`, `ARCHITECTURE.md` §1e        | Документы готовы, консилиум (опц.)  |
| **F. Финал**               | 17:30–19:00 | Teamlead                | LGTM, merge, архив дня                                              | PR merged, дневной отчёт            |

---

## 🚫 Что НЕ делаем сегодня

- ❌ Реализация 2-го и 3-го детекторов (spectral-flux, cepstral) → отдельные task-промпты на следующие дни.
- ❌ YAMNet/CLAP нейросетевые детекторы → Этап 1.B.
- ❌ Полевая валидация дронов → на следующей неделе.
- ❌ Многоузловая архитектура (TDOA, localizer, tracker) → заморожена до stage-gate.
- ❌ Расширение синтетического датасета сверх v0.1 → параллельно в течение недели.

---

## 🚀 Предусловия для успеха

1. **Разделение ответственности ясно** — Структурщик (контракты), Математик (алгоритм), Музыкант (синтез данных).
2. **FFT-analyzer-service готова** (вчера завершена) — harmonic-детектор вызывает её чистые функции.
3. **Media Library готова** (вчера завершена) — может хранить и проигрывать аудио для тестирования.
4. **Ветка techies68 актуальна** — все коммиты идут сюда; PR в `main` в конце дня.

---

## 📊 Матрица Issues ↔ день

| GitHub                                     | Статус          | Связь                                          |
| ------------------------------------------ | --------------- | ---------------------------------------------- |
| **#47** (main-day-issue: Single-Node gate) | 🟢 **в работе** | Центральный фокус: контрактный слой + бенчмарк |
| **#54, #53, #52** (MCP phases B/C)         | 🟡 отложены     | Фаза A завершена; B/C — после stage-gate       |
| **#49** (Mic UI)                           | 🟡 в беклоге    | После stage-gate 1→2                           |
| **#28–#36** (code-review issues)           | 🟡 backlog      | Не активируем сегодня                          |

---

## ✅ Критерии завершения дня

**Если всё успешно:**

- ✅ `@membrana/detector-base` существует, типы работают.
- ✅ `yarn benchmark:detectors` запускается на синтетическом датасете.
- ✅ Harmonic-детектор прошёл unit-тесты (≥70%).
- ✅ Документация актуальна.
- ✅ Нет нарушений слабой связанности.
- ✅ Можно сразу на завтра переходить к итеративному совершенствованию детектора и расширению бенчмарка.

**Если критично не успеваем:**

- 🔴 Приоритет 1: `@membrana/detector-base` + `yarn benchmark:detectors` (инфра).
- 🔴 Приоритет 2: harmonic-детектор scaffold + unit-тесты.
- 🔴 Приоритет 3: документация (откладываем на завтра, но форма не откладывается).

---

## 🎯 Успех дня = контрактный слой + демонстрируемая инфра + stage-gate граница

Это **не день реализации полного детектора**, а **день установления фундамента и границ**. К вечеру должно быть ясно:

1. **Как пишут детекторы** (контракт).
2. **На чём их тестируют** (бенчмарк + датасет).
3. **Почему многоузловая архитектура сейчас невозможна** (заморозка в ARCHITECTURE.md).
4. **Какая метрика нужна для разморозки** (stage-gate criteria в DETECTOR_BENCHMARK.md).

---

**Хранитель дня:** Teamlead (Vesnin).
**Ветка:** techies68.
**Архив:** `docs/archive/daily-day/{DATE}/` (вечер).
