<!-- Сгенерировано: 2026-05-14T14:28:03.521Z (yarn code-review:full) -->

# Code Review: Текущее состояние Membrana (Ozhegov branch)

## [Teamlead / Vesnin]

### Стратегический обзор

За день выполнено **3 ключевых направления**:
1. **Инфраструктура персонажей** (`e0e253c`) — добавлен Ozhegov persona; создана точка входа для ролевого взаимодействия ✅
2. **Телеметрия & журнал** (merged в main) — интеграция journal-module в client + телеметрия-сервис ✅
3. **FFT-анализатор: рефакторинг и тесты** — вычленение `matchesDetectionThresholds` в чистую функцию, Vitest покрытие ✅

**Риск-оценка**:
- ⚠️ **CRITICAL**: тест `@membrana/core#test` падает с exit code 1 → блокирует CI/слияние. Нужна немедленная диагностика.
- ⚠️ **HIGH**: история коммитов показывает тесты для FFT добавлены, но результаты не видны в `TEST STATUS`. Проверить, почему Vitest не интегрирован в турбо-пайплайн.
- 🟡 **MEDIUM**: журнал теперь в client с категорией "Мониторинг" — нужно убедиться, что UI-слой не содержит бизнес-логику (только отображение).

**Что стоит проверить перед LGTM**:
- [ ] Почему `core#test` падает? (Похоже, Vitest UI добавлен, но конфиг турбо не обновлён?)
- [ ] Граница между `telemetry-service` (данные) и `telemetry-journal` (UI) соблюдена?
- [ ] FFT-математика действительно чистая функция (без Web Audio, без побочных эффектов)?

---

## [Структурщик / Ozhegov]

### Анализ связанности и архитектуры

**Что было сделано сегодня (хорошо)**:

✅ **Rефакторинг FFT** (`830f8fb`):
- Вычлена `matchesDetectionThresholds` → чистая функция в фсп `math`
- Уменьшена связанность `fft-analyzer-service` с бизнес-логикой
- Паттерн соответствует ARCHITECTURE.md (сервис-обвязка вокруг чистых функций)

✅ **Интеграция agenda** (`0749b5e`):
- Registry + plugin lifecycle + client registration
- Правильная обвязка для регистрации плагинов по контракту

✅ **Телеметрия** (merged):
- `telemetry-service` — отдельный сервис (правильная изоляция)
- `telemetry-journal` — UI-модуль в client (слой представления)
- `background-office` — NestJS gateway (бэк-координация)

**Потенциальные нарушения (⚠️ требуют проверки)**:

🔴 **Импорты в mic-плагине** (`e94fabc`, `5403860`):
- Коммит: "wire telemetry-service, register journal module, mic plugin logs"
- **Вопрос**: есть ли **прямой импорт** из mic-плагина в telemetry-service или наоборот?
- **Ожидание по ARCHITECTURE.md**: плагины общаются через шину (EventBus, AgendaRegistry), не напрямую
- **Action**: скан `packages/plugins/mic` → проверить, нет ли `import … from '@membrana/telemetry-service'`

🟡 **Телеметрия в client/modules** (`0106aca`):
- `telemetry-journal` — модуль в client, получает данные от сервиса
- **Риск**: если модуль содержит бизнес-логику фильтрации/трансформации логов, это нарушает разделение ответственности
- **Ожидание**: UI должна быть **dummy component** (читает `props.logs`, отображает). Логика — только в `telemetry-service` или `background-office`

🔴 **Core#test: критический падеж**:
- Коммит `44cfae8` добавляет Vitest UI, но тесты core падают
- Возможные причины:
  - Vitest конфиг не совместим с текущей версией Jest/Turbo
  - Тесты core нуждаются в обновлении (старый формат)
  - Отсутствует `passWithNoTests` флаг в конкретном сценарии

---

## [Математик / Dynin]

### Анализ FFT-математики и чистоты функций

**Проверено из коммитов**:

✅ **Правильно**: `matchesDetectionThresholds` вычленена как чистая функция
- Это сигнал хорошей рефакторизации
- Функция должна принимать массив/типизированный буфер с параметрами, возвращать boolean/Result

⚠️ **Требует осмотра** (из коммита `7978fd1` — "test coverage for math fft, metrics, statistics"):

**Вопросы к коду**:
1. Какие **edge cases** покрыты в тестах?
   - Пустой буфер (длина 0)?
   - NaN / Infinity в сэмплах?
   - Граничные значения частот (Nyquist frequency)?

2. **FFT-окно** (hanning, hamming) — где оно применяется?
   - Если в `fft-analyzer-service`, это OK
   - Если в какой-то функции, переданной из другого слоя, нарушена чистота

3. **Статистика** (metrics):
   - Среднее значение амплитуды?
   - Пиковая частота?
   - RMS (Root Mean Square)?
   - Все ли функции возвращают числа (не объекты с побочными эффектами)?

**Action для Dynin**:
- [ ] Убедиться, что все функции в `packages/services/fft-analyzer/math/` **не импортируют**:
  - `Web Audio API`
  - `React`
  - Другие сервисы
  - DOM
- [ ] Добавить тесты на граничные значения (особенно для `matchesDetectionThresholds`)

---

## [Музыкант]

—

---

## [Верстальщик]

### Анализ UI-модуля журнала

**Коммит `0106aca`**: "feat(client): telemetry-journal UI module, category Мониторинг"

✅ **Хорошо**:
- Модуль отделён в отдельный package (правильная изоляция)
- Категория "Мониторинг" указывает на правильную кластеризацию в UI

⚠️ **Требует проверки**:

1. **Props контракт**:
   - Компонент принимает готовые логи от сервиса или выполняет фильтрацию?
   - **Ожидание**: `<TelemetryJournal logs={logs} onClear={() => ...} />` (данные готовые)

2. **a11y и адаптив**:
   - Логи в журнале — скроллируемый список?
   - Есть ли `aria-live="polite"` для новых записей?
   - Мобильная адаптация (узкие экраны)?

3. **Соответствие DESIGN.md**:
   - Цветовая схема (для логов: warning/error/info)?
   - Типография (код в лог-строках должен быть monospan)?

**Action для верстальщика**:
- [ ] Убедиться, что модуль не содержит бизнес-логику (фильтра, сортировки, парсинга)
- [ ] Добавить скриншоты компонента в DESIGN.md или создать Storybook сторию

---

## Итоговый артефакт

### Checklist для слияния в main (после диагностики)

```markdown
### 🔴 BLOCKING (исправить перед LGTM)

- [ ] **core#test падает**: диагностировать причину (Vitest конфиг? Jest версия? пустые тесты?)
  - Команда: `yarn test @membrana/core --no-coverage` (локально)
  - Логи CI/Turbo должны показать конкретный тест

- [ ] **mic-плагин → telemetry**: проверить, нет ли прямых импортов
  - Grep: `packages/plugins/mic` → `from '@membrana/telemetry'`
  - Должны использовать только EventBus/AgendaRegistry

### 🟡 HIGH PRIORITY (до финального LGTM)

- [ ] **FFT-математика**: убедиться, что все функции в `math/` чистые (no Web Audio, no React)
  - Проверить импорты в `packages/services/fft-analyzer/src/math/`
  - Тесты должны покрывать edge cases (NaN, пустой буфер, Nyquist)

- [ ] **telemetry-journal**: отделить логику от UI
  - Если фильтрация логов — в сервис
  - Если парсинг timestamp — в сервис
  - UI получает готовые данные, отображает

- [ ] **Vitest в турбо-пайплайн**: добавить в `turbo.json`
  - Task: `test` должен запускать `vitest` с `--reporter=json` для CI

### 🟢 NICE TO HAVE (после основных исправлений)

- [ ] Добавить аватары персонажам "Музыкант" и "Верстальщик" в таблицу
- [ ] Создать сторию для telemetry-journal в Storybook (если он есть)
- [ ] Документировать edge cases FFT в комментариях функций
```

---

## Definition of Done

✅ **Для текущего дня (ozhegov branch)**:
- [ ] `yarn lint` **полностью зелёный** (сейчас ОК ✅)
- [ ] `yarn test` **не падает** (критический риск ⚠️)
- [ ] Нет прямых импортов между плагинами и сервисами (требует grep-проверки ⚠️)
- [ ] FFT-математика — чистые функции + edge-case тесты (в процессе ✅)
- [ ] telemetry-journal UI соответствует DESIGN.md (требует осмотра ⚠️)
- [ ] Teamlead (Vesnin) даёт LGTM после исправлений выше

---

### 📋 Рекомендуемый порядок действий

1. **Срочно** (это час):
   ```bash
   yarn test @membrana/core --no-coverage
   # Скопировать full error → вставить в /ask vesnin --task "core#test падает: …"
   ```

2. **Параллельно** (10 мин):
   ```bash
   grep -r "from '@membrana/telemetry" packages/plugins/mic/src/
   grep -r "from '@membrana/fft-analyzer" packages/plugins/mic/src/
   ```

3. **Проверить** FFT-тесты:
   ```bash
   yarn test @membrana/fft-analyzer-service --coverage
   ```

4. **Затем** → `/ask dynin --task "edge cases в FFT: что точно проверять?"`

5. **Вернуться к Teamlead** → `/review` перед слиянием в main.