<!-- Сгенерировано: 2026-05-15T14:24:05.360Z (yarn code-review) -->

# Code Review: Состояние репозитория от 15 мая

## [Teamlead — Vesnin]

**Стратегический взгляд:**

За сегодня закрыта крупная фича — телеметрийный журнал с визуализацией FFT-анализа (задача архивирована в `docs/tasks/registry.json`). Ветка `feat/telemetry-journal-report-viz` готовится к слиянию в `main`. Параллельно активна работа над детектором дронов (GitHub #45, L-задача, промт добавлен в `docs/tasks/`).

**Риск 1: Очистка `packages/temp/three-param-analyzer`** 
Удаление временного пакета — хороший знак (не накапливаем мусор), но важно убедиться, что **никакие сервисы из `packages/services/` не импортировали** из этого пакета. Проверить:
```bash
grep -r "three-param-analyzer" packages/services/ apps/
```
Если есть — нарушена архитектура (прямая зависимость от temp в production).

**Риск 2: Тесты и линтер падают**
```
@membrana/core#test → (1)
@membrana/fft-analyzer-service#lint → (-1073741523 ошибка)
```
Это **блокирующее условие** для LGTM. Нужно разобраться перед мерджем.

**Требование к Definition of Done:** Все тесты проходят (`yarn test`), линтер чист (`yarn lint`), нет импортов из `temp/` в основной код.

---

## [Структурщик]

**Связанность и модульность:**

1. **AppFooter.tsx** — единственный измененный исходный файл. Это UI-компонент верстальщика, но почему он трогался в ветке `feat/telemetry-journal-report-viz`? 
   - Требуется проверить diff: должен быть **только** добавлен новый элемент UI (например, индикатор качества звука или ссылка на отчёт).
   - Если там логика импорта из `services/` — это задача **Структурщика** привести к фасаду (через `useService()`).

2. **Новый скрипт `scripts/sync-github-token.mjs`** — неслужебное изменение, не связано с фичей. Это чистка CI или подготовка к автоматизации? Нужна краткая аннотация в коммите.

3. **FFT-анализатор + детектор дронов:** убедиться, что сервисы:
   - `@membrana/fft-analyzer-service` — чистая математика + Web Audio интеграция (без React).
   - `@membrana/drone-detector-service` (если существует) — только обвязка анализа, не UI.

**Red flag:** линтер на FFT-сервисе упал с кодом `-1073741523` (обычно stack overflow на Windows или зацикливание). Возможна:
- Циклическая зависимость между сервисами.
- Импорт `index.ts` внутри собственной папки.
- Задача для **Структурщика**: запустить `yarn turbo graph` и посмотреть граф зависимостей.

---

## [Математик — Dynin]

**Алгоритмическая часть:**

Из коммитов видно, что:
- FFT-плагин готов (параметры и окна захвачены в docs).
- Детектор дронов в активной разработке (GitHub #45, промт L-задачи добавлен).

**Контрольный вопрос для Dynin:**
- Какие edge cases покрыты в FFT-анализаторе?
  - Молчание (нулевой сигнал)?
  - Очень тихий звук (ниже порога шума)?
  - Насыщение (клиппинг)?
- Какие параметры окна (Hann, Blackman) выбраны и почему?

**Замечание:** в коммите `9aacf6e` упомянуты «v0.1 params and FFT window». Проверить, что это задокументировано в `packages/services/fft-analyzer/README.md` или в коде как константа, а не hardcoded magic number.

---

## [Музыкант]

**Качество звука и обработка:**

В истории коммитов упоминается плагин `sound-quality-viz` (issue #42, отмечена как закрытая). Это пересекается с FFT-отчётом: оба анализируют частотный спектр. 

**Вопрос к Музыканту:**
- Не дублируется ли логика анализа между `sound-quality-viz` и `fft-analyzer-service`?
- Если да — нужен **единый сервис** с интерфейсом: `analyzeAudio(buffer) → {fft, peaks, quality, ...}`.

**По детектору дронов:**
- Параметры чувствительности (пороги частот, амплитуда) — они конфигурируемые из UI или hardcoded?
- Целевой диапазон: 80–300 Hz (типичный размах моторов квадрокоптеров)?

---

## [Верстальщик]

**UI по DESIGN.md:**

Единственное изменение в исходном коде — `AppFooter.tsx`. 

**Проверить:**
1. Добавлены ли новые визуальные элементы (индикатор FFT, кнопка отчёта)?
2. Соответствуют ли они палитре / типографике из `DESIGN.md`?
3. Адаптивность: как выглядит на мобильной ширине?
4. a11y: есть ли `aria-label` на новых интерактивных элементах?

**Замечание:** если в AppFooter добавлена логика (например, условный рендер, useService), это нарушает правило Верстальщика — логика должна быть в контейнере выше.

---

## Итоговый артефакт: Чек-лист перед LGTM

```markdown
### ❌ БЛОКИРУЮЩИЕ ПРОБЛЕМЫ (must-fix перед мерджем)

1. [ ] Падение тестов @membrana/core#test
   - Запустить: `cd packages/core && yarn test --verbose`
   - Исправить или задать сроком на следующий спринт

2. [ ] Ошибка линтера @membrana/fft-analyzer-service#lint (-1073741523)
   - Возможная циклическая зависимость или stack overflow
   - Команда: `cd packages/services/fft-analyzer && yarn lint --debug`

3. [ ] Проверить, нет ли импортов из `packages/temp/` в production
   ```bash
   grep -r "three-param-analyzer\|temp/" packages/services/ apps/client/src --include="*.ts" --include="*.tsx"
   ```

### ⚠️ РЕКОМЕНДАЦИИ (high-priority review)

4. [ ] **AppFooter.tsx**: Привести diff к форме
   - Если добавлена логика → переместить в контейнер
   - Проверить соответствие DESIGN.md (цвета, отступы, иконки)

5. [ ] **FFT-сервис**: Убедиться в модульности
   ```bash
   yarn turbo graph --filter=@membrana/fft-analyzer-service
   ```
   - Нет циклических зависимостей
   - Экспортирует только через `index.ts`

6. [ ] **Дублирование анализа**: Согласовать `sound-quality-viz` и `fft-analyzer-service`
   - Если обе анализируют FFT → извлечь в единый сервис с фасадом

### ℹ️ ИНФОРМАЦИОННЫЕ (техдолг / next sprint)

7. [ ] Аннотировать `scripts/sync-github-token.mjs` в коммите или README
8. [ ] Добавить параметры FFT (окна, размер, частота дискретизации) в README каждого сервиса
9. [ ] Покрыть edge cases в тестах (тишина, насыщение, шум)
```

---

## Definition of Done

- ✅ Все тесты проходят: `yarn test`
- ✅ Линтер чистый: `yarn lint`
- ✅ Нет импортов из `packages/temp/` в исходный код
- ✅ `AppFooter.tsx` соответствует DESIGN.md
- ✅ FFT-сервис не имеет циклических зависимостей
- ✅ Граница между `fft-analyzer` и `sound-quality-viz` ясна (нет дублирования)
- ✅ Коммит-сообщение содержит ссылку на GitHub Issue и Linear-ID
- ✅ **Teamlead** дал LGTM перед мерджем в `main`

---

**Следующий шаг:** Вызвать `yarn ask vesnin --gh-issue 45 "какие параметры FFT и диапазон частот для детектора дронов финализировать?"` чтобы зафиксировать контракт перед пуллом в `main`.