# REPORT — Шторм: сущность процедуры — фреймы

| Поле | Значение |
|------|----------|
| Дом | `docs/storm/storm-cold-start-entry-2026-07-22/` |
| Открыт / закрыт | 2026-07-22, один день; закрыт **по развилке** (S-S2) при 5/7 вдохах — потолок не потребовался |
| Секретарь | Ангелина (инлайн, фоновой нет — названо при открытии) |
| Вход | прецеденты cold-start-autostart 22.07 (3-й рецидив) + mechanics-pass-value-fail 21.07 |
| Механика вдохов | `storm-breath.mjs` (K0=1 · Kmin=1 · c=1), вещдок `breath-state.json`; закрытие честно НЕ вдох (d=0) |
| Развилка (слово владельца) | **Спринт «фреймы»** — конспект передаётся входом |

## Подтверждённые тезисы (все — слова владельца, провенанс в THESES)

| # | Тезис |
|---|-------|
| T1 | Уровень поднят: не чинить утро, а доработать сущность процедуры |
| T2 | Определения: процедура = цепочка команд под сценарий; фрейм = элемент сценария с ответственным лицом и версией по PINNED_SUBGRAPH_VERSIONING |
| T3 | Прецедент упаковывается во фрейм → рефакторинг заказывается лицу фрейма |
| T4 | Провода к процедуре — часть процедуры; фрейм-подводка — недостающий элемент (3 дня без хозяина) |
| T5 | Смысл фрейма — адресный ремонт вместо инлайн-фикса «первой попавшейся сессией» |
| T6 | Процедура — буквально очередь фреймов (полная декомпозиция сценария) |
| T7 | Родовой узор: контейнер = список/стек + версионируемый сабграф; в скриптах сабграф — КИТ, в процедурах — ФРЕЙМ |
| T8 | Гранулярность пина — отрезок (строка/абзац), не документ; фрейм = набор разрозненных отрезков |
| T9 | Оговорка снята: сам контейнер не пинится (снимковая дисциплина), сабграф под контейнером — не оверкод; паттерн уточнить |
| T10 | Лицо фрейма выводится из структуры процедуры; подводка утра — Ожегов |
| T11 | Развилка: спринт «фреймы» |

## Кандидаты (сырьё для спринта)

K1 (Математик): механика пина отрезка — (файл, якорь/диапазон, хэш отрезка); дрейф отрезка ≠ дрейф файла.

## Развилка

**Спринт «фреймы»** (вариант «а», слово владельца): `frames[]` в манифесте
процедуры (id, лицо, пин), механика пина отрезков (K1), упаковка прецедента
22.07 в первый фрейм «подводка утра» → ремонт дверей (CLAUDE.md, зеркала
developer-rhythm) руками Ожегова.

## Осталось нерешённым

1. Напряжение «автостарт: механика можно, интерпретация нельзя» — формально не
   чеканено тезисом (память 22.07 vs T7 шторма 21.07); первый фрейм подводки
   его материализует, но словарная фиксация — за спринтом/владельцем.
2. Обновление паттернов (T9) — правка PINNED_SUBGRAPH_VERSIONING в спринте.
3. Судьба прецедента 22.07 (untracked в основном дереве) — донести до main.

## Handoff

Конспект → вход спринта «фреймы» (регистрация карточки — следующий шаг вне шторма).
Директив с двумя подписями шторм не порождал.
