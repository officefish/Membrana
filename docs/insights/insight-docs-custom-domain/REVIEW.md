# REVIEW — insight-docs-custom-domain

## Формальная панель НЕ прогнана (честно)

`yarn insight review` вызывает Anthropic API — 22.07 вернул **лимит исчерпан** (доступ
вернётся 2026-08-01). Независимую пятиролевую панель тул сгенерировать не смог.

**Решение владельца (22.07): не гонять ревью ради ревью — решать по research.** Основание:
для этого инсайта панель низкосигнальна. Существа для спора нет — research закрыл механику
(CNAME + TXT + авто-SSL), **репо-side вклад нулевой** (контент влит, ссылки зелёные, граница
публичности проверена). Единственная настоящая развилка — **тариф Mintlify** — не решается
виртуальной панелью, только аккаунтом владельца.

## Решение (decide, презентационный уровень)

- **Статус: adopted** (по слову владельца 22.07).
- **Типизированный мандат (ось D) не материализован** — тот же tooling-gap [#963](https://github.com/officefish/Membrana/issues/963):
  `yarn insight decide` не заводит мандат для greenfield-инсайта. Связь фиксируется здесь и
  презентационно; ось D закрыть после фикса #963.

## Следующий шаг

**Не код-спринт.** Репо-side выход выполнен: чек-лист
[`apps/docs/CUSTOM_DOMAIN_SETUP.md`](../../../apps/docs/CUSTOM_DOMAIN_SETUP.md) +
проверка границы публичности (только `apps/docs` наружу; агент-truth приватен).

Два шага — **за владельцем** (вне репозитория):
1. Свериться по тарифу Mintlify (гейт: доступен ли custom domain на текущем плане).
2. Добавить DNS-записи (`CNAME docs` + TXT) по чек-листу; не трогать `office.`.

Плюс гейт до включения публичности — проверен: первые строки README контейнеров
публично-безопасны (они попадают в атлас-страницу).
