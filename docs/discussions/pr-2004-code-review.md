<!-- Сгенерировано: 2026-08-20T11:24:41.024Z (yarn code-review; pr, pr-2004, llm-anthropic) -->

<!-- review-verdict sha:d4120e64503ea993ac9484799c091d4fab938283 base:776e9d67fe8c5936cee34eb7f3f590dbb2d677ab verdict:LGTM lead:ozhegov at:2026-08-20T11:24:41.024Z -->

<!-- review-source
  source: gh-compare
  base_ref: main
  merge_base: 776e9d67fe8c5936cee34eb7f3f590dbb2d677ab
  head_sha: d4120e64503ea993ac9484799c091d4fab938283
  head_match: false
  files: 10
  truncated: false
-->

Tier: T1

**PR size: oversized (~644 lines, target ≤400)**

---

**[Ozhegov — ведущий ревью]:** Пропуск.

Бестиарий пройден по диффу:

- **B1** — нет LLM-конкатенации в хвосте.
- **B2** — `samples.module.ts` (+2 строки, `exports`) — правка минимальна, не перекрёсток.
- **B3** — DoD в OPEN.md обновлён фактом («подпись Ожегова 19.08, очередь эфемерна — названо»), не механикой.
- **B4** — посылки ссылаются на существующие символы (`TaskQueueService`, `FirebatNodeController`); «ждёт» осталось только в b4–b6.
- **B6** — `complete` при неизвестном taskId возвращает `unknown_task`; зуб это ловит явно — молчания нет.
- **B8** — `NodeKeyGuard` объявлен в модуле и закреплён в комментарии; словарь `POLL_OUTCOMES/TASK_KINDS/TASK_STATES` экспортирован из `index.ts`.
- **B9** — утверждение «эфемерная очередь, переживает только процесс» подкреплено шапкой `task-queue.service.ts` и записью в `discussions/`; прозой без носителя не висит.
- **B10** — флаг паузы устройства `(б)` назван отсутствующим явно («не заведён — названо»), не заглушкой.

Единственная структурная пометка (не блокер): `backoff` оператору при `enqueue` описан как `queue_full` в типе, но в реальности потолок не достигается — старейшее молча роняется и новое принимается (`outcome: 'ok'`). Тип `EnqueueResult` объявляет ветку `backoff | queue_full`, которая в текущей реализации никогда не возвращается. Это расхождение контракта с реализацией — P2, не блокер, но тикетом зафиксировать.

---

**[Teamlead]:** Tier T1. PR size oversized (~644 lines, +244 сверх цели) — обоснование принято: b3 атомарен по спринту (ADR-0027 Р1/Р4), дальнейшее дробление нарушает логику блока. P1 рекомендация: следующие блоки b4/b5 держать в пределах 400.

Соответствие MAIN_DAY_ISSUE: PR закрывает `firebat-node-device` блок b3 — очередь заданий и API опроса. По таблице живых состояний #2004 MERGED — ревью post-factum; вердикт фиксирует качество для цепочки b4+.

C1 — границы `background-media` соблюдены; единственная новая межмодульная связь `firebat-node → samples` однонаправленна, зафиксирована в комментарии модуля. C4 — ядро без React, NestJS-модуль корректен. C7 — 11 зубов (очередь 7, контроллер 4), критичные ветки (backoff, stale, lease expiry, unknown task) покрыты. C8 — `console.log` отсутствует. C9 — секреты в диффе отсутствуют.

Один P2 (не блокер): ветка `{ outcome: 'backoff', reason: 'queue_full' }` в типе `EnqueueResult` недостижима — потолок обходится drop-oldest, не отказом оператору. Тип обещает поведение, которого нет. Рекомендация: либо убрать ветку из типа, либо добавить режим «жёсткий потолок» с реальным backoff — выбор словом, не молчанием. Завести тикетом до b5.

`yarn turbo run typecheck test --filter=@membrana/background-media` — судья CI.

**LGTM** (post-merge; блокеров нет; P2 — тикетом).

---

**[Структурщик]:** — (ведущий ревью).

**[Математик]:** — (DSP и чистые функции не затронуты).

**[Музыкант]:** — (Web Audio и аудио-путь не затронуты).

**[Верстальщик]:** — (UI не затронут).

---

**Итоговый артефакт:** `docs/discussions/pr-2004-code-review.md`

**Definition of Done:**
```
yarn turbo run typecheck test --filter=@membrana/background-media
```

**Риски:**
- P1 — oversized PR; принято по обоснованию; b4/b5 держать ≤400.
- P2 — `EnqueueResult.backoff` недостижима в текущей реализации; расхождение типа с поведением; зафиксировать тикетом до b5.

**Вердикт: LGTM**