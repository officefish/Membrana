# Code review — ветка `feat/vds-tooling-0715` (vds-panel-tooling, #485 п.1–3)

> Режим: **branch** (`--base origin/main`). Дата: 2026-07-15.
> **Фолбэк**: `node scripts/code-review.mjs --branch` вернул HTTP 400 — Anthropic
> без кредита (`req_011Cd3EB1QHeYHswocjFyU4L`). Ревью проведено в IDE-чате по
> `CODE_REVIEW_REGULATION.md` v0.2 (подсказку про фолбэк дал ti-7 из #469).

Tier: **T2** — скрипты чеканят owner-cookie и читают прод-секрет с VDS
(security-зона регламента), плюс трогают живой контур деплоя панели.

Diff: 9 файлов, +604/−12. PR size: **OK** (3 независимых ti + 2 попутные нормы).

---

[Teamlead]: Пакет закрывает п.1–3 ретро #485 и попутно п.3/п.7 ретро #476 — вместо
нормы «не забудь chmod» норма уехала в код. Ценность подтверждена не рассуждением,
а живыми прогонами на `msk-mmbrn-office` и фикстурой в песочнице VDS. Два дефекта,
найденные по ходу (libuv-ассерт с кодом 127 и бэктики, рвущие bash-шаблон),
доказывают, что проверка была настоящей, а не «выглядит правильно». Обе правки
ревью (таймер в `endConnection`, `exitCode` в usage-ветке) внесены до мёржа.
P0 нет. **LGTM**.

[Структурщик]: Границы соблюдены: `_ssh-office-exec` стоит на готовом
`_ssh-office-config` (#349) и повторяет форму `_ssh-media-exec` — новой обвязки
ssh2 не заведено, слой один. Чистые функции (`stripCarriageReturns`,
`buildScriptCommand`, `normalizeArgv`, `mintOwnerToken`) отделены от I/O и потому
покрыты тестами без сети. Гард `process.argv[1]?.endsWith(...)` в обоих скриптах
держит модульный импорт безопасным — без него тест увёл бы смоук на живой прод.
Скрипты не импортируют internals пакетов: сверка с ядром `panel-auth` идёт через
собранный dist и деградирует в skip, а не в ложную зелень.

[Математик]: `permVersion` проверяется строгим неравенством `>` от снятого до
правки значения, а не «!= 0» — эпоха ловится корректно. HMAC-вектор фиксирован
детерминированно (тот же вход → тот же токен), плюс проверено, что смена секрета
меняет подпись при том же payload, а протухший TTL ядро отвергает. Квотирование
аргументов проверено на трёх злых случаях: пробел, апостроф, `$(whoami)`.

[Музыкант]: Не применимо — audio path не затронут. Единственное касание аудио
здесь — `dist/compare-audio` как статика: и это как раз главный содержательный
фикс, бандл из 120 wav вне git больше не сносится выкладкой.

[Верстальщик]: Не применимо — UI не затронут. Косвенно чинит выдачу статики:
`chmod -R a+rX` убирает ложный `index.html` вместо `/assets` и `/compare-data`.

---

## Риски

| Severity | Находка | Статус |
|---|---|---|
| **P1** | `endConnection` мог ждать `close` вечно, если сервер закрыл соединение сам (второго события на `end()` нет) — молчаливое зависание | **исправлено до мёржа**: `setTimeout(5s).unref()` + флаг `settled` |
| **P1** | В usage-ветке остался `process.exit(1)` — тот самый паттерн, что ронял libuv под пайпом | **исправлено до мёржа**: `exitCode` + `else`-ветка |
| **P2** | `stripCarriageReturns` не трогает одиночный `\r` (классический Mac-EOL) | принято осознанно, зафиксировано тестом |
| **P2** | `exec` отдаёт `code ?? 0`: команда, убитая сигналом, вернёт 0 | принято; для смоука/деплоя сигнал не сценарий |
| **P2** | Секрет читается `cut -d= -f2-` — кавычки в `office.env` попали бы в значение | не воспроизводится: `ensure_env` пишет через `printf '%s=%s'` без кавычек |
| **P2** | Гард дрейфа пропускается (skip), если `background-office/dist` не собран | честный skip вместо ложной зелени; `yarn office:build` включает проверку |

## Definition of Done

- [x] `yarn office:ssh` / `yarn vds:run` — живой прогон: CRLF-скрипт отработал,
      аргументы с пробелами и кириллицей целы, коды 7/3 пробрасываются, `/tmp` чист.
- [x] `_ssh-panel-deploy` — фикстура в песочнице VDS: аудио переживает выкладку,
      права 700→755 читаются не-root, stale-tar игнорируется, `bash -n` зелёный.
- [x] `_ssh-panel-smoke --read-only` на `panel.mmbrn.tech`: owner 200, аноним 404.
- [x] `yarn test:scripts` — 321/321.
- [ ] Полный (пишущий) прогон смоука на проде — **ждёт слова владельца**: оставляет
      в сторе отозванного партнёра и промокод (строки не удаляются).

## Вердикт: **LGTM**

---

## Ревью-2: ti-4/5/6 (дельта, тот же день)

Tier: **T2** — гард закрытия GitHub Issues и гейт closure-ревью: цена ошибки —
молча закрытый эпик или пропущенный P0 в LGTM.

Diff дельты: 10 файлов, +224/−13.

[Teamlead]: Дельта добивает #485 до всех 6 пунктов, поэтому `githubIssue` карточки
поднят с null на 485 — привязка стала корректной. ti-5 оказался существенно
крупнее заявленного в ретро («вынулил 3 фазы» → на деле 189 носителей, 33 в
очереди), и это меняет вес пункта: механизм уже срабатывал массово (#131 и #144
закрыты в одну секунду). Одна дыра, открытая самой правкой ti-4, найдена ревью и
закрыта до мёржа. P0 нет. **LGTM**.

[Структурщик]: Гард поставлен в `listPendingGithubClose`, то есть на пути ВСЕХ
потребителей очереди (`task:close-github` и утренний `_main-day-issue`), а не в
одном скрипте — обойти его случайно нельзя. Наивный тест «коллизий не бывает» был
невозможен: он упал бы на 189 живых строках. Поэтому регрессия сформулирована
инвариантом «в очередь не попадает ни один Issue эпика» — он держится и на
исторических данных, и на будущих. `findEpicIssueCollisions` не спотыкается о
битый `parentEpic` (в реестре 6 таких) и об эпик без Issue.

[Математик]: Гард `hasP0P1Blockers` — предикат с явной асимметрией цены:
fail-closed, всё неопознанное считается блокером. Разбор случаев полный: тире
одиноко → отрицание; тире с продолжением → список блокеров; словесное отрицание
допускает пояснение. 20 формулировок проверены явно, включая обе живые.

[Музыкант]: Не применимо.

[Верстальщик]: Не применимо.

## Риски дельты

| Severity | Находка | Статус |
|---|---|---|
| **P1** | Правка ti-4 сделала гард **fail-open**: `[—–-]` матчил тире + любой текст, поэтому `P0/P1: - гонка в сторе` (markdown-пункт) читался как «блокеров нет» → настоящий P0 прошёл бы в LGTM. Хуже исходного бага | **исправлено до мёржа**: тире засчитывается только одиноко; тест на `- гонка в сторе` → блокер |
| **P1** | `\b` в JS определён по ASCII: после кириллического «нет» границы слова нет вовсе, первая версия гарда не распознавала русское отрицание | **исправлено до мёржа**: юникод-лукахед `(?![\p{L}\p{N}])` |
| **P2** | 33 заблокированные фазы не получат `githubIssueClosedAt` и будут показывать варнинг, пока реестр не починят руками | принято осознанно: варнинг — это и есть напоминание починить поле |
| **P2** | Данные реестра не чинятся этим PR (189 носителей остаются) | вне скоупа: правка 189 строк — отдельная задача с ревью; гард уже снимает вред |

## DoD дельты

- [x] `yarn task:close-github:dry` на живом реестре: 33 фазы заблокированы с
      разбивкой по эпикам, очередь 53 → 20 законных, сам эпик #438 в очереди остался.
- [x] `task:review:prepare --dry-run`: stderr пуст, exit 0 (ложный красный снят).
- [x] `hasP0P1Blockers` — 20 формулировок, включая обе живые и fail-open-случай.
- [x] `yarn test:scripts` — 330/330 (было 321 на старте дня).

## Вердикт дельты: **LGTM**

Follow-up (не блокирует merge):

1. Тот же libuv-ассерт (код 127) наблюдался у `scripts/code-review.mjs` — класс
   бага шире этого пакета, стоит отдельным XS-проходом по `process.exit()`.
2. Починить данные реестра: 189 фаз-носителей `githubIssue` эпика (33 архивных
   в очереди). Гард уже не даёт им навредить, но поле остаётся битым.
3. Остаток #476: merge-driver реестров, `pr:land`, mojibake-гейт, Tier из
   манифеста, ветко-гард сессии.
