# Обсуждение: block-resolution-core-ozhegov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-03 11:55 UTC · ozhegov

**Контекст:** scripts/lib/worktree-resolution.mjs
**Вопрос:** Два блока за тобой в спринте worktree-foreign-resolution-probe (#1647): resolution-core (чистый предикат: classifyResolution — own|foreign|absent, битые симлинки отдельным числом broken, foreignSample детерминирован сортировкой и урезан лимитом 3; ФС-обход инъекцией из проводов, ядро путей не читает) и два провода — worktree-sync (строка резолюции по каждому дереву K2 после строки синхрона, находка не exit) и prepush-typecheck-scope (строка ПЕРЕД вердиктом, печатается только при НЕ-own, сторож обёрнут в try — его осечка не роняет push). Живые прогоны: дерево tooling — своих 1 чужих 36, angelina/work — 2/35, деревья со своим node_modules — «все 37 внутрь», llm-panel без node_modules — «замер не состоялся (это не всё своё)». Ответь как структурщик коротко: 1) слова строки — «МЕЖПАКЕТНЫЙ typecheck локально недостоверен: tsc читает чужой dist; однопакетный честен; сквозная проверка — CI» — точны ли; 2) в pre-push строка молчит на own — а в worktree-sync печатается всегда: оправдана ли асимметрия; 3) absent-строка «это не всё своё» — не перестраховка ли.

**Ответ:**

[память:подсознание] emerge ozhegov-2026-08-01-evening-review-predicate-m3-volume-measure — тот же ход: различать функцию на элементе и агрегат по множеству; здесь — «межпакетный» vs «локальный typecheck» как разные области определения предиката, нельзя схлопывать.
[память:подсознание] emerge ozhegov-2026-07-30-task-archive-cold-store-m0-order — правило «отсутствие данных ≠ данные об отсутствии»; absent-состояние держится ровно им, поэтому третий вопрос — не перестраховка, а закрытие тернарности.

По существу.

1) Слова точны, с одной оговоркой. «МЕЖПАКЕТНЫЙ» — правильно суженная лемма (граница пакета = граница словаря; однопакетная правка не оболгана). «tsc читает чужой dist» — точное определение через механизм, не через ассоциацию. «однопакетный честен» — необходимая контр-статья, иначе читатель обобщит. «сквозная проверка — CI» — корректный указатель на инстанцию истины. Одно уточнение: «чужой dist» верно для типового случая (`types` из `dist/`), но пакет может резолвиться и в исходники соседнего дерева через `exports`. Если хотите герметичности — «tsc читает артефакт чужого дерева»; если оставить конкретику — так и оставить, это самый частый случай, и он объясняет молчание зелёного tsc лучше абстракции. Не блокирую.

2) Асимметрия оправдана и вытекает из назначения проводов, а не из вкуса.
- `worktree-sync` — инвентарный обход K2: сводка per-tree; молчание на own = «не мерили» неотличимо от «всё своё». Печатать всегда — закрытие тернарности.
- `prepush-typecheck-scope` — сторож у ворот одной операции; own — норма, шум на норме девальвирует сигнал (тот же принцип, что у зубов: говорить, когда есть что сказать). Абсолютный факт резолюции доступен через sync, дублировать в pre-push незачем.
Разные области определения — разные контракты вывода. Это не рассинхрон, это разделение ролей.

3) Не перестраховка. Это третье состояние предиката, а не оговорка к двум. Без него `absent` схлопнется в `own` (молча) или в `foreign` (ложно), и прибор потеряет свойство, ради которого сделан: снимать молчание. Скобка «(это не всё своё)» — определение через отрицание смежной леммы, ровно по делу — читатель по инерции прочтёт «нет чужих = всё своё». Оставить.

Рисков связанности не вижу: ядро без ФС, инъекция из проводов, `foreignSample` детерминирован — граница Веснина держится.

---
