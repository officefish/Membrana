# Блок b2-cabinet-promo-ui · Родченко · 2026-08-12

## Контекст-прогон (до кода, след в op-log/rodchenko ~17:33Z)

Форма: внутри карточки «Тариф» (регулярное действие, не модалка); успех — только после
ответа сервера (оптимизма нет, emerge tariff-grid-m6-switch: «фальшивого переключения не
показываем»), рефетч данных мембраны без reload; все отказы одним alert-error — различие
в тексте, не в цвете; словарь причин вне компонента.

## Ревью-проход (по построенному, ~17:40Z)

Форма принята. Разборы: (1) успех виден ДВУМЯ линиями — карточка тарифа обновляется
рефетчем (первая), alert-success «Тариф переключён» (вторая); (2) словарь — чистый map
без React/i18n-инжекта в `src/lib` рядом с `formatBytes` (дом follower-словарей);
локаль — когда появится, не раньше; (3) форма после успеха НЕ глушится сознательно:
у пользователя может быть второй код на тариф выше, а повтор того же кода честно
отвечает `same_tariff`/`promo_already_redeemed` различимым текстом.

Зубы словаря: полнота 1:1 по 12 причинам, попарная различимость, promo_unknown ≠
promo_already_redeemed, fallback «Неизвестная причина: <код>» не молчит.
Кабинет: typecheck + test зелены (12 задач turbo).
