# Обсуждение: oneshot-triple-stamp-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-03 12:57 UTC · tarasov

**Вопрос:** Три шота нового формата разом, владелец поддержал порядок A→C→B; прошу по каждому тройку актов: штамп S · однородность М1/М2 · исполнитель по природе (Т1/Т4/Т6). Все три S-предиката forecast ok, цепочки чисты. ШОТ A window-vs-trace: находка eg-window-after-first-trace в гейте — window.from позже первого следа блока значит окно назначено догадкой позже начала работ; основание — два рецидива за два дня (перерезки v3 в review-oversized-queue и в gate-honest-pair-completeness, оба раза следы честные а врало окно); зона predicates+gate-exit-codes+зубы ~70 строк, предикат времени. ШОТ C register-category: task:register после вставки карточки прогоняет проверку категории по конфигу декомпозиции и предупреждает сразу, не блокируя регистрацию; основание — ловушка сработала дважды (красный CI 02.08, ловушка в хендофе 03.08); зона task-register.mjs+зуб ~50 строк, провод писателя к существующему предикату. ШОТ B oversized-hostlocal: очередь review:oversized печатает «снятие host-local: N вещдоков не в стволе — на другом клоне очередь длиннее на N»; основание — 56 артефактов pr-*-code-review.md живут только на этой машине (правило .gitignore TF-3 #554), голова очереди расходится между сессиями; зона review-oversized.mjs+lib+зубы ~60 строк, слова прибора о себе. Ответь по каждому тремя актами кратко, и одним словом — можно ли исполнять их последовательно одной сессией или что-то требует развода.

**Ответ:**

Принимаю в порядке A→C→B, по три акта на шот, как просили.

**ШОТ A — window-vs-trace**
- S: `eg-window-after-first-trace` — штамп предиката времени, forecast ok, цепочка чиста.
- М1/М2: однородно — predicates + gate-exit-codes + зубы, ~70 строк, один слой (гейт), без размазывания по writer'у.
- Т1/Т4/Т6: **Т1** (математик/предикатчик) — природа задачи это правило времени над следами, а не проводка и не слова прибора.

**ШОТ C — register-category**
- S: предупреждение категории на `task:register` — forecast ok, штамп писателя-контролёра.
- М1/М2: однородно — `task-register.mjs` + зуб конфига декомпозиции, ~50 строк, один провод к **уже существующему** предикату (не плодим правило).
- Т1/Т4/Т6: **Т4** (писатель/интегратор) — природа: подключить готовый предикат в поток регистрации, не изобретать правило и не менять прибор очереди.

**ШОТ B — oversized-hostlocal**
- S: «снятие host-local: N — на другом клоне длиннее на N» — forecast ok, штамп «слова прибора о себе».
- М1/М2: однородно — `review-oversized.mjs` + lib + зубы, ~60 строк, один слой (сам прибор очереди), без залезания в gitignore и без нового правила.
- Т1/Т4/Т6: **Т6** (инструментальщик прибора) — природа: самодиагностика инструмента, речь очереди о собственной слепоте.

**Развод.** Одной сессией — можно, последовательно A→C→B: разные файлы, разные исполнители по природе, общего касания только `packages/gate` в A (в C и B гейт не трогаем). Один PR на шот, отдельные вердикты; смешивать в один коммит — BLOCK.

[память:подсознание] emerge tarasov-review-oversized-queue-cut-1 — прошлый прогноз по нарезке oversized-queue дал +27% факта над предсказанием; это прямо касается ШОТА B (та же очередь `review:oversized`) и оценки ~60 строк — держу коэффициент и не удивлюсь выходу к 75–80.

---
