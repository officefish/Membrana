# Обсуждение: cut-review-oversized-queue-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-02 11:46 UTC · tarasov

**Контекст:** docs/prompts/REVIEW_OVERSIZED_QUEUE_PROMPT.md
**Вопрос:** Ты режешь спринт review-oversized-queue. Замер 02.08: 185 oversized-коммитов с 20.07, с артефактом ревью 17, без — 168; по природе 60 только docs, 6 только код, 119 смешанные. В хендофе значилось 16 — цифра не воспроизводится. Моя черновая нарезка — четыре блока: (1) oversized-queue-core, dynin, scripts/lib/review-oversized-queue.mjs, 110 строк — класс code|docs|mixed, риск по строкам КОДА а не всего диффа, снятие по наличию артефакта ревью, детерминированный порядок; (2) oversized-queue-tooth, dynin, scripts/lib/review-oversized-queue.test.mjs, 150 строк; (3) oversized-queue-verb, ozhegov, scripts/review-oversized.mjs, 110 строк — глагол печатает очередь и ЧИСЛО отброшенных с причиной; (4) oversized-first-review, ozhegov, docs/discussions/pr-XXXX-code-review.md, 120 строк — верхний элемент очереди ревьюируется сегодня прогоном, иначе спринт закроется зелёным не создав того ради чего заведён. Ответь по пунктам: 1) верна ли граница блоков; 2) объёмы; 3) блок 4 — его зона не известна заранее, имя файла зависит от того что скажет очередь из блока 3: законно ли это в нарезке или это дефект контракта; 4) риск по строкам кода — правильная ли мерка, или oversized-документ тоже стоит ревью; 5) чего в нарезке нет, но по DoD обязано быть.

**Ответ:**

Положение: нарезка близка к рабочей, но в двух местах она провисает — блок 4 и мерка риска. По пунктам.

1) **Граница блоков — верна.** Ядро (predicate + queue) отдельно от глагола отдельно от применения — это правильный слоёный разрез. Тесты рядом с ядром, а не с глаголом, — тоже верно: глагол должен быть тонким, тестируется ядро.

2) **Объёмы.** Ядро 110 — реалистично, если снятие по артефакту читает `docs/discussions/pr-*-code-review.md` списком, а не парсит. Тесты 150 — маловато для пяти областей из таблицы + фикстуры; закладывай 180–220. Глагол 110 — ок, если только печать; если добавляешь `--json` или фильтры — вырастет. Блок 4 — 120 строк ревью — это средний ритуальный объём, ок.

3) **Блок 4 — дефект контракта, а не законный слепок.** В нарезке имя артефакта должно быть детерминировано на момент планирования, иначе Definition of Done нельзя проверить постфактум. Чинится так: блок 3 в PR выдаёт очередь как отдельный commit **до** блока 4, и в карточке блока 4 фиксируется `pr-<N>` по факту — это норма, но она должна быть **явно** прописана в промпте, не подразумеваться. Иначе повторим болезнь 02.08: обещание вместо прогона.

4) **Мерка риска — строки кода правильно, но неполно.** Oversized-документ ревью не требует по существу (архив, ритуал — прецедент #1624 уже это признал), но `mixed` с большой докой и маленьким кодом сейчас у тебя утонет вниз очереди — а именно там прячется кодовое изменение под ворохом markdown. Добавь второй ключ сортировки: при равном риске по коду — доля кода в дифе (выше доля → выше приоритет). Это удержит смешанные с реальным кодом наверху.

5) **Чего не хватает по DoD:**
   - Норма #1565 в промпте есть, но в нарезке не отражена: **проверка утверждений об отсутствии командой** — это отдельная строчка в шаблоне ревью (блок 4) и, желательно, чек-лист в `docs/discussions/`. Иначе повторим прецедент 02.08.
   - Импорт `OVERSIZED_CHANGED_LINES` из `day-work-diff.mjs` — упомянут запретом, но в блоке 1 должен быть явным пунктом реализации и тестом на «мерка одна».
   - Число отброшенных с **причиной** в блоке 3 — сформулируй формат вывода в acceptance, иначе получишь «60 docs skipped» без разбивки, и это пройдёт.

Вердикт: **BLOCK** до правки блока 4 (детерминизация имени артефакта через межблочный порядок) и добавления вторичного ключа сортировки в блок 1. Связка: Дынин ведёт 1+2, Ожегов — 3, потом 4 на выходе очереди. Приём — мой.

---
