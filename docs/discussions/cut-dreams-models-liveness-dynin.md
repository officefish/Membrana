# Обсуждение: cut-dreams-models-liveness-dynin

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-10 15:30 UTC · dynin

**Контекст:** C:/Users/USER19~1/AppData/Local/Temp/claude/c--Users-user190825-practice-Membrana-tooling/25c9417e-b1dd-4cc1-a8c0-d7de40958b11/scratchpad/ticket-dreams-models.md
**Вопрос:** Ты резчик этого спринта. Ответь по пяти вопросам конкретно и проверяемо: шов зуба к настоящему реестру и что краснеет на третью копию; дом и каденция проверки живости; поведение при недоступном провайдере; границы; порядок и объём блоков. Где я неправ — скажи прямо.

**Ответ:**

[память:подсознание] emerge dynin-2026-07-18-evening-auditor-doc2-report-generator — та же форма: аудит как чистая функция `audit(state)→{passed,failed,evidence}` с детерминированным входом. Здесь ровно тот же скелет: `check_liveness(registry)→{alive[],dead[],evidence[]}`, вход — множество id из реестра, выход — HTTP-ответы `/api/v1/models` с таймстампом. Без evidence зуб зелен на честном слове.

[память:подсознание] emerge dynin-2026-07-09-insight-hermes-liaison-agent — детерминизм сбора: одинаковый вход → одинаковый выход, байт в байт. Проверка живости обязана быть детерминированной по id, флейк-риск изолируется в отдельный глагол с фиксированным таймаутом и кодом возврата.

Теперь по делу. Ты прав в разделении двух дефектов; я бы усилил формулировку: **это два разных предиката, и каждый нуждается в своём гарде**. Смешивать их в один тест — снова строить вторую копию правды.

**1. Шов зуба к настоящему реестру.** Реестр `.mjs` — единственный носитель. Тест обязан импортировать `scripts/lib/dreams-providers.mjs` тем же способом, что и прод (динамический `import()` по file-URL из `dreams.service.ts:96-101`) — иначе снова разойдёмся. Третий аргумент `routes` из сигнатуры `synthesizeForProvider` **убрать** или пометить `@internal, only for adversarial tests`. Гард против третьей копии: отдельный тест-инвариант `no-hardcoded-model-ids.test.ts`, который грепает `packages/background-office/src/**/*.ts` на регексп `/(x-ai|google|anthropic|openai)\/[a-z0-9.-]+/i` и падает, если найдено вне `dreams-providers.mjs`. Простой, тупой, работает.

**2. Дом и каденция проверки живости.** Отдельный глагол `yarn dreams:probe-models` — читает реестр, дёргает `GET https://openrouter.ai/api/v1/models`, сверяет каждый id на присутствие в ответе. Не в юнит-тестах (флейк). Каденция: **ночь** (`vitest-nightly.yml` уже влит) + **ручной запуск при смене реестра** (git hook на изменение `dreams-providers.mjs` — предупреждение, не блок). В мердж-гейт **не ставить**: сеть на каждый PR = зелёный шум при первом же 5xx у провайдера. Когда ночной станет блокирующим (это отдельное решение Teamlead) — тогда пересмотреть.

**3. Недоступный OpenRouter.** Три исхода, не два: `alive` (200 + id найден), `dead` (200 + id отсутствует), `inconclusive` (сеть/5xx/таймаут). Коды возврата: `0` / `1` / `2`. Прецедент `execution-gate` — держаться его. `inconclusive` **не** красит ночь красным сам по себе, но пишет запись в `docs/truth/dreams-liveness.jsonl` с таймстампом; три `inconclusive` подряд за 72 часа = красный (правило троекратности, чтобы не будить по одному 502).

**4. Границы спринта.** Внутри: шов зуба, гард против третьей копии, глагол `dreams:probe-models`, запись в `dreams-liveness.jsonl`. Снаружи: интеграция с мердж-гейтом, автоматическое обновление реестра при `dead`, алертинг. Автозамена мёртвых id — соблазнительно и опасно: провайдер решает за нас, что «эквивалент». Оставить человеку.

**5. Порядок и объём.** Сначала гард (§1, ~20 строк теста) — фиксирует поверхность. Потом шов (переписать `dreams.service.test.ts:26-42` на импорт реестра, ~15 строк). Потом глагол (§2, отдельный `.mjs`, ~80 строк с ретраями и таймаутом). Потом каденция в ночь (~10 строк YAML). Итого один PR, ≤150 строк без учёта тестов probe-глагола.

Где ты неправ: формулировка «девять дней ложного диагноза стоили именно этого» — риторически сильна, но численно не проверена. Сколько раз за эти 9 дней зуб реально зеленел на CI при мёртвом проде? Если один — это не «стоило девяти дней», это «стоило одного прогона». Посчитай по логам Actions прежде, чем закладывать это в обоснование приоритета.

---
