# Обсуждение: tariff-matrix-2331-b2-ozhegov-context-v2

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-09-08 16:56 UTC · ozhegov

**Контекст:** GitHub Issue #2331: «Матрица тарифов как единственный источник правды: гранулы по ресурсу + шаблон в контейнере strategic-docs, сетка — производная (сессия Б, спринт)»
**Вопрос:** Прогон контекста по РАТИФИЦИРОВАННОЙ версии плана tariff-matrix-2331 (ратификация владельца 08.09 19:54 после перерезки b3; зона b2 matrix-granules-rights: гранулы nodes, workspaces, datasets, system-datasets, instruments). Ты — ответственный контекст b2. Прежний прогон (11:57) гейт счёл судившим другую версию контракта. Подтверди или поправь по ратифицированному плану: приборы 1/4/9 (exact-count, слова прибор/прибора/приборов), сценарии 3/3/3 (старшие — по аналогии с датчиком, не закреплено), наборы звуков — свой у датчика и блокпоста, у наблюдательного пункта заём набора блокпоста словами (stub, ratifiedAt null у значения), системные наборы — отдельная гранула вне квоты (matrix-only, в сетку не едет), инструменты — составная гранула (enabled-flag, gated-by-precondition, produce-scope). Что словарно главное и есть ли возражение. Коротко.

**Ответ:**

Всплытие: [id=ozhegov-2026-07-29-tariff-grid-m3-tri] — леммы entitlement/precondition/гейта из tariff-grid повторяются здесь в форме гранул прав.

Словарно главное — носитель заёма. В ратифицированном плане у наблюдательного пункта значение `catalogId` = набор блокпоста **с полем stub** {since, reason, resolvesBy}, `ratifiedAt: null` у значения. Это честно: ячейка полна (кабинет читает checkpoint-v1-catalog), но паспорт говорит «не закреплено, заём». Не путать с `gated-by-precondition` — это другое: не «что выдано», а «при каком условии действует».

**Возражений нет**, форма проходит. Уточнение по системным наборам: гранула `tariff-system-datasets` — matrix-only, в сетку не едет. Значит, `tariff-grid.json` (производная) её не содержит — и зуб сверки это учитывает. Если сетка вдруг получит системные наборы — это порча, а не дополнение.

Термины в порядке: `exact-count` для приборов, `enabled-flag` для инструментов, `produce-scope` для produce.own. Сценарии 3/3/3 у старших — «по аналогии», ratifiedAt null — нижняя граница, не норма. Всё сходится.

Вердикт: **pass** по ратифицированному плану.

---
