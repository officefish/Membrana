# Обсуждение: block-e2-oversized-status-dynin

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-05 21:13 UTC · dynin

**Вопрос:** Ты держатель блока e2-oversized-status-source спринта review-honesty (ратифицирован владельцем 05.08 21:05Z). Зона: scripts/lib/review-oversized-queue.mjs, scripts/review-oversized.mjs, scripts/review-oversized-queue.test.mjs (новый). Оценка ~120 строк. Предикат снятия — твой предмет.

**Ответ:**

[память:подсознание] emerge dynin-2026-08-03-one-shot-manifest-m4-homogeneity — там я формализовал предикат `pathFamily(p)` и однородность `families(shot)` над списком путей; ровно тот же жанр — предикат-функция над множеством файлов очереди. Переиспользую форму.

[память:подсознание] emerge dynin-2026-08-01-evening-review-predicate-m4-memoization — там разводил два несовместимых предиката над одним органом. Для oversized-очереди тот же риск: «снят» может значить и «удалён из файла-источника», и «покрыт ревью». Разводим явно.

**Предикат снятия (черновик).**

Пусть `Q(t)` — множество записей в источнике очереди на момент `t`. Каждая запись `q = (path, sizeBytes, addedAt, reason)`.

Определяю два **разных** предиката, и настаиваю на именовании обоих:

- `removed(q, t) ⇔ q ∉ Q(t)` — механическое отсутствие в файле-источнике.
- `resolved(q, t) ⇔ removed(q, t) ∧ ∃ evidence(q)`, где `evidence(q)` — одно из: (a) размер `path` в HEAD ≤ порога, (b) файл удалён из репо, (c) явная запись `waived` с автором и датой в receipts.

Снятие без evidence — это `dropped`, а не `resolved`; тест должен ловить такой переход и красить его.

**Что нужно уточнить, прежде чем резать код (2 вопроса):**

1. Источник истины — это один файл (JSON/NDJSON) под `scripts/lib/…`, или снапшот собирается из репо на лету? От этого зависит, `Q(t)` — читаемо или вычисляемо.
2. Порог oversized — конфиг или константа? Если конфиг, `resolved` должен читать тот же источник, что и попадание в очередь, иначе предикат несимметричен по времени.

Дайте ответы — прикладываю чистую реализацию `isResolved(q, ctx)` + тест-таблицу (removed∧evidence, removed∧¬evidence, ¬removed, waived, size-below-threshold, file-deleted).

---
