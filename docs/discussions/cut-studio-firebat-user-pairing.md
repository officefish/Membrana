# Обсуждение: cut-studio-firebat-user-pairing

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-20 07:40 UTC · vesnin

**Вопрос:** Вопросы резчику: (1) форма дня — что здесь БЛОКИ РАЗРАБОТКИ, а что операционные шаги владельца (установка Studio на узел, вход, ключ)? Дай 4-7 блоков с персонами из реестра голосов (tarasov/vesnin/angelina/dynin/ozhegov/rodchenko/kuryokhin), зонами-файлами и оценками строк; (2) развилка словарей: mediaToken служебный у КАЖДОГО спаренного клиента vs NodeKey per-device — чинить сегодня (media выдаёт per-device ключ при регистрации через мост кабинета?) или зафиксировать ADR-ом/Issue и не мешать магистрали дня? Учти: менять контракт PairResponse = трогать cabinet+client+media разом, день не резиновый; (3) чем доказываем «запись в наборе пользователя» — какой вещдок приёмки? Коротко, по пунктам.

**Ответ:**

[память:подсознание] emerge vesnin-2026-06-17-desktop-product-line — прямая связка: Studio как Electron-оболочка над apps/client + FS для media/journal, там же зафиксировал границу Studio/Device — сегодня она нагружается pairing-контрактом.
[память:подсознание] emerge vesnin-2026-07-29-night-triage-insight-harvest — «одна идея, не веер»: сегодня магистраль — постановка Studio+ключ, а не переписывание словаря токенов; развилка mediaToken vs NodeKey хочет утащить день.

Конструкция дня. Магистраль — «пользовательский узел работает через Studio по ключу из кабинета и пишет в свой набор». Всё, что не на этой линии, — ADR/Issue, не код дня.

**Блоки (5):**
1. **B1 · Установка и вход на Firebat** — операционный шаг владельца, не блок разработки. Артефакт: скрин Studio на узле + trail установки. 0 строк кода. Персона отчёта: **tarasov** (приёмка).
2. **B2 · Pairing E2E через Studio** (`apps/client/src/features/pairing/*`, `apps/client/electron/*` в части IPC хранения токенов) — проверка, что MembranePairingPanel + api/pairing работают внутри Electron (secure storage токена, а не localStorage «в воздухе»). ~80–150 строк, преимущественно IPC-мост и хранение. Персона: **dynin**, ревью границ — я.
3. **B3 · Датасет = набор пользователя** (`apps/client/src/features/media/*`, ensure-reserved после pair) — после успешного pair клиент садится на `membrane.id` + `mediaApiUrl`, первая запись падает в reserved-коллекцию пользователя. ~60–120 строк. Персона: **ozhegov** (media-контур), сцена приёмки — **angelina**.
4. **B4 · Вещдок приёмки** — скрипт/чек-лист: (а) `POST /v1/pair` → 200 с `membrane.id=<ожидаемый>`; (б) в media GET коллекций под `membraneId` показывает reserved-набор; (в) запись из Studio → файл виден в наборе владельца с корректным `deviceId`; (г) trail-запись дня с этими тремя пруфами. ~40 строк скрипта + md. Персона: **tarasov** (вердикт), сборка — **rodchenko**.
5. **B5 · ADR-0028 «PairResponse.mediaToken → per-device»** — только документ, кода не трогаем. Фиксируем: сегодня mediaToken остаётся служебным (тех-долг класса риска), завтра — per-device по образцу NodeKey из ADR-0027. ~1 файл, 60–100 строк. Персона: я, соавтор — **ozhegov**.

**Развилка (2).** Не чинить сегодня. Контракт PairResponse трогает cabinet+client+media одновременно — это трёхсторонняя правка, магистраль порвётся. Форма не выдержит нагрузки: «поставить Studio на узел» и «перекроить словарь токенов» — две L-задачи, не одна. Решение: **B5 (ADR) сегодня, реализация — отдельный спринт `media-per-device-token` со ссылкой на ADR-0027 как образец (таблица ключей, guard, отзыв)**. Риск фиксируем явно в trail, чтобы завтра не забылось.

**Вещдок приёмки (3).** Три пункта, все машинно-проверяемые:
- pair-response лог с `membrane.id`, `deviceId`, `pairedKeyId` (не токены — их маскируем);
- media GET по `membraneId` показывает reserved-коллекцию пользователя (id + owner);
- первая запись из Studio на Firebat → файл в этой коллекции, `deviceId` совпадает с pair-response.

Рекомендация: **форма** — правка не требует ADR-канона (ADR-0028 идёт как плановый техдолг, не блокер). Консилиум не нужен.

---
## 2026-08-20 07:43 UTC · vesnin

**Вопрос:** Заход 2 резчику studio-firebat-user-pairing. Уточнение зон по коду: креды парринга (включая служебный mediaToken!) живут в apps/client/src/stores/nodeConnectionStore.ts через localStorage — в Electron это плоский файл на диске узла; панели в apps/client/src/components/node-connection/; api в apps/client/src/api/pairing.ts + pairingCredentials.ts; Studio IPC — apps/membrana-studio/src/main.ts и preload.ts. Кастинг: B2 (хранение кредов в Electron: safeStorage через preload вместо голого localStorage ИЛИ фиксация «localStorage до ADR-0028» с сужением скоупа) ты отдал Дынину — он математик чистых функций; предлагаю B2 → ozhegov (границы/словарь хранения) или rodchenko (клиентский код), но ozhegov у тебя уже на B3. Подтверди коротко: (а) финальный кастинг B1..B5, один исполнитель на блок; (б) скоуп B2 на день: safeStorage сейчас (шифруем то что есть, контракт не трогаем) или только фиксация долга; (в) оценки строк.

**Ответ:**

[память:подсознание] emerge vesnin-2026-06-17-desktop-product-line — там же зафиксировано разделение Studio/Device Electron-оболочек и доступ к FS; текущий кастинг про safeStorage в Studio-preload — прямое продолжение той границы.

По делу.

**(а) Кастинг B1..B5, один исполнитель на блок:**
- B1 (панели `components/node-connection/*`) — rodchenko.
- B2 (хранение кредов) — **ozhegov**. Дынин здесь неуместен: это не чистая функция, это граница «где живут секреты и через какой словарь их зовут». B3 передвигаем.
- B3 (api/pairing + pairingCredentials — словарь и контракт хранилища) — dynin, если сводится к чистым адаптерам поверх порта; иначе оставляем ozhegov и B3 отдаём rodchenko как продолжение B1.
- B4 (Studio main.ts/preload.ts — IPC `secureStorage.get/set/del`) — rodchenko.
- B5 (док/ADR-0028 заглушка + запись в SERVICES/ARCHITECTURE о границе) — vesnin (я).

Уточни: B3 — это переписывание api-слоя или только правка словаря под порт? От этого зависит, dynin или rodchenko.

**(б) Скоуп B2 на день:** только **фиксация долга + сужение скоупа**. Контракт `PairingCredentialsStore` не трогаем; вводим порт с двумя адаптерами: `localStorageAdapter` (web/dev) и `electronSafeStorageAdapter` (Studio, через preload IPC). Сегодня — порт + web-адаптер + заглушка Electron-адаптера, помеченная `@stage ADR-0028`. Шифрование включаем отдельным PR после ADR — иначе получим safeStorage без миграции существующих ключей и без политики ротации `mediaToken`. Это «дорого менять», не спешим.

**(в) Оценки строк:** дать не могу без ответа на вопрос по B3 и без размера текущего `pairingCredentials.ts` в глазах резчика. Ориентир формы: B2 ≈ порт (~40) + web-адаптер (~60) + рефактор точек вызова (зависит от B3). Пусть Тарасов сведёт с оценками rodchenko/dynin.

Рекомендация: **форма** (порт + два адаптера, safeStorage — за ADR-0028); полноценный ADR — отдельным PR завтра.

---
