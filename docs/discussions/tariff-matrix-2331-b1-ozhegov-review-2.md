# Обсуждение: tariff-matrix-2331-b1-ozhegov-review-2

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-09-08 12:03 UTC · ozhegov

**Контекст:** C:/Users/USER19~1/AppData/Local/Temp/claude/c--Users-user190825-practice-Membrana/cc734f7c-3147-44cc-917e-9c0cb538c99e/scratchpad/review-b1-subject.md
**Вопрос:** Повторное ревью b1 matrix-passport-storage спринта tariff-matrix-2331 — теперь с ПРЕДМЕТОМ на руках (файлы приложены в контексте задачи). Первый вердикт fail опирался на предположение, что гранула cold без паспорта и лежит в packages/strategic-docs — такого пути нет; смотри реальный resource.json: паспорт полон (version, ratifiedAt: null = честно не закреплено, owner, source, note), значения с ratifiedAt у каждого. Проверь по приложенному: паспорт cold; закрытые списки domain/kind/reseedRule в ядре и в дизайне; слово «Коллекции (горячая память)» против storage.hot; единицы МиБ. Вердикт бинарный pass | fail с причиной, только по тому, что в приложенных файлах.

**Ответ:**

Смотрю по приложенным файлам, не по памяти.

**Контекст:** повторное ревью b1 — matrix-passport-storage, предмет на руках.

**Проверка по существу:**

1. **Паспорт cold** — полон: `version`, `ratifiedAt: null` (честно «не закреплено», не «нет паспорта»), `owner`, `source`, `note`. Значения `free-v1 0` с явной пометкой «ноль = нельзя», `checkpoint/observatory 2048` — с `ratifiedAt: null` и пояснением. Замечание снято: паспорт есть, незакреплённость названа прямо, а не спрятана.

2. **Закрытые списки** — в `passport.mjs`: `DOMAINS` (`storage` — есть), `KINDS` (`quota` — есть), `RESEED_RULES` — `exact-bytes` присутствует. В дизайне: род `quota` ↔ правило `exact-bytes` — связка замкнута (`KIND_RULES`).

3. **Слово «Коллекции (горячая память)» против `storage.hot`** — расхождение термина и ресурса. В `resource.json` у гранулы `collections` явно: `resource: "storage.hot"`, title «Коллекции (горячая память)». Это не синоним-слой: одно имя в словаре (`collections`), второй ресурс в сетке (`storage.hot`) — но связь зафиксирована в одной грануле, не размазана по пакетам.

4. **Единицы** — `unit: "MiB"` у всех трёх; в `passport.mjs` `UNITS` содержит `'MiB'`. Единица одна на дом, путаницы нет.

**Решение:**  
**pass** — паспорт cold корректен и честен (`ratifiedAt: null` — словарная статья «не закреплено», не «отсутствует»); списки доменов/родов/правил в ядре и дизайне согласованы; единицы однородны. Затронутые пути: `granules/tariff-cold/resource.json`, `docs/containers/strategic-docs/design/TARIFF-MATRIX-DESIGN.md`, `passport.mjs`.

Риск связанности: единственное место, где термин коллекций расходится с ресурсом `storage.hot`, — осознанный выбор владельца, зафиксированный в note; синонимов-фасадов нет.

---
