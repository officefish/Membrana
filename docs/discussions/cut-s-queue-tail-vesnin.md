# Обсуждение: cut-s-queue-tail-vesnin

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-10 12:15 UTC · vesnin

**Контекст:** docs/prompts/S_QUEUE_TAIL_2026_08_10_PROMPT.md
**Вопрос:** Прогон контекста резчика для нарезки спринта s-queue-tail-2026-08-10 (membrana-local-sprint). Промпт приложен. Прошу нарезку на accountable-блоки с зонами и оценками строк, и решения резчика по b2: (1) ГДЕ жить гейту обязательности записи прогноза — варианты: (а) execution-gate.mjs при закрытии прогона (closeProcedureRun зовётся именно там), (б) task:archive, (в) sprint-cut при ратификации; заметь: провод plan-to-forecast/gate-to-forecast уже есть в scripts/lib/sprint-integration/, а запись прогонов идёт в docs/procedure-runs (лента) и docs/sprint/experience/forecast-records.jsonl (роды); (2) стоп или предупреждение — долг мостика прямо требует «стоп, не жалоба»; (3) как не сломать закрытие СТАРЫХ прогонов, у которых прогноза исторически нет (амнистия по дате плана? по schema-версии?); (4) b1 — какой дом для вердикта-вещдока typecheck (docs/audit/? прямо в OPEN.md прогона?) и имя карточки починки линков. Зоны блоков не должны пересекаться; сервер не трогаем.

**Ответ:**

[память:подсознание] emerge vesnin-2026-08-01-evening-review-predicate-m4s-existing-organs — прецедент о том, что новые проверки надо ставить в **уже существующие органы**, а не плодить точки. Прямо про выбор (а) execution-gate против (б)/(в): гейт живёт там, где уже есть закрытие.

[память:подсознание] emerge vesnin-2026-07-19-insight-archive-lifecycle-c2-state-axes-r4 — «форма следует функции, декор без конструкции не существует». Здесь — против соблазна дублировать гейт в task:archive и sprint-cut: одна функция «нельзя закрыть без записи прогноза» — одна точка.

---

**Конструкция.** Две накладные полосы одного дня, общий шов — `scripts/lib/sprint-integration/`. Границы блоков режу по владельцу артефакта, чтобы зоны не пересекались.

**Нарезка (2 блока):**

- **B1 · вердикт долга typecheck + карточка линков** (~80–120 строк)
  - зона: `docs/audit/typecheck-verdict-2026-08-10.md` (новый вещдок, обе половины); `docs/tasks/registry.json` (+1 карточка `fix-node-modules-links-1647`); строка в `docs/bridge/DEBTS.md` → `settled` со ссылкой на вещдок.
  - НЕ трогает: `packages/*`, `scripts/*`.

- **B2 · гейт обязательности «предсказание ↔ исход»** (~150–220 строк с тестом)
  - зона: `scripts/execution-gate.mjs` (одна точка входа при `closeProcedureRun`), `scripts/lib/sprint-integration/forecast-record-gate.mjs` (новый — проверка + амнистия), тест в `scripts/__tests__/`.
  - НЕ трогает: `task:archive`, `sprint:cut`, `packages/*`.

**Решения резчика по b2:**

1. **Где жить гейту.** Вариант (а) — `execution-gate.mjs` при `closeProcedureRun`, и только для `procedureId === 'membrana-local-sprint'`. Причины: (i) провода `plan-to-forecast/gate-to-forecast` уже там же в `scripts/lib/sprint-integration/` — гейт становится третьим проводом того же жгута, не новым органом (прецедент 01.08); (ii) `task:archive` и `sprint:cut` — не про закрытие прогона, ставить туда — декор без конструкции; (iii) DoD прямо говорит «закрытие прогона … невозможно» — это `closeProcedureRun`, буквально.

2. **Стоп, не предупреждение.** Долг мостика формулирует «стоп, не жалоба» — читаю дословно. Exit non-zero + сообщение с командой `yarn sprint:experience --run <id>` для починки. Warning здесь = ещё один месяц ручной записи.

3. **Амнистия старых прогонов.** Не по дате (хрупко и спорно), а по **schema-версии прогона**: ввести `schemaVersion: 2` для новых `docs/procedure-runs/*.md` (front-matter), гейт требует запись только при `schemaVersion >= 2`. Старые прогоны без поля — амнистированы автоматически, без списков-исключений. Бонус: тот же ключ пригодится следующим гейтам.

4. **b1 — дом вердикта и имя карточки.**
   - Вердикт: **`docs/audit/typecheck-verdict-2026-08-10.md`** (не в OPEN.md прогона — прогон закроется, вещдок должен пережить). В OPEN.md — ссылка.
   - Карточка: **`fix-node-modules-links-1647`** в `docs/tasks/registry.json`, класс #1647, размер S, зона `node_modules/@membrana/*` + `yarn install` политика. Владелец решает штамп.

**Затронутые модули:** `scripts/execution-gate.mjs`, `scripts/lib/sprint-integration/forecast-record-gate.mjs` (новый), `scripts/__tests__/`, `docs/audit/`, `docs/tasks/registry.json`, `docs/bridge/DEBTS.md`, шаблон front-matter `docs/procedure-runs/`. Сервер и `packages/*` не трогаются — соответствует out-of-scope.

**Рекомендация:** форма готова, ADR/консилиум не нужны — оба блока в границах уже существующих органов. Уточнение к Тарасову перед стартом: **schemaVersion как ключ амнистии** — согласуешь как канон, или закрываем локально в комментарии гейта?

---
## 2026-08-10 12:16 UTC · tarasov

**Вопрос:** Вопрос резчика по спринту s-queue-tail-2026-08-10, блок B2 (гейт обязательности записи прогноза при закрытии прогона membrana-local-sprint в execution-gate). Амнистия старых прогонов — по версии схемы записи, не по дате и не по спискам исключений. Носитель прогонов — docs/procedure-runs/trail/*.jsonl (procedure-run-journal@1, append-only лента): предлагается новым прогонам писать в open-записи явный маркер требования прогноза (поле схемы), гейт требует запись рода «предсказание ↔ исход» только у прогонов с маркером; все прогоны без маркера — амнистированы автоматически. Согласуешь такой ключ амнистии как канон, или закрыть локально комментарием гейта?

**Ответ:**

[память:подсознание] emerge tarasov-review-oversized-queue-cut-1 — прямо в тему: там я уже фиксировал «предсказание ↔ исход» по нарезке (110/200, 200/258 и т.д.). Это тот самый род записи, которую B2-гейт хочет сделать обязательной; значит ключ амнистии должен быть таким, чтобы не переписывать задним числом старые прогоны вроде этого cut'а.

Положение. Носитель — append-only лента `procedure-run-journal@1`; переписывать историю нельзя, амнистия по дате/спискам — хрупкая (Тень «список исключений»).

Решение. Согласую как канон, но с двумя оговорками — иначе это локальный костыль, а не правило:

1. Маркер живёт в **schemaVersion ленты**, а не в свободном поле open-записи. Ключ — `procedure-run-journal@2` (или `@1.1` через additive-миграцию): у новых open-записей поле `forecastRequired: true|false` обязательно; у `@1` — отсутствует и трактуется гейтом как `false` (амнистия).
2. Маркер ставит **не резчик и не гейт**, а держатель прогона при open (executor из one-shot-manifest, ср. `one-shot-manifest-m2-assignment`). Гейт только читает. Иначе смешаем ответственности.

Связка. Веснин — форма записи в схеме журнала (ADR на bump версии, правило amnesty-by-schema); резчик (ты) — текст гейта B2 со ссылкой на ADR; я — приёмка.

Вердикт: **BLOCK локальному комментарию гейта — нужен консилиум на ADR у архитектора.** Канонизировать амнистию комментарием в одном гейте — Тень «правило в подворотне»; такие вещи живут в схеме журнала, иначе следующий гейт напишет свою амнистию по-своему.

---
