# Обсуждение: cut-handoff-7-9-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-02 13:18 UTC · tarasov

**Вопрос:** Ты резчик. Две строки десятки хендофа отданы владельцем нашей линии: пункты 7 и 9. Реши нарезку, кода не пиши.

ПУНКТ 7 — триггер второго такта недельной процедуры «мёртвые провода» на 09.08.
Обзор до нарезки: механизм УЖЕ умеет истечение — в закрытом списке находок есть pending_expired («запись pending есть и валидна, но срок вышел»), срок разбирается и сравнивается календарно (scripts/lib/dead-wire.mjs). В перечне docs/tasks/dead-wire-pending.json шесть записей, у ВСЕХ until=2026-08-09. Прогон сегодня чист: находок 0, потому что pending законен до срока. В манифесте честно: trigger.kind = captain-word, «планировщика нет — процедуру поднимает владелец такта, и это записано честно, а не выдано за автоматизм».
Настоящая дыра иная: глагол dead-wire:check не зовут НИОТКУДА. Он есть в package.json и в своём зубе, и всё. То есть 09.08 шесть записей истекут, прибор бы это увидел — но прибор никто не запустит. Класс знакомый: сегодня то же было у глаголов оп-лога и у recut_act — род объявлен, источника нет.

ПУНКТ 9 — прибор перестаёт судить собственной копией детектора.
Пакет @membrana/mfcc-analyzer-service отдаёт из корня судящую машинерию поимённо: magnitudeOf, cosineOf, judgeRun, evaluatePipe, evaluateTrend, inBounds, boundsProblem, corpusProblem, meanOf, refuse. В самом индексе пакета записано: «Цена промедления уже уплачена — прибор 31.07 судил собственной копией счёта, потому что до детектора пакета было не дотянуться».
Плагин apps/client/src/plugins/mfcc-analyzer-test/mfccAnalyzerPlugin.ts держит свои vectorMagnitude, judgeFrame, judgeSeries. В панели прибора записано прямо: независимость от пакета — «ПО ЧУЖОЙ ПРИЧИНЕ, а не по решению»; завести зависимость значило бы править apps/client/package.json, который не был в зоне ни одного блока того спринта, и долг назван, а не закрыт молча.
ВАЖНО: считалка коэффициентов (mfccExtractor, meyda) остаётся у клиента ПО РЕШЕНИЮ — ядро держит экстрактор инъецируемым, имя библиотеки за периметр ядра не выходит. Это трогать нельзя.

Ответь по пунктам.

1) Один спринт на обе строки или два. Предметы разные (тулинг процедур против клиентского плагина), исполнители, вероятно, тоже. Обоснуй через то, что окно и ратификация у плана одни на всё.

2) Блоки и зоны для пункта 7. Куда именно вешать вызов dead-wire:check: утренняя цепочка, вечерняя, недельная (plan:week вшит в утро через plan-week-if-monday.mjs), или пре-пуш. Учти: 09.08 — это суббота, а такт объявлен выходным и недельным. И назови, чем «зовут каждый день» отличается от «зовут в такт» по цене ложного шума.

3) Блоки и зоны для пункта 9. Кандидаты в зону: apps/client/package.json, mfccAnalyzerPlugin.ts, его тесты. Риск, который я вижу: судящие функции плагина работают по пресету (MfccPresetSpec), а пакет судит по границам (Bounds) — это может оказаться не заменой одного на другое, а сведением двух разных семантик. Скажи, чем резать, если семантики вправду разойдутся: одним блоком с обзором внутри, или обзор отдельным блоком до правки.

4) Держатели по каждому блоку и почему именно они.

5) Оценки в строках. Сегодня эта сессия трижды занизила объём (100→151, 260→475, 270→~400); четвёртый раз оценку назвал ты, и она сошлась. Назови и эти.

6) Что обязано попасть в DoD каждого блока — проверяемые признаки, не намерения. Для пункта 7 в особенности: чем доказать, что триггер сработает 09.08, не дожидаясь 09.08.

**Ответ:**

Положение принял. Владелец отдал две строки — режу как тимлид, кода не пишу.

**1) Один спринт или два.**
Один. Окно и ратификация у плана одни, обе строки — про «объявлено, но не позвано» (тулинг процедуры и клиентский плагин судят собой вместо канона). Класс дефекта общий — «род объявлен, источника нет». Исполнители разные, но это два блока внутри одного спринта, а не два спринта.

**2) Нарезка пункта 7 (dead-wire:check).**
- Основное место — **утренняя цепочка** (тот же слот, где сидит `plan-week-if-monday.mjs`): один вход, календарный разбор `until` уже есть, лишний шум нулевой пока pending валиден.
- **Пре-пуш — нет**: 09.08 суббота, никто не пушит — триггер промахнётся.
- **Только «в такт» (по субботам) — нет**: если владелец такт не поднял, дыра не закроется, а сегодняшний класс болезни именно про «поднимает владелец, а прибор молчит».
- Цена шума: «каждый день» на чистом прогоне даёт 0 находок (проверено сегодня) — шума нет; «в такт» экономит ~6 запусков в неделю, но платит риском промаха. Режем по утренней цепочке.

**3) Нарезка пункта 9 (mfcc — судить пакетом).**
Два блока, обзор отдельно и **до** правки. Твой риск про `MfccPresetSpec` vs `Bounds` — реальный: это не замена one-to-one, это сведение двух семантик. Смешивать обзор с правкой в одном блоке = получить полу-решение под давлением объёма.
- Блок 9.a — обзор соответствия: `judgeFrame/judgeSeries` (пресет) ↔ `judgeRun/evaluatePipe/inBounds` (границы). Итог обзора: либо мост «пресет→границы» в пакете, либо явный вердикт «две разные машины, оставляем обе, плагин зовёт пакет для X, своё — для Y».
- Блок 9.b — правка: `apps/client/package.json` + `mfccAnalyzerPlugin.ts` + его тесты по итогу 9.a. Экстрактор (meyda/mfccExtractor) не трогаем — так решено.

**4) Держатели.**
- 7 — **Крылов** (структурщик тулинга и цепочек, глаголы package.json — его зона).
- 9.a (обзор семантик) — **Ключников** (математик; граница/пресет — это его язык).
- 9.b (правка плагина и тестов) — **Плотников** (верстальщик клиента; правит plugin и package.json apps/client).

**5) Оценки в строках.**
- 7 — **90–130** (одна точка вызова в утренней цепочке + честная строка в манифесте про источник + smoke, что вызов реально идёт).
- 9.a — **120–160** (сравнительная таблица семантик, вердикт, что именно заменяется).
- 9.b — **220–300** (зависимость, замена трёх функций либо мост, обновление тестов, снятие долга из панели).

**6) DoD.**
Общее: канон переписан (в 7 — манифест `trigger.kind` уточнён; в 9 — панель прибора и index-комментарий пакета сняли долг именами).

Пункт 7 — как доказать, не дожидаясь 09.08:
- прогон утренней цепочки на подложенной копии `dead-wire-pending.json` со сдвинутым `until` (вчера/сегодня) даёт **pending_expired ≥ 1** и ненулевой код возврата;
- тот же прогон на реальном файле — **0 находок, exit 0**;
- в цепочке видно имя вызова (grep по `dead-wire:check` находит источник, не только объявление в package.json);
- манифест обновлён: источник назван поимённо, `captain-word` больше не единственный.

Пункт 9:
- 9.a — таблица «функция плагина → функция пакета → семантика совпадает / расходится / моста нет», подписана Ключниковым;
- 9.b — `apps/client/package.json` содержит `@membrana/mfcc-analyzer-service`; в `mfccAnalyzerPlugin.ts` нет `vectorMagnitude/judgeFrame/judgeSeries` (grep пуст); тесты плагина зелёные на тех же фикстурах; строка про «судил собственной копией» в панели и в index пакета либо снята, либо переписана в прошедшем времени.

Вердикт: **LGTM на нарезку** — один спринт, четыре блока (7, 9.a, 9.b + обзорный артефакт 9.a как вход в 9.b), держатели Крылов / Ключников / Плотников. Открываю после ратификации плана.

---
