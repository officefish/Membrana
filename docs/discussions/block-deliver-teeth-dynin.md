# Обсуждение: block-deliver-teeth-dynin

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-01 14:58 UTC · dynin

**Контекст:** C:/Users/USER19~1/AppData/Local/Temp/blocks-1-2-context.md
**Вопрос:** Блок зубов: какие случаи обязаны быть покрыты у условной позиции со свидетелем. Что проверять при нечитаемом и отсутствующем свидетеле. Легальное «нет с причиной».

**Ответ:**

Спасибо за контекст. Смотрю архитектурный узел: **условное деление артефактов на обязательные и условные** с опорой на внешний свидетель. Вопрос в шахматном поле случаев и честном ответе, если я вообще не тот.

---

## Давайте разберём случаи

**Форма условия хорошая:** предикат (`when: 'bridge-open'`) в гейте доставки, свидетель в `docs/bridge/state.json` — это **правильное разграничение**. Гейт спрашивает внешний факт, не придумывает его сам. Нарушения границ модуля здесь нет.

**Теперь матрица:**

| Мостик открывали? | Конспект в списке | Свидетель читаем | Решение | Признак |
|---|---|---|---|---|
| Да | Есть | ✓ согласуется | ✅ доехать | normal |
| Да | Нет | ✓ рассогласование | ⚠️ **RED** — нарушение инварианта | anomaly |
| Нет | Есть | ✓ не должно быть | ⚠️ **RED** — левая запись | spurious |
| Нет | Нет | ✓ согласуется | ✅ пропустить (законно) | normal |
| — | — | ✗ не читается | **ПРОВЕРИТЬ** (нечитаемость ≠ поблажка) | blocked |
| — | — | ✗ отсутствует | **ПРОВЕРИТЬ** (неизвестность ≠ поблажка) | blocked |

**Ключевой принцип:** Если свидетель невозможно прочитать или он отсутствует, **позиция попадает в проверку**, как если бы условие было неясно. Это не игнорирование условия — это **безопасное консервативное поведение**: лучше пересчитать, чем отпустить подозрительное.

---

## На вопросы блока:

**Ожегову (блок 1):**

1. ✅ Опись снимка дня — **верно выходит из списка**. Она несёт `dayKey` и `archivedAt` (провенанс), но требует ОТДЕЛЬНЫЙ гейт свежести (JSON-специфичный). Вторую форму завести — это загрязнение доставки.

2. ✅ Признак `when` — **на месте**. Это не чужое знание; это метаданные артефакта, сигнализирующие гейту, при каком условии позицию проверять.

3. ⚠️ Перенос целиком (`...a`) — **открывает риск**. Если завтра кто-нибудь добавит в резолвер лишнее поле (скажем, приватный флаг или временный тег), оно проскочит в лист неучтённым. **Лучше:** явно взять нужные поля (`{ rel: a.rel, label: a.label, when: a.when, ... }`). Это шумно, но гарантирует, что список всегда знает, что туда ложится.

**Веснину (блок 2):**

1. ✅ Форма условия верна; границы модулей не нарушены. **Вопрос:** читается ли `state.json` синхронно в момент вызова гейта, или есть риск race? Если асинхронно — нужна обработка обещания (promise) на уровне доставки. Перепроверь транспорт.

2. ✅ Правило верное: **нечитаемость → проверка, а не снятие условия**. Это логика отказоустойчивости. Документируй это правило явно в комментарии гейта (`// Unknown witness state forces verification`).

3. ❓ Чтение `state.json` напрямую vs. через процедуру мостика — **зависит от архитектуры**. Если мостик держит state и экспортирует функцию-геттер (`bridge.getState()`), дёргай её. Если `state.json` — это «источник истины» и процедура мостика тоже его читает, прямое чтение допустимо. **Уточни:** кто хозяин `state.json`? Если мостик — попросись через его API.

---

## Легальное «нет»

Я **не владею** деталями того, как именно ритуалы и гейты устроены в вашем `scripts/`-слое; смотрю сквозь призму чистоты архитектуры. Если вопросы относятся к фактическому баг-фиксу конкретного сценария (скажем, что происходит при ошибке чтения `state.json` в текущей реализации), это область для **твоего проверочного кода**, не для моего суждения о принципе.

---
