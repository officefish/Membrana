## Phase B — отложена

Фаза B (Perplexity + Playwright) **не стартует**, пока нет `PERPLEXITY_API_KEY` (`pplx-…`).

**Не блокирует разработку:** research через браузер, `docs/`, `yarn analyzers:research:week` (Anthropic отдельно) — см. `docs/MCP_USAGE.md`.

**Возобновление:** ключ в vault → merge `docs/mcp/tier1-perplexity.fragment.json` в локальный MCP config → `docs/prompts/MCP_WORKSTATION_PHASE_B_PROMPT.md`.

Зафиксировано в `docs/MCP_ROLLOUT_PLAN.md`.
