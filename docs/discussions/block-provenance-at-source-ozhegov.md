# Обсуждение: block-provenance-at-source-ozhegov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-01 14:41 UTC · ozhegov

**Контекст:** C:/Users/USER19~1/AppData/Local/Temp/block-provenance-ozhegov.md
**Вопрос:** Согласие или отказ на смену вида артефакта твоей процедуры. Проверь форму, границы и дай легальное «нет с причиной».

**Ответ:**

Читаю с вниманием к контракту между выходом и проверкой.

**Анализ.**

Твой отчёт — вещдок из процедуры `membrana-leveling`, владение моё. Сейчас он рождается без строки провенанса, и кадр доставки вечера не может отличить свежий файл от старого — останавливает вечер зря.

Предложение добавить первой строкой `<!-- Сгенерировано: … -->` из `builtAt` отчёта — это **не косметика**. Это изменение вида артефакта, и Резчик правильно потребовал письменного согласия хозяина процедуры.

**Согласие: ДА.**

Условия:

1. **Форма верна.** Первая строка, `tool: 'yarn leveling:evening'`, время из уже имеющегося поля `builtAt` — не выдумываем, не добавляем часов в поле. ✓

2. **Границы проверены:**
   - Отчёт не наследуется в чужих местах; архивные копии в `docs/seanses/` живут как независимые файлы — добавление строки не сломает их.
   - Парсеры, которые читают отчёт (кадр доставки через `readDated`) — знают про строку провенанса, это стандарт для вещдоков.
   - Манифест `membrana-leveling` в `SERVICES.md` / `ARCHITECTURE.md` не описывает деталей вида артефакта — это правка ниже уровня манифеста. ✓

3. **Обновлять манифест не нужно.** Тип остаётся: `workspace-leveling-report`, меняется только физический вид в байтах.

**Требование к коду:**

Импорт `provenanceLine` добавить в `scripts/lib/membrana-leveling-report.mjs` перед конечным `console.log` или `fs.writeFile` отчёта. Убедись, что `builtAt` — монотонное значение (`new Date().toISOString()`) и совпадает с моментом пуска генератора, а не расчёта дат.

**Легально: согласие — с условием атомарного обновления обеих строк кода (импорт + вызов). Половинчатое внедрение = отказ.**

---
