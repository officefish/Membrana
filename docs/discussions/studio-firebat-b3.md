# Обсуждение: studio-firebat-b3

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-20 10:23 UTC · dynin

**Вопрос:** Блок b3 studio-firebat-user-pairing — твой (словарь API, чистые адаптеры). Функции apps/client/src/api/pairingCredentials.ts: pairResponseToCredentials (PairResponse → PairedNodeCredentials) и mergePairStatusTariff (обновляет maxUserWorkspaces из статус-полла, с identity-возвратом когда не изменилось) — уже чистые, но БЕЗ зубов. Твоя задача по нарезке: зубы на эти чистые функции (80 строк с учётом кода). Вопрос: какие свойства проверять? Мой набор: (1) полное отображение всех полей включая pairedKeyId/tariff (опциональные); (2) отсутствие лишних полей в результате (креды не тащат мусор из ответа); (3) mergePairStatusTariff: identity при равной квоте и при undefined; новая ссылка при изменении; остальные поля нетронуты. Дополни/поправь коротко — и скажи, нужен ли рефактор самих функций или они словарно чисты.

**Ответ:**

[память:подсознание] emerge dynin-2026-08-13-network-container-m7-machine-policy — там я ровно формализовал «зуб» как проверяемый предикат на границе пакета; здесь та же задача — превратить «чистоту» в набор предикатов, которые ловятся автоматом.

Твой набор корректен, но неполон. Добавь и уточни:

- **(1) полнота отображения:** проверять оба режима — с присутствующими `pairedKeyId/tariff` и без; в отсутствие — поля в результате отсутствуют (не `undefined`-как-ключ). Это разные предикаты: `key in result` vs `result[key] === undefined`.
- **(2) «нет мусора»:** формализуй как `Object.keys(result).sort()` ⊆ фиксированный whitelist. Без сортировки и без множества — не зуб, а надежда.
- **(3) идемпотентность:** `pairResponseToCredentials(pairResponseToCredentials⁻¹(c))` — если инверсии нет, то хотя бы `f(x)` даёт стабильную ссылку при повторном вызове на структурно равном входе (или явно нет — зафиксировать что именно).
- **(4) mergePairStatusTariff — identity строже:** `Object.is(merge(c, s), c)` при `s.maxUserWorkspaces === c.maxUserWorkspaces` **и** при `s.maxUserWorkspaces === undefined`. Именно `Object.is`, не deep-equal — это ловит случайный `{...c}`.
- **(5) новая ссылка при изменении:** `merge(c, s) !== c` **и** остальные поля — `Object.is` по каждому ключу (не `toEqual`). Иначе прошляпишь скрытый клон вложенного объекта.
- **(6) отсутствие мутации входа:** снимок `structuredClone(c)` до вызова, сравнение после. Для чистой функции это дешёвый и обязательный зуб.
- **(7) граничные значения квоты:** `0`, отрицательное (если тип допускает), `NaN` — что делает функция? Если контракт молчит — зафиксировать поведение тестом, чтобы не поехало молча.

Про рефактор: если функции уже возвращают `c` через `Object.is`-совместимую ветку и не растекаются spread'ом без нужды — словарно чисты, рефактор не нужен. Если внутри `mergePairStatusTariff` стоит безусловный `{...c, maxUserWorkspaces: s.maxUserWorkspaces ?? c.maxUserWorkspaces}` — это тихий провал identity-контракта, нужен explicit early-return. Покажи 10 строк тела `mergePairStatusTariff` — скажу точно.

---
## 2026-08-20 10:24 UTC · dynin

**Вопрос:** Тело mergePairStatusTariff (дословно): const nextQuota = status.tariff?.maxUserWorkspaces; if (nextQuota === undefined || nextQuota === pairing.maxUserWorkspaces) { return pairing; } return { ...pairing, maxUserWorkspaces: nextQuota }; — explicit early-return есть, identity держится. Зубы написаны по твоим предикатам, 8 зелёных: полнота отображения; whitelist Object.keys().sort() === фикс-список; опциональные фиксированы как «ключ есть, значение undefined» (текущий словарь, менять контракт запрещено спринтом); Object.is-identity при undefined и при равной квоте; новая ссылка + пополевой Object.is при изменении; квота 0 доезжает; отсутствие мутации входа (structuredClone-снимок) в обеих функциях. Идемпотентность повторного вызова не фиксировал — f(x) каждый раз новый объект, это словарно честно для отображения. Подписываешь b3? Одним словом или список недостающего.

**Ответ:**

[память:подсознание] emerge dynin-2026-07-22-procedure-frames-m1-frames-contract — там я формализовал контракт через `keys(m) = fixed` и требование строгого равенства; здесь ровно тот же паттерн: whitelist + опциональные-как-undefined, и подпись должна проверять инвариант ключей, а не смысл.

[память:подсознание] emerge dynin-2026-07-23-tasks-workshop-m4a-inspect — оттуда предикат «идентичность держится ⇔ ничего не меняется»: тут это `nextQuota === undefined || nextQuota === pairing.maxUserWorkspaces ⇒ return pairing`. Ровно тот же early-return-инвариант.

Подписываю: **b3**.

Явные оговорки к подписи (не отменяют её, но фиксируют границы):

- Идемпотентность `f(f(x)) ≡ f(x)` по значению — следует из твоих зубов 4+5: при изменении второй вызов попадает в ветку равной квоты и вернёт тот же объект по identity. Отдельного зуба не требую, но записываю как **следствие**, не как аксиому.
- «Новая ссылка каждый раз при изменении» — словарно честно для отображения, согласен; identity-стабильность гарантируется только при отсутствии изменения, и это ровно то, что нужно потребителю.
- `structuredClone` в обеих функциях — принято как способ доказать неизменность входа; если позже появится Date/Map в `pairing`, снимок останется валидным, но зуб на «отсутствие мутации» придётся уточнять по типу.
- Не покрыто зубами и не блокирует b3: поведение при `status.tariff === null` vs `undefined` — сейчас оба уходят в early-return через optional chaining, это корректно, но словарь спринта пусть подтвердит, что `null` = «нет тарифа», а не «сбросить квоту».

Если по `null`-семантике словарь молчит — это вопрос к Музыканту/Структурщику, не блокер математики.

---
## 2026-08-20 10:25 UTC · dynin

**Вопрос:** Слово «подписываю» в прошлом ответе не прозвучало явно, а гейт исполнения требует бинарного вердикта. null-семантика: тип PairStatusLinked.tariff — optional (tariff?: {...}), null в типе нет, TypeScript не пропустит null без as-каста; вопрос словаря снят типом. Подписываешь b3 — да или нет, одним словом.

**Ответ:**

Да.

[память:подсознание] cloud_rejected reason: «облако поднято по темам journal/execution/pairing/plugin-manifest — ни одна память не касается null vs optional в типе tariff и не влияет на бинарный вердикт b3»

---
