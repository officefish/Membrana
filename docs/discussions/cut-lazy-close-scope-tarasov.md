# Обсуждение: cut-lazy-close-scope-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-06 13:26 UTC · tarasov

**Вопрос:** Ты резчик. Строка 9 десятки 06.08 — иссью #1705, отдана мне владельцем. Шотом не проходит: предикат S отказал честно (capability_chaining, 7 выстрелов в семье scripts/lib при пороге 3) — значит спринт.

ДЕФЕКТ (вещдок 04.08, воспроизведён): `openProcedureRun` перед новой open-записью закрывает ВСЕ незакрытые прогоны той же процедуры как сирот (fail + gap orphaned). Для ritual-day это верно: обрыв утра ловится следующим утром, runId разные (day-1 → day-2). Для `membrana-local-sprint` это ЛОЖЬ: два спринта живут параллельно законно, и ратификация второго закрыла open первого как сироту — спринт ещё не начинался, а в журнале уже fail. Живой случай 04.08: sprint-dictionary-to-lib получил fail от ратификации run-journal-sequence-validator.

МОЁ РЕШЕНИЕ ФОРМЫ (подтверди или поправь): у ленивого закрытия появляется ЯВНАЯ область, которую называет вызывающий:
- `lazyCloseScope: "procedure"` — закрывать любые незакрытые прогоны процедуры (ритуалы: один живой прогон в единицу времени по построению);
- `lazyCloseScope: "run"` — закрывать только сирот С ТЕМ ЖЕ runId (спринты и всё, где несколько прогонов законно живут разом).
Умолчания НЕТ: область называется явно, иначе тихий выбор снова соврёт про чужой прогон. Зона: scripts/lib/procedure-run-journal.mjs + зубы; потребители (sprint-run.mjs, procedure-run-record.mjs) объявляют область.

ВОПРОСЫ: 1) подтверди/перережь: один блок или два (ядро + потребители)? 2) держатель — Веснин (контракт журнала) или Дынин (предикат)? 3) умолчание: правда ли его не должно быть, или для обратной совместимости старых вызовов оставить "procedure"? 4) окно — сегодня. Коротко по пунктам, кода не пиши.

**Ответ:**

Положение: дефект признаю, вещдок 04.08 читаю как есть — ленивое закрытие путает две разные природы прогонов. Форма с явным `lazyCloseScope` — правильная резка. По пунктам:

1) **Два блока, один PR.** Ядро (`procedure-run-journal.mjs`) и потребители (`sprint-run.mjs`, `procedure-run-record.mjs`) режутся вместе — иначе первый merge оставит контракт без вызывающих, а второй — вызывающих без контракта. Один PR, две зоны, один вердикт.

2) **Держатель — Веснин.** Это контракт журнала: семантика lazy-close меняется в ядре, зубы туда же. Дынин — ревьюер по своей линии (предикат S остаётся честным), но резчик формы — архитектор.

3) **Умолчания нет.** Ты прав: тихий выбор — тот же самый зверь, только с другим лицом. Обратную совместимость закрываем миграцией всех текущих вызовов в этом же PR (их пересчитать — минуты), а не default-ом. Отсутствие default'а — зуб: TypeScript/JSDoc обязателен параметр, вызов без области падает на этапе загрузки, не в проде.

4) **Окно сегодня — принимаю.** Связка: Веснин режет контракт → Дынин проверяет предикат на обоих сценариях (ritual-day и sprint параллельный) → ты закрываешь #1705. Вердикт по PR — мой, до конца дня.

**LGTM** на форму, идём в работу.

---
