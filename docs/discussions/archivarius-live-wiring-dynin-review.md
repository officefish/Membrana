[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
[память:подсознание] emerge dynin-2026-07-14-cowork-sprint-format — собственный DoD-инвариант: каждый блок должен пройти изолированный DoD без соседей; этот ревью именно такой случай — сравниваю план↔код без зависимости от соседних блоков.

---

Смотрю по пунктам плана против описания диффа.

**Инварианты, которые я ставил:**

1. **Составной индекс `{ts, sessionId, uuid}`** — ✅ добавлен, сортировка тройкой соответствует.
2. **Кодек курсора base64url([ts, sessionId, uuid]), мусор→null** — ✅ кодек обратим, граничный кейс покрыт тестом.
3. **Cursor при равных ts** — ✅ тест есть, это был мой главный страховочный предикат.
4. **Projection без bytes/maskedCuts** — ✅ тест «страница меты без bytes» закрывает.
5. **`integrity` батчами + страховка от зацикливания при ломаном порядке** — ✅ `afterCursorFilter` + счётчик зацикливания, план выполнен.
6. **`auditDuplicates` отдельным методом** — ✅, capability-путь делегируется, тест с `listSpans бросает` — правильная форма.

**Расхождения план↔код:**

- **JS-fallback: substring-семантика «названа»** — это настораживает. В плане memory-store должен был реализовать поиск без регрегулярок, не просто задокументировать поведение. Если в `service.ts` написано только «substring», а реализация fallback не проверена тестом на memory-store — это findings. Есть ли тест на JS-fallback отдельно, или только контракт?
- **`$text` в mongo-store** — full-text search требует text-индекса на поле name/title. Он создан? В описании диффа вижу только составной `{ts,sessionId,uuid}`. Если text-индекс не добавлен — `$text` упадёт на холодной базе.

**Вердикт: conditional pass с двумя findings:**

1. Уточни: text-индекс для `$text` есть в `mongo-store.ts` (или миграции)?
2. Уточни: JS-fallback для memory-store покрыт тестом, или только прокомментирован?

Если оба — да, то **pass**. Если нет — findings, не блокер релиза, но долг до следующего PR.

---

**Ответ исполнителя на два уточнения (вещдоки):**
1. Text-индекс ЕСТЬ: `archivarius.mongo-store.ts`, `spans()` init — `createIndex({ bytes: 'text' })` (существует с 27.07, сохранён при переписке; $text не упадёт на холодной базе — индексы создаются до первого запроса).
2. JS-fallback покрыт тестами на живом MemoryArchivariusStore: «search отдаёт страницу меты без bytes (fallback memory)» и «курсор листает страницы при РАВНЫХ ts» — оба через MemoryArchivariusStore, не через мок.

По правилу вердикта («если оба — да, то pass»): **PASS**.
