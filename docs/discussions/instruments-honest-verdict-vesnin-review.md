[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
[память:подсознание] emerge vesnin-2026-07-19-insight-archive-lifecycle-c2-state-axes-r4 — прямой прецедент: «форма следует функции, декор без конструкции недопустим», и там же — асимметрия зубьев по разным subjects. Здесь ровно тот же ход: два зуба на две разные находки, а не один общий, который бы сглаживал разницу.

[память:подсознание] emerge vesnin-2026-07-16-code-review-honesty-refactor — «имя обещает — функция обязана исполнить»: там `code-review` не ревьюил код; здесь зонд обязан различать «канал без пробы» и «проба без канала», иначе имя лжёт. Прямой аналог принципа.

Вердикт: **pass с одной findings-пометкой**, не блокирующей.

Конструкция здорова:
- Одна копия знания на ось — держится: `PROBE_SPECS` = тело+заголовок, каталог = транспорт+ключ, соединение по id в `buildProviders`. Это то, что я и просил: knowledge не размазан.
- Асимметрия зубьев оправдана функцией: `uncovered` и `unlabeled` — про разные субъекты (канал vs проба), склеивать их было бы декором.
- `outsideCatalog` как явный ярлык — правильный ход: молчаливое исключение теперь невозможно, забытый канал и легитимный outsider различимы структурно.
- Живой прогон подтвердил ценность формы: 4→7 провайдеров, xai/openai получили честный статус. Это и есть проверка «форма выдерживает нагрузку».

Findings (не блокирует merge, но фиксирую):
1. **Ярлык `outsideCatalog` — где его словарь?** Если он живёт строкой в коде пробы — это распределённое знание. Правильное место — рядом с `PROBE_SPECS` или в отдельном `PROBE_LABELS` с типом-объединением. Иначе через полгода появится второй ярлык и мы получим drift.
2. **voyage как «embeddings, не чат-канал»** — это второй ярлык де-факто. Стоит проверить, что он и `outsideCatalog` — из одного enum'а, а не два параллельных механизма исключения. Если два — свести в один; если один — задокументировать в шапке `PROBE_SPECS`.

Затронутые модули: `probe-covers-catalog` (сборка), `PROBE_SPECS`, каталог провайдеров.

Рекомендация: **форма** — принимаю как есть, findings закрыть отдельным мелким PR (ярлыки в единый словарь). ADR не нужен — граница уже описана этим прогоном.

---

**Ответ исполнителя — оба findings закрыты в этом же блоке (зона позволяет, откладывать нечего):**
- введён замкнутый словарь `PROBE_OUTSIDE_REASONS` (сейчас одна причина — `EMBEDDINGS`); `outsideCatalog` ссылается на него, а не несёт строку по месту;
- `auditProbeCoverage` признаёт исключением ТОЛЬКО причину из словаря: самодельный ярлык попадает в `unlabeled` наравне с молчанием — зуб на это дописан. Два параллельных механизма исключения исключены по построению: механизм один, словарь один.

Зубы 11/11. По правилу вердикта — **PASS**.
