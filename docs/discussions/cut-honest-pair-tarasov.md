# Обсуждение: cut-honest-pair-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-03 09:32 UTC · tarasov

**Контекст:** docs/prompts/GATE_HONEST_PAIR_COMPLETENESS_PROMPT.md
**Вопрос:** Ты режешь спринт gate-honest-pair-completeness (#1641). ГЛАВНЫЙ ВОПРОС нарезки — развилка из трёх решений, она в промпте, выбор твой и его надо сделать ДО нарезки блоков: (1) требовать оба рода, один след опускать в НОВЫЙ вердикт девятым в закрытом списке — честно по имени, но перекрасит историю, замер перекраски обязателен до мерджа; (2) вердикт оставить, добавить находку на неполный состав — мягко, историю не трогает, но и не останавливает пропуск; (3) требуемый состав родов сделать свойством плана — гибко, но даёт полю плана смысл которого у него нет и требует ратификации формы. Факты для решения: рода contract_signature и session_prep носителя НЕ имеют (CARRIER_EXISTS=false), требовать исполнимо только пару context_run+review_pass; прецедент расширения списка есть — stale_partial добавлен актом владельца 01.08; вещдок пропуска 02.08 — блок с одним review_pass прошёл зелёным и в итоге неотличим от честного. Моя черновая нарезка ПОД ВАРИАНТ 1: (а) completeness-core, dynin, predicates.mjs + gate-exit-codes.mjs, 120 строк — предикат состава, девятый вердикт partial_evidence классом stop, текст honest_pair перестаёт печатать список родов как достижение; (б) completeness-tooth, dynin, execution-gate.test.mjs, 150 строк; (в) history-repaint-measure, dynin, разовый замер по docs/sprint/trail/*.jsonl числом в PR, 40 строк скрипта-однодневки или вручную с командой в PR. Ответь: 1) какой вариант развилки берём и почему; 2) если вариант 1 — верна ли нарезка и классификация нового вердикта как stop; 3) объёмы; 4) чего нет но по DoD обязано быть.

**Ответ:**

Положение: развилка не резчика, а того, кто держит планку. Пропуск 02.08 — мой, и вердикт врал именем два дня. Мягкая находка эту рану не закрывает, план — не то поле, куда прячут состав следа.

**Решение по развилке: вариант 1.** Имя вердикта обязано соответствовать проверке; если проверки состава нет — имя `honest_pair` было ложью по построению. Вариант 2 оставляет пропуск возможным (не останавливает), вариант 3 нагружает план смыслом, которого у него нет, и требует ратификации формы — соседний предмет. Прецедент расширения закрытого списка есть (stale_partial, 01.08) — расширяем актом: «список стал из девяти».

**Класс девятого вердикта — stop, верно.** Неполный след — это не «зелёный с оговоркой», это несыгранный матч при сданной тренировке. Классифицировать `pass_not_green` было бы повторением той же лжи мягче.

**Нарезка — принимаю с одной правкой:**
- (а) completeness-core: 120 строк — ок; в шапке PR обязателен довод «стал из девяти» и имя нового вердикта (предлагаю `incomplete_trace`, не `partial_evidence` — «partial» уже занято семантикой `stale_partial`);
- (б) completeness-tooth: 150 строк — ок, зубы по DoD все четыре плюс `carrier=false не требуется`;
- (в) history-repaint-measure: **до (а)**, не после и не параллельно. Замер решает, мерджим ли вообще: если перекраска массовая (условно >50% прогонов) — это не гейт, это перепись истории, идём в консилиум. 40 строк скрипта-однодневки, число в PR обязательно.

**Чего нет, но по DoD обязано быть:**
1. Перепрогон вещдока 02.08 (`memory-report-surfacing`) с новым исходом, названным в PR — у тебя это в DoD, в нарезке блока нет. Добавь четвёртым блоком или пришей к (б).
2. Обновление текста сообщения `honest_pair` — упомянуто в (а), но это отдельная строка проверки: сообщение больше не печатает найденные рода как достижение. Зубом закрыть.

**Вердикт: LGTM с правкой порядка (в→а→б→перепрогон) и именем `incomplete_trace`.** Связка: ты режешь и ведёшь, замер перекраски — до открытия (а).

---
