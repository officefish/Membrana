# Обсуждение: tariff-matrix-2331-b2-ozhegov-review-2

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-09-08 12:03 UTC · ozhegov

**Контекст:** C:/Users/USER19~1/AppData/Local/Temp/claude/c--Users-user190825-practice-Membrana/cc734f7c-3147-44cc-917e-9c0cb538c99e/scratchpad/review-b2-subject.md
**Вопрос:** Повторное ревью b2 matrix-granules-rights спринта tariff-matrix-2331 — с ПРЕДМЕТОМ на руках (файлы приложены). По твоим двум замечаниям: (1) «нижняя граница» у сценариев старших тарифов заменена на «по аналогии с датчиком — число владельцем не названо; не закреплено» (в resource.json и в дизайне); (2) produce-scope — в закрытом списке правил дизайна с самого начала (строка таблицы правил приложена), первый вердикт опирался на неполное чтение. Проверь по приложенному: nodes (exact-count, unitWords), workspaces (поправленная формулировка), datasets (заём наблюдательного пункта: catalogId + stub словами + ratifiedAt null у значения), system-datasets (matrix-only, вне квоты), instruments (entries; produce-scope). Вердикт бинарный pass | fail с причиной, только по приложенным файлам.

**Ответ:**

**Вердикт: pass**.

Проверил по файлам:

1. **workspaces** — формулировка исправлена: у старших `ratifiedAt: null` + note «по аналогии… не закреплено». Термин «нижняя граница» изъят. Соответствует моему замечанию.
2. **produce-scope** — присутствует в закрытом списке правил дизайна (`KIND_RULES`), связка produce ↔ produce-scope корректна. Первый вердикт снимаю.
3. **datasets** — заём записан честно: `catalogId: checkpoint-v1-catalog`, `ratifiedAt: null`, `stub { since, reason, resolvesBy }`. Полнота сетки сохранена, статус «не закреплено» не скрыт. Это правильная форма.
4. **system-datasets** — `reseedRule: matrix-only`, форма `available/outsideQuota` в паспорте, отдельная гранула. Пересев такие записи пропускает — слабая связанность не нарушена.
5. **nodes / instruments** — роды и единицы в замкнутых списках, названия однозначны.

Риск: у system-datasets `ratifiedAt` проставлен (2026-09-08), тогда как значение — «вне квоты на всех тарифах», что скорее положение, чем закреплённое число. Но это не нарушение формы matrix-only — примечание в паспорте снимает неоднозначность. Противоречий нет.

---
