# Обсуждение: tariff-matrix-2331-b1-ozhegov-review-3

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-09-08 16:59 UTC · ozhegov

**Контекст:** C:/Users/USER19~1/AppData/Local/Temp/claude/c--Users-user190825-practice-Membrana/cc734f7c-3147-44cc-917e-9c0cb538c99e/scratchpad/review-b1-subject.md
**Вопрос:** Ревью блока b1 matrix-passport-storage спринта tariff-matrix-2331 ПОСЛЕ твоего прогона контекста по ратифицированной версии плана (v2, 08.09 вечер). Предмет приложен (файлы коммита 950f98a3: дизайн, ядро паспорта, гранулы purpose/buffer/collections/cold). Прежний вердикт pass (второй заход) записан раньше опорной точки прогона — гейт требует порядок context_run ≺ review_pass, потому ревью повторяется. Судить только по приложенному; вердикт бинарный pass | fail с причиной.

**Ответ:**

Контекст прогона отношу к предмету: из всплывшего — [4] (лемма «вердикт A раньше B»: gate требует `context_run` ≺ `review_pass`, порядок соблюдён), [5] (граница словаря: `ratifiedAt: null` — честное незакрепление, не синоним «примерно»), [1] (точка момента: значения `buffer`/`collections` привязаны к 08.09, `cold` — к 29.07, смешения нет).

**Решение: pass.** Причина: б1 корректен по словарю и связанности.

- Род ↔ правило замкнут (`KIND_RULES`): `quota` несёт только `exact-bytes`; `matrix-only` — у `catalog`, контрабанды формы `available/outsideQuota` в квоту нет.
- `ratifiedAt` разведён: 08.09 у закреплённых владельцем, `null` у `cold` — помечено «не закреплено», не «неизвестно».
- Значения `cold` (0 у датчика) сопровождены нотой «ноль — нельзя» — термин однозначен.
- Единица `MiB`, а не `МБ`, — пересчёт только у render/пересева; сервисного слоя в б1 нет, рисков связанности не вижу.

Замечаний, меняющих вердикт, по приложенному нет.

---
