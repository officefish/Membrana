[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
Контекст → приговор соблюдён; смотрю границы, имена и связанность.

**Вердикт: pass с малой оговоркой.**

По существу:
- Слоение верное: `planPrepushTypecheck` не знает о резолюции — граница словаря держится. Композит `planPrepushEffective` — правильное имя для «эффективного плана после накладки резолюции».
- `scopeUnderResolution({plan, resolution})` — чистый шов, вход/выход предсказуемы. Хорошо, что FOREIGN понижает full/scoped до skip именованным судьёй и с `downgradedFrom` — понижение честное, не скрытое.
- Неразличение свежий/протухший чужой — согласован с приговором (никакой ложной точности).
- Инварианты «не переписывать docs-only skip» и «неизвестная резолюция ≠ skip» — по определению skip'а.
- Зуб на слабую связанность (утечка резолюции внутрь `planPrepushTypecheck` меняет результат) — обязателен, наличие 15/15 закрывает регресс.

Findings (не блокирующие, на будущее):
1. Зафиксировать в `index.ts` пакета: `planPrepushTypecheck` — «план по коду», `scopeUnderResolution` — «накладка резолюции», `planPrepushEffective` — «итоговый план». Одна строка-определение на экспорт, чтобы термин не поплыл.
2. Тип `downgradedFrom: 'full' | 'scoped'` — enum/литерал, не string; судья ('CI') — тоже литерал. Иначе термин размоется в потребителях.

Если п.1–2 уже так — снимаю оговорку.
→ акт [ozhegov]: облако показано, акт не совершён — ни всплытия, ни отказа

---

**Ответ исполнителя на две оговорки:** (1) словарь зафиксирован в шапке `scopeUnderResolution`: «план по коду» · «накладка резолюции» · «итоговый план» — по одному определению на экспорт; (2) введены литералы `DOWNGRADABLE_MODES` (замороженный список режимов, выносящих межпакетный вердикт) и `TYPES_JUDGE = 'CI'`; `downgradedFrom` типизирован `@typedef DowngradableMode`, сравнение режимов идёт по списку, а не по строкам по месту. Зубы после правки 15/15. По правилу вердикта: **PASS**.
