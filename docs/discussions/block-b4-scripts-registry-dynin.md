# Блок b4-scripts-registry-wire — конспект контекста dynin (s-queue-2026-08-11)

След прогонов: `op-log/dynin/2026-08-11.jsonl` (origin `ask-persona`, два захода:
ревью провода и вердикт по разбивке broken).

## Предмет

Карточка `friction6-hygiene-notes` (#1265), пункт 1: снимок реестра скриптов
`scripts/registry/SCRIPTS_LIST.md` регенерировался только руками и дрейфовал
(30.07: 407 глаголов против 439 живых) — регенерацию не звал ни вечер, ни CI.

## Сделано

1. Шаг `scripts-registry` в манифесте вечерней цепочки
   (`docs/tasks/evening-ritual-steps.json`): mechanic, noncritical с причиной,
   позиция ДО `ritual-artifacts` — автозабор берёт `produces[]`, свежий снимок
   уезжает в коммит артефактов вечера, а не остаётся грязью в дереве (класс
   утреннего инцидента 11.08 «хвост не догнал поезд»).
2. Снимок регенерирован: 407 (30.07) → **439** глаголов, Date 2026-08-11.
3. Зубы манифеста вечера (bridge-room, step-status, validate-procedure) —
   зелёные без правок.

## Разбивка broken=11 (запрос контекста — дан ответ фактами)

- **6/11** — ровно шесть законных dead-wire pending, ратифицированных
  владельцем до 2026-08-16: названный долг, не шум.
- **5/11** — глаголы `templates:content:real:*` зовут
  `scripts/materialize-free-v1-real.py`, и это **ложная находка самого
  инвентаря**: файл жив в main (`3a51fc64`) и на диске; `isScriptCodePath`
  слеп к `.py` — носитель не попадает в fileSet и помечается missing.
- evening-* скриптов среди broken нет; дельта к 30.07 тем же прибором не
  измерима (старый снимок счётчик не нёс).

## Вердикт (review_pass)

Позиция шага и noncritical — подписаны: дрейф не теряет данных дня, evening-*
в broken нет, 6/11 — легальный pending, 5/11 — слепота прибора, не дыра
контура. Дифф в зоне (манифест + снимок), за зону не вышел.

## Находки сверх зоны (кандидаты, не строятся здесь)

1. `scripts-inventory.mjs`: SCRIPT_CODE_EXTS расширить предикатом
   (`.py`/`.sh`), класс `missing-carrier` отдельно от pending — снять 5 ложных.
2. `dead-wire`/`extractCarrierPaths` не извлекает `.py`-носители — те же 5
   глаголов невидимы и этому зубу; предикат расширений, не хардкод.
3. Тест-гард: `templates:content:real:*` резолвятся в существующий файл.
