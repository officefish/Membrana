# Блок b1-node-links-env — конспект контекста dynin (s-queue-2026-08-11)

След прогона: `op-log/dynin/2026-08-11.jsonl` (origin `ask-persona`, emerge
`evening-auditor-doc2-report-generator` — «артефакт закрытия = чистая функция
от входа»).

## Предмет

Карточка `fix-node-modules-links-1647`: 35 симлинков `node_modules/@membrana/*`
от 29.07 смотрят в `Membrana-grok`, 2 — в своё дерево (инвентарь «до» снят).

## Вердикт контекста

- `symlink_capability(session) = false`: все три пробы (fs `dir`, fs `junction`,
  `mklink /J`) — EPERM, включая temp; Developer Mode включён. Ограничение — на
  уровне токена процесса, не политики ОС: сессия починить резолюцию не может.
- Формулировка блокера: «сессия без привилегии создания reparse points»; фикс
  владельца — перезаход/терминал с привилегией в токене, не «cmd от админа»
  вслепую.
- Однопакетный typecheck СЕЙЧАС не гонять: tsc пойдёт по чужим линкам —
  зелёный будет ложным плюсом о своём дереве. DoD-пункт typecheck помечен
  `blocked-by-env`, не `passed`. Порядок починки: снос чужих линков → install
  владельцем → realpath-приёмка → typecheck.

## Ревью диффа блока (review_pass)

`docs/CONTRIBUTING.md` §«Workspace-резолюция»: шаг 0 (явный снос чужих линков,
риск ложного зелёного назван), install только из своего корня, диагностика
worktree:resolve + приёмка realpath, ограничение среды с TTL диагноза «на
терминал». Дифф в зоне блока, за зону не выходит. Вердикт: соответствует
контракту b1 (вход: инвентарь; выход: honest_pair на вещдоке; инвариант:
node_modules в дифф не входит).

## Находки сверх зоны (карточки-кандидаты, не строить здесь)

1. `yarn env:check` / preinstall-проба привилегии reparse points — чтобы
   следующий агент не повторял цикл диагностики.
2. `worktree:resolve --strict` — падать, если realpath уходит за границу репо.

## Остаток карточки fix-node-modules-links-1647 (NOT_DONE, живая)

Пункты «переустановка + зелёный typecheck живьём» — блокер среды, исполняются
владельцем из привилегированного терминала; политика и диагноз — сделаны.
