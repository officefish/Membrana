# Обсуждение: block-analyze-sample-rewire-vesnin

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-02 14:23 UTC · vesnin

**Контекст:** packages/services/detectors/base/src/analyze-sample.ts
**Вопрос:** Блок analyze-sample-rewire того же спринта. Приватная iterWindows снята, analyzeSample зовёт общий fftFrames; носитель реэкспортирован из index.ts пакета; README пакета дополнен таблицей «какой свод какой природе». Резчик потребовал доказать неизменность чисел бенчмарка ЗУБОМ, а не словами: золотой зуб на число кадров написан ОТДЕЛЬНЫМ блоком ДО правки и был зелёным на старом коде — семь случаев, включая границу «на один сэмпл меньше следующего кадра», явный hopSize и hopRatio. После свода он зелёный же: 30 зубов пакета проходят. Ответь как архитектор коротко: 1) достаточно ли числа кадров как мерки неизменности, или золотой зуб обязан фиксировать и вердикт; 2) реэкспорт из index.ts делает механику окна публичным API пакета — не рано ли, ведь потребителей ровно три и все внутри монорепы; 3) есть ли риск, что снятие приватной функции изменило порядок или тайминги кадров незаметно для зуба.

**Ответ:**

[память:подсознание] emerge vesnin-2026-07-28-memory-subconscious-m6-extractor — та же логика: планка матча не «убрали дубль», а исполнить зубы C1–C5; здесь — не «свели носитель», а доказать неизменность вердикта, не только счётчика кадров.
[память:подсознание] emerge vesnin-2026-07-19-insight-archive-lifecycle-c2-state-axes-r4 — «форма следует функции, декор без конструкции — не оправдан»: прямо про реэкспорт `fftFrames` при трёх внутренних потребителях.

Конструкция: у зуба две оси — **счёт кадров** (границы окна, hop) и **содержимое кадра** (порядок, выравнивание, значения). Свод носителя формально трогает обе.

1) **Числа кадров недостаточно.** Число — только геометрия окна. Инвариант, который держит бенчмарк, — это `verdict` детектора на фиксированном буфере: `isDrone`, `confidence`, `latencyMsTotal` устойчиво к перестановкам, `fundamentalsHz` — нет. Золотой зуб обязан фиксировать хотя бы: `frameCount`, последовательность `(index → isDrone, round(confidence, 3))` и итоговый `verdict.isDrone/confidence` на 2–3 эталонных сэмплах (дрон / тишина / граница). Иначе зуб зелёный при молчаливом сдвиге на полкадра.

2) **Реэкспорт `fftFrames` из `index.ts` — рано.** Потребителей три, все внутри монорепы — это `internal`, не публичный контракт. Публикация замораживает сигнатуру раньше, чем мы поняли, нужен ли ей `hopRatio` по умолчанию, отдача `timestampMs`, стратегия хвоста. Форма: оставить в `src/sample-window.ts`, импорт по относительному пути внутри пакета; трём внутренним потребителям этого хватает. Экспортировать — когда появится **четвёртый вне пакета** или запрос от analyzer'а на переиспользование.

3) **Риск незаметного сдвига есть, и он ровно про то, что зуб не ловит.** Приватная `iterWindows` совпадала «до символа» — но копии в четырёх местах эволюционировали независимо; равенство тела не равенство краевых случаев: остаток < fftSize (дописываем нулями или отбрасываем?), последний неполный hop, целочисленное округление `hop` при нецелом `hopRatio*fftSize`, поведение при `samples.length < fftSize`. Число кадров совпадёт, а `chunk` последнего кадра — нет.

Затронутые модули: `packages/services/detectors/base/{sample-window.ts, analyze-sample.ts, index.ts}`, зубы пакета.

Рекомендация: **форма** — снять реэкспорт из `index.ts`, оставить носитель internal; расширить золотой зуб вердиктом и хвостовыми случаями (пустой буфер, `samples.length = fftSize - 1`, `samples.length = fftSize`, `samples.length = fftSize + hop - 1`). ADR не нужен: правило «`services/*` не публикуют инфраструктуру до появления внешнего потребителя» уже следует из `SERVICES.md`.

---
