# Обсуждение: cut-detectors-window-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-02 13:16 UTC · tarasov

**Контекст:** docs/prompts/DETECTORS_WINDOW_SINGLE_CARRIER_PROMPT.md
**Вопрос:** Ты режешь спринт detectors-window-single-carrier. Замер: копий обхода кадров четыре, четвёртая приватная в самом detector-base (iterWindows, analyze-sample.ts:63-72), все три детектора уже зависят от базы. Моя черновая нарезка — четыре блока: (1) base-window-carrier, vesnin, packages/services/detectors/base/src/sample-window.ts, 130 строк — публичный носитель: prepareFftSamples, fftFrames, averageMagnitudes, geometricMeanMagnitudes с доказательной шапкой почему сводов два; (2) base-window-tooth, dynin, packages/services/detectors/base/src/sample-window.test.ts, 170 строк; (3) analyze-sample-rewire, vesnin, packages/services/detectors/base/src/analyze-sample.ts + index.ts, 60 строк — приватная iterWindows снимается, зовётся fftFrames, экспорт наружу; (4) detectors-drop-copies, dynin, три пакета detectors/*/src/core/, 180 строк — копии удаляются, импорт из базы, зубы переезжают. Ответь по пунктам: 1) верна ли граница блоков, особенно блок 4 — он трогает ТРИ пакета разом, не должен ли делиться; 2) объёмы; 3) главный риск: analyzeSample это путь бенчмарка и UI-плагина, чем доказать что числа не сдвинулись, кроме слов; 4) зоны блоков 1 и 3 в одном каталоге base/src — не пересекаются ли они по правилу зон; 5) чего в нарезке нет, но по DoD обязано быть.

**Ответ:**

Положение: копий четыре, носитель уже в базе, рёбра графа проложены — задача техническая, но путь через `analyzeSample` — путь бенчмарка, и это единственный настоящий риск.

По пунктам:

1) **Граница блоков.** В целом верна, но блок 4 надо делить. Три пакета в одном блоке — это три независимых удаления копий, каждое со своим набором зубов и своей вероятностью сдвига числа. Режу на 4a/4b/4c по детектору (harmonic → cepstral → spectral-flux в порядке риска: последний не сводит спектр вовсе, у него меньше поверхности). Каждый — отдельный коммит внутри PR, зелёный зуб пакета после каждого. Если 4c споткнётся — не тянет за собой 4a/4b.

2) **Объёмы.** Блок 1 — 130 строк на четыре функции с доказательной шапкой про два свода — реалистично, шапка займёт треть. Блок 2 — 170 строк зубов на четыре функции многовато; если выходит больше — значит, зубы дублируют то, что уже проверено в детекторах, режь. Блок 3 — 60 строк на снятие приватной `iterWindows` и реэкспорт — чувствую 30–40, но пусть будет запас. Блок 4 после дробления — по 50–70 строк на детектор.

3) **Главный риск и чем доказать.** Слова не годятся. Нужен **золотой зуб на `analyzeSample`** до свода: фиксирует ровно тот вердикт и число кадров, что даёт текущий код на нескольких синтетических буферах (в т.ч. `samples.length < fftSize`, `length == fftSize`, `length == fftSize + hop - 1`, длинный). Пишется **в блоке 3 первым коммитом до правки**, зелёный на старом коде — потом на новом. Плюс: прогнать `yarn benchmark:detectors` до и после, приложить diff в PR (ноль строк изменений). Без этих двух свидетельств блок 3 не принимаю.

4) **Зоны 1 и 3.** Оба в `base/src`, но файлы разные (`sample-window.ts` vs `analyze-sample.ts` + `index.ts`), это не пересечение зон, а соседи в одном каталоге. Правило зон о файлах, не о каталогах. Порядок обязателен: 1 → 2 → 3 → 4a/b/c, иначе блок 3 некуда звать.

5) **Чего не хватает по DoD:**
- Зуб-`grep` в CI/локальной проверке на «ровно одно совпадение `start + fftSize <= samples.length`» — сам DoD этого требует, в нарезке его нет.
- Обновление `README.md` пакета `detector-base`: публичный API расширяется, шапка про два свода должна быть отражена в доке пакета, не только в комментарии.
- Явное свидетельство неизменности чисел бенчмарка (пункт 3) — это отдельный артефакт PR, не растворяй в блоке 3.

Связка: **vesnin (1,3) → dynin (2,4a,4b,4c)**; я принимаю после золотого зуба и diff бенчмарка.

Вердикт: **BLOCK** до дробления блока 4 и явной фиксации доказательства неизменности бенчмарка. Перенарежь — пришлю LGTM.

---
