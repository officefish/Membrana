# Обсуждение: block-b1-typecheck-verdict-ozhegov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-10 12:25 UTC · ozhegov

**Вопрос:** Блок b1-typecheck-verdict спринта s-queue-tail-2026-08-10 (план ратифицирован; ты держатель). Предмет: долг typecheck из ревью 08.08 — DoD дважды требовал yarn turbo run typecheck --filter=@membrana/background-cabinet --filter=@membrana/background-office, дважды перенесён. Вещдоки разведки: cabinet — зелёный живьём 10.08; office — локально красный ТОЛЬКО из-за node_modules-резолюции в чужое дерево Membrana-grok от 29.07 (core без контракта static-registry #1828, @membrana/static-registry-service отсутствует в node_modules), при этом CI ствола на 273f936d зелёный (включая office). Класс #1647. Твоя зона: docs/audit/typecheck-verdict-2026-08-10.md (вещдок), карточка fix-node-modules-links-1647 в реестре + её промпт. Вопросы терминов и границ: (1) структура вещдока — какие разделы обязательны, чтобы вердикт был проверяем командами, а не утверждением; (2) формулировка вердикта долга — «закрыт» или «закрыт наполовину» (office судим по CI, не живьём)? (3) карточка починки: что в промпте обязательно (симлинки *-detector-service тоже чужие — их 10+; политика yarn install после мерджей ствола?), размер, lead?

**Ответ:**

Ozhegov. Термины сначала: «долг» — обязательство из DoD; «вердикт» — доказуемое утверждение с воспроизводимой командой; «зелёный CI» и «зелёный локально» — **два разных наблюдения**, и смешивать их в одну лемму нельзя.

**(1) Структура вещдока `docs/audit/typecheck-verdict-2026-08-10.md`** — обязательные разделы:
- `Предмет` — что именно из DoD 08.08 проверяем (дословная цитата команды).
- `Объекты` — `@membrana/background-cabinet`, `@membrana/background-office` как отдельные леммы, каждый со своим статусом.
- `Команды и вывод` — точная команда, SHA, дата/время, exit code, хвост лога (для office — и локально, и CI-ссылка на 273f936d).
- `Наблюдения` — cabinet: локально + CI; office: **только CI**, локально FAIL с причиной (node_modules резолвится в `Membrana-grok`, `@membrana/static-registry-service` отсутствует, контракт #1828 не в core).
- `Границы утверждения` — что вердикт **не** покрывает (локальная воспроизводимость office).
- `Вердикт` — по каждому объекту отдельно, с формулой доказательства.
- `Отложенное` — ссылка на карточку `fix-node-modules-links-1647`.

**(2) Формулировка вердикта.** «Закрыт наполовину» — синоним-фасад, отклоняю. Два объекта — два вердикта:
- cabinet: **долг закрыт** (доказательство: локальный прогон 10.08 + CI 273f936d, оба зелёные).
- office: **долг закрыт условно по CI-наблюдению**; локальная воспроизводимость — **открытый дефект среды**, вынесен в `fix-node-modules-links-1647`, класс #1647.

Так DoD 08.08 закрывается без подмены смыслов, а честный дефект остаётся видимым.

**(3) Карточка `fix-node-modules-links-1647`.** Обязательное в промпте:
- Определение проблемы одной строкой: `node_modules/@membrana/*` резолвятся симлинками в чужое дерево (`Membrana-grok`, снимок 29.07).
- Инвентаризация симлинков: `find node_modules/@membrana -maxdepth 2 -type l -exec ls -l {} \;` — включая `*-detector-service` (10+), `static-registry-service`, `core`.
- Критерий приёмки: `readlink` всех `node_modules/@membrana/*` указывает в текущее дерево; `yarn turbo run typecheck --filter=@membrana/background-office` локально зелёный на текущем HEAD.
- Политика: `yarn install --check-files` (или `--force`) после мерджа ствола + git hook / doc-note в `CONTRIBUTING.md`.
- Границы: **не** трогает контракты `@membrana/core` и не создаёт `static-registry-service` — это класс #1828, отдельная карточка.
- Размер: **S** (инвентарь + reinstall + короткая заметка). Lead: Математик (среда/tooling), ревью — я (границы, чтобы починка не вылезла в core).

Уточнение: подтверди, что #1828 (контракт static-registry в core) уже висит отдельной карточкой — если нет, заведу связанную S-карточку `core-static-registry-contract-1828` рядом, чтобы `fix-node-modules-links-1647` не поглотил её область.

---
