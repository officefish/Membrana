# Обсуждение: cut-worktree-resolution-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-03 11:44 UTC · tarasov

**Контекст:** docs/prompts/WORKTREE_FOREIGN_RESOLUTION_PROBE_PROMPT.md
**Вопрос:** Ты режешь спринт worktree-foreign-resolution-probe (#1647). Направление выбрано иссью — прибор-сторож, не починка резолюции. Моя черновая нарезка — три блока: (1) resolution-core, dynin, scripts/lib/worktree-resolution.mjs, 90 строк — чистый предикат: список пакетов с реальными путями → {own, foreign, foreignSample, исход отсутствия node_modules отдельным словом}; (2) resolution-tooth, dynin, scripts/lib/worktree-resolution.test.mjs, 120 строк; (3) resolution-wire, ozhegov, scripts/worktree-sync.mjs, 80 строк — строка резолюции по каждому дереву из K2, слова о недостоверности локального typecheck, находка ≠ exit-код по правилу дома. Ответь по пунктам: 1) верна ли граница и объёмы; 2) сторож бежит утром в ritual:day — а межпакетная правка случается днём: не окажется ли предупреждение всегда вчерашним, и надо ли поэтому строку резолюции давать ещё где-то (pre-push уже есть отдельно; prepush-typecheck-scope ЕГО НЕ заменяет — он падает с ложным красным, а не предупреждает); 3) слова «локальный typecheck недостоверен» — не слишком ли широко, ведь ОДНОпакетная правка проверяется честно; 4) чего нет, но по DoD обязано быть.

**Ответ:**

[память:подсознание] emerge tarasov-ritual-magistral-source-freshness-cut-1 — предсказания по нарезке систематически недооценивали объём (freshness-core +87%, tooth +35%). Три блока по 90/120/80 — это тот же ряд; надо закладывать поправку, особенно на зуб (реально ждать 160–170).

По пунктам:

1) **Граница верна, объёмы занижены.** Ядро/провод/зубы — правильный ряд (как freshness). Но 90+120+80=290 — почти в точности мой недоцен в предсказании нарезки от 02.08; фактически будет ~400. Заложи tooth=160, core=110, wire=100. Границу пакетов не размывай: `worktree-resolution.mjs` — только пути и классификация; чтение `package.json`/обход `node_modules` — инъекцией из wire, не в ядре (это и держит зуб чистым).

2) **Утреннего сторожа мало — но это не порок нарезки, это второе место установки.** Межпакетная правка случается днём, предупреждение от `ritual:day` протухает к обеду. Правило дома «находка ≠ exit» держим для утренней цепочки. Второе место — **`prepush-typecheck-scope`**: не заменять его логику, а **добавить предупреждающую строку резолюции перед его вердиктом** — тот же провод, тот же предикат, слова те же, exit не меняется. Это в scope промпта («тот же ряд, что сторож мёртвых проводов»). Если по объёму не влезает — отдельный зуб #1648, но провод в pre-push назвать в PR явно как следствие.

3) **Слова слишком широки — переформулируй.** «Локальный typecheck недостоверен» — правда только для **межпакетных** правок (tsc читает чужой `dist` соседней ветки). Однопакетная правка честна (vitest в исходники, tsc в свой пакет). Формулировка: «пакеты резолвятся в чужие деревья — **межпакетный** typecheck локально недостоверен, однопакетный честен; сквозная проверка — CI». Числа своих/чужих дают читателю самому оценить риск.

4) **Чего нет по DoD:**
   - зуб на «отсутствие `node_modules` — отдельный исход» назван в DoD, но в нарезке слит с core — вынеси третьим случаем предиката явно, отдельным тестом;
   - `foreignSample` — сколько имён показывать и как выбирать (первые N? детерминированно?) — не задано, зуб на детерминизм обязателен;
   - гипотетическое «дерево со своим `node_modules`» в DoD — как построишь фикстуру? Инъекция ФС-обхода это и решает, но зуб должен быть назван.

Связка: Тарасов режет — Ожегов ведёт core+wire — Диньин зубы — Веснин смотрит границу ядра (не утечёт ли ФС внутрь).

Вердикт: **BLOCK** — переписать объёмы (×1.4), сузить слова про typecheck до «межпакетного», назвать второе место установки (pre-push) явно: в этом PR или отдельным #1648 с записью в PR-описании.

---
