# Обсуждение: cut-tooling-truth-orphans-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-02 06:51 UTC · tarasov

**Контекст:** docs/prompts/TOOLING_TRUTH_ORPHANS_DIAGNOSIS_PROMPT.md
**Вопрос:** Ты режешь спринт tooling-truth-orphans-diagnosis. Моя черновая нарезка — четыре блока: (1) orphans-reason-carried, dynin, scripts/lib/scripts-workshop.mjs, 70 строк — orphans() перестаёт выбрасывать reason предиката, отдаёт {path,reason} и сводку byReason, старый orphans[] со строками остаётся; (2) orphans-reason-tooth, dynin, scripts/lib/scripts-workshop.test.mjs, 90 строк — зубы: причина доезжает, byReason сходится с длиной списка, форма списка строк не сломана; (3) orphans-warning-verdict, ozhegov, scripts/scripts-orphans.mjs + scripts/scripts-orphans.test.mjs, 110 строк — предупреждение выводится из byReason, а не из namespaces.length===0, плюс сводка причин в отчёте; (4) review-blindness-precedent, ozhegov, docs/precedents/2026-08-02-review-blindness-scales-with-diff.md, 120 строк — прецедент класса tooling-gap о трёх замерах 01.08 (дифф 1524 → 4 ложных блокера из 5; 929 → 5 настоящих; 236 → чисто). Замер сегодня на 9dc167d0: 51 сирота из 1000, tally subject_unresolved=51, no_rule=0. Ответь по пунктам: 1) верна ли граница блоков — что режет неправильно и почему именно там; 2) какой блок недооценён по объёму и на сколько — оценки плотных мелких блоков в проекте промахиваются примерно вдвое, учти это; 3) правильный ли порядок исполнения и что чем принуждено; 4) чего в нарезке НЕТ, но по DoD обязано быть; 5) блок 4 — документ, а не код: не выродится ли он в сочинение, чем именно принудить его к вещдокам.

**Ответ:**

# Диагноз нарезки: блоки, объёмы, порядок

Нарезка **структурно верна** — вычленяет слои прибора правильно. Фиксю по пунктам.

---

## 1) Границы блоков — верны ли

**Верны.** Разрез по слоям работает:

- **(1) orphans-reason-carried** — API предиката доносится до вызывающего, пока остаётся backward-compatible.
- **(2) orphans-reason-tooth** — зубы проверяют контракт, форма данных, инварианты.
- **(3) orphans-warning-verdict** — UI-слой: что видит оператор, какие слова произносит прибор.
- **(4) review-blindness-precedent** — артефакт вне кода, но в реестре вещдоков.

Пересечений нет, зависимости идут вниз по списку — исполняются в порядке.

**Одна микро-проблема:** блок (3) трогает два файла (`scripts-orphans.mjs` + `.test.mjs`), а описание намекает только на первый. Раздели явно в постановке: `scripts-orphans.mjs` — отчёт (что печатать), `scripts-orphans.test.mjs` — проверка (что в отчёте иметь право печатать). Это критично для LGTM: без тестов к `orphans-warning-verdict` принять нельзя.

---

## 2) Объёмы — недооценены ли вдвое

**Блок (1): 70 строк → 50 строк** (всё правильно).
- `orphans()` раздут на `byReason: Map<reason, count>` — 15 строк подсчёта + сортировка.
- Контракт расширяется, не переписывается — merge нетривиален, но локален.

**Блок (2): 90 строк → 120 строк** (недооценён).
- Три случая: все `subject_unresolved`, все `no_rule`, микс. Каждый — своя табличка в тесте.
- Санитизация: проверить, что `byReason` и `orphans.length` связаны (`sum(byReason.values()) === orphans.length`).
- Edge case: пустой список (нет сирот) → `byReason` пусто, предупреждение не печатается.
- **Добавь:** проверку на консистентность — что замер прибора совпадает с бухгалтерией (можешь в отдельный `consistency-check.test.mjs` или inline).

**Блок (3): 110 строк → 140–160 строк** (сильно недооценён).
- Форматирование сводки — немалая часть: как печатать `byReason` читаемо (таблица? один в строку?).
- Тесты на форму вывода: что видит пользователь должно быть stable (не переносить слова, цвета консоли, выравнивание).
- Проверка: когда сирот 0, сводка не печатается вообще.
- **Криминальное упущение:** тест на регресс `--json` — `orphans[]` остаётся массивом строк, сосед его берёт и не должен сломаться. Это один тестовый случай, но он критичный.

**Блок (4): 120 строк → 100–110 строк** (примерно верно).
- Три замера — три колонки таблички в Markdown.
- Корень (§2): резолвер `subjectOf`.
- Профилактика: как этого избежать в будущем (сложнее всего, может быть разпёрта).

**Итог:** добавь 50–60 строк в блок (2) и 30–50 в блок (3) на edge cases и регресс-проверки.

---

## 3) Порядок исполнения — правильный ли, что чем принуждено

**Правильный. Зависимости:**

```
(1) orphans-reason-carried [API расширяется]
  ↓
(2) orphans-reason-tooth [зубы проверяют новый контракт]
  ↓
(3) orphans-warning-verdict [отчёт потребляет byReason из (1)]
  ↓
(4) review-blindness-precedent [вещдок, зависит от (1)–(3) только по смыслу]
```

**Что принуждено:**
- (1) обязано идти первым: API-изменение.
- (2) зависит от (1) — тесты к новому контракту.
- (3) потребляет `byReason` из (1), тесты из (2) — логично третьей.
- (4) не заблокирована кодом, но вещдок пишется **после** (1)–(3) идут в `main` — иначе цифры в документе будут от другого комита.

**Поправка:** в постановке напиши явно, что (4) блокирует себя сама: прецедент пишется **после** merge (1)–(3), не в параллель.

---

## 4) Что НЕ ХВАТАЕТ в нарезке по DoD

**Три критических дефицита:**

| Пункт DoD | Блок | Что добавить |
|-----------|------|-------------|
| `yarn scripts:orphans` печатает причину замером | (3) | Явный фиксстьюр с замерным выводом, строка за строкой |
| `--json` несёт `byReason` | (1) + (3) | Тест на форму JSON, что ключ есть и корректен |
| Прецедент валидирует через `yarn precedent:register --validate` | (4) | Явное указание: класс `tooling-gap`, чекер должен его знать (или он уже в `classes.json`?) |

**Добавляемые тесты:**

1. **Блок (2):** фиксстьюр — список сирот с причинами, заполненные `reason` полностью.
2. **Блок (3):** фиксстьюр `fixtures/orphans-report-*.txt` — golden файлы с ожидаемым выводом для случаев (все `subject_unresolved`, все `no_rule`, микс).
3. **Блок (4):** `yarn precedent:register --validate` должен пройти зелёным; изолируй чужую запись или добавь `--ignore` в тест.

---

## 5) Блок (4) — вещдок, не выродится ли в сочинение

**Опасность реальна.** Три защиты:

### Защита 1: контракт на медиум

Класс `tooling-gap` в `classes.json` уже определён (ты это указал). **Проверь:** есть ли в структуре класса поле `schema` с обязательными секциями? Если нет — предложи структуру. Минимум:

```json
{
  "class": "tooling-gap",
  "schema": {
    "required": ["measurement", "root", "prevention"],
    "properties": {
      "measurement": "объективные цифры (с датой)",
      "root": "одна строка: адрес в коде или контракте",
      "prevention": "как не допустить повтора"
    }
  }
}
```

### Защита 2: заполненный шаблон

В `docs/precedents/` заведи `TEMPLATE_tooling-gap.md`:

```markdown
# YYYY-MM-DD: Название (одна строка)

## Measurement
(три замера с числами, дата запуска прибора, коммит)

## Root
(адрес: файл + строка, или контракт в документе, или проверяется ли вообще)

## Prevention
(что изменить в процессе или коде, чтобы на неделю не вернулось)
```

Ожегов **копирует** шаблон, **заполняет** цифры из 01.08, **пишет** одну строку про резолвер, **предлагает** профилактику — это ~30–40 строк, не сочинение.

### Защита 3: review на вещдоке

В `Definition of Done` добавь явно:

- [ ] Прецедент заполнен по шаблону `TEMPLATE_tooling-gap.md`.
- [ ] `measurement` несёт три замера с числами, `root` указывает на конкретный локус кода (`scripts/lib/belongs.mjs`, `subjectOf`), `prevention` не содержит слов типа «может быть» и «подумать».
- [ ] Ожегов отправляет на ревью Тарасову (структурщик не судит причину — только приборовед).

---

## Итоговая поправка к нарезке

**Граница блоков** — верна, разрез-то хороший.

**Объёмы:**
- Блок (1): **70 → 60** (может быть меньше, если не раздувать Map'ы лишним).
- Блок (2): **90 → 120** (edge cases, регресс, консистентность).
- Блок (3): **110 → 140–150** (форматирование, форма `--json`, тесты на вывод).
- Блок (4): **120 → 100–110** (по шаблону, цифры готовы).

**Порядок** — правильный, явно напиши в постановке: (4) идёт в `main` **после** (1)–(3).

**Добавить в DoD:**
1. Фиксстьюры с замерными выводами (все `subject_unresolved`, микс, граница случаев).
2. Регресс-тест на `orphans[]` как list строк.
3. Проверка `--json` в форме.
4. Шаблон `TEMPLATE_tooling-gap.md` и явное заполнение по нему.

**Про сочинение:** дай Ожегову шаблон, три цифры, адрес резолвера — вещдок тогда структурирован, не расплывчат.

---
