# Обсуждение: firebat-node-device-b4

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-19 13:57 UTC · ozhegov

**Контекст:** C:/Users/USER19~1/AppData/Local/Temp/b4-ticket.md
**Вопрос:** Блок b4 firebat-node-device — poller узла (код в ticket, 7 зубов на чистых функциях). По ADR-0027 Р5: без npm-зависимостей, тракт съёмки берётся из field-capture.mjs рядом (который с #1997 импортирует ./lib/capture-sidecar.mjs — значит комплект узла = папка из трёх файлов + .env, это названо в шапке; установщик b5 несёт папку). Словарь исходов тот же ok|stale_key|backoff; 401 → stale_key → поллер останавливается (код 3); backoff — ждёт сколько сказал сервер с потолком 60 с; транспортная ошибка — не исход, удвоение задержки; служебный токен в .env узла — именованный отказ; объявленное задания → meta словом, измеряемых полей нет; --once для приёмки b7, --dry-run сдаёт словом. Подписываешь b4? «подписываю» или список недостающего, коротко.

**Ответ:**

[память:подсознание] emerge ozhegov-2026-08-17-server-plugin-foundation-m1-vocabulary — тем же приёмом фиксирую словарь исходов и .env-контракт как леммы до подписи.

Подписываю.

Проверил по контракту:
- Словарь исходов закрыт (`POLL_OUTCOMES`), 401→`stale_key`→exit 3 — без долбёжки сервера, с прощальным heartbeat.
- `parseEnv` именованно отказывает при отсутствии четырёх ключей и при наличии `VITE_MEDIA_API_TOKEN` — служебный токен узлу не положен.
- `nextDelay`: backoff с потолком 60 с, транспорт — удвоение, ok — штатный период. Транспорт не пишется в `lastOutcome`.
- `buildResultMeta`: только объявленное задания, измеряемых полей нет — контракт #1950 не нарушен.
- Тракт съёмки — тот же `ffmpeg`/`measureWav`/`pickFieldDevice` из `field-capture.mjs`, порог `SILENCE_PEAK_DBFS` уважается, тишина — именованный отказ наружу.
- Границы: без npm, комплект узла = 3 файла + `.env`, названо в шапке; `--once`/`--dry-run` под приёмку b7.
- Публичные имена (`classifyPoll`, `nextDelay`, `validateTask`, `buildResultMeta`, `POLL_OUTCOMES`, `NODE_KEY_HEADER`) — словарные, без синонимов-слоёв.

Мелочь на будущее (не блок b4): `lastOutcome` инициализируется `'ok'` до первого цикла — при первом `--once` с transport_error в heartbeat уйдёт `'ok'`. Если это смутит приёмку b7 — поправить в b5 отдельным штрихом.

---
