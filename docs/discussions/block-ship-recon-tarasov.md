# Обсуждение: block-ship-recon-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-07-31 07:56 UTC · tarasov

**Контекст:** C:/Users/USER19~1/AppData/Local/Temp/block-ship-recon.md
**Вопрос:** Вынеси вердикт по каждой ветке по своим же порогам и скажи, что делать с Ш1.

**Ответ:**

# Вердикт по веткам и Ш1

## Ветка 1: `fix/review-gate-pr-head` 

**Готовность: 85%** → **ИНТЕГРИРОВАТЬ**

Дифф чистый, причина ясна: SHA берётся от PR, не от локального HEAD, избегаем переколчика LLM при каждом вызове gate'а. Флаг `--restamp` — ортогональный и полезный. Предупреждение о фолбэке честное. Это закрывает Ш2 полностью и по факту, не частично.

**Оставить для PR:** тест на `--restamp` (переразбор уже полученного тела без LLM — критический путь, должен быть unit-проверен).

---

## Ветка 2: `feat/ship-merge-state-guard`

**Готовность: 65%** → **ИНТЕГРИРОВАТЬ с сомнением**

Гард MERGE_HEAD — срабатывает и закрывает Ш2 с другой стороны (молчаливый зелёный при зависнувшем слиянии). Unit-тест в месте. **НО:**

- Тест `pr-ship.test.mjs` прошёл через черновик, т.е. покрыт логики, но не обвязан сценарию «слияние сломалось между pre-commit и push» — реальному кейсу Ш2.
- registry.json строчка не объяснена в PR-описании (рутина, но лишняя сложность).

**Оставить:** интеграционный тест, где репо в состоянии MERGE_HEAD перед вызовом `pr:ship` — должна выхватить и вывести юзабельное сообщение.

**Результат:** обе ветки в main, но не одновременно — сначала gate (нулевых побочных эффектов), потом guard (с уточненным тестом).

---

## По Ш1: `checkout main` остаётся висеть

Ветка 2 её не трогает — факт. **Ш1 отложить в отдельный task**, не спешить в спринт: Ш2 критичнее (LLM-стоимость) и поддалась уже. Ш1 — терпима (ветка остаётся на мейне, следующий `pr:ship` переключит). Разбор: есть ли путь переключиться на main БЕЗ checkout (git ветку не менять, только мержить)? Если да — это новый флаг, если нет — архитектурно норма.

---

## По объёму разведки

По факту — 110 строк (30 на ветку + 20 на пересечение + контекст). Прогноз 140 держал буфер на случай, если бы ветки переплетались глубже. Они ортогональны, поэтому расход ниже.

---
