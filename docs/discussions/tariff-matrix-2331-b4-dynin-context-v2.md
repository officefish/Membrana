# Обсуждение: tariff-matrix-2331-b4-dynin-context-v2

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-09-08 16:56 UTC · dynin

**Контекст:** GitHub Issue #2331: «Матрица тарифов как единственный источник правды: гранулы по ресурсу + шаблон в контейнере strategic-docs, сетка — производная (сессия Б, спринт)»
**Вопрос:** Прогон контекста по РАТИФИЦИРОВАННОЙ версии плана tariff-matrix-2331 (ратификация владельца 08.09 19:54 после перерезки b3 — движок каркаса шаблона; зона b4 reseed-grid не менялась). Ты — ответственный контекст b4. Прежний прогон (11:54) гейт дисквалифицировал как судивший контракт до перерезки. Подтверди или поправь предикаты по ратифицированному плану: yarn tariff:reseed — G := proj(R) из release.json и resource.json гранул, детерминированно (порядок pins → реестр, rank → строки, реестр → ячейки; дат в теле нет; //provisional из паспортов; matrix-only пропускается); зуб tariff:grid: red ⇔ ¬form(G) ∨ ¬supersede(S) ∨ G ≠ proj(R); S↔R — печать seedEpochDriftReport, на код не влияет; релиза нет → exit 2. Живой пересев уже прошёл: сетка совпадает с релизом tariff-matrix@0.1.0, зуб зелёный, drift S↔R напечатан (буфер датчика 1024→512, null старших). Есть ли возражение к предикатам или порядку. Коротко.

**Ответ:**

Предикаты и порядок — принимаю, возражений нет.

Проверил по ратифицированному плану, зона b4 — контракт целостный:
- `yarn tariff:reseed` детерминирован, даты исключены из тела, `provisional` и `matrix-only` обработаны — согласен.
- Зуб `tariff:grid`: `red ⇔ ¬form(G) ∨ ¬supersede(S) ∨ G ≠ proj(R)` — корректен, покрывает порчу ручной правки.
- `S↔R` — только отчёт, на код не влияет — ок.
- Живой пересев прошёл, сетка совпадает с `tariff-matrix@0.1.0`, зуб зелёный.

Одна ремарка, не блокер: буфер датчика 1024→512 — это пересев базы, по Т1 он закрывается заданием В. Здесь он лишь отражён в релизе, что не нарушает контракт b4. Дрейф напечатан — это фиксация факта, а не действие.

Коротко: предикаты валидны, порядок соблюдён, к выполнению готов.

---
