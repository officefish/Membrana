# Блок b5-secret-inventory — конспект контекста dynin (s-queue-2026-08-11)

След прогонов: `op-log/dynin/2026-08-11.jsonl` (origin `ask-persona`).

## Предмет

Карточка `friction6-secret-inventory` (#1266): `yarn secret:inventory` — вход
ротации ключей; глагола не было, ядро резака было.

## Сделано (guard соблюдён: ядро secret-redact.mjs не тронуто)

- `scripts/secret-inventory.mjs`: чистые `buildSecretInventory` /
  `renderInventoryTable` + CLI (файл/каталог/суффикс-глоб, SKIP_DIRS, бинарные
  расширения мимо); классы = имена правил резака + `json:<ключ>`; значения не
  печатаются ни в одном режиме (ядро в cuts значений не несёт); честная
  оговорка «ВЕРХНЯЯ ГРАНИЦА» в обоих режимах; exit 0/2.
- Глагол `secret:inventory` — одна строка рядом с `secret:redact` (первая
  вставка сортировкой всех scripts отвергнута: дифф 793 строки на S-блоке,
  откатано до 1 строки).
- Зубы 6: детерминизм байт-в-байт (сортировка file+class явная, не порядок
  ФС), синтетика PEM/JSON ловится, чистый файл молчит, значения не в выводе,
  CLI живьём, ошибки входа кодом 2, **контрольная подсадка** (чистый корпус +
  1 ключ = ровно 1 находка — принято шестым зубом по п.4 ревью).
- Живой прогон: `docs/comms/drafts` — 55 файлов, 0 находок (с контрольной
  подсадкой доказуемо, не декларативно).

## Ревью (review_pass) — судьба рисков

Приняты: п.4 (подсадка — зуб), п.5 (детерминизм сортировки — явные ключи,
закрыт зубом байт-в-байт). Названы известными ограничениями (в шапке скрипта
и здесь, не строятся в S): п.1 base64-вкрапления вне правил резака — верхняя
граница остаётся честной формулировкой «где смотреть», не сертификатом;
п.2 глоб-формы кроме суффиксной = ошибка входа с причиной; п.3 JSON5/JSONC/
битый JSON идут текстовым режимом без `json:*`-классов.
