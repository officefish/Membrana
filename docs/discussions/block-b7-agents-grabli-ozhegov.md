# Блок b7-agents-grabli — конспект контекста ozhegov (s-queue-2026-08-11)

След прогона: `op-log/ozhegov/2026-08-11.jsonl` (origin `ask-persona`).

## Предмет

Карточка `notes-regex-cyrillic-translit` + хвост #1265: три рукописные грабли
в дом AGENTS.md (норма TF-7 — генератор их не добудет).

## Сделано (зона: AGENTS.md, таблица граблей)

1. `\w`/`\b` ASCII даже с флагом `u` — вещдок 26.07 (пустой результат молча),
   лечение `[\p{L}\p{N}]` с `u`.
2. Сверка латинских id/slug с русским склоняемым текстом «по вхождению» —
   вещдок числом (39 ложных против 8 настоящих), лечение словарь/транслит.
   Формулировки — прогоном контекста Ожегова, дословно.
3. Foreground `sleep` заблокирован средой целиком (хвост #1265, процессная
   природа) — строкой-соседкой к «Фоновый bash без timeout»: та про фон, эта
   про передний план, соседство названо в обеих.

## Решение о месте (развилка контекста)

Ожегов голосовал за мини-раздел «Кириллица в поиске» при отсутствии соседей.
Резчик оставил форму дома (единая таблица без подразделов — заведение
подраздела меняло бы структуру дома сверх S-зоны): обе лексические строки
стоят смежной парой, лемма-связка названа в первой. Отступление от голоса
контекста названо здесь, не спрятано.

## Review_pass

Три строки в стиле дома: «что врёт/кусает», вещдок с датой/числом, лечение.
Дифф — только AGENTS.md, в зоне.
