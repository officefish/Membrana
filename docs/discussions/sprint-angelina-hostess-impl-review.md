# Сводное ревью спринта angelina-hostess-impl (C→B+G→H+GC) — 2026-07-21

<!-- Локальный ревью-артефакт (норма gitignore-ревью). Интеграционный проход ПОВЕРХ
     трёх пре-мёрж ревью (#756 LGTM, #762/765 LGTM+P2-укреплён, #767/769 LGTM). -->

## Гейты

- Полный `test:scripts` на итоговом main: **0 провалов** (вкл. ~37 новых тестов спринта).
- Живой смоук контура (5/5):
  1. Скилл-маршрутизация: `membrana-morning-ritual` — status live, триггер «утро»;
  2. `|entry|=1`: автозапусков старого утра 0;
  3. Ружьё: telegram-шагов в `ritual:day` и вечернем манифесте — 0;
  4. Гейты+встреча живьём: Ангелина здоровается (имя·head·|entry|=1), честно называет
     «состояние не заведено»; `morning:gate status` — оба ждут, canSend=false, exit 3;
  5. Void: каталог+README на месте, CI-барьер зелёный.

## Вердикт: LGTM спринту в целом

Все пять вердиктов заседания исполнены по своим DoD; отступления — названы ниже, не скрыты.

## Честные хвосты (не блокеры, follow-up)

1. **#768** — осиротевший `telegram-ritual-digest` (P1 ревью #767): вердиктом → rejected →
   первый переезд в void; экстрактор отделить (используется swallow-трактом).
2. **verify-canon-гейт живёт в тестах, не в команде**: механика M1 (статусы/ацикличность/
   morningLeak) закодирована в `skill-status` и красна в CI через `test:scripts`; в сам
   `docs:verify-canon` (verify-docs-canon.mjs) не вшита. По духу DoD («красный CI») —
   закрыто; по букве («verify-canon расширен») — перенос.
3. **gc-раннер не вшит шагом в вечерний манифест**: ядро GC (isDead/epitaph/report)
   готово и оттестировано; сам проход `gc.mjs` как шаг ritual:evening «после закрытия
   вердиктов дня» — следующий заход (нужен носитель статуса «вердикт закрыт»).
4. **Партиция-тест на захардкоженных списках** (P2 #756): извлекать S из фактических
   скиллов, чтобы ловить дрейф шагов.
5. **`fill` per slot в генераторе**: контракт+assemble готовы и интегрированы с гейтом
   скелета; перевод `_main-day-issue` на пять fill-вызовов — при первом живом прогоне
   (жжёт кредит, смоук = завтрашнее утро).

## Процессные находки спринта (в копилку выводов)

- Капкан merge→reset→force-push сработал ×2 (коммиты спасены reflog) — правило «сначала
  подтверди MERGED» записано в память.
- `gh pr merge` без `--auto` при DIRTY = конфликт с уехавшим main, не защита ветки.
- Молчун в собственном тесте (require в ESM под try/catch) — пойман и вычищен; класс
  живуч даже у того, кто с ним воюет.
