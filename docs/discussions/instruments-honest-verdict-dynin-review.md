[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
[память:подсознание] emerge dynin-2026-07-25-insight-cast-carrier-contract — там мой критерий: контракт зачтён, если предикат разрешим офлайн, за один проход, без модели. Здесь ровно тот же приём — «зуб проверяет, что каждый внутренний класс отображается в legacy» — это разрешимый предикат на конечном множестве, я обязан его назвать по имени, а не по вкусу.

Вердикт: **pass с двумя findings** — оба не блокирующие, но зафиксировать нужно, иначе через месяц вернутся как долг.

По фактам план↔код:

1. Семь классов без `auth` в имени — да, ровно та развилка, которую я требовал 05.08: `auth_denied_unstable` вычисляется по истории, а не по одному ответу. Ложной точности больше нет.
2. `no_key` называет переменную — предикат «нет ключа» стал разрешим до сети, за один проход, офлайн. Это то, чего мне не хватало утром.
3. `confirmAuthDenial(attempts)`: N≥2 в окне без промежуточного успеха, успех обнуляет — корректно. **Finding-1:** не вижу в описании ширины окна и что такое «попытка» — по времени или по счётчику. Без числа это «скорее всего», а не предикат. Назови: `window = ?` (секунды или последние K вызовов) и зафиксируй в зубе.
4. Перенос 5xx/сеть/status 0 в `transient` — согласен, `protocol` теперь честно про формат ответа.
5. Зуб отображения `internal → legacy` на конечном множестве из 7 — это и есть моя «ратификация перерезки». Пока он зелёный, чужая зона цела.

**Finding-2 (долг, не блокер):** `auth_denied_unstable` — асимметрия ошибок не названа в коде. Ложный плюс (закрыли живой ключ) дороже ложного минуса (лишний retry на мёртвом). Порог N=2 сейчас — вкус; когда будет корпус инцидентов, пересчитаем. Пометь TODO с этим словом, чтобы не забылось к enum v2.

22/22 принимаю. Долг про enum v2 — записан, жду слова владельца.

---

**Ответ исполнителя на два findings:**
- **finding-1 (предикат назван числами):** введены `DEFAULT_DENIAL_WINDOW_MS = 3600000` (окно — ВРЕМЯ, не счётчик вызовов) и `DEFAULT_MIN_DENIALS = 2`; оба возвращаются наружу в результате `confirmAuthDenial`, причина печатает ширину окна. Попытка без `at` считается лежащей в окне — история без времени не повод молча выбрасывать наблюдение; это названо в шапке.
- **finding-2 (асимметрия ошибок):** записана TODO прямо у предиката: ложный плюс (объявили мёртвым живой ключ) дороже ложного минуса; порог N=2 назван ВКУСОМ, а не расчётом, с указанием пересчитать по корпусу инцидентов вместе с enum v2.

Зубы после правок: 22/22. По правилу вердикта — **PASS**.
