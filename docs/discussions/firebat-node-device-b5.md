# Обсуждение: firebat-node-device-b5

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-19 14:38 UTC · ozhegov

**Контекст:** scripts/firebat-service-install.ps1
**Вопрос:** Блок b5 firebat-node-device — установщик (скрипт в ticket). Узел Windows Home: приложение = задача планировщика при входе пользователя (dshow живёт в интерактивной сессии; автовход — владелец), перезапуск через минуту; комплект 3 файла + .env узла с ключом узла (служебного токена нет по построению, параметр -NodeKey обязателен); node/ffmpeg через winget; -Uninstall снимает задачу, записи оставляет; идемпотентно. Studio NSIS (yarn studio:package) собрался впервые — артефакт 150 МБ в release/ (игнорируется git), комплект узла в NSIS не вшит — названо остатком. Посылка дня «инсталлятор не собирался» снята вещдоком в момент влития. Подписываешь b5? «подписываю» или список недостающего, коротко.

**Ответ:**

[память:подсознание] emerge ozhegov-2026-07-23-llm-procedure-channels-m2b-control-plane — там я держал границу «канал = замкнутый путь носителей до потребителя»; здесь та же проверка: путь «оператор → -NodeKey → .env узла → poller» замкнут, служебный токен в него не входит по построению, это ровно норма ADR-0027 Р3.

Контекст: b5 — установщик поля, тракт «winget → комплект → .env → задача планировщика при входе → рестарт 1 мин», -Uninstall не трогает записи, идемпотентно; NSIS собрался, комплект в него не вшит — назван остатком.

Подписываю — с одной оговоркой по словарю, не по существу.

Что в норме:
- Р3 ADR-0027 соблюдён: `-NodeKey` обязателен, служебного токена media в .env узла нет.
- Триггер `-AtLogOn -User $env:USERNAME` под dshow оправдан и назван в docs/field/firebat-node.md — это словарная статья, а не молчаливое решение.
- Идемпотентность: `Set-ScheduledTask` при наличии, `Register` иначе; комплект копируется поверх; .env перезаписывается — предсказуемо.
- `-Uninstall` снимает только задачу; границы «инструмент — данные — записи» разведены.
- NSIS-артефакт 150 МБ в `release/` под gitignore, комплект в него не вшит и **явно назван остатком** — это допустимо: два разных носителя (studio-инсталлятор ≠ node-kit), смешивать их сейчас — синоним-фасад.

Остаток (не блокирует b5, в бэклог одной карточкой):
- `.SYNOPSIS`/README поля должны в одну строку сказать: «служебного токена media в .env узла нет по построению; ключ узла выдаёт оператор через POST /v1/devices/:id/node-key» — это уже в комментарии скрипта, продублировать в docs/field/firebat-node.md, чтобы словарь жил вне исходника.
- Решение «вшивать ли kit в NSIS» — отдельная карточка со словарной статьёй: `studio-installer` vs `node-kit` — что кому принадлежит; пока — не смешивать.

Подписываю b5.

---
