# Обсуждение: cut-memory-report-surfacing-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-02 11:19 UTC · tarasov

**Вопрос:** Ты резчик. Нужна нарезка малого спринта по иссью #1637. Реши, не пиши код.

ПРЕДМЕТ. Третья строка отчёта памяти команды прибита намертво: scripts/lib/team-memory-report.mjs:92 печатает у КАЖДОЙ персоны «всплывало сегодня: контур не поставлен (#1366 ч.2)» и не смотрит ни на какие данные. Шапка отчёта (строка 66) утверждает то же: «контуры подсознания и всплытия не поставлены».

ПОЧЕМУ ЭТО ТЕПЕРЬ ЛОЖЬ. Контур всплытия поставлен сегодня спринтом subconscious-lift-c3, три PR влиты (#1634, #1635, #1636). Живой прогон дал журналу Веснина: cloud_query 5, surface_invoke 5, emerge 2, reject 1. Всплывшее — подлинные записи архива с объяснениями персоны. Это последний невыполненный пункт DoD того спринта, и он несущий: признак включения был проверяемым именно по этой строке.

ЧТО УЖЕ ЕСТЬ (обзор до нарезки, норма #1573). Модуль lib/team-memory-report.mjs ЧИСТЫЙ: «вход — текст git-диффа, выход — структура/markdown; fs/git — у CLI». Прецедент подачи данных снаружи уже работает: причины перетока приходят опцией opts.reasonsByPersona, а CLI scripts/team-memory-report.mjs УЖЕ читает op-log того же дня по тем же персонам (parseOpLog, ищет transfer_to_archive) и складывает Map. То есть всплытие ложится ровно в этот же ход. Зуб у отчёта есть: scripts/team-memory-report.test.mjs.

ЧЕТЫРЕ СОСТОЯНИЯ, которые журнал различает и которые нельзя схлопывать в одну фразу: (1) были emerge — всплыло, поимённо и с объяснением персоны; (2) был reject — облако показывали, персона отвергла, причина названа; (3) ноль cloud_query — лифт сегодня не звали (это НЕ «контур не поставлен»); (4) cloud_query без surface_invoke — облако собрано пустым, показывать было нечего.

Ответь по пунктам.

1) Сколько блоков и где границы зон. Кандидаты в зону: scripts/lib/team-memory-report.mjs (чистое), scripts/team-memory-report.mjs (CLI, fs), scripts/team-memory-report.test.mjs (зуб). Один блок на всё или резать по шву чистое/грязное? Обоснуй через то, что зоны блоков одного плана не должны пересекаться и что каждый блок обязан иметь дом для своего зуба.

2) Держатель. Дынин (чистые функции, счёт) или Ожегов (термины, формулировки состояний)? Предмет двойной: механика счёта по журналу и НАЗВАНИЯ четырёх состояний в отчёте. Назови одного и скажи, чем второй тут не нужен.

3) Оценка в строках. Сегодня я трижды занизил оценку блока: 100→151, 260→475, 270→~400. Назови оценку сам, зная это.

4) Окно спринта. Отдельное на сегодня или посадить в окно родительского subconscious-lift-c3 (01.08 17:30 — 02.08 20:00)? Родительский спринт ЗАКРЫТ, гейт 4 из 4.

5) Что обязано попасть в DoD, чтобы блок не стал заглушкой. Проверяемые признаки, не намерения.

**Ответ:**

Положение: строка 92 врёт после subconscious-lift-c3; данные для правды уже есть в op-log того же дня, CLI умеет их читать (прецедент reasonsByPersona + transfer_to_archive), чистый модуль принимает через opts. Работа — не «фикс копирайта», а достройка контура отчётности лифта: механика счёта + именование четырёх состояний.

1) **Два блока по шву чистое/грязное.**
   - Блок A (чистое + зуб): `scripts/lib/team-memory-report.mjs` + `scripts/team-memory-report.test.mjs`. Приём новой опции (напр. `liftByPersona`), ветвление на четыре состояния, рендер строки, тесты на все четыре ветки на мок-данных.
   - Блок B (грязное): `scripts/team-memory-report.mjs` — парсер op-log дня (`cloud_query`/`surface_invoke`/`emerge`/`reject` по персонам), сборка Map, передача в чистый модуль. Зуб — интеграционный: фикстура op-log → снапшот отчёта.
   Один блок на всё — плохо: зоны разной природы, зуб чистого не должен зависеть от fs, а у CLI свой дом для интеграционной фикстуры. Пересечения нет: шапка (строка 66) правится в Блоке A вместе со строкой 92.

2) **Держатель — Ожегов.** Предмет ведут ЧЕТЫРЕ имени состояний, которые пойдут в отчёт и станут терминами журнала («всплыло», «отвергнуто с причиной», «лифт не звали», «облако пустое»). Счёт по журналу — механика в две строки на состояние, вокруг которой не нужен математик; Дынин избыточен. Ожегов же обязан пройтись и по шапке, чтобы формулировка «контур не поставлен» не осталась там мёртвой.

3) **Оценка: ~360 строк суммарно.** Блок A ~220 (ветвление + рендер + четыре теста на мок-данных, включая правку шапки). Блок B ~140 (парсер op-log по образцу `parseOpLog`+`transfer_to_archive`, склейка Map, одна интеграционная фикстура). Занижения учтены — не режу по нижней границе.

4) **Отдельное окно на сегодня.** Родитель закрыт с гейтом 4/4; переоткрывать закрытый спринт ради хвоста — размывать журнал закрытий. Ссылка на subconscious-lift-c3 в описании иссью — да; окно — своё.

5) **DoD (проверяемые признаки):**
   - На фикстуре op-log с `emerge` — в отчёте у нужной персоны имя всплывшего и объяснение персоны (не заглушка).
   - На фикстуре с `reject` без `emerge` — строка «отвергнуто, причина: …».
   - На фикстуре с нулём `cloud_query` — строка «лифт сегодня не звали» (НЕ «контур не поставлен»).
   - На фикстуре `cloud_query` без `surface_invoke` — строка «облако пустое».
   - Шапка (строка 66): фраза про «контуры не поставлены» удалена; вместо неё — краткая сводка по дню (сколько персон в каком из четырёх состояний).
   - Живой прогон дня даёт журналу Веснина строку, соответствующую его цифрам (cq5/si5/em2/rj1) — приложить в PR как артефакт.
   - Тесты покрывают все четыре ветки; grep `контур не поставлен` по репозиторию — пусто.

Связка: Ожегов ведёт оба блока; ревью по чистоте шва — архитектор. Вердикт по нарезке: **LGTM** при условии, что иссью #1637 разложат на A+B с DoD выше.

---
