# Обсуждение: plugin-results-bridge-b5

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-20 11:06 UTC · vesnin

**Контекст:** docs/plugins/results-bridge-live-run-2026-08-19.md
**Вопрос:** Блок b5 (приёмка моста живым прогоном): мост ожил после деплоя 19.08 вечера — runId 01a01ac2, bridge sent, читается обратно; вещдок дописан (в ticket). Как архитектор и держатель b5 — принимаешь ли приёмку, что дописать до закрытия спринта и архивации карточки?

**Ответ:**

[память:подсознание] emerge vesnin-2026-08-18-team-evening-feedback — там я уже ратифицировал форму решения по #1961 17.08 и день подтвердил кодом на `server-plugin-foundation`; текущий b5 — тот же контур, замыкается по той же норме «читаем из дома, не из лога».

Конструкция. Приёмка b5 — это не «мост отправил», а «RunRecord прочитан из дома по HTTP тем же адресом, каким пишется». Оба условия выполнены: `bridge.outcome=sent`, документ поднят GET `/plugin-results/runs`, скрипт и туннель не в кадре. Границы соблюдены: media отправитель, office приёмник, сид onResult, `configHash` не дрогнул относительно 18.08 — ворота те же. **Приёмку принимаю.**

Варианты закрытия с ценой:
- Закрыть как есть — риск: молчаливые допущения про `office-not-configured` и про `stale`-по-чтению растворятся в чате. Дёшево сейчас, дорого через месяц.
- Дописать до закрытия минимум (рекомендую) — цена: полчаса письма, зато карточка встаёт в архив самодостаточной.

До архивации допишите в вещдок:
1) **Негативный контур**: явная строка, что без `OFFICE_API_URL`/`OFFICE_API_TOKEN` сервис жив, прогон идёт, `bridge.outcome=office-not-configured` — это норма, не отказ (сослаться на форму §«Порядок деплоя»).
2) **Идемпотентность/повтор**: один пример второго вызова с тем же `sampleId` — что видит дом (тот же `runId` или новый? один RunRecord или два?). Это фиксирует контракт `fingerprints` на живом.
3) **Протухание по чтению**: одну строку-ссылку на `mfcc-first-field-probe-2026-08-20.md` с пометкой «этот RunRecord ожидаемо `stale=true` после того прогона» — M3 объявлен, пусть будет виден.
4) **Хеши как якорь**: `inputHash`/`configHash` уже в таблице — добавьте, чем `configHash` совпадает с 18.08 (одна строка: «ворота 48 кГц не менялись»).

Затронутые модули: `docs/plugins/results-bridge-live-run-2026-08-19.md` (дописать), `docs/plugins/results-bridge-form.md` (не трогать — форма выдержала), карточка спринта `plugin-results-bridge` → в архив после п.1–3.

Рекомендация: **форма**. ADR не нужен — контракт моста уже зафиксирован PR #1981/#1985/#1988; консилиум не нужен — расхождений с манифестом нет.

---
