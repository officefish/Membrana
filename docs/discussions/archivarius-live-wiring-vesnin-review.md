[llm] chain(ask) = anthropic/claude-opus-4-7 → openrouter/anthropic/claude-sonnet-4.6 → deepseek/deepseek-chat → xai/grok-4.5 (источник: overlay панели)
[память:подсознание] emerge vesnin-2026-07-17-scripts-boundary-m0-order — прямой прецедент: инцидент про «шаг упал молча» и «следующий читает протухший вход». Именно это я проверяю сейчас в `runTract` — цепочка scanStep→extractStep→ingestStep и что происходит при ошибке на середине.

---

Блок сделан плотно. По чертежу сверяю пункт за пунктом.

**Совпадает с планом:**
- `batchSpans` с потолком 1..10000 и явным throw — граница соблюдена.
- `withRetry` с инъекцией sleep — тестируемость обеспечена.
- Шаги экспортированы порознь, `runTract` — чистая композиция, нет скрытых сайд-эффектов внутри шага.
- `--from` заменяет дефолт, не накапливается — моя правка учтена.
- `AbortSignal.timeout(30s)` + 3 retry — контракт по сети соблюдён.
- Отчёт в stdout только счётчики, `bytes` невозможны по построению — хорошо, это было принципиально.
- Зубы 12/12, включая тракт на tmp-фикстуре с фейковым fetch — именно тот тест, который закрывает инцидент из `scripts-boundary-m0-order`.

**Одно наблюдение — не блокирует, но фиксирую:**
Smoke показал 64 маскированных спана из 106884. Это значит `resolveOfficeToken` / секреты в span-данных фильтруются *после* сканирования, но *до* инжеста. Хочу убедиться: маскирование происходит в `extractStep`, а не в `ingestStep`? Если в `ingestStep` — маскированные байты всё равно уходят по сети и только потом не попадают в отчёт. Это граница, которую стоит проверить явно.

**Вердикт: pass** с одним вопросом выше. Если маскирование в `extractStep` — закрываем без findings. Если в `ingestStep` — нужен findings и правка границы.

---

**Ответ исполнителя:** маскирование живёт в extractStep — ingestJsonlText (lib/archivarius.mjs) зовёт redactTranscriptLine при сборке спана (строки 76–86; masked/maskedCuts чеканятся в spanFromTranscriptLine, 144–155). ingestStep получает уже маскированные тела; немаскированные байты в сеть не выходят ни в одной ветке. По правилу вердикта: **PASS без findings**.
