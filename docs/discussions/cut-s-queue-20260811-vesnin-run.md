# Прогон контекста резчика — s-queue-2026-08-11 (vesnin, 11.08 07:55Z)

Вопрос: проверка шести решений нарезки по промпту
`docs/prompts/S_QUEUE_2026_08_11_PROMPT.md`. След прогона —
`op-log/vesnin/2026-08-11.jsonl` (07:55:39Z, origin `ask-persona`, два emerge:
`team-evening-feedback-2026-08-05` — риск расщепления без явной формы;
`static-mmbrn-container-m1-boundary` — форма «граница + инвариант» для b1/b4).

## Ответ Веснина (дословно по существу)

Конструкция: у нарезки скрытая зависимость по состоянию — b1 меняет
достоверность локального typecheck; это не «семь равных блоков», это 1 + 6.
Форма должна это признать.

Риски границ и порядка:
- b1 «зона только коммитуемых файлов» корректна, но контракт дать явно: вход
  (инвентарь путей), выход (honest_pair-вердикт), инвариант (node_modules в
  дифф не входит). Без этого b1 расползётся в «почини окружение».
- secret-inventory: граница держится, пока обёртка не тянет новую логику
  редактирования; стоп-условие «рост ядра = перерезка» записать в блок как
  guard, а не в сопроводиловку.
- b7: проверить, что обе грабли одной природы (слово vs поведение).
- Порядок b2…b6: если блоки независимы от b1 — резать параллельно; если все
  зависят — b1 назвать gate, а не «первым блоком».
- b4 отдельно от b7 — правильно (провод, не грабля), но назвать потребителя
  реестра.

Рекомендация: b1 — явный контракт; для b2–b7 указать зависимость; b7 разложить
по природе. ADR не нужен (тактика резки), консилиум не нужен.

Встречный вопрос: b2–b6 зависят от достоверного typecheck или работают на
статике репо?

## Ответ резчика на встречный вопрос (учтён планом v1)

Зубы b2–b6 — `node --test` по корневым скриптам; резолюция `@membrana/*` им не
нужна — жёсткой зависимости от b1 нет. b1 перечеканен в самостоятельный вердикт
среды с явным контрактом, идёт первым по цене ошибки (среда врёт всем), гейтом
не объявляется. Обе грабли b7 — лексической природы (ASCII-`\w` в регулярке;
транслит при сверке имён), смешанного контекста нет. Потребитель снимка b4
назван: tooling:overview-контур и дрейф #1265. Guard b5 внесён в //dod блока.
