<!-- Сгенерировано: 2026-08-20T11:23:37.000Z (yarn code-review; pr, pr-2003, llm-anthropic) -->

<!-- review-verdict sha:44ff59c7651f7588270a4f8b9f85976ffb7e571f base:af9d3ecff0c418a4c7b7a4e5a908f21bd5622fdd verdict:LGTM lead:ozhegov at:2026-08-20T11:23:37.000Z -->

<!-- review-source
  source: gh-compare
  base_ref: main
  merge_base: af9d3ecff0c418a4c7b7a4e5a908f21bd5622fdd
  head_sha: 44ff59c7651f7588270a4f8b9f85976ffb7e571f
  head_match: false
  files: 15
  truncated: false
-->

Tier: T2

PR size: oversized (+74 lines, ~474 vs target ≤400)

---

**[ozhegov] (ведущий ревью — leadPersona карточки sanitation-2026-08-20):**

Сверяю дифф с бестиарием.

**B8 (Немой носитель) — не найден:** `LIVE_SERVICES.md` обновлён в том же PR, строка `/v1/devices/:deviceId/node-key` объявлена с верным словарём заголовка, пометкой «живой провод не подтверждён до b4/b7» и ссылкой на вещдок-артефакт — это честная запись. Носитель не немой.

**B9 (Проза) — не найден:** контракт `NodeKeyStore` — машинный интерфейс, зубы в `node-key.service.test.ts` проверяют поведение (не только символ), словарь вердиктов `NODE_KEY_VERDICTS` — `as const` с теми же именами, что гвард ест в `switch`. Ни одно утверждение не зависит только от прозы.

**B3 (DoD-на-механику) — граничный случай:** зуб «словарь вердиктов закрыт — пять имён» проверяет массив, а не продукт. Но b2 явно не продуктовый DoD — это технический блок; приёмку продукта закрывает b7. Нарушения не фиксирую, отмечаю как след для b7.

**B6 (Молчаливый зелёный) — проверить:** `SKIP_PREPUSH=1` в теле PR — typecheck пропущен. Это не B6 в диффе (код не молчит), но создаёт ту же ямму: CI может быть судьёй, а может не запуститься на b2 без явного триггера. Помечаю как риск, не как блокер b2 (честно задокументировано автором).

**Остальные звери (B1, B2, B4, B5, B7, B10) — не найдены** в диффе.

**Структурный вывод:** `NodeKeyGuard` не навешен на `NodeKeyController` (правило из комментария модуля выполнено); `index.ts` экспортирует только модуль и типы словаря, внутренности не просачиваются; `DevicesModule` импортируется как зависимость, прямых репозиториев через граничку нет. Инвариант «один активный ключ на устройство» защищён и в коде (`already_active`/`rotate`), и в схеме (индекс `deviceId, revokedAt`). Три мелочи из подписи b2 (словарь заголовка, guard-порядок, дебаунс `touch`) зафиксированы в комментарии модуля — долг b3/наблюдаемости, не b2.

**Пропуск** — структурных блокеров не обнаружено; oversized задокументирован (ревизия плана ратифицирована владельцем 12:59Z).

---

**[Teamlead]:**

Tier T2. PR size: oversized (+74 lines, 474 vs ≤400) — ревизия b2 ратифицирована владельцем в `recut_act` 12:59Z; split не требую, P1 не ставлю: расширение зоны обосновано решением Ожегова («обходов без миграции не подписывает»), документ `docs/discussions/firebat-node-device-b2.md` содержит полный след. PR #2003 по таблице живых состояний: MERGED — вердикт ретроспективный, не блокирующий деплой.

C1 — границы пакета соблюдены: `FirebatNodeModule` живёт только в `background-media`, экспорт через `index.ts`; `DevicesModule` — легальная зависимость внутри того же пакета; циклов нет.
C8 — `console.log` в production-коде отсутствует.
C9 — секретов и deploy-логов в диффе нет; `keyHash` — только sha256-hex, сырой ключ нигде не персистируется.
C10 — `LIVE_SERVICES.md` обновлён в PR с корректным словарём заголовка и честной пометкой «живой провод не подтверждён до b4/b7».

Typecheck пропущен (`SKIP_PREPUSH=1`) — единственный риск P1: судьёй назначен CI. До b3 обязательно убедиться, что CI прошёл typecheck на `@membrana/background-media`; если нет — b3 не стартует.

Вердикт по MAIN_DAY_ISSUE: b2 закрыт (7/7 спринта по состоянию на вчера), сегодня магистраль — `studio-firebat-user-pairing`; PR #2003 не блокирует её.

**LGTM** — при зелёном CI typecheck.

Утренние команды: `yarn turbo run typecheck test --filter=@membrana/background-media` — проверить до старта b3.

---

**[Структурщик]:**

C4 — `NodeKeyService` не содержит React, хуков, фреймворковых побочных эффектов; `@Injectable()` — Nest, не React-контейнер, зона чистая. C3 — `MembranaRegistry` в диффе не задействован; применимость «—». Хранилище за интерфейсом `NodeKeyStore` — паттерн правильный: зубы работают без Postgres, `PrismaNodeKeyStore` подключается только в production-пути конструктора; единственный писатель таблицы `NodeKey` соблюдён. Один структурный след для b3: `touch` вызывается на каждый `verify` — при высокочастотных ручках узла это лишний write; дебаунс (N секунд на `keyId`) следует добавить до b7, не обязательно в b3.

---

**[Математик]:**

C6 — в диффе нет чистых вычислительных функций, FFT, спектра; применимость «—». `hashNodeKey` — детерминированная функция без побочных эффектов, `hexEqual` сравнивает буферы равной длины через `timingSafeEqual` — корректно. Единственный edge case: `Buffer.from(a, 'hex')` и `Buffer.from(b, 'hex')` при разной длине hex-строк вернут буферы разной длины, `timingSafeEqual` бросит исключение — но оба входа `hashNodeKey` одного алгоритма (sha256, всегда 64 hex-символа), так что в реальном пути длины совпадают; в тесте покрыто. Отмечаю как след: явная проверка `ba.length === bb.length` уже есть — хорошо.

---

**[Музыкант]:**

C2 — Web Audio в диффе отсутствует; применимость «—». IoT/MCU-контекст: ключ полевого узла Firebat — верная архитектура для edge-захвата: отзываемый NodeKey вместо служебного токена минимизирует радиус взрыва при потере устройства (ADR-0027 Р3 выполнен). Живой провод (b4 poller, b7 приёмка) — следующий шаг; без него аудио-контур узла не замкнут.

---

**[Верстальщик]:**

C5 — UI-компонентов в диффе нет; применимость «—». `ApiErrorBodyDto` в Swagger-аннотациях — типовое оформление, не новый UI-контрол.

---

**Итоговый артефакт:** `docs/discussions/pr-2003-code-review.md`

**Definition of Done:**
```
yarn turbo run typecheck test --filter=@membrana/background-media
```
Typecheck обязателен до старта b3; тесты (`node-key.service.test.ts`, 6 зубов) должны быть зелёными.

**Риски:**
- P1 — typecheck пропущен при push (`SKIP_PREPUSH=1`); судья — CI; b3 не стартует без подтверждения зелёного.
- P2 — `touch` на каждый `verify` без дебаунса; след для b3/наблюдаемости, не блокер b2.
- P2 — oversized (+74 строки); обоснован ревизией плана, split не требуется.

**Вердикт: LGTM** (при зелёном CI typecheck на `@membrana/background-media`)