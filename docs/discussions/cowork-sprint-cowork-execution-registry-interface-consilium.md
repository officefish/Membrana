# Interface Consilium — Cowork Sprint `cowork-execution-registry` (Phase 3)

> 2026-07-19. Ведёт координатор (Веснин, Teamlead). Протокол по регламенту
> `docs/COWORK_SPRINT_REGULATION.md` §Phase 3: одновременное вскрытие трёх
> EXPECTATIONS → сведение в `INTERFACE_CONTRACT.md`.
> Вход: EXPECTATIONS блоков `lead-persona` (freeze `740f2bdd`),
> `units-trace-measure` (freeze `7fff51ec`), `snapshot-cold-migration` (freeze `000e1bd9`).
> Скомпрометированных блоков НЕТ — нарушений изоляции не зафиксировано, разбор
> начинается сразу со швов.

---

## Вскрытие

[Teamlead — Веснин]: Открываю три EXPECTATIONS одновременно — до этой минуты их не читал никто, кроме авторов. Констатирую по свежим головам веток: изоляция выдержана, все три DoD зелёные (30/30, 41/41, 50/50 + scoped CI office 178). Скомпрометированных нет, значит идём по швам brief: приёмка, снимок↔измерение, словарь↔схема, exit-коды, владелец времени. Начнём с самого приятного — со шва, который сошёлся сам.

[Структурщик — Ожегов]: Шов «субъект ↔ след». `lead-persona` гипотетически ждал поле `acceptedBy: "<persona-id>"` рядом с ревизией head — и построил иллюстративный стаб. `units-trace-measure`, не видя этого, реализовал `{ acceptedBy: <персона>, headRev: <sha> }`. Совпадение дословное, вплоть до семантики «подпись субагента не существует и не требуется». Предлагаю канонизировать имена как есть: `acceptedBy` + `headRev`.

[Математик — Дынин]: Подтверждаю с одной поправкой наблюдателя: третий блок в стабе S3 назвал те же две сущности `{lgtmBy, headRevision}`. Стаб в интеграцию не мёржится, так что это не конфликт кода — но `classifyDebt(record, closureArtifact?)` принимает артефакт закрытия как вход, и на интеграции он обязан получать канонические ключи. Фиксируем: канон `acceptedBy`/`headRev`, маппинг фикстурных имён — внутри тестов блока, наружу не выходит.

[Музыкант — Курёхин]: И второй вопрос того же шва: пускаем ли `angelina` в `leadPersona`? Словарь единиц говорит «строковый идентификатор персоны из канона» и перечисляет `vesnin, angelina, ...` — то есть блок учёта сам уже ждал её. Ведущая допустима как значение; двойного счёта нет, потому что Ангелина ведёт процесс, а не подменяет исполнителя задачи.

[Teamlead — Веснин]: Принято: `acceptedBy` + `headRev` канон; `angelina` — допустимое значение `leadPersona`; след — персона, принявшая выход, приёмка строго ПОСЛЕ `isValid = true`; безымянный субагент в след не попадает, его публичный признак — только род из `SUBAGENT_KINDS` (`analyst`/`scribe`), эти строки канонизируем для любого упоминания рода в следе и логе.

[Верстальщик — Родченко]: Шов «снимок ↔ измерение». `units-trace-measure` ждал `{ header: { capturedAt, sourceRevision }, cards: [...] }` с полями `id, status, leadPersona, createdAt, closedAt/archivedAt`. `snapshot-cold-migration` отдаёт `linear-snapshot@1`: `header{ capturedAt, sourceRevision, source, trigger, recordCount } + records[]`. Расхождение — имя массива (`cards` vs `records`) и состав полей записи. Шапка потребителя — строгое подмножество шапки производителя: сходится без жертв.

[Математик — Дынин]: Значит контракт обязан назвать: (1) канон формы — `linear-snapshot@1` производителя, header расширенный; (2) обязательный минимум полей записи под пять величин: `id`, `status`, `leadPersona`, `createdAt`, `closedAt`, `archivedAt`, `githubIssueRefs`; (3) адаптер `snapshot-records-to-cards` на интеграции — тонкая проекция `records[]` в вид потребителя. Блоки не переписываем — это ровно случай «несогласованность гасится адаптером на шве».

[Структурщик — Ожегов]: Внимание к слову: у производителя запись снимка = Linear-issue (допущение A1), у словаря единица счёта = GitHub-issue через биекцию центральной под-задачи. Это не противоречие, а два уровня: снимок несёт сырые записи источника, счёт ведётся по проекции через `githubIssueRefs`. Но термин обязан быть один: в глоссарии «запись снимка» ≠ «единица счёта», и величины считаются ТОЛЬКО по единицам счёта. Иначе `wip` посчитает контейнеры — прямое нарушение вердикта M1.

[Музыкант — Курёхин]: Признак движения для `wip`: потребитель ждал `status ∈ {open, in-progress, ...}` на записи. Производитель поле `status` в записи несёт (S1-фикстуры). Осталось произнести инвариант: снимок передаёт состояние источника как есть, интерпретация «что считается движущимся» — у словаря единиц, не у производителя. Производитель не фильтрует.

[Teamlead — Веснин]: Принято: канон `linear-snapshot@1`, минимум полей назван, адаптер-проекция на интеграции, счёт только по единицам словаря. Теперь самое напряжённое — коды возврата. У нас три кодовых пространства: снимок `0/10/20`, миграционный гейт `0/11/21`, зубы `0/1`. И заявленное блоком данных напряжение M2↔M4: M2 требует «протух — мягкий, но ненулевой различимый», M4 говорит «мягкий режим — exit 0». Дынин, разбери формально.

[Математик — Дынин]: Напряжение снимается различением двух пространств, которые мы до сих пор смешивали словом «код». Первое — **вердикт-код**: значение в структуре `{code, verdict}`, возвращаемой чистой функцией; он обязан различать «в порядке / мягко / жёстко» (требование M2 — ненулевой различимый для «протух»). Второе — **exit процесса**: то, что видит хук или CI; там действует закон M2 эпика 1 «отказ = exit ≠ 0» и вердикт M4 «мягкий режим не останавливает работу», то есть exit 0. Проекция: жёсткий вердикт → exit = вердикт-коду (ненулевой, различимый); мягкий вердикт → exit 0 + обязательная печать замечания с вердикт-кодом; чистый → exit 0 молча. Оба вердикта заседаний исполняются буквально, ни один не отозван.

[Верстальщик — Родченко]: Тогда единая таблица вердикт-кодов спринта, конвенция производителя данных подтверждается: `0` — чисто; `1–19` — мягкие; `≥20` — жёсткие. Раскладка: `10 SNAPSHOT_STALE`, `11 LEGALITY_MIGRATING`, `12 ACCEPTANCE_UNCONFIRMED` (отказ-II в soft-режиме); `20 SNAPSHOT_NO_INPUT`, `21 LEGALITY_HARD`, `22 LEAD_PERSONA_MISSING` (отказ-I), `23 ACCEPTANCE_FALSE` (отказ-II после миграции: приёмка ложна — LGTM не на той ревизии). Код `11` был назван блоком, не комнатой — комната его сейчас ратифицирует как часть таблицы.

[Структурщик — Ожегов]: Возражение по зубам. Блок учёта реализовал `checkLeadPersona` с локальным контрактом «`0` пройден, `1` отказ-hard». Таблица требует `22`. Переписывать блок запрещено регламентом. Решение — тем же инструментом, что и всё здесь: адаптер `trace-gate-cli` на интеграции транслирует локальный `{code: 1}` в вердикт-код `22` и делает проекцию на exit. Локальные коды блока остаются его внутренним делом; наружу — только таблица.

[Музыкант — Курёхин]: Поддержу и добавлю инвариант, чтобы таблица не расползлась: вердикт-код принадлежит домену, диапазон общий. Новый код — только через правку контракта, не через «у себя в блоке завёл». И код следует за вердиктом, не наоборот — инвариант `traceResult` блока учёта возводим в закон спринта.

[Teamlead — Веснин]: Принято целиком: два пространства, таблица `0/10/11/12/20/21/22/23`, проекция hard→exit=код, soft→exit 0 + печать, адаптер для зубов. Владелец времени — Ожегов.

[Структурщик — Ожегов]: Три часовых претензии вскрылись идеально совместимыми. Производитель заявил: `capturedAt` ставит office, UTC — он владелец времени съёмки. Измерение заявило: окно отчёта выводится из `capturedAt` обоих снимков, свои часы не использует, вырожденное окно — ошибка. Ангелина заявила: «день» журнала — календарный день владельца, `YYYY-MM-DD`, и журнал уступает снимку без боя. Канон: **владелец времени спринта — производитель снимка (office, UTC, ISO-8601)**; всё остальное — потребители и производные. День журнала — производная календаря владельца, в таймлайн не претендует. Ни один блок не меняется.

[Математик — Дынин]: Honest-шапки. Их три: у снимка (`capturedAt, sourceRevision, source, trigger, recordCount`), у холода (дата снимка, ревизия head, источник — условие приёмки, writer без неё отвергает), у отчёта измерения (окно, ревизии обоих снимков, множество артефактов). Общий закон уже действует у всех троих: **вход без провенанса отвергается, выход несёт провенанс**. В контракт — единый словарь полей провенанса: `capturedAt` / `sourceRevision` называются одинаково во всех трёх шапках; шапка отчёта обязана нести их для ОБОИХ снимков дословно.

[Верстальщик — Родченко]: Кириллические ключи возвратов субагентов (`фактура`, `источник`, `пробелы`, `дифф`, `что_открыто`) — дословно из вердикта M1, и ни один шов этого спринта их не пересекает: возврат субагента живёт целиком внутри домена Ангелины, до `isValid`. ASCII-адаптер не нужен — фиксируем это явно, чтобы на интеграции никто «на всякий случай» не транслитерировал. Форма = сообщение; лишний слой — враг.

[Музыкант — Курёхин]: `acceptanceHistory` — опциональное расширение артефакта закрытия от блока учёта: по ней наблюдаются эскалация и отзыв приёмки, без неё метрики деградируют честно. Кто пишет? Не блоки: это closure lifecycle, то есть адаптер интеграции при вшивании гейтов. Холод хранит её, если есть, — расширять `ARCHIVE_FORMAT` не надо, содержание карточки он и так несёт целиком.

[Teamlead — Веснин]: Два внешних факта, которые комнате решать не дано, но назвать обязана. Первый: office жив, а плечо office→Linear стабильно отвечает 400 на любые issue-запросы — похоже на ключ Linear на VDS; это гейт владельца, в контракт — как открытый пункт, боевой pull без него не полетит. Второй: `LinearIssueView` офиса не запрашивает `assignee`/`delegatedAgent` — вопрос «кто дополняет issue-view полями двухслойности» выходит за зоны всех трёх блоков; предлагаю НЕ тащить в этот спринт: producer честно отдаёт `delegatedAgent: null` (допущение A3 так и построено), расширение сервиса — отдельная задача офиса после ключа. Возражения?

[Математик — Дынин]: Нет возражений — с условием, что в контракте это записано не как «потом», а как проверяемое ограничение: до расширения issue-view и живого ключа поле `delegatedAgent` в снимке ВСЕГДА `null`, и ни одна величина на нём не строится. Проверил по словарю: пять величин читают `leadPersona` и статусы — на `delegatedAgent` не смотрит никто. Ограничение бесплатное.

[Структурщик — Ожегов]: Глоссарий стыка добиваю терминами, за которые сегодня уже спотыкались: **снимок** (не «кэш» — слово запрещено вердиктом M2 в этой подсистеме), **приёмка** (`acceptedBy`, событие «персона приняла выход»), **единица счёта** (GitHub-issue через биекцию), **запись снимка** (сырая запись источника, не единица счёта), **род субагента** (`analyst`/`scribe`, единственный публичный признак), **вердикт-код** и **exit процесса** (два пространства, не путать), **honest-шапка** (провенанс, без которого вход отвергается).

[Верстальщик — Родченко]: Интеграционный smoke собираю из наброска brief с уточнениями контракта: (1) фикстурный источник → producer → снимок `linear-snapshot@1` с полной шапкой; предикат свежести: свежий и протухший (вердикт-код 10, exit 0 с печатью); (2) адаптер-проекция records→cards → `checkLeadPersona` на карточке без персоны → вердикт-код 22, exit ≠ 0; отказ-II в soft → 12, exit 0; (3) `computeTeamMetrics` из двух снимков → отчёт с ревизиями обоих и подписью «wip = очередь намерений»; (4) `classifyDebt` на смешанном наборе → счётчик читает только `work`; (5) cold-writer: с шапкой принимает, без — отвергает; (6) `decideDelegation` на кейсе долгоживущего документа → писарь; `isValid` обоих родов; (7) все коды — по таблице контракта; собственные тесты трёх блоков зелёные в собранной ветке. Каждый пункт — исполняемый тест интеграционной ветки, не чек-лист на глаз.

[Музыкант — Курёхин]: Список адаптеров, чтобы Phase 4 не изобретал на ходу: (а) `snapshot-records-to-cards` — проекция снимка в вид батча; (б) `trace-gate-cli` — трансляция локальных кодов зубов в таблицу + проекция на exit, вшивание в `pre-push`/`task:archive`/closure review; (в) регистрация `LinearSnapshotModule` в `app.module.ts` офиса; (г) скрипты `package.json` (`angelina:*`, `trace:*`, `measure:*`, `snapshot:*`, `cold:*`, `debt:*` — по фактическим CLI); (д) провода Ангелины: `VIRTUAL_TEAM_PROMPT.md`, реестр псевдонимов, `standup-routing`; (е) запись `acceptanceHistory` в closure lifecycle. Стабы не мёржатся — все шесть фикстурных семейств остаются в тестах блоков.

[Teamlead — Веснин]: Голосую сведение. Канон приёмки `acceptedBy`/`headRev`; `angelina` в `leadPersona`; снимок `linear-snapshot@1` с минимумом полей; счёт только по единицам словаря; два кодовых пространства и таблица `0/10/11/12/20/21/22/23` с проекцией; владелец времени — производитель снимка; единый словарь провенанса трёх шапок; кириллица возвратов без транслитерации; `acceptanceHistory` пишет lifecycle-адаптер; `delegatedAgent = null` до ключа и расширения issue-view — вне спринта, гейт владельца; глоссарий, smoke из семи пунктов, шесть адаптеров. Кто против?

[Структурщик — Ожегов]: За. Термины разведены, ни одно слово не несёт двух смыслов.

[Математик — Дынин]: За. Оба вердикта M2/M4 исполнены буквально, инварианты проверяемы, ограничение на `delegatedAgent` бесплатное.

[Музыкант — Курёхин]: За. Ни один блок не переписывается — шесть адаптеров, ровно по регламенту.

[Верстальщик — Родченко]: За. Smoke исполняемый, каждый шов покрыт.

[Teamlead — Веснин]: Единогласно. Контракт пишу в `docs/cowork-sprint/cowork-execution-registry/INTERFACE_CONTRACT.md`, Phase 4 открывается: ветка `cowork/cowork-execution-registry/integration`, мёрж трёх блоков механически чист (зоны не пересекались), поверх — шесть адаптеров и smoke. Блоки в Phase 4 не пишут — ведёт координатор. Протокол закрыт, 27 реплик.

---

**Консенсус (письменно):** три EXPECTATIONS сведены без пере-резки brief и без
ведущей модели (S-C2 не понадобился); все расхождения гасятся шестью адаптерами;
напряжение M2↔M4 снято различением «вердикт-код / exit процесса»; два внешних
ограничения (ключ Linear на VDS, расширение issue-view) вынесены владельцу и в
спринт не входят. Скомпрометированных блоков нет.
