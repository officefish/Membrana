# Блок ritual-wiring — контекст Ожегова

Дата: 2026-08-03, вечер (перенос с завтра — слово владельца «есть ресурс делать сегодня»).

Глаголы package.json — мой предмет. Решения врезки:

1. **Обе цепочки получают open в начале и close в конце** — буква DoD. Утро:
   open ДО первого шага (обрыв на любом шаге оставляет сироту — её найдёт
   завтрашний open через границу файлов, это и есть требуемое «ловится со
   следующего утра»); close — ПОСЛЕ `ritual-deliver-to-main`, статус `pass`
   законен: `&&`-цепочка довела до конца. Вечер симметрично вокруг
   `ritual-evening-run`.

2. **Сторожа не глушат**: open/close идут БЕЗ `|| true`. Глушёный open ломает
   пару (close отказал бы «закрыть нечего» в самом конце, ПОСЛЕ доставки — худшее
   место), а молчаливый пропуск записи — ровно та болезнь, против которой спринт
   (47 версий документа дня, ноль записей). Сломанный журнал утром — честный
   красный владельцу, не вечерняя загадка. Прецедент M1-C того же рода: нет
   инструмента → СТОП, не импровизация.

3. **Вещдоки** — существующие носители, без выдумки: утро open —
   `docs/tasks/morning-ritual-steps.json` (манифест утра, есть до старта); утро
   close — `docs/MAIN_DAY_ISSUE.md` (рождён цепочкой); вечер open —
   `docs/MAIN_DAY_ISSUE.md` (к вечеру есть); вечер close — `docs/HANDOFF.md`.

4. **Зуб сверяет цепочку ПО ФАКТУ package.json** (как прецедент
   prepush-env-guard): читает строку глагола и проверяет порядок — open раньше
   первого шага, close позже deliver; никакой проверки «по догадке» через
   отдельный конфиг, который может разъехаться с глаголом.

5. Коммит package.json — поимённо (файл горячий, //zone-note плана).

## Поправка после красного CI (дописано 03.08 вечером — подмена предмета названа)

Гард ствола `step-status.test.mjs` («ritual:evening ведётся МАНИФЕСТОМ, а не сырой
&&-цепочкой», резон — инцидент 15.07: критичность выражалась порядком строк) поймал
мою врезку: я обернул вечерний глагол в цепочку. Гард прав, врезка переделана:

- `ritual:evening` — снова чистый раннер `ritual-evening-run.mjs`;
- open/close вечера живут В РАННЕРЕ, не шагами манифеста. Причина глубже гарда:
  шаги манифеста независимы, close-шаг со статусом `pass` отработал бы и при
  упавшем критичном — ложь статусом. Раннер — единственный наблюдатель итога:
  close пишется ПО ФАКТУ (`pass`/`fail` + gaps из имён упавших критичных шагов);
- `--dry` и `--only` записей не оставляют: это не вечер;
- отказ журнала громкий (не-ноль всего прогона), но шаги вечера не заложники
  записи — обязательства перед союзниками исполняются до последнего шага.

Зона блока расширилась фактом: `ritual-evening-run.mjs` и манифест вечера
(`//journal`-комментарий) — вне ратифицированной зоны 3б. Расширение вынуждено
гардом ствола, сделано в открытую и названо владельцу; утро (`ritual:day`)
осталось в зоне — цепочкой, как ратифицировано. Отдельный урок в friction свода:
локальный свип не включал `step-status.test.mjs` — сторож соседнего предмета
узнал о моей правке раньше меня.

## Операционный контракт close (дописано по ревью PR #1680, P1)

`--status pass` в цепочке зашит НАМЕРЕННО, и это не дыра, а контракт:

- `pass` достижим ровно одним путём — вся `&&`-цепочка дошла до конца. Упал любой
  шаг → close НЕ вызывается вовсе; никакой ветки, где обрыв пишет `pass`, нет.
- Оборванный прогон остаётся открытым ДО СЛЕДУЮЩЕГО open той же процедуры —
  завтрашнее утро закроет его `fail`/`orphaned` кросс-файловым промётом CLI.
  **Открытая запись в ленте — не баг инструмента, а честное «прогон не довезён»;
  журнал не ясновидящий, обрыв виден со второго запуска** (тот же контракт, что
  у ленивого закрытия библиотеки, //fail-mechanism плана).
- Цепочка сама себе `fail` не пишет принципиально: статус провала требует
  наблюдателя, пережившего обрыв, — им и является следующий прогон.
