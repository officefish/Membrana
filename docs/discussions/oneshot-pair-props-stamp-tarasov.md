# Обсуждение: oneshot-pair-props-stamp-tarasov

<!-- Автогенерация yarn ask. Каждый блок ниже — одно обращение к персонажу. -->

## 2026-08-03 12:35 UTC · tarasov

**Вопрос:** Прогон one-shot по НОВОМУ формату (тезисы шторма 03.08, Т1-Т8): прошу три акта разом — штамп S, назначение исполнителя (Т4) и суждение однородности с измеримым основанием (Т5-Т6). НАХОДКА (подобранная, из разбора Дынина при ревью ядра #1641): два непокрытых свойства пары honest_pair. Свойство 1 — порядок внутри пары: пара review_pass(10:00)+context_run(15:00) даёт honest_pair, ревью судило работу до прогона контекста; существующая находка ловит только 'ревью раньше ПОДПИСИ'. Лечение: находка eg-review-before-run (канал находки, вердикт не меняет) ПЛЮС замер по 30 лентам истории — если обратный порядок частый, поднятие до вердикта отдельным решением владельца. Свойство 5 — ревизия сдвинулась между следами пары: разбор показал, что это УЖЕ закрыто композицией #1641+#1638 (протухший прогон → dq по акту → incomplete_trace), лечение — один зуб-сценарий, закрепляющий композицию, и снятие свойства из списка пределов. ПЛАН ФАЗ (пункты чек-листа по Т7, не сущности): (1) находка eg-review-before-run в predicates+gate-exit-codes ~20 строк; (2) зубы обоих свойств ~60 строк; (3) замер порядка пары по docs/sprint/trail/*.jsonl числом в отчёт ~разово; (4) след и отчёт. Предикат S: forecast ok, 3 файла ~120 строк, цепочка чиста. ИЗМЕРИМОЕ ОСНОВАНИЕ однородности: М1 — один корень scripts/, один предмет (гейт исполнения); М2 — связный подграф: predicates.mjs + его реестр кодов + его зуб. Ответь тремя актами: 1) штамп S — да/нет; 2) однородность по М1/М2 — шот или спринт; 3) исполнитель по природе находки (Т1: профильный контекст, не лицо) — кого назначаешь и почему.

**Ответ:**

Положение: находка предметная, разбор чист, композиция #1641+#1638 закрывает свойство 5 — это фиксируется зубом, не новым вердиктом. Свойство 1 — реальный пробел канала находки, лечение соразмерно (канал + замер, без эскалации до вердикта без данных).

Три акта:

1) **Штамп S — да.** Forecast ok, 3 файла ~120 строк, цепочка чиста, предикат держится. Замер по 30 лентам — разовый скрипт в отчёт, не отдельный артефакт кода.

2) **Однородность по М1/М2 — шот.** М1: один корень `scripts/`, один предмет (гейт honest_pair). М2: `predicates.mjs` + `gate-exit-codes` + зубы одного гейта — связный подграф, спринт разрывать нечего. Порядок фаз внутри шота: (1)→(2)→(3)→(4), замер идёт после зубов, чтобы число легло в отчёт вместе с ними.

3) **Исполнитель — Структурщик.** Природа находки: канал находки в реестре предикатов + коды выходов + связность подграфа гейта. Это предмет структурщика (границы модулей внутри `scripts/`, реестр кодов, консистентность каналов), не математика и не DSP. Математик подключается только если замер по 30 лентам вскроет распределение, требующее решения владельца о поднятии до вердикта — тогда отдельная связка, не сейчас.

Вердикт: **LGTM** на шот; принимаю по факту трёх файлов + числа замера в отчёте.

[память:подсознание] emerge tarasov-2026-07-29-tariff-grid-m4-memories — та же дисциплина «планка матча узкая, не лезем в соседние M»: здесь ровно так же — свойство 5 закрыто композицией, не тащим его в вердикт; свойство 1 — канал находки, не эскалация без замера.

---
