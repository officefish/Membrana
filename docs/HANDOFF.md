# HANDOFF → 2026-08-05 · **магистраль: выбор владельца из утреннего топ-3 (кандидат дня — возврат продукта)**

**Точка входа новой сессии.** Хендоф 04.08 снят (архив —
[`docs/handoff/HANDOFF-2026-08-04.md`](handoff/HANDOFF-2026-08-04.md)). Его десятка:
строки 1–5 закрыты (магистраль спринтом `archivarius-live-wiring`, runId-фикс #1694),
6–7 закрыты соседом (#1706, #1707), 9 — ревизией PR (#1715); живы хвосты строк 2–4
(ADR принят → реализация) и 8 (oversized-рутина).

> **Провенанс:** отчеканено вечером 04.08, **перечеканено в середине дня 05.08**
> после закрытия санитарной строки — прежняя десятка протухла за полдня (строка 1
> сделана, строка 3 закрыта соседом ночью). Дерево `Membrana-weave`, ветка
> `angelina/work/2026-08-05`, свежее относительно `origin/main` (`51cba97c`).
> Замеры середины дня: 100 открытых иссью, 2 открытых PR, 227 активных карточек,
> прогоны CI по `gh run list` — не из памяти.

## Что случилось, в двух предложениях

**04.08 закрыл магистраль живьём** (контейнер сессий: 106 884 спана трактом, поиск
Mongo-индексами, ADR-0023 DRAFT → ACCEPTED за день), но продукт не двигался четвёртый
день подряд. **Утро 05.08** выбрало магистралью ночную сборку инструментов и закрыло
санитарную строку делом: **образ кабинета зелёный впервые с 02.08** — оборвана серия
из 72 красных прогонов; при этом посылка вечернего фидбека о «трёх красных сервисах»
не подтвердилась — падало другое и по другой причине.

## Что вскрылось при этом — два инструмента судят недостоверно

**Пре-пуш валит отгрузку ложным красным** (#1725): межпакетный typecheck читает
протухший `dist` чужого дерева (`Membrana-grok`), своих пакетов **2 из 37**. #1647
сделал диагностику честной — хук печатает «локально недостоверен» — и всё равно гонит
проверку и блокирует push. Сегодня обойдён руками по слову владельца, запись обхода
в прогоне шота.

**Цепочка ревью не имеет запаса прочности**: три звена, из них первое и третье — одна
модель за одной квотой (исчерпались вместе), у среднего (`grok`) в дереве **нет ключа
вовсе** (живой #1549). Ревью встало намертво; разблокировано разовым словом владельца
на умолчания (`source=default` в артефакте).

## Топ-10 на 05.08 (перечеканено в середине дня)

| # | Задача | Что даст проекту | Почему именно сейчас | Размер | Занято |
|---|---|---|---|---|---|
| 1 | **`agent-tooling-night-build`** — ночная сборка инструментов агента | Утро начинается с готового «всё цело» вместо часа выяснений; ночь проверяет, но не сливает и не выкладывает | Магистраль дня, выбор владельца из замороженного топ-3 (`74f6d216…`). Лечит КЛАСС болезни, из-за которой красный CI жил трое суток | L | **магистраль** |
| 2 | **#1725** — пре-пуш блокирует отгрузку ложным красным | Инструмент перестаёт судить тем, что сам объявил недостоверным | Живой блокер: сегодня обойдён руками по слову владельца, следующий обойдёт молча. Замер: своих пакетов 2 из 37, остальные резолвятся в чужие деревья | S | свободно |
| 3 | **Цепочка ревью без запаса прочности** — завести долг и развести звенья | Ревью перестаёт вставать целиком при одной исчерпанной квоте | Сегодня встало намертво: 2 звена из 3 — одна модель за одной квотой, у третьего нет ключа. Разблокировано только разовым словом владельца — это не механизм | S | свободно |
| 4 | **`detector-scoreboard` Ф1** — план таблицы (DoD до кода) | Первая витрина измеренного качества детекции | Продукт не двигался пятый день; CI теперь зелёный — довод «числа нечем подтвердить» снят сегодня же | M | свободно |
| 5 | **Первый настоящий прогон `deploy`** — DoD ADR-0023 | Решение о форме узла перестаёт быть бумагой: выкладка оставляет запись | Манифесты двух процедур и обёртка `deploy:run` влиты ночью (#1717), но прогонов в журнале **ноль**. Упирается в owner-gate выкладки — рука владельца | S | **ждёт владельца** |
| 6 | **#1710** — дверь отзыва `recut_act` в гейте мертва | Перерезка перестаёт давать ложный красный | Однострочный провод (sprintId↔planId) + зуб на настоящем плане; зубы гоняли только стабы — потому дыра и жила | S | свободно |
| 7 | **#1714** — `archivarius-mongo` в манифест живых служб + бэкап тома | Носитель со 106К записей перестаёт быть немым | Данные живут третьи сутки, страховки нет; признано условием мерджа #1711 | S | свободно |
| 8 | **#1703 + #1704** — два зуба детекторного контура | Сведение четырёх копий окна получает настоящие инварианты | P1 ревью #1648; фундамент строки 4 — без них витрина покажет числа, которым нечем верить | S | свободно |
| 9 | **`procedure-run-journal-panel-reader`** — первый читатель ленты | Неделя вложений в журнал превращается из письма в чтение; закрывает #1626 п.1 | Производители на рельсах, лента полна; карточка и промпт готовы со вчера | M | свободно |
| 10 | **Зависшие PR #1613 и #1720** + рутина oversized по одной в день | Доска не копит непринятое | #1613 ждёт LGTM архитектора по контракту; #1720 — ночной триаж, течёт мимо канала инсайтов (#1445) | S | свободно |

## Горизонт — вне десятки, но не потеряно

| Работа | Срок | Почему держим на виду |
|---|---|---|
| **Корпус «один тракт — два класса», 600 звуков** | весь август | Приёмный предикат оформлен карточкой `corpus-track-acceptance-predicate` (S, критерии из инсайта) — сделать ДО записи; сам корпус — отдельным сервисом, упирается в процедуру deploy (строка 3) |
| Прод-выкладка office с парами `ARCHIVARIUS_*` + smoke | owner-gate | `office-stack.sh smoke` готов; генератор подсказывает дописку env; выкладка — рука владельца (`deploy:when-green`) |
| Предикат `amnestyLifted` (остаток гейта `secret-parser-built`) | рука владельца | Отложен словом владельца 03.08; единственный формальный хвост гейта |
| Вторая опора дня 04.08 (кристаллизация, вопрос 2) | слово владельца | Первый кристалл отчеканен (`code-only-through-format-with-responsible-executor`); полный урожай 3+3 вечером не собран — вечер ушёл в доставку |
| Замер распределения размеров спанов | P2 Дынина | bodyLimit 25 МиБ оправдан наблюдённым максимумом ∈ (1, 25) МиБ; p99 не зафиксирован числом |

## Ловушки, проверенные боем 04.08

- **База уезжает под медленным CI большого PR** — три круга посадки #1711 и два у #1721.
  Лечится `yarn task:pr-land <N> --execute`; конфликты append-only JSONL (журнал,
  оп-логи) — union обеих сторон, НЕ выбор стороны. **Ветку PR не убирать до
  `gh pr view <N> --json state` = MERGED** — поспешная уборка закрыла PR дважды за день.
- **`pr:ship --merge-only` резолвит PR по ТЕКУЩЕЙ ветке** — фоновый мердж при
  переключённой ветке бьёт мимо. Ветко-зависимое — только последовательно.
- **Windows-алиас `python` молча глотает heredoc-скрипты** — файл не пишется, коммит
  берёт болванку (BLOCK #1696). Писать файлы инструментом, свой `print` проверять глазом.
- **Локальный curl к 127.0.0.1 уходит в прокси окружения** — 502/400 на живом сервисе;
  `--noproxy "*"` или `NO_PROXY`.
- **HTTP 413 на батчах спанов** — счёт спанов ≠ счёт байтов; лимит теперь маршрутный
  (25 МиБ только на `POST /v1/archivarius/ingest`), деление батча в тракте есть.
- **Пре-пуш валит отгрузку на сетевом снимке старше 48 ч** — `yarn network:snapshot`, штатно.
- **Авто-open журнала = дерево ритуала «грязное» своим же трейлом** — вылечено #1689
  (манифест утра знает продукт), но следить: у каждой цепочки свой манифест.

## Хвосты — состояние, не задачи

- **`main` никем не держится** — Membrana-tooling уведён на `chore/tooling-idle` утром
  05.08 (дерево было чистым, вчерашняя правка `registry.json` уже влита).
- **Вечер 04.08 в журнале закрыт `fail` на кадре доставки** — честно: цепочка встала,
  доставка доведена руками следом (#1721, deliver ✓). Читать как «цепочка не умеет
  ждать PR», кандидат в норму, не как провал вечера.
- **~~Красный CI~~ — закрыт 05.08**: корень был не там, где называл фидбек (не три
  сервиса, а docker-сборка Prisma-пакетов мимо turbo, #1724). Cabinet images зелёный
  на `51cba97c`, серия из 72 красных оборвана. Зуб инварианта не даст повторить молча.
- **Два обхода дня записаны, не спрятаны** — в прогоне шота `one-shot-1724-2026-08-05`:
  `predicate-s-red-owner-routed` (шот тронул серверные пути — запрет по построению) и
  `prepush-bypassed-owner-routed` (ложный красный, #1725).
- **Дерево `Membrana-weave`** — на `origin/main`, чистое; дневная ветка `angelina/work/2026-08-05`.

## Точки входа

| Что | Где |
|---|---|
| фидбек и фокус завтра | `docs/seanses/team-evening-feedback-2026-08-04.md` |
| хроника и ревью дня | `docs/DAILY_AUDIT.md` · `docs/DAILY_CODE_REVIEW.md` |
| журнал прогонов (живой день) | `docs/procedure-runs/trail/2026-08-04.jsonl` |
| рамка деплоя | `docs/adr/ADR-0023-deploy-node-form-one-procedure-per-echelon.md` (ACCEPTED) |
| архивариус: контракт и тракт | `docs/archivarius/README.md` · `yarn archivarius:push --dry-run` |
| очередь oversized | `yarn review:oversized` |
