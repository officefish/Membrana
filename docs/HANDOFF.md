# HANDOFF → 2026-08-19 (утро) · **плагинная основа принята, узел ожил — магистраль: узел как устройство**

**Точка входа новой сессии.** За четыре дня (15–18.08) пройден путь от «оборудование
приехало» до принятой серверной плагинной основы и живого полевого узла. Каждая строка
ниже сверена по стволу и GitHub 19.08 утром. Магистраль дня здесь **не объявлена** — её
даёт owner-choice утреннего гейта; слово владельца вечером 18.08: **узел как устройство**
(приложение живёт на узле, само держит канал к серверу, получает права при установке).

## Что принято (не работа — контекст)

| Что | Вещдок |
|---|---|
| **Плагинная основа сервера — первая волна принята (Т3.11)** | шторм → собрание 10 комнат + аудит + 3 переигрывания → ратификация 17.08 → 13 PR четырёх сессий 18.08 → живой след `runId 01a0150f-95e6-718b-bfa7-4ba313511a10` в `plugin-results`; маркер `docs/plugins/first-live-run-2026-08-18.md`; вердикт `docs/meeting/server-plugin-foundation/MEETING_VERDICT.md`; эпик [#1961](https://github.com/officefish/Membrana/issues/1961) |
| **Узел Firebat пишет и отправляет сам** | заводской режим карты без драйвера Focusrite, **48 кГц**, две записи приняты сервером 18.08 (`62fe8ea1…`, `c4cfbf06…`); скрипт переносим одним файлом (`scripts/field-capture.mjs`, фикс пути к `.env` PR #1977); SSH-сервер стоит, но узел за вторым роутером — вход снаружи закрыт |
| Тракт записи | `yarn field:capture`: микрофон → карта → запись → сервер сам измеряет; устройство `field-node-2026-08`, коллекция «Полевые записи 2026-08» на media |
| Слепой корпус пилота | 33 записи обезличены (`yarn vdr:blind`, PR #1940); mfcc в измерителе (F1 ноль целых шестьсот шестьдесят семь тысячных на пилоте — предварительно) |

## Живой остаток (кустами, с маркерами)

| Куст | Что | Маркер |
|---|---|---|
| **Узел как устройство** (магистраль-кандидат) | приложение на узле держит канал к серверу само; рабочее усиление с запасом (щелчок дал −1 dBFS); отдельный ключ на загрузку вместо служебного токена; круглосуточный режим — другой глагол, не `field:capture` | план `docs/prompts/SESSION_A_FIREBAT_NODE_2026-08-18.md` (ступень 1 закрыта, 2–3 нет: клиентский инсталлятор ни разу не собирался) |
| Плагинная основа — хвосты | мост media → office для результатов внутри сервисов (сегодня пишет скрипт); [#1972](https://github.com/officefish/Membrana/issues/1972) общий WAV-декодер, зуб границ; замечание Ожегова: модульный синглтон динамического импорта в хосте | эпик #1961 открыт до моста |
| Приём записи | объявленное перебивает измеренное по трём полям | [#1950](https://github.com/officefish/Membrana/issues/1950) |
| Слепота корпуса | провенанс лежит в репозитории рядом с манифестом | [#1941](https://github.com/officefish/Membrana/issues/1941) |
| Санитария | `rag-service#test` красный третий день (диагноз не поставлен); резак секретов — веха `secret-parser-built` не закрыта; `makeIsIgnored` throw; `aria-live` промокода | фидбек 18.08 |

## Уроки недели (в канон, кандидатами)

- **Несущее — только именами в строке вопроса**; вердикт комнаты — отдельным файлом (аудит собрания 17.08: три подмены посылок там, где передавали ссылкой).
- **Посылка, опровергнутая влитым PR, снимается в момент влития** — иначе блокирует соседа (18.08: ревью В блокировало по моей протухшей посылке; третий случай класса).
- **Здоровье дерева меряется живой сборкой**, а не наличием папки; песочница агента ссылок не создаёт — `yarn install` от владельца или `winLinkType: junctions` (в стволе с 16.08).
- **Измеренное не подменяется объявленным** — граница на приёме (#1950) и на съёмке (`field-capture` не кладёт measured-поля в meta).

## Среда

Деревья `tooling` · `product` · `weave` · `grok` — здоровы (38/38/38/37 связей); в `tooling` четвёртые сутки два несохранённых файла соседа (`docs/discussions/*-run.md` от 14.08) — **не трогать, эскалировано**. Ночь зелёная и свежая ежедневно; подтяжка ночного отчёта починена (16.08).

_Сверено 2026-08-19 утром. Строки без проверяемого маркера в этот список не попадают (норма #1744)._
