# HANDOFF → 2026-07-18 (день) · спринт `тулинг-контейнер` (линзы верификации)

**Центральная задача: первая проба линз — навести бестиарий антипаттернов на процесс стратегии, получить ЧИСЛО разрывов.**

Опора распределения дня (кристалл `standup-distribution-18-07`, волеизъявление владельца): команда наводится на **тулинг-контейнер «рефакторинг инструментов»**.

## Контекст одной фразой

Вчера-сегодня родился класс **линз верификации** (инструмент, который наводится на объект и подсвечивает дефекты одного класса). Вердикт: это класс, не наблюдение. Контейнер «рефакторинг инструментов» = **сессия наведения** линз на наш тулинг, НЕ пакет. Первая проба отвечает на «нужен ли спринт рефакторинга процесса» **числом**, а не ощущением.

## Что читать первым

1. **Вердикт линз:** [`docs/seanses/lenses-verification-class-container-2026-07-18.md`](seanses/lenses-verification-class-container-2026-07-18.md) — форма `lens: (object, ruleset) → finding[]`, наводимость = каррирование, контейнер = сессия, матрица покрытия `инструмент × линза → {not-run, clean, N}`.
2. **Инсайты (по одному research-вопросу каждый):** `insight-weekly-antipattern-audit-bestiary` (бестиарий + детекторы), `insight-ozhegov-lens-speech-purity-skill`, `insight-evening-ritual-three-fuel-documents`, `insight-landing-challenge-sources-in-battle`.
3. **Находки утра (долг):** [`docs/seanses/morning-findings-2026-07-18.md`](seanses/morning-findings-2026-07-18.md) — 7 находок с локусами.

## Бестиарий (детекторы вживую)

Каждый зверь с грубым детектором (доказано грепом 17.07 — 30 `|| true`, 58 `?? null`, 4 сироты):
- **Молчун** — `exit 0`/`?? null`/пустой `catch`/ручной список ≠ реальности. Различать намеренный (декларирован) vs дефектный (нет).
- **Эхо-камера** — снимок в N местах = мнимый консенсус. Детектор `origin-hash` (готов в `lib/truth-graph.mjs`).
- **Половина без провода** — экспорт есть, потребителей `grep=0`.
- **Украшение** — работает, никто не читает (`grep` потребителей = 0).
- **Смещение цели** (Goodhart) · **Жаргон наружу** (стоп-лист `externalizeQuery`, дыряв — #599).

## Первая проба (центральная)

Навести бестиарий-линзу на **процесс стратегии** (генератор `strategy-day.mjs` + цепочка `ritual:day` + `main-day-issue`, которые чинили утром) → заполнить строку матрицы покрытия. Гейт: каждая находка с владельцем фикса; не аудит ради аудита (Goodhart-гейт из вердикта).

## Состояние: всё в main, ветка `docs/truth-registry-live`

Утро 18.07 закрыто: генератор стратегии **подключён к ритуалу** (три провода, `ritual:day`→новый генератор, `main-day-issue` читает граф+owner-source, магистраль не синтезируется). Эпик регламента утра **[#605](https://github.com/officefish/Membrana/issues/605)** — ядро закрыто (барьер против проглоченного гейта, 4 фазы, тесты 11/11); остаток Ф5 оркестратор / Ф6 UI. Опоры дня 18.07 отчеканены.

## Ловушки дня (в спринт)

**1. Инструмент не врёт — он молчит; молчание = мнимый успех.** Весь смысл линз. Сегодня фоновый агент САМ сел на эхо (`MAIN_DAY_ISSUE` вместо кода) — контракт правлен на сверку с кодом. **Сверяй находки с КОДОМ, не с документом.**

**2. Параллельные сессии — коллизия.** Сосед активен (Replit, night-build-format), main двигается. Коммить свои файлы **поимённо**, `git add -A` не использовать. Union-драйвер только для `docs/tasks/registry.json`; `truth/registry.json` и `insights/registry.json` — сливать union по id вручную (был скрипт в scratchpad). Перед push всегда `git fetch` + merge.

**3. Тесты в CI — не сироты.** Новый `*.test.mjs` → зарегистрировать в `test:scripts`. Сосед добавил `test-list-coverage.test` — форсинг-функция, сама поймает сироту.

**4. Граф правды: опора с derived-детьми при отзыве → `superseded`, не `revoked`** (revoke осиротит детей, I3). Урок утренней кристаллизации.

## Инструменты линз (что уже есть)

- `yarn morning-ritual:run` — барьер утреннего ритуала (витрина/мостик/`--decide`).
- `node scripts/truth.mjs verify` — граф правды (I1–I7, evidenceLabel).
- `origin-hash` в `lib/truth-graph.mjs` — детектор эха, переиспользовать.
- `main-day-probe` — линза на посылки плана.

## Тулинг-долг в контейнер (открытые issue)

`#595` (бестиарий/тулинг-friction) · `#599` жаргон-стоп-лист · `#598` night:research молчун · `#584` scripts не под линтом · `#554` friction-3 · `#476` tooling-needs · `#605` регламент утра (Ф5/Ф6). Смежно: `scripts-boundary` (сосед, M0 ратифицирован).

## Что решение обязано дать (по вердикту линз)

Контракт формы линзы (документ, не ABC) · реестр линз (5 существующих: Ожегов, бестиарий, probe, evidenceKind, origin-hash) как словарные статьи, НЕ в `registry.json` · регламент сессии «рефакторинг инструментов» + шаблон матрицы покрытия · схлопывание столбцов одного `defectClass` · `not-run` ≠ `clean`.

## Инфра (живое)

office `176.124.218.4` (МСК): LLM через media-прокси (мёртв — чинить, #593). RAG падает на гео-403 (openai-эмбеддер не через прокси, #593 — починка БЕЗ новых кредитов). Perplexity доступен через туннель `127.0.0.1:12334` (научился ходить 17.07). DeepSeek — пополнить (402 баланс); Voyage/OpenRouter — после починки сети.

## Тулинг-грабли (Windows/git-bash)

`curl` нет → node/undici. Пайп съедает exit-код (`cmd > f; echo $?`). PowerShell 5.1 портит UTF-8 → Write/Edit. `yarn docs:lint` перед коммитом. `node --input-type=module` + `require()` = ошибка → только `import`.

## Предыдущий хэндофф

Затёр HANDOFF → 17.07 (спринт `strategy-day-generator` #592 — генератор написан и подключён, эпик закрыт по факту). Это закрывает находку утра «HANDOFF протух».
