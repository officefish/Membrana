# HANDOFF → 2026-08-25 (утро) · **контур наблюдаемости на проде; магистраль дня — панель отбора в кабинетной библиотеке**

**Точка входа новой сессии.** 23.08 ночное дежурство положило кабинет (квадратичный обход
журнала); 24.08 за один день: шторм → собрание → пять кусков наблюдаемости, четыре из них на
проде к вечеру, причина аварии снята в стволе и выкачена, сторож диска шлёт тревоги владельцу
в личный Telegram. Каждая строка ниже сверена по стволу, GitHub и живому проду утром 25.08
(`yarn task:handoff` / `yarn tasks:handoff-liveness` читают таблицу ниже машинно). Магистраль
дня — **кандидат по слову владельца 24.08 вечером: панель отбора в кабинетной библиотеке**;
чеканка — рукой владельца утром из замороженного топ-3.

## Что принято (контекст, не работа)

| Что | Вещдок |
|---|---|
| **Прод кабинета** на `a1ca7f05`+: журнал берёт страницу из базы (#2127), куски A–D наблюдаемости, DI-починка #2142; смоук 6/6; `/health/deep` отдаёт числа (лента 3209, база 3 мс) | деплой 24.08 рукой ведущей сессии (стена `cabinet:deploy` снята владельцем); трейл `docs/procedure-runs/trail/2026-08-24.jsonl` |
| **Прод media** на стволе 24.08: витрина отбора `membrana.showcase.library-chart-list` на доме `background-media/collections` | деплой 24.08 вечером, `Deploy finished OK` |
| **Сторож диска (Д1) на посту** с 20:15 МСК 24.08: бот `@MembranaWatchdog_bot` → личный чат владельца; боевое сообщение дошло | комментарий в #2118; таймеры ×2 по 5 мин на media-VPS |
| **Фрейм фидбека судит прожитый день** (#2107): магистраль из гейта, `readAt` семи входов, чистый предикат; первый живой прогон — 7,8/10 | PR #2136; `docs/seanses/team-evening-feedback-2026-08-24.md` |
| **Библиотека сэмплов в Studio и в кабинете — близнецы**, функционал ложится в оба (слово владельца 24.08) | панель в Studio — #2130; ядро с промежутком дат — #2124 |
| **Ночь 23.08 цела:** 1136 проб, 99,5 мин, 0 разрывов, 48 кГц; разбор аварии и три антипаттерна в бестиарии | `docs/field/2026-08-23-night-duty-journal-congestion.md` (#2111) |
| **Офис измерен под Сентри:** 2 vCPU · 3,8 GiB · после уборки 30 GB диска; апгрейд не раньше **10.09** | комментарии в #2122 |

## Топ-10 дня

| # | Работа | Носители | Занято | Вещдок | Заметка |
|---|---|---|---|---|---|
| 1 | `sample-library-cabinet-chart-list` — панель отбора в **кабинетной** библиотеке: близнец Studio-панели в ряд к `SampleLibraryPlayerPanel`, та же витрина на текущей коллекции из сайдбара; проверить b4 (свежие сверху, подпись per-sample, перенос со словом) в кабинетной таблице — #2110 | #2110 | — | витрина на проде media; ядро #2124 | **кандидат в магистраль** (слово владельца 24.08 вечером); приёмка — владелец собирает выборку из ночного улова на проде |
| 2 | `week-without-repeats` — пять зубов под повторившиеся классы: DI-смоук на три Nest-приложения · `pr:ship` падает ненулём · предикат доставки в докладе сессии · зуб `llm-panel-wire` в CI · `studio:package` собирает зависимости — #2147 | #2147 | — | пять из семи эпизодов 24.08 — повторы | подкрепление; приёмка — колонка «повтор» на 28.08 пуста, каждый зуб доказан порчей |
| 3 | `journal-linearization-acceptance` — мерочное «после» для линеаризации журнала (PR 2127, на проде) на живой ленте ≥2500: число запросов на пробу не зависит от длины ленты — #2113 | #2113 | — | «до» в комментарии к Issue; фикс на проде | закрыть на дежурстве **пт 28.08**; до того Issue держать открытым |
| 4 | `journal-delete-reconcile` — явный полный пересчёт после инкрементальной синхронизации: серверные удаления невидимы `since`-watermark — #2131 | #2131 | — | находка сессии В при #2127 | малый; можно взять сессией, свободной от магистрали |
| 5 | `health-deep-busy-in-idle` — `/health/deep` отдаёт `busy` в простое (дежурства нет → записей нет по праву); фикс (PR 2144, кусок D) в стволе, **на проде ещё нет** — пересборка кабинета подберёт; носитель — эпик наблюдаемости #2117 | #2117 | — | `genus: busy` на проде утром 25.08 | деплой кабинета при следующем окне; не срочно |
| 6 | `disk-watchdog-calibration` — калибровать `DW_WRITE_RATE_REF_MB_MIN` по ≥30 мин живой записи (ночь 23.08: ~5,3 МБ/мин против опорных 10); перевыпуск токена бота (дважды попадал в переписку) — #2148 | #2148 | владелец | README сторожа | калибровка — на дежурстве 28.08; токен — рука владельца |
| 7 | `media-vps-docker-cleanup` — media-VPS на 76 %; тот же класс мусора, что на офисе (там уборка дала 11→30 GB): `docker system df` → `docker builder prune -af` — #2148 | #2148 | владелец | замер 24.08 (кусок A) | рука владельца (ssh агентам закрыт); удлиняет горизонт сторожа кратно |
| 8 | `buffer-sorting` — разбор и подпись сэмплов владельцем: буфер **1727 проб / 797 МБ из 1024**, наборы 100 проб / 46 МБ; перед 28.08 буфер разгрузить — #2109 | #2109 | владелец 25–27.08 | quota media 25.08 | чистка дублей — показывать пары, не удалять по порогу |
| 9 | `sentry-container` — кусок E ждёт апгрейда офиса (2→4 vCPU, 3,8→16 GiB) **не раньше 10.09**; диск уже готов (30 GB); до того спящий чекан `TMP-…` — #2122 | #2122 | — | замер и уборка 24.08 в #2122 | **в план до 10.09 не брать** |
| 10 | `studio-rebuild` — установщик Studio для панели отбора: `studio:package` падал на stale dist зависимостей; сначала `turbo build --filter=@membrana/client...` (вошло в #2147 зубом) — #2110 | #2110 | — | последний установщик от 21.08 | нужен владельцу для разбора буфера новым инструментом; после зуба #2147 |

## Слово владельца, действующее (не переоткрывать)

- **Библиотека сэмплов в Studio и в кабинете — близнецы;** всё, что умеет одна, обязана уметь другая. Новую возможность планировать двумя блоками с общим ядром в `packages/`.
- **Следующее дежурство — пятница 28.08;** до него разбор задач и буфера, дежурств не ставить.
- **Сентри — полный self-hosted, свой дом на офисе;** при упоре — наращивание офиса, не лёгкий двойник. Апгрейд не раньше 10.09.
- **Сентри — отдельный контейнер-картотека**, не часть дома логов; три рода хранения сшиты номером происшествия; пульс в картотеку не летит — только его сбой.
- **Д1: сторож диска — первым, до всех сборщиков** (директива шторма, две подписи).
- **Деплой кабинета — рука ведущей сессии** (стена снята владельцем 24.08); ssh напрямую, прод офиса/media через docker-команды, force-push, reset --hard — закрыты как были. Деплой при живом сеансе записи запрещён (#2048).
- **Окно деплоя = мораторий на мерджи в main** (24.08: три круга гонки с CI-гейтом); ведущая рассылает сессиям сигнал «деплой завершён».
- **Наблюдаемость — издавать прежде чем собирать; следить нашими числами, не машинными** (Т1/Т2 шторма 24.08).

## Ловушки, пойманные 24.08 (для холодной сессии)

- `pr:ship` при уже сделанном руками коммите выходит **0 без пуша и PR** («nothing to commit») — мердж сверять только `gh pr view --json state`; дважды за день.
- Деплой-гейт требует зелёного CI на **вершине** ствола: параллельные мерджи перебивают прогон — просить сессии придержать main.
- `chat_id` сторожа — это **id получателя из `getUpdates`**, не id бота из `getMe` (путали дважды).
- `studio:package` не собирает зависимости — после переключения ветки dist пакетов протухает.
- Классификатор не даёт агенту править собственные разрешения — стену снимает владелец руками в `.claude/settings.local.json`.

## Открытые PR (не ритуальные)

`#1939` (слепой корпус пилота, 15.08) · три Night Hunt (#1831/#1846/#1876) · `#1728`, `#1793` — все старше недели, в очередь дня не входят; ревизия — `membrana-pr-audit`, по слову владельца.
