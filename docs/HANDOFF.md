# HANDOFF → 2026-07-17 (вечер) · спринт `strategy-day-generator`

**Центральная задача: спринт по эпику [#592](https://github.com/officefish/Membrana/issues/592) — генератор стратегии дня.**

Спека: [`docs/prompts/STRATEGY_DAY_GENERATOR_PROMPT.md`](prompts/STRATEGY_DAY_GENERATOR_PROMPT.md) — **читать первой, она полная** (вердикты, работы S1–S7, DoD, границы). Здесь только то, чего в ней нет.

## Контекст одной фразой

Генератор стратегии дня задуман определять **дальние границы дня** (горизонт), а по факту пишет DoD и назначает исполнителей. Расследование 17.07: **из семи задуманных функций работает одна** — порядок запуска. Пять заседаний консилиума дали вердикты, они сверстаны в спеку. **Кода нет — спринт с чистого листа.**

## Состояние ветки

`docs/truth-registry-live`, **8 коммитов, не смёржена**:
- `245f4f9d` fix(truth): экран графа врал о доказательствах — 21/21
- `e12b3195` · `397b291e` · `ef01cbe0` — кристаллы (реестр 24 → 38)
- `741ac77f` · `470c6f54` — протоколы Q4b и Q5
- `4966d7b3` — эпик #592

В дереве **чужое, не трогать**: `apps/demos/Research-Tree/vite.config.js`, черновики в `docs/comms/drafts/`, `docs/prompts/DETECTOR_METRICS_CHARACTERIZATION_PROMPT.md`.

## Три ловушки дня — они про эту работу, не про настроение

**1. Инструмент не врёт — он молчит, и молчание читают как успех.** За один день:
- Канал аналайзеров: крон падает **9 раз подряд с 18.05**, артефакта нет нигде, `readBoundedFile → null` → генератор молча идёт дальше. **Два месяца никто не заметил.**
- Таблица `truth verify` печатала «доказательства нет» **всем 21 кристаллу с указателем**, пока инвариант I7 честно молчал.
- Тесты ядра давали 46/46 зелёных — стояли ровно там, где ошибки не было.

**В DoD эпика два теста-памятника этому. Не выкидывать:** отсутствующий вход обязан дать **видимую пометку**, а не `null`; три строки одного `origin-hash` → `n = 1`.

**2. Половины построены, провод не кинут.** `includeInsights` написан и выключен · граф правды в `main`, а стратегия о нём не знает (`grep = 0`) · `insight-sunrise-flash` принят **25.06**, статус `adopted`, не построен · парсер секретов: детектор есть (`night-triage-secret-scan.mjs`, паттерны готовы), резака нет. **Прежде чем писать новое — грепнуть, нет ли половины.**

**3. Отрицательный результат поиска предъявлять со способом поиска и его границей.** 17.07 греп трижды промолчал, и трижды молчание едва не уехало в канон как факт. Указатель на реплику владельца живёт в **трёх местах**: `type: "user"` · `type: "attachment"` (реплика посреди хода, текст в `attachment.prompt[].text`) · `tool_result` (клик). Фильтр по `user` слеп к двум из трёх.

## Что решено сегодня и чего нет в спеке

- **Реестр правды: 36 активных кристаллов** (`docs/truth/registry.json`), `yarn truth:verify` зелёный, **первые два отзыва** в истории — append-only отработал. Стратегия реестр **не читает**: это работа S7.
- **Пять вопросов владельцу висят в `openGaps`.** Первый острый: амнистия на правку архива привязана к предикату «парсер построен», но парсер **никто не строит** → разрешение бессрочно, стимул обратный нужному.
- **Ротация засвеченных ключей не сделана.** Скан: до 186 совпадений по секрет-паттернам в 44 сессиях (156 МБ), доля живых неизвестна. Решение владельца: **ротация + один датированный проход с манифестом**. Инвентарь ключей не начат.

## Хвост, который эпик не прячет

**S5 — судьба мёртвого крона** `weekly-analyzers-research.yml`. Вердикт Q5 неявно отправил его в отставку (ночной ресёрч занял место), но крон стоит и падает. **Причина 9 падений неизвестна**: гейт `ANTHROPIC_API_KEY` проходит, падает сам шаг `Generate`, логи забиты шумом установки. Решить явно: чинить, удалить как фикцию или заморозить вместе с недельной.

## Направление владельца: реестр и граф уезжают НА СЕРВЕР

**Не догадка агента — слово владельца с указателями в транскрипте 17.07:**

- **10:06** (`f92a2cda`) — реестр задач и граф правды делать **через `office.mmbrn.tech`, возможно с mongodb**: правда подкреплена сессиями, а сессии **локальны** → нужны **бэкапы сессий на сервере**, чтобы каждый агент мог проверить факт на бэкапе.
- **10:12** (`cca67f84`) — в реестре задач **постоянная гонка**: то один агент записал задачу первой, то второй. «Такого быть не должно». Каждому worktree — **свой кеш под актуальную задачу**; за `CURRENT_TASK` постоянно дерутся.
- **10:13** (`3fb84f41`) — **можно вместо монго пока попробовать редис**, но **граф планировать основательнее**. «То, что всё уходит на сервер, меня как раз устраивает». **Скоро оркестрация разработки с нескольких компьютеров.**
- **17.07 вечер** — подтверждено ещё раз: для задач и агентских сессий строить **новое серверное окружение на MongoDB + Redis**.

**День подтвердил это трижды, не спрашивая:** 201 «активная» карточка · хэндофф как файл, за который дерутся сессии · указатель на правду живёт **только на локальном диске** — сосед проверить не может.

**Эпик #592 этого НЕ включает** и включать не должен: это отдельный контур. Но при работе над S7 (стратегия читает граф) — **не закапываться в локальный `registry.json` как в вечное**: он временный.

## Цифра, которую надо назвать

**201 активная карточка в реестре.** «Активная» сейчас не значит ничего — а `main-day-issue` читает именно это. Карточка `rt-7-priorities-from-registry` ровно про это, и она же **симптом** направления выше. Всплывёт в спринте: **не изобретать решение на ходу**.

## Инфра (живое, из прошлого хэндоффа — не трогать без нужды)

office `176.124.218.4` (Timeweb МСК; блок ТСПУ был IP-specific): night-triage (02:30 МСК) и night-hunt (будни 07:10) боевые, LLM через **media-прокси** `72.56.27.58:8888` (tinyproxy, NL, обход гео-блока — `OpenRouterService` прокси **не видит**, только `ClaudeService`).

## Тулинг — грабли, на которые уже наступали

- **Труба съедает код возврата:** `cmd | head; echo $?` вернёт код `head`. Писать в файл: `cmd > f; echo $?`.
- **PowerShell 5.1 портит UTF-8** без BOM (`Get/Set-Content`) — файлы править только Write/Edit.
- **Параллельные сессии:** коммитить свои файлы **поимённо**, `git add -A` не использовать.
- `yarn docs:lint` — гейт кодировки, гонять перед коммитом.
- Тесты скриптов: `node --test scripts/<name>.test.mjs`; новый тест **зарегистрировать в `test:scripts`** — иначе не побежит в CI и правка откатится молча.
- `yarn truth:verify` — гейт графа; `node scripts/truth.mjs radius <id>` — кого утащит отзыв.

## Предыдущий хэндофф

Этот файл перезаписал HANDOFF → 13.07 (спринт `drift-anchor-contour` #396, фазы DA4/DA5). **Карточка всё ещё активна**; содержание — в истории git (`b420235d`) и в `docs/prompts/DRIFT_ANCHOR_CONTOUR_PROMPT.md`. Хвосты того дня, не закрытые: живой тест 3 сценариев (Alpha/Beta/Gamma), катание сценариев в device-board.
