# HANDOFF → 2026-08-06 · **магистраль: `agent-tooling-night-build` перенесена — сдвинуть хотя бы на набросок**

**Точка входа новой сессии.** Хендоф 05.08 снят (архив —
[`docs/handoff/HANDOFF-2026-08-05.md`](handoff/HANDOFF-2026-08-05.md)). Из его десятки
закрыты строки 1–3 (спринт `instruments-honest-verdict`: ложный красный пре-пуша,
свалка `auth`, слепой зонд) и 6 (дверь отзыва). Строка 4 (`detector-scoreboard`)
и хвосты — живы.

> **Провенанс:** отчеканено закрывающей сессией 05.08 после `ritual:evening` и
> отправленной ласточки. Дерево `Membrana-weave`, ветка `angelina/work/2026-08-05`.
> Замеры вечера: 20+ открытых иссью, 5 открытых PR, командный фидбек 5.5/10
> (`team-evening-feedback-2026-08-05.md`), ревью дня — не из памяти.

## Что случилось, в двух предложениях

**05.08 чинил не поломки, а причину их невидимости**: четыре прибора перестали выносить
приговор на недостоверном входе (спринт `instruments-honest-verdict`, #1729), образ
кабинета зелёный впервые за трое суток — корень был не там, где называл фидбек (#1724).
**Цена: продукт снова не двигался, магистраль в код не сдвинулась** — команда поставила
**5.5/10** и назвала это прямо; владелец отнёс потерю темпа к внешним обстоятельствам.

## Топ-10 на 06.08

| # | Задача | Что даст проекту | Почему именно сейчас | Размер | Занято |
|---|---|---|---|---|---|
| 1 | **Красный CI трёх детекторов** (`cepstral`, `harmonic`, `spectral-flux`) — вердикт по каждому: аудио-дефект или инфраструктура | Право доверять зелёному в продуктовом контуре | Единогласное требование фидбека 05.08: **до** любой новой работы. Вчера чинили другой красный (docker-сборку), этот остался | S | свободно |
| 2 | **`agent-tooling-night-build` — набросок ADR**: что зубит ночь, кто судья зелёного, где живёт журнал прогона | Магистраль перестаёт быть словом: появляется форма до кода | Перенесена целиком за 05.08. Фидбек просит сдвинуть **минимум на один зубок** — ADR дешевле кода и снимает риск построить не то | M | **магистраль** |
| 3 | **`detector-scoreboard` Ф1 — план-таблица (DoD до кода)** на измеренных числах из `dads-lab-report-2026-07-18.md` | Первая витрина измеренного качества детекции | Продукт стоит шестой день; Курёхин и Родченко просят четвёртый; макет владельцу **до** кода | M | свободно |
| 4 | **#1714** — `archivarius-mongo` в манифест живых служб + бэкап тома | 106К записей перестают жить без страховки | Данные четвёртые сутки в проде-по-факту, инвентарь их не видит | S | свободно |
| 5 | **#1703 + #1704** — два зуба детекторного контура (stride-слепота, регрессия flux) | Сведение четырёх копий окна получает инварианты | Фундамент строки 3: без них витрина покажет числа, которым нечем верить | S | свободно |
| 6 | **Проба 3: красный `media-library-service#test`** — аудио или инфра | Решает приоритет `media-library-a3` | Задача активна, предусловий не называлось, стареет | S | свободно |
| 7 | **#1613** — контракт `EXECUTION_PROCEDURE` + `procedureKind` | Зависший PR перестаёт висеть | Ждёт LGTM архитектора третьи сутки; санитарка маленьким проходом | S | свободно |
| 8 | **Ревью oversized по одному в день**: #1729 (1076 строк) и `95f139a7` (585) | «Не смотрел» ≠ «одобрил» | Вчерашний спринт сам ушёл oversized — своё же и разобрать | S | свободно |
| 9 | **#1705** — параллельная ратификация двух спринтов одной процедуры | Журнал не путается при двух спринтах разом | Заведено на живом случае; вчера в спринте было **две** перерезки — узкое место видно | S | свободно |
| 10 | **Гигиена реестра**: `githubIssueClosedAt` для закрытых, перечеканка `main-day-assertions.json` под У1 | Доска и посылки перестают числить сделанное открытым | Три карточки активны при закрытых иссью (замер 05.08) — работа для `tasks:audit` | XS | свободно |

## Горизонт — вне десятки, но не потеряно

| Работа | Срок | Почему держим на виду |
|---|---|---|
| **Корпус «один тракт — два класса», 600 звуков** | весь август | Приёмный предикат оформлен карточкой `corpus-track-acceptance-predicate` — сделать ДО записи |
| Прод-выкладка office с парами `ARCHIVARIUS_*` + smoke | owner-gate | Готово всё, кроме твоего слова; `deploy:when-green` печатает, запускаешь ты |
| Первый настоящий прогон `deploy` | owner-gate | Манифесты и обёртка влиты 05.08, прогонов в журнале ноль — DoD ADR-0023 не замкнут |
| **Enum эмиссии v2** (`no_key`/`auth_denied_unstable` вместо legacy `auth`) | слово владельца | Названный долг спринта: сейчас наружу мапит адаптер, чужая зона цела |
| Предикат `amnestyLifted` | рука владельца | Единственный формальный хвост гейта `secret-parser-built` |
| Разнородность overlay ревью | рука владельца | 2 звена из 3 — одна модель за одной квотой; 05.08 ревью встало намертво |

## Ловушки, проверенные боем 05.08

- **Своя рукописная метка `revisionAt` в нарезке — ловушка**: объявишь круглым числом
  позже фактического старта — гейт честно даст `stale_partial` при живой работе.
  Ставить ДО начала работ или не ставить вовсе. Задним числом не править: канон
  называет это замком, ключ от которого не даётся запираемому.
- **Окно спринта тоже метка**: 05.08 задано «18:40» при старте в 18:33 → `plan_lied`
  при живых вещдоках. Гейт сам называет ремонт: «перерезать ОКНО, не следы».
- **`task:register` создаёт болванку промпта** — оставишь как есть, ревью даст BLOCK по
  B10. Заполнять указателем на нарезку, не второй редакцией контрактов.
- **После регистрации карточки — `yarn tasks:decompose`**, иначе CI краснеет на
  «новые префиксы обязаны»; после правки запиненных файлов — `yarn kits:pins --id <кит> --write`.
- **Локальный `curl` к 127.0.0.1 уходит в прокси** — `--noproxy "*"` или `NO_PROXY`.
- **Проверять переменную окружения по имени из каталога**, а не по памяти: 05.08 я искал
  `XAI_API_KEY` вместо `X_AI_API_KEY` и записал в хендоф ложный факт. Теперь отказ
  `no_key` называет переменную сам.

## Хвосты — состояние, не задачи

- **`main` никем не держится**; Membrana-tooling на `chore/tooling-idle` с утра 05.08.
- **Вечер 05.08 в журнале закрыт `fail`** на кадре доставки — цепочка не умеет ждать PR,
  доставка доведена руками (#1733). Кандидат в норму, не провал вечера.
- **Спринт 05.08 закрыт `fail`** с блоком 1 в `stale_partial` — честный остаток, см. ловушки.
- **Открытые PR:** #1733 (артефакты вечера, в мердже), #1731, #1728, #1720, #1613.

## Точки входа

| Что | Где |
|---|---|
| фидбек и фокус завтра | `docs/seanses/team-evening-feedback-2026-08-05.md` |
| хроника и ревью дня | `docs/DAILY_AUDIT.md` · `docs/DAILY_CODE_REVIEW.md` |
| журнал прогонов | `docs/procedure-runs/trail/2026-08-05.jsonl` |
| закон вчерашнего спринта | `docs/sprint/cut/instruments-honest-verdict.json` (`//why`) |
| рамка деплоя | `docs/adr/ADR-0023-deploy-node-form-one-procedure-per-echelon.md` (ACCEPTED) |
| очередь oversized | `yarn review:oversized` |
