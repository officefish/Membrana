# HANDOFF → 2026-08-06 · **магистраль снята: `agent-tooling-night-build` заархивирован 06.08 — строить нечего**

**Точка входа новой сессии.** Хендоф 05.08 снят (архив —
[`docs/handoff/HANDOFF-2026-08-05.md`](handoff/HANDOFF-2026-08-05.md)). Из его десятки
закрыты строки 1–3 (спринт `instruments-honest-verdict`: ложный красный пре-пуша,
свалка `auth`, слепой зонд) и 6 (дверь отзыва). Строка 4 (`detector-scoreboard`)
и хвосты — живы.

> **Провенанс:** отчеканено закрывающей сессией 05.08 после `ritual:evening` и
> отправленной ласточки. Дерево `Membrana-weave`, ветка `angelina/work/2026-08-05`.
> Замеры вечера: 20+ открытых иссью, 5 открытых PR, командный фидбек 5.5/10
> (`team-evening-feedback-2026-08-05.md`), ревью дня — не из памяти.

## Что случилось, в двух предложениях

**05.08 чинил не поломки, а причину их невидимости**: четыре прибора перестали выносить
приговор на недостоверном входе (спринт `instruments-honest-verdict`, #1729), образ
кабинета зелёный впервые за трое суток — корень был не там, где называл фидбек (#1724).
**Цена: продукт снова не двигался, магистраль в код не сдвинулась** — команда поставила
**5.5/10** и назвала это прямо; владелец отнёс потерю темпа к внешним обстоятельствам.

## Топ-10 на 06.08

| # | Задача | Что даст проекту | Почему именно сейчас | Размер | Занято |
|---|---|---|---|---|---|
| 1 | **Красный CI трёх детекторов** (`cepstral`, `harmonic`, `spectral-flux`) — вердикт по каждому: аудио-дефект или инфраструктура | Право доверять зелёному в продуктовом контуре | Единогласное требование фидбека 05.08: **до** любой новой работы. Вчера чинили другой красный (docker-сборку), этот остался | S | свободно |
| 2 | ~~**`agent-tooling-night-build` — набросок ADR**~~ | — | **СТРОКА СНЯТА 06.08: строить нечего**, эпик заархивирован (см. вердикт ниже) | — | **магистрали нет** |
| 3 | ~~**`detector-scoreboard` Ф1 — план-таблица (DoD до кода)**~~ | Витрина и лестница обучения в стволе | **ЗАКРЫТО:** Ф1 сделана 18.07, Ф2 прожита 06.08 (см. вердикт ниже) | M | **закрыто** |
| 4 | **#1714** — `archivarius-mongo` в манифест живых служб + бэкап тома | 106К записей перестают жить без страховки | Данные четвёртые сутки в проде-по-факту, инвентарь их не видит | S | свободно |
| 5 | **#1703 + #1704** — два зуба детекторного контура (stride-слепота, регрессия flux) | Сведение четырёх копий окна получает инварианты | **#1703 закрыт работой (PR #1748), #1704 наполовину (зуб flux — PR #1750; машинный контракт «кадры без padding» не сделан)**; обе иссью в GitHub ещё OPEN | S | **остаток: контракт padding** |
| 6 | **Проба 3: красный `media-library-service#test`** — аудио или инфра | Решает приоритет `media-library-a3` | Задача активна, предусловий не называлось, стареет | S | свободно |
| 7 | **#1613** — контракт `EXECUTION_PROCEDURE` + `procedureKind` | Зависший PR перестаёт висеть | Ждёт LGTM архитектора третьи сутки; санитарка маленьким проходом | S | свободно |
| 8 | **Ревью oversized по одному в день**: #1729 (1076 строк) и `95f139a7` (585) | «Не смотрел» ≠ «одобрил» | Вчерашний спринт сам ушёл oversized — своё же и разобрать | S | свободно |
| 9 | **#1705** — параллельная ратификация двух спринтов одной процедуры | Журнал не путается при двух спринтах разом | Заведено на живом случае; вчера в спринте было **две** перерезки — узкое место видно | S | свободно |
| 10 | **Гигиена реестра**: `githubIssueClosedAt` для закрытых, перечеканка `main-day-assertions.json` под У1 | Доска и посылки перестают числить сделанное открытым | Три карточки активны при закрытых иссью (замер 05.08) — работа для `tasks:audit` | XS | свободно |

## Горизонт — вне десятки, но не потеряно

| Работа | Срок | Почему держим на виду |
|---|---|---|
| **Корпус «один тракт — два класса», 600 звуков** | весь август | Приёмный предикат оформлен карточкой `corpus-track-acceptance-predicate` — сделать ДО записи |
| Прод-выкладка office с парами `ARCHIVARIUS_*` + smoke | owner-gate | Готово всё, кроме твоего слова; `deploy:when-green` печатает, запускаешь ты |
| Первый настоящий прогон `deploy` | owner-gate | Манифесты и обёртка влиты 05.08, прогонов в журнале ноль — DoD ADR-0023 не замкнут |
| **Enum эмиссии v2** (`no_key`/`auth_denied_unstable` вместо legacy `auth`) | слово владельца | Названный долг спринта: сейчас наружу мапит адаптер, чужая зона цела |
| Предикат `amnestyLifted` | рука владельца | Единственный формальный хвост гейта `secret-parser-built` |
| Разнородность overlay ревью | рука владельца | 2 звена из 3 — одна модель за одной квотой; 05.08 ревью встало намертво |

## Ловушки, проверенные боем 05.08

- **Своя рукописная метка `revisionAt` в нарезке — ловушка**: объявишь круглым числом
  позже фактического старта — гейт честно даст `stale_partial` при живой работе.
  Ставить ДО начала работ или не ставить вовсе. Задним числом не править: канон
  называет это замком, ключ от которого не даётся запираемому.
- **Окно спринта тоже метка**: 05.08 задано «18:40» при старте в 18:33 → `plan_lied`
  при живых вещдоках. Гейт сам называет ремонт: «перерезать ОКНО, не следы».
- **`task:register` создаёт болванку промпта** — оставишь как есть, ревью даст BLOCK по
  B10. Заполнять указателем на нарезку, не второй редакцией контрактов.
- **После регистрации карточки — `yarn tasks:decompose`**, иначе CI краснеет на
  «новые префиксы обязаны»; после правки запиненных файлов — `yarn kits:pins --id <кит> --write`.
- **Локальный `curl` к 127.0.0.1 уходит в прокси** — `--noproxy "*"` или `NO_PROXY`.
- **Проверять переменную окружения по имени из каталога**, а не по памяти: 05.08 я искал
  `XAI_API_KEY` вместо `X_AI_API_KEY` и записал в хендоф ложный факт. Теперь отказ
  `no_key` называет переменную сам.

## Хвосты — состояние, не задачи

- **`main` никем не держится**; Membrana-tooling на `chore/tooling-idle` с утра 05.08.
- **Вечер 05.08 в журнале закрыт `fail`** на кадре доставки — цепочка не умеет ждать PR,
  доставка доведена руками (#1733). Кандидат в норму, не провал вечера.
- **Спринт 05.08 закрыт `fail`** с блоком 1 в `stale_partial` — честный остаток, см. ловушки.
- **Открытые PR:** #1733 (артефакты вечера, в мердже), #1731, #1728, #1720, #1613.

## Точки входа

| Что | Где |
|---|---|
| фидбек и фокус завтра | `docs/seanses/team-evening-feedback-2026-08-05.md` |
| хроника и ревью дня | `docs/DAILY_AUDIT.md` · `docs/DAILY_CODE_REVIEW.md` |
| журнал прогонов | `docs/procedure-runs/trail/2026-08-05.jsonl` |
| закон вчерашнего спринта | `docs/sprint/cut/instruments-honest-verdict.json` (`//why`) |
| рамка деплоя | `docs/adr/ADR-0023-deploy-node-form-one-procedure-per-echelon.md` (ACCEPTED) |
| очередь oversized | `yarn review:oversized` |

## Находки дня 06.08 (дописано в течение дня, не при чеканке)

- **ВЕРДИКТ по строке 1 десятки («красный CI трёх детекторов»). Формулировка строки неверна:
  красных не три, а один, и это не CI.** Замер 06.08, воспроизводится
  `cd packages/services/detectors/<сервис> && yarn test`:

  | Сервис | Локально | Вердикт |
  |---|---|---|
  | `cepstral` | 6/6 зелёные | красным не является |
  | `spectral-flux` | 11/11 зелёные | красным не является |
  | `harmonic` | **3/13 падают** | **инфраструктура, не аудио** |

  CI на `main` зелёный: с 2026-08-05T21:26Z все 38 завершённых прогонов `success`
  (`gh run list --branch main`). CI ставит зависимости чисто и болезни не видит — её видит
  только агент в worktree.
  **Корень `harmonic`:** три падения в `src/contract.test.ts`, все с
  `(0 , averageMagnitudes) is not a function`. Символ экспортирован из
  `detectors/base/src/index.ts` и есть в свежем `dist` ЭТОГО дерева, но
  `node_modules/@membrana/detector-base` — симлинк в `Membrana-grok`, чей `dist` собран
  21 июля и символа не содержит. Аудиокод исправен, ломается провод. Это #1647
  (35 из 37 пакетов резолвятся наружу); разбор с вещдоками — коммент 06.08 в #1647.
  **Per-worktree `yarn install` НЕ лечит:** прошёл без ошибок за 5 с, резолюция не
  изменилась ни на один пакет — yarn счёл существующие junction'ы удовлетворёнными.
  **Сопутствующее:** `yarn workspace:links` даёт 3 битых из 37 и `detector-base` среди них
  НЕТ — прибор проверяет наличие `dist`, а не его свежесть. Функционально ссылка сломана,
  для прибора цела; тот же класс, что `retired-17-07`.
- **ВЕРДИКТ по строке 10 («три карточки активны при закрытых иссью»). Их двадцать пять,
  и признак «иссью закрыта» свидетельством выполненной работы НЕ является.**
  Замер 06.08 по 56 активным карточкам с иссью (`gh issue view` по каждой): закрытых — 25.
  Старейшая пятёрка сидит на одной иссью #47, закрытой 11.06 (`real-dataset-live-calibration`,
  `neural-tier-1b-contract`, `vdr-hard-gate`, `vdr-hg3-trends-benchmark`, `vdr-hg4-hard-gate-report`).
  **Направление ошибки обратно тому, что предполагает строка.** Два случая доказывают:
  #1647 закрыта 03.08, а дефект жив и воспроизведён сегодня; #1738 закрыта 06.08 автоматически
  по `Closes` при мердже PR #1740, тогда как спринт не закончен (мутирующий прогон под
  owner-гейтом, замер «прогноз ↔ исход» не сделан). **Обе переоткрыты 06.08 с вещдоком.**
  Массовое архивирование по признаку закрытой иссью закопало бы живые задачи.
  Остальные 23 карточки требуют вердикта по свидетельству в стволе — это ревизия реестра,
  не уборка; по слову владельца отложено, сегодня взят только собственный вред дня.
  **Системное:** `githubIssueClosedAt` пуст у ВСЕХ 56 карточек, потому что производителя
  у поля нет — `scripts/archive-task.mjs:124` выставляет `null` и даты не пишет никогда,
  а потребитель (`scripts/lib/debt-classify.mjs:issueClosed`) ждёт непустую строку.
  Класс тот же, что [#1626](https://github.com/officefish/Membrana/issues/1626).
- **ВЕРДИКТ по строке 2 («магистраль `agent-tooling-night-build` — набросок ADR»).
  Магистрали НЕТ: строить нечего.** Эпик заархивирован 06.08 (`docs/tasks/registry.json`,
  `status: archived`, `archivedAt: 2026-08-06`). Все девять фаз доставлены ночью **08.07**
  и проверены пофайлово: NB1 — строка `.gitignore:149`; NB2 — `pr-ship.mjs` + зуб;
  NB3 — `build-affected.mjs` + зуб; NB4 — `verify-wire-sync.mjs` + зуб и шаг pre-push;
  NB5 — `.githooks/commit-msg`; NB6 — `deploy-when-green.mjs` и `prisma-migration-new.mjs`
  с зубами; NB7 — `tasks-archive-closed.mjs`, `lib/git-day-context.mjs`; NB8 — скиллы
  `membrana-ship` и `membrana-tooling-doctor`. Три вопроса, ради которых просился ADR
  («что зубит ночь», «кто судья зелёного», «где живёт журнал прогона»), **уже имеют
  ответы** в `yarn night:checkpoint` и [`docs/NIGHT_BUILD_LOG.md`](NIGHT_BUILD_LOG.md);
  формат ратифицирован заседанием 18.07.
  **Цена молчания этой строки названа в notes архива:** карточка **два дня подряд**
  (05.08 и 06.08) выбиралась магистралью дня, и оба дня магистраль не двигалась — двигать
  было нечего. Строка снята из таблицы, чтобы следующая сессия не пошла писать ADR по
  сделанному третьим днём.
- **ВЕРДИКТ по строке 3 («`detector-scoreboard` Ф1 — план-таблица до кода»). Формулировка
  неверна: Ф1 закрыта 18.07** коммитом `7862d947` — плагин `mic-detector-scoreboard` живёт
  в клиенте, зуб на сходимость чисел с отчётом зелёный. Живой шаг был Ф2, и он **прожит
  06.08** спринтом `scoreboard-spectral-ladder` (PR #1749, карточка archived): стенд пишет
  версионируемый отчёт `spectral-ladder-report@1.0.0`, витрина показывает лестницу
  обучения, гейт 3/3 `honest_pair`.
  **Продуктовый ответ отрицательный и честный:** замер **не различает ступени** — precision
  52–57 %, recall 96–100 %, ROC-AUC `percentile` 0.75–0.83 без монотонности, интервалы
  Уилсона перекрываются на всех соседних ступенях; ступень `envelope@10` вырождена
  («обнаружено 25 из 25» при «23 ложных из 23» — сработал на всё). Вход в Ф3 назван числом:
  пул дронов ≥ 60 либо кросс-валидация по группам с бутстрапом.
  Попутный перемер: промпт эпика обещает «наши 253 звука», фактически корпус `v0.2` — **137
  записей** (63 дрона / 74 чистых), в обучающем пуле 38 дронов, поэтому ступени 40/60
  недостижимы.
- **ПОПРАВКА к строке 5 и к моей же атрибуции.** Движение больше, чем значилось: **#1703
  закрыт целиком** (квадратичный кейс против слепоты к шагу нарезки, PR #1748), **#1704 —
  наполовину** (регрессионный зуб flux есть, PR #1750; машинного контракта «кадры без
  padding» нет). Обе иссью в GitHub при этом **ещё OPEN** — тот же класс, что вердикт по
  строке 10, только в обратную сторону: работа сделана, а иссью открыта.
  Отдельно снимаю собственное ложное утверждение: PR #1750 я приписал «соседу», не проверив
  автора (`gh pr view` даёт `officefish`, работа этой же сессии). Атрибуция без проверки —
  та же болезнь, что и числа без замера.
- **Десятка второй раз за день рассыпалась при проверке** — [#1744](https://github.com/officefish/Membrana/issues/1744).
  Первый случай: «незакоммиченная правка детекторов» (снято выше). Второй: строка 1.
  Обе — проза без проверяемого маркера, ни даты замера, ни команды воспроизведения.
  Контур посылок `main-day-issue` такого требования уже требует, десятка — нет.

- **ПОПРАВКА к прежней редакции этого блока. Утверждение «в canon-дереве `Membrana` лежит
  незакоммиченная живая правка трёх красных детекторов» было НЕВЕРНЫМ и снято.**
  Ошибка сделана дважды. Во-первых, «22 грязных файла» — это был вывод `git status`
  под `head -25`, обрезок принят за полный список; на деле строк было 2165.
  Во-вторых, файлы `detectors/base/src/sample-window.ts` и его зуб числились в том дереве
  `untracked` не потому, что это новая работа, а потому что дерево сидит на ветке от 01.08:
  **свод окна в `detectors/base` УЖЕ доставлен в ствол**, отдельных `sample-window.ts` у
  `cepstral`/`harmonic`/`spectral-flux` в `origin/main` не осталось. Ничьей несохранённой
  работы по детекторам там не было. Диагностику красного CI это не осложняет.
  Реально незакоммиченным в том дереве остаётся спринт `scripts-orphans-warning-2026-08-02`:
  7 изменённых файлов (`scripts-orphans.mjs`, `scripts-workshop.mjs` + зуб, реестр, доки)
  и 6 untracked-артефактов нарезки. Дерево сидит на `codex/mintlify-workshops-procedures-hackathon`
  (8 коммитов вне ствола).
- **ИНЦИДЕНТ 06.08, 09:15 — я снёс 2152 файла в каноническом дереве `Membrana`; восстановлено.**
  Причина: цикл `git worktree remove` по семи деревьям, два из которых лежали ВНУТРИ
  `Membrana` (`.claude/worktrees/agent-…`, `.worktrees/product-docs-…`). Вложенность я
  не проверил — это одна команда. Снесено содержимое `apps/` и `packages/`
  (2152 из 7825 отслеживаемых файлов, 27%); `docs/` и `scripts/` уцелели. Окно удаления —
  четыре секунды, 09:15:26–09:15:30; git в том дереве при этом не работал (reflog пуст
  с 01.08, индекс от 03.08, замков нет), поэтому иного автора у события нет.
  **Восстановление выполнено в тот же день:** правка спринта снята патчем, `git checkout -- .`
  вернул все 2152 файла, патч применён обратно. Итог: 7825 в индексе, отсутствующих 0,
  состояние дерева тождественно доинцидентному. Потерь нет.
  **Урок в тулинг:** перед пакетным `git worktree remove` проверять вложенность путей —
  дерево внутри другого дерева сносится не как отдельная запись, а вместе с содержимым
  родителя. Кандидат в зуб `worktrees-align` (#1738) и в норму: пакетный снос деревьев
  сначала печатает, какие пути вложены друг в друга.
- **Каскад Ангелины ложно протухает после `deliver-to-main`** — [#1736](https://github.com/officefish/Membrana/issues/1736).
  Squash переписывает SHA документов, предикат свежести сравнивает версию раньше
  дайджеста и выходит с `STALE` на неизменённом содержании. Было невидимо, потому что
  раньше документы уезжали в ствол вечером, когда Ангелину уже не перезапускали.
- **Деревья разобраны: 17 → 10.** Снесены семь перекрытых стволом (свидетельство по
  каждому — в разборе 06.08). Их ветки НЕ удалены: две живут только локально
  (`worktree-agent-ae43a2ec288ea290c`, `chore/archive-tw-v1-v2`), пять имеют близнеца
  на origin. Осталось разобрать шесть: `Membrana` (canon на feature-ветке + грязь),
  `Membrana-codex` (92 грязных файла), `Membrana-llm-panel` (7 грязных),
  `Membrana-dreams-deploy`, `Membrana-grok`, `Membrana-product` (три последних —
  перекрыты стволом, но с карточками `sprint`/`canon`, сносу по классификатору
  не подлежат без перевода карточки).
