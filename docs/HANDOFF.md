# HANDOFF → 2026-07-24 · **процедурный слой: прогон доказан, паттерн фреймов — магистраль**

**Точка входа новой сессии.** Вчерашний хендоф (23.07, «первые живые прогоны one-shot») снят —
три прогона проведены, разобраны, шторм из них закрыт, спринт заведён. Ниже: что уже в `main`
(не переделывать), сегодняшняя магистраль, один живой S-кандидат на решении владельца и
ловушки, которые стоили времени.

> **Провенанс:** отчеканено сессией Claude Code (openrouter, Anthropic лимитирован до 01.08).
> Дерево `Membrana-openrouter` на `origin/main` (tip `337b8c5c` на момент записи), ветка
> `work/2026-07-24`, чисто. `main` физически держит соседнее дерево `Membrana-rails`.

## Что уже сделано (в `main`, НЕ переделывать)

**Три живых прогона one-shot** (первый фрейм) — доказаны по артефактам:

| Заход | Что | Исход |
|---|---|---|
| #1046 | `protocolBody` слепа к жирным репликам | согласие, PR смерджен, 6 ложных красных → 0 (доказано на живых протоколах) |
| #1047 | канал `hand` выдавался за инструментальный | согласие, PR смерджен, ⚠ ровно на 3 ручных протоколах |
| #1051 | тест пути сохранения протокола | **отказ тимлида на штампе S** → выведен в обычную задачу |

Штамп у тимлида запрашивался обходом (штатный `yarn ask` мёртв по лимиту Anthropic):
`PROMPT_TEAMLEAD.md` + `invokeProcedureLlm({procedureId:'consilium'})` → openrouter. Дверь
процедурой не описана — слабость названа явно в прецедентах.

**Шторм «прогон процедуры»** — [`docs/storm/storm-procedure-run-2026-07-23/REPORT.md`](storm/storm-procedure-run-2026-07-23/REPORT.md),
12 тезисов, закрыт полом владельца (вдох 7/7). Конструкция: каждая процедура порождает
**честный прогон**; артефакт кадра → отчёт прогона → **серверная подпись по контенту и хешу
предыдущего прогона** → слепок в репозитории → основание закрытия. Форма — подписанный
transparency log, не блокчейн; sha256 + ed25519 из `node:crypto`. **T12 (инвариант):**
криптография НЕ зависит от жизненного цикла сервера — `verify(checkpoint, proof)` обязан
работать офлайн, из гита.

**Развилка шторма → спринт `run-ledger`** (Issue [#1088](https://github.com/officefish/Membrana/issues/1088),
active, фаза эпика `frame-rails-2307`). Промпт заполнен целиком:
[`docs/prompts/RUN_LEDGER_PROMPT.md`](prompts/RUN_LEDGER_PROMPT.md). **Кода ноль** — это
следующая стройка. Первый шаг = канонформа с тестом CRLF/LF (Windows — не теория).

**Прочее в main:**

- Задача из отказа #1051 — `consilium-save-path-test` (M, active, промпт заполнен): гард
  запуска `main()` + вынос `wrapSeanseFile`/метки в чистую функцию + тест пути сохранения с
  мокнутой цепочкой (три ветки звеньев, обе ветки записи, регресс на код до PR #1032).
- **Карантин синка README** (PR #1092): `syncTasksReadme` по умолчанию не пишет (стирал
  ручные секции), обход `TASKS_README_SYNC_FORCE=1`. Дом такого описания — атлас контейнеров.
- Семь хвостов шторма разобраны в `JOURNAL.md`: #1/#3/#5 в DoD run-ledger, #4/#6 — следующие
  карточки, #2 эпик-вопрос, #7 housekeeping. Новых активных карточек ноль.

## Магистраль дня (24.07) — из утреннего #1095

**#1094 (L, OPEN): «Мягкий рекомендательный паттерн фреймов процедуры»** — owner-choice из
топ-3, родитель — шторм `storm-membrana-leveling-2026-07-24` (ветвь B). Это и есть «шаблон
процедуры» правильным словом: не файл-скелет, а **паттерн фреймов**:

- **Сюжет = СЕРИЯ «сюжетных» фреймов** (тег «сюжетный») — основная линия процедуры.
- Три **служебных** фрейма вокруг: **подвести провода** (входы/LLM-канал), **времянки**
  (где живут, как убираются, что не коммитится), **доставка** (до сервера/соседей).

**Живой вещдок магистрали:** утром `ritual:day` упал HTTP 400 — LLM-шаги ритуала бьют в
Anthropic напрямую, мимо канала панели (OpenRouter→Sonnet), потому что у ритуала нет
служебного фрейма «подвести провода» (не зарегистрирован как LLM-процедура). Паттерн #1094
закрывает это по построению.

**Связь с вчерашним:** `run-ledger` = чем прогон ДОКАЗЫВАЕТСЯ; #1094 = из чего прогон
СОСТОИТ. Служебный фрейм «доставка» = слепок архивации из тезиса T9. Смежные активные
карточки: `frames-alive-ozhegov`/`-dynin`/`-rodchenko` (#979/#980/#981).

## На решении владельца (не начинать без слова)

1. **Один живой S-кандидат для one-shot:** `countRoleReplies` и `unknownBracketLabels`
   ([`protocol-validator.mjs`](../scripts/lib/protocol-validator.mjs)) слепы к жирной метке —
   смежная находка #1046, оставленная за границей тимлидом. Предикат чист, НО это **выстрел
   3/3** окна 7 суток (#1046+#1047 ещё в окне; `chainShots 3 · chainFiles 7 · chainLines 131`).
2. **Куда двигаться:** подключиться к магистрали #1094 (паттерн фреймов) ИЛИ дочитать ветвь A
   того же шторма (`membrana-leveling` → заседание, 14 тезисов в регламент зонтичной
   процедуры). Уточнить, держит ли #1094 другая сессия (магистраль L, вероятны соседи).
3. **HOME_WORKSHOP** живёт копией в `docs/tasks/README.md` — принадлежит эпику соседей
   (tasks-workshop, DRU-403), не заводить свою карточку (был бы дубль). Отдать им строкой.

## Ловушки (стоили времени сегодня — перенести)

- **`pr:ship` дал ложный зелёный ДВАЖДЫ.** (а) `pr-wait` умер на транзиентном сбое
  `gh pr view` → merge не выполнился; (б) branch-cleanup снёс remote-ветку ДО подтверждённого
  merge → GitHub автозакрыл PR (mergeCommit null). **Факт мерджа проверять состоянием**
  (`gh pr view N --json state,mergeCommit`), НЕ exit code. При провале: работа цела локально,
  перебазировать на свежий `origin/main`, домержить контролируемо (push → CI до CLEAN →
  merge → cleanup ПОСЛЕ). Это грань нормы #700, стоит завести отдельно.
- **Anthropic исчерпан до 2026-08-01** (HTTP 400 usage limits). Цепочка процедур падает на
  openrouter (кредиты есть). Провод ритуала к панели чинить ПРОЦЕДУРНО, не ad-hoc (слово
  владельца 24.07) — это и есть носитель служебного фрейма «подвести провода» из #1094.
- **Linear-карточки руками не заводить** — автосинхрон от GitHub-иссью (урок DRU-235).
- **`.env` у каждого worktree свой;** Linear/Anthropic только через прокси (`undici`
  ProxyAgent; встроенный `fetch` прокси игнорирует). Маршруты — `yarn llm:probe`.
- **Windows-путь в скриптах:** node отображает POSIX `/c/...` как `C:\c\...` — пути только
  явным `C:/` или через `process.argv`, не инлайном bash-переменной (прецедент align 24.07).
- **`yarn task:start`** ставит labels `tooling` по умолчанию — метки нет в репо, падает;
  задавать `--labels "package:infra,enhancement"` (или валидные из `gh label list`).

## Открытые хвосты

- **#1088 `run-ledger`** — active, кода нет. Следующий шаг: канонформа + тест CRLF/LF.
- **#1051 `consilium-save-path-test`** — active, кода нет.
- **6 WIP-снимков выравнивания** (`chore: wip snapshot before main-align`) в шести деревьях —
  по T11 шторма развести: мусор → корзина, `ready` → main, незавершённое → регистрация.
- **Вечерний фидбек 22.07/23.07 не сохранён файлом** вопреки CLAUDE.md.
- **Сны смерджены, но не задеплоены** на office (держит старый билд).
