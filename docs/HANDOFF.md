# HANDOFF → 2026-07-18 (после первой пробы линз) · **митинг по контейнеру «рефакторинг инструментов»**

**Центральная задача новой сессии: провести заседание по итогам первой пробы линз.** Проба сделана, число получено — решать надо, что дальше.

## Что уже случилось (не переделывать)

**Первая проба линз выполнена** (`a6ebadac`). Наведён бестиарий на процесс стратегии → **матрица покрытия**:

- **8 из 9 инструментов процесса — `clean` по всем 4 линзам.**
- **Ответ на «нужен ли спринт рефакторинга процесса» получен ЧИСЛОМ: НЕТ.** Утренняя починка (три провода: `ritual:day`→новый генератор, `main-day-issue` читает граф+owner-source) закрыла разрывы. Процесс чист.
- 4 находки-кандидата, все вне ядра процесса. Проверены руками: **1 подтверждена** (`collectState` в `hermes-brief:228` — экспорт без потребителей), **2 ложные** (детектор считал читателей только в `scripts/`, а они в `docs/`, `.claude/`, `.cursor/`), 1 мелкая.

Инструменты: `node scripts/lens-run.mjs` (наведение + матрица), ядро `scripts/lib/lens-bestiary.mjs` (форма `lens: (object, ruleset) → finding[]`, наводимость = каррирование).

## Повестка митинга (три вопроса, один вердикт на каждый)

**1. Пятый зверь — `stale-source` (устаревающий источник).** Главное открытие пробы. Владелец заподозрил, что бриф несвежий, — и оказался прав, но **бестиарий этого НЕ ловит**. Бриф читается (не украшение), не молчит (не молчун), провод кинут — **а питается протухшим входом**: показал ВЫПОЛНЕННУЮ утром магистраль как фокус дня, потому что читает `MAIN_DAY_ISSUE` (где нет статуса выполнения) и **не читает граф правды** (опоры дня, кристалл `standup-distribution-18-07`). Тот же класс: снимок `06.07` в трёх шагах, дрейф-якоря `13.07` как «ok 8», магистраль-фантом 13–17.07. По инварианту вердикта **новый `defectClass` = новая линза**. Решить: заводим пятого зверя, каков его детектор.

**2. Точность детекторов.** 2 из 4 находок — ложные (узкий охват `ruleset.readersOf/consumersOf`: только `scripts/`). Решить: расширять охват (`docs/`, `.claude/`, `.cursor/`, `apps/`) или принять как «кандидаты для человека» и не гнаться за точностью.

**3. Контейнер: три артефакта из вердикта — делать ли сейчас.** Контракт формы линзы (документ, не ABC) · **реестр линз** (5 существующих: Ожегов, бестиарий, `main-day-probe`, `evidenceKind/I7`, `origin-hash`) как словарные статьи, НЕ в `registry.json` · регламент сессии «рефакторинг инструментов» + шаблон матрицы. Гейт против Goal Displacement: каждая находка с владельцем фикса, не аудит ради аудита.

## Что читать первым

1. Вердикт линз: [`docs/seanses/lenses-verification-class-container-2026-07-18.md`](seanses/lenses-verification-class-container-2026-07-18.md) — класс, форма, наводимость, матрица, границы.
2. Инсайт бестиария: `insight-weekly-antipattern-audit-bestiary` (звери + детекторы + рекурсия «аудитор не молчун»).
3. Находки утра (долг): [`docs/seanses/morning-findings-2026-07-18.md`](seanses/morning-findings-2026-07-18.md).

## Состояние проекта

- **Всё в main**, ветка `docs/truth-registry-live` = main. Граф правды: 64 токена, **0 нарушений**, 3 протухших в вечернюю ревизию.
- **Опоры дня 18.07** отчеканены: горизонт `secret-parser-built` (вехевой, `d/dt=0` — верен, пока парсер не построен) · магистраль «подключить генератор» (выполнена) · распределение «тулинг-контейнер».
- **Эпик регламента утра [#605](https://github.com/officefish/Membrana/issues/605)**: ядро закрыто (барьер против проглоченного гейта, фазы 1–4, тесты 11/11). Остаток: Ф5 оркестратор `ritual:day` (ломающее, слово владельца), Ф6 витрина-UI.
- Открытых PR — 7, активных карточек — 204 (раздуто, `rt-7`).

## Ловушки (проверено на себе за день)

**1. Молчание = мнимый успех.** Фоновый агент сегодня САМ сел на эхо (`MAIN_DAY_ISSUE` вместо кода) и выдал 2 ложные находки. Контракт правлен. **Сверяй с КОДОМ, не с документом.**

**2. Грубый детектор даёт кандидатов, не приговор.** 2 из 4 находок пробы — ложные. Проверяй каждую руками прежде, чем докладывать.

**3. Коллизия параллельных сессий.** Сосед активен (Replit, night-build-format) — main двигается часто. `git fetch` перед push, коммитить **поимённо**. Union-драйвер только для `docs/tasks/registry.json`; `truth/` и `insights/registry.json` — сливать union по id вручную.

**4. Граф: опора с derived-детьми при отзыве → `superseded`, не `revoked`** (revoke осиротит детей, I3 — поймано утром).

**5. Тесты не сироты** — новый `*.test.mjs` в `test:scripts`; `test-list-coverage.test` поймает.

## Тулинг-долг в контейнер

`#595` friction · `#599` жаргон-стоп-лист · `#598` night:research молчун · `#584` scripts без линта · `#554` · `#476` · `#605` (Ф5/Ф6). Смежно: `scripts-boundary` (сосед, M0 ратифицирован).

## Инфра / грабли

RAG падает на гео-403 (#593, починка **без новых кредитов** — openai-эмбеддер мимо прокси). media-прокси мёртв. Perplexity жив через туннель `127.0.0.1:12334`. DeepSeek — пополнить; Voyage/OpenRouter — после сети. `curl` нет → node/undici. Пайп съедает exit-код. PowerShell 5.1 портит UTF-8 → Write/Edit. `yarn docs:lint` перед коммитом.

## Предыдущий хэндофф

Затёр HANDOFF → тулинг-спринт (08:58): спринт **выполнен** — линза построена, проба проведена, число получено. Новая сессия входит в **митинг**, а не в стройку.
