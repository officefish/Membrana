# Сессия Г · Санитария контура прогонов, ЧЕРЕЗ СПРИНТ

> **Это твоя точка входа.** Читай только этот файл — он самодостаточен. Остальные
> сессии идут своими полосами; в их файлы и деревья не заходи.
>
> **Правило дня — слово владельца 19.08:** вся работа идёт через `membrana-local-sprint`.
> Код до ратифицированной нарезки — нарушение. Санитария — не повод обойти команду:
> у каждого дефекта есть профильная персона.

## Где ты и что происходит

**Среда 19 августа.** За неделю накопились дефекты контура прогонов, которые каждый день
называет вечерний фидбек и никто не берёт. Они не блокируют магистраль, но делают
статус тестов недостоверным — а на этой неделе на тестах стоит приёмка.

## Дерево

`C:\Users\user190825\practice\Membrana-product` (38 связей, сборка проверена).

```bash
cd C:/Users/user190825/practice/Membrana-product
git fetch origin && git switch -c fix/contour-sanity origin/main
echo "owner: сессия Г (санитария контура) · занято: 2026-08-19" > .worktree-owner
```

## Предмет — четыре дефекта с профильными держателями

| # | Дефект | Факт | Держатель |
|---|---|---|---|
| 1 | `@membrana/rag-service#test` красный **третий день** без диагноза | фидбек 16–18.08; Дынин: «изолированный прогон `yarn workspace @membrana/rag-service test`, диагностика до правок» | Дынин |
| 2 | Ревью-долг субботних диффов | PR #1951 (mfcc в измерителе, 632 строки) и #1953 (`field:capture`, 415) влиты без развёрнутого разбора и **питают калибровочный корпус** | Веснин / Курёхин (#1953: без ресемплинга и нормализации — сырые отсчёты) |
| 3 | Хвосты #1972 | третья копия WAV-декодера → `libs`; `nodeId: null`; зуб границ импортов на линтер | Ожегов |
| 4 | Модульный синглтон динамического импорта в хосте | `plugin-host.service.ts`: `pluginContractsPromise` — риск при двух экземплярах (тест + рантайм); фидбек Ожегова 18.08 | Ожегов |

**Что НЕ дефект (сверено 18.08):** «красный `background-media#test`» из фидбека тимлида —
локальный прогон без связи на новый пакет в песочнице; CI на стволе зелёный. Не чинить.

## Порядок — спринт

1. Скилл `membrana-local-sprint`; карточку завести (`yarn task:register --id
   contour-sanity-2026-08-19 --size M --lead ozhegov --support dynin,vesnin,kuryokhin`)
   со `sprintKind: "membrana-local-sprint"`; Issue — `--issue-mention 1972`.
2. **Нарезка** (`yarn sprint:cut`) — по одному блоку на дефект, каждый со своей персоной
   (таблица выше). Диагноз красного теста — **отдельный блок до правки**: сначала
   изолированный прогон и причина письменно, потом решение чинить/архивировать/оставить.
3. **Ратификация нарезки владельцем** — до кода.
4. Исполнение: каждый дефект — свой PR ≤ 400 строк, `yarn pr:ship` после ревью;
   ревью #1951/#1953 — не PR, а протоколы `yarn code-review:pr` с вердиктами в
   `docs/discussions/`, ссылкой в отчёт.
5. `sprint:experience` в конце.

## Границы

- Не трогать контракты плагинов и хост глубже снятия синглтона.
- Не чинить зелёное (media#test).
- Не переписывать mfcc-детектор — только ревью и границы.
- Коммитить только своё, поимённо.

## Доложить владельцу

Ратифицированная нарезка · диагноз красного теста (причина, не «починил») · вердикты
ревью #1951/#1953 · что закрыто из #1972 · снят ли синглтон · номера PR.
