# Задание: регистрация в кабинете по промокоду из панели администратора (21.09)

**Слово владельца 21.09:** «Мы сделаем условно открытую регистрацию: условной она будет потому, что доступна пока только с промокодом, который генерируется в панели администратора».
**Зачем прямо сейчас:** сценарий тарифной лестницы (free → checkpoint → observatory и обратно) требует второй кабинет на бесплатном тарифе, а завести его на проде нечем: `POST /v1/auth/register` заперт флагом `ALLOW_REGISTRATION=false` (`deploy/generate-cabinet-env.sh:34`), формы регистрации в веб-кабинете нет, админского пути «выдать учётку» нет. Лендинг при этом обещает «Кабинет — регистрация» (`deploy/root-site/index.html:154`).
**Дерево:** своё, отдельное, от свежего `origin/main`.

## Что уже есть — не изобретать заново

| Кусок | Где | Что делает |
|---|---|---|
| Дверь регистрации | `packages/background-cabinet/src/modules/auth/auth.controller.ts:21`, `auth.service.ts:15` | `register(login, password)`: глобальный флаг → валидация (логин ≥3, пароль ≥8) → `user.create(role: 'user')` → сессия |
| Мембрана | `modules/membrane/membrane.service.ts:157` `getOrCreateMembraneForUser` | заводится сама при первом входе, тариф `free-v1` — новому пользователю ничего создавать не надо |
| Промокод кабинета | `prisma/schema.prisma` `model PromoCode` | код → `targetTariffId`, `maxRedemptions`, `expiresAt`, `createdByAdminId`, статус; гасится `POST /v1/membranes/me/tariff/promo-redemptions` (`tariff.controller.ts:137`). **Создающего маршрута в кабинете нет** |
| Админ-роль кабинета | `common/guards/admin.guard.ts` (+ тест) | уже используется в `sample-library.controller.ts` |
| Тот же паттерн целиком в панели офиса | `packages/background-office/src/modules/panel-users/panel-users.controller.ts`: `POST /v1/panel/register` (по промокоду), `@OwnerAdmin` `GET admin/users`, `POST admin/users/:id/revoke`, минт `POST /v1/panel/admin/promo-codes` и `…/revoke` (см. `scripts/_ssh-panel-smoke.mjs:130,177`) | ADR-0005: регистрация партнёров по промокоду, rate-limit 5/час/IP, ошибки регистрации не различают причину наружу |
| Веб-кабинет | `apps/cabinet/src/pages/LoginPage.tsx` (строка 79 — «учётная запись выдаётся администратором»), `apps/cabinet/src/api/auth.ts`, `context/AuthContext.tsx` | формы регистрации нет |

## Развилка, которую надо решить ДО кода (консилиум по норме)

Где живут промокоды регистрации и кто их выдаёт:
- **А. В кабинете.** Расширить `PromoCode` видом `registration` (или отдельная модель), добавить админский минт/отзыв под `AdminGuard` и страницу в веб-кабинете для роли admin. Плюс: один сервис, одна база, промокод тарифа и промокод регистрации рядом. Минус: в кабинете сегодня нет админского UI вовсе.
- **Б. В панели офиса.** Панель уже умеет минтить и отзывать коды под `@OwnerAdmin`; кабинет при регистрации спрашивает офис «код действителен?» и гасит его. Плюс: «панель администратора» уже существует и это она. Минус: межсервисная зависимость регистрации от офиса; при недоступном офисе регистрация встаёт.

Владелец назвал «панель администратора» — проверить с ним, какую именно он имеет в виду. Ответ владельца — вход консилиума, не замена ему.

## Задача (после консилиума)

1. Сервер: `register(login, password, promoCode)` — флаг `ALLOW_REGISTRATION` заменяется на проверку кода: код существует, активен, не исчерпан, не истёк → создать пользователя, погасить код (атомарно, гонка «два запроса на один код» — тем же приёмом, что #2313: `P2002` → повтор чтения). Ошибки регистрации наружу не различать (Q3 из ADR-0005). Rate-limit как в панели.
2. Админ: минт кода (срок, число погашений) и отзыв — под админской ролью, по решению консилиума в кабинете или в панели.
3. Веб-кабинет: форма регистрации на `LoginPage` (логин, пароль, промокод), убрать строку «выдаётся администратором», ошибки — одной фразой.
4. Лендинг: карточка «Кабинет — регистрация» становится правдой; пока формы нет — текст «по приглашению».
5. Зубы: сервис — код истёк / исчерпан / отозван / гонка двух регистраций на один код (красный вход до правки существует: сейчас `register` не принимает код вовсе); guard — не-admin не минтит; веб — форма отправляет код.
6. Прод: после выкатки владелец минтит первый код и регистрирует второй кабинет — это и есть приёмка. Тариф новой мембраны обязан быть `free-v1`, предел буфера по `GET /v1/devices/:deviceId/quota` — 536870912.

## Границы

- Код до вердикта консилиума не писать. Развилка выше — предмет заседания, не заметка в PR.
- `ALLOW_REGISTRATION=false` на проде не трогать руками — дверь меняется кодом, не флагом.
- Секреты и прод — слово владельца.
- Свои файлы коммитить поимённо.

## Готово, когда

- консилиум дал вердикт по развилке А/Б, вердикт ратифицирован владельцем;
- регистрация по коду работает на стволе с зубами на все четыре отказа и гонку;
- владелец на проде создал код в панели и завёл второй кабинет; его мембрана на `free-v1`, квота прибора 536870912;
- лендинг не обещает того, чего нет.
