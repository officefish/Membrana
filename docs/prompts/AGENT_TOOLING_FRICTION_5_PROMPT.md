# Промпт: эпик тулинг-спринта friction-5 (PR/merge надёжность)

> **Статус: ЗАРЕГИСТРИРОВАНО, НЕ В РАБОТЕ.** Не начинать без явного слова владельца.
> Тулинг-спринт `agent-tooling-friction-5` (сессия 2026-07-24), образец — `agent-tooling-friction` (#433).

## Спецификация

Полный спек с эпизодами-evidence — в GitHub Issue **#1166-#1169 (4 инструмента)** и в `notes` карточки реестра
(`docs/tasks/registry.json`). Этот файл — точка входа; наполнить деталями реализации ПОСЛЕ
слова владельца на стройку (тогда: план 1–2 абзаца + список файлов, консилиум-гейт оценить честно —
скрипты по готовому канону обычно inline).

## Definition of Done

- [ ] Реализация по спеку иссью #1166-#1169 (4 инструмента); тесты, где есть детерминированная механика.
- [ ] `yarn turbo run lint typecheck test build --continue` зелёный (или scope).
- [ ] LGTM Teamlead.
