# Промпт: Дефолтная модель офиса живёт литералом в коде и никем не проверяется на живость

> **Task-промпт для агента-разработчика** (Cursor IDE / Claude / другой LLM).
> Процесс постановки: [`TASK_PROMPT_WORKFLOW.md`](./TASK_PROMPT_WORKFLOW.md).
> Скопируй блок **«Промпт целиком»** в начало диалога. Размер задачи: **S**.
> Ожидаемый артефакт: **1 PR** — дефолт модели перестаёт быть непроверяемым литералом.
> Реестр: `id` = `openrouter-default-model-unverified` в [`docs/tasks/registry.json`](../tasks/registry.json).

---

## Контекст

Офис при пустом `OPENROUTER_MODEL` молча подставляет `anthropic/claude-haiku-4.5` прямо
в коде. Это ТРЕТИЙ носитель id моделей: реестр снов `scripts/lib/dreams-providers.mjs`
и каталог каналов `scripts/lib/llm-provider-catalog.json` — первые два, и оба под присмотром.
Этот — нет. 07.08 два id реестра снов оказались мертвы (HTTP 404), и нашёл это прод, а не
проверка; спринт `dreams-models-liveness` (PR #1835) закрыл ту дыру глаголом
`yarn dreams:probe-models`, но дефолт офиса в него не попал — он вне реестра.

Уже есть: гард `scripts/dreams-model-ids.test.mjs` (он этот литерал и нашёл), глагол
проверки живости, ночная каденция. НЕ трогаем: реестр снов и его маршруты, сам глагол,
каталог каналов ask/code-review — у него своя судьба.

**Связанные документы:**

| Документ | Зачем |
|----------|--------|
| [`VIRTUAL_TEAM_PROMPT.md`](../VIRTUAL_TEAM_PROMPT.md) | Роли, порядок работы |
| [`ARCHITECTURE.md`](../ARCHITECTURE.md) | Границы модулей |
| [`DESIGN.md`](../DESIGN.md) | UI (если есть) |
| [`TASKS_MANAGEMENT.md`](../TASKS_MANAGEMENT.md) | Issue / PR |
| [`OPEN.md` спринта](../local-sprint/dreams-models-liveness/OPEN.md) | откуда пришла находка |

**Референс (только идеи UX, не копировать код):** `packages/temp/...` — если есть.

**GitHub Issue:** — (не заведён)

---

## Промпт целиком (для вставки агенту)

> Всё ниже до раздела **«Заметки для человека-постановщика»** — текст задания для агента.

---

### Кто ты

Ты — **координатор виртуальной команды Membrana** под руководством **Vesnin** (Teamlead). Перед кодом — краткий план (1–2 абзаца + список файлов). Соблюдай [`VIRTUAL_TEAM_PROMPT.md`](../VIRTUAL_TEAM_PROMPT.md) и [`TASK_PROMPT_WORKFLOW.md`](./TASK_PROMPT_WORKFLOW.md).

---

### Что построить (продуктовое описание)

1. **Дефолт модели перестаёт быть литералом в прод-коде.**
   `packages/background-office/src/modules/openrouter/openrouter.service.ts:20` возвращает
   `anthropic/claude-haiku-4.5` при пустом `OPENROUTER_MODEL`. Это третий носитель id
   моделей: первые два — реестр снов `scripts/lib/dreams-providers.mjs` и каталог каналов
   `scripts/lib/llm-provider-catalog.json`; этот единственный живёт вне всякого реестра и
   на живость не проверяется.
2. **Развилка — решить ЯВНО, а не «как получится».** Либо дефолт переезжает в реестр и
   попадает под `yarn dreams:probe-models` вместе с остальными, либо пустой
   `OPENROUTER_MODEL` становится явным отказом конфигурации. Тихая подстановка на месте
   остаться не может: если `claude-haiku-4.5` снимут, офис уйдёт на мёртвый дефолт молча —
   ровно то, что 07.08 произошло с двумя id реестра снов и что нашёл прод, а не проверка.
3. **Оговорка снимается вместе с починкой.** Сегодня строка закрыта
   `dreams-model-ids:allow openrouter-default-model-unverified`; оговорка существует ради
   этой карточки и переживать её не должна.

---

### Архитектура / контракт

| Слой | Путь | Ответственность |
|------|------|-----------------|
| Место дефекта | `packages/background-office/src/modules/openrouter/openrouter.service.ts:20` | тихая подстановка модели при пустом конфиге |
| Гард, нашедший его | `scripts/dreams-model-ids.test.mjs` | литерал `<вендор>/<модель>` вне реестра |
| Проверка живости | `scripts/dreams-probe-models.mjs` | сюда дефолт попадёт, если поедет в реестр |
| Схема конфига | `packages/background-office/src/config/env.schema.ts` | сюда — если выбран путь явного отказа |

**Запрещено:**

- Заводить ЧЕТВЁРТЫЙ носитель id — новый список моделей где бы то ни было.
- Снимать оговорку `dreams-model-ids:allow` не починив предмет: оговорка существует ради
  этой карточки и переживать её не должна.
- Молча менять поведение при пустом `OPENROUTER_MODEL` — выбор между «дефолт из реестра»
  и «отказ конфигурации» объявляется вслух, он меняет поведение прода.

---

### Визуальный дизайн (если есть UI)

UI нет.

---

### Тесты

| Область | Минимум |
|---------|---------|
| `openrouter.service.ts` | пустой `OPENROUTER_MODEL` даёт ВЫБРАННОЕ поведение (модель из реестра либо явный отказ), а не тихую подстановку |
| Гард | после починки литерала в пакете нет, и оговорка снята — `yarn test:scripts` зелёный без неё |
| Живость | если дефолт поехал в реестр, `dreams-probe-models` спрашивает и его тоже |

---

### Definition of Done

- [ ] Развилка решена ЯВНО и записана в PR: дефолт в реестре либо отказ конфигурации.
- [ ] Оговорка `dreams-model-ids:allow openrouter-default-model-unverified` снята, гард зелен без неё.
- [ ] `yarn dreams:probe-models` покрывает дефолт (если выбран путь реестра).
- [ ] `yarn turbo run lint typecheck test build --continue` — зелёный (или указать scope).
- [ ] LGTM Teamlead.

---

### Out of scope

- Каталог каналов `scripts/lib/llm-provider-catalog.json` — соседний реестр, своя судьба.
- Реестр снов и глагол `dreams-probe-models` — уже закрыты PR #1835, здесь только
  подключение дефолта, если выбран путь реестра.
- Автозамена мёртвых id где бы то ни было: провайдер не решает за нас, что эквивалент.

---

### Порядок работы ролей

1. **Teamlead** — держит развилку: «дефолт в реестре» против «отказ конфигурации». Решение
   принимается ДО кода и пишется в PR; молча выбранный путь не принимается.
2. **Структурщик** — граница: офисный пакет не импортирует `scripts/` статически (прод
   грузит реестр динамическим импортом по file-URL, см. `dreams.service.ts` → `lib()`).
3. **Математик** — не участвует, предмет без вычислений.
4. **Музыкант** — не участвует, предмет без звука.
5. **Верстальщик** — не участвует, UI нет.

---

### Формат ответа координатора (планирование)

```text
[Teamlead]: …
[Структурщик]: …
[Математик]: …
[Музыкант]: …
[Верстальщик]: …

Итоговый артефакт: …
Definition of Done: …
```

---

## Заметки для человека-постановщика

1. GitHub Issue (`wish` / `bug` / `imperfection`) + ссылка на этот файл.
2. Запись в `docs/tasks/registry.json` (`status: active`).
3. После merge: отчёт в Issue → `yarn task:archive <slug> --notes "…"`.

### Проверка после PR

```bash
yarn test:scripts
yarn turbo run typecheck test --filter=@membrana/background-office
yarn dreams:probe-models --dry-run
```

---

## Связь с дорожной картой

- Достоверность LLM-каналов: ни один id модели не должен жить там, где его никто не проверяет.
  Соседи по теме — долг #llm-probe-still-lies-net (погашен PR #1834) и спринт dreams-models-liveness (PR #1835).
