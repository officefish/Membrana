# Промпт: RT-7: приоритеты планирования из реестра, а не хардкод-текстом

> **Стаб подзадачи эпика** `ritual-trust-contour` ([#539](https://github.com/officefish/Membrana/issues/539)).
> Детали доопределяет `rt-0-gate`: часть состава к моменту взятия может оказаться сделанной.
> **Заполнить до кода.** Эпик-промпт: [`RITUAL_TRUST_CONTOUR_PROMPT.md`](./RITUAL_TRUST_CONTOUR_PROMPT.md).

## Что построить

Этап продуктовой магистрали (S2/S3/S4/S5) должен генерироваться из реестра/форсайта, а не быть статическим markdown-текстом, который правят рукой.

## Прецедент (живой — на нём и демонстрировать)

`detection-planning-priorities.mjs` — снимок от 06.07, подключён к **трём** шагам ритуала (standup, plan:day, main-day-issue). Одна строка «магистраль = S2 combined UC (fusion) → S3 упаковка (3+1)» дала 16.07 и «fusion», и «три каркаса», хотя S2 слит 13.07. Форсайт 06.07 (строки 67–70) предсказал этот дрейф и предложил «отдельную небольшую задачу» — её не завели; за 10 дней выросло из «зашита детекция» в «зашит устаревший этап».

## Источник

Корень 3 разбора 16.07; сейчас закрыто XS — дата снимка + предупреждение о протухании (`fdc05d05`).

## Нормы эпика (обязательны)

- **Живая демонстрация на прецеденте выше** — unit-тестов недостаточно. PG3 #533: гейт соврал при 12 зелёных тестах ядра, поймал только реальный прогон.
- **Регрессия проверяется возвратом бага** — верни поломку, тест обязан покраснеть. Иначе это декорация.
- **Сначала проверь, что инструмента нет.** За 15–16.07 подтвердилось трижды: `isBaseHeldElsewhere` жил в `pr:ship`, механика `skipped` — в `morning-care`, union-драйвер был зарегистрирован.
- Новых пакетов нет; `@membrana/core` не трогаем (ритуальной логике там не место).

## Главный риск задачи

Не добавить **ещё один** датированный хардкод. Эпик уже рискует стать третьим эхом: гейт, промпт и снимок все ссылаются на разбор 16.07. Решать в общем виде.
