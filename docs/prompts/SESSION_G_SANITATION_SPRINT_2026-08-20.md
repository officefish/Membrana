# Сессия Г — 20.08: санитария — ревью-долг oversized + e2e-smoke подъёма (#2009) + secret-parser

> Точка входа отдельной сессии. **Всё — через `membrana-local-sprint`** (нарезка →
> ратификация владельцем в чате → исполнение). Магистраль дня — НЕ ваша.

## Задачи (по приоритету фидбека 19.08)

1. **Вердикты по 6 oversized-PR** (P1 вчерашнего DAILY_CODE_REVIEW): #1980 · #1981 ·
   #1987 · #2003 · #2004 · HEAD-847. Каждому — отдельный прогон `yarn code-review:pr N`
   с явным LGTM/BLOCK; находки — Issue, не молчание. PR влиты — ревью постфактум, вердикт
   в `docs/discussions/pr-<N>-code-review.md` (артефакт в репо).
2. **#2009 — e2e-smoke подъёма сервисов в CI**: 19.08 три сервиса падали на старте Nest
   (@Optional-параметры) → restart-loop прода 8 минут; зубы строили сервисы напрямую и
   не видели. Нужен CI-шаг «приложение поднимается» для background-media и background-office
   (без Postgres/Mongo — least-effort: Nest app.init() со стабами env или testcontainers —
   решит ваш резчик). Тарасов без этого следующий прод-деплой НЕ подписывает.
3. **Гейт `secret-parser-built`**: веха не закрыта (резак секретов в
   night-triage-secret-scan). Разобраться и закрыть либо эскалировать владельцу с ценой.

## Запрещено

Магистраль и спринты Б/В не трогать · прод не деплоить · `git add -A` запрещён ·
oversized-ревью не «пакетом одним абзацем» — по PR отдельно.

## DoD

6 вердиктов в репо · CI-шаг подъёма зелёный в PR · secret-parser закрыт/эскалирован ·
спринтовый след (нарезка ратифицирована, гейт, опыт).
