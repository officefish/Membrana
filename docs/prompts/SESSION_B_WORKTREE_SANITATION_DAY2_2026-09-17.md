# Сессия Б — снос мёртвых веток и закрытых деревьев (17.09, второй день санитарии)

**Магистраль дня:** `worktree-sanitation-day2`, выбор владельца от 17.09 (`docs/tasks/main-day-assertions.json`, `sources[0]`, author=human).
**Дерево:** своё, отдельное. В чужих деревьях не работать.

## Предмет

Замер утра 17.09 на стволе `4542603a` (`yarn repo:clean` — сухой прогон, он же первый шаг задания):

| Что | Сколько |
|---|---|
| Локальные ветки всего | 292 |
| Локальные — к удалению (PR MERGED/CLOSED) | 39 |
| Ветки на сервере всего | 162 |
| На сервере — к удалению (PR MERGED/CLOSED) | 72 |
| Рабочие деревья всего | 62 |
| Из них `sprint-closed` (инструмент сносит сам) | 7 |
| Из них `unregistered` (нет карточки — не твоя зона) | 41 |
| Из них `canon` (не трогать) | 5 |
| Заблокированное дерево | 1 — `Membrana-ritual-night` |

## Задача

1. Прогнать `yarn repo:clean` без ключей и убедиться, что числа сошлись с таблицей выше. **Разошлись — остановись и доложи**, не подгоняй.
2. Снести локальные и серверные ветки с мёртвым PR: `yarn repo:clean --execute --remote`.
3. Снести деревья класса `sprint-closed`: `yarn repo:clean --execute --worktrees`. **По одному.** После каждого сноса — проверка, что живые деревья на месте.
4. Записать вещдок: `docs/evidence/worktree-audit-2026-09-17.md` — что снесено поимённо, что отказано и почему.

## Границы — читать до первой команды

- **Сносить только инструментом.** `git worktree remove` руками запрещён, тем более циклом. Прецедент 06.08: рекурсивное удаление ушло сквозь junction `node_modules/@membrana/*` в пакеты соседнего дерева и снесло 2152 файла.
- **`unregistered` — не твоя зона.** 41 дерево без карточки инструмент намеренно отдаёт человеку. Их разбирает владелец, не ты.
- **Заблокированное дерево не разблокировать.** `Membrana-ritual-night` держит лок от 29.08 — доложить, не снимать.
- **103 локальные ветки без PR и без копии на сервере не трогать ни при каких условиях.** Их уникальные коммиты пропадут безвозвратно; это отдельное задание сессии Г.
- Коммитить свои файлы поимённо. `git add -A` при параллельной работе запрещён.

## Известные дефекты инструмента — чтобы не принять их за свою ошибку

1. Окно опроса запросов на слияние — 500 последних. Более старые PR инструмент не видит и считает ветку неразобранной.
2. На ветке, которой нет на сервере, инструмент сравнивает её с несуществующей копией и печатает `fatal: ambiguous argument`. Сегодня утром так споткнулись 25 веток из перебора. Это шум, не отказ.
3. После перебазирования при отправке инструмент считает вершину ветки неотправленной и отказывается сносить дерево. Из-за этого 22 закончённых дерева не сносятся никогда.

Дефекты **записывать, не чинить** — починка отдельным заданием, не посреди сноса.

## Готово, когда

- `yarn repo:clean` (сухой прогон) показывает 0 веток к удалению;
- деревьев класса `sprint-closed` не осталось;
- живые деревья на месте, ни одно не пострадало;
- вещдок `docs/evidence/worktree-audit-2026-09-17.md` записан и отправлен в ствол через `yarn pr:ship`.

**Слияние — только по прямому слову владельца тебе.** Пересказанное слово словом не является.
