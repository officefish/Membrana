# Сессия Б — 20.08: хвосты моста результатов + первая проба mfcc через мост

> Точка входа отдельной сессии. **Всё — через `membrana-local-sprint`** (нарезка с персонами,
> ратификация владельцем В ЧАТЕ, гейт исполнения, опыт): сессии грешат делать всё сами без
> виртуальной команды — это запрещено словом владельца 19.08. Подкрепление №1 дня
> (Day Issue 20.08, owner-choice `studio-firebat-user-pairing` — магистраль НЕ ваша).

## Контекст (сверено 20.08 утром)

Деплой 19.08 вечера ожил: office и media на стволе. Мост media → office живой —
первый RunRecord через HTTP-путь: `runId 01a01ac2-cd88-7197-86d8-e2228d6ccad0`,
`bridge.outcome sent`, читается обратно из `office.mmbrn.tech/plugin-results/runs`
(вещдок в `docs/field/firebat-node-acceptance-2026-08-19.md`). Спринт `plugin-results-bridge`
(#1961): b1–b4 закрыты, **b5 стоял blocked «ждёт деплоя»** — разблокирован фактом выше.
Вчерашняя запись узла Firebat: `sampleId 89e428ba-beb1-4689-8169-fd5e5eb13f7b`
(9,975 с · 48 кГц · моно), коллекция «Полевые записи 2026-08».

## Задачи (по порядку)

1. **Закрыть b5 спринта `plugin-results-bridge`.** Дописать вещдок
   `docs/plugins/results-bridge-live-run-2026-08-19.md` (runId, чтение обратно, дата деплоя);
   `sprint:gate` (blocked-запись b5 → honest), `sprint:experience` (прогноз↔исход),
   журнал прогона закрыть, карточку `plugin-results-bridge` архивировать со свидетельствами;
   `LIVE_SERVICES.md` строка моста: «не подтверждено» → живой вещдок.
2. **Мини-спринт «первая проба mfcc через мост»** (новая карточка, нарезка → ратификация
   владельцем в чате → код): вызвать `POST /v1/devices/:d/collections/:c/plugins/membrana.handler.mfcc/request`
   на **записи 89e428ba** (боевой вход b4, НЕ скрипт с туннелем), убедиться что RunRecord
   доехал в `plugin-results` мостом (`bridge.outcome sent`), результат прочитать обратно и
   оформить вердикт-документ `docs/plugins/mfcc-first-field-probe-2026-08-20.md`:
   адрес прогона, отпечатки, что намерил mfcc на реальном полевом фоне.
3. **Материал для разблокировки `mfcc-compare-sprint`** (Курёхин просил 19.08): одним
   разделом в том же документе — годится ли проба как старт сравнения, чего не хватает
   (датасета? ворот 48 кГц? — см. #2001).

## Запрещено

Магистраль дня (Studio/парринг) не трогать. Контракты `plugin-contracts` не менять (#1982 —
вопрос Архитектору, не вам). Прод не деплоить (после #2009 деплой без e2e-smoke Тарасов
не подписывает; сегодня деплой не нужен — всё уже на стволе). Прямая Mongo из media, скрипт
с SSH-туннелем как путь результата — запрещены (форма моста). `git add -A` запрещён.

## DoD

b5 закрыт и карточка архивирована · RunRecord пробы mfcc по 89e428ba виден GET-ом из
office · вердикт-документ в стволе · порядок спринта соблюдён (нарезка ратифицирована
владельцем ДО кода, гейт и опыт записаны).
