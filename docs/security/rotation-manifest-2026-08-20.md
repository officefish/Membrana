# Манифест реза секретов — dirty-fixture-2026-08-20.txt

- Дата прохода: 2026-08-20
- Режим: запись очищенной копии
- Вырезано фрагментов: 3

## Классы (кандидаты на ротацию ключа)

| Правило | Вхождений |
|---|---|
| `anthropic-key` | 1 |
| `github-token` | 1 |
| `bearer-token` | 1 |

## Места (без значений)

| Правило | Где | Длина, симв. |
|---|---|---|
| `anthropic-key` | строка 2 | 59 |
| `github-token` | строка 3 | 36 |
| `bearer-token` | строка 4 | 37 |

> Значения не приводятся намеренно: манифест сам не должен быть утечкой.
> Порядок канона — сначала ротация ключей владельцем, затем датированный проход.

## Контекст прохода (спринт `sanitation-2026-08-20`, блок 9)

- Вход — **грязная фикстура** (`dirty-fixture-2026-08-20.txt`, синтетические паттерны трёх
  классов), не прод и не архив: так велит MAIN_DAY_ISSUE 20.08 («прогнать на грязной фикстуре»).
- Резак — `node scripts/night-triage-secret-scan.mjs --redact … --manifest … --date 2026-08-20`;
  механика — ядро `scripts/lib/secret-redact.mjs` (то же, что у `yarn secret:redact`).
  Копия чиста (0 вхождений паттернов), вход не перезаписан, зубы 7/7.
- **Критерий 1 вехи закрыт:** сканер имеет резак (режим `--redact`, детекторный гейт не тронут).
- **Критерий 2 закрыт в форме дня:** один датированный проход с манифестом — этот файл.
- **Остаток — акт владельца, не агента:** ротация реальных засвеченных ключей и датированный
  проход АРХИВА (кристалл `archive-cleanup-rotate-then-single-dated-pass`: сначала ротация,
  затем один проход, после — правка архива запрещена навсегда); отметка прохождения гейта —
  `docs/strategy/day-horizon.json.lastGateTransition` («веха ставится ЧЕЛОВЕКОМ», см. `_note`).
