# Манифест ротации засвеченных ключей — 2026-08-03

> Датированный проход по гейту `secret-parser-built` (критерий «один датированный проход
> с манифестом ротации»). Первый прогон. Собран шотом `shot-rotation-manifest`
> (исполнитель ozhegov, назначен тимлидом; запись прогона — journal
> `one-shot-2026-08-03-rotation-manifest`).

| поле | значение |
|---|---|
| дата прохода | 2026-08-03 |
| scope | `docs/archive/` — 372 файла (md/json/jsonl/txt/yml), read-only |
| инструмент | `scripts/lib/secret-redact.mjs` (резак #1240, коммит `211243df`) поверх правил `night-triage-secret-scan.mjs` |
| дерево | `origin/main @ 7c98e78f` |
| живые пути чтения | сканер `night-triage-secret-scan.mjs`: **0 находок** (прогон 03.08) |

## cuts

```json
[]
```

**Находок нет.** Архив просканирован названной версией резака — вырезать и ротировать нечего.

## Подтверждение владельца

Нулевой результат подтверждён владельцем **явно, не молчанием** (канон: «амнистия не
автоматизируется молчанием» — контекст исполнителя, 03.08): слово «ок» в чате 03.08,
после фразы сессии «оно здесь двойное — ратификация чек-листа и подтверждение нулевого
результата скана». Ключей к ротации по этому проходу — ноль.

## Что остаётся до снятия амнистии

Критерий (в) гейта — «амнистия снимается предикатом, не датой». Предикат
(`amnestyLifted`) в этот шот **не вошёл**: прогноз предиката S дал `capability_chaining`
— четвёртый `scripts/lib`-шот за семь суток, и анти-цепочка отказала. Это её работа, не
помеха. Предикат едет отдельной работой, когда окно очистится; форма согласована
контекстом исполнителя: `amnestyLifted({manifest, ownerConfirmation})`, при пустых
`cuts` подтверждение владельца всё равно обязательно.
