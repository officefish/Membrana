# Отчёт сессии: утренний ритуал 21.07 (тёплая сессия, дерево Membrana-angelina)

- **Дата:** 2026-07-21
- **Session ID:** `7f931953-ca19-490e-a919-6ca27a60a8c9`
- **Рабочее дерево:** `C:/Users/user190825/practice/Membrana-angelina` (по прямому указанию владельца)
- **Ветка:** `developer-rhythm-lifecycle` (head на старте `8d7ef4f1`)
- **Агент:** Claude Code (Fable 5)

---

## Хронология по шагам

### 1. Вход и топология (до ритуала)

1. Запрос владельца «перейди на ветку developer-rhythm-lifecycle». Обнаружено:
   ветка уже выкачана в worktree `Membrana-angelina`; основное дерево
   (`practice/Membrana`, ветка `fix/adr-0013-accepted`) несло **2115 незакоммиченных
   удалений** (снесён `apps/cabinet` и другие воркспейсы — по виду хвост инцидента
   junction-wipe 20.07). Переключение основного дерева не выполнялось: git не даёт
   одну ветку двум деревьям, забирать ветку у чужого worktree — нарушение нормы
   параллельных сессий (инцидент 2026-07-09).
2. По команде владельца сессия закреплена за деревом `Membrana-angelina`. Особенность
   среды: хост сбрасывает cwd шелла после каждой команды → каждая команда явно
   начинается с `cd` в Angelina-дерево.
3. Владельцу отдан session id (этот же — в заголовке).

### 2. Подготовка по переданным инструкциям

4. `git fetch` / `checkout` / `pull` в Angelina-дереве — ветка актуальна.
5. Реставрация основного дерева: `git ls-files -d -z | xargs -0 git restore --` —
   **возвращено 2074 удалённых tracked-файла**, untracked не тронуты. После — 0 удалений.
6. Повторный прогон fetch/checkout/pull + `yarn install` (~11с и ~5.5с, только
   peer-warnings) + самопроверка: `scripts/angelina.mjs`, `scripts/morning-gate.mjs`,
   `.cursor/skills/membrana-morning-ritual/SKILL.md` — все на месте.
7. Находка при повторном checkout (вывод отдан владельцу дословно): дерево между
   командами кто-то уводил с ветки (`Switched to branch` вместо `Already on`) и в нём
   висела чужая правка `M package.json` (косметика: расклейка строк
   `test:scripts`/`task:list`). Правка не моя, не коммитилась.

### 3. Предритуальный порядок (по SKILL membrana-morning-ritual)

8. Прочитан `docs/seanses/team-evening-feedback-2026-07-18.md` (свежее нет — вечера
   19–20.07 не проводились). Вынесено на сегодня: тест интеграции цепочки ритуала (C7),
   разворот 18 oversized (P1), deprecated-пометка `strategic-plan-day.mjs` и др.
9. Просмотрены открытые развилки владельца: #768 (похороны telegram-ritual-digest),
   #761 (кит/SHA-пины), #741/#745 (Ангелина-хозяйка, шторм).
10. Ветка утра — не `main`, а назначенная владельцем `developer-rhythm-lifecycle`;
    грязь дерева (`M package.json` + чужой untracked `docs/discussions/
    sprint-angelina-hostess-impl-review.md`) эскалирована владельцу до запуска,
    продолжение — по его «поехали».

### 4. Прогон №1 `yarn ritual:day` — стоп на страже свежести (exit 2)

11. Прошли: morning-care (все ok), worktree-sync (ff `tooling` 800bef8a→82fe25f9,
    +25), repo-clean (dry-run: 9 веток к удалению, 2 fatal-варнинга на ветках без
    upstream — не блокирующие), deps-watch (59 advisories: 1 critical/14 high —
    корзина «задача СРАЗУ» для Teamlead), plan:week (вторник — пропуск),
    strategy-day (`docs/STRATEGY_DAY.md` записан, горизонт `secret-parser-built`).
12. **Стоп:** гейт RT-9 — `docs/DAILY_CODE_REVIEW.md` устарел на 3 дня (штамп 18.07,
    допуск ≤1). Причина корневая: вечерние ритуалы 19–20.07 не проводились.
13. Разрешение: выпуск 18.07 уже лежал в архиве
    (`docs/archive/daily-code-review/DAILY_CODE_REVIEW-2026-07-18T18-19-55-030Z.md`),
    тело сверено — идентично. Живая копия снята `git rm docs/DAILY_CODE_REVIEW.md`
    (гейт по коду пропускает честное «файла нет»; потерь нет — архив + git-история).

### 5. Прогон №2 — стоп на гейте скелета M2 (exit 22)

14. Стендап записан (`docs/DAILY_STANDUP.md`), несвежий вход MAIN_DAY_ISSUE (3 дня)
    честно помечен в теле. main-day-probe: посылок нет.
15. **Стоп:** `main-day-issue` — сначала транзиентный `TLS handshake timeout` к
    GitHub GraphQL, затем «LLM уронил все 5 слотов — файл НЕ записан».
16. Повторный запуск хвоста — та же ошибка, мгновенно. Все 5 слотов подряд — признак
    системной причины, не флейка.

### 6. Диагностика → найден реальный баг генератора

17. Промпт снят `main-day-issue --dry-run` (95 051 символ — у потолка 95К),
    API вызван напрямую отладочным скретч-скриптом (вне репо): ответ **200 OK,
    4 767 символов, содержательно корректный**, но заголовки слотов декорированы
    («## 🎯 Магистраль дня…» вместо строгого `## Магистраль`).
18. Проверка собранного промпта грепом: **блок «# Задание» со строгими заголовками
    слотов в промпт не попал** — сборка кладёт задание ПОСЛЕДНЕЙ секцией, обрезка
    95К режет хвост. Модель инструкций не видела и писала по образцу вчерашнего файла.
    Контекст раздут: 176 активных task-промптов в реестре.
19. Попутная находка: `.env` переопределяет модель генератора на `claude-opus-4-8`
    (дефолт кода — `claude-haiku-4-5`). `--full` не выход — он только раздувает
    контекст, обрезка остаётся.

### 7. Фикс (код, не закоммичен — на ревью владельцу)

20. Ровно этот класс бага уже чинили в стендапе 16.07: `assembleStandupPrompt`
    (`scripts/_daily-standup.mjs:360`) режет контекст и **защищает задание**, со
    строгим тестом «задание доживает до модели» (`scripts/standup-routing.test.mjs`).
21. Правка `scripts/_main-day-issue.mjs`: секция задания вынута из общего массива,
    сборка идёт через `assembleStandupPrompt({context, assignment, maxChars})`.
    Диff минимальный: +импорт, ~15 строк в сборке. Синтаксис проверен, смежные тесты
    `main-day-issue-paths` / `main-day-skeleton` / `standup-routing` — **22/22 зелёные**.

### 8. Прогон №3 — зелёный каскад (exit 0)

22. `MAIN_DAY_ISSUE.md` записан: все 5 слотов на месте, магистраль взята из
    owner-source (не синтез), таблица обоснования — 1 первичный источник.
23. Ангелина: встреча дня первой репликой (M4-H), каскад чист:
    STRATEGY_DAY (human) · DAILY_STANDUP (vesnin) · MAIN_DAY_ISSUE (vesnin) — все
    свежи и подписаны. «Можно начинать день».

### 9. Двухгейтовое утро (руками, по вердикту M3-G)

24. Детерминированный топ-3 из реестра пока вырожден (зоны не размечены,
    `zone=null` у всех 43 L-карточек) — это недострой #592. Топ-3 собран из
    материалов дня и заморожен: `strategy-day-generator` ·
    `free-distribution-s2-s5` · `detector-scoreboard-f2` (digest `a5d6ac2e…`).
25. **Владелец выбрал `strategy-day-generator`** (AskUserQuestion) →
    `morning:gate magistral --choose` → гейт закрыт.
26. Перечеканка в `docs/tasks/main-day-assertions.json`: sources[0] =
    `owner-choice@8d7ef4f1/AskUserQuestion-magistral-21-07` (полный клейм с топ-3);
    выбор 18.07 сохранён второй строкой (append-only). JSON провалидирован.
27. Ласточка: ссылки #592/#621 отрезолвлены живыми (`gh issue view` — обе OPEN),
    черновик написан линзой Ожегова →
    `docs/comms/drafts/swallow-morning-2026-07-21.md`, зафиксирован в гейте
    (digest `608e46c9…`), показан владельцу целиком.
28. **Владелец сказал «Нет»** — отправка НЕ выполнена. Состояние гейта:
    `magistral: выбран · swallow: ждёт` → `canSend: false`. Вместо отправки
    запрошен этот отчёт.

---

## Изменения в рабочем дереве (Membrana-angelina, НЕ закоммичены)

| Файл | Изменение | Автор |
|------|-----------|-------|
| `scripts/_main-day-issue.mjs` | фикс: задание защищено от обрезки (assembleStandupPrompt) | эта сессия |
| `docs/DAILY_CODE_REVIEW.md` | удалён (staged): протухший дубликат архивной копии 18.07 | эта сессия |
| `docs/tasks/main-day-assertions.json` | перечеканка owner-choice 21.07, история 18.07 сохранена | эта сессия |
| `docs/tasks/morning-gates-state.json` | состояние гейтов: топ-3 + magistral + черновик swallow | эта сессия (через morning:gate) |
| `docs/STRATEGY_DAY.md`, `docs/DAILY_STANDUP.md`, `docs/MAIN_DAY_ISSUE.md` | артефакты ритуала | цепочка ritual:day |
| `docs/security/deps-watch-snapshot.json` | снапшот deps-watch | цепочка ritual:day |
| `docs/comms/drafts/swallow-morning-2026-07-21.md` | черновик ласточки (НЕ отправлен) | эта сессия |
| `package.json` | косметика (расклейка строк) — **чужая правка**, была до сессии | не эта сессия |
| `docs/discussions/sprint-angelina-hostess-impl-review.md` | untracked — **чужой файл**, не тронут | не эта сессия |

В основном дереве (`practice/Membrana`): реставрация 2074 tracked-файлов
(`git restore`) — рабочее дерево приведено к HEAD, ничего не коммитилось.

## Внешние действия

- Отправок наружу **не было**: ласточка не ушла (нет «ок»), Telegram/office не трогались.
- GitHub — только чтение (`gh issue list/view`).
- Anthropic API — вызовы цепочки ритуала + один отладочный вызов с тем же промптом.

## Открытые хвосты

1. **Ласточка утра не отправлена** — гейт ждёт «ок» владельца (или отмены).
2. **Фикс `_main-day-issue.mjs` не закоммичен** — нужен коммит/ревью на этой ветке;
   кандидат на регрессионный тест: «задание доживает до модели» для main-day-issue
   (сейчас гвардит только сестринский тест стендапа).
3. Удаление `docs/DAILY_CODE_REVIEW.md` staged, не закоммичено.
4. Чужая правка `package.json` и чужой untracked в `docs/discussions/` — ждут хозяина.
5. deps-watch: 1 critical (node-tar) + 14 high — по вердикту M1 превращается в
   задачу реестра Teamlead.
6. Вечера 19–20.07 без `team-evening-feedback` — дыра в каденции, из-за неё и был
   стоп №1.
7. Зоны карточек реестра не размечены → детерминированный топ-3 вырожден
   (лексикографика) — поле магистрали #592.

## Уроки (кандидаты в бестиарий/канон)

- **«Инструкция в хвосте + обрезка хвоста»** — класс бага уже был пойман в стендапе
  16.07 и починен локально, но не распространён на соседний генератор. Признак:
  LLM «роняет» ВСЕ слоты разом. Чинить переиспользованием защищённой сборки, не
  ретраями.
- Гейт скелета M2 и страж RT-9 отработали как задуманы: ни одного битого артефакта
  не записано, каждый стоп имел читаемую причину.
- `sent=true`-дисциплина: черновик показан целиком ДО отправки, «Нет» владельца
  остановило отправку штатно — гейт `canSend` не открывался.
