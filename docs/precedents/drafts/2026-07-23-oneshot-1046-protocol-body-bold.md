# Черновик прецедента one-shot · выстрел 1 · Issue #1046

**Статус:** ратифицирован владельцем 23.07 («Да, исполняй»), код дописан. Исход — согласие
с обоснованием (см. ниже).
**Процедура:** [`docs/procedures/one-shot/`](../../procedures/one-shot/README.md), первый фрейм.
**Дата:** 2026-07-23. **Агент:** Claude Code (сессия одна, основное дерево).

> Дублируется в момент показа чек-листа владельцу — требование §5 процедуры:
> чат-единственный план не переживает обрыв сессии, а импровизация запрещена.

## Находка

`protocolBody()` (`scripts/lib/protocol-validator.mjs:106-110`) ищет первую реплику роли
регэкспом `/^\s*\[[^\]]+\]\s*:/u`. Жирная форма записи (`**[Структурщик]:**`) не совпадает,
тело протокола считается пустым, и все проверки по телу дают ложный красный.
Живой эпизод: три ручных протокола заседания `tasks-workshop` получили от
`yarn meeting:audit --id tasks-workshop` шесть нарушений на здоровых файлах.

## Штамп S — тимлид

Запрошен у Vesnin (`docs/virtual-team/PROMPT_TEAMLEAD.md`) через цепочку процедурных каналов;
anthropic исчерпан до 2026-08-01, ответ пришёл через openrouter (`anthropic/claude-haiku-4.5`).

Вердикт: **S ✓**. Основания названы: 2 файла / ~30 строк, регэксп + тесты, границы пакетов
не пересекает, урон подтверждён живым эпизодом. Условие тимлида: комментарий в коде о
причине расширения регэкспа и тесты на обе формы записи. Смежную слепоту `countRoleReplies`
тимлид согласился оставить за границей выстрела — отдельная находка.

## Предикат `evaluateOneShotS`

Вход: `paths = [scripts/lib/protocol-validator.mjs, scripts/protocol-validator.test.mjs]`,
`linesChanged = 30`, `recentShots = []` (первый живой прогон формата).

```
ok: true
reasons: within_file_limit, within_line_limit, no_server_paths, chain_clear
chainShots 1 · chainFiles 2 · chainLines 30
```

## Чек-лист, показанный владельцу

```text
One-shot чек-лист · выстрел 1 · #1046
- [x] Находка одной фразой: protocolBody слепа к жирной метке роли — ложный красный аудитора
- [x] Штамп S запрошен у тимлида (не поставлен самим агентом) → S ✓ с условием
- [x] evaluateOneShotS прогнан: ok=true, reasons приложены
- [x] Файлы: scripts/lib/protocol-validator.mjs, scripts/protocol-validator.test.mjs (2 ≤ 8)
- [x] Серверных путей нет (background-office/media/cabinet, deploy/, prisma/migrations)
- [x] Ратификация владельца
- [x] Весь код дописан до «готово к ревью»; ревью/мердж — снаружи формата
```

## Исход — согласие с обоснованием

Владелец ратифицировал чек-лист без правок объёма (вариант «Да, исполняй»; предложенное
расширение на `countRoleReplies` отклонено — остаётся отдельной находкой).

**Обоснование согласия:** урон не гипотетический, а предъявленный (шесть ложных нарушений
на трёх здоровых протоколах); правка — расширение регэкспа на легальную форму markdown,
семантику проверок не меняет; оба порога предиката пройдены с большим запасом (2/8 файлов,
44/200 строк); откат — одна строка.

**Что сделано:** `REPLY_START_RE` вынесен константой с комментарием о причине
(условие тимлида), `protocolBody` использует её; добавлены два теста — на три формы записи
реплики и на живой сценарий заседания (секции вердикта и посылок видны при жирных метках).
`node --test scripts/protocol-validator.test.mjs` — 33/33 зелёных, старые тесты не тронуты.

Фактический дифф: 2 файла, 44 строки. Предикат на фактическом диффе: `ok=true`,
`within_file_limit, within_line_limit, no_server_paths, chain_clear`.

**Выход фрейма:** «весь код дописан». Ветка `fix/oneshot-1046-protocol-body-bold`;
ревью и мердж — снаружи формата.
