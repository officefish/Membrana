# Прецедент отказа one-shot · Issue #1683 · валидатор монотонности sequence внутри runId

**Статус:** отказ на шаге предиката S — исполнение не начиналось.
**Процедура:** [`docs/procedures/one-shot/`](../../procedures/one-shot/README.md), первый фрейм,
исход «промах» (§6).
**Дата:** 2026-08-04. **Агент:** Claude Code.

## Находка

Строка 7 хендофа 04.08 (#1683): библиотека журнала валидирует запись, но не ленту —
дубль `sequence` внутри runId (вещдок `1, 1, 2` в ленте 03.08) не ловится ничем;
нужна проверка уровня ленты рядом с `summarizeProcedureRunTrail`.

## Предложенный объём (не исполнен)

2 файла / ~80 строк прогноза: `scripts/lib/procedure-run-journal.mjs` +
`scripts/procedure-run-journal.test.mjs`.

## Отказ предиката — capability_chaining (forecast)

`evaluateOneShotS` forecast: **отказ**, основание `capability_chaining` — цепочка
**8 выстрелов / 12 файлов / 236 строк** в окне 7 суток при порогах 3 / 8 / 200; семья
`scripts/lib` — та же плотная семья, что у #1681 (шесть влитых выстрелов 01–03.08 плюс
утренний шот #1693 ровно в этих файлах). Дифф сам по себе S; сумма цепочки — нет.

## Четыре маршрута (§6)

| Маршрут | Годится ли здесь |
|---|---|
| **Шторм** | Нет — предмет один и сформулирован. |
| **Инсайт** | Нет — механический долг валидатора. |
| **Консилиум** | Нет — контракт не меняется, судится существующий (`nextSequenceOf` уже даёт корректные номера). |
| **Спринт** | **Да** — вместе с #1681 в одном спринте узла журнала (общий дом, общая родня #1694/ADR-0022). |

## Мотивированная рекомендация (не вердикт за владельца)

Не резать отдельно: класс «лента, написанная мимо `nextSequenceOf`» и кросс-файловый счёт
через границу дней (конспект Веснина) — одна семья проверок уровня ленты. Войти блоком в
локальный спринт узла журнала (см. прецедент отказа #1681 того же дня).
