# Прецедент отказа one-shot · Issue #1681 · словарь спринта в lib + структурный orphanedBy

**Статус:** отказ на шаге предиката S — исполнение не начиналось.
**Процедура:** [`docs/procedures/one-shot/`](../../procedures/one-shot/README.md), первый фрейм,
исход «промах» (§6).
**Дата:** 2026-08-04. **Агент:** Claude Code.

## Находка

Строка 6 хендофа 04.08 (#1681): импорт скрипт-к-скрипту `execution-gate.mjs` →
`sprint-cut-check.mjs` (`SPRINT_PROCEDURE_ID`, `sprintTrailRelPath`) — «тайное API»;
плюс второй долг того же узла — структурный `orphanedBy` через `closeProcedureRun`.

## Предложенный объём (не исполнен)

~5 файлов / ~150 строк прогноза: новый модуль словаря в `scripts/lib/`, правки
`sprint-cut-check.mjs`, `execution-gate.mjs`, `scripts/lib/procedure-run-journal.mjs`, зубы.

## Отказ предиката — capability_chaining (forecast)

`evaluateOneShotS` forecast: **отказ**, основание `capability_chaining` — цепочка
**8 выстрелов / 15 файлов / 306 строк** в окне 7 суток при порогах 3 / 8 / 200.
Семья `scripts/lib` за окно: dead-wire (01.08), persona-memory (02.08), execution-trace ×2,
review-oversized-queue, sprint-cut stubs (03.08) + утренний шот #1693 (04.08, 156 строк,
в ленте анти-цепочки отсутствует — включён в расчёт руками). Это ровно сценарий, против
которого контрмера построена: каждый шаг формально S, сумма — нет. Прецедент того же дня:
«приёмный предикат корпуса» отказан тем же основанием и ушёл карточкой (кристалл #1698).

## Четыре маршрута (§6)

| Маршрут | Годится ли здесь |
|---|---|
| **Шторм** | Нет — предмет сформулирован, расходиться нечему. |
| **Инсайт** | Нет — это долг узла, не стратегическая идея. |
| **Консилиум** | Сам по себе нет, но смежный вход есть: иссью прямо связывает переезд с ADR схемы журнала @1.1 (`runPhase`/`friction`/`amends`/`orphanedBy`), который ждёт слова владельца. |
| **Спринт** | **Да** — `membrana-local-sprint`, зона плана включает `scripts/lib`; ревьюер #1685 просил «ближайший спринт». |

## Мотивированная рекомендация (не вердикт за владельца)

Один локальный спринт узла журнала прогонов, объединяющий #1681 и #1683 (родня по дому и
по ревью-условиям): переезд словаря, пронос `orphanedBy` через `closeProcedureRun`,
валидатор монотонности `sequence` — и там же зафиксировать ADR схемы @1.1, которого оба
долга ждут. Формат с ответственным исполнителем удовлетворяет кристалл дня #1698.
