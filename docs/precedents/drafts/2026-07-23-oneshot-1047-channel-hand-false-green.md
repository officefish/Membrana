# Черновик прецедента one-shot · выстрел 2 · Issue #1047

**Статус:** ратифицирован владельцем 23.07 («Да, исполняй»), код дописан. Исход — согласие
с обоснованием.
**Процедура:** [`docs/procedures/one-shot/`](../../procedures/one-shot/README.md), первый фрейм.
**Дата:** 2026-07-23. **Агент:** Claude Code.

## Находка

Проверка [6] аудитора заседаний (`scripts/lib/meeting-audit.mjs:207-221`) смотрит на
НАЛИЧИЕ пометки канала, а не на её значение. Три ручных протокола `tasks-workshop`
с честной шапкой `<!-- канал: hand -->` получили «✓ произведён инструментом — гейт посылок
отработал при записи». Гейт при записи их не видел: `consilium.mjs` не вызывался.
Ложный **зелёный**: аудитор утверждает, что процедура отработала, когда она не вызывалась.

## Штамп S — тимлид

Запрошен у Vesnin через цепочку процедурных каналов (anthropic исчерпан → openrouter,
`anthropic/claude-haiku-4.5`). Вердикт: **S ✓**. Основания: ложный зелёный подрывает доверие
к аудитору; 3 файла / ~55 строк; правка ограничена значением пометки канала.
**Условие тимлида:** третий выстрел (#1051) остаётся в цепочке, только если он правда
1–2 файла и < 100 строк; разрастётся — переводить в обычную задачу.

## Предикат `evaluateOneShotS`

Вход: 3 файла (`scripts/lib/meeting-audit.mjs`, `scripts/meeting-audit.mjs`,
`scripts/meeting-audit.test.mjs`), `linesChanged = 55`,
`recentShots = [выстрел 1: 2 файла / 44 строки, сегодня]`.

```
ok: true
reasons: within_file_limit, within_line_limit, no_server_paths, chain_clear
chainShots 2 · chainFiles 5 · chainLines 99
```

## Чек-лист, показанный владельцу

```text
One-shot чек-лист · выстрел 2 · #1047
- [x] Находка одной фразой: канал hand читается как «произведён инструментом» — ложный зелёный
- [x] Штамп S запрошен у тимлида → S ✓ с условием по цепочке
- [x] evaluateOneShotS прогнан на цепочке (2 выстрела): ok=true, reasons приложены
- [x] Файлы: scripts/lib/meeting-audit.mjs, scripts/meeting-audit.mjs, scripts/meeting-audit.test.mjs
- [x] Серверных путей нет
- [x] Ратификация владельца
- [x] Весь код дописан до «готово к ревью»; ревью/мердж — снаружи формата
```

## Исход — согласие с обоснованием

Владелец ратифицировал мягкий вариант (предупреждение, а не FAIL): ручной протокол — другой
канал записи, а не нарушение регламента; нарушением было бы выдать его за произведённый
инструментом.

**Обоснование согласия:** ложный зелёный подрывает то, ради чего аудитор существует —
правду о процедуре; правка читает значение уже существующей пометки, новых требований к
авторам протоколов не вводит; код возврата не меняется, значит ни один сегодняшний зелёный
прогон не станет красным задним числом.

**Что сделано:** `TOOL_CHANNEL` + `protocolChannel()` (значение канала, регистр не значим);
проверка [6] разведена на три ветки — `llm` → PASS, иной канал → `ПРЕДУПР` с прямой
формулировкой «гейт посылок при записи НЕ вызывался», пометки нет → FAIL как прежде;
печать получила знак `⚠`; три теста.

**Живая проверка:** `yarn meeting:audit --id tasks-workshop` — ровно три ручных протокола
(m3, m4b, m4d) стали ⚠, семь инструментальных остались ✓, «Нарушений: 0» не изменилось.

Фактический дифф: 3 файла, 57 строк. Предикат на цепочке: `ok=true`, `chain_clear`
(2 выстрела, 5 файлов, 104 строки).

**Выход фрейма:** «весь код дописан». Ветка `fix/oneshot-1047-channel-hand` (от `origin/main`).
