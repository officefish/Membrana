---
name: membrana-meeting
description: >-
  Runs Membrana Meeting (заседание): multi-question matter split into one-question consilia
  under a single unifying brief, with its own container protocol and a separate read-only
  auditor. Use when user says заседание, meeting, «по одному вопросу», «вердикты в эпик», or
  a consilium agenda carries more than one question. Do NOT use for a single question (plain
  yarn consilium), for Competition (3 answers, 1 winner) or Cowork (3 parts, all merged).
---

# Membrana Заседание

Канон: [`docs/MEETING_REGULATION.md`](../../../docs/MEETING_REGULATION.md) —
**черновик, подлежит ратификации консилиумом** (прецедент: формат Cowork утверждён
консилиумом 14.07).

## Суть

Заседание **≠ серия консилиумов**. Серия — N прогонов. Заседание — контейнер с тремя
несущими элементами:

- **общее объединяющее задание** — одно на все вопросы;
- **свой протокол** — `MEETING_ACTIVE.md`, отдельный от протоколов консилиумов;
- **свой аудитор** — отдельный агент, read-only, следит за процедурой.

Без этих трёх это просто несколько консилиумов подряд — и они разойдутся так же, как один.

## Почему формат вообще есть

Три ступени эскалации, каждая от провала предыдущей:

1. **Проза** (урок 11.07 в `membrana-consilium`): «пронумеруй точки, требуй эхо с вердиктом».
   17.07 её **исполнили** — и консилиум всё равно уронил центральный вопрос.
2. **Детекция** (rt-6 `findUncoveredAgendaItems`): ловит, но уже по сожжённому прогону.
3. **Структура** (этот формат): ронять нечего — вопрос один.

Срывов задокументировано шесть: 2× 11.07 (#355→ADR #356), 3× 16.07, 1× 17.07.

## Фазы

```
M0 — все вопросы в одной комнате; единственный вердикт: ПОРЯДОК (DAG)
   → ратификация порядка ВЛАДЕЛЬЦЕМ (не председателем)
     → M1..Mn — по одному вопросу в этом порядке, каждое → свой вердикт
       → сборка активных вердиктов в эпик
```

M0 — единственная общая комната, и она нужна: увидеть DAG через замочную скважину нельзя.
Изоляция защищает от **потери** вопроса, но не от **неверного ранга**.

**M0 не спецслучай:** его повестка тоже несёт один ID-вопрос («какой порядок?»), а
кандидаты лежат в теле **простым текстом без жирных ID** — тогда правило единообразно.

## Hard rules

- Один вопрос — одно заседание (**S-M1**, машинный отказ до прогона).
- Вердикта нет → заседание **не состоялось**; «частично» не существует (**S-M2**).
- Задание после M0 меняется только через `BRIEF_AMENDMENT.md` + LGTM владельца (**S-M4**).
- **Аудитор ≠ председатель** (**S-M5**). 17.07 обе роли играл один агент: сам написал
  повестку, сам запустил, сам оценил покрытие — поймал гейт, а не он. В Competition жюри
  не реализует код команд; изоляция здесь проходит между тем, кто ставит вопрос, и тем,
  кто проверяет процедуру.

## Run

```bash
yarn consilium --meeting <id> --topic-file <повестка.md> --save-as <id>-m<n>-<slug> "…"
```

`--meeting` включает S-M1: повестка обязана нести ровно один ID-вопрос, иначе **отказ
до прогона** (exit 2), кредит не тратится. Без флага обычный консилиум работает как
прежде — многовопросность там законна.

## Шов

Форму вердикта задаёт эпик `truth-graph-contour`, не этот формат. Кандидат из обсуждения
17.07: вердикт = токен, зависимость = `parents`, отзыв каскадом (S-M3) — тогда «вёрстка в
эпик» перестаёт быть ручной. **Не проектировать здесь** — иначе две несовместимые схемы.

## Честная цена

DAG — последовательность, не параллель: `Mn` ждёт `Mn-1`. Изоляция убивает сшивки (I4
родился оттого, что Музыкант связал эхо-камеру с графом) — M0 остаётся ровно для этого.
Осознанный размен эмерджентности на покрытие.
