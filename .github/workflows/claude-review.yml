name: Claude PR Review

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Claude Code Review
        uses: some-claude-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
